# Phase 72.1: close-gap-gate-01-authoritative-milestone-close-gate - Pattern Map

**Mapped:** 2026-06-06
**Files analyzed:** 7
**Analogs found:** 6 / 7

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `.planning/phases/70-question-stats-post-class-recap/70-VERIFICATION.md` | test | batch | `.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-VERIFICATION.md` | exact |
| `.planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-VERIFICATION.md` | test | batch | `.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-VERIFICATION.md` | role-match |
| `.planning/phases/72-end-to-end-verify-phase-close-gate/72-VERIFICATION.md` | test | batch | `.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-VERIFICATION.md` | exact |
| `.planning/phases/72.1-close-gap-gate-01-authoritative-milestone-close-gate/72.1-CLOSEOUT.md` | utility | batch | `.planning/milestones/v2.2-phases/38-cutover-verification-fallback-and-operational-hardening/38-CLOSEOUT.md` | exact |
| `.planning/phases/72.1-close-gap-gate-01-authoritative-milestone-close-gate/72.1-PROOF-MAPPING.md` | utility | transform | _No exact analog_ (compose from `72.1-VALIDATION.md` + `69-VERIFICATION.md` + `38-CLOSEOUT.md`) | partial |
| `scripts/verify-phase72-close-gate.ts` | test | batch | `scripts/verify-phase38-cutover-closeout.ts` | role-match |
| `scripts/verify-phase71-marketplace-lifecycle.ts` | test | batch | `scripts/verify-phase71-marketplace-lifecycle.ts` | exact |

## Pattern Assignments

### `.planning/phases/70-question-stats-post-class-recap/70-VERIFICATION.md` (test, batch)

**Primary analog:** `.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-VERIFICATION.md`

**Frontmatter + report header pattern** (lines 1-25):
```markdown
---
phase: 69-interactive-single-choice-quiz-sample-plugin
verified: 2026-06-04T01:48:40Z
status: passed
score: 8/8 must-haves verified
...
---

# Phase 69: Interactive Single-Choice Quiz Sample Plugin Verification Report

**Phase Goal:** ...
**Verified:** 2026-06-04T01:48:40Z
**Status:** passed
```

**Observable-truth table pattern** (lines 26-41):
```markdown
## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | ... | ✓ VERIFIED | `src/...:line-line` ... |
```

**Required-artifacts table pattern** (lines 43-59):
```markdown
### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `src/...` | ... | ✓ VERIFIED | ... |
```

**Requirements coverage pattern** (lines 90-98):
```markdown
### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| QUIZ-01 | 69-01, 69-02, 69-05 | ... | ✓ SATISFIED | ... |
```

**Populate the evidence from Phase 70 proof seam, not from summary prose:**
- `scripts/verify-phase70-quiz-stats.ts` lines 10-48: statically names the exact cache tag, recap DTO, latest-only DAL aggregation, and rendered empty-state copy.
- `src/lib/dal/classroom.ts` lines 890-984: use as the authoritative data-truth evidence for `buildQuizSampleRecapStats`.
- `src/components/classroom/classroom-session-recap-surface.tsx` lines 98-177: use as the rendered recap surface evidence.
- `src/components/classroom/classroom-session-recap-surface.test.tsx` lines 143-188 and `src/lib/dal/classroom.test.ts` grep hits at lines 1716-1852: use as testing evidence.

---

### `.planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-VERIFICATION.md` (test, batch)

**Primary analog:** `.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-VERIFICATION.md`

**Use the same verification-report scaffold** (69 lines 19-98), but fill it with lifecycle-specific evidence instead of quiz runtime evidence.

**Core evidence sources to cite directly:**

**Verifier structure and stage naming** — `scripts/verify-phase71-marketplace-lifecycle.ts` lines 209-255:
```typescript
console.log("[1/3] Static seam checks...");
...
console.log("[2/3] Focused vitest stage...");
...
console.log("[3/3] Isolated SQLite proof stage...");
```

**Focused-suite pattern** — `scripts/verify-phase71-marketplace-lifecycle.ts` lines 169-182:
```typescript
const suites = [
  "src/lib/dal/plugins.test.ts",
  "src/actions/plugin-actions.test.ts",
  "src/components/surfaces/plugin-marketplace-surface.test.tsx",
]
```

**Lifecycle preflight / retain / cleanup proof seam** — `src/lib/dal/plugins.ts` lines 1369-1484 and 1494-1648:
```typescript
export async function preflightUninstallPluginWithTx(...) {
  ...
  cleanupConfirmationToken: buildCleanupConfirmationToken({...})
}

export async function uninstallPluginWithTx(input: UninstallPluginWithTxInput) {
  ...
  if (retentionMode === "cleanup") {
    if (input.confirmationToken !== preflight.cleanupConfirmationToken) {
      ...
      throw new Error("PLUGIN_CLEANUP_CONFIRMATION_REQUIRED");
    }
  }
}
```

**Marketplace surface proof** — `src/components/surfaces/plugin-marketplace-surface.tsx` lines 20-27 and 82-152:
```tsx
const bundle = schoolId && actorId
  ? await readMarketplaceSurfaceBundle({ schoolId, actorId })
  : null;

{externalRows.map((row) => (
  <article key={row.pluginKey} ...>
    ...
    {row.posture === "upgrade-available" ? "查看升级预检" : ...}
  </article>
))}
```

**Testing pattern** — `src/components/surfaces/plugin-marketplace-surface.test.tsx` lines 29-137: keep concrete UI text assertions such as `External Section`, `查看升级预检`, and `detail-panel:...`.

---

### `.planning/phases/72-end-to-end-verify-phase-close-gate/72-VERIFICATION.md` (test, batch)

**Primary analog:** `.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-VERIFICATION.md`

**Gate-verification intro pattern** (lines 12-18):
```markdown
# Phase 65: Eval, Guardrails & verify:phase Close Gate — Verification Report

**Phase Goal:** ...
**Verified:** ...
**Status:** PASSED
**Methodology:** Goal-backward.
```

**Truth / artifact / link sections** (lines 20-55):
```markdown
## Goal Achievement
### Observable Truths ...
### Required Artifacts ...
### Key Link Verification ...
```

**Requirement coverage pattern** (lines 71-77):
```markdown
### Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| EVAL-03 | `verify:phase` close gate ... | ✓ SATISFIED | ... |
```

**Populate with exact Phase 72 gate evidence:**
- `scripts/verify-phase72-close-gate.ts` lines 41-75: package-script wiring checks.
- `scripts/verify-phase72-close-gate.ts` lines 86-124: ordered `67 -> 68 -> 69 -> 70 -> 71` orchestration.
- `package.json` lines 68-74: `verify:phase67..72` and `verify:phase` alias.
- `72-VALIDATION.md` lines 20-23 and 38-49: current validation contract, then document how 72.1 strengthens it.

**Secondary analog for “combined verifier + doc/static checks”**: `scripts/verify-phase38-cutover-closeout.ts` lines 51-119.

---

### `.planning/phases/72.1-close-gap-gate-01-authoritative-milestone-close-gate/72.1-CLOSEOUT.md` (utility, batch)

**Primary analog:** `.planning/milestones/v2.2-phases/38-cutover-verification-fallback-and-operational-hardening/38-CLOSEOUT.md`

**Section scaffold to copy almost verbatim** (lines 1-31):
```markdown
# Phase 38 Closeout

## Milestone

`v2.2 ...`

## Close conclusion
...

## Delivered scope
...

## Proof chain summary
| Proof | Role |
| --- | --- |
```

**Exclusions / deferred pattern** (lines 72-91):
```markdown
## Explicit exclusions
1. ...

## Deferred next steps
1. ...
```

**72.1-specific content that should slot into this skeleton:**
- Proof chain must explicitly name `verify:phase67`, `verify:phase68`, `verify:phase69`, `verify:phase70`, `verify:phase71`, `verify:phase72`, `70-VERIFICATION.md`, `71-VERIFICATION.md`, `72-VERIFICATION.md`, and `72.1-PROOF-MAPPING.md`.
- Delivered scope must be limited to authoritative close proof strengthening; do **not** imply new product capability.

---

### `.planning/phases/72.1-close-gap-gate-01-authoritative-milestone-close-gate/72.1-PROOF-MAPPING.md` (utility, transform)

**No exact analog found.** Build it by combining three existing patterns:

1. **Task / requirement matrix pattern** — `72.1-VALIDATION.md` lines 37-57:
```markdown
## Per-Task Verification Map
| Task ID | Plan | Wave | Requirement | ... | Automated Command | ... |
...

## Wave 0 Requirements
- [ ] ...
```

2. **Requirement coverage pattern** — `69-VERIFICATION.md` lines 90-98:
```markdown
### Requirements Coverage
| Requirement | Source Plan | Description | Status | Evidence |
```

3. **Proof-chain summary pattern** — `38-CLOSEOUT.md` lines 31-38:
```markdown
## Proof chain summary
| Proof | Role |
```

**Recommended shape for 72.1 proof mapping:**
- first table: requirement → flow segment → proof files/scripts/tests
- second table: UI surface → action/command → DAL/data seam
- third table: archival artifact → purpose → dependency

---

### `scripts/verify-phase72-close-gate.ts` (test, batch)

**Primary analog:** `scripts/verify-phase38-cutover-closeout.ts`

**Keep current import/helper style from the existing file** (lines 1-39):
```typescript
import { execFileSync } from "node:child_process";
import { existsSync, readFileSync } from "node:fs";
import path from "node:path";

function read(filePath: string) { ... }
function run(command: string, args: readonly string[], label: string) { ... }
```

**Keep current package-script verification style** (lines 41-75):
```typescript
return [
  { label: "package.json exposes phase67 ...", passed: ... },
  ...
  { label: "package.json routes verify:phase alias to phase72", passed: ... },
];
```

**Preserve ordered runner spine** (lines 104-117):
```typescript
run("pnpm", ["verify:phase67"], ...)
run("pnpm", ["verify:phase68"], ...)
run("pnpm", ["verify:phase69"], ...)
run("pnpm", ["verify:phase70"], ...)
run("pnpm", ["verify:phase71"], ...)
```

**Borrow the “artifact/static closeout checks” pattern from Phase 38** (lines 37-107 and 109-122):
```typescript
const closeoutSource = read(".../38-CLOSEOUT.md");

const staticChecks: StaticCheck[] = [
  {
    label: "closeout document keeps milestone proof chain and explicit exclusions",
    passed: ...,
  },
];
```

**What to add in 72.1 using this analog:**
- after running phase 67-71, assert presence/content of `70-VERIFICATION.md`, `71-VERIFICATION.md`, `72-VERIFICATION.md`, `72.1-CLOSEOUT.md`, `72.1-PROOF-MAPPING.md`
- assert proof-chain wording mentions both `/settings/plugins` lifecycle surface and recap surface, not only nested verifier commands

---

### `scripts/verify-phase71-marketplace-lifecycle.ts` (test, batch)

**Primary analog:** preserve the current file’s own structure

**Imports + fixture helper pattern** (lines 1-9):
```typescript
import { execFileSync } from "node:child_process";
import { existsSync, readFileSync } from "node:fs";
import path from "node:path";

import {
  createPhase71IsolatedDb,
  seedPhase71MarketplaceFixtures,
} from "./lib/phase71-marketplace-fixtures";
```

**Vitest runner helper** (lines 57-73):
```typescript
function runVitest(paths: readonly string[], label: string) {
  const directRunner = path.join(process.cwd(), "node_modules", "vitest", "vitest.mjs");
  ...
  run("pnpm", ["exec", "vitest", ...args], label);
}
```

**Static-check bundle pattern** (lines 84-135):
```typescript
return {
  required: [ ... ],
  advisory: [ ... ],
};
```

**Smoke/full proof split** (lines 137-207):
```typescript
async function runSmokeProof() { ... }
async function runFullProof() { ... }
```

**Main-stage pattern** (lines 216-250):
```typescript
console.log("[1/3] Static seam checks...");
...
console.log("[2/3] Focused vitest stage...");
...
console.log("[3/3] Isolated SQLite proof stage...");
```

**What 72.1 should preserve when strengthening:**
- keep the three-stage skeleton
- add stronger assertions, don’t replace the runner contract
- continue citing `src/actions/plugin-actions.test.ts`, `src/lib/dal/plugins.test.ts`, `src/components/surfaces/plugin-marketplace-surface.test.tsx` as focused suites

## Shared Patterns

### Single authoritative gate alias
**Sources:** `package.json` lines 68-74, `scripts/verify-phase72-close-gate.ts` lines 41-75
**Apply to:** `72-VERIFICATION.md`, `72.1-CLOSEOUT.md`, strengthened `scripts/verify-phase72-close-gate.ts`
```json
"verify:phase67": "...scripts/verify-phase67-plugin-owned-data.ts",
"verify:phase68": "...scripts/verify-phase68-data-access-verbs.ts",
"verify:phase69": "...scripts/verify-phase69-quiz-sample.ts",
"verify:phase70": "...scripts/verify-phase70-quiz-stats.ts",
"verify:phase71": "...scripts/verify-phase71-marketplace-lifecycle.ts",
"verify:phase72": "...scripts/verify-phase72-close-gate.ts",
"verify:phase": "pnpm verify:phase72"
```

### Lifecycle proof must bind real route -> surface -> action/command -> DAL truth
**Sources:**
- `src/app/settings/plugins/page.tsx` lines 1-10
- `src/components/surfaces/plugin-marketplace-surface.tsx` lines 20-27, 82-152
- `src/features/platform-core/actions/registry.ts` lines 410-546
- `src/actions/plugin-actions.ts` lines 114-121, 640-706, 712-767, 793-815, 911-955
- `src/lib/dal/plugins.ts` lines 956-1013, 1369-1484, 1494-1648
**Apply to:** `71-VERIFICATION.md`, `72-VERIFICATION.md`, `72.1-PROOF-MAPPING.md`, `72.1-CLOSEOUT.md`
```tsx
// page.tsx
<Suspense fallback={null}>
  <PluginMarketplaceSurface />
</Suspense>

// surface.tsx
const bundle = schoolId && actorId
  ? await readMarketplaceSurfaceBundle({ schoolId, actorId })
  : null;

// action boundary
async function requireCurrentActorId() {
  const user = await getCurrentUserDTO();
  if (!user?.id) throw new Error("AUTH_REQUIRED");
  return user.id;
}

// DAL truth
export async function preflightUninstallPluginWithTx(...) { ... }
export async function uninstallPluginWithTx(...) { ... }
```

### Recap proof must bind real route -> rendered recap surface -> latest-only DAL stats
**Sources:**
- `src/app/(classroom)/classroom/page.tsx` lines 33-41
- `src/components/classroom/classroom-session-recap-surface.tsx` lines 98-177
- `src/actions/classroom-actions.ts` lines 318-329
- `src/lib/dal/classroom.ts` lines 890-984, 3675-3716
**Apply to:** `70-VERIFICATION.md`, `72-VERIFICATION.md`, `72.1-PROOF-MAPPING.md`, `72.1-CLOSEOUT.md`
```tsx
// real recap route
const recap = activeSession?.status === 'ended'
  ? await getClassroomSessionRecapDTO({ sessionId: activeSession.id, studentId, stepId, detailTab: recapTab })
  : null

// rendered surface copy
<p className="text-sm text-on-surface-variant">题目复盘</p>
...
当前课堂没有 quiz sample 题目，或还没有可用于复盘的作答记录。

// latest-only DAL truth
eq(pluginOwnedQuizResponses.isLatest, true)
...
quizSampleStats: recap.quizSampleStats,
```

### Server Action cache invalidation stays at the entrypoint
**Sources:** `src/actions/plugin-actions.ts` lines 25, 199-203, 702-705, 754-757, 808-812; `src/actions/classroom-actions.ts` lines 323-329
**Apply to:** any strengthened proof narrative that mentions freshness/read-your-writes
```typescript
// plugin-actions.ts
// keep updateTag() at the entrypoint.
function updateInferredTags(tags: string[]) {
  for (const tag of tags) {
    updateTag(tag);
  }
}

// classroom-actions.ts
updateTag(cacheTags.classroom(parsed.data.sessionId));
updateTag(cacheTags.progress(parsed.data.lessonId, result.studentId));
updateTag(cacheTags.submission(parsed.data.lessonId, result.studentId));
updateTag(cacheTags.teacherReview(parsed.data.lessonId));
updateTag(cacheTags.quizStats(parsed.data.sessionId));
```

### Verification-doc structure is table-first, not prose-first
**Sources:** `69-VERIFICATION.md` lines 26-98; `65-VERIFICATION.md` lines 20-77
**Apply to:** `70-VERIFICATION.md`, `71-VERIFICATION.md`, `72-VERIFICATION.md`
```markdown
## Goal Achievement
### Observable Truths
### Required Artifacts
### Key Link Verification
### Requirements Coverage
```

### Milestone closeout is conclusion-first, then proof-chain, then exclusions
**Source:** `38-CLOSEOUT.md` lines 7-31, 72-91
**Apply to:** `72.1-CLOSEOUT.md`
```markdown
## Close conclusion
## Delivered scope
## Proof chain summary
## Explicit exclusions
## Deferred next steps
```

## No Analog Found

| File | Role | Data Flow | Reason |
|---|---|---|---|
| `.planning/phases/72.1-close-gap-gate-01-authoritative-milestone-close-gate/72.1-PROOF-MAPPING.md` | utility | transform | Repo has closeouts and verification reports, but no prior standalone `*-PROOF-MAPPING.md`. Compose from `72.1-VALIDATION.md` task matrix, `69-VERIFICATION.md` requirement coverage, and `38-CLOSEOUT.md` proof-chain table. |

## Metadata

**Analog search scope:** `.planning/phases/**`, `.planning/milestones/**`, `scripts/verify-phase*.ts`, `src/app/**`, `src/components/**`, `src/actions/**`, `src/features/platform-core/actions/**`, `src/lib/dal/**`, `package.json`

**Files scanned:** 24

**Pattern extraction date:** 2026-06-06
