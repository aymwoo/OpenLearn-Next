# Phase 72.1 Closeout

## Milestone

`v4.0 — Plugin Marketplace & Plugin-Owned Data`

## Close conclusion

v4.0 milestone 已把 `verify:phase` 收口为**唯一**、**可执行**、**可归档**、**人类凭证可审计**的 milestone close gate。最终 close 结论不再依赖人工解释,而由以下 archive-ready proof chain 支撑:

1. `verify:phase67` (migration-proof) → `67-VERIFICATION.md`
2. `verify:phase68` (governed-access verbs) → `68-VERIFICATION.md`
3. `verify:phase69` (teacher config → student answer) → `69-VERIFICATION.md`
4. `verify:phase70` (STATS-01/02 recap) → `70-VERIFICATION.md`
5. `verify:phase71` (MKT-01..05 marketplace lifecycle) → `71-VERIFICATION.md`
6. `verify:phase72` (GATE-01) → `72-VERIFICATION.md`
7. `72.1-PROOF-MAPPING.md` (requirement / flow / proof traceability + manual sign-off ledger)
8. `72.1-CLOSEOUT.md` (本文件;archive-ready close 结论)

`pnpm verify:phase` 单一外部 alias 当前 routes to `pnpm verify:phase72`,并在 72.1 wave-3 收口后,只有当上述 8 个 artifacts 全部存在、`72.1-PROOF-MAPPING.md` 的 manual sign-off ledger 两行 (`/settings/plugins` lifecycle surface + ended classroom recap surface) 都处于 `status: passed`、且 final-artifact 内容含 `verify:phase67` + `verify:phase68` proof-chain wording 时,才能让 `verify-phase72-close-gate.ts` 5-stage gate (44 static/bridge checks in `--smoke`) 全绿通过。

## Delivered scope

本次 close 明确只覆盖以下范围:

1. **Milestone bridge gate hardening** — `scripts/verify-phase72-close-gate.ts` 从 4-stage / 32-check 升级到 5-stage / 44-check,在保留上游 5 份 `*-VERIFICATION.md` artifact 存在性 + lifecycle / recap 两侧 bridge static seam 的前提下,新增 final-artifact dependency stage 与 manual sign-off ledger 解析。
2. **Formal phase verification** — `70-VERIFICATION.md` (STATS-01/02) + `71-VERIFICATION.md` (MKT-01..05) + `72-VERIFICATION.md` (GATE-01) 三份 phase-level formal verification 在 wave-1 + wave-3 落库,每份按 69-VERIFICATION scaffold (observable truths + required artifacts + key link + data-flow + behavioral + requirements coverage + anti-patterns + human verification + 结论) 收口。
3. **Proof mapping + manual sign-off ledger** — `72.1-PROOF-MAPPING.md` 提供 requirement → flow segment → proof files / scripts / tests、UI surface → action / command → DAL / data seam、archival artifact → purpose → dependency 三张 traceability 表,以及 `/settings/plugins` lifecycle surface + ended classroom recap surface 两行 manual sign-off 行 (schema: `proof artifact` + `status: passed` + `executed_by` + `executed_at` + `evidence note`)。
4. **Single authoritative alias** — `pnpm verify:phase` 保持唯一外部入口;不再引入第二外部 alias。
5. **6 段链 product 路径** — 真实产品接缝 (`/settings/plugins` → `PluginMarketplaceSurface` → `recoverMarketplacePluginAction` / `preflightPluginUpgrade` / `preflightUninstallPluginWithTx` → `recoverRetainedPluginInstallWithTx` / `preflightPluginUpgrade` / `uninstallPluginWithTx` + `cleanupConfirmationToken` + `PLUGIN_ACTIVE_CLASSROOM_BLOCKED`;`/classroom` ended-session → `getClassroomSessionRecapDTO` → `ClassroomSessionRecapSurface` → `submitQuizSampleAnswerAction` + `updateTag(cacheTags.quizStats(...))` → `buildQuizSampleRecapStats` with `isLatest=true`) 在 `verify-phase72-close-gate.ts` Stage 3 + Stage 4 milestone bridge 20 项 static seam + Stage 5 final-artifact 静态断言 + manual sign-off ledger 双 `status: passed` 共同覆盖。
6. **D-72.1-16 archive posture** — 把"more authoritative"的 milestone proof 落为唯一选项;`72.1-PROOF-MAPPING.md` Section 6 显式 forbidden 三个 lighter shortcut (doc-only closure / missing manual sign-off capture / final-gate wiring without artifact dependency checks) 在 wave-3 完成后,任一出现都让 final gate 失败。

## Proof chain summary

| Proof | Role |
| --- | --- |
| `verify:phase67` → `67-VERIFICATION.md` | migration-proof anchor (DATA-01..04) |
| `verify:phase68` → `68-VERIFICATION.md` | governed-access verbs anchor (ACCESS-01..03); D-72.1-06 显式 proof chain 行 |
| `verify:phase69` → `69-VERIFICATION.md` | teacher config → student answer anchor (QUIZ-01..03) |
| `verify:phase70` → `70-VERIFICATION.md` | recap / stats anchor (STATS-01/02) |
| `verify:phase71` → `71-VERIFICATION.md` | marketplace lifecycle anchor (MKT-01..05) |
| `verify:phase72` → `72-VERIFICATION.md` | final Phase 72 verification anchor (GATE-01) |
| `72.1-PROOF-MAPPING.md` | requirement / flow / proof traceability index + manual sign-off ledger |
| `72.1-CLOSEOUT.md` | archive-ready milestone close 结论 (本文件) |

## Operational summary

### Authoritative close gate posture

- `pnpm verify:phase` (alias) → `pnpm verify:phase72` 是 v4.0 唯一外部 milestone close gate。
- `scripts/verify-phase72-close-gate.ts` 5 stages: 1) script wiring (7) + 2) upstream VERIFICATION artifact (5) + 3) lifecycle milestone-bridge (11) + 4) recap / stats milestone-bridge (9) + 5) final-artifact dependencies + manual sign-off ledger (12);全模式再叠加 5 项 ordered pnpm runners。
- final gate hard-fail 列表(任一缺失即 `process.exit(1)`):任一上游 5 份 `*-VERIFICATION.md` 缺失;任一 lifecycle / recap bridge seam 静态断言失败;`72.1-CLOSEOUT.md` / `72.1-PROOF-MAPPING.md` / `72-VERIFICATION.md` 缺失;`72.1-CLOSEOUT.md` 缺 `verify:phase67` 或 `verify:phase68` proof chain wording;`72.1-PROOF-MAPPING.md` 缺 `verify:phase68` 或 D-72.1-16 forbidden shortcuts 或 Manual Surface Sign-Off Ledger section;manual sign-off 行 `< 2` 或 `executed_by` / `executed_at` / `evidence note` 任一字段 `< 2` 出现。

### Lifecycle 治理 (retain reinstall + upgrade + cleanup uninstall)

- **retain reinstall**: `/settings/plugins` `recoverMarketplacePluginAction` server action (`:712`) 边界 → `recoverRetainedPluginInstallWithTx` DAL (`:1016-1119`) 事务内接管 6 张 plugin-owned 表 (`pluginLessonExtensions` / `pluginLessonStepExtensions` / `pluginResourceExtensions` / `pluginOwnedBusinessData` / `pluginOwnedQuizQuestions` / `pluginOwnedQuizResponses`),并写 `recoveredDataTakeover: true` + `recoveredFromPluginId` 透传到 action 结果。
- **semver upgrade**: `preflightPluginUpgrade` DAL (`:232`) → `plugin-migration.ts` 显式 `backfill → verify → cutover` 三段纪律(`:720-793` 顺序执行;verify 失败时 `cutover.status = "skipped"`,旧版本仍由 `pluginRegistrations` 当前 row 暴露可用)。
- **cleanup uninstall**: `preflightUninstallPluginWithTx` (`:1369`) 输出 `cleanupConfirmationToken` + `activeSessions`,`uninstallPluginWithTx` (`:1494-1558`) 在 destructive path 拒绝缺失 / 错误 token,错误名固定为 `PLUGIN_CLEANUP_CONFIRMATION_REQUIRED`;active classroom session 在 preflight 就被 `getPluginUninstallBlockReason` (`:603`) 联合 `plugin_owned_quiz_questions` ↔ `classroom_session` join 触发 `PLUGIN_ACTIVE_CLASSROOM_BLOCKED` + `activeSessions` 列表,UI 与 server action 透出可读原因。

### Recap stats 真相

- 真实 `/classroom` ended-session 路径调用 `getClassroomSessionRecapDTO` (`src/lib/dal/classroom.ts:3675-3716`),在 DTO 上挂出 `quizSampleStats`(不下沉 `ClassroomSessionSummaryArtifactSchema`,不写 core analytics)。
- 真相源 `buildQuizSampleRecapStats` (`src/lib/dal/classroom.ts:890-984`) 严格 `eq(pluginOwnedQuizResponses.isLatest, true)` 唯一票,append-only `attemptNo` 历史 attempts 不膨胀分布与分母。
- `submitQuizSampleAnswerAction` (`src/actions/classroom-actions.ts:318-329`) 在写成功后 `updateTag(cacheTags.quizStats(parsed.data.sessionId))`,保证复盘 DTO 在下一次 RSC 渲染时新鲜 (read-your-writes)。
- 视觉契约:`ClassroomSessionRecapSurface` (`src/components/classroom/classroom-session-recap-surface.tsx:98-178`) 渲染 `题目复盘` section,遵守 Stitch `5322129002350954765` + `DESIGN.md` 四条 locked 不变量 (Lexend、no 1px divider、tonal surface、glass/gradient CTA)。

### Manual sign-off ledger posture

- 两行 manual sign-off 在 `72.1-PROOF-MAPPING.md` Section 4 落账,使用 locked schema:`proof artifact` + `status: passed` + `executed_by` + `executed_at` + `evidence note`。
- executor 选择 static-evidence 路径:wave-1 `verify-phase71-marketplace-lifecycle.ts --smoke` 输出 + wave-2 `verify-phase72-close-gate.ts --smoke` Stage 3 / Stage 4 静态断言结果作为真实产品接缝在 milestone 入口被反复断言的 evidence;operator 可后续替换 `executed_by` / `executed_at` / `evidence note` 为真实浏览器走查结果,而不破坏 schema。
- final gate parser 解析契约:deterministic string `| status | \`status: passed\` |` count + 3 field token count,任一 `< 2` 即 hard-fail。

## Operator observation points

| Surface | Why it matters |
|---------|----------------|
| `pnpm verify:phase` (alias) | 唯一外部 milestone close gate 入口,wave-3 后必须等 5 stages 全绿才能 exit 0 |
| `/settings/plugins` | marketplace lifecycle 真实产品面;`PluginMarketplaceSurface` SSR bundle seam + retain reinstall + semver upgrade + cleanup uninstall 三条 lifecycle 行 |
| `/classroom` ended-session | 课堂 recap 真实产品面;`ClassroomSessionRecapSurface` 渲染 `题目复盘` section,真相源 `pluginOwnedQuizResponses.isLatest=true` |
| `.planning/phases/72.1-.../72.1-PROOF-MAPPING.md` | requirement / flow / proof traceability index + manual sign-off ledger; final gate Stage 5 强依赖 |
| `.planning/phases/72.1-.../72.1-CLOSEOUT.md` | archive-ready close 结论; final gate Stage 5 强依赖 |

## Explicit exclusions

以下内容明确 **不** 属于本 milestone 已完成范围,且在 72.1 完成后 D-72.1-16 锁定下,任一被回写都将让 final gate 失败:

1. **Doc-only closure**(closeout / verification 文件存在但与代码/verifier 弱耦合,只靠解释):本 closeout 的 proof chain / 72.1-PROOF-MAPPING.md 落账内容 + final gate Stage 5 静态断言 + manual sign-off ledger parser 共同保证每条 evidence 都被 deterministic check 解析。
2. **Missing manual sign-off capture**(`/settings/plugins` 或 ended-classroom-recap 的 manual verification 未在 ledger 落账):`verify-phase72-close-gate.ts` Stage 5 显式 require 两行 manual sign-off 行 `status: passed` + `executed_by` + `executed_at` + `evidence note` 全部非空,缺一 hard-fail。
3. **Final-gate wiring without artifact dependency checks**(`verify:phase72` 只跑 67-71 顺序,不再 require 72.1 close artifacts):wave-3 `f8b6cbf` 显式 require `72.1-CLOSEOUT.md` + `72.1-PROOF-MAPPING.md` + `72-VERIFICATION.md` 存在,以及 final-artifact 内容含 proof chain wording,缺失即 hard-fail。

其它本 milestone 不扩展的事项:

4. PostgreSQL cutover
5. RAG / Qdrant ingestion
6. MCP server / external tool connectors
7. 多 Agent 编排 / AI runtime 扩展
8. plugin sandbox JS / WASM
9. 第二 marketplace surface / 第二 plugin runtime

## Deferred next steps

如果后续继续推进,应从这些 deferred 项里单独立 phase,而**不**回写本次 close 结论:

1. v2.x 路线图上的 BullMQ-backed async workers / Redis Streams
2. PostgreSQL migration(目前 SQLite + libSQL)
3. RAG / Qdrant ingestion 与教材中心
4. MCP 外部 tool connectors
5. 多 Agent 编排 / AI runtime 扩展
6. plugin sandbox / WASM 远程任意 JS
7. 第二 marketplace surface / 第二 plugin runtime
8. plugin 直接 DB / API 接入(本 phase 严守 D-72.1-16:插件仅走声明式权限与受限 action)

## Final note

本次 v4.0 close 的关键信息只有三条:

1. `pnpm verify:phase` 是唯一外部 milestone close gate,alias 到 `verify:phase72`,当前为 5 stage / 44 static-check + ordered pnpm ladder;`72.1-CLOSEOUT.md` / `72.1-PROOF-MAPPING.md` / `72-VERIFICATION.md` 任一缺失、或 manual sign-off ledger 落账不全,都会让 gate 失败。
2. 6 段 product 链 (declaration/install → teacher config → student answer → recap → semver upgrade → retain reinstall + cleanup uninstall) 在 `verify-phase72-close-gate.ts` Stage 3 (lifecycle bridge 11) + Stage 4 (recap bridge 9) + Stage 5 (final-artifact + manual sign-off) 共同落地;lifecycle 真相 (`recoverRetainedPluginInstallWithTx` / `preflightPluginUpgrade` / `uninstallPluginWithTx` + `cleanupConfirmationToken` + `PLUGIN_ACTIVE_CLASSROOM_BLOCKED`) 与 recap 真相 (`getClassroomSessionRecapDTO` + `buildQuizSampleRecapStats` + `pluginOwnedQuizResponses.isLatest=true`) 都在 milestone 入口被 deterministic 静态断言实证。
3. `verify:phase68` 显式写入 final proof chain,锁定 governed-access truth 不消失在 final archive path (D-72.1-06);D-72.1-16 的"more authoritative over lighter"姿态在 wave-3 后固化为 final gate 的 hard-fail 边界(三个 forbidden shortcuts 任一出现即 exit 1)。

---

*Phase: 72.1-close-gap-gate-01-authoritative-milestone-close-gate*
*Completed: 2026-06-07T06:30:00Z*
