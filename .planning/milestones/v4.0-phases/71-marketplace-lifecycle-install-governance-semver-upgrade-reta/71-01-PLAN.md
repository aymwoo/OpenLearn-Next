---
phase: 71-marketplace-lifecycle-install-governance-semver-upgrade-reta
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/phase71-marketplace-fixtures.ts
  - scripts/verify-phase71-marketplace-lifecycle.ts
  - package.json
autonomous: true
requirements:
  - MKT-02
  - MKT-03
  - MKT-05
must_haves:
  truths:
    - "Phase 71 的 zero-loss upgrade、cleanup blast radius、active-session blocking 证明都基于真实 quiz owned-data，而不是空库或 mock-only 语境。"
    - "Operator-facing destructive lifecycle guarantees 由单一 `verify:phase71` 权威脚本收口验证，可重复证明升级不丢数据、cleanup 有真实影响面、active session 会硬阻断。"
  artifacts:
    - path: scripts/lib/phase71-marketplace-fixtures.ts
      provides: Deterministic external-plugin lifecycle fixtures with retained rows, live sessions, and quiz-owned data
    - path: scripts/verify-phase71-marketplace-lifecycle.ts
      provides: Phase 71 end-to-end proof runner
    - path: package.json
      provides: verify:phase71 script entry
  key_links:
    - from: package.json
      to: scripts/verify-phase71-marketplace-lifecycle.ts
      via: pnpm script mapping
      pattern: 'verify:phase71'
    - from: scripts/verify-phase71-marketplace-lifecycle.ts
      to: scripts/lib/phase71-marketplace-fixtures.ts
      via: fixture imports
      pattern: seedPhase71MarketplaceFixtures
---

<objective>
建立 Phase 71 的权威验证跑道与可重复 fixture，确保后续 install / upgrade / uninstall / recovery / active-session blocking 都能在“有真实 quiz owned-data”的前提下验证。

Purpose: 先补齐验证真相源，避免后续计划在空表或 UI 文案上“看起来完成”。
Output: isolated SQLite fixture helper、`verify:phase71` 脚本入口、带真实答题/retain/live-session 样本的 proof lane。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-CONTEXT.md
@.planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-RESEARCH.md
@.planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-PATTERNS.md
@.planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-VALIDATION.md
@src/db/schema/generated/plugin-owned/quiz.ts

<interfaces>
From src/lib/cache-policy.ts:
```ts
quizStats: (sessionId: string) => `quiz-stats:${sessionId}`
pluginRegistry: 'plugin:registry'
plugin: (id: string) => `plugin:${id}`
```

From src/db/schema/generated/plugin-owned/quiz.ts:
```ts
pluginOwnedQuizQuestions(classroomSession, question, prompt, optionAText...correctOption)
pluginOwnedQuizResponses(classroomSession, student, question, selectedOption, attemptNo, isLatest)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 Phase 71 fixture helper 与真实数据种子</name>
  <files>
    scripts/lib/phase71-marketplace-fixtures.ts
  </files>
  <read_first>
    .planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-CONTEXT.md
    .planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-RESEARCH.md
    .planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-PATTERNS.md
    .planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-VALIDATION.md
    src/db/schema/generated/plugin-owned/quiz.ts
    scripts/lib/phase71-marketplace-fixtures.ts
  </read_first>
  <action>新建 `scripts/lib/phase71-marketplace-fixtures.ts`，导出 `createPhase71IsolatedDb()` 与 `seedPhase71MarketplaceFixtures()` 两个 helper，并显式沿用 `71-PATTERNS.md` 中 `scripts/verify-phase69-quiz-sample.ts` / `scripts/verify-phase70-quiz-stats.ts` 的 isolated SQLite seed analog。`createPhase71IsolatedDb()` 必须返回可被 smoke verify 调用的临时 DB 上下文（至少包含 dispose/cleanup 能力）；`seedPhase71MarketplaceFixtures()` 必须返回具名 fixture ids/counts，供脚本与测试直接断言。fixture 必须显式创建：1) 至少一个 `sourceType='external'` 的 quiz external registration；2) 一条 `retain` 卸载后的 retained registration；3) 一个 `status='live'` 的 classroom session 与占用该 external plugin 的 `plugin_owned_quiz_questions` / `plugin_owned_quiz_responses` 行；4) 一个 ended session 与非空答题数据，用于后续 MKT-02 的 row-count / stats parity；5) 真实 impact counts（作答条数、受影响 ended session 数）可被 cleanup token 计算。所有 fixture 值都用确定性常量命名，便于后续 verifier 与测试直接断言。该 helper 负责 D-71-10、D-71-13、D-71-15 的真实数据前提，不得用空数组或占位数值代替。若 helper 需要复用现有 schema / migration runner，执行前必须先对相关符号运行 GitNexus upstream impact analysis 并记录 blast radius。</action>
  <acceptance_criteria>
    - `scripts/lib/phase71-marketplace-fixtures.ts` 存在并导出 `createPhase71IsolatedDb` 与 `seedPhase71MarketplaceFixtures`。
    - 文件内可 grep 到 `status: "live"`、`sourceType: "external"`、`uninstallRetentionMode: "retain"`、`pluginOwnedQuizResponses`。
    - helper 返回值中包含可供 verifier 断言的 `liveSessionId`、`retainedPluginId`、`endedSessionId`、`responseCount` 等 fixture 标识。
    - helper 可被 `node --import tsx` 成功 import、建库、seed、dispose，而不是仅靠文本存在通过。
  </acceptance_criteria>
  <verify>
    <automated>node --import tsx -e "const mod = await import('./scripts/lib/phase71-marketplace-fixtures.ts'); const ctx = await mod.createPhase71IsolatedDb(); try { const seeded = await mod.seedPhase71MarketplaceFixtures(ctx); if (!seeded.liveSessionId || !seeded.endedSessionId || !seeded.retainedPluginId) throw new Error('phase71 fixture seed incomplete'); } finally { if (typeof ctx.dispose === 'function') await ctx.dispose(); }"</automated>
  </verify>
  <done>Phase 71 后续所有 backend / UI / verifier 都能从单一 fixture helper 拉起“非空 quiz owned-data + retain + active classroom”测试语境。</done>
</task>

<task type="auto">
  <name>Task 2: 落地 verify:phase71 脚本与 npm 入口</name>
  <files>
    scripts/verify-phase71-marketplace-lifecycle.ts
    package.json
  </files>
  <read_first>
    .planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-VALIDATION.md
    .planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-PATTERNS.md
    .planning/phases/68-governed-declarative-data-access-verbs/68-04-SUMMARY.md
    .planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-04-SUMMARY.md
    .planning/phases/70-question-stats-post-class-recap/70-02-SUMMARY.md
    scripts/verify-phase71-marketplace-lifecycle.ts
    package.json
  </read_first>
  <action>新增 `scripts/verify-phase71-marketplace-lifecycle.ts` 并在 `package.json` 注册 `verify:phase71`。脚本结构固定为三段：`[1]` 静态 seam 检查（新 action / command / marketplace surface / no-core-backdoor contract）；`[2]` focused vitest 子集；`[3]` isolated SQLite 实证，调用 Task 1 fixture helper 先 seed quiz data，再跑 upgrade / retain / cleanup / recover / active-session block proof。实现时必须沿用 `71-PATTERNS.md` 中 `verify-phase48/69/70` 的 shared verifier 模式。此任务必须直接写出完整 verifier 收口结构，而不是 placeholder-only skeleton：即使后续 71-02/03/04 还会继续补断言，脚本此时也要已经具备真实 stage runner、具名断言函数、失败即退出的 proof harness、以及对 `backfill -> verify -> cutover`、retain/recover、cleanup token、active session blocker 的可执行断言入口。脚本还必须支持 `--smoke` 模式：在 smoke 模式下跑静态 seam + fixture seed + 最小 proof，不依赖完整 backend 实现即可验证 harness 可执行。不得把 proof 简化成只跑 vitest。同步把 `package.json` 加入 `verify:phase71`，命令格式固定为 `node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase71-marketplace-lifecycle.ts`。</action>
  <acceptance_criteria>
    - `package.json` 中存在 `"verify:phase71"`，且命令精确指向 `scripts/verify-phase71-marketplace-lifecycle.ts`。
    - verifier 文件支持 `--smoke` 模式，且 smoke 模式至少执行静态 seam、fixture seed、最小 proof 三步。
    - verifier 脚本明确包含三段日志标签（静态 seam / vitest / isolated SQLite proof），不是单段测试 runner。
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase71 -- --smoke</automated>
  </verify>
  <done>`pnpm verify:phase71` 入口就位，且脚本已是完整 proof harness：后续 plans 只是在其上补实 backend/UI 断言，不需要再新增另一条 verifier 收口计划。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier → isolated SQLite fixture DB | proof 脚本写入/读取测试数据，必须防止空数据误判 |
| fixture helper → lifecycle proof steps | seed 出来的 retained/live-session truth 会直接影响 destructive-op 断言 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01-1 | T | phase71 fixture helper | mitigate | fixture helper 必须显式写入 non-empty quiz question/response rows、retained row、live session，占位空数据视为无效实现 |
| T-71-01-2 | R | verify:phase71 script | mitigate | verifier 输出具名 proof stage 和 assertion label，失败时指向具体 seam，不允许只返回模糊测试失败 |
| T-71-01-3 | D | package script mapping | mitigate | 将 `verify:phase71` 固定到 server-only shim + tsx 脚本路径，避免调用错误脚本导致 close gate 漏检 |
</threat_model>

<verification>
完成本计划后，执行者应能通过 `pnpm verify:phase71 -- --smoke` 看到明确的 Phase 71 proof stages，并可独立复用 fixture helper 生成非空 quiz lifecycle 数据。</verification>

<success_criteria>
- Phase 71 verifier 不再依赖空库或手工脑补数据。
- 后续 plans 可以直接复用统一 fixture helper 验证 upgrade / uninstall / recovery / blocker。
- `package.json` 已提供单一权威 `verify:phase71` 入口。</success_criteria>

<output>
After completion, create `.planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-01-SUMMARY.md`
</output>
