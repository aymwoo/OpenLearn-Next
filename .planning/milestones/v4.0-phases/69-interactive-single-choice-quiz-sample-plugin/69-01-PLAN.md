---
phase: 69-interactive-single-choice-quiz-sample-plugin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/quiz-sample/data-model.ts
  - src/db/schema/generated/plugin-owned/quiz.ts
  - src/db/schema/generated/plugin-owned/data-access-allowlist.ts
  - drizzle/*.sql
  - src/lib/dto/resource-ai.ts
  - scripts/bootstrap-dev-db.ts
  - src/lib/dal/plugins.builtins.test.ts
  - src/lib/dal/plugins.test.ts
autonomous: true
requirements: [QUIZ-03, QUIZ-01]
must_haves:
  truths:
    - "quiz sample 不再只是 data-model 文件；它在 built-in plugin host 中有真实可注册身份（QUIZ-03）"
    - "plugin_owned_quiz_questions 能承载完整 session snapshot：prompt + option A-D text + correctOption（D-03/D-04）"
    - "plugin:compile 后生成 schema/allowlist 零漂移，迁移 checked-in"
  artifacts:
    - path: "plugins/quiz-sample/data-model.ts"
      provides: "扩展后的 quiz sample question snapshot schema 单一真相源"
      contains: "optionAText"
    - path: "src/lib/dto/resource-ai.ts"
      provides: "quiz sample built-in definition / template discoverability"
      contains: "quiz sample"
    - path: "scripts/bootstrap-dev-db.ts"
      provides: "dev bootstrap built-in plugin registration for quiz sample"
      contains: "builtIn: true"
  key_links:
    - from: "plugins/quiz-sample/data-model.ts"
      to: "src/db/schema/generated/plugin-owned/quiz.ts"
      via: "pnpm plugin:compile codegen"
      pattern: "plugin_owned_quiz_questions"
    - from: "scripts/bootstrap-dev-db.ts"
      to: "pluginRegistrations"
      via: "producePluginInstallCommand installSource=bootstrap"
      pattern: "BUILT_IN_PLUGIN_DEFINITIONS"
---

<objective>
把 Phase 69 的样板 quiz 从“只有 schema 的占位物”补到“真实进入 built-in plugin host 的可治理样板插件”，并扩展 question snapshot schema 以容纳完整题面。这个 plan 先解决 Phase 69 的底座：插件身份与结构化表必须先成立，后续 authoring / launch / runtime 才有可依附的真实对象。

Purpose: 先把 plugin host 与 plugin-owned schema 定住，避免后续功能实现建立在不存在的插件外壳上。
Output: 扩 question schema、生成迁移/allowlist、built-in definition/bootstrapping 入口就位。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-CONTEXT.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-RESEARCH.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-PATTERNS.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-VALIDATION.md

<interfaces>
<!-- 现有 schema 真相源 -->
plugins/quiz-sample/data-model.ts

<!-- 现有 built-in teaching step catalog -->
src/lib/dto/resource-ai.ts

<!-- 现有 built-in plugin bootstrap -->
scripts/bootstrap-dev-db.ts -> BUILT_IN_PLUGIN_DEFINITIONS -> producePluginInstallCommand(... installSource: "bootstrap")

<!-- 现有 codegen -->
package.json -> "plugin:compile": "tsx scripts/compile-plugin-data-model.ts"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 quiz sample question snapshot schema（A-D 文本槽位）</name>
  <read_first>
    - 修改文件：plugins/quiz-sample/data-model.ts
    - 生成产物：src/db/schema/generated/plugin-owned/quiz.ts、src/db/schema/generated/plugin-owned/data-access-allowlist.ts、drizzle/*.sql
    - PATTERNS：69-PATTERNS.md 中 data-model self pattern
    - 注意当前迁移基线：drizzle/0005_lean_sage.sql 与 0006_worried_wallow.sql
  </read_first>
  <files>plugins/quiz-sample/data-model.ts, src/db/schema/generated/plugin-owned/quiz.ts, src/db/schema/generated/plugin-owned/data-access-allowlist.ts, drizzle/</files>
  <behavior>
    - Test: `plugin_owned_quiz_questions` 新增固定槽位文本列（如 `optionAText`...`optionDText`）
    - Test: `correctOption` 仍是 A-D enum；questions 表仍是单表结构
    - Test: `pnpm plugin:compile` 后生成 schema/allowlist 可重放且零漂移
  </behavior>
  <action>
    修改 `quizDataModel`，在 `plugin_owned_quiz_questions` 上新增 4 个固定文本槽位列，用于 session 冻结题面。保持 `correctOption` 的 A-D enum 与 question 单表结构；不要新建 options 子表。随后运行 codegen，生成新的 drizzle schema、allowlist 与 checked-in migration。必要时补充 migration/backfill，使旧 question 行能安全迁移到新结构。
  </action>
  <verify>
    <automated>pnpm plugin:compile && git diff --exit-code src/db/schema/generated/plugin-owned/quiz.ts src/db/schema/generated/plugin-owned/data-access-allowlist.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "optionAText\|optionBText\|optionCText\|optionDText" plugins/quiz-sample/data-model.ts` 命中
    - questions 表仍只有一张：不存在新的 `plugin_owned_quiz_options` 声明
    - `src/db/schema/generated/plugin-owned/quiz.ts` 与 allowlist 生成文件均包含新增列
    - 迁移文件 checked-in，重跑 `pnpm plugin:compile` 无新增 diff
  </acceptance_criteria>
  <done>question snapshot schema 已能承载完整题面，codegen/迁移产物齐全。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 让 quiz sample 成为真实 built-in plugin host 可达对象</name>
  <read_first>
    - 修改文件：src/lib/dto/resource-ai.ts、scripts/bootstrap-dev-db.ts
    - 参考：`classroomVoting` built-in definition；`BUILT_IN_PLUGIN_DEFINITIONS`
    - 目标不是完成全部 UI，而是把宿主接缝建立起来
  </read_first>
  <files>src/lib/dto/resource-ai.ts, scripts/bootstrap-dev-db.ts, src/lib/dal/plugins.builtins.test.ts, src/lib/dal/plugins.test.ts</files>
  <behavior>
    - Test: dev bootstrap 后 quiz sample built-in plugin 能被注册为 built-in/defaultEnabled/bootstrap source
    - Test: built-in teaching-step catalog / template path 中有 quiz sample 的宿主入口（名称、pluginKey、stepType）
    - Test: 不影响现有 built-ins（classroomVoting, inClassQuiz 等）
  </behavior>
  <action>
    锁定唯一宿主接缝，不留条件分支：
    1. 仅在 `src/lib/dto/resource-ai.ts` 的 `BUILT_IN_TEACHING_STEP_DEFINITIONS` 中新增 quiz sample 的 built-in teaching-step definition，明确 `builtInKey`、`pluginKey`、`pluginName`、`stepType` 与最小 authoring contract/discoverability metadata；
    2. 仅在 `scripts/bootstrap-dev-db.ts` 的 `BUILT_IN_PLUGIN_DEFINITIONS` 中新增对应 manifest，由既有 `producePluginInstallCommand(... installSource: "bootstrap")` 路径注册；
    3. 后续 authoring / launch / runtime 只能复用这两个入口提供的 plugin identity，不允许再引入“假插件”或替代宿主。
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/dal/plugins.builtins.test.ts src/lib/dal/plugins.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/bootstrap-dev-db.ts` 的 built-in bootstrap 定义里出现 quiz sample manifest
    - quiz sample manifest 带 `builtIn: true`、`defaultEnabled: true`、`installSource: "bootstrap"` 路径可达
    - `BUILT_IN_TEACHING_STEP_DEFINITIONS` 与 `BUILT_IN_PLUGIN_DEFINITIONS` 使用同一个稳定 `pluginKey`
    - 宿主入口有稳定 pluginKey，后续 authoring/launch 不必硬编码“假插件”
    - 既有 built-in tests 保持通过
  </acceptance_criteria>
  <done>quiz sample 成为真实 built-in/governance host 可达插件，不再只是 schema 孤岛。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data-model declaration -> generated schema | compile-time single source; zero runtime DDL |
| built-in bootstrap -> plugin registration | sample plugin must still enter the same governance/lifecycle path as other built-ins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | Tampering | question schema | mitigate | 单表固定槽位，避免第二真相源和运行时结构漂移 |
| T-69-02 | Elevation | sample plugin identity | mitigate | 走现有 built-in plugin registration/governance path，避免隐藏后门插件 |
| T-69-03 | Tampering | generated schema drift | mitigate | `plugin:compile` + checked-in generated files + migration diff 守卫 |
</threat_model>

<verification>
- `pnpm plugin:compile` 零漂移
- built-in/plugin registration focused tests 通过
- 生成 schema 与 bootstrap manifests 能被 grep 到 quiz sample 新入口
</verification>

<success_criteria>
Phase 69 的样板插件拥有真实宿主身份与可承载完整 session 快照的结构化表，为后续 QUIZ-01/02 的 authoring 与 runtime 实现提供可信底座。
</success_criteria>

<output>
完成后创建 `.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-01-SUMMARY.md`
</output>
