---
phase: 69-interactive-single-choice-quiz-sample-plugin
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - scripts/verify-phase69-quiz-sample.ts
  - package.json
  - .planning/ROADMAP.md
autonomous: true
requirements: [QUIZ-01, QUIZ-02, QUIZ-03]
must_haves:
  truths:
    - "存在 phase69 close gate：authoring -> launch freeze -> student answer -> no-core-backdoor 可一键复跑"
    - "`verify:phase69` 在 `package.json` 中显式串联 compile / focused tests / end-to-end close gate wiring"
    - "ROADMAP.md 不再是 Plans: TBD，Phase 69 计划列表已落盘"
  artifacts:
    - path: "scripts/verify-phase69-quiz-sample.ts"
      provides: "phase69 close gate script"
      contains: "plugin_owned_quiz_questions"
    - path: "package.json"
      provides: "verify:phase69 script wiring"
      contains: "verify:phase69"
    - path: ".planning/ROADMAP.md"
      provides: "phase69 plan list reflected in roadmap"
      contains: "69-01-PLAN.md"
  key_links:
    - from: "scripts/verify-phase69-quiz-sample.ts"
      to: "package.json"
      via: "verify:phase69"
      pattern: "close gate entrypoint"
    - from: "package.json"
      to: "scripts/verify-phase69-quiz-sample.ts"
      via: "verify:phase69 -> pnpm plugin:compile + focused tests + close gate"
      pattern: "compile/focused tests/close gate chain"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-01-PLAN.md"
      via: "phase 69 plan bullets"
      pattern: "69-01..05-PLAN.md"
---

<objective>
关闭 Phase 69：补齐 end-to-end close gate，让本 phase 的关键承诺能被自动验证，并把 roadmap 的 `Plans: TBD` 更新为实际计划清单。这个 plan 的目标不是再开发功能，而是把“样板插件确实通过第三方同款治理链路工作”固定成回归闸门。

Purpose: 让 Phase 69 从一组实现任务变成可重复验收的阶段性成果。
Output: verify script、package scripts、roadmap plan list。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-CONTEXT.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-RESEARCH.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-VALIDATION.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 编写 verify-phase69 close gate</name>
  <read_first>
    - 新建文件：scripts/verify-phase69-quiz-sample.ts
    - 参考：scripts/verify-phase68-data-access-verbs.ts
    - 来源：69-VALIDATION.md 的 end-to-end expectations
  </read_first>
  <files>scripts/verify-phase69-quiz-sample.ts</files>
  <action>
    编写一个 end-to-end verification runner，最少覆盖：
    0. quiz sample built-in plugin 已经通过 `BUILT_IN_PLUGIN_DEFINITIONS` + bootstrap 注册，且 `BUILT_IN_TEACHING_STEP_DEFINITIONS` 可发现同一 `pluginKey`；
    1. 老师保存 quiz sample 配置（合法配置成功、非法配置被拦）；
    2. 开课后 question snapshot 写入 `plugin_owned_quiz_questions`，且包含完整题面；
    3. 学生首次作答 + 改答后 `plugin_owned_quiz_responses` 行数递增、latest 正确切换；
    4. quiz sample 路径不写 core `quizAttempts`；
    5. 关闭/切走后再次提交被拒；
    6. plugin-owned 写路径在 governance audit / Phase 68 facade 可见。
    脚本失败时输出具体断言上下文并以非零退出，成功时以 0 退出。
  </action>
  <verify>
    <automated>node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase69-quiz-sample.ts</automated>
  </verify>
  <acceptance_criteria>
    - close gate 覆盖 built-in registration/bootstrap/discoverability、老师配置、开课冻结、学生作答、改答、关闭拒答、无 core backdoor
    - 脚本可独立运行并用退出码表达结果
    - 至少显式查询 `pluginRegistrations` / `plugin_owned_quiz_questions` / `plugin_owned_quiz_responses` / `quizAttempts`
  </acceptance_criteria>
  <done>Phase 69 有端到端 close gate，可证明样板插件走的是治理路径而非内置后门。</done>
</task>

<task type="auto">
  <name>Task 2: 串联 verify:phase69 并更新 roadmap plan list</name>
  <read_first>
    - 修改文件：package.json、.planning/ROADMAP.md
    - 参考：verify:phase68 脚本写法；ROADMAP 中 Phase 67/68 的 Plans 列表样式
  </read_first>
  <files>package.json, .planning/ROADMAP.md</files>
  <action>
    在 `package.json` 中新增 `verify:phase69`，串联 Phase 69 所需 codegen、focused tests 与 close gate 脚本；不要改动全局 `verify:phase` alias，Phase 72 再统一收口。随后更新 `ROADMAP.md` 中 Phase 69 的计划状态，使 summary 区与 detail 区保持一致：保留 `5 plans` 和 5 个 plan bullet，文案与已创建的 `69-01..05-PLAN.md` 对齐，并同步把 progress 行从 `0/TBD` 改为与当前计划数一致的状态（例如 `0/5`）。
  </action>
  <verify>
    <automated>grep -n '"verify:phase69"' package.json && grep -n '\*\*Plans\*\*: 5 plans\|69-01-PLAN.md\|69-02-PLAN.md\|69-03-PLAN.md\|69-04-PLAN.md\|69-05-PLAN.md\|0/5' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` 中有 `verify:phase69`
    - `verify:phase` alias 仍保持现状，不在 Phase 69 切换
    - `ROADMAP.md` 不再显示 Phase 69 的 `Plans: TBD`
    - roadmap 计划标题与已写 `69-01..05-PLAN.md` 基本一致
    - `ROADMAP.md` 的 Phase 69 progress 行与 `5 plans` 状态一致，不再保留 `0/TBD`
    - `pnpm verify:phase69` 仅作为阶段 close-gate 运行，不作为该 task 的常规快速反馈验证
  </acceptance_criteria>
  <done>Phase 69 的验证命令和 roadmap 计划清单全部落盘。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regression gate -> shipped behavior | close gate must prove real path, not mocks-only illusions |
| roadmap plan list -> execution traceability | roadmap should truthfully reflect actual plan files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-13 | Repudiation | verify coverage | mitigate | end-to-end runner covers complete quiz sample chain, not just isolated helpers |
| T-69-14 | Tampering | stale roadmap | mitigate | replace `Plans: TBD` with concrete plan list after files exist |
| T-69-15 | Elevation | hidden core backdoor regression | mitigate | close gate explicitly asserts no `quizAttempts` writes for quiz sample path |
</threat_model>

<verification>
- `pnpm verify:phase69` 退出码 0
- roadmap 已同步到具体计划列表
</verification>

<success_criteria>
Phase 69 拥有可重复运行的 close gate 和真实的 roadmap 计划清单，后续 verify-work / verifier 能直接消费这些产物。
</success_criteria>

<output>
完成后创建 `.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-05-SUMMARY.md`
</output>
