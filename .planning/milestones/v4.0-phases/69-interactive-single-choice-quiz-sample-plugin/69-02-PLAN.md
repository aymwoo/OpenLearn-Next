---
phase: 69-interactive-single-choice-quiz-sample-plugin
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/lib/dto/lesson-authoring.ts
  - src/lib/dal/lesson-authoring.ts
  - src/actions/lesson-authoring-actions.ts
  - src/components/authoring/lesson-step-editor.tsx
  - src/lib/dal/lesson-authoring.test.ts
  - src/components/authoring/lesson-step-editor.test.tsx
autonomous: true
requirements: [QUIZ-01, QUIZ-03]
must_haves:
  truths:
    - "老师通过插件专属配置卡保存单选题配置，配置 durable truth 落在 lesson step + plugin step extension（QUIZ-01）"
    - "保存前即拦非法配置：题干为空、有效选项 < 2、正确答案不在启用项（D-07）"
    - "作者侧不直接写 plugin_owned question snapshot 表（D-01）"
  artifacts:
    - path: "src/lib/dal/lesson-authoring.ts"
      provides: "quiz sample config save path + hydration"
      contains: "quiz sample"
    - path: "src/components/authoring/lesson-step-editor.tsx"
      provides: "插件专属配置卡 UI"
      contains: "保存题目配置"
  key_links:
    - from: "src/components/authoring/lesson-step-editor.tsx"
      to: "src/actions/lesson-authoring-actions.ts"
      via: "saveQuizSampleLessonStepAction"
      pattern: "plugin-specific editor save"
    - from: "src/actions/lesson-authoring-actions.ts"
      to: "src/lib/dal/lesson-authoring.ts"
      via: "saveQuizSampleLessonStepConfig"
      pattern: "fieldErrors + lesson/plugin extension save"
    - from: "src/lib/dal/lesson-authoring.ts"
      to: "src/lib/cache-policy.ts"
      via: "cacheTags.lesson/cacheTags.steps/cacheTags.draftLesson"
      pattern: "authoring read-your-writes invalidation"
---

<objective>
交付 QUIZ-01 的老师侧闭环：在现有 authoring shell 中提供 quiz sample plugin 的独立配置卡，并通过 `lesson step + plugin step extension` 受治理保存配置。该 plan 只处理老师编辑、校验、保存、回显，不提前创建 session snapshot。

Purpose: 让样板插件先具备真实可用的作者侧配置闭环，同时保持 D-01 的备课真相源边界。
Output: DTO、DAL、Server Action、编辑器 UI 全链路可保存并原位校验。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-CONTEXT.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-UI-SPEC.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-RESEARCH.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-PATTERNS.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-VALIDATION.md

<interfaces>
<!-- 可复用作者侧保存链 -->
saveVotingLessonStepConfig / saveVotingLessonStepAction / upsertPluginStepExtensionWithTx

<!-- 当前 editor shell -->
src/components/authoring/lesson-step-editor.tsx

<!-- UI contract -->
69-UI-SPEC.md Teacher UI Contract
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 quiz sample authoring DTO / validation / DAL save path</name>
  <read_first>
    - 修改文件：src/lib/dto/lesson-authoring.ts、src/lib/dal/lesson-authoring.ts、src/actions/lesson-authoring-actions.ts
    - 参考：`ClassroomVotingAuthoringConfigSchema` 与 `saveVotingLessonStepConfig`
  </read_first>
  <files>src/lib/dto/lesson-authoring.ts, src/lib/dal/lesson-authoring.ts, src/actions/lesson-authoring-actions.ts, src/lib/dal/lesson-authoring.test.ts</files>
  <behavior>
    - Test: quiz sample config schema 支持 2-4 个选项、正确答案为 A-D 中启用项
    - Test: save path 更新 lesson step shell + plugin step extension，并 bump lesson revision
    - Test: 非法配置保存前返回 fieldErrors，而不是拖到 publish / launch
    - Test: save path 不写 `plugin_owned_quiz_questions`
  </behavior>
  <action>
    新增或扩展 quiz sample authoring contract / frozen contract DTO，并在 `lesson-authoring.ts` 中实现 `saveQuizSampleLessonStepConfig`：复用 voting save 的事务骨架，但落入 quiz sample 的 shell payload 与 extension payload。Server Action 层提供 `saveQuizSampleLessonStepAction` 并返回结构化 fieldErrors 以支撑 editor inline validation。保存成功后明确刷新 `cacheTags.lesson(lessonId)`、`cacheTags.steps(lessonId)`、`cacheTags.draftLesson(lessonId)`；明确禁止此路径直接写 plugin-owned question snapshot 表。
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/dal/lesson-authoring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 保存 helper 有 quiz sample 专用 schema / payload builder
    - 非法配置能在测试里拿到字段级错误
    - 保存后 lesson revision / updatedAt / extension payload 都更新
    - 保存成功后 authoring 相关 cache tags 被显式刷新
    - grep 不应显示该 save path 触达 `plugin_owned_quiz_questions`
  </acceptance_criteria>
  <done>quiz sample 作者侧配置可被受治理保存，且坏配置会在保存前被拦截。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在现有 authoring shell 中落地插件专属配置卡</name>
  <read_first>
    - 修改文件：src/components/authoring/lesson-step-editor.tsx
    - UI 来源：69-UI-SPEC.md Teacher UI Contract
    - 参考：当前 classroomVoting editor branch
  </read_first>
  <files>src/components/authoring/lesson-step-editor.tsx, src/components/authoring/lesson-step-editor.test.tsx</files>
  <behavior>
    - Test: 选中 quiz sample step 时出现插件专属配置卡，而不是普通 quiz textarea
    - Test: 可编辑题干、2-4 个选项、正确答案；未启用槽位不作为可选正确答案
    - Test: inline validation / 保存中 / 保存成功反馈符合 UI-SPEC 文案与行为
  </behavior>
  <action>
    在 step editor 里抽离/扩展 plugin-specific 编辑器分支，为 quiz sample 提供独立配置卡、预览卡、字段级报错和保存按钮。保留既有 authoring shell、左右栏结构、command event/save/reset 机制，不重写整页。文案与状态遵循 `69-UI-SPEC.md`，并直接调用 `saveQuizSampleLessonStepAction`，不再复用模糊的通用 quiz 保存分支。
  </action>
  <verify>
    <automated>pnpm vitest run src/components/authoring/lesson-step-editor.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - UI 中能明确看到插件专属配置身份，不再复用 voting 话术
    - 正确答案选择器只显示启用选项
    - 保存按钮文案、错误文案、预览行为与 UI-SPEC 对齐
    - 保留当前 editor shell 和 reset/save command integration
  </acceptance_criteria>
  <done>老师能在现有备课壳内完成 quiz sample 配置，且视觉/交互符合 Phase 69 UI 契约。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authoring client -> server action | 所有配置必须经 schema 校验与 teacher scope 授权 |
| lesson step shell -> plugin extension | 双写仅限这两个作者态真相源，不跨进 session snapshot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-04 | Tampering | invalid authoring config | mitigate | field-level validation + server schema parse before save |
| T-69-05 | Repudiation | hidden built-in backdoor write | mitigate | save path only updates lesson step + plugin step extension; no plugin-owned snapshot writes |
| T-69-06 | Spoofing | wrong plugin binding | mitigate | validate plugin identity/compatibility similar to voting writable checks |
</threat_model>

<verification>
- lesson-authoring DAL tests 通过
- editor component tests 通过
- 手动查看配置卡是否符合 UI-SPEC（可选人工补充）
</verification>

<success_criteria>
老师能明确地作为“插件专属配置卡”编辑并保存 quiz sample 单选题，坏配置保存前被拦截，且备课真相源仍停留在 lesson step + plugin step extension。
</success_criteria>

<output>
完成后创建 `.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-02-SUMMARY.md`
</output>
