---
phase: 72-end-to-end-verify-phase-close-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-phase72-close-gate.ts
  - package.json
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - GATE-01
must_haves:
  truths:
    - "存在单一 `verify:phase` 命令，能按依赖顺序复跑 67-71 整链 close gates。"
    - "Phase 72 只做 authoritative orchestration，不复制已有 proof 逻辑。"
    - "planning/traceability/bookkeeping 与代码入口保持一致。"
  artifacts:
    - path: scripts/verify-phase72-close-gate.ts
      provides: milestone-level aggregated close gate
    - path: package.json
      provides: verify:phase72 + verify:phase alias wiring
    - path: .planning/ROADMAP.md
      provides: phase72 completion state
    - path: .planning/STATE.md
      provides: milestone progress synced to 100%
    - path: .planning/REQUIREMENTS.md
      provides: completed traceability for STATS/MKT/GATE rows
---

<objective>
把 v4.0 Plugin Marketplace & Plugin-Owned Data 的真实 proof lanes 收口到单一 `verify:phase` 闸门，并同步所有 planning/bookkeeping 到完成态。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 实现 Phase 72 聚合 verifier 与 package 入口</name>
  <files>scripts/verify-phase72-close-gate.ts, package.json</files>
  <action>
    新增 `scripts/verify-phase72-close-gate.ts`，按固定顺序调用 `verify:phase67`、`verify:phase68`、`verify:phase69`、`verify:phase70`、`verify:phase71`。脚本在开始时校验 package scripts wiring，并在每个阶段输出明确标签。随后在 `package.json` 新增 `verify:phase72`，并把全局 `verify:phase` alias 改为 `pnpm verify:phase72`。
  </action>
  <verify>
    <automated>pnpm verify:phase72</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: 同步 milestone planning 与 traceability</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    把 Phase 70/71/72 的 requirements traceability 改为 complete，把 roadmap 和 state 从 Phase 72 in progress 更新为 v4.0 所有 phases complete，并记录 `pnpm verify:phase` 已通过。
  </action>
  <verify>
    <automated>pnpm verify:phase</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- `pnpm verify:phase` 成为 v4.0 单一权威 close gate。
- Phase 72 完成后，ROADMAP / STATE / REQUIREMENTS 与实际代码入口一致。
</success_criteria>

<output>
After completion, create `.planning/phases/72-end-to-end-verify-phase-close-gate/72-01-SUMMARY.md`
</output>
