---
phase: 70-question-stats-post-class-recap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/cache-policy.ts
  - src/lib/dto/classroom.ts
  - src/lib/dal/classroom.ts
  - src/lib/cache-policy.test.ts
  - src/lib/dal/classroom.test.ts
autonomous: true
requirements: [STATS-01]
must_haves:
  truths:
    - "quiz sample stats 只读真相源锁定为 plugin_owned_quiz_questions + plugin_owned_quiz_responses(isLatest)"
    - "新增 session-scoped quizStats cache tag，submit 后可刷新"
    - "ClassroomSessionRecapDTO 有 typed quiz sample stats section；summary artifact 不扩写"
  artifacts:
    - path: "src/lib/cache-policy.ts"
      provides: "quizStats cache tag single source"
      contains: "quizStats"
    - path: "src/lib/dto/classroom.ts"
      provides: "quiz sample recap section DTO schema"
      contains: "quizSampleStats"
    - path: "src/lib/dal/classroom.ts"
      provides: "single DAL aggregate helper for quiz sample stats"
      contains: "plugin_owned_quiz_questions"
---

<objective>
建立 Phase 70 的后端基础：锁定 quiz sample 统计契约、单一 DAL 聚合 seam 和 dedicated cache tag。这个 plan 不做 UI，只把“统计该怎么读、怎么缓存、怎么进 recap DTO”固定下来。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/70-question-stats-post-class-recap/70-CONTEXT.md
@.planning/phases/70-question-stats-post-class-recap/70-RESEARCH.md
@.planning/phases/70-question-stats-post-class-recap/70-PATTERNS.md
@.planning/phases/70-question-stats-post-class-recap/70-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 quiz stats cache tag 与 recap section DTO</name>
  <files>src/lib/cache-policy.ts, src/lib/dto/classroom.ts, src/lib/cache-policy.test.ts</files>
  <action>
    1. 在 `cacheTags` 中新增 `quizStats(sessionId)`；不要硬编码字符串散落在 action 或 component 内。
    2. 在 `src/lib/dto/classroom.ts` 为 quiz sample 题目复盘新增 typed DTO section（question card + option rows + denominator labels），并把它接进 `ClassroomSessionRecapDTOSchema`。
    3. 保持 `ClassroomSessionSummaryArtifactSchema` 不变，避免 durable core analytics 扩容。
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/cache-policy.test.ts src/lib/dal/classroom.test.ts -t "recap contracts"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 classroom DAL 中实现 latest-only quiz sample 聚合 helper</name>
  <files>src/lib/dal/classroom.ts, src/lib/dal/classroom.test.ts</files>
  <action>
    在 `src/lib/dal/classroom.ts` 内新增私有 helper，按 ended session scope 读取 `plugin_owned_quiz_questions`、`plugin_owned_quiz_responses(isLatest)` 和 `classroomParticipants`，用 SQL `GROUP BY` 构造每题 stats。要求：
    - latest-only
    - 正确率分母为 answered count
    - 作答/未作答分母相对 classroom participants
    - 不回读 lesson/plugin extension
    - 不写任何 summary/core analytics 表
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/dal/classroom.test.ts -t "quiz sample stats"</automated>
  </verify>
</task>

</tasks>

<success_criteria>
Phase 70 有了明确的 cache / DTO / DAL 聚合基础，后续 recap integration 和 UI 只需消费这一条单一统计 seam。
</success_criteria>

<output>
完成后创建 `.planning/phases/70-question-stats-post-class-recap/70-01-SUMMARY.md`
</output>
