---
phase: 70-question-stats-post-class-recap
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - scripts/verify-phase70-quiz-stats.ts
  - package.json
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [STATS-01, STATS-02]
must_haves:
  truths:
    - "存在 phase70 close gate：latest-only stats truth + recap seam + no core analytics writeback"
    - "package.json 有 verify:phase70，global verify:phase alias 保持不变"
    - "ROADMAP/STATE 与真实计划数、当前 focus 保持一致"
---

<objective>
关闭 Phase 70 的 planning/verification 回路：补齐 close gate，新增 `verify:phase70`，并同步 roadmap/state，使这个 phase 成为可执行、可验收的正式阶段。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 编写 verify-phase70 close gate</name>
  <files>scripts/verify-phase70-quiz-stats.ts</files>
  <action>
    编写一个 focused close-gate runner，最少覆盖：
    1. question snapshot 与 latest response 来自 plugin-owned tables；
    2. superseded attempts 不进入最新分布；
    3. unanswered denominator 相对 classroom participants；
    4. quiz stats 不写入 core `classroomSessionSummary` / 其他 analytics tables；
    5. recap DTO / surface seam 可见 quiz sample question stats。
  </action>
  <verify>
    <automated>node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase70-quiz-stats.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: 串联 verify:phase70 并同步 roadmap/state</name>
  <files>package.json, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    在 `package.json` 中新增 `verify:phase70`。更新 `ROADMAP.md` 中 Phase 70 的计划列表和 progress 行，把 `Plans: TBD` 改为真实 plan bullets。更新 `STATE.md` 的 current focus / current position / operator next steps 到 Phase 70 ready-to-execute。
  </action>
  <verify>
    <automated>grep -n 'verify:phase70\|70-01-PLAN.md\|70-02-PLAN.md\|70-03-PLAN.md\|70-04-PLAN.md\|0/4' package.json .planning/ROADMAP.md .planning/STATE.md</automated>
  </verify>
</task>

</tasks>

<success_criteria>
Phase 70 的统计口径、UI 接缝和 close gate 都已经落盘，可直接进入执行而不再回到“Plans: TBD”。
</success_criteria>

<output>
完成后创建 `.planning/phases/70-question-stats-post-class-recap/70-04-SUMMARY.md`
</output>
