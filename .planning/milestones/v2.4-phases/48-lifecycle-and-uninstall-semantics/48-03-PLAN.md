---
phase: 48-lifecycle-and-uninstall-semantics
plan: 03
type: execute
wave: 3
depends_on:
  - 48-02
files_modified:
  - package.json
  - scripts/verify-phase48-lifecycle-and-uninstall.ts
autonomous: true
gap_closure: true
requirements:
  - LIFE-01
  - LIFE-02
  - LIFE-03
  - LIFE-04
must_haves:
  truths:
    - "verify:phase48 会先运行 Phase 48 的 DAL/action/UI 行为测试，再做 Phase 44-47 级联回归，不再依赖 hollow static proof。"
    - "Phase 48 的 close gate 日志会明确证明 UI wiring、mounted/ready runnable、theme side-effect ordering 与 preflight/uninstall parity 已被验证。"
  artifacts:
    - path: scripts/verify-phase48-lifecycle-and-uninstall.ts
      provides: behavior-first phase 48 close gate
    - path: package.json
      provides: explicit verify script chain for phases 44-48
  key_links:
    - from: package.json
      to: scripts/verify-phase48-lifecycle-and-uninstall.ts
      via: verify:phase48 script entry
      pattern: verify:phase48
    - from: scripts/verify-phase48-lifecycle-and-uninstall.ts
      to: src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx
      via: phase 48 UI wiring proof
      pattern: plugin-lifecycle-operator-surface.test.tsx
---

<objective>
把 Phase 48 的 close gate 从“旧摘要声称已通过”改成真实可运行的行为证明链，并修复当前 proof chain 因缺 verify script entry 造成的不可信问题。

Purpose: 48-01 关于 `verify:phase48 已通过` 的结论不可信；本计划只负责把 verifier 收口成可信 gate，不再混入新的 DAL/UI 产品改动。
Output: behavior-first `verify:phase48` 与完整的 Phase 44-48 verify script entry。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/48-lifecycle-and-uninstall-semantics/48-01-SUMMARY.md
@.planning/phases/48-lifecycle-and-uninstall-semantics/48-VERIFICATION.md
@.planning/phases/48-lifecycle-and-uninstall-semantics/48-02-PLAN.md
@scripts/verify-phase48-lifecycle-and-uninstall.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 把 verify:phase48 改成行为化 close gate，并补齐 verify script chain</name>
  <files>scripts/verify-phase48-lifecycle-and-uninstall.ts, package.json</files>
  <read_first>.planning/phases/48-lifecycle-and-uninstall-semantics/48-01-SUMMARY.md, .planning/phases/48-lifecycle-and-uninstall-semantics/48-VERIFICATION.md, .planning/phases/48-lifecycle-and-uninstall-semantics/48-02-PLAN.md, scripts/verify-phase48-lifecycle-and-uninstall.ts, package.json</read_first>
  <action>重写 `scripts/verify-phase48-lifecycle-and-uninstall.ts` 的 close gate 顺序，使其先验证本 phase 的关键行为，再做前序回归：1) 保留必要静态检查，但只用于 entrypoint / action seam 存在性，不再让“源码包含固定字符串”充当主要证据；2) 显式执行 `src/lib/dal/plugins.test.ts`、`src/actions/plugin-actions.test.ts`、`src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx`、`src/components/surfaces/plugin-marketplace-surface.test.tsx`、`src/components/surfaces/settings-surface.test.tsx`；3) 更新最终日志，明确输出 UI wiring、mounted/ready runnable、theme ordering、preflight/uninstall parity 已验证；4) 在 `package.json` 中补齐缺失的 `verify:phase44`、`verify:phase45`、`verify:phase46`、`verify:phase47` script entry（若当前仓库未声明），让 `verify:phase48` 的级联回归链与既有 verifier exact-script 要求一致。不要再把 `48-01-SUMMARY.md` 中“verify:phase48 已通过”的旧结论当作证据来源。</action>
  <acceptance_criteria>
    - `pnpm run verify:phase48` 会执行至少一组 DAL/action 行为测试与一组 UI wiring 测试，而不是只做 hollow static scan。
    - `package.json` 中存在 Phase 44-48 所需的 verify script entry，避免 Phase 48 verifier 因缺脚本声明而假失败。
    - verifier 日志会明确说明本次关闭的是哪些 gap：UI wiring、runnable states、theme ordering、uninstall block parity。
  </acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase48</automated>
  </verify>
  <done>Phase 48 close gate 重新具备行为证明价值；48-01 关于 verifier 已通过的旧摘要被正式替换。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier script → test chain | close gate 若没有真实执行行为测试，会把旧摘要或静态字符串误当成已完成事实。 |
| package.json script entry → upstream verifiers | Phase 48 级联回归依赖 Phase 44-47 exact script names，缺 entry 会造成假失败。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-03-01 | R | `scripts/verify-phase48-lifecycle-and-uninstall.ts` | mitigate | 用显式测试执行和结果日志替代静态字符串 proof，避免 verifier 误报已验证。 |
| T-48-03-02 | D | `package.json` verify chain | mitigate | 补齐 Phase 44-47 verify script entry，防止 cascade regression 因脚本缺失而中断。 |
</threat_model>

<verification>
- `pnpm run verify:phase48`
</verification>

<success_criteria>
- `verify:phase48` 会真实执行 Phase 48 的 DAL/action/UI 行为测试。
- Phase 44-48 的 verify script chain 完整可调用，不再因脚本名缺失导致假失败。
- close gate 日志能明确证明本次 gap closure 覆盖了哪些关键行为。
</success_criteria>

<output>
After completion, create `.planning/phases/48-lifecycle-and-uninstall-semantics/48-03-SUMMARY.md`
</output>
