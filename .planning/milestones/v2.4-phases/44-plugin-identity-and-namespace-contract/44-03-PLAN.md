---
phase: 44-plugin-identity-and-namespace-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 44-01
  - 44-02
files_modified:
  - src/lib/dto/resource-ai.ts
  - src/lib/dal/plugins.ts
  - src/server/plugins/registry.ts
  - scripts/bootstrap-dev-db.ts
  - src/lib/dal/plugins.builtins.test.ts
  - scripts/bootstrap-dev-db.test.ts
autonomous: true
requirements:
  - PLUG-04
must_haves:
  truths:
    - "默认插件会通过与普通插件一致的正式安装/reconcile 路径进入学校插件注册表，而不是继续走 built-in 特例写入。"
    - "默认插件修复或重跑 bootstrap 后仍保留既有 `pluginId` 连续性，不会让历史 lesson payload 失效。"
    - "built-in 行为解析依赖正式 canonical key，而不是依赖展示名是否刚好匹配。"
  artifacts:
    - path: "scripts/bootstrap-dev-db.ts"
      provides: "default plugin bootstrap 改走共享 reconcile path"
    - path: "src/server/plugins/registry.ts"
      provides: "pluginKey-based built-in definition lookup"
    - path: "src/lib/dto/resource-ai.ts"
      provides: "built-in definition carries canonical pluginKey mapping"
  key_links:
    - from: "scripts/bootstrap-dev-db.ts"
      to: "src/lib/dal/plugins.ts"
      via: "bootstrap imports shared install/reconcile helper"
      pattern: "installOrReconcilePlugin"
    - from: "src/lib/dal/plugins.ts"
      to: "src/server/plugins/registry.ts"
      via: "runPluginHook injects pluginKey while registry resolves built-ins by pluginKey"
      pattern: "pluginKey"
---

<objective>
把默认插件 seed / reconcile 与 built-in dispatch 一起收口到正式 identity contract，彻底拔掉 `plugin.name` / `payload.pluginName` 的 canonical 角色。

Purpose: PLUG-04 的真正风险不在“能不能 seed”，而在于既要统一安装模型，又不能打断已持久化 lesson payload 对 `pluginId` 的依赖。
Output: built-in/default plugin shared reconcile path、pluginKey-based built-in registry lookup、default-plugin bootstrap regression tests。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/44-plugin-identity-and-namespace-contract/44-CONTEXT.md
@.planning/phases/44-plugin-identity-and-namespace-contract/44-RESEARCH.md
@.planning/phases/44-plugin-identity-and-namespace-contract/44-PATTERNS.md
@src/lib/dto/resource-ai.ts
@src/lib/dal/plugins.ts
@src/server/plugins/registry.ts
@scripts/bootstrap-dev-db.ts
@src/lib/dal/lesson-authoring.ts

<interfaces>
From `src/lib/dal/lesson-authoring.ts`:
```ts
const builtInSource = parsedStep.payload.builtInSource;
if (!pluginAvailability.get(builtInSource.pluginId)) {
  blockingIssues.push({
    code: "BUILT_IN_PLUGIN_UNAVAILABLE",
    pluginId: builtInSource.pluginId,
    builtInKey: builtInSource.builtInKey,
    pluginName: builtInSource.pluginName,
  });
}
```

From `src/server/plugins/registry.ts`:
```ts
const BUILT_IN_TEACHING_STEP_BY_NAME = new Map(
  BUILT_IN_TEACHING_STEP_DEFINITIONS.map((definition) => [definition.pluginName, definition] as const),
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Run GitNexus impact preflight for bootstrap and built-in dispatch symbols</name>
  <files>scripts/bootstrap-dev-db.ts, src/server/plugins/registry.ts, src/lib/dal/plugins.ts, src/lib/dal/plugins.builtins.test.ts, scripts/bootstrap-dev-db.test.ts</files>
  <read_first>
    @AGENTS.md
    @scripts/bootstrap-dev-db.ts
    @src/server/plugins/registry.ts
    @src/lib/dal/plugins.ts
  </read_first>
  <action>在编辑任何符号前，先按项目 GitNexus 约束对本计划会修改的主符号执行 impact analysis，并把 blast radius 记录到 summary：至少覆盖 bootstrap 中的 built-in reconcile 主流程、`resolveBuiltInTeachingStep`、以及 `runPluginHook` 的 built-in dispatch 路径。如果任一结果为 HIGH 或 CRITICAL，执行器必须先停下并汇报风险。完成全部改动后，再运行 `gitnexus_detect_changes()`，确认受影响执行流仍聚焦默认插件 reconcile 与 built-in registry。</action>
  <acceptance_criteria>
    - 执行 summary 记录了 bootstrap reconcile 主流程、`resolveBuiltInTeachingStep`、`runPluginHook` 的 GitNexus impact 结果。
    - 若存在 HIGH 或 CRITICAL risk，summary 记录了暂停与升级处理。
    - 执行 summary 记录了 `gitnexus_detect_changes()` 输出。
  </acceptance_criteria>
  <verify>
    <manual>Read `.planning/phases/44-plugin-identity-and-namespace-contract/44-03-SUMMARY.md` and confirm GitNexus impact + detect_changes were logged.</manual>
  </verify>
  <done>执行器在改 bootstrap / registry 之前完成了 blast-radius 预分析，并在收尾时校验了影响范围。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Move default plugin bootstrap onto the shared reconcile path</name>
  <files>scripts/bootstrap-dev-db.ts, scripts/bootstrap-dev-db.test.ts, src/lib/dal/plugins.ts</files>
  <read_first>
    @.planning/phases/44-plugin-identity-and-namespace-contract/44-PATTERNS.md
    @scripts/bootstrap-dev-db.ts
    @scripts/bootstrap-dev-db.test.ts
    @src/lib/dal/plugins.ts
    @src/lib/dal/lesson-authoring.ts
  </read_first>
  <behavior>
    - Test 1: built-in/default plugin seeding imports and calls the shared install/reconcile helper instead of matching existing rows by `pluginRegistrations.name`.
    - Test 2: bootstrap keeps update-in-place posture so the same pluginKey reuses the existing `pluginId`.
    - Test 3: reconcile passes `installSource: "bootstrap"` for built-ins and does not silently re-enable plugins that operators already disabled.
  </behavior>
  <action>按 D-44-02、D-44-13、D-44-14、D-44-15、D-44-16，把 `scripts/bootstrap-dev-db.ts` 中 `upsertBuiltInPlugins()` 与 `upsertDevThemePlugin()` 的按 `name` 查询/insert-update 逻辑改为调用 Plan 02 交付的统一 install/reconcile seam，显式传 `installSource: "bootstrap"`（built-in/default）或 `installSource: "seed"`（dev theme sample）。这里必须显式复用 `44-PATTERNS.md` 已锁定的 bootstrap analog：沿用现有 `scripts/bootstrap-dev-db.ts` 的幂等 seed 结构，但把 identity truth 收口到共享 helper，并把 **update-in-place, never delete+insert** 写成硬约束，因为 `src/lib/dal/lesson-authoring.ts` 仍依赖历史 `builtInSource.pluginId`。built-in/default plugin 首次缺失时可插入，已有同 `pluginKey` 记录时必须 reconcile 到同一 `id`；如果已有记录是 operator 手动停用状态，bootstrap 只能更新 manifest/identity metadata，不得强行重置 `enabled`。同步把 `scripts/bootstrap-dev-db.test.ts` 改成断言：源码不再包含 `eq(pluginRegistrations.name, definition.name)`；脚本显式使用共享 reconcile helper；built-in/dev theme definitions 继续存在并保持幂等 seed posture。</action>
  <acceptance_criteria>
    - `scripts/bootstrap-dev-db.ts` 不再包含 `eq(pluginRegistrations.name, definition.name)`。
    - `scripts/bootstrap-dev-db.ts` 包含 `installSource: "bootstrap"` 或 `installSource: "seed"` 的显式调用。
    - `scripts/bootstrap-dev-db.test.ts` 断言脚本已改走共享 helper。
    - `src/lib/dal/plugins.ts` 中的 reconcile 逻辑没有 delete+insert 默认插件记录的分支。
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest --run scripts/bootstrap-dev-db.test.ts</automated>
  </verify>
  <done>默认插件 bootstrap/repair 已统一走正式安装模型，并保持既有 `pluginId` 稳定。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve built-in teaching definitions by pluginKey, not pluginName</name>
  <files>src/lib/dto/resource-ai.ts, src/lib/dal/plugins.ts, src/server/plugins/registry.ts, src/lib/dal/plugins.builtins.test.ts</files>
  <read_first>
    @.planning/phases/44-plugin-identity-and-namespace-contract/44-PATTERNS.md
    @src/lib/dto/resource-ai.ts
    @src/lib/dal/plugins.ts
    @src/server/plugins/registry.ts
    @src/lib/dal/plugins.builtins.test.ts
  </read_first>
  <behavior>
    - Test 1: built-in registry can resolve template/suggestion definitions from a canonical `pluginKey` without reading `payload.pluginName`.
    - Test 2: `pluginName` still survives in returned payloads as display text.
    - Test 3: `runPluginHook()` injects `pluginKey` for built-in dispatch so empty payload template requests still resolve correctly.
  </behavior>
  <action>按 D-44-01、D-44-03、D-44-04，在 `src/lib/dto/resource-ai.ts` 为 built-in teaching definitions 增加正式 `pluginKey` 映射（值必须与 bootstrap 中的 built-in `manifest.id` 一致），不要再让 definition registry 只认识展示名。在 `src/server/plugins/registry.ts` 删除 `BUILT_IN_TEACHING_STEP_BY_NAME` 作为 canonical lookup，改成优先按 `payload.pluginKey` 解析，必要时可兼容 `payload.builtInKey` 作为辅助，但不能再依赖 `payload.pluginName` 决定 definition。这里必须显式复用 `44-PATTERNS.md` 已锁定的 registry analog：`pluginName` 只保留为 display-only 文案，不再承担 canonical lookup 角色。在 `src/lib/dal/plugins.ts` 的 built-in `runPluginHook()` 输入组装里，为 built-in action payload 注入 `pluginKey: plugin.pluginKey`，保留 `pluginName: plugin.name` 仅做显示文案。同步更新 `src/lib/dal/plugins.builtins.test.ts`：fixture/plugin mock 必须含 `pluginKey` 与 `dbNamespace`；断言 dispatch 结果继续返回 `pluginName` 展示文案，但测试不再需要把 `pluginName` 作为 lookup 前提。</action>
  <acceptance_criteria>
    - `src/server/plugins/registry.ts` 不再包含 `BUILT_IN_TEACHING_STEP_BY_NAME`。
    - `src/server/plugins/registry.ts` 的 `resolveBuiltInTeachingStep` 读取 `pluginKey` 或 `builtInKey`，不读取 `payload.pluginName` 做 canonical lookup。
    - `src/lib/dal/plugins.ts` 在 built-in dispatch payload 中注入 `pluginKey: plugin.pluginKey`。
    - `src/lib/dal/plugins.builtins.test.ts` 包含 `pluginKey` fixture，并继续断言模板返回 `pluginName` 作为展示值。
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest --run src/lib/dal/plugins.builtins.test.ts scripts/bootstrap-dev-db.test.ts</automated>
  </verify>
  <done>默认插件写入与 built-in dispatch 都已摆脱 `plugin.name` 的 canonical 身份角色。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bootstrap script → plugin installation truth | 系统 seed 进入正式学校插件注册表，必须遵守同一身份与生命周期规则 |
| plugin hook payload → built-in registry lookup | built-in action dispatch 不能再把展示名当成系统真相 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-10 | T | `scripts/bootstrap-dev-db.ts` | mitigate | bootstrap 改走共享 install/reconcile seam，禁止 name-based private upsert |
| T-44-11 | R | default plugin recovery path | mitigate | `installSource` 显式记录为 `bootstrap` / `seed`，保留统一 lifecycle/audit 轨迹 |
| T-44-12 | S | `src/server/plugins/registry.ts` | mitigate | registry 通过 `pluginKey`/`builtInKey` 解析 built-in definition，移除 `pluginName` canonical role |
| T-44-13 | D | lesson publish readiness | mitigate | reconcile 保持 update-in-place，避免 `pluginId` 变化导致历史 lesson payload 全部误报 unavailable |
</threat_model>

<verification>
- `pnpm exec vitest --run src/lib/dal/plugins.builtins.test.ts scripts/bootstrap-dev-db.test.ts`
</verification>

<success_criteria>
- 默认插件不再走 built-in 特例 upsert。
- built-in definition lookup 不再依赖 `pluginName`。
- 既有 lesson payload 的 `pluginId` 连续性被保住。
</success_criteria>

<output>
After completion, create `.planning/phases/44-plugin-identity-and-namespace-contract/44-03-SUMMARY.md`
</output>
