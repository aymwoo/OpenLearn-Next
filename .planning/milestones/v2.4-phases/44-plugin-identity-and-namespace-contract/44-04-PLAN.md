---
phase: 44-plugin-identity-and-namespace-contract
plan: 04
type: execute
wave: 4
depends_on:
  - 44-01
  - 44-02
  - 44-03
files_modified:
  - src/components/surfaces/settings-surface.tsx
  - src/components/surfaces/plugin-marketplace-surface.tsx
  - src/components/surfaces/settings-surface.test.tsx
  - src/components/surfaces/plugin-marketplace-surface.test.tsx
  - scripts/verify-phase44-plugin-identity.ts
  - package.json
autonomous: true
requirements:
  - PLUG-01
  - PLUG-02
  - PLUG-04
must_haves:
  truths:
    - "平台维护者至少在 settings/labs 和 built-in marketplace 中能直接看到 `pluginKey`、`dbNamespace` 与来源信息。"
    - "UI 展示与 verifier 都围绕正式 SQL truth，而不是继续展示 `manifestJson.id` 充当二级真相。"
    - "维护者可以通过单一 Phase 44 verifier 快速确认 schema、DAL、bootstrap、registry 和 operator surfaces 已收口到同一正式 contract。"
  artifacts:
    - path: "src/components/surfaces/settings-surface.tsx"
      provides: "settings labs formal plugin metadata rendering"
    - path: "src/components/surfaces/plugin-marketplace-surface.tsx"
      provides: "built-in marketplace formal identity/namespace metadata"
    - path: "src/components/surfaces/plugin-marketplace-surface.test.tsx"
      provides: "marketplace surface metadata rendering regression"
    - path: "scripts/verify-phase44-plugin-identity.ts"
      provides: "canonical Phase 44 verifier"
    - path: "package.json"
      provides: "verify:phase44 script entry"
  key_links:
    - from: "src/components/surfaces/settings-surface.tsx"
      to: "src/actions/plugin-actions.ts"
      via: "listPluginsAction returns DTO fields rendered directly in badges/meta copy"
      pattern: "pluginKey|dbNamespace|sourceType|installSource"
    - from: "scripts/verify-phase44-plugin-identity.ts"
      to: "package.json"
      via: "verify:phase44 canonical gate"
      pattern: "verify:phase44"
---

<objective>
把正式 identity/namespace contract 暴露给维护者界面，并建立一个 Phase 44 的 canonical verifier，防止后续代码回退到 `manifestJson.id` 或 `pluginName` 旧路径。

Purpose: goal 要求“平台维护者直接看到正式字段”，所以本阶段不能只改后端；同时需要独立 close gate 把 schema、DAL、bootstrap、registry、UI 一起锁住。
Output: settings/marketplace 正式元数据展示、UI regression、`verify:phase44` 脚本。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/44-plugin-identity-and-namespace-contract/44-CONTEXT.md
@.planning/phases/44-plugin-identity-and-namespace-contract/44-RESEARCH.md
@.planning/phases/44-plugin-identity-and-namespace-contract/44-PATTERNS.md
@src/components/surfaces/settings-surface.tsx
@src/components/surfaces/plugin-marketplace-surface.tsx
@src/components/surfaces/settings-surface.test.tsx
@scripts/verify-phase43-validation-workloads.ts
@package.json
@DESIGN.md

<interfaces>
From `src/components/surfaces/settings-surface.tsx`:
```tsx
<Badge className="bg-surface-container-low text-on-surface-variant">
  {plugin.manifestJson.id}
</Badge>
```

From `package.json`:
```json
"verify:phase43": "node --import tsx scripts/verify-phase43-validation-workloads.ts"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Run GitNexus impact preflight for operator surfaces and verifier entrypoints</name>
  <files>src/components/surfaces/settings-surface.tsx, src/components/surfaces/plugin-marketplace-surface.tsx, scripts/verify-phase44-plugin-identity.ts, package.json</files>
  <read_first>
    @AGENTS.md
    @src/components/surfaces/settings-surface.tsx
    @src/components/surfaces/plugin-marketplace-surface.tsx
    @package.json
  </read_first>
  <action>在编辑任何符号前，先按项目 GitNexus 约束对本计划会修改的主符号执行 impact analysis，并把 blast radius 记录到 summary：至少覆盖 settings surface 的主导出组件、plugin marketplace surface 的主导出组件，以及 package script / verifier 入口。如果任一结果为 HIGH 或 CRITICAL，执行器必须先停下并汇报风险。完成全部改动后，再运行 `gitnexus_detect_changes()`，确认影响范围仍聚焦 operator surfaces 与 Phase 44 verifier。</action>
  <acceptance_criteria>
    - 执行 summary 记录了 settings surface、plugin marketplace surface、verifier entrypoint 的 GitNexus impact 结果。
    - 若存在 HIGH 或 CRITICAL risk，summary 记录了暂停与升级处理。
    - 执行 summary 记录了 `gitnexus_detect_changes()` 输出。
  </acceptance_criteria>
  <verify>
    <manual>Read `.planning/phases/44-plugin-identity-and-namespace-contract/44-04-SUMMARY.md` and confirm GitNexus impact + detect_changes were logged.</manual>
  </verify>
  <done>执行器在改 operator surfaces / verifier 前完成了 blast-radius 预分析，并在收尾时校验了影响范围。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Render formal plugin identity metadata in operator-facing surfaces</name>
  <files>src/components/surfaces/settings-surface.tsx, src/components/surfaces/plugin-marketplace-surface.tsx, src/components/surfaces/settings-surface.test.tsx, src/components/surfaces/plugin-marketplace-surface.test.tsx</files>
  <read_first>
    @.planning/phases/44-plugin-identity-and-namespace-contract/44-PATTERNS.md
    @src/components/surfaces/settings-surface.tsx
    @src/components/surfaces/plugin-marketplace-surface.tsx
    @src/components/surfaces/settings-surface.test.tsx
    @src/components/surfaces/plugin-marketplace-surface.test.tsx
    @src/lib/dto/resource-ai.ts
    @DESIGN.md
  </read_first>
  <behavior>
    - Test 1: settings labs plugin cards show `pluginKey`, `dbNamespace`, and source/install metadata from DTO fields instead of `manifestJson.id` badges.
    - Test 2: plugin marketplace keeps built-in/default/non-deletable product copy, while secondary metadata now comes from formal fields.
    - Test 3: existing toggle forms and visual rhythm remain intact.
  </behavior>
  <action>按 D-44-03、D-44-04、D-44-05、D-44-07、D-44-16 与 RESEARCH 的最小 UI 建议，更新 `src/components/surfaces/settings-surface.tsx` 和 `src/components/surfaces/plugin-marketplace-surface.tsx`：保留 `teacherSurfaceRhythm`、`StageHero`、内置插件“默认开启、可停用、不可删除”文案，并显式遵守 `DESIGN.md` 的 Lexend、no-line、tonal surfaces 约束，只在既有 surface rhythm 内补 metadata，不改视觉系统结构。这里必须显式复用 `44-PATTERNS.md` 已锁定的 UI analog：沿用 settings / marketplace 现有 tonal/no-line card rhythm，只替换 metadata 来源为正式 DTO 字段。把 metadata badge/说明从 `plugin.manifestJson.id` 改成正式 `plugin.pluginKey`、`plugin.dbNamespace`、`plugin.sourceType`、`plugin.installSource`。至少在 settings labs 卡片上完整展示四项字段；marketplace 可精简为 `pluginKey + dbNamespace + sourceType`，但不能再把 `manifestJson.id` 当作正式 identity badge。同步更新 `src/components/surfaces/settings-surface.test.tsx` 与新增 `src/components/surfaces/plugin-marketplace-surface.test.tsx` 的 mock DTO 和渲染断言：settings 断言完整四项字段可见；marketplace 断言 built-in 卡片真实渲染 `pluginKey`、`dbNamespace`、`sourceType`，同时保留“默认开启、可停用、不可删除”的既有产品语义。不要修改 toggle form 的 `pluginId/schoolId/enabled` 提交结构。</action>
  <acceptance_criteria>
    - `settings-surface.tsx` 与 `plugin-marketplace-surface.tsx` 不再渲染 `plugin.manifestJson.id` 作为插件身份 badge。
    - `settings-surface.test.tsx` mock DTO 含 `pluginKey`、`dbNamespace`、`sourceType`、`installSource`，并断言页面能看到至少 `pluginKey` 与 `dbNamespace`。
    - `plugin-marketplace-surface.test.tsx` 断言 built-in marketplace 卡片真实显示 `pluginKey`、`dbNamespace`、`sourceType`，并保留默认插件产品文案。
    - plugin toggle form 仍提交 `pluginId`、`schoolId`、`enabled`。
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest --run src/components/surfaces/settings-surface.test.tsx src/components/surfaces/plugin-marketplace-surface.test.tsx</automated>
  </verify>
  <done>维护者界面已直接消费正式 identity/namespace truth，而不是继续借 `manifestJson.id` 解释插件是谁。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the canonical Phase 44 verifier and script entry</name>
  <files>scripts/verify-phase44-plugin-identity.ts, package.json</files>
  <read_first>
    @.planning/phases/44-plugin-identity-and-namespace-contract/44-PATTERNS.md
    @scripts/verify-phase43-validation-workloads.ts
    @package.json
    @src/db/schema.ts
    @src/lib/dal/plugins.ts
    @src/server/plugins/registry.ts
    @scripts/bootstrap-dev-db.ts
  </read_first>
  <behavior>
    - Test 1: `verify:phase44` exists as a single canonical package script.
    - Test 2: verifier fails if SQLite runtime schema/index truth drifts from the Phase 44 contract after migration/apply.
    - Test 3: verifier static checks fail if schema/DAL/bootstrap/registry/UI regress to `manifestJson.id`, `plugin.name` identity, missing unique constraints, or namespace parity drift.
    - Test 4: verifier runs focused Phase 44 suites so closeout can be executed with one command.
  </behavior>
  <action>创建 `scripts/verify-phase44-plugin-identity.ts`，沿用 `scripts/verify-phase43-validation-workloads.ts` 的结构：实现 `read()`、`nonCommentIncludes()`、`run()`、`runVitest()` helper。这里必须显式复用 `44-PATTERNS.md` 已锁定的 verifier analog，也就是沿用 `scripts/verify-phase43-validation-workloads.ts` 的 phase verifier 脚本骨架，而不是临时拼一个一次性检查脚本。verifier 必须把 SQLite runtime schema/index 校验纳入单一 close gate：对迁移后的 SQLite（标准 dev DB 或临时库）执行 `PRAGMA table_info("pluginRegistration")` 与 `sqlite_master` 索引检查，确认 `pluginKey`、`dbNamespace`、`sourceType`、`installSource` 四列和 `pluginRegistration_school_pluginKey_unique`、`pluginRegistration_school_dbNamespace_unique` 两个 unique index 都真实存在。静态检查至少覆盖：1) `src/db/schema.ts` 存在 `pluginKey`、`dbNamespace`、`sourceType`、`installSource` 与学校范围唯一索引；2) `src/lib/dal/plugins.ts` 存在 `installOrReconcilePlugin`、`deriveDbNamespace`、`PLUGIN_KEY_CONFLICT`、`PLUGIN_DB_NAMESPACE_CONFLICT`、`PLUGIN_DB_NAMESPACE_FROZEN`；3) `scripts/bootstrap-dev-db.ts` 不再按 `pluginRegistrations.name` upsert；4) `src/server/plugins/registry.ts` 不再按 `pluginName` canonical lookup；5) `src/components/surfaces/settings-surface.tsx` 与 `plugin-marketplace-surface.tsx` 不再用 `manifestJson.id` 做正式 badge。并新增一个 namespace parity gate：verifier 必须构造临时 SQLite 场景或等价 contract corpus，逐例比较 Phase 44 migration/backfill 产出的 `dbNamespace` 与 `deriveDbNamespace()` 对供应商前缀、连字符、连续分隔符、首字符非字母、超长 key 的输出，失败即退出非零。然后串最小 focused suites：`src/lib/dal/plugins.test.ts`、`src/actions/plugin-actions.test.ts`、`src/lib/dal/plugins.builtins.test.ts`、`scripts/bootstrap-dev-db.test.ts`、`src/components/surfaces/settings-surface.test.tsx`、`src/components/surfaces/plugin-marketplace-surface.test.tsx`。最后在 `package.json` 注册唯一脚本：`"verify:phase44": "node --import tsx scripts/verify-phase44-plugin-identity.ts"`。</action>
  <acceptance_criteria>
    - `package.json` 包含精确脚本 `"verify:phase44": "node --import tsx scripts/verify-phase44-plugin-identity.ts"`。
    - `scripts/verify-phase44-plugin-identity.ts` 同时包含 SQLite runtime schema/index gate、`staticChecks`、`runVitest(`、namespace parity gate 与对 schema/DAL/bootstrap/registry/UI 的静态断言。
    - verifier 动态测试列表包含 `src/lib/dal/plugins.test.ts`、`src/actions/plugin-actions.test.ts`、`src/lib/dal/plugins.builtins.test.ts`、`scripts/bootstrap-dev-db.test.ts`、`src/components/surfaces/settings-surface.test.tsx`、`src/components/surfaces/plugin-marketplace-surface.test.tsx`。
    - `verify:phase44` 对 `pluginRegistration` 运行时表结构执行 `PRAGMA table_info` 与 `sqlite_master` 索引检查，验证 4 个正式列和 2 个 unique index 都存在。
  </acceptance_criteria>
  <verify>
    <automated>node --import tsx scripts/verify-phase44-plugin-identity.ts</automated>
  </verify>
  <done>`verify:phase44` 成为本阶段唯一 canonical close gate，能一把锁住 identity/namespace contract 不回退。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DAL DTO → operator-facing UI | 页面只能展示正式字段，不能重新解析 JSON 形成第二套 identity |
| verifier script → repository truth | close gate 必须检查真实源码 token 与 focused suites，而不是凭肉眼验收 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-14 | I | settings / marketplace surfaces | mitigate | UI 直接展示正式 `pluginKey/dbNamespace/sourceType/installSource`，杜绝继续误导性展示 `manifestJson.id` |
| T-44-15 | T | verifier drift | mitigate | `verify:phase44` 对 schema/DAL/bootstrap/registry/UI 做静态断言并串 focused suites |
| T-44-16 | R | phase close proof | mitigate | package script 提供单一 canonical gate，执行结果可复现、可记录 |
</threat_model>

<verification>
- `pnpm exec vitest --run src/components/surfaces/settings-surface.test.tsx`
- `node --import tsx scripts/verify-phase44-plugin-identity.ts`
</verification>

<success_criteria>
- settings / marketplace 至少一个管理面直接显示正式 plugin identity/namespace 元数据。
- `verify:phase44` 可一键回归 Phase 44 contract。
- 页面、脚本和测试都不再把 `manifestJson.id` / `pluginName` 当成 canonical identity。
</success_criteria>

<output>
After completion, create `.planning/phases/44-plugin-identity-and-namespace-contract/44-04-SUMMARY.md`
</output>
