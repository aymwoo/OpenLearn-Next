---
phase: 45-extension-and-plugin-owned-schema-patterns
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - src/lib/dal/plugin-data.test.ts
  - scripts/verify-phase45-plugin-schema.ts
autonomous: true
gap_closure: true
requirements: [EXT-04, OWN-03]
must_haves:
  truths:
    - "Deleting a pluginRegistration in a real SQLite database removes rows from plugin_ext_lesson, plugin_ext_lesson_step, plugin_ext_resource, and plugin_owned_business_data."
    - "Deleting lesson, lessonStep, or resource parent rows in a real SQLite database removes only the matching extension rows while preserving unrelated plugin-owned rows."
    - "verify:phase45 fails if the real cascade behavior proof fails, even when schema metadata still exists."
  artifacts:
    - path: "src/lib/dal/plugin-data.test.ts"
      provides: "Real SQLite cascade regression coverage for plugin-owned and extension tables"
      min_lines: 140
    - path: "scripts/verify-phase45-plugin-schema.ts"
      provides: "Behavior-first close gate with real delete/assert cascade proof"
      contains: "PRAGMA foreign_key_check"
  key_links:
    - from: "scripts/verify-phase45-plugin-schema.ts"
      to: "src/lib/dal/plugin-data.test.ts"
      via: "runVitest([\"src/lib/dal/plugin-data.test.ts\"], ...)"
      pattern: "runVitest\\(\\[\"src/lib/dal/plugin-data.test.ts\"\\]"
    - from: "scripts/verify-phase45-plugin-schema.ts"
      to: "temporary SQLite proof database"
      via: "createClient + delete/assert + PRAGMA foreign_key_check"
      pattern: "foreign_key_check"
---

<objective>
Close the only failed Phase 45 verification gap by converting the cascade proof from metadata-only checks into executable SQLite delete regression.

Purpose: make Phase 45's close gate prove physical cascade behavior, not just table/index existence.
Output: strengthened cascade regression tests and a behavior-first `verify:phase45` script.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-extension-and-plugin-owned-schema-patterns/SPEC.md
@.planning/phases/45-extension-and-plugin-owned-schema-patterns/45-VERIFICATION.md
@.planning/phases/45-extension-and-plugin-owned-schema-patterns/45-01-SUMMARY.md
@src/lib/dal/plugin-data.test.ts
@scripts/verify-phase45-plugin-schema.ts

<interfaces>
From `src/features/platform-core/events/ledger.test.ts`:
```ts
const client = createClient({ url: databaseUrl });
await client.execute("PRAGMA foreign_keys = ON");
```

From `scripts/verify-phase45-plugin-schema.ts`:
```ts
function runVitest(paths: readonly string[], label: string): void
async function runVerification()
```

From `src/lib/dal/plugin-data.ts`:
```ts
export async function upsertPluginExtension(input: UpsertExtensionInput): Promise<void>
export async function getPluginExtension(input: GetExtensionInput): Promise<Record<string, any> | null>
export async function upsertPluginOwnedBusinessData(input: UpsertOwnedBusinessDataInput): Promise<void>
export async function getPluginOwnedBusinessData(input: GetOwnedBusinessDataInput): Promise<Record<string, any> | null>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add real SQLite cascade regression coverage</name>
  <files>src/lib/dal/plugin-data.test.ts</files>
  <read_first>
    - src/lib/dal/plugin-data.test.ts
    - src/features/platform-core/events/ledger.test.ts
    - src/db/schema.ts
    - .planning/phases/45-extension-and-plugin-owned-schema-patterns/45-VERIFICATION.md
  </read_first>
  <action>Replace the current cascade string-count proof in `src/lib/dal/plugin-data.test.ts` with executable SQLite behavior tests per the failed gap in `45-VERIFICATION.md`. Keep the existing mocked DAL authz/idempotency coverage, but add a separate real-database describe block that creates a throwaway SQLite file via `createClient`, enables `PRAGMA foreign_keys = ON`, bootstraps the minimal parent tables (`school`, `pluginRegistration`, `course`, `lesson`, `lessonStep`, `resource`) plus the four Phase 45 plugin tables with the same cascade edges as production, inserts fixture rows, and proves all required delete paths: deleting `pluginRegistration` clears all four plugin tables; deleting `lesson` clears `plugin_ext_lesson`; deleting `lessonStep` clears `plugin_ext_lesson_step`; deleting `resource` clears `plugin_ext_resource`; deleting a core entity does not remove unrelated `plugin_owned_business_data`, which only disappears on plugin delete. Use deterministic row-count assertions and clean up the temp DB in teardown. Implement this exactly to satisfy EXT-04 and OWN-03; do not leave the primary proof as a static schema token count.</action>
  <acceptance_criteria>
    - `src/lib/dal/plugin-data.test.ts` contains `createClient` and `PRAGMA foreign_keys = ON`.
    - The file has explicit tests for plugin-registration delete, lesson delete, lessonStep delete, and resource delete.
    - At least one assertion proves `plugin_owned_business_data` survives a core-entity delete and reaches zero after plugin delete.
    - Existing mocked DAL guardrail tests remain in the file.
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs --run src/lib/dal/plugin-data.test.ts</automated>
  </verify>
  <done>Focused Vitest coverage now proves physical SQLite cascade behavior instead of inferring it from schema text.</done>
</task>

<task type="auto">
  <name>Task 2: Make verify:phase45 behavior-first</name>
  <files>scripts/verify-phase45-plugin-schema.ts</files>
  <read_first>
    - scripts/verify-phase45-plugin-schema.ts
    - src/lib/dal/plugin-data.test.ts
    - .planning/phases/45-extension-and-plugin-owned-schema-patterns/45-VERIFICATION.md
    - .planning/phases/45-extension-and-plugin-owned-schema-patterns/SPEC.md
  </read_first>
  <action>Upgrade `scripts/verify-phase45-plugin-schema.ts` so `verify:phase45` proves the failed truth in `45-VERIFICATION.md` before reporting success. Add a dedicated runtime behavior step that uses a throwaway SQLite database, enables foreign keys, creates the minimal parent/plugin tables, seeds sample rows, executes the same delete flows required by UAT-45-04, asserts post-delete row counts for each table, and runs `PRAGMA foreign_key_check` to confirm zero remaining violations. Keep the existing physical table/index inspection as supporting evidence, not the primary gate. Preserve the current `runVitest(["src/lib/dal/plugin-data.test.ts"], ...)` call and the chained Phase 44 regression, but make the script exit non-zero if the inline behavior proof, the focused test suite, or the Phase 44 verifier fails. Use a temp database file for the behavior proof so the verifier never mutates `local.db` or the developer's canonical workspace data.</action>
  <acceptance_criteria>
    - `scripts/verify-phase45-plugin-schema.ts` contains a real cascade behavior-proof step using a temporary SQLite database.
    - The script checks delete/assert coverage for `pluginRegistration`, `lesson`, `lessonStep`, and `resource` paths.
    - The script runs `PRAGMA foreign_key_check` or an equivalent zero-violation assertion on the temp database.
    - The success banner only prints after behavior proof, focused Vitest, and Phase 44 regression all pass.
  </acceptance_criteria>
  <verify>
    <automated>node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase45-plugin-schema.ts</automated>
  </verify>
  <done>`verify:phase45` can no longer pass on metadata-only proof; it proves real cascade behavior end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier script → temporary SQLite database | destructive delete assertions must stay inside an isolated throwaway database |
| test harness → production schema assumptions | regression code must mirror the real FK graph instead of inventing a weaker local schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02-01 | T | `scripts/verify-phase45-plugin-schema.ts` | mitigate | Add inline delete/assert behavior proof plus `PRAGMA foreign_key_check` so stale metadata cannot masquerade as cascade proof. |
| T-45-02-02 | D | temp SQLite regression harness | mitigate | Use throwaway DB files under temp storage and clean them up in teardown/finally so repeated verifier runs do not accumulate state or lock files. |
| T-45-02-03 | E | verifier database target selection | mitigate | Hardcode the behavior proof to a dedicated temporary database instead of `DB_FILE_NAME`/`local.db`, preventing destructive delete checks from touching authoritative dev data. |
</threat_model>

<verification>
- `node ./node_modules/vitest/vitest.mjs --run src/lib/dal/plugin-data.test.ts`
- `node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase45-plugin-schema.ts`
</verification>

<success_criteria>
- Phase 45 verification truth 5 becomes passable with real SQLite delete proof.
- The gap in `.planning/phases/45-extension-and-plugin-owned-schema-patterns/45-VERIFICATION.md` is fully addressed without adding new scope.
- Phase 48's uninstall assumption that cleanup relies on cascade delete is now backed by executable Phase 45 proof instead of metadata-only inference.
</success_criteria>

<output>
After completion, create `.planning/phases/45-extension-and-plugin-owned-schema-patterns/45-02-SUMMARY.md`
</output>
