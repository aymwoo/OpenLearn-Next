---
phase: 40-bullmq-infra-seam-and-worker-reliability-posture
plan: 03
type: execute
wave: 3
depends_on:
  - 40-01
  - 40-02
files_modified:
  - src/features/async-tasks/server/registry.ts
  - src/features/async-tasks/shared/idempotency.ts
  - src/features/async-tasks/server/enqueue.ts
  - src/features/async-tasks/worker/registry.ts
  - src/features/async-tasks/worker/processors/platform-healthcheck.ts
  - src/features/async-tasks/worker/bootstrap.ts
  - scripts/verify-phase40-bullmq-runtime.ts
  - package.json
autonomous: true
requirements:
  - ATP-06
  - ATP-07
  - ATP-10
must_haves:
  truths:
    - Each task type can declare retry, backoff, dead-letter, and idempotency posture through the shared platform contract.
    - Worker execution still flows through DAL/typed helpers and cache invalidation discipline.
    - Phase 40 has a dedicated verification gate proving runtime loop, reliability helpers, and process boundary discipline.
  artifacts:
    - path: src/features/async-tasks/shared/idempotency.ts
      provides: Stable job identity and deduplication helpers
    - path: src/features/async-tasks/worker/processors/platform-healthcheck.ts
      provides: Minimal platform verification processor
    - path: scripts/verify-phase40-bullmq-runtime.ts
      provides: Phase-specific verifier for worker/runtime/reliability boundaries
  key_links:
    - from: src/features/async-tasks/server/enqueue.ts
      to: src/features/async-tasks/shared/idempotency.ts
      via: enqueue seam derives stable job identity and dedupe posture
      pattern: buildAsyncTaskJobId
    - from: src/features/async-tasks/worker/processors/platform-healthcheck.ts
      to: src/features/async-tasks/worker/registry.ts
      via: minimal platform processor is registered through worker registry
      pattern: platform.healthcheck
---

<objective>
收敛 Phase 40 的 reliability layer：retry/backoff/idempotency helper、minimal platform processor、以及 dedicated `verify:phase40` gate。

Purpose: 把“BullMQ 能跑起来”升级为“平台 reliability 语义已经被代码和验证锁住”，为 Phase 41 真 workload 接入扫清边界不确定性。
Output: reliability metadata/assist helpers、minimal worker processor、focused runtime verifier。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-bullmq-infra-seam-and-worker-reliability-posture/40-CONTEXT.md
@.planning/phases/40-bullmq-infra-seam-and-worker-reliability-posture/40-RESEARCH.md
@.planning/phases/40-bullmq-infra-seam-and-worker-reliability-posture/40-PATTERNS.md
@.planning/phases/40-bullmq-infra-seam-and-worker-reliability-posture/40-01-PLAN.md
@.planning/phases/40-bullmq-infra-seam-and-worker-reliability-posture/40-02-PLAN.md

<interfaces>
From Context7 BullMQ docs:
```ts
await queue.add('job', data, {
  attempts: 5,
  backoff: { type: 'exponential', delay: 1000 },
  jobId: 'stable-id',
});
```

From src/features/async-tasks/server/registry.ts:
```ts
reliability: {
  queueName: "platform-health",
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 registry reliability metadata，并新增 stable job identity helper</name>
  <files>src/features/async-tasks/server/registry.ts, src/features/async-tasks/shared/idempotency.ts, src/features/async-tasks/server/enqueue.ts, src/features/async-tasks/worker/registry.ts</files>
  <read_first>src/features/async-tasks/server/registry.ts, src/features/async-tasks/server/enqueue.ts, .planning/phases/40-bullmq-infra-seam-and-worker-reliability-posture/40-RESEARCH.md, .planning/research/PITFALLS.md</read_first>
  <behavior>
    - Test 1: task definitions can declare retry/backoff/idempotency metadata explicitly through the shared registry contract.
    - Test 2: enqueue seam derives stable BullMQ job identity from helper code instead of ad hoc string concatenation.
    - Test 3: duplicate dispatch attempts for the same durable task resolve to the same stable job identity or dedupe posture.
  </behavior>
  <action>扩展 `src/features/async-tasks/server/registry.ts` 中的 reliability metadata，让 task definition 至少能表达 `queueName`、`attempts`、`backoff`、`deadLetter` 或 terminal failure posture、以及 idempotency strategy slot。新增 `src/features/async-tasks/shared/idempotency.ts`，收口 stable `jobId` / deduplication key helper；Phase 40 先以 durable task id 或 registry-driven helper 作为平台级稳定 identity，不要求提前完成每个真实 workload 的业务幂等。更新 `src/features/async-tasks/server/enqueue.ts` 与 `worker/registry.ts`，使 enqueue 与 worker 都消费同一套 reliability metadata。先写 focused tests，再实现直到通过。</action>
  <acceptance_criteria>
    - Registry reliability metadata explicitly includes retry/backoff and idempotency-related fields.
    - `src/features/async-tasks/shared/idempotency.ts` exists and exports stable identity helpers.
    - Enqueue source no longer hard-codes queue runtime policy outside the shared helper path.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/features/async-tasks/server/registry.reliability.test.ts src/features/async-tasks/shared/idempotency.test.ts src/features/async-tasks/server/enqueue.runtime.test.ts</automated>
  </verify>
  <done>ATP-06/07 的平台入口已固定：后续 workload 只需补业务级细节，不需要再定义新的 reliability contract。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 增加 minimal platform processor 与 verify:phase40 gate</name>
  <files>src/features/async-tasks/worker/processors/platform-healthcheck.ts, src/features/async-tasks/worker/registry.ts, src/features/async-tasks/worker/bootstrap.ts, scripts/verify-phase40-bullmq-runtime.ts, package.json</files>
  <read_first>src/features/async-tasks/server/registry.ts, src/features/async-tasks/worker/registry.ts, scripts/verify-phase37-redis-fanout.ts, scripts/verify-phase39-async-tasks.ts, .planning/phases/39-async-contracts-and-durable-task-truth/39-03-SUMMARY.md</read_first>
  <behavior>
    - Test 1: a minimal platform healthcheck task can enqueue, execute in the worker, and project terminal status durably without introducing a real product workload.
    - Test 2: worker processor routes through typed helper/DAL-facing platform APIs rather than bypassing them.
    - Test 3: dedicated verifier fails if `server.ts` starts worker code, if actions/DAL import `bullmq` directly, or if worker/runtime proof files are missing.
  </behavior>
  <action>创建 `src/features/async-tasks/worker/processors/platform-healthcheck.ts` 作为最小 platform verification processor：它可以做 noop/health summary，但必须真实经过 queue enqueue、worker execution、progress/result writeback 和 terminal projection。更新 `worker/registry.ts` 与 bootstrap 让该 processor 被正式注册。再创建 `scripts/verify-phase40-bullmq-runtime.ts`，沿用仓库 verifier 结构，先做 static source guards，再跑 focused suites；静态 guard 至少检查 dedicated worker entry 存在、`server.ts` 不导入 worker、actions/DAL 不直接 import `bullmq`、QueueEvents projector 存在、graceful shutdown token 存在、reliability/idempotency helpers 存在。在 `package.json` 注册 `verify:phase40`。不要把 Phase 41 的真实 workload smoke 混进此 verifier。</action>
  <acceptance_criteria>
    - Minimal platform processor exists and is registered in the worker registry.
    - `scripts/verify-phase40-bullmq-runtime.ts` exists and is registered as `verify:phase40`.
    - Verifier covers process boundary, projector presence, direct BullMQ import discipline, and focused runtime suites.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/features/async-tasks/worker/processors/platform-healthcheck.test.ts src/features/async-tasks/worker/bootstrap.test.ts && pnpm verify:phase40</automated>
  </verify>
  <done>Phase 40 的 runtime/reliability 交付声明已经可执行验证，Phase 41 可以在稳定平台边界上接真实 workload。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| task definition → runtime policy | retry/backoff/idempotency 需要从 shared registry metadata 进入运行时，而不是散落硬编码 |
| worker processor → DAL/cache discipline | processor 执行不能绕过 server-side discipline 或失去 cache invalidation 责任 |
| verifier → source tree | phase gate 必须及时阻断 process boundary drift 与 reliability helper 缺失 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-07 | T | `src/features/async-tasks/server/registry.ts` | mitigate | 把 retry/backoff/idempotency 定义收敛到 shared metadata，避免任务各自漂移 |
| T-40-08 | I | `src/features/async-tasks/worker/processors/platform-healthcheck.ts` | mitigate | processor 通过 typed helper 与 durable writeback 路径运行，不裸写数据库 |
| T-40-09 | R | `scripts/verify-phase40-bullmq-runtime.ts` | mitigate | dedicated verifier 记录静态边界检查与 focused runtime suite 结果 |
</threat_model>

<verification>
- `pnpm test --run src/features/async-tasks/server/registry.reliability.test.ts src/features/async-tasks/shared/idempotency.test.ts src/features/async-tasks/server/enqueue.runtime.test.ts`
- `pnpm test --run src/features/async-tasks/worker/processors/platform-healthcheck.test.ts src/features/async-tasks/worker/bootstrap.test.ts`
- `pnpm verify:phase40`
</verification>

<success_criteria>
- retry/backoff/idempotency 的平台 contract 已被显式化。
- 最小平台 processor 证明 worker loop 与 durable projection 闭环可运行。
- `verify:phase40` 成为 Phase 40 的单一交付 gate。
</success_criteria>

<output>
After completion, create `.planning/phases/40-bullmq-infra-seam-and-worker-reliability-posture/40-03-SUMMARY.md`
</output>
