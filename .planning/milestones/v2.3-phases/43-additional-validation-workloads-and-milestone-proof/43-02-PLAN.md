---
phase: 43-additional-validation-workloads-and-milestone-proof
plan: 02
type: execute
wave: 2
depends_on:
  - 43-01
files_modified:
  - src/db/schema.ts
  - scripts/prepare-dev-db.ts
  - drizzle/0008_phase43_validation_workloads.sql
  - drizzle/meta/0008_snapshot.json
  - drizzle/meta/_journal.json
  - src/features/async-tasks/server/registry.ts
  - src/features/async-tasks/worker/registry.ts
  - src/features/async-tasks/worker/processors/classroom-session-summary.ts
  - src/features/async-tasks/worker/processors/classroom-session-summary.test.ts
  - src/lib/dal/classroom.ts
  - src/lib/dal/classroom.test.ts
autonomous: true
requirements:
  - ATP-21
must_haves:
  truths:
    - "`classroomEvents` 会触发增量与 finalize 两类 session summary async task，但这些任务只写 derived artifact，不改 classroom realtime canonical truth。"
    - "derived summary artifact 复用现有 recap vocabulary，但不把 recap 读模型升级成第二条主写链路。"
    - "classroom summary workload 仍共享 registry、enqueue seam、worker posture 与 operator visibility。"
  artifacts:
    - path: "src/features/async-tasks/worker/processors/classroom-session-summary.ts"
      provides: "classroom event derived summary processor"
    - path: "src/db/schema.ts"
      provides: "classroom event-derived artifact schema"
  key_links:
    - from: "src/lib/dal/classroom.ts"
      to: "src/features/async-tasks/server/enqueue.ts"
      via: "incremental/finalize summary enqueue hooks"
      pattern: "classroom.session_summary"
---

<objective>
把 classroom event post-processing 接到共享 async platform：实现 derived session summary、补齐 migration-first 路径，并锁死 derived-only write 边界。

Purpose: 落实 D-43-06 ~ D-43-09，证明平台除了 manual/scheduled 之外还能承载 derived workload，且不篡改 canonical classroom truth。
Output: classroom summary task family、必要 schema/migration、focused regressions。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-additional-validation-workloads-and-milestone-proof/43-CONTEXT.md
@.planning/phases/43-additional-validation-workloads-and-milestone-proof/43-RESEARCH.md
@.planning/phases/43-additional-validation-workloads-and-milestone-proof/43-PATTERNS.md
@src/db/schema.ts
@src/lib/dal/classroom.ts

<interfaces>
From `src/lib/dal/classroom.ts`:
```ts
export async function getClassroomSessionRecapDTO(rawInput: unknown) {
  if (session.status !== "ended") throw new Error("CLASSROOM_RECAP_NOT_AVAILABLE");
  const [lesson, clazz, published, participants, evidenceRows, timelineRows] = await Promise.all([...]);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为 classroom event summary 建立 derived-write task family 与 summary artifact</name>
  <read_first>
    - src/db/schema.ts
    - src/lib/dal/classroom.ts
    - src/features/async-tasks/server/registry.ts
    - src/features/async-tasks/worker/registry.ts
    - .planning/phases/43-additional-validation-workloads-and-milestone-proof/43-CONTEXT.md
  </read_first>
  <files>src/db/schema.ts, src/features/async-tasks/server/registry.ts, src/features/async-tasks/worker/registry.ts, src/features/async-tasks/worker/processors/classroom-session-summary.ts, src/features/async-tasks/worker/processors/classroom-session-summary.test.ts, src/lib/dal/classroom.ts, src/lib/dal/classroom.test.ts</files>
  <behavior>
    - Test 1: `active_step_changed` / `lock_mode_changed` / `slide_changed` 等关键事件会触发增量 summary task；`ended` 会触发 finalize summary task，符合 D-43-08。
    - Test 2: processor 只从 `classroomEvents`、evidence、timeline 读取事实，写 `classroomSessionSummary` derived artifact，不更新 `classroomSessions` / 不插入新的 canonical `classroomEvents`，符合 D-43-06 ~ D-43-09。
    - Test 3: recap 读取面能消费新 artifact，或至少校验 artifact 与现有 recap vocabulary 对齐。
  </behavior>
  <acceptance_criteria>
    - `src/db/schema.ts` 新增 session summary artifact 表，字段至少包含 `sessionId`、`lastEventVersion`、`summaryJson`、`status`、`finalizedAt`、`updatedAt`，且 `sessionId` 级唯一约束存在。
    - `src/features/async-tasks/server/registry.ts` 包含 `taskType: "classroom.session_summary"`、`featureArea: "runtime"`、`entityRefKind: "classroom_session"`。
    - `src/lib/dal/classroom.ts` 在 canonical event write 后调用 `enqueueAsyncTask(`，但 processor 与 helper 不包含 `update(classroomSessions)` 或新的 `insert(classroomEvents)`。
    - `src/features/async-tasks/worker/processors/classroom-session-summary.test.ts` / `src/lib/dal/classroom.test.ts` 覆盖 incremental/finalize 触发、derived-only write、防止改写 canonical truth。
  </acceptance_criteria>
  <action>按 D-43-06、D-43-07、D-43-08、D-43-09 新增 classroom derived workload。先在 `src/db/schema.ts` 定义 `classroomSessionSummary` 表：`id`、`sessionId`（FK 到 `classroomSessions.id`、唯一）、`schoolId`（FK 到 `schools.id`）、`status`（`processing|completed|failed`）、`triggerMode`（`incremental|finalize`）、`lastEventVersion`、`summaryJson`、`failureReason`、`createdAt`、`updatedAt`、`finalizedAt`；所有 FK 用 `onDelete: "cascade"`。在 `src/features/async-tasks/server/registry.ts` 增加 `classroom.session_summary` task family，`featureArea: "runtime"`、`visibilityScope: "school_operator"`、`entityRefKind: "classroom_session"`、`reliability.queueName = "classroom-summary"`、`attempts = 3`、`backoff.delay = 2_000`、`operatorRecovery.enabled = true`。在 `src/features/async-tasks/worker/registry.ts` 注册 `processClassroomSessionSummaryJob()`。创建 `src/features/async-tasks/worker/processors/classroom-session-summary.ts`，payload 固定包含 `sessionId`、`schoolId`、`triggerMode`、`eventType`、`eventVersion`，processor 只解析 payload、写 `job.updateProgress()`、调用 `executeClassroomSessionSummaryTask(payload)`。在 `src/lib/dal/classroom.ts` 抽出 summary builder/helper，复用 `getClassroomSessionRecapDTO()` 当前聚合 vocabulary 生成 summary artifact，并在 canonical event insert 成功后 enqueue：关键事件 `active_step_changed`、`lock_mode_changed`、`slide_changed` 触发 `triggerMode: "incremental"`，`ended` 触发 `triggerMode: "finalize"`。helper 明确禁止回写 `classroomSessions`、禁止新增 canonical `classroomEvents`；只 upsert `classroomSessionSummary` 与 async task result summary。focused tests 锁住 trigger points、derived-only writes、summary artifact 与 recap vocabulary 对齐。</action>
  <verify>
    <automated>pnpm exec vitest --run src/features/async-tasks/worker/processors/classroom-session-summary.test.ts src/lib/dal/classroom.test.ts</automated>
  </verify>
  <done>classroom event summary workload 已有独立 task family、derived artifact、incremental/finalize 双触发，且不改写 classroom realtime canonical truth。</done>
</task>

<task type="auto">
  <name>Task 2: 生成并应用 Phase 43 schema migration，再用本地 SQLite 验证 derived artifact 已落库</name>
  <read_first>
    - src/db/schema.ts
    - scripts/prepare-dev-db.ts
    - package.json
    - AGENTS.md
    - .planning/phases/39-async-contracts-and-durable-task-truth/39-02-PLAN.md
  </read_first>
  <files>src/db/schema.ts, scripts/prepare-dev-db.ts, drizzle/0008_phase43_validation_workloads.sql, drizzle/meta/0008_snapshot.json, drizzle/meta/_journal.json</files>
  <acceptance_criteria>
    - `drizzle/0008_phase43_validation_workloads.sql`、`drizzle/meta/0008_snapshot.json`、`drizzle/meta/_journal.json` 被更新，并反映 `classroomSessionSummary` 新表。
    - `scripts/prepare-dev-db.ts` 能识别新 schema，并在仅有 reminder claim schema 时返回 `0007_phase43_scheduled_reminder_dispatch_claim`、在包含 summary artifact 时返回 `0008_phase43_validation_workloads`。
    - `pnpm db:migrate` 退出 0，且本地 SQLite 能成功读取 `classroomSessionSummary`。
  </acceptance_criteria>
  <action>这是本 plan 的顺序闸门。完成 Task 1 中的 `src/db/schema.ts` 修改后，运行 `node_modules/.bin/drizzle-kit generate --name phase43_validation_workloads` 生成正式 migration SQL 与 metadata，提交生成的 `drizzle/0008_phase43_validation_workloads.sql`、`drizzle/meta/0008_snapshot.json`、`drizzle/meta/_journal.json`。随后更新 `scripts/prepare-dev-db.ts` 的 `detectExistingSchemaTag()`：若仓库只落了 reminder dispatch claim schema，应桥接到 `0007_phase43_scheduled_reminder_dispatch_claim`；若已包含 `classroomSessionSummary` 及其关键列，则桥接到 `0008_phase43_validation_workloads`，继续遵守 repo 的 migration-first bridge discipline。然后执行 `pnpm db:migrate` 应用 migration；若本地已有旧 schema，修正 migration metadata 或 detection 逻辑，但不要用 `drizzle-kit push` 替代。最后用非交互命令验证 live DB：`node --input-type=module --import tsx -e "import { db } from './src/db'; import { classroomSessionSummary } from './src/db/schema'; await db.select().from(classroomSessionSummary).limit(0);"`。本 plan 的最终 verification 依赖这里先通过。</action>
  <verify>
    <automated>node_modules/.bin/drizzle-kit generate --name phase43_validation_workloads && pnpm db:migrate && node --input-type=module --import tsx -e "import { db } from './src/db'; import { classroomSessionSummary } from './src/db/schema'; await db.select().from(classroomSessionSummary).limit(0);"</automated>
  </verify>
  <done>migration-first 路径已完成，live SQLite 与代码 schema 同步，后续 classroom verification 不再停留在 TypeScript-only contract 层。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical classroom writes → derived summary enqueue | 课堂主写路径产出的 `classroomEvents` 跨入异步 derived processor，必须防止反向污染 canonical truth |
| schema migration → live SQLite | 新 summary artifact schema 进入真实数据库，若 migration 漏跑会造成 verification 假通过 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-05 | T | `src/lib/dal/classroom.ts` enqueue hooks | mitigate | 只在 canonical event insert 成功后 enqueue summary task；processor 禁止 `update(classroomSessions)` / 新增 canonical `classroomEvents`，通过 focused tests 与 verifier static guard 锁死 derived-only write |
| T-43-06 | I | `classroomSessionSummary` artifact | mitigate | summary artifact 只保存 recap/aggregation 所需字段，不复制敏感 payload 原文；继续由 teacher session scope 控制 recap 读取 |
| T-43-09 | D | schema drift during verification | mitigate | Task 2 强制 `drizzle-kit generate` + `pnpm db:migrate` + live SQLite read probe 后再做最终 verification |
</threat_model>

<verification>
- `node_modules/.bin/drizzle-kit generate --name phase43_validation_workloads && pnpm db:migrate`
- `node --input-type=module --import tsx -e "import { db } from './src/db'; import { classroomSessionSummary } from './src/db/schema'; await db.select().from(classroomSessionSummary).limit(0);"`
- `pnpm exec vitest --run src/features/async-tasks/worker/processors/classroom-session-summary.test.ts src/lib/dal/classroom.test.ts`
- `pnpm typecheck`
</verification>

<success_criteria>
- classroom summary workload 采用 incremental + finalize 双触发，且只写 derived artifact。
- schema migration-first 路径真实跑通，verification 针对 live SQLite 而非纯静态合同。
- recap 领域语义继续由 classroom 边界拥有，summary artifact 只做 derived projection。
</success_criteria>

<output>
After completion, create `.planning/phases/43-additional-validation-workloads-and-milestone-proof/43-02-SUMMARY.md`
</output>
