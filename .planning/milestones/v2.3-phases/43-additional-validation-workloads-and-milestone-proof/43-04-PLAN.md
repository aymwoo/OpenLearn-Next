---
phase: 43-additional-validation-workloads-and-milestone-proof
plan: 04
type: execute
wave: 4
depends_on:
  - 43-01
  - 43-02
  - 43-03
files_modified:
  - package.json
  - scripts/verify-phase43-validation-workloads.ts
  - .planning/phases/43-additional-validation-workloads-and-milestone-proof/43-WORKLOAD-PROOF.md
  - src/features/async-tasks/server/registry.reliability.test.ts
  - src/lib/dal/async-task-operator.test.ts
  - src/components/surfaces/async-task-operator-surface.test.tsx
  - src/actions/async-task-operator-actions.test.ts
autonomous: true
requirements:
  - ATP-23
must_haves:
  truths:
    - "manual（batch import）、scheduled（reminder delivery）、derived（classroom session summary / resource knowledgeSource ingest）四类 workload 被同一 verifier 与 proof matrix 同时证明。"
    - "proof 不只证明 taskType 存在，还明确覆盖 registry、enqueue path、worker posture、durable truth、operator visibility、retry/backoff/idempotency posture、recovery/result semantics。"
    - "Phase 43 close artifact 可让后续阶段直接复用作为 milestone 级平台成立证据。"
  artifacts:
    - path: "scripts/verify-phase43-validation-workloads.ts"
      provides: "canonical Phase 43 verification gate"
    - path: ".planning/phases/43-additional-validation-workloads-and-milestone-proof/43-WORKLOAD-PROOF.md"
      provides: "human-readable workload coverage matrix"
  key_links:
    - from: "scripts/verify-phase43-validation-workloads.ts"
      to: "src/features/async-tasks/server/registry.ts"
      via: "staticChecks on four real workloads plus platform healthcheck"
      pattern: "verify:phase43"
    - from: "scripts/verify-phase43-validation-workloads.ts"
      to: "src/lib/dal/async-task-operator.test.ts"
      via: "operator-facing regression slices"
      pattern: "operator visibility / retry eligibility"
---

<objective>
为 Phase 43 建立 milestone 级 close gate：用一个 verifier 和一个 proof matrix，同时证明 manual、scheduled、derived 三类 workload 都共享同一 async platform contract。

Purpose: 落实 D-43-14、D-43-15、D-43-16 与 ATP-23，避免 milestone close 只凭口头说明或 taskType token 过关。
Output: `verify:phase43` 脚本、operator-facing regression coverage、`43-WORKLOAD-PROOF.md`。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 Phase 43 verifier，并显式覆盖 operator-facing regression</name>
  <read_first>
    - scripts/verify-phase41-batch-import.ts
    - scripts/verify-phase42-operator-recovery.ts
    - package.json
    - src/features/async-tasks/server/registry.ts
    - src/lib/dal/async-task-operator.test.ts
    - src/components/surfaces/async-task-operator-surface.test.tsx
    - src/actions/async-task-operator-actions.test.ts
    - .planning/phases/43-additional-validation-workloads-and-milestone-proof/43-RESEARCH.md
  </read_first>
  <files>package.json, scripts/verify-phase43-validation-workloads.ts, src/lib/dal/async-task-operator.test.ts, src/components/surfaces/async-task-operator-surface.test.tsx, src/actions/async-task-operator-actions.test.ts</files>
  <behavior>
    - Test 1: verifier 以 `staticChecks[] + focused suites + prior phase regressions` 形式运行，并检查四类 workload family 的 registry / enqueue / worker / retry/backoff/idempotency / operator visibility/recovery tokens。
    - Test 2: verifier 会显式阻断 classroom summary processor 改写 canonical truth、teacher surface 出现 local retry drift、resource page 暴露 task-center drift。
    - Test 3: verifier 会显式检查新 workload families 仍可被 operator overview/detail/retry 现有读模型消费，而不是只假设 `school_operator` visibility 自动成立。
    - Test 4: `package.json` 新增唯一脚本 `verify:phase43`，命令固定为 `node --import tsx scripts/verify-phase43-validation-workloads.ts`。
  </behavior>
  <acceptance_criteria>
    - `package.json` 包含精确字符串 `"verify:phase43": "node --import tsx scripts/verify-phase43-validation-workloads.ts"`。
    - `scripts/verify-phase43-validation-workloads.ts` 包含 `staticChecks`、`runVitest(`、`verify-phase42-operator-recovery.ts`、`verify-phase41-batch-import.ts`、`src/lib/dal/async-task-operator.ts`、`src/components/surfaces/async-task-operator-surface.test.tsx`、`src/actions/async-task-operator-actions.test.ts`、以及对新 workload family label/summary/retryEligibility 的 operator-facing 断言。
    - verifier 至少静态检查五个 task family：`platform.healthcheck`、`course_import.apply_batch`、`schedule.reminder_delivery`、`classroom.session_summary`、`resource.knowledge_source_ingest` 的存在与关键 contract token，并对四个真实 workload family 显式断言 `reliability.queueName`、`reliability.attempts`、`reliability.backoff`、`reliability.idempotency`、`operatorRecovery`。
  </acceptance_criteria>
  <action>创建 `scripts/verify-phase43-validation-workloads.ts`，沿用 Phase 41/42 verifier 结构：定义 `StaticCheck`、`read()`、`nonCommentIncludes()`、`runVitest()`、`run()` helper。静态检查必须覆盖：1) `src/features/async-tasks/server/registry.ts` 同时包含 `course_import.apply_batch`、`schedule.reminder_delivery`、`classroom.session_summary`、`resource.knowledge_source_ingest`；2) reminder 侧不在 rule save 时直接创建 reminder delivery task，而是通过 system-owned due seam 在到点后调用 `enqueueAsyncTask(`，且源码里存在 dispatch claim/binding 语义；3) `src/lib/dal/classroom.ts` 触发 incremental/finalize enqueue，且 derived processor 源文件不含 `update(classroomSessions)` 或 `insert(classroomEvents)`；4) `src/lib/dal/ai-rag.ts` 以 `knowledgeSource` 为 entity identity；5) `src/components/surfaces/schedule-reminder-surface.tsx` 与 `src/components/surfaces/library-surface.tsx` 继续保持 business-entity-first，不暴露 task-center drift；6) `src/lib/dal/async-task-operator.ts`、`src/components/surfaces/async-task-operator-surface.test.tsx`、`src/actions/async-task-operator-actions.test.ts` 共同证明 operator overview/detail/retry 仍覆盖新的 workload family，而且至少要补一个 operator-facing regression：detail/overview fixture 中显式出现 `schedule.reminder_delivery`、`classroom.session_summary`、`resource.knowledge_source_ingest` 三类 task，并断言它们的 label/summary/retryEligibility 能被现有读模型正确消费；7) 对四个真实 workload family 的 registry reliability metadata 做显式断言，最少覆盖 `queueName`、`attempts`、`backoff`、`idempotency`、`operatorRecovery`，必要时同步更新 `src/features/async-tasks/server/registry.reliability.test.ts` 以符合 `43-PATTERNS.md`；8) operator recovery 依旧通过 Phase 42 surface/recovery service 提供，而不是 reminder/resource feature 页本地重试。动态验证先跑 Phase 43 focused suites（43-01、43-02、43-03 新增 tests，再加 operator-facing regression slices），再串联 `node --import tsx scripts/verify-phase42-operator-recovery.ts` 与 `node --import tsx scripts/verify-phase41-batch-import.ts`。最后在 `package.json` 注册 `verify:phase43`。</action>
  <verify>
    <automated>node --import tsx scripts/verify-phase43-validation-workloads.ts</automated>
  </verify>
  <done>`verify:phase43` 成为本 milestone 的 canonical close gate，且它会回归 batch import 与 operator recovery，不只验证新 workload 存在性。</done>
</task>

<task type="auto">
  <name>Task 2: 编写 workload proof / coverage matrix，逐项证明四类任务共享同一平台 contract</name>
  <read_first>
    - .planning/phases/43-additional-validation-workloads-and-milestone-proof/43-CONTEXT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - scripts/verify-phase43-validation-workloads.ts
    - .planning/phases/41-first-real-product-slice-batch-import-async-workflow/41-03-SUMMARY.md
    - .planning/phases/42-operator-visibility-and-recovery/42-03-SUMMARY.md
  </read_first>
  <files>.planning/phases/43-additional-validation-workloads-and-milestone-proof/43-WORKLOAD-PROOF.md</files>
  <acceptance_criteria>
    - 文档包含四个 workload family：batch import、reminder delivery、classroom session summary、resource knowledgeSource ingest。
    - 文档至少包含列：`workload family`、`trigger mode`、`business truth`、`taskType`、`enqueue path`、`worker posture`、`retry/backoff/idempotency`、`operator visibility`、`result semantics`。
    - 文档明确标注 manual / scheduled / derived 三种 workload 类型，并逐条回应 D-43-14、D-43-15、D-43-16 与 ATP-23。
  </acceptance_criteria>
  <action>创建 `.planning/phases/43-additional-validation-workloads-and-milestone-proof/43-WORKLOAD-PROOF.md`，作为本 phase 的主 close artifact。文档必须是人工可读矩阵，而不是纯日志转储：第一部分写 1 段 milestone proof 摘要，说明 v2.3 Async Task Platform 已由四类真实 workload 证明成立；第二部分给出 coverage matrix，按 workload family 行列出 `batch import (manual)`、`reminder delivery (scheduled)`、`classroom session summary (derived)`、`resource knowledgeSource ingest (derived)`，列至少覆盖 `shared registry entry`、`canonical enqueue seam`、`worker queue/processors`、`durable business truth`、`teacher-facing truth`、`retry/backoff/idempotency posture`、`operator visibility`、`recovery posture`、`result semantics`、`verifier evidence`；第三部分补充 anti-regression notes，明确“不重开 classroom realtime mainline”“不把 Redis/BullMQ 提升为 business truth”“教师页保持 business-entity-first” 这三条 guardrail 仍成立。矩阵里的证据项使用具体文件路径、taskType 与 reliability metadata 名称，不要写空泛叙述。</action>
  <verify>
    <automated>node --input-type=module --import tsx -e "import { readFileSync } from 'node:fs'; const text = readFileSync('.planning/phases/43-additional-validation-workloads-and-milestone-proof/43-WORKLOAD-PROOF.md', 'utf8'); const required = ['batch import', 'reminder delivery', 'classroom session summary', 'resource knowledgeSource ingest', 'workload family', 'trigger mode', 'business truth', 'taskType', 'enqueue path', 'worker posture', 'retry/backoff/idempotency', 'operator visibility', 'result semantics', 'classroom realtime mainline', 'Redis/BullMQ', 'business-entity-first']; const missing = required.filter((token) => !text.includes(token)); if (missing.length > 0) throw new Error('Missing proof tokens: ' + missing.join(', '));"</automated>
  </verify>
  <done>milestone proof matrix 已能独立说明四类 workload 共享同一 async platform contract，并把 manual / scheduled / derived 三种类型一起证明完毕。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier script → source-of-truth files | verifier 读取多个 phase/source 文件来做 close gate，必须避免只检查表面 token 造成错误结论 |
| proof matrix → milestone close consumer | 人工 proof 文档会被后续 phase / milestone close 复用，必须确保描述与代码、requirements、context 决策一致 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-10 | T | `scripts/verify-phase43-validation-workloads.ts` | mitigate | verifier 同时做 static checks、focused suites、operator-facing regression slices、prior-phase regression chain，避免只靠 taskType token 造成 coverage 伪证明 |
| T-43-11 | R | `43-WORKLOAD-PROOF.md` | mitigate | proof matrix 每个结论都绑定具体文件路径、taskType、verifier evidence，防止 milestone close 时无法追溯 |
| T-43-12 | I | proof artifact wording | accept | proof 文档只记录平台 contract 与 workload coverage，不写 secrets、payload 原文或 operator-only internal identifiers，信息披露风险低 |
</threat_model>

<verification>
- `node --import tsx scripts/verify-phase43-validation-workloads.ts`
- `node --input-type=module --import tsx -e ...` 文本断言校验 `43-WORKLOAD-PROOF.md` 包含四类 workload、矩阵列与 guardrail 文案
</verification>

<success_criteria>
- `verify:phase43` 成为 canonical gate，并回归 Phase 41 / Phase 42。
- `43-WORKLOAD-PROOF.md` 明确证明四类 workload 共享 registry、enqueue、worker、durable truth、retry/backoff/idempotency posture、operator visibility 与 recovery/result semantics。
- ATP-23 的 milestone proof 不再依赖口头说明，而有自动化 gate + 人工矩阵双重证据。
</success_criteria>

<output>
After completion, create `.planning/phases/43-additional-validation-workloads-and-milestone-proof/43-04-SUMMARY.md`
</output>
