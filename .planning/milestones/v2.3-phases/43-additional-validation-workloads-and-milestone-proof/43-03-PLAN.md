---
phase: 43-additional-validation-workloads-and-milestone-proof
plan: 03
type: execute
wave: 3
depends_on:
  - 43-01
  - 43-02
files_modified:
  - src/features/async-tasks/server/registry.ts
  - src/features/async-tasks/worker/registry.ts
  - src/features/async-tasks/worker/processors/resource-knowledge-source.ts
  - src/features/async-tasks/worker/processors/resource-knowledge-source.test.ts
  - src/lib/dal/ai-rag.ts
  - src/lib/dal/ai-rag.test.ts
  - src/actions/ai-rag-actions.ts
  - src/lib/dal/resources.ts
  - src/lib/dto/resource-ai.ts
  - src/components/surfaces/library-surface.tsx
  - src/components/surfaces/library-surface.test.tsx
  - src/features/schedule/reminders/actions.ts
  - src/components/surfaces/schedule-reminder-surface.tsx
  - src/components/surfaces/schedule-reminder-surface.test.tsx
autonomous: true
requirements:
  - ATP-20
  - ATP-22
must_haves:
  truths:
    - "每个 `knowledgeSource` 都会在 `ragEligible=true` 前提下进入同一 async platform，并把 `knowledgeSources.status` / `knowledgeChunks.indexingStatus` 作为教师可见 truth。"
    - "教师在 `/teacher/schedule/reminders` 继续看到规则与 delivery 列表，而不是通用任务中心，且 delivery 状态诚实映射 async truth。"
    - "resource processing 与 reminder teacher surface 都保持 business-entity-first；失败恢复继续收口到 operator 面。"
  artifacts:
    - path: "src/features/async-tasks/worker/processors/resource-knowledge-source.ts"
      provides: "knowledgeSource ingest/indexing processor"
    - path: "src/lib/dal/ai-rag.ts"
      provides: "以 knowledgeSource 为 identity 的 shared enqueue path"
    - path: "src/components/surfaces/library-surface.tsx"
      provides: "教师可见的 resource business status surface"
    - path: "src/components/surfaces/schedule-reminder-surface.tsx"
      provides: "教师可见的 reminder delivery honest status surface"
  key_links:
    - from: "src/lib/dal/ai-rag.ts"
      to: "src/features/async-tasks/server/enqueue.ts"
      via: "knowledgeSource task identity"
      pattern: "resource.knowledge_source_ingest"
    - from: "src/lib/dal/resources.ts"
      to: "src/components/surfaces/library-surface.tsx"
      via: "knowledgeSource business status DTO"
      pattern: "knowledgeSourceStatus"
    - from: "src/features/schedule/reminders/actions.ts"
      to: "src/components/surfaces/schedule-reminder-surface.tsx"
      via: "reminder center refresh + honest delivery DTO"
      pattern: "delivery.status"
---

<objective>
把 resource processing 与 reminder teacher surface 这两条剩余产品面接上：资源侧以 `knowledgeSource` 为 identity 跑 ingest/indexing，reminder 侧保持业务实体优先并去掉 feature-local recovery。

Purpose: 落实 D-43-04、D-43-05、D-43-10 ~ D-43-13，让教师真正消费 async 产生的业务 truth，而不是让 Phase 43 只停留在 operator/proof 层。
Output: resource knowledgeSource task family、teacher-visible resource status、teacher-visible reminder honest status、focused regressions。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 以 `knowledgeSource` 为 identity 落 resource ingest/indexing workload，并把业务状态映射回资源中心</name>
  <read_first>
    - src/lib/dal/ai-rag.ts
    - src/actions/ai-rag-actions.ts
    - src/lib/dal/resources.ts
    - src/lib/dto/resource-ai.ts
    - src/components/surfaces/library-surface.tsx
    - .planning/phases/43-additional-validation-workloads-and-milestone-proof/43-CONTEXT.md
  </read_first>
  <files>src/features/async-tasks/server/registry.ts, src/features/async-tasks/worker/registry.ts, src/features/async-tasks/worker/processors/resource-knowledge-source.ts, src/features/async-tasks/worker/processors/resource-knowledge-source.test.ts, src/lib/dal/ai-rag.ts, src/lib/dal/ai-rag.test.ts, src/actions/ai-rag-actions.ts, src/lib/dal/resources.ts, src/lib/dto/resource-ai.ts, src/components/surfaces/library-surface.tsx, src/components/surfaces/library-surface.test.tsx</files>
  <behavior>
    - Test 1: `registerKnowledgeSourceForResource()` 只允许 `ragEligible=true` 资源创建 source，并立即 enqueue `resource.knowledge_source_ingest` task，符合 D-43-10 ~ D-43-13。
    - Test 2: processor 会把 `knowledgeSources.status` 从 `pending -> processing -> completed/failed` 推进，并为 chunk metadata 写入 `knowledgeChunks.indexingStatus`，teacher-facing truth 仍在业务表。
    - Test 3: 资源中心显示 knowledge source 的业务状态 badge/说明，不显示通用 async task table；失败细节仍由 operator 面查看。
  </behavior>
  <acceptance_criteria>
    - `src/features/async-tasks/server/registry.ts` 包含 `taskType: "resource.knowledge_source_ingest"`、`featureArea: "resource_processing"`、`entityRefKind: "knowledge_source"`。
    - `src/lib/dal/ai-rag.ts` 包含 `enqueueAsyncTask(` 且 payload/entityRef 以 `knowledgeSource.id` 为唯一 task identity。
    - `src/components/surfaces/library-surface.tsx` 出现基于 `knowledgeSourceStatus` 或等价字段的业务状态展示，不出现 `taskId`、`queueJobId`、operator retry 按钮。
    - `src/lib/dal/ai-rag.test.ts` 与 `src/components/surfaces/library-surface.test.tsx` 作为本 task 新增的 focused test files 被一并纳入 canonical verification。
  </acceptance_criteria>
  <action>按 D-43-10、D-43-11、D-43-12、D-43-13 实现 resource processing。`src/features/async-tasks/server/registry.ts` 新增 `resource.knowledge_source_ingest` task family，固定 `featureArea: "resource_processing"`、`visibilityScope: "school_operator"`、`entityRefKind: "knowledge_source"`、`labelKey: "asyncTasks.resource.knowledgeSourceIngest.label"`、`summaryKey: "asyncTasks.resource.knowledgeSourceIngest.summary"`、`reliability.queueName = "resource-processing"`、`attempts = 3`、`backoff.delay = 2_000`、`operatorRecovery.enabled = true`。在 `src/lib/dal/ai-rag.ts` 改造 `registerKnowledgeSourceForResource()`：成功插入 `knowledgeSources(status: "pending")` 后立即调用 `enqueueAsyncTask()`，其中 `entityRef.entityType = "knowledge_source"`、`entityRef.entityId = source.id`、payload 至少包含 `knowledgeSourceId`、`resourceId`、`schoolId`、`actorId`。创建 `src/features/async-tasks/worker/processors/resource-knowledge-source.ts` 与执行 helper：开始时把 `knowledgeSources.status` 更新为 `processing`，从 `resources.content` 或 `resources.url` 构造 source text / metadata，按稳定 chunking 策略写入 `knowledgeChunks`，随后把每个 chunk 的 `indexingStatus` 更新为 `indexed`；失败时把 `knowledgeSources.status = "failed"` 并写 `error`。扩展 `src/lib/dto/resource-ai.ts` 与 `src/lib/dal/resources.ts`，在 `ResourceCardDTO` 中加入 `knowledgeSourceStatus`、`knowledgeSourceError`、`indexedChunkCount`、`failedChunkCount` 或等价业务字段；`src/components/surfaces/library-surface.tsx` 用 badge/辅助文案展示“RAG 处理中 / 已完成 / 处理失败”，但不要暴露任务中心字段。直接新建 `src/lib/dal/ai-rag.test.ts`、`src/components/surfaces/library-surface.test.tsx` 与 `src/features/async-tasks/worker/processors/resource-knowledge-source.test.ts` 这三组 focused tests，锁住 ragEligible 守卫、status progression、teacher-facing business truth。</action>
  <verify>
    <automated>pnpm exec vitest --run src/features/async-tasks/worker/processors/resource-knowledge-source.test.ts src/lib/dal/ai-rag.test.ts src/components/surfaces/library-surface.test.tsx</automated>
  </verify>
  <done>`knowledgeSource` 已成为 resource processing 的唯一 async identity，资源中心读取的是业务状态而不是队列内部状态。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 保持教师 reminder surface 业务实体优先，并把 delivery 状态诚实映射为 async truth</name>
  <read_first>
    - src/features/schedule/reminders/actions.ts
    - src/components/surfaces/schedule-reminder-surface.tsx
    - src/features/schedule/shared/dto/reminders.ts
    - .planning/phases/43-additional-validation-workloads-and-milestone-proof/43-CONTEXT.md
    - .planning/phases/42-operator-visibility-and-recovery/42-03-SUMMARY.md
  </read_first>
  <files>src/features/schedule/reminders/actions.ts, src/components/surfaces/schedule-reminder-surface.tsx, src/components/surfaces/schedule-reminder-surface.test.tsx</files>
  <behavior>
    - Test 1: `/teacher/schedule/reminders` 继续展示两类 rule + 最近 deliveries，不出现通用 async task 列表或 feature-local retry orchestration，符合 D-43-04。
    - Test 2: delivery 卡片能显示 queued/running/retrying 对应的 honest 业务状态文案，而不是只认 `planned/sent/failed/retry_required` 四态。
    - Test 3: 当 delivery 失败时，教师页显示“需 operator 处理/查看最近状态”之类业务文案，不再出现本地“重试”按钮，符合 D-43-05。
  </behavior>
  <acceptance_criteria>
    - `src/components/surfaces/schedule-reminder-surface.tsx` 不再包含 `retryScheduleReminderDispatchAction` 或按钮文案 `重试`。
    - 页面仍包含 `开课前提醒`、`调课变更提醒`、`delivery.targetLabel`，且文案中出现 operator-only 恢复提示。
    - `src/components/surfaces/schedule-reminder-surface.test.tsx` 断言 business-entity-first layout 仍存在，并覆盖 async honesty copy。
  </acceptance_criteria>
  <action>按 D-43-04、D-43-05 改造 teacher surface，但不把页面升级成任务中心。`src/features/schedule/reminders/actions.ts` 只保留 `saveScheduleReminderRuleAction()` 与 `refreshScheduleReminderCenterAction()`；移除教师本地 retry action 导出，避免 feature 页继续 orchestration recovery。`src/components/surfaces/schedule-reminder-surface.tsx` 保留 `rules + deliveries` 结构与现有 hero/copy，继续渲染 `delivery.targetLabel`、`channel`、`scheduledFor`，但改用 reminder server 提供的 honest delivery status 映射：把 async queued/running/retrying 显示为诸如“排队中 / 正在投递 / 正在恢复”，`failed` / `retry_required` 显示为“投递失败，需 operator 处理”，删除教师页 `重试` 按钮与任何 feature-local recovery CTA。补 `src/components/surfaces/schedule-reminder-surface.test.tsx`，锁住规则卡片、delivery 列表、operator-only 恢复文案，以及“不是通用任务中心”的 DOM 结构。</action>
  <verify>
    <automated>pnpm exec vitest --run src/components/surfaces/schedule-reminder-surface.test.tsx</automated>
  </verify>
  <done>教师 reminder 页仍然以规则和 delivery 为主；用户能看到诚实的投递状态；失败恢复已完全回收到 operator 面。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resource action → async ingest worker | 教师对资源的 `ragEligible` 与 knowledgeSource 注册跨入后台 processing，必须限制输入与 school scope |
| teacher page → reminder/resource business DTO | 前端只能消费 DAL/DTO truth，不能把队列内部状态直接显示成产品真相 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-07 | E | `registerKnowledgeSourceForResource()` | mitigate | 保留 `assertActiveTeacher()` + school scope + `ragEligible` gate；只有 teacher scope 内资源允许创建 knowledgeSource task |
| T-43-08 | T | resource ingest processor | mitigate | 以 `knowledgeSource` 为唯一 identity，worker 只推进 `knowledgeSources.status` / `knowledgeChunks.indexingStatus`；禁止直接伪造 operator-visible 完成态 |
| T-43-09 | R | teacher business surfaces | mitigate | reminder/resource 页面只显示业务字段与 honest 文案，不显示 `taskId` / `queueJobId` / 本地 recovery CTA，避免用户把 orchestration 状态误认成业务结论 |
</threat_model>

<verification>
- `pnpm exec vitest --run src/features/async-tasks/worker/processors/resource-knowledge-source.test.ts src/lib/dal/ai-rag.test.ts src/components/surfaces/library-surface.test.tsx src/components/surfaces/schedule-reminder-surface.test.tsx`
- `pnpm typecheck`
</verification>

<success_criteria>
- 每个 `knowledgeSource` 都能通过 shared async platform 完成 ingest/indexing，并把结果回写业务状态。
- 资源中心与 reminder 中心都继续以业务实体为主，不出现 feature-local task center drift。
- failed reminder delivery 与 failed resource ingest 的恢复动作继续统一收口到 operator 面。
</success_criteria>

<output>
After completion, create `.planning/phases/43-additional-validation-workloads-and-milestone-proof/43-03-SUMMARY.md`
</output>
