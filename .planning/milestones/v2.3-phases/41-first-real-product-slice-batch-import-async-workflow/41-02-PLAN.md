---
phase: 41-first-real-product-slice-batch-import-async-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - 41-01
files_modified:
  - src/lib/dto/course-authoring.ts
  - src/lib/dal/course-authoring.ts
  - src/lib/dto/course-import.ts
  - src/lib/dal/course-import.ts
  - src/components/surfaces/course-import-review-surface.tsx
  - src/components/surfaces/teacher-course-center-surface.tsx
autonomous: true
requirements:
  - ATP-12
  - ATP-13
  - ATP-14
must_haves:
  truths:
    - "batch detail page 继续是 review / queued / running / retrying / completed / partially completed / failed 的单一事实页面（D-41-02, D-41-05）。"
    - "课程中心顶部出现轻量最近导入任务卡，完成后仍保留最近一次结果摘要并可回到批次详情（D-41-06, D-41-07, D-41-08）。"
    - "partial success 文案、信息层级、失败引导都保持 honest posture：先汇总计数，再逐行明细，不做就地重跑（D-41-09, D-41-10, D-41-11, D-41-12）。"
  artifacts:
    - path: src/lib/dal/course-import.ts
      provides: batch DTO with latest async task summary
    - path: src/lib/dal/course-authoring.ts
      provides: teacher course center DTO with recent import task card data
    - path: src/components/surfaces/course-import-review-surface.tsx
      provides: unified async review/result surface
    - path: src/components/surfaces/teacher-course-center-surface.tsx
      provides: recent import task hero card
  key_links:
    - from: src/lib/dal/course-import.ts
      to: src/lib/dal/async-tasks.ts
      via: batch detail joins latest entity-scoped async task
      pattern: getEntityAsyncTaskListDTO
    - from: src/lib/dal/course-authoring.ts
      to: src/lib/dal/async-tasks.ts
      via: actor-scoped recent import task card data
      pattern: getActorAsyncTaskListDTO
    - from: src/components/surfaces/teacher-course-center-surface.tsx
      to: src/components/surfaces/course-import-review-surface.tsx
      via: link back to batch detail page
      pattern: /teacher/courses/import/
---

<objective>
把 async batch import 变成 teacher/staff 真正可见、可解释的产品面：批次详情页承载完整运行与结果，课程中心顶部承载轻量最近任务入口。

Purpose: 完成用户可见价值，确保 queued/running/partial/failure 都是 honest UI，而不是 infra-only capability。
Output: 扩展后的 batch/course-center DTO 与两处产品 surface。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-first-real-product-slice-batch-import-async-workflow/41-CONTEXT.md
@.planning/phases/41-first-real-product-slice-batch-import-async-workflow/41-RESEARCH.md
@.planning/phases/41-first-real-product-slice-batch-import-async-workflow/41-UI-SPEC.md
@.planning/phases/41-first-real-product-slice-batch-import-async-workflow/41-01-PLAN.md
@src/lib/dal/course-authoring.ts
@src/lib/dal/course-import.ts
@src/components/surfaces/course-import-review-surface.tsx
@src/components/surfaces/teacher-course-center-surface.tsx

<interfaces>
From `src/lib/dto/course-authoring.ts`:
```ts
export const TeacherCourseCenterDTOSchema = z.object({
  includeArchived: z.boolean().default(false),
  defaultSchoolId: z.string().nullable(),
  availableSchools: z.array(TeacherCourseScopeSchoolDTOSchema).default([]),
  courses: z.array(TeacherCourseCardDTOSchema),
});
```

From `src/lib/dto/course-import.ts`:
```ts
export const CourseImportBatchDTOSchema = z.object({
  id: z.string(),
  status: CourseImportBatchStatusSchema,
  applySummary: CourseImportApplySummarySchema,
  rows: z.array(CourseImportRowReviewDTOSchema),
});
```

From `src/lib/dal/async-tasks.ts`:
```ts
export async function getActorAsyncTaskListDTO(...): Promise<AsyncTaskListItemDTO[]>;
export async function getEntityAsyncTaskListDTO(...): Promise<AsyncTaskListItemDTO[]>;
export async function getAsyncTaskDetailDTO(...): Promise<AsyncTaskDetailDTO>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 batch detail 与 course center DTO 为 async-aware read model</name>
  <files>src/lib/dto/course-authoring.ts, src/lib/dal/course-authoring.ts, src/lib/dto/course-import.ts, src/lib/dal/course-import.ts</files>
  <read_first>src/lib/dto/course-authoring.ts, src/lib/dal/course-authoring.ts, src/lib/dto/course-import.ts, src/lib/dal/course-import.ts, src/lib/dal/async-tasks.ts, .planning/phases/41-first-real-product-slice-batch-import-async-workflow/41-UI-SPEC.md</read_first>
  <action>扩展 `CourseImportBatchDTO`，加入当前 batch `latestAsyncTask` / `asyncTaskSummary` 字段，用 entity-scoped async task lookup 读取最近一个 `course_import_batch` task，并把 `dispatch_failed`、`queued`、`running`、`retrying`、`completed`、`partially_completed`、`failed` 转成 UI 所需摘要字段；不要把 batch domain status 与 async runtime status 混成一个枚举（per research finding 2, D-41-05）。同时扩展 `TeacherCourseCenterDTO`，新增顶部最近导入任务卡所需字段，数据来源固定为当前 actor 最近一个 `featureArea = "course_import"` task，再补对应 batch label、进度/终态摘要与回批次详情链接（per D-41-06 ~ D-41-08）。DTO 必须只读 SQLite/DAL truth，不允许直读 BullMQ/Redis state（per ATP-12, ATP-14）。</action>
  <acceptance_criteria>
    - `src/lib/dto/course-import.ts` contains explicit async-task summary fields separate from batch domain status.
    - `src/lib/dal/course-import.ts` uses `getEntityAsyncTaskListDTO` or equivalent entity-scoped async lookup for batch detail data.
    - `src/lib/dto/course-authoring.ts` and `src/lib/dal/course-authoring.ts` expose recent import task card data for the hero surface.
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>批次详情页与课程中心读取面都能从 durable async DTO 看到真实任务状态、进度与终态摘要。</done>
</task>

<task type="auto">
  <name>Task 2: 按 UI-SPEC 把批次详情页与课程中心改成 honest async surface</name>
  <files>src/components/surfaces/course-import-review-surface.tsx, src/components/surfaces/teacher-course-center-surface.tsx</files>
  <read_first>src/components/surfaces/course-import-review-surface.tsx, src/components/surfaces/teacher-course-center-surface.tsx, .planning/phases/41-first-real-product-slice-batch-import-async-workflow/41-UI-SPEC.md, .planning/phases/41-first-real-product-slice-batch-import-async-workflow/41-CONTEXT.md</read_first>
  <action>把 `CourseImportReviewSurface` 扩成单一事实页面：在审核态保留现有 “更新/跳过” 决策；任务创建成功后立即把逐行决策切成只读并显示 queued/running/retrying/terminal 状态；dispatch failure 必须停留在当前页并显示 error-toned banner；terminal 状态顶部固定先展示状态/进度，再展示 `created / updated / skipped / failed` 汇总，再展示逐行明细（per D-41-02, D-41-03, D-41-04, D-41-10）。`partially_completed` 文案固定使用“已完成，但有失败项”，失败引导固定为修正 CSV/冲突后重新创建新任务，不做失败行局部重跑（per D-41-09, D-41-11, D-41-12）。同时在 `TeacherCourseCenterSurface` hero 顶部加入轻量最近导入任务卡：仅显示标题、状态/进度或终态摘要、以及返回批次详情的 link；完成后卡片仍保留最近结果，不变成 operator console（per D-41-06 ~ D-41-08, UI-SPEC section 7）。</action>
  <acceptance_criteria>
    - `src/components/surfaces/course-import-review-surface.tsx` contains explicit UI branches for `queued`, `running`, `retrying`, `partially_completed`, `failed`, and `dispatch_failed`.
    - The surface source contains the exact partial-success headline `已完成，但有失败项`.
    - The review surface source keeps `更新` and `跳过` as the only matched-row decisions and contains read-only handling after task creation.
    - `src/components/surfaces/teacher-course-center-surface.tsx` contains a recent import task card near the hero region with a link back to `/teacher/courses/import/` batch detail.
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>teacher/staff 能在批次详情页与课程中心看到 honest async 状态、结果摘要与回详情入口，且 UI 没有把 queued 伪装成 done。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DAL DTO → teacher UI | 产品面消费 durable task truth，必须防止 optimistic or leaked state |
| course center hero → batch detail link | 必须只暴露当前 actor 有权访问的 batch/task 信息 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-07 | I | course center recent-task card | mitigate | 只返回当前 actor 最近 `course_import` task 摘要与 batch link，不暴露 school-wide operator data。 |
| T-41-08 | T | batch detail async status mapping | mitigate | batch domain status 与 async runtime status 分字段承载，避免 UI 错把 queued/rendering 当作 applied。 |
| T-41-09 | R | partial success/result copy | mitigate | 固定 summary-before-detail hierarchy 与 exact partial-success headline，保持 counts 和 failure reason 来自 durable DTO。 |
| T-41-10 | D | stale caches across surfaces | mitigate | 只消费 DAL DTO + explicit cache tags，依赖 Plan 01 的 dual invalidation，避免课程中心与 batch 页状态漂移。 |
| T-41-11 | E | read-only decision freeze | mitigate | active task 存在时将 matched-row controls 切成只读，不保留继续改决策的 UI path。 |
</threat_model>

<verification>
- `pnpm typecheck`
- 代码阅读确认 UI 分支、partial-success headline、recent-task card、read-only freeze 全部存在。
</verification>

<success_criteria>
- batch detail page 成为 review + runtime + result 的唯一事实页。
- course center 顶部出现轻量 recent import card，完成后仍保留最近摘要。
- queued/running/retrying/partial/failure 全部有 honest copy 与稳定层级。
</success_criteria>

<output>
After completion, create `.planning/phases/41-first-real-product-slice-batch-import-async-workflow/41-02-SUMMARY.md`
</output>
