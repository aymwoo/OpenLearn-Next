---
phase: 39-async-contracts-and-durable-task-truth
plan: 02
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - src/db/schema.ts
  - drizzle/0004_phase39_async_tasks.sql
  - drizzle/meta/_journal.json
  - drizzle/meta/0004_snapshot.json
  - src/lib/cache-policy.ts
  - src/lib/dal/async-tasks.ts
  - src/lib/dal/async-tasks.test.ts
  - src/features/async-tasks/server/status.ts
  - src/features/async-tasks/server/mapper.ts
autonomous: true
requirements:
  - ATP-03
must_haves:
  truths:
    - Product surfaces can read task status, progress, and result from SQLite durable tables without depending on BullMQ state.
    - The platform stores latest task snapshot and append-only event history as two deliberate persistence layers.
    - Async task read models expose actor/school/entity ownership and honest enqueue posture.
  artifacts:
    - path: src/db/schema.ts
      provides: Async task ledger and history tables
    - path: src/lib/dal/async-tasks.ts
      provides: Cached async task read models and DTO parsing
    - path: drizzle/0004_phase39_async_tasks.sql
      provides: Migration for async task durable truth tables
  key_links:
    - from: src/lib/dal/async-tasks.ts
      to: src/features/async-tasks/shared/dto.ts
      via: DAL returns parsed DTOs
      pattern: AsyncTaskDetailDTOSchema.parse
    - from: src/db/schema.ts
      to: src/lib/dal/async-tasks.ts
      via: DAL queries new async task tables only
      pattern: asyncTask
---

<objective>
建立 SQLite durable task truth：新增 async task 主表、append-only event 表、cache tags 与 DAL read models，让 ATP-03 在 Phase 39 就可被验证。

Purpose: 让平台状态、进度和结果的产品真相先落在 SQLite + DAL，而不是等 BullMQ 再决定如何观察任务。
Output: Drizzle schema/migration、cache tags、async-task DAL/read model、focused ledger tests。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-async-contracts-and-durable-task-truth/39-CONTEXT.md
@.planning/phases/39-async-contracts-and-durable-task-truth/39-RESEARCH.md
@.planning/phases/39-async-contracts-and-durable-task-truth/39-PATTERNS.md
@.planning/phases/39-async-contracts-and-durable-task-truth/39-01-PLAN.md

<interfaces>
From src/db/schema.ts:
```ts
export const courseImportBatch = sqliteTable("courseImportBatch", { ... });
export const courseImportRow = sqliteTable("courseImportRow", { ... });
export const runtimeStepStates = sqliteTable("runtimeStepState", { ... });
export const runtimeEventOutbox = sqliteTable("runtimeEventOutbox", { ... });
```

From src/lib/dal/course-import.ts:
```ts
cacheTag(cacheTags.teacherCourses(input.actorId));
cacheTag(cacheTags.courseImportBatch(input.batchId));
return CourseImportBatchDTOSchema.parse({ ... });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 async task ledger schema、migration 与 cache tags</name>
  <files>src/db/schema.ts, drizzle/0004_phase39_async_tasks.sql, drizzle/meta/_journal.json, drizzle/meta/0004_snapshot.json, src/lib/cache-policy.ts</files>
  <read_first>src/db/schema.ts, drizzle/0003_phase37_redis_fanout.sql, drizzle/meta/_journal.json, src/lib/cache-policy.ts, .planning/phases/39-async-contracts-and-durable-task-truth/39-CONTEXT.md</read_first>
  <behavior>
    - Test 1: main task table stores actorId, schoolId, taskType, featureArea, visibilityScope, entity ref, latest progress, and latest result per D-39-01 to D-39-07.
    - Test 2: append-only event table references taskId and preserves dispatch/progress/outcome history.
    - Test 3: cache-policy exposes dedicated async task tags for list/detail/entity refresh.
  </behavior>
  <action>在 `src/db/schema.ts` 中新增 durable task truth tables，采用“主表 latest snapshot + append-only events”双层形态 per D-39-07。主表字段必须显式包括 `actorId`、`schoolId`、`taskType`、`featureArea`、`visibilityScope`、`entityType`、`entityId`、可选 `entityLabel`、`payloadJson`、`latestProgressJson`、`latestResultJson`、`queueJobId`（nullable 供后续 Phase 40 使用）、honest enqueue posture 字段、时间戳字段；事件表必须至少包括 `taskId`、`eventType`、`payloadJson`、`createdAt`，并通过 foreign key cascade 到主表。新增 `drizzle/0004_phase39_async_tasks.sql` 及对应 meta journal/snapshot，遵守仓库 migration-first 习惯。同步在 `src/lib/cache-policy.ts` 中新增 async task 相关 tags，至少支持 task detail、actor-scoped task list、entity-scoped task list。不要把 BullMQ runtime state 或 retry semantics 提前写入 schema；只预留 nullable `queueJobId` 与 intent/status vocabulary。</action>
  <acceptance_criteria>
    - `src/db/schema.ts` contains `export const asyncTasks = sqliteTable(` and `export const asyncTaskEvents = sqliteTable(`.
    - Main task table includes `actorId`, `schoolId`, `taskType`, `featureArea`, `visibilityScope`, `entityType`, and `entityId` columns.
    - Event table includes `taskId`, `eventType`, and `payloadJson` columns.
    - `src/lib/cache-policy.ts` contains async task cache tag helpers.
    - `drizzle/0004_phase39_async_tasks.sql` exists and references both async task tables.
  </acceptance_criteria>
  <verify>
    <automated>Deferred to Task 2 after `src/lib/dal/async-tasks.test.ts` exists; do not mark Wave 2 complete until Task 2 runs `pnpm test --run src/lib/dal/async-tasks.test.ts src/db/schema.learning.test.ts` successfully.</automated>
  </verify>
  <done>SQLite durable task truth 已有 migration-ready schema 与缓存边界，后续 read model 与 enqueue seam 可在此基础上工作。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立 async task DAL/read model，并执行 [BLOCKING] schema apply</name>
  <files>src/lib/dal/async-tasks.ts, src/lib/dal/async-tasks.test.ts, src/features/async-tasks/server/status.ts, src/features/async-tasks/server/mapper.ts, src/db/schema.ts, src/lib/cache-policy.ts</files>
  <read_first>src/lib/dal/course-import.ts, src/lib/dal/runtime-inspector.ts, src/features/async-tasks/shared/contract.ts, src/features/async-tasks/shared/dto.ts, src/db/schema.ts, src/lib/cache-policy.ts</read_first>
  <behavior>
    - Test 1: DAL maps SQLite rows into parsed task list/detail DTOs without leaking raw JSON.
    - Test 2: read models surface honest enqueue statuses like pending enqueue / dispatch failed.
    - Test 3: [BLOCKING] local database schema apply succeeds after migration/schema changes, so verification is not a false positive.
  </behavior>
  <action>创建 `src/features/async-tasks/server/mapper.ts` 与 `src/features/async-tasks/server/status.ts`，负责把主表 + event rows 转成 product-facing summary helper；再创建 `src/lib/dal/async-tasks.ts`，提供至少 actor-scoped task list、task detail、entity-scoped task list 这三类 cached read helpers，全部通过 `AsyncTaskListItemDTOSchema` / `AsyncTaskDetailDTOSchema` parse 输出，沿用 `"use cache" + cacheLife() + cacheTag()` 模式。创建 `src/lib/dal/async-tasks.test.ts`，覆盖 structured progress/result parse、honest enqueue posture、actor/entity scoping、append-only event timeline ordering。**[BLOCKING]** 在 schema 与 migration 文件完成后，先运行 `pnpm db:migrate` 将新表应用到本地 SQLite；若本地 schema 漂移导致验证不能覆盖 live DB，就修正 migration 或 migration metadata，让 `pnpm db:migrate` 成为唯一通过路径，不要用 `drizzle-kit push` 代替 migration-first 基线。此任务必须明确证明 Phase 39 的 durable status 读取不依赖 Redis/BullMQ state per ATP-03。</action>
  <acceptance_criteria>
    - `src/lib/dal/async-tasks.ts` contains `"use cache"`, async task cache tags, and DTO parse calls.
    - `src/lib/dal/async-tasks.ts` exports actor-scoped list and task-detail read helpers.
    - `src/lib/dal/async-tasks.test.ts` asserts pending enqueue / dispatch failed states parse into DTOs.
    - `pnpm db:migrate` exits 0 after async task schema changes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm db:migrate && pnpm test --run src/lib/dal/async-tasks.test.ts src/db/schema.learning.test.ts && pnpm typecheck</automated>
  </verify>
  <done>Phase 39 已拥有真正可读的 SQLite task ledger/read model，且本地数据库 schema 已同步应用，不存在“代码通过但 live DB 没变”的假验证状态。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server read models → SQLite ledger | DAL 从 durable truth 读取任务状态，必须保持 actor/school/entity scope 约束 |
| migration/schema change → local database | schema 变更必须真正应用到 live SQLite，避免验证只停留在 TypeScript 类型层 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04 | T | `src/db/schema.ts` async task tables | mitigate | 显式存储 actor/school/entity ownership 字段并用 foreign key cascade 约束 durable truth |
| T-39-05 | I | `src/lib/dal/async-tasks.ts` | mitigate | DAL 只返回 DTO summary/history，不直接泄露 raw payload JSON 给 product surfaces |
| T-39-06 | D | migration apply step | mitigate | 通过 `[BLOCKING]` `pnpm db:migrate` 与 migration metadata 修正，防止 schema drift 导致 verification 虚假通过 |
</threat_model>

<verification>
- `pnpm db:migrate`
- `pnpm test --run src/lib/dal/async-tasks.test.ts src/db/schema.learning.test.ts`
- `pnpm typecheck`
</verification>

<success_criteria>
- SQLite durable task ledger 与 event history 已存在于 schema 和 migration 中。
- DAL read models 能独立于 BullMQ 返回 task list/detail DTO。
- 本地数据库已执行 schema apply，后续 Phase 40 不需要为“任务表是否真的存在”做补救。
</success_criteria>

<output>
After completion, create `.planning/phases/39-async-contracts-and-durable-task-truth/39-02-SUMMARY.md`
</output>
