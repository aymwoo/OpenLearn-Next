---
phase: 28-runtime-bridge-contracts-and-session-persistence
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - src/db/schema.ts
  - drizzle/*.sql
  - drizzle/meta/_journal.json
  - src/lib/dto/classroom.ts
  - src/features/runtime-platform/classroom/index.ts
  - src/features/runtime-platform/classroom/runtime-session-contracts.ts
  - src/features/runtime-platform/classroom/runtime-session-contracts.test.ts
autonomous: true
requirements: [BRDG-03]
must_haves:
  truths:
    - "D-05 / D-06：runtime session identity 绑定 `classroomSession + stepId + actor + runtime version`，同 actor 重入同一步骤时默认恢复 latest session。"
    - "D-07 / D-08：runtime session 和 state 保留历史并显式标记 latest，不做原地覆盖。"
    - "D-09 / D-10 / D-12：bootstrap 合同只返回最小 step/classroom/actor/recovery 摘要，不暴露 full snapshot、cookies、secrets 或 raw rows。"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "durable runtime session/state/outbox persistence tables"
      contains: "runtimeStepSessions"
    - path: "src/features/runtime-platform/classroom/runtime-session-contracts.ts"
      provides: "typed runtime bootstrap/session/state DTO contracts"
      contains: "RuntimeBootstrapDTO"
    - path: "src/lib/dto/classroom.ts"
      provides: "classroom-facing bootstrap and runtime session DTO exports"
      contains: "RuntimeBootstrap"
  key_links:
    - from: "src/db/schema.ts"
      to: "src/features/runtime-platform/classroom/runtime-session-contracts.ts"
      via: "shared runtime session/state field names"
      pattern: "runtimeStepSessions"
    - from: "src/features/runtime-platform/classroom/index.ts"
      to: "src/features/runtime-platform/classroom/runtime-session-contracts.ts"
      via: "public barrel export"
      pattern: "runtime-session-contracts"
---

<objective>
建立 runtime session durability 的数据库与 DTO 合同，让后续 host-side
bootstrap/save/submit 能站在 append-only、latest-recoverable 的基础上实现。

Purpose: 先把 durable truth 和 recovery contract 立住，避免后续 host action
边写边猜 session identity 和 bootstrap shape。
Output: runtime session/state/outbox schema，typed bootstrap/session DTO，以及
public barrel exports。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-runtime-bridge-contracts-and-session-persistence/28-CONTEXT.md
@.planning/phases/28-runtime-bridge-contracts-and-session-persistence/28-RESEARCH.md
@.planning/phases/28-runtime-bridge-contracts-and-session-persistence/28-PATTERNS.md
@src/db/schema.ts
@src/lib/dto/classroom.ts
@src/features/runtime-platform/classroom/index.ts

<interfaces>
Existing append-only latest precedent:

```ts
uniqueIndex("taskSubmissions_latest_unique").on(
  table.publishedVersionId,
  table.stepId,
  table.studentId,
  table.isLatest,
)
```

Current classroom DTO export style:

```ts
export const ClassroomSnapshotDTOSchema = z.object({...});
export type ClassroomSnapshotDTO = z.infer<typeof ClassroomSnapshotDTOSchema>;
```

Recommended runtime persistence names from research:

```text
runtimeStepSessions
runtimeStepStates
runtimeEventOutbox
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义 runtime session/state/outbox SQLite schema</name>
  <read_first>
    - src/db/schema.ts
    - .planning/phases/28-runtime-bridge-contracts-and-session-persistence/28-CONTEXT.md
    - .planning/phases/28-runtime-bridge-contracts-and-session-persistence/28-RESEARCH.md
    - .planning/phases/28-runtime-bridge-contracts-and-session-persistence/28-PATTERNS.md
  </read_first>
  <files>src/db/schema.ts</files>
  <action>按 D-05、D-06、D-07、D-08、`SAFE-03`、`RTSE-01` 在 `src/db/schema.ts` 新增三张 Drizzle SQLite 表：`runtimeStepSessions`（字段至少含 `id`, `classroomSessionId`, `publishedVersionId`, `lessonId`, `stepId`, `runtimeId`, `runtimeVersion`, `actorId`, `actorScope`, `schoolId`, `isLatest`, `resetReason`, `createdAt`）；`runtimeStepStates`（至少含 `id`, `runtimeSessionId`, `stateVersion`, `kind`, `stateJson`, `summaryJson`, `isLatest`, `createdAt`）；`runtimeEventOutbox`（至少含 `id`, `runtimeSessionId`, `classroomSessionId`, `stepId`, `eventType`, `messageId`, `correlationId`, `payloadJson`, `deliveryChannel`, `deliveryStatus`, `createdAt`, `deliveredAt`）。所有外键继续 `onDelete: cascade`，并为 latest identity、session history 和 outbox delivery 建立显式 index / uniqueIndex；不要把 runtime state 塞回 `classroomSessions` 或 `classroomEvidence` 原表里。</action>
  <acceptance_criteria>
    - `schema.ts` 出现 `runtimeStepSessions`、`runtimeStepStates`、`runtimeEventOutbox` 三个表常量。
    - `runtimeStepSessions` 同时包含 `runtimeVersion` 与 `isLatest`。
    - 至少一个 unique index 锁定 session latest identity，不允许同 identity 多条 `isLatest=true`。
    - 新表外键全部使用 `{ onDelete: "cascade" }`。
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>runtime durability 的物理 truth 已落到 SQLite schema，后续 host-side handlers 可直接依赖 append-only persistence。</done>
</task>

<task type="auto">
  <name>Task 2: 定义 bootstrap、session、recovery 的 typed DTO contracts</name>
  <read_first>
    - src/lib/dto/classroom.ts
    - src/features/runtime-platform/classroom/index.ts
    - src/features/runtime-platform/contracts/bridge.ts
    - src/features/runtime-platform/contracts/descriptors.ts
  </read_first>
  <files>src/features/runtime-platform/classroom/runtime-session-contracts.ts, src/features/runtime-platform/classroom/index.ts, src/lib/dto/classroom.ts, src/features/runtime-platform/classroom/runtime-session-contracts.test.ts</files>
  <action>按 D-09、D-10、D-11、D-12 与 `BRDG-03` 定义 runtime bootstrap/session contracts：在 `src/features/runtime-platform/classroom/runtime-session-contracts.ts` 新增 `RuntimeSessionIdentitySchema`、`RuntimeSessionSummarySchema`、`RuntimeStateSummarySchema`、`RuntimeBootstrapDTOSchema`、`CreateOrResumeRuntimeSessionInputSchema` 等，并把 bootstrap 输出字段固定为 `sessionId`, `runtimeVersion`, `stepSummary`, `lessonSummary`, `classroomSummary`, `actor`, `capabilityContext`, `latestStateSummary`。在 `src/lib/dto/classroom.ts` 增加对这些 runtime DTO 的 re-export 或 classroom-facing alias，使现有 classroom/player 层能使用同一 typed contract；在 `src/features/runtime-platform/classroom/index.ts` 暴露这些 contracts。测试要断言 bootstrap DTO 不包含 `snapshotJson`, `cookies`, `secret`, `password`, `dbRow` 之类字段名。</action>
  <acceptance_criteria>
    - `runtime-session-contracts.ts` 出现 `RuntimeBootstrapDTOSchema` 与 `CreateOrResumeRuntimeSessionInputSchema`。
    - bootstrap DTO 字段包含 `latestStateSummary`，不包含 `snapshotJson` 或 `cookies`。
    - `src/features/runtime-platform/classroom/index.ts` 导出 runtime session contract 模块。
    - `runtime-session-contracts.test.ts` 包含 banned field token 断言和最小 bootstrap parse 断言。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/features/runtime-platform/classroom/runtime-session-contracts.test.ts</automated>
  </verify>
  <done>host-side bootstrap 和 recovery 已有稳定 typed contract，后续实现不再需要临时猜字段。</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] 生成并验证正式 runtime migration</name>
  <read_first>
    - src/db/schema.ts
    - drizzle.config.ts
    - package.json
  </read_first>
  <files>src/db/schema.ts, drizzle.config.ts, drizzle/*.sql, drizzle/meta/_journal.json</files>
  <action>在 Task 1 的 schema 修改完成后，走项目要求的 migration-first 正式路径：先用 `npx drizzle-kit generate`（或等价的非交互 migration 生成方式）为 `runtimeStepSessions`、`runtimeStepStates`、`runtimeEventOutbox` 生成新的 Drizzle migration SQL 与 metadata，再通过仓库既有 `pnpm db:migrate` 应用/校验这些 migration。允许 `scripts/prepare-dev-db.ts` 处理本地旧库的 migration bridge，但不要把 `drizzle-kit push` 作为阻塞主路径，也不要把可能卡住的交互命令放进 autonomous 阻塞任务里。</action>
  <acceptance_criteria>
    - `drizzle/` 下出现新的 runtime durability migration SQL，且 `drizzle/meta/_journal.json` 记录该 migration。
    - `pnpm db:migrate` 成功退出，并应用 runtime 相关新表。
    - 后续依赖 schema 的 tests 使用的是 migration-first live database schema，而不只是 TS 类型。
  </acceptance_criteria>
  <verify>
    <automated>pnpm db:migrate</automated>
  </verify>
  <done>runtime durability schema 已通过正式 migration 路径落到本地 SQLite，后续实现不会建立在只改 schema.ts 却未迁移的假状态上。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime durability tables → classroom truth | 若 session/state/outbox identity 设计错误，会导致恢复错绑或覆盖历史 |
| bootstrap contract → runtime consumer | 若 bootstrap 泄露 full snapshot / secrets，后续 runtime host 会越权读取 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | T | runtime session identity | mitigate | 用 `classroomSessionId + stepId + actorId + actorScope + runtimeVersion + isLatest` 的唯一约束锁定 latest recovery 身份 |
| T-28-02-02 | I | bootstrap DTO | mitigate | bootstrap contract 只暴露最小 summary 字段，并在 tests 中禁止 `snapshotJson`、`cookies`、`secret`、`dbRow` 令牌 |
| T-28-02-03 | D | schema drift | mitigate | 通过生成正式 Drizzle migration 并运行 `pnpm db:migrate` 保证 live SQLite schema 与代码同步，不依赖 `drizzle-kit push` |
</threat_model>

<verification>
- `pnpm test --run src/features/runtime-platform/classroom/runtime-session-contracts.test.ts`
- `pnpm db:migrate`
- 读取 `src/db/schema.ts`，确认 runtime 三张表和 latest identity index 已存在
</verification>

<success_criteria>
- runtime session/state/outbox 已有 durable SQLite truth。
- bootstrap/recovery 合同已明确且最小化。
- 同一 actor 在同一步骤可恢复 latest session，历史记录仍被保留。
</success_criteria>

<output>
After completion, create `.planning/phases/28-runtime-bridge-contracts-and-session-persistence/28-02-SUMMARY.md`
</output>
