---
phase: 28-runtime-bridge-contracts-and-session-persistence
plan: 04
type: execute
wave: 4
depends_on: [28-02, 28-03]
files_modified:
  - src/lib/dal/learning.ts
  - src/lib/cache-policy.ts
  - src/actions/classroom-actions.ts
  - src/features/runtime-platform/classroom/runtime-session.test.ts
  - src/lib/dal/learning.test.ts
  - src/lib/dal/classroom.test.ts
  - scripts/verify-phase28-runtime-bridge.ts
  - package.json
autonomous: true
requirements: [RTSE-04, SAFE-03]
must_haves:
  truths:
    - "D-12 / SAFE-03：player personal runtime path 可读取 latest runtime session/state recovery 摘要，刷新后仍可恢复。"
    - "D-16 / RTSE-04：runtime submit 后 teacher/student downstream surfaces 立即看到 classroom/progress/submission 的最新状态。"
    - "D-16 / RTSE-04：`progress` 相关 freshness 只能建立在 Plan 03 已显式更新权威 progress truth（或已落地 progress 派生链）的前提上，不能只靠 tag invalidation。"
    - "Phase gate：仓库拥有单一 `verify:phase28` 入口，同时检查 durability、cache invalidation 和 runtime truth bridge。"
  artifacts:
    - path: "src/lib/dal/learning.ts"
      provides: "player personal DTO runtime recovery integration"
      contains: "latestRuntime"
    - path: "scripts/verify-phase28-runtime-bridge.ts"
      provides: "phase 28 runtime durability and cache verifier"
      contains: "verify:phase28"
    - path: "src/lib/cache-policy.ts"
      provides: "explicit runtime cache invalidation matrix"
      contains: "classroom"
  key_links:
    - from: "src/actions/classroom-actions.ts"
      to: "src/lib/cache-policy.ts"
      via: "updateTag(cacheTags.*)"
      pattern: "updateTag(cacheTags."
    - from: "scripts/verify-phase28-runtime-bridge.ts"
      to: "src/features/runtime-platform/classroom/runtime-session.test.ts"
      via: "focused runtime regression command"
      pattern: "pnpm"
---

<objective>
把 runtime durability 接到 player/classroom 的恢复与 freshness 路径，并建立
Phase 28 的一键 verifier。

Purpose: 没有 read-your-writes 和恢复证明，Phase 28 的 runtime durability 只是
表结构，不是可执行的平台边界。
Output: runtime cache invalidation matrix、player recovery integration、focused
DAL regressions、`verify:phase28`。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-runtime-bridge-contracts-and-session-persistence/28-CONTEXT.md
@.planning/phases/28-runtime-bridge-contracts-and-session-persistence/28-RESEARCH.md
@src/lib/dal/learning.ts
@src/lib/cache-policy.ts
@src/actions/classroom-actions.ts
@package.json

<interfaces>
Current explicit invalidation pattern:

```ts
updateTag(cacheTags.classroom(sessionId));
updateTag(cacheTags.progress(lessonId, userId));
updateTag(cacheTags.submission(lessonId, userId));
```

Current player personal runtime pattern:

```ts
return StudentPlayerPersonalDTOSchema.parse({
  progress: {...},
  runtime: {
    classroomSessionId,
    classroomVersion,
    ...
  },
});
```

Existing verifier script entry pattern:

```json
"verify:phase27": "tsx scripts/verify-phase27-runtime-platform.ts"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 把 latest runtime recovery 和 cache invalidation 接入 teacher/student 读路径</name>
  <read_first>
    - src/lib/dal/learning.ts
    - src/lib/cache-policy.ts
    - src/actions/classroom-actions.ts
    - src/features/runtime-platform/classroom/runtime-session.ts
    - .planning/phases/28-runtime-bridge-contracts-and-session-persistence/28-RESEARCH.md
  </read_first>
  <files>src/lib/dal/learning.ts, src/lib/cache-policy.ts, src/actions/classroom-actions.ts, src/lib/dal/learning.test.ts, src/lib/dal/classroom.test.ts</files>
  <action>按 D-12、D-16 与 `RTSE-04` 把 latest runtime recovery 接入现有读路径：在 `getStudentPlayerPersonalDTO()` 中增加 `latestRuntime` / `latestRuntimeStateSummary` / `runtimeRecoveryStatus`（命名可略调，但必须是 runtime latest recovery 摘要），来源只读取当前 actor + step + runtimeVersion 的 latest runtime session/state，不把 full state JSON 塞回 shell；在 `classroom-actions.ts` 的 runtime save/submit/teacher-control 入口上补齐 `updateTag(cacheTags.classroom(sessionId))`，并在 runtime submit 时按桥接类型额外刷新 `cacheTags.progress(lessonId,userId)`、`cacheTags.submission(lessonId,userId)`、`cacheTags.teacherReview(lessonId)`。这里必须把 invalidation 与 Plan 03 中新增的权威 progress truth 更新（优先 `lessonStepProgress` upsert，或已实现的等价 progress 派生链）一起验证：测试不仅要断言 tag 被刷新，还要断言对应 teacher/student DTO 真的因为 progress 数据变化而变新。必要时在 `cache-policy.ts` 增加专用 runtime tag，但不能替代现有 classroom/progress/submission 既有 truth tags。</action>
  <acceptance_criteria>
    - `learning.ts` 返回的 personal DTO 出现 runtime latest recovery 摘要字段。
    - runtime submit 入口同时刷新 `classroom` 与对应 downstream tags，而不是只刷新一个 tag。
    - `learning.test.ts` 覆盖“刷新后仍能读到 latest runtime recovery 摘要”的断言。
    - `classroom.test.ts` 或等价测试覆盖 runtime submit 后 classroom/progress/submission 可见状态更新，且该变化由 `lessonStepProgress` 或等价 progress truth 变更驱动，而不是仅靠 tag 调用断言。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/learning.test.ts src/lib/dal/classroom.test.ts src/features/runtime-platform/classroom/runtime-session.test.ts</automated>
  </verify>
  <done>runtime 相关写入已具备 teacher/student surface 的 read-your-writes 和刷新恢复能力，且 progress freshness 建立在真实数据变化之上。</done>
</task>

<task type="auto">
  <name>Task 2: 建立 canonical `verify:phase28` durability and cache gate</name>
  <read_first>
    - package.json
    - scripts/verify-phase27-runtime-platform.ts
    - src/features/runtime-platform/classroom/runtime-session.test.ts
    - src/lib/dal/learning.test.ts
    - src/lib/dal/classroom.test.ts
  </read_first>
  <files>scripts/verify-phase28-runtime-bridge.ts, package.json, src/features/runtime-platform/classroom/runtime-session.test.ts</files>
  <action>新增 `scripts/verify-phase28-runtime-bridge.ts` 并在 `package.json` 注册 `verify:phase28`。该 verifier 必须先做 Phase 28 静态 guards：检查 `lesson-authoring` payload 仍通过 `runtime` optional block 承载 descriptor、schema 中存在 `runtimeStepSessions/runtimeStepStates/runtimeEventOutbox`、runtime host actions 仍通过 `createGuardedHostAction`、`save` 路径没有写 `taskSubmissions/quizAttempts`、`submit` 路径存在 `updateTag(cacheTags.classroom(...))` 与 downstream tag invalidation。然后再组合运行 `src/features/runtime-platform/classroom/runtime-session.test.ts`、`src/lib/dal/learning.test.ts`、`src/lib/dal/classroom.test.ts` 的 focused suites。输出要区分 contract drift、durability drift、cache drift 三类 failure label。</action>
  <acceptance_criteria>
    - `package.json` 出现 `verify:phase28`。
    - verifier 脚本包含 runtime session table、guard wrapper、save-vs-submit、cache invalidation 四类静态 guard。
    - verifier 会执行 runtime-session、learning、classroom 三组 focused tests。
    - verifier 输出含 `contract drift`、`durability drift`、`cache drift` 或等价分类文案。
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase28</automated>
  </verify>
  <done>Phase 28 拥有单一 canonical gate，可在后续 Runtime Host phase 之前持续保护 durability 与 cache semantics。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime durability writes → cached teacher/student surfaces | 若 tag invalidation 不完整，用户会看到过时状态 |
| recovery summary → player personal DTO | 若恢复逻辑读取错误 identity，学生会恢复到错误状态或看不到已保存内容 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-01 | I | stale teacher/student reads | mitigate | runtime save/submit/teacher-control 明确刷新 `classroom` 及 downstream `progress/submission/teacherReview` tags |
| T-28-04-02 | T | latest runtime recovery selection | mitigate | personal DTO 只按 actor + step + runtimeVersion 的 latest session/state 读取恢复摘要，并以测试锁定刷新后恢复行为 |
| T-28-04-03 | R | verifier blind spots | mitigate | `verify:phase28` 同时做静态 guards 与 focused suites，失败时输出 contract/durability/cache 分类 |
| T-28-04-04 | I | tag-only progress freshness | mitigate | verifier 与 focused tests 必须证明 runtime submit 后 `progress` 相关 DTO 变化来自权威 progress truth 更新，而不是只有 `updateTag(cacheTags.progress(...))` |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/learning.test.ts src/lib/dal/classroom.test.ts src/features/runtime-platform/classroom/runtime-session.test.ts`
- `pnpm verify:phase28`
- 读取 `src/actions/classroom-actions.ts`，确认 runtime submit 刷新 classroom + downstream tags
- 读取 `src/lib/dal/learning.ts` / `src/lib/dal/classroom.ts`，确认 runtime submit 后 progress 可见变化绑定到真实 progress truth 更新
</verification>

<success_criteria>
- runtime related writes 在 teacher/student surfaces 上保持 read-your-writes。
- player personal DTO 能恢复 latest runtime session/state 摘要。
- 仓库存在单一 `verify:phase28` 入口证明 durability 与 cache semantics。
- `progress` freshness 由权威 truth 更新 + invalidation 共同保证，而不是 tag-only 假刷新。
</success_criteria>

<output>
After completion, create `.planning/phases/28-runtime-bridge-contracts-and-session-persistence/28-04-SUMMARY.md`
</output>
