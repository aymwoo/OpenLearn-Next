---
phase: 29-runtime-host-and-html-courseware-pilot
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - src/components/surfaces/teacher-lesson-preview-surface.tsx
  - src/components/learning/classroom-runtime-client.tsx
  - src/components/surfaces/classroom-console-surface.tsx
  - src/features/runtime-platform/player/index.ts
  - src/features/runtime-platform/classroom/index.ts
  - src/lib/dal/learning.test.ts
  - src/components/surfaces/student-player-surfaces.test.ts
  - src/components/surfaces/classroom-console-surface.test.tsx
autonomous: true
requirements: [RHOST-01, RHOST-02]
must_haves:
  truths:
    - "D-06：teacher preview 中的 runtime host 仍是 draft-only，不读取 student progress 或 live classroom state。"
    - "D-07：student player 中的 runtime host 仍落在 streamed personal/runtime region，不把 runtime 恢复态塞回 shell。"
    - "D-08 / D-14：`/classroom` 继续是 live teacher 主域，runtime host 只消费现有 classroom snapshot 更新。"
  artifacts:
    - path: "src/components/surfaces/teacher-lesson-preview-surface.tsx"
      provides: "draft-only runtime preview embedding"
      contains: "RuntimeHost"
    - path: "src/components/learning/classroom-runtime-client.tsx"
      provides: "student current-step runtime host rendering"
      contains: "RuntimeHost"
    - path: "src/components/surfaces/classroom-console-surface.tsx"
      provides: "classroom-compatible teacher runtime host stage"
      contains: "RuntimeHost"
  key_links:
    - from: "src/components/learning/classroom-runtime-client.tsx"
      to: "src/lib/dto/learning.ts"
      via: "personal runtime state and latest runtime recovery"
      pattern: "latestRuntime|runtimeRecoveryStatus"
    - from: "src/components/surfaces/classroom-console-surface.tsx"
      to: "src/lib/dto/classroom.ts"
      via: "live snapshot-powered runtime updates"
      pattern: "ClassroomSnapshotDTO"
---

<objective>
把共享 Runtime Host 接入 teacher preview、student player 和 classroom-compatible
surface，保持各自既有路由与 truth boundary 不变。

Purpose: 让 Phase 29 真正把 host 嵌入三条现有产品链，而不是只停留在共享组件层。
Output: preview/player/classroom host embedding and focused regressions.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-runtime-host-and-html-courseware-pilot/29-CONTEXT.md
@.planning/phases/29-runtime-host-and-html-courseware-pilot/29-UI-SPEC.md
@src/app/(teacher)/teacher/editor/preview/page.tsx
@src/app/(student)/student/player/page.tsx
@src/app/(classroom)/classroom/page.tsx
@src/components/surfaces/teacher-lesson-preview-surface.tsx
@src/components/learning/classroom-runtime-client.tsx
@src/components/surfaces/classroom-console-surface.tsx
@src/lib/dto/learning.ts
@src/lib/dto/classroom.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 teacher preview 与 student player 接入 Runtime Host</name>
  <read_first>
    - src/components/surfaces/teacher-lesson-preview-surface.tsx
    - src/components/learning/classroom-runtime-client.tsx
    - src/components/surfaces/player-surface.tsx
    - src/lib/dto/learning.ts
    - .planning/phases/29-runtime-host-and-html-courseware-pilot/29-UI-SPEC.md
  </read_first>
  <files>src/components/surfaces/teacher-lesson-preview-surface.tsx, src/components/learning/classroom-runtime-client.tsx, src/components/surfaces/student-player-surfaces.test.ts, src/lib/dal/learning.test.ts</files>
  <action>在 preview 与 player 两处接入 shared Runtime Host。`teacher-lesson-preview-surface.tsx` 必须在 runtime-capable step（`step.payload.runtime?.kind === "html-courseware"` 或等价判断）时渲染一个 draft-only Runtime Host，并显示明确的“草稿预览 / 不包含学生进度与课堂运行态”说明。`classroom-runtime-client.tsx` 必须在 current step 为 runtime-capable 时，用 shared Runtime Host 替代当前 step-specific content body 分支或嵌入其主动作区，同时继续保留 `StepActivityShell`、player reconnect banner、locked/unlocked 语义与 `latestRuntime` 恢复上下文。测试必须锁住：preview 不读取 personal/live state；player 仍维持 shell/personal split，且 runtime host 读取的是 personal runtime 区域信息。</action>
  <acceptance_criteria>
    - `teacher-lesson-preview-surface.tsx` 出现 runtime-capable step 判断和 shared Runtime Host 使用点。
    - preview runtime host 周围包含“草稿预览”或“不包含学生进度与课堂运行态”的明确文案。
    - `classroom-runtime-client.tsx` 在 runtime-capable step 时使用 shared Runtime Host，而不是继续只回退纯文本占位。
    - `student-player-surfaces.test.ts` 仍包含 `Suspense`, `getStudentPlayerShellDTO`, `getStudentPlayerPersonalDTO` 相关断言。
    - `learning.test.ts` 继续锁定 latest runtime recovery 摘要被 personal DTO 读取。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/surfaces/student-player-surfaces.test.ts src/lib/dal/learning.test.ts</automated>
  </verify>
  <done>teacher preview 和 student player 已能在既有 surface 结构中渲染同一 Runtime Host。</done>
</task>

<task type="auto">
  <name>Task 2: 在 `/classroom` live stage 接入 classroom-compatible Runtime Host</name>
  <read_first>
    - src/components/surfaces/classroom-console-surface.tsx
    - src/lib/dto/classroom.ts
    - src/app/(classroom)/classroom/page.tsx
    - src/features/runtime-platform/classroom/index.ts
  </read_first>
  <files>src/components/surfaces/classroom-console-surface.tsx, src/features/runtime-platform/classroom/index.ts, src/components/surfaces/classroom-console-surface.test.tsx</files>
  <action>把 shared Runtime Host 接入 live classroom stage，但不改变 `/classroom` route posture。`classroom-console-surface.tsx` 在 `initialSnapshot` 存在且当前 active step 带 `payload.runtime` 时，必须在 live stage 的主区渲染 teacher-facing Runtime Host，并把课堂当前 snapshot、active step、locked 状态、version 或等价 live summary 作为 host update 输入传入。该 host 必须保持 teacher role 语义，不出现第二条独立 teacher runtime 路径或新导航入口。`classroom-console-surface.test.tsx` 要补断言，证明 live snapshot 分支仍在同一路由内工作，并且 runtime-capable step 会落到共享 host，而 ended recap 分支不受影响。</action>
  <acceptance_criteria>
    - `classroom-console-surface.tsx` 在 live snapshot 分支出现 runtime-capable step 的 host embedding。
    - runtime host 输入来自 `initialSnapshot` 或等价 classroom DTO，而不是 iframe 自己建立 SSE 连接。
    - `classroom-console-surface.test.tsx` 同时保留 live 和 recap 分支覆盖。
    - `/classroom` 页面与 route imports 不新增第二个 teacher runtime 主路径。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/surfaces/classroom-console-surface.test.tsx</automated>
  </verify>
  <done>classroom teacher stage 已能显示 runtime-capable 当前步骤，并继续服从现有 classroom truth 与 route posture。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| preview surface -> runtime host | preview 不能泄露 student progress 或 live classroom state |
| classroom snapshot -> runtime iframe | live classroom updates 必须来源于 durable snapshot，而不是 iframe 自建实时真相源 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-29-02-01 | I | teacher preview | mitigate | preview host 只消费 draft preview DTO，并显式标识不含学生/课堂运行态 |
| T-29-02-02 | T | student player split boundary | mitigate | runtime host 只接在 personal/runtime region，维持 shell/personal split |
| T-29-02-03 | E | classroom live surface | mitigate | `/classroom` 继续是唯一 teacher live runtime 主域，不新增第二条 route |

</threat_model>

<verification>
- `pnpm test --run src/components/surfaces/student-player-surfaces.test.ts src/lib/dal/learning.test.ts`
- `pnpm test --run src/components/surfaces/classroom-console-surface.test.tsx`
- 读取 `teacher-lesson-preview-surface.tsx`，确认 draft-only runtime host cue 存在
</verification>

<success_criteria>
- preview、player、classroom 三条现有 surface 都能渲染共享 Runtime Host。
- preview 继续保持 draft-only。
- player 继续保持 shell/personal split。
- classroom 继续保持 same-route live stage posture。
</success_criteria>

<output>
After completion, create `.planning/phases/29-runtime-host-and-html-courseware-pilot/29-02-SUMMARY.md`
</output>
