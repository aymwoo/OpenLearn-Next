---
phase: 31-transport-boundary-and-runtime-inspector
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/features/runtime-platform/seams/transport/contract.ts
  - src/features/runtime-platform/seams/transport/sse-adapter.ts
  - src/features/runtime-platform/seams/transport/gateway.ts
  - src/features/runtime-platform/seams/transport/gateway.test.ts
  - src/features/runtime-platform/seams/index.ts
  - src/features/runtime-platform/seams/seams.test.ts
autonomous: true
requirements: [TRNS-01, TRNS-02]
must_haves:
  truths:
    - "per D-01/D-04，runtime 与 classroom producer 后续都只能走单一 transport gateway，而不是直连 SSE adapter。"
    - "per D-02，gateway 依据 channel/kind 路由，不按 producer type 写死 dispatch。"
    - "per D-03，gateway 返回固定二段式语义：truth persisted 与 delivery attempted 分开表达。"
  artifacts:
    - path: "src/features/runtime-platform/seams/transport/gateway.ts"
      provides: "single transport publish entry with channel/kind routing"
    - path: "src/db/schema.ts"
      provides: "durable transport attempt persistence that does not replace runtime/classroom truth"
    - path: "src/features/runtime-platform/seams/transport/gateway.test.ts"
      provides: "regression coverage for two-stage publish semantics and SSE default adapter posture"
  key_links:
    - from: "src/features/runtime-platform/seams/transport/gateway.ts"
      to: "src/features/runtime-platform/seams/transport/sse-adapter.ts"
      via: "adapter registry dispatch by channel/kind"
      pattern: "channel"
    - from: "src/features/runtime-platform/seams/transport/gateway.ts"
      to: "src/db/schema.ts"
      via: "persisted transport attempt record"
      pattern: "deliveryAttempted"
---

<objective>
定义 Phase 31 的 transport gateway foundation：单入口、按 channel/kind 路由、二段式结果、
且 transport 记录只是 delivery trace，不成为新的 truth path。

Purpose: 先把 transport boundary contract 立住，后续 runtime/classroom producer 才能安全迁移，
同时为 inspector 提供可查询的 delivery attempt durable facts。
Output: transport gateway contract, durable attempt schema, SSE-first adapter registry, focused tests.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-transport-boundary-and-runtime-inspector/31-CONTEXT.md
@.planning/phases/27-compatibility-baseline-and-v2-boundary-scaffolding/27-04-SUMMARY.md
@src/features/runtime-platform/seams/transport/contract.ts
@src/features/runtime-platform/seams/transport/sse-adapter.ts
@src/features/runtime-platform/seams/index.ts
@src/db/schema.ts

<interfaces>
From `src/features/runtime-platform/seams/transport/contract.ts`:
```ts
export const RuntimeTransportEnvelopeSchema = z.object({
  sessionId: z.string().min(1),
  channel: z.string().min(1),
  payload: z.record(z.string(), z.unknown()),
});

export interface RuntimeTransportAdapter {
  readonly id: string;
  readonly mode: RuntimeTransportMode;
  readonly ownership: RuntimeTransportOwnership;
  describeOwnership(): RuntimeTransportOwnership;
  deliver(envelope: RuntimeTransportEnvelope): Promise<void>;
}
```

From `src/features/runtime-platform/seams/index.ts`:
```ts
export const runtimePlatformSeams = {
  transport: {
    defaultAdapter: "sseRuntimeTransportAdapter",
  },
} as const;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义统一 transport gateway contract 与 durable attempt persistence</name>
  <files>src/db/schema.ts, src/features/runtime-platform/seams/transport/contract.ts, src/features/runtime-platform/seams/index.ts, src/features/runtime-platform/seams/seams.test.ts</files>
  <behavior>
    - Test 1: gateway input 必须显式区分 `truthPersisted` 与 `deliveryAttempted`，不能只返回 delivered/failed。
    - Test 2: transport persistence 只记录 truth reference、channel、kind、adapter、attempt/result metadata，不复制 classroom/runtime 主真相。
  </behavior>
  <action>按 D-01、D-02、D-03、D-04 扩展 transport seam contract。新增一个 shared、可追加的 transport attempt persistence 结构（建议放在 `src/db/schema.ts`），字段至少包括 source truth reference、channel、kind、adapterId、correlationId、truthPersisted、deliveryAttempted、attempt status、consumer trace timestamps 或等价字段。不要把 runtime/classroom payload 真相复制成新的主表，也不要把 gateway 设计成按 producer type 分支。同步更新 `seams/index.ts` 与 `seams/seams.test.ts`，明确当前默认 adapter 仍是 SSE，但 ownership posture 继续声明 transport 只是 delivery concern。</action>
  <verify>
    <automated>pnpm test --run src/features/runtime-platform/seams/seams.test.ts src/features/runtime-platform/seams/transport/gateway.test.ts</automated>
  </verify>
  <done>transport gateway 的输入/输出 contract 与 durable attempt schema 已稳定存在，且不会把 delivery 结果误写成系统真相。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 SSE-first transport gateway 与 channel/kind 路由回归</name>
  <files>src/features/runtime-platform/seams/transport/gateway.ts, src/features/runtime-platform/seams/transport/sse-adapter.ts, src/features/runtime-platform/seams/transport/gateway.test.ts</files>
  <behavior>
    - Test 1: 同一 publish API 可依据 channel/kind 选中 SSE adapter，而不是读取 producer type。
    - Test 2: gateway publish 返回 two-stage result，且测试能看到 attempt record 被写入。
  </behavior>
  <action>新增 `src/features/runtime-platform/seams/transport/gateway.ts`，提供 Phase 31 之后唯一的 `publishTransportEvent()` 或等价 API。API 必须接收标准化 transport event（含 channel、kind、truthRef、session/runtime/plugin scope），先写 attempt record，再选择当前 adapter 执行 delivery attempt，最后返回 two-stage result。保留 `sseRuntimeTransportAdapter` 为首个 adapter，但不要再鼓励业务代码直接 import 并调用 `deliver()`。测试中锁住 channel/kind 路由、SSE default posture、two-stage result、以及 adapter failure 不会回滚 truth ownership 表达。</action>
  <verify>
    <automated>pnpm test --run src/features/runtime-platform/seams/transport/gateway.test.ts src/features/runtime-platform/seams/seams.test.ts</automated>
  </verify>
  <done>仓库已有单一 transport gateway，可在不切 WebSocket 的前提下把后续业务发送统一收口。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| runtime/classroom truth -> transport gateway | durable truth 与 delivery trace 在这里分离，若设计错误会把 transport 冒充为新真相源 |
| gateway -> adapter | adapter 只能承担 delivery attempt，不能反向决定 truth 是否成立 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-01-01 | T | transport gateway contract | mitigate | 用 D-03 two-stage result 固定 `truthPersisted` 与 `deliveryAttempted` 分离语义 |
| T-31-01-02 | R | transport attempt persistence | mitigate | attempt record 必须带 truth reference、correlationId、adapterId，供后续 inspector 追溯 |
| T-31-01-03 | E | adapter dispatch | mitigate | 仅允许 gateway 内部选 adapter；业务层不再直接 import `sseRuntimeTransportAdapter.deliver()` |

</threat_model>

<verification>
- `pnpm test --run src/features/runtime-platform/seams/transport/gateway.test.ts src/features/runtime-platform/seams/seams.test.ts`
</verification>

<success_criteria>
- transport boundary 已有单一 publish 入口。
- 当前默认 SSE adapter 仍保留，但不再是业务代码直连接口。
- gateway 的 durable output 能支撑后续 inspector 解释 delivery attempt。
</success_criteria>

<output>
After completion, create `.planning/phases/31-transport-boundary-and-runtime-inspector/31-01-SUMMARY.md`
</output>
