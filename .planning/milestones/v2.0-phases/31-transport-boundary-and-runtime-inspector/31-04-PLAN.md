---
phase: 31-transport-boundary-and-runtime-inspector
plan: 04
type: execute
wave: 4
depends_on: [31-02, 31-03]
files_modified:
  - scripts/verify-phase31-transport-inspector.ts
  - package.json
  - src/features/runtime-platform/seams/transport/gateway.test.ts
  - src/lib/dal/classroom.test.ts
  - src/lib/dal/runtime-inspector.test.ts
  - src/components/surfaces/runtime-inspector-surface.test.tsx
autonomous: true
requirements: [GOVR-04, TRNS-01, TRNS-02]
must_haves:
  truths:
    - "transport boundary 与 inspector 都有单一 phase gate，能同时守 direct-adapter drift、scope drift、health drift。"
    - "SSE parity 仍被验证；Phase 31 不做 WebSocket cutover。"
    - "inspector 的 role scope、single timeline posture、deterministic health 都被回归保护。"
  artifacts:
    - path: "scripts/verify-phase31-transport-inspector.ts"
      provides: "canonical phase 31 verifier"
    - path: "package.json"
      provides: "verify:phase31 script entry"
    - path: "src/lib/dal/runtime-inspector.test.ts"
      provides: "role-scope and health regression coverage"
  key_links:
    - from: "scripts/verify-phase31-transport-inspector.ts"
      to: "src/features/runtime-platform/host-actions/runtime-host.ts"
      via: "static guard against direct transport adapter calls"
      pattern: "sseRuntimeTransportAdapter"
    - from: "scripts/verify-phase31-transport-inspector.ts"
      to: "src/app/settings/labs/runtime-inspector/page.tsx"
      via: "independent operator page guard"
      pattern: "runtime-inspector"
---

<objective>
为 Phase 31 建立 canonical verification gate，并把 transport parity、inspector scope、deterministic health 与 rollback-safe posture 一起锁住。

Purpose: Phase 31 一旦没有单一 gate，后续 Phase 32 很容易静默回流到 direct adapter 或把 inspector 做成第二真相源。
Output: `verify:phase31`, focused regression suites, static guards for transport and inspector drift.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-transport-boundary-and-runtime-inspector/31-CONTEXT.md
@scripts/verify-phase29-runtime-host.ts
@scripts/verify-phase30-governance.ts
@package.json
@src/features/runtime-platform/host-actions/runtime-host.ts
@src/lib/dal/classroom.ts
@src/lib/dal/runtime-inspector.ts
@src/app/settings/labs/runtime-inspector/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 补齐 Phase 31 focused regressions，锁住 transport parity 与 inspector scope</name>
  <files>src/features/runtime-platform/seams/transport/gateway.test.ts, src/lib/dal/classroom.test.ts, src/lib/dal/runtime-inspector.test.ts, src/components/surfaces/runtime-inspector-surface.test.tsx</files>
  <behavior>
    - Test 1: direct adapter call 被移除后，runtime/classroom publish 仍通过 gateway 保持 SSE parity。
    - Test 2: inspector teacher/admin/developer scope 与 deterministic health 行为都可回归证明。
  </behavior>
  <action>为 Phase 31 新增或扩充 focused tests。至少覆盖：runtime/classroom 通过 gateway publish 的 parity、SSE route 仍是现有 delivery channel、教师看不到非自己 session、管理员不越校、developer scope 可读系统/开发范围、single timeline surface 不出现 tab-first 结构、health 不含阈值/预测字段。测试文件可复用 31-01/31-03 产物，但断言必须围绕真实 contract，不做注释型字符串守卫替代行为测试。</action>
  <verify>
    <automated>pnpm test --run src/features/runtime-platform/seams/transport/gateway.test.ts src/lib/dal/classroom.test.ts src/lib/dal/runtime-inspector.test.ts src/components/surfaces/runtime-inspector-surface.test.tsx</automated>
  </verify>
  <done>Phase 31 的关键行为已有 focused suites，后续 refactor 不能静默破坏 transport boundary 或 inspector posture。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立 canonical `verify:phase31` transport + inspector gate</name>
  <files>scripts/verify-phase31-transport-inspector.ts, package.json</files>
  <behavior>
    - Test 1: verifier 先做静态 guard，能直接指出 transport drift、scope drift、health drift、route drift。
    - Test 2: verifier 再运行 focused suites，而不是只转发单个测试命令。
  </behavior>
  <action>新增 `scripts/verify-phase31-transport-inspector.ts` 并在 `package.json` 注册 `verify:phase31`。静态 guards 至少检查：业务代码不再直接调用 `sseRuntimeTransportAdapter.deliver()`；`/api/classroom/[sessionId]/events` 仍是 `text/event-stream`；独立 inspector route 位于独立页面而不是 `/classroom`；health read model 不出现 threshold/anomaly/prediction/alert 这类推测型词汇；role-scope resolver 仍显式区分 teacher/admin/developer。之后组合运行本计划 focused suites。输出分类至少包含 `transport drift`、`scope drift`、`health drift`、`route drift` 四类。</action>
  <verify>
    <automated>pnpm verify:phase31</automated>
  </verify>
  <done>仓库已有单一 `verify:phase31` gate，可在进入 Phase 32 前持续保护 transport boundary 与 runtime inspector 语义。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| future refactor -> transport boundary | 若没有 canonical gate，direct adapter call 会重新渗回业务层 |
| future refactor -> inspector semantics | 若没有 static guard + focused suites，独立页面容易变成 classroom 附件或第二真相源 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-04-01 | T | transport drift | mitigate | `verify:phase31` 静态检查 direct adapter 调用、SSE route posture 与 gateway publish wiring |
| T-31-04-02 | I | scope drift | mitigate | verifier + focused tests 同时守 teacher/admin/developer scope contract |
| T-31-04-03 | R | health drift | mitigate | verifier 阻止 threshold/anomaly/prediction 型推测字段进入 deterministic health |

</threat_model>

<verification>
- `pnpm test --run src/features/runtime-platform/seams/transport/gateway.test.ts src/lib/dal/classroom.test.ts src/lib/dal/runtime-inspector.test.ts src/components/surfaces/runtime-inspector-surface.test.tsx`
- `pnpm verify:phase31`
</verification>

<success_criteria>
- Phase 31 拥有单一 canonical verifier。
- transport boundary、独立 inspector、role scope、deterministic health 都被 gate 锁住。
- 后续 Phase 32 可以直接在此基础上做 hardening 与 milestone proof，而不是重新找回 Phase 31 contract。
</success_criteria>

<output>
After completion, create `.planning/phases/31-transport-boundary-and-runtime-inspector/31-04-SUMMARY.md`
</output>
