---
phase: 31-transport-boundary-and-runtime-inspector
plan: 03
type: execute
wave: 3
depends_on: [31-01, 31-02]
files_modified:
  - src/lib/dto/runtime-inspector.ts
  - src/lib/dal/runtime-inspector.ts
  - src/lib/dal/runtime-inspector.test.ts
  - src/components/surfaces/runtime-inspector-surface.tsx
  - src/components/surfaces/runtime-inspector-surface.test.tsx
  - src/app/settings/labs/runtime-inspector/page.tsx
autonomous: true
requirements: [GOVR-04]
must_haves:
  truths:
    - "per D-05/D-06/D-07，inspector 是独立页面、首屏单条统一 timeline、默认锚点是 runtime session。"
    - "per D-08/D-09，health 仅由 durable lifecycle/governance/runtime/classroom/transport facts 聚合，不引入阈值告警或第二真相源。"
    - "per D-10/D-11，教师只看自己相关 traces，管理员看本校范围，开发者看系统/开发范围。"
  artifacts:
    - path: "src/lib/dal/runtime-inspector.ts"
      provides: "role-scoped runtime inspector read model anchored on runtime session"
    - path: "src/components/surfaces/runtime-inspector-surface.tsx"
      provides: "single-timeline operator surface with deterministic health summary"
    - path: "src/app/settings/labs/runtime-inspector/page.tsx"
      provides: "independent inspector entry page under existing operator shell"
  key_links:
    - from: "src/lib/dal/runtime-inspector.ts"
      to: "src/features/runtime-platform/classroom/runtime-session.ts"
      via: "runtime session + governance + transport trace aggregation"
      pattern: "runtimeSessionId"
    - from: "src/app/settings/labs/runtime-inspector/page.tsx"
      to: "src/components/surfaces/runtime-inspector-surface.tsx"
      via: "independent inspector route"
      pattern: "runtime-inspector"
---

<objective>
交付 operator-grade runtime inspector：独立页面、统一 timeline、runtime session 默认锚点、确定性 health、按角色分层读取。

Purpose: 让教师、管理员、开发者能解释一次 runtime run 的完整因果链，而不是在 classroom 面板或多 tab 里拼接排障信息。
Output: inspector DTO/read model, independent route, single timeline surface, scope-safe tests.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-transport-boundary-and-runtime-inspector/31-CONTEXT.md
@src/lib/theme-layout/route-surface-registry.ts
@src/app/(teacher)/teacher/trends/page.tsx
@src/components/classroom/classroom-control-panel.tsx
@src/lib/dal/plugins.ts
@src/features/runtime-platform/classroom/runtime-session.ts

<interfaces>
From `src/lib/theme-layout/route-surface-registry.ts`:
```ts
if (pathname.startsWith("/settings/labs")) {
  return "/settings/labs";
}
```

From `src/components/classroom/classroom-control-panel.tsx` current timeline anchor:
```tsx
<ClassroomTimelinePanel entries={currentSnapshot.teacherTimeline} />
```

From `src/db/schema.ts` and current governance/runtime durability:
```ts
export const runtimeEventOutbox = sqliteTable(...)
export const governanceAudits = sqliteTable(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 runtime-session anchored inspector read model 与 deterministic health 聚合</name>
  <files>src/lib/dto/runtime-inspector.ts, src/lib/dal/runtime-inspector.ts, src/lib/dal/runtime-inspector.test.ts</files>
  <behavior>
    - Test 1: 输入 runtimeSessionId 时，read model 返回按时间排序的单条 unified timeline，能串起 runtime、governance、transport 与 classroom traces。
    - Test 2: teacher/admin/developer 三类 scope 返回不同范围的数据；教师不会看到跨教师或跨学校 runtime session。
    - Test 3: health 仅由持久化事实推导，不包含阈值、anomaly、prediction 等推测字段。
  </behavior>
  <action>按 D-05、D-06、D-08、D-09、D-10、D-11 新增 `runtime-inspector` DTO 与 DAL。默认查询参数固定围绕 `runtimeSessionId`，并聚合 runtime session、runtime/classroom events、governance audits、plugin traces、transport attempts 与 consumer-facing traces 为一条时间线。health summary 只能基于最新 lifecycle state、最近 governance decision、transport attempt/result、consumer trace 完成度等 deterministic facts 计算，不得加入“超时”“异常频率”“建议告警”之类推测规则。权限上继续沿用 actor scope + school scope：教师只取自己相关 classroom/runtime session；管理员可看本校；开发者可看系统/开发范围。</action>
  <verify>
    <automated>pnpm test --run src/lib/dal/runtime-inspector.test.ts</automated>
  </verify>
  <done>inspector 已有可复用的服务端 read model，且 scope 与 deterministic health contract 被测试锁定。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 构建独立 inspector 页面与单条统一 timeline surface</name>
  <files>src/components/surfaces/runtime-inspector-surface.tsx, src/components/surfaces/runtime-inspector-surface.test.tsx, src/app/settings/labs/runtime-inspector/page.tsx</files>
  <behavior>
    - Test 1: 页面首屏是统一 timeline，不分 tabs，不以 plugin/classroom 先做顶层切换。
    - Test 2: 页面默认围绕 runtime session 展示 health、allowed/denied、transport trace、consumer trace。
  </behavior>
  <action>按 D-05、D-06、D-07 实现独立页面 `src/app/settings/labs/runtime-inspector/page.tsx`，保持它是 operator surface，而不是 `/classroom` 附属面板。`runtime-inspector-surface.tsx` 首屏必须是一条统一 timeline，可在同一轴上展示 runtime event、classroom event、governance audit、plugin trace、transport attempt、consumer-facing trace；允许顶部有轻量筛选和 deterministic health summary，但不能退化成 overview 卡片 + tabs。默认加载逻辑固定以 runtime session 为锚点；如无显式 query，展示用户当前可见范围内最近一条 runtime session。</action>
  <verify>
    <automated>pnpm test --run src/components/surfaces/runtime-inspector-surface.test.tsx</automated>
  </verify>
  <done>仓库已存在独立 inspector 页面，且产品 posture 与单条 timeline 设计被回归锁定。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| durable audit/transport/runtime facts -> inspector DTO | 若聚合层自行发明状态，会形成第二真相源 |
| scope resolver -> independent inspector page | 若新页面绕过 actor/school scope，会泄漏跨角色或跨学校 trace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-03-01 | I | inspector scope | mitigate | DAL 内按 D-10/D-11 强制 teacher/admin/developer 分层，并在测试里锁住跨学校不可见 |
| T-31-03-02 | T | health summary | mitigate | health 仅由 persisted lifecycle/governance/transport facts 聚合，禁止引入 prediction/threshold 字段 |
| T-31-03-03 | R | unified timeline | mitigate | timeline item 必带 truth reference、decision/reason、correlation 或 session anchor，确保链路可追溯 |

</threat_model>

<verification>
- `pnpm test --run src/lib/dal/runtime-inspector.test.ts src/components/surfaces/runtime-inspector-surface.test.tsx`
</verification>

<success_criteria>
- inspector 独立页面已可打开。
- timeline 默认按 runtime session 聚合并展示完整链路。
- health 与 scope 语义都由服务端 deterministic/read-model contract 控制。
</success_criteria>

<output>
After completion, create `.planning/phases/31-transport-boundary-and-runtime-inspector/31-03-SUMMARY.md`
</output>
