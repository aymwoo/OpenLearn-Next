---
phase: 30-capability-enforcement-and-plugin-lifecycle
plan: 04
type: execute
wave: 3
depends_on: [30-01, 30-02, 30-03]
files_modified:
  - src/db/schema.ts
  - src/lib/dal/plugins.ts
  - src/features/runtime-platform/host-actions/runtime-host.ts
  - src/features/runtime-platform/host-actions/plugin-host.ts
  - src/features/runtime-platform/host-actions/guards.test.ts
  - src/lib/dal/plugins.test.ts
  - src/features/runtime-platform/classroom/runtime-session.test.ts
  - scripts/verify-phase30-governance.ts
  - package.json
autonomous: true
requirements: [GOVR-01, GOVR-03]
must_haves:
  truths:
    - "D-16 / D-17：runtime 与 plugin 的 governance audit 都要统一记录 allowed 与 denied，且具备 actor、school、reason、lifecycle snapshot 与 correlation 级元数据。"
    - "D-18：无论是扩展现有 plugin audit 还是引入 shared governance audit root，最终都必须暴露统一 downstream query model。"
    - "D-19：本计划只交付 durable audit truth 与 verifier，不建设 inspector UI。"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "durable governance audit persistence for runtime and plugin decisions"
      contains: "audit"
    - path: "scripts/verify-phase30-governance.ts"
      provides: "canonical phase 30 verifier for capability, lifecycle, and audit drift"
      contains: "verify:phase30"
    - path: "package.json"
      provides: "phase 30 verifier entry"
      contains: "verify:phase30"
  key_links:
    - from: "src/features/runtime-platform/host-actions/runtime-host.ts"
      to: "src/db/schema.ts"
      via: "allowed/denied governance audit write"
      pattern: "correlationId"
    - from: "scripts/verify-phase30-governance.ts"
      to: "src/features/runtime-platform/host-actions/guards.test.ts"
      via: "focused governance regression suite"
      pattern: "pnpm"
---

<objective>
统一 runtime 与 plugin 的 allowed / denied audit semantics，并建立
`verify:phase30`，为 Phase 31 inspector 提供 durable truth。

Purpose: 把 capability、manifest、lifecycle 的治理结果沉淀成可查询、可验证的
统一审计模型，而不是让 runtime 与 plugin 继续各写各的日志语义。
Output: governance audit persistence, unified query shape, phase verifier.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-capability-enforcement-and-plugin-lifecycle/30-CONTEXT.md
@.planning/phases/30-capability-enforcement-and-plugin-lifecycle/30-RESEARCH.md
@.planning/phases/30-capability-enforcement-and-plugin-lifecycle/30-PATTERNS.md
@src/db/schema.ts
@src/lib/dal/plugins.ts
@src/features/runtime-platform/host-actions/runtime-host.ts
@src/features/runtime-platform/host-actions/plugin-host.ts
@scripts/verify-phase29-runtime-host.ts
@scripts/verify-phase28-runtime-bridge.ts
@package.json

<interfaces>
Current plugin audit shape:

```ts
pluginId
action
payloadJson
actorId
createdAt
```

Phase 30 audit needs to additionally expose decision, reason code, school,
lifecycle snapshot, capability or permission summary, and correlation metadata.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为 runtime 与 plugin 建立统一 governance audit 持久层</name>
  <read_first>
    - src/db/schema.ts
    - src/lib/dal/plugins.ts
    - src/features/runtime-platform/host-actions/runtime-host.ts
    - src/features/runtime-platform/host-actions/plugin-host.ts
  </read_first>
  <files>src/db/schema.ts, src/lib/dal/plugins.ts, src/features/runtime-platform/host-actions/runtime-host.ts, src/features/runtime-platform/host-actions/plugin-host.ts</files>
  <action>把 runtime 与 plugin 的 governance result 统一持久化。可扩展现有 `pluginActionAudits` / `pluginHookRuns` 或新增 shared governance audit 表，但最终必须有一条统一 query shape，能表达 allowed 与 denied 两类结果，并至少包含 `action`、decision、reason code、actorId、actorScope、schoolId、pluginId 或 runtimeId、runtimeSessionId 或 runtimeInstanceId、lifecycle snapshot、requested capabilities / required permission 摘要、correlationId、timestamp。runtime allowed path 不能继续只记成功事件；plugin denied path 也不能继续只把 reason 塞在松散 payload 里。</action>
  <acceptance_criteria>
    - runtime 与 plugin 都会写 governance audit，且同时覆盖 allowed 与 denied。
    - audit 结构拥有统一 query shape，而不是两套互不兼容 payload。
    - reason code、decision、lifecycle snapshot、correlationId 都是显式字段或稳定 DTO 字段。
    - 本计划不引入 inspector UI route 或 timeline surface。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/plugins.test.ts src/features/runtime-platform/classroom/runtime-session.test.ts src/features/runtime-platform/host-actions/guards.test.ts</automated>
  </verify>
  <done>governance audit 已成为统一 durable truth，Phase 31 inspector 有稳定的下游查询输入。</done>
</task>

<task type="auto">
  <name>Task 2: 建立 canonical `verify:phase30` governance gate</name>
  <read_first>
    - package.json
    - scripts/verify-phase28-runtime-bridge.ts
    - scripts/verify-phase29-runtime-host.ts
    - src/features/runtime-platform/host-actions/guards.test.ts
    - src/lib/dal/plugins.test.ts
  </read_first>
  <files>scripts/verify-phase30-governance.ts, package.json, src/features/runtime-platform/host-actions/guards.test.ts, src/lib/dal/plugins.test.ts, src/features/runtime-platform/classroom/runtime-session.test.ts</files>
  <action>新增 `scripts/verify-phase30-governance.ts` 并在 `package.json` 注册 `verify:phase30`。该 verifier 必须先做静态 drift guards：检查 `createGuardedHostAction` 仍是唯一 host entry、manifest v2 contract 存在且 local-only entry 仍被限制、canonical lifecycle vocabulary 包含 `failed`、runtime/plugin governance audit 同时记录 allowed 与 denied。随后组合运行 focused suites，至少覆盖 capability denied、lifecycle blocked、legacy manifest fallback、allowed audit write、denied audit write。输出需区分 capability drift、manifest drift、lifecycle drift、audit drift 四类失败标签。</action>
  <acceptance_criteria>
    - `package.json` 出现 `verify:phase30`。
    - verifier 包含静态 guard 与 focused tests 两部分，不只是转发单个测试命令。
    - verifier 输出含 capability / manifest / lifecycle / audit 四类 drift 标签或等价分类。
    - focused tests 能证明 allowed 与 denied 两类 audit 都被持久化。
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase30</automated>
  </verify>
  <done>Phase 30 拥有单一 canonical governance gate，可在进入 Phase 31 前持续保护治理语义不漂移。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| governance decision -> durable audit truth | 若 allowed/denied 未统一持久化，Phase 31 inspector 将失去可依赖的事实源 |
| phase verifier -> future refactors | 若缺少单一 verifier，manifest/lifecycle/audit drift 会在后续 phase 静默扩散 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-30-04-01 | R | allowed path audit | mitigate | runtime 与 plugin 的 allowed decision 都写 durable audit，不再只记成功副作用 |
| T-30-04-02 | R | denied path semantics | mitigate | denied audit 必须带 decision、reason、lifecycle snapshot 与 correlation metadata |
| T-30-04-03 | T | governance drift | mitigate | `verify:phase30` 同时守 capability、manifest、lifecycle、audit 四类 drift |

</threat_model>

<verification>
- `pnpm test --run src/features/runtime-platform/host-actions/guards.test.ts src/lib/dal/plugins.test.ts src/features/runtime-platform/classroom/runtime-session.test.ts`
- `pnpm verify:phase30`
- 读取 `scripts/verify-phase30-governance.ts`，确认包含 static guards 与 focused governance suites
</verification>

<success_criteria>
- runtime 与 plugin 都拥有统一的 allowed / denied governance audit 语义。
- durable audit truth 足够支撑 Phase 31 inspector，而无需先实现 UI。
- 仓库存在单一 `verify:phase30` 入口保护治理语义。
</success_criteria>

<output>
After completion, create `.planning/phases/30-capability-enforcement-and-plugin-lifecycle/30-04-SUMMARY.md`
</output>
