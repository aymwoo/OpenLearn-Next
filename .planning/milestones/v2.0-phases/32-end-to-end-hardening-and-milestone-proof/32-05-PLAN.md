---
phase: 32-end-to-end-hardening-and-milestone-proof
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/runtime-platform/host/runtime-host-client.tsx
  - src/app/runtime/html-courseware/pilot/page.tsx
  - src/features/runtime-platform/host/runtime-host.test.tsx
autonomous: true
requirements: [RHOST-04]
gap_closure: true
must_haves:
  truths:
    - "学生打开 seeded runtime step 后，不会再卡在『runtime bootstrap 已返回，等待 iframe ready。』，而是能继续进入真实互动与 submit terminal state。"
    - "per D-03、D-05、D-06，proof runtime 的首次 ready 握手在 bootstrap 前也能被宿主接受，但 submit 成功后的锁定终态与摘要语义保持不变。"
  artifacts:
    - path: "src/features/runtime-platform/host/runtime-host-client.tsx"
      provides: "pre-bootstrap iframe ready handshake acceptance with strict non-ready instance filtering"
    - path: "src/app/runtime/html-courseware/pilot/page.tsx"
      provides: "placeholder ready handshake contract before final runtimeInstanceId arrives"
    - path: "src/features/runtime-platform/host/runtime-host.test.tsx"
      provides: "regression coverage for pre-bootstrap ready handshake and non-ready filtering"
  key_links:
    - from: "src/app/runtime/html-courseware/pilot/page.tsx"
      to: "src/features/runtime-platform/host/runtime-host-client.tsx"
      via: "runtime-frame-ready with placeholder runtimeInstanceId before bootstrap"
      pattern: "runtime-frame-ready"
    - from: "src/features/runtime-platform/host/runtime-host-client.tsx"
      to: "submitRuntimeStateAction"
      via: "host only reaches submit path after frameReady and bootstrap are both established"
      pattern: "submitRuntimeStateAction"
---

<objective>
关闭 UAT Test 1 的 runtime deadlock：让宿主在 bootstrap 前接受 iframe 首次
`runtime-frame-ready` 握手，同时保持其余 runtime 消息仍按最终
`runtimeInstanceId` 严格过滤。

Purpose: 这条链路不通，Phase 32 的 canonical proof 无法进入真实互动、提交与
terminal success，D-03 的可重复 proof 也无法成立。
Output: 预 bootstrap ready 握手修复、pilot 占位 id 合同锁定、focused regression。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-HUMAN-UAT.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-CONTEXT.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-RESEARCH.md
@.planning/debug/runtime-bootstrap-waits-for-iframe-ready.md
@src/features/runtime-platform/host/runtime-host-client.tsx
@src/app/runtime/html-courseware/pilot/page.tsx
@src/features/runtime-platform/host/runtime-host.test.tsx

<interfaces>
From `src/features/runtime-platform/host/runtime-host-client.tsx`:
```ts
const parsedMessage = parseRuntimeBridgeMessage(event.data)
if (parsedMessage.kind === "runtime-frame-ready") {
  setFrameReady(true)
  return
}
const message = isRuntimeBridgeMessageForInstance(parsedMessage, runtimeInstanceIdRef.current)
```

From `src/app/runtime/html-courseware/pilot/page.tsx`:
```ts
postToHost({
  channel: RUNTIME_HOST_BRIDGE_CHANNEL,
  kind: "runtime-frame-ready",
  runtimeInstanceId: "runtime-pilot-pending",
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 接受 pre-bootstrap iframe ready，但不放宽其他 runtime 消息过滤</name>
  <files>src/features/runtime-platform/host/runtime-host-client.tsx, src/features/runtime-platform/host/runtime-host.test.tsx</files>
  <behavior>
    - Test 1: `runtime-frame-ready` 在最终 `runtimeInstanceId` 未同步前也会让 `frameReady` 成立。
    - Test 2: 除 `runtime-frame-ready` 外，其余 `runtime-ready`、`runtime-interaction`、`runtime-save`、`runtime-submit` 仍必须通过 `isRuntimeBridgeMessageForInstance(...)` 过滤。
    - Test 3: bootstrap 成功后宿主会从 waiting-ready 进入真实 ready/submit 链路，而不是永久停留在 loading copy。
  </behavior>
  <action>按 D-03、D-05、D-06 修改 `runtime-host-client.tsx` 的消息处理顺序：只对 `runtime-frame-ready` 提前接收并置 `frameReady=true`，其余 bridge message 继续使用最终 `runtimeInstanceId` 过滤，避免把 fix 扩成全局放宽。不要删除 `isRuntimeBridgeMessageForInstance(...)`，不要把 runtime host 改成接受任意实例的 save/submit/interact。同步补强 `runtime-host.test.tsx`，锁住“pre-bootstrap ready 可接受 + non-ready message 仍严格按实例过滤”的双重语义。</action>
  <verify>
    <automated>pnpm test --run src/features/runtime-platform/host/runtime-host.test.tsx</automated>
  </verify>
  <done>宿主不会再因为首个 ready 使用占位 id 而卡死，同时 runtime save/submit 的实例隔离边界保持原样。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 固定 pilot 首次 ready 协议与回归文案，避免 placeholder drift</name>
  <files>src/app/runtime/html-courseware/pilot/page.tsx, src/features/runtime-platform/host/runtime-host.test.tsx</files>
  <behavior>
    - Test 1: pilot 在 mount 时继续发送一次 `runtime-frame-ready`，并明确使用 `runtime-pilot-pending` 占位 id。
    - Test 2: pilot 只在收到 `runtime-bootstrap` 后切换到最终 `runtimeInstanceId`，不提前假设宿主实例 id。
    - Test 3: pilot 仍只通过 browser `postMessage` bridge 与宿主通信，不新增 DB/API 直连。
  </behavior>
  <action>按 UAT root cause 和 debug session 保持 `page.tsx` 的 pre-bootstrap ready contract 显式可见：首次 ready 必须继续带 `runtime-pilot-pending`，直到 `runtime-bootstrap` 到达才切换到最终实例 id。不要把 pilot 改成等待 bootstrap 后才发送 ready，那会重新引入宿主/iframe 死锁；也不要新增 API route、DAL import 或 fetch 旁路。把该占位协议写进 `runtime-host.test.tsx` 的 focused regression，防止后续重构误删。</action>
  <verify>
    <automated>pnpm test --run src/features/runtime-platform/host/runtime-host.test.tsx</automated>
  </verify>
  <done>iframe-ready 协议对 executor 与后续 verifier 都是稳定、可 grep、可回归的 contract，canonical proof 可继续进入互动与终态提交。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| iframe -> shared host bridge | 浏览器内未受信 iframe 首次 ready 握手进入宿主消息处理 |
| shared host -> runtime submit path | 若实例过滤被误放宽，错误 iframe 可能污染当前课堂 session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-05-01 | S | runtime bridge handshake | mitigate | 只提前接受 `runtime-frame-ready`，其余消息继续走 `isRuntimeBridgeMessageForInstance(...)` |
| T-32-05-02 | T | runtime-host-client.tsx | mitigate | 禁止删除实例级过滤或把 save/submit 也放宽到 pre-bootstrap 阶段 |
| T-32-05-03 | D | pilot iframe bootstrap | mitigate | 保持 `runtime-pilot-pending` 协议与 focused regression，避免宿主/iframe 死锁回归 |
</threat_model>

<verification>
- `pnpm test --run src/features/runtime-platform/host/runtime-host.test.tsx`
</verification>

<success_criteria>
- UAT Test 1 的“等待 iframe ready”卡死根因被修复。
- pre-bootstrap ready 被接受，但 save/submit/interact 仍保持实例级隔离。
- canonical proof 可从 bootstrap 继续走到真实互动与 submit terminal posture。
</success_criteria>

<output>
After completion, create `.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-05-SUMMARY.md`
</output>
