---
phase: 61-ai-provider-abstraction-layer
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - src/server/ai/providers/facade.ts
  - src/server/ai/providers/facade.test.ts
  - src/server/ai/providers/index.ts
  - src/server/ai/providers/no-leak.test.ts
autonomous: true
requirements: [PROV-01, PROV-02]

must_haves:
  truths:
    - "aiGenerateText / aiGenerateObject 是教师侧调用的唯一入口：先限流→装配模型→调 AI SDK→错误归一"
    - "aiGenerateObject 用传入 Zod schema 校验，校验失败归一为 ProviderParseError"
    - "任一上游/解析/限流错误经 mapProviderError 归一后抛出（不裸抛 AI SDK 原生错误）"
    - "barrel index.ts 只 re-export 公共面（generate 入口 + typed errors），不导出 config/registry 内部"
    - "无任何 Edge/proxy/client/plugin 模块 import @/server/ai/providers，且返回面不含 apiKey"
  artifacts:
    - path: "src/server/ai/providers/facade.ts"
      provides: "限流+装配+生成+错误映射的编排入口（PROV-01 调用半边）"
      exports: ["aiGenerateText", "aiGenerateObject"]
    - path: "src/server/ai/providers/index.ts"
      provides: "公共 barrel（generate 入口 + typed error 类型）"
      exports: ["aiGenerateText", "aiGenerateObject", "ProviderError"]
  key_links:
    - from: "src/server/ai/providers/facade.ts"
      to: "rate-limit.ts / registry.ts / error-mapping.ts"
      via: "enforceRateLimit → getLanguageModel → generateText|generateObject → mapProviderError"
      pattern: "enforceRateLimit|getLanguageModel|mapProviderError"
    - from: "src/server/ai/providers/facade.ts"
      to: "ai (generateText, generateObject)"
      via: "把 LanguageModel + prompt/schema 喂给 AI SDK"
      pattern: "generateObject|generateText"
---

<objective>
PROV-01（编排半边）+ PROV-02（可证明不泄漏）：facade 把 Wave 1/2 的零件串成教师侧唯一入口 —— 先 enforceRateLimit、再 getLanguageModel、调 AI SDK generate、最后 mapProviderError 归一。barrel 收口公共面，no-leak.test.ts 静态证明密钥/模块不进 client/edge。

Purpose: 给 Phase 62 一个干净、限流过、错误已归一、零泄漏的 AI 调用门面。
Output: `facade.ts`、`index.ts`、`facade.test.ts`、`no-leak.test.ts`，全绿。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-ai-provider-abstraction-layer/61-RESEARCH.md
@.planning/phases/61-ai-provider-abstraction-layer/61-PATTERNS.md
@.planning/phases/61-ai-provider-abstraction-layer/61-VALIDATION.md

# barrel 收口 analog
@src/features/runtime-platform/seams/index.ts

<interfaces>
<!-- Plan 01 -->: import { mapProviderError } from "./error-mapping"; import type { ProviderError } from "./errors";
<!-- Plan 02 -->: import { enforceRateLimit } from "./rate-limit";
<!-- Plan 03 -->: import { getLanguageModel } from "./registry";
<!-- AI SDK (ai@6) -->: import { generateText, generateObject } from "ai";
//   generateObject({ model, schema(zod), prompt }) → { object }
//   generateText({ model, prompt }) → { text }

<!-- facade 入口签名（建议）: -->
//   aiGenerateText({ teacherId, prompt, modelId? }) → Promise<string>
//   aiGenerateObject<T>({ teacherId, prompt, schema: ZodSchema<T>, modelId? }) → Promise<T>
//   本 phase 不含 prompt 内容逻辑（D-11 → Phase 62）；prompt 由调用方传入。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: facade.ts + index.ts — 限流→装配→生成→错误映射 编排入口（PROV-01）</name>
  <files>src/server/ai/providers/facade.ts, src/server/ai/providers/index.ts, src/server/ai/providers/facade.test.ts</files>
  <behavior>
    - Test 1: aiGenerateText 成功路径：调用顺序 enforceRateLimit → getLanguageModel → generateText，返回 result.text。
    - Test 2: aiGenerateObject 成功路径：把传入 zod schema 透传给 generateObject，返回 result.object。
    - Test 3: 限流命中（enforceRateLimit 抛 ProviderRateLimitError）→ facade 直接抛该错误，**不调用 getLanguageModel/generate**（断言 generate 未被调用 — retry-amplification 防护）。
    - Test 4: generateObject 抛 NoObjectGeneratedError → 经 mapProviderError 抛 ProviderParseError（不裸抛原生错误）。
    - Test 5: generateText 抛 APICallError(503,retryable) → 抛 ProviderUpstreamError(retryable true)。
    - Test 6: index.ts re-export 含 aiGenerateText/aiGenerateObject 与 ProviderError 类型，不含 getProviderConfig/getLanguageModel。
  </behavior>
  <action>
    创建 `facade.ts`，首行 `import "server-only";`。import `generateText, generateObject` from "ai"；`enforceRateLimit`、`getLanguageModel`、`mapProviderError`、error 类。
    `aiGenerateText({ teacherId, prompt, modelId? })`：`await enforceRateLimit(teacherId)`（先于一切，限流抛错则直接冒泡，**不进入 generate** → 防 retry 放大）；`const model = getLanguageModel(modelId)`；`try { const { text } = await generateText({ model, prompt }); return text; } catch (e) { throw mapProviderError(e); }`。
    `aiGenerateObject({ teacherId, prompt, schema, modelId? })`：同结构，`generateObject({ model, schema, prompt })` 返回 `object`，catch → mapProviderError。**禁止内层 retry**（D：AI SDK maxRetries 用默认/或显式设小；retry-amplification 威胁）—— 显式 `maxRetries: 2` 上限，注释说明不叠加自定义重试。
    `index.ts` barrel：`export { aiGenerateText, aiGenerateObject } from "./facade";` `export type { ProviderError } from "./errors";` `export { ProviderRateLimitError, ProviderUpstreamError, ProviderParseError, ProviderTimeoutError } from "./errors";`（供调用方判型）。**不** re-export config/registry/rate-limit/redis-client。
    `facade.test.ts`：`vi.mock("server-only")`、`vi.mock("ai")`（generateText/generateObject = vi.fn）、`vi.mock("./rate-limit")`、`vi.mock("./registry")`；用 `vi.fn` 断言调用顺序与「限流命中时 generate 未被调用」。
  </action>
  <verify>
    <automated>pnpm vitest run src/server/ai/providers/facade.test.ts</automated>
  </verify>
  <done>facade.test.ts 全绿：两入口成功路径、限流短路不触发 generate、错误经 mapProviderError 归一、index 导出面正确。</done>
</task>

<task type="auto">
  <name>Task 2: no-leak.test.ts — 静态证明无泄漏 / 无错误边界 import（PROV-02 / T-61-key-leak）</name>
  <files>src/server/ai/providers/no-leak.test.ts</files>
  <action>
    创建 `no-leak.test.ts`（纯静态分析，不执行 provider 代码）。两组断言：
    **A. import-graph 边界（无 client/edge/plugin 引用 provider）**：用 fast-glob/fs 读取仓库源码，扫描以下文件集合的 import：① 任何含 `"use client"` 指令的文件；② `src/proxy.ts` 与所有 `export const runtime = "edge"` 的 route；③ `src/features/**/plugins/**`（插件系统）。断言这些文件**均不** import `@/server/ai/providers`（或相对路径指向该目录）。用 grep/正则匹配 import 行；命中即 fail 并打印文件名。
    **B. 返回面无凭证**：动态构造一个成功调用的返回值样本（mock ai + 固定 config），对 aiGenerateText/aiGenerateObject 的返回值做深度遍历（walk 所有嵌套 key/value），断言不含 apiKey 值、不含 "Authorization"/"sk-" 前缀字符串、不含 baseURL 里的密钥段。
    **grep gate hygiene**：统计 import 命中用 `grep -v '^#' | grep -c`，避免注释/字符串误计。
    参考 VALIDATION.md 的 no-leak 策略段落落实具体匹配清单。
  </action>
  <verify>
    <automated>pnpm vitest run src/server/ai/providers/no-leak.test.ts</automated>
  </verify>
  <done>no-leak.test.ts 全绿：client/edge/plugin 零 import provider；facade 返回面深walk 无 apiKey/Authorization/sk- 泄漏。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 教师调用方（Phase 62 RSC/Server Action）→ facade | 唯一公共入口；teacherId/prompt/schema 为输入，schema 经 zod 校验，错误经归一后外发 |
| server-only provider 模块 → client/edge bundle | 编译期 server-only + 静态 import-graph 双重隔离，确保 apiKey 永不进入浏览器/边缘运行时 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-key-leak | Information Disclosure | facade.ts / index.ts / no-leak.test.ts | mitigate | barrel 不导出 config/registry；facade 返回纯 text/object；no-leak.test.ts A 组静态断言 client/edge/plugin 不 import provider，B 组深walk 返回面无凭证（PROV-02 可证明手段） |
| T-61-retry-amplification | Denial of Service | facade.ts | mitigate | 限流在 generate 之前短路（Test 3：命中时 generate 不被调用）；AI SDK `maxRetries` 显式封顶（≤2），不叠加自定义重试，避免单次教师请求放大为多次上游+多次计数 |
| T-61-dos | Denial of Service | facade.ts | mitigate | 每次入口必经 enforceRateLimit（teacher+global），无绕过路径 |
</threat_model>

<verification>
- `pnpm vitest run src/server/ai/providers` 全套 6 个 test 文件全绿。
- `pnpm typecheck && pnpm lint` 通过。
- `pnpm vitest run` 全仓回归无回退。
</verification>

<success_criteria>
- aiGenerateText/aiGenerateObject 为唯一入口，依「限流→装配→生成→归一」顺序执行。
- aiGenerateObject 用 zod schema 校验，失败归一为 ProviderParseError。
- 限流命中短路，不触发上游调用（retry-amplification 防护）。
- index barrel 仅暴露公共面；no-leak 静态证明零泄漏、零边界 import。
</success_criteria>

<output>
完成后创建 `.planning/phases/61-ai-provider-abstraction-layer/61-04-SUMMARY.md`，记录：公共入口签名、index 导出清单、no-leak 校验覆盖的文件集合，供 Phase 62 接入。
</output>
