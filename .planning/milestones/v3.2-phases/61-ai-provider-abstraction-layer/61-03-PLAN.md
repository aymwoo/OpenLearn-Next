---
phase: 61-ai-provider-abstraction-layer
plan: 03
type: execute
wave: 2
depends_on: [00, 01]
files_modified:
  - src/server/ai/providers/registry.ts
  - src/server/ai/providers/registry.test.ts
autonomous: true
requirements: [PROV-01]

must_haves:
  truths:
    - "registry 用 createOpenAICompatible + config 装配出可调用的语言模型，按 modelId 解析"
    - "模型实例被 memoize（同一 config 不重复 new provider），但可在 test 间重置"
    - "registry 只导出 getLanguageModel，不导出 apiKey/原始 provider 客户端"
  artifacts:
    - path: "src/server/ai/providers/registry.ts"
      provides: "config → OpenAI-compatible provider → LanguageModel 的装配收口"
      exports: ["getLanguageModel"]
  key_links:
    - from: "src/server/ai/providers/registry.ts"
      to: "@ai-sdk/openai-compatible (createOpenAICompatible)"
      via: "用 config.baseURL/apiKey/providerName 构造 provider"
      pattern: "createOpenAICompatible"
    - from: "src/server/ai/providers/registry.ts"
      to: "src/server/ai/providers/config.ts"
      via: "getProviderConfig() 取装配参数"
      pattern: "getProviderConfig"
---

<objective>
PROV-01（装配半边）：把 env config 装配成 AI SDK 可用的 LanguageModel，对外只暴露 `getLanguageModel(modelId?)`。这是 facade 之下的「模型工厂」，与 facade（编排 generate/限流/错误映射）分层，仿 seams 的 contract/adapter 切分。

Purpose: 收口 `createOpenAICompatible` 的唯一调用点；禁用 `@ai-sdk/openai`（D-02），保证可换 provider。
Output: `registry.ts` + 测试，全绿。

> **范围说明（对齐 D-03 / PATTERNS）**：D-03 描述的是「多 provider 注册 + 运行时选择」的可扩展注册表，PATTERNS §registry.ts 给出 `AiProviderAdapter` 接口 + `Map` + `setProvider`/`__resetRegistry` 的完整 seam。本 plan 按 CONTEXT「N=1 强样板优先，不过度设计」的 Discretion 条款，**刻意**收敛为单 provider 的 `getLanguageModel(modelId?)` + 模块级 memoize，测试注入走 `vi.mock("@ai-sdk/openai-compatible")` + `vi.mock("./config")`。load-bearing 的 PROV-01 判据（换 provider 调用方不变）仍成立：换 provider 只改 `registry.ts` 装配点。这是有意简化而非范围削减；待真有 N>1 需求时，再把 `getLanguageModel` 升级为 Map-based registry（向后兼容）。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-ai-provider-abstraction-layer/61-RESEARCH.md
@.planning/phases/61-ai-provider-abstraction-layer/61-PATTERNS.md

# contract/adapter 分层 analog
@src/features/runtime-platform/seams/index.ts

<interfaces>
<!-- 来自 Plan 01: -->
import { getProviderConfig } from "./config"; // → { baseURL, apiKey, modelId, providerName }

<!-- 装配 API（RESEARCH lines 240-260）: -->
import { createOpenAICompatible } from "@ai-sdk/openai-compatible";
const provider = createOpenAICompatible({ name, baseURL, apiKey });
const model = provider(modelId); // → LanguageModelV2，喂给 ai 的 generateText/generateObject

<!-- Wave 0 不 mock 真实网络；registry.test.ts mock "@ai-sdk/openai-compatible" 与 "./config" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: registry.ts — config → OpenAI-compatible LanguageModel 装配</name>
  <files>src/server/ai/providers/registry.ts, src/server/ai/providers/registry.test.ts</files>
  <behavior>
    - Test 1: getLanguageModel() 用 getProviderConfig() 的 baseURL/apiKey/name 调 createOpenAICompatible 一次，并用 config.modelId 调 provider(modelId)，返回该 model。
    - Test 2: 传入显式 modelId 覆盖 config 默认（provider(传入值)）。
    - Test 3: 连续两次 getLanguageModel() 复用同一 provider（createOpenAICompatible 只被调一次）；resetModules 后重新构造。
    - Test 4（PROV-02 面）：registry 导出表只含 getLanguageModel，不导出 apiKey/provider 客户端。
  </behavior>
  <action>
    创建 `registry.ts`，首行 `import "server-only";`。`import { createOpenAICompatible } from "@ai-sdk/openai-compatible";` `import { getProviderConfig } from "./config";`。模块级 memoize provider（`let provider | undefined`，仿 connection 单例但同步）：
    ```
    let cachedProvider: ReturnType<typeof createOpenAICompatible> | undefined;
    function provider() {
      if (!cachedProvider) {
        const { baseURL, apiKey, providerName } = getProviderConfig();
        cachedProvider = createOpenAICompatible({ name: providerName, baseURL, apiKey });
      }
      return cachedProvider;
    }
    export function getLanguageModel(modelId?: string) {
      const id = modelId ?? getProviderConfig().modelId;
      return provider()(id);
    }
    ```
    **禁止** import `@ai-sdk/openai`（D-02）。registry.test.ts：`vi.mock("server-only")`、`vi.mock("@ai-sdk/openai-compatible", () => ({ createOpenAICompatible: vi.fn(() => vi.fn((id)=>({ modelId:id }))) }))`、`vi.mock("./config")` 返回固定 config；用 `vi.resetModules()` + 动态 import。
  </action>
  <verify>
    <automated>pnpm vitest run src/server/ai/providers/registry.test.ts</automated>
  </verify>
  <done>registry.test.ts 全绿：装配调用正确、modelId 可覆盖、provider memoize、导出面仅 getLanguageModel。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config（含 apiKey）→ AI SDK provider | apiKey 只在 createOpenAICompatible 调用点被消费，不再外传；registry 返回的是 LanguageModel handle 而非凭证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-key-leak | Information Disclosure | registry.ts | mitigate | `import "server-only"`；apiKey 仅传入 createOpenAICompatible，不出现在导出/返回值；Test 4 断言导出面仅 getLanguageModel；no-leak.test.ts（Plan 04）再做 import-graph 静态校验 |
</threat_model>

<verification>
- `pnpm vitest run src/server/ai/providers/registry.test.ts` 全绿。
- `grep -v '^#' src/server/ai/providers/registry.ts | grep -c "@ai-sdk/openai\"" ` 为 0（确认未引禁用包 `@ai-sdk/openai`，注意尾引号区分 openai-compatible）。
</verification>

<success_criteria>
- getLanguageModel 用 config + createOpenAICompatible 产出可调用 LanguageModel，modelId 可覆盖。
- 未引入 `@ai-sdk/openai`；provider 被 memoize。
- 导出面仅 getLanguageModel。
</success_criteria>

<output>
完成后创建 `.planning/phases/61-ai-provider-abstraction-layer/61-03-SUMMARY.md`，记录 getLanguageModel 签名与装配链，供 Plan 04 facade 调用。
</output>
