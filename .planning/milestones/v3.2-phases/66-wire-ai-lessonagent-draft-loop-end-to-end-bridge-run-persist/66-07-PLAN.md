---
phase: 66-wire-ai-lessonagent-draft-loop-end-to-end-bridge-run-persist
plan: 07
type: execute
wave: 3
depends_on: [01, 03, 04]
files_modified:
  - src/server/ai/agents/lesson-draft-loop.e2e.test.ts
autonomous: true
requirements: [DRAFT-01, AGENT-03, REVIEW-01, REVIEW-03]
must_haves:
  truths:
    - "The assembled loop is exercised by one automated spec, not just isolated seams"
    - "Flag-enabled (test-DB fixture) → draft server action → run+persist both dispatched → a draft row lands in draftLessonVersions"
    - "Accept via Command Bus emits lesson.draft.accepted carrying version >= 1 (never 0) and the draft enters the publish chain"
  artifacts:
    - path: "src/server/ai/agents/lesson-draft-loop.e2e.test.ts"
      provides: "end-to-end closure integration spec for the full draft loop"
      contains: "toBeGreaterThanOrEqual(1)"
  key_links:
    - from: "draftLessonWithAgentAction (flag-enabled)"
      to: "draftLessonVersions row + lesson.draft.accepted event (version >= 1)"
      via: "run→persist→accept Command Bus chain in one spec"
      pattern: "lesson\\.draft\\.accepted"
---

<objective>
Add the missing end-to-end closure spec. Every seam in this phase is integration-tested in
isolation (run→persist in 66-02, flag gate in 66-03, accept rewire in 66-04, version fix in
66-01) but NOTHING exercises the assembled loop. VALIDATION.md flags
"flag → trigger → run+persist → review → accept via command → publish" as the audit's core
delivery assertion (Wave 0, marked ❌). This plan asserts that whole chain once.

This is a final verification plan gated behind the three seams it composes: the version fix
(66-01), the draft server action (66-03), and the accept rewire (66-04). It can run with or
before the 66-05 human-verify checkpoint. It adds NO production code — only a test that drives
the already-wired loop and asserts the closure invariants.

Purpose: closes the audit's flows 0/1 core gap — proves the loop is reachable in one assembled
path and that the accepted event carries a real version (>= 1, never the old hardcoded 0).
Output: NEW `src/server/ai/agents/lesson-draft-loop.e2e.test.ts`.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/66-wire-ai-lessonagent-draft-loop-end-to-end-bridge-run-persist/66-CONTEXT.md
@.planning/phases/66-wire-ai-lessonagent-draft-loop-end-to-end-bridge-run-persist/66-VALIDATION.md
@.planning/phases/66-wire-ai-lessonagent-draft-loop-end-to-end-bridge-run-persist/66-PATTERNS.md

<read_first>
<!-- The executor MUST read these before writing the spec — they define the assembled chain. -->
- src/actions/lesson-agent-actions.ts — `draftLessonWithAgentAction` (created in 66-03): flag gate via
  getAgentRegistryDTO, then draftLessonStep (run→persist closure from 66-02).
- src/server/ai/agents/lesson-agent.ts — `draftLessonStep`: dispatches lesson.draft.run then
  lesson.draft.persist; accepts a `deps` injection point (store + persistPlatformEvents +
  publicationPort) reused by the existing in-memory test harness in lesson-agent.test.ts.
- src/server/ai/agents/lesson-agent.test.ts — copy its in-memory deps/store harness + command
  capture pattern; this spec reuses the same injection approach.
- src/actions/lesson-agent-actions.test.ts (created in 66-03) — how the flag-ON registry row is
  injected via the test DB / mock; reuse the SAME fixture style (direct-write agentRegistry row
  enabled=true; NEVER flip the production seed default — see 66-CONTEXT D-03b).
- src/features/platform-core/commands/handlers/lesson-draft.ts — accept handler now emits
  lesson.draft.accepted with `version: result.version` (66-01); resultSummary re-carries courseId.
- src/features/platform-core/commands/handlers/lesson-draft.events.test.ts — assertion style for the
  accepted event payload version.
- src/actions/lesson-authoring-actions.draft-routing.test.ts (66-04) — how the accept action dispatches
  lesson.draft.accept via the Command Bus; reuse its dispatch capture pattern for the accept leg.
</read_first>

<interfaces>
Assembled chain under test (all already wired by 66-01/02/03/04):
```
draftLessonWithAgentAction({ lessonId, stepType, intent })   // 66-03, flag-gated
  → draftLessonStep(...)                                       // 66-02
    → dispatch lesson.draft.run  → produces step
    → dispatch lesson.draft.persist → writes draftLessonVersions row (version >= 1)
applyDraftLessonVersionAction({ lessonId, draftVersionId })   // 66-04
  → dispatch lesson.draft.accept → emits lesson.draft.accepted { version: result.version }  // 66-01
  → draft enters publish chain (applyDraftToLiveLesson / publishLesson reuse)
```
Flag fixture: agentRegistry LessonAgent row `enabled=true` written directly into the test DB (or
mocked getAgentRegistryDTO) — do NOT change the seed default (66-CONTEXT D-03b).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: End-to-end draft-loop closure spec</name>
  <files>src/server/ai/agents/lesson-draft-loop.e2e.test.ts</files>
  <read_first>
    - src/server/ai/agents/lesson-agent.test.ts (in-memory deps/store + command capture harness)
    - src/actions/lesson-agent-actions.test.ts (flag-ON registry fixture pattern)
    - src/actions/lesson-authoring-actions.draft-routing.test.ts (accept dispatch capture)
    - src/features/platform-core/commands/handlers/lesson-draft.events.test.ts (accepted version assertion)
  </read_first>
  <behavior>
    - GIVEN agentRegistry LessonAgent row enabled=true (test-DB fixture / mocked getAgentRegistryDTO,
      NOT a seed-default flip): the draft server action proceeds (no AGENT_DISABLED)
    - WHEN draftLessonWithAgentAction is invoked with a valid { lessonId, stepType, intent }: BOTH
      lesson.draft.run AND lesson.draft.persist are dispatched (captured from the shared deps/store),
      and a draft row lands in draftLessonVersions with version >= 1
    - WHEN the resulting draftVersionId is accepted via applyDraftLessonVersionAction: a
      lesson.draft.accept command is dispatched through the Command Bus AND a lesson.draft.accepted
      event is emitted whose payload.version is a positive integer (>= 1, never 0)
    - AND the accepted draft enters the publish chain (applyDraftToLiveLesson invoked / publish path
      reached — assert via the captured DAL/handler call or resultSummary, not a second source of truth)
    - control assertion: this single spec composes the seams end-to-end (one test file, one assembled run)
  </behavior>
  <action>Create lesson-draft-loop.e2e.test.ts. Reuse the in-memory deps/store + command-capture
  harness from lesson-agent.test.ts and the flag-ON registry fixture from lesson-agent-actions.test.ts
  (direct-write agentRegistry enabled=true OR mock getAgentRegistryDTO → enabled:true; document that the
  seed default stays enabled=false per D-03b). Drive the chain: (1) call draftLessonWithAgentAction;
  assert both run+persist dispatched and a draftLessonVersions row exists with version >= 1; (2) take the
  produced draftVersionId and call applyDraftLessonVersionAction; assert a lesson.draft.accept command was
  dispatched and the lesson.draft.accepted event payload.version `toBeGreaterThanOrEqual(1)` (and `not.toBe(0)`);
  (3) assert the accept reaches the publish chain (applyDraftToLiveLesson called / publish path captured).
  Keep teacherId/source out of payloads (handler injects). Mock provider/LLM output deterministically so a
  known single step is produced. Do NOT add or change production code — drive only the already-wired path.</action>
  <verify>
    <automated>npx vitest run src/server/ai/agents/lesson-draft-loop.e2e.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/server/ai/agents/lesson-draft-loop.e2e.test.ts` exits 0
    - `grep -c "toBeGreaterThanOrEqual(1)" src/server/ai/agents/lesson-draft-loop.e2e.test.ts` >= 1
    - `grep -c "lesson.draft.persist" src/server/ai/agents/lesson-draft-loop.e2e.test.ts` >= 1
    - `grep -c "lesson.draft.accept" src/server/ai/agents/lesson-draft-loop.e2e.test.ts` >= 1
    - spec asserts the accepted event version `not.toBe(0)`
  </acceptance_criteria>
  <done>One spec drives flag-enabled → run+persist (draft row, version >= 1) → accept via command
  (accepted version >= 1, never 0) → publish chain; spec green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → assembled command chain | the spec drives the production loop end-to-end with injected deps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-18 | Tampering | accepted event version regression | mitigate | spec asserts payload.version >= 1 / not 0, locking the 66-01 fix against future regression |
| T-66-19 | EoP | flag-bypass regression | mitigate | spec exercises the flag-ON path via test-DB fixture only; the AGENT_DISABLED hard-stop remains covered by 66-03 |
| T-66-20 | Repudiation | broken loop provenance | mitigate | end-to-end correlation (run→persist→accept) asserted in one path, restoring auditable closure |
</threat_model>

<verification>
- `npx vitest run src/server/ai/agents/lesson-draft-loop.e2e.test.ts` green
- full gate after wave: `pnpm test run`
- spec composes all seams (run+persist+accept+publish) in a single assembled path
</verification>

<success_criteria>
The assembled draft loop is exercised by one automated closure spec: flag-enabled → run+persist
(draft row, version >= 1) → accept via Command Bus (accepted version >= 1, never 0) → publish chain.
No production code changed — the loop's reachability is now asserted, closing the audit's flows 0/1 gap.
</success_criteria>

<output>
After completion, create `.planning/phases/66-wire-ai-lessonagent-draft-loop-end-to-end-bridge-run-persist/66-07-SUMMARY.md`
</output>
