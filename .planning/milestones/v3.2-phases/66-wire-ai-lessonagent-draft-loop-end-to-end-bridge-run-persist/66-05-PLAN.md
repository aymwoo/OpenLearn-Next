---
phase: 66-wire-ai-lessonagent-draft-loop-end-to-end-bridge-run-persist
plan: 05
type: execute
wave: 3
depends_on: [03]
files_modified:
  - src/components/authoring/lesson-authoring-workspace.tsx
autonomous: false
requirements: [DRAFT-01]
must_haves:
  truths:
    - "A teacher sees an 「AI 起草」 trigger in the Flow-main header action row"
    - "Activating it opens an inline panel with a stepType segmented control (内容/任务/测验) + intent text + 生成草稿"
    - "Submitting calls draftLessonWithAgentAction; success surfaces feedback, failure surfaces the error"
    - "When lesson_agent_enabled is OFF (server-provided), the trigger is hidden"
  artifacts:
    - path: "src/components/authoring/lesson-authoring-workspace.tsx"
      provides: "AI draft trigger button + inline intent panel"
      contains: "draftLessonWithAgentAction"
  key_links:
    - from: "AI 起草 panel submit"
      to: "draftLessonWithAgentAction"
      via: "server action call with { lessonId, stepType, intent }"
      pattern: "draftLessonWithAgentAction"
---

<objective>
Add the teacher-facing 「AI 起草」 trigger to the lesson authoring workspace per 66-UI-SPEC (locked
decision D-02 frontend). A primary gradient button with a `Sparkles` icon sits in the existing
Flow-main header action row (workspace ~lines 295-303, the `flex flex-wrap items-center justify-end gap-2`
container next to the 保存 button). Activating it reveals an inline panel/popover containing a stepType
segmented control (内容/任务/测验), an intent textarea, and a 「生成草稿」 submit that calls
`draftLessonWithAgentAction` (Plan 03). The trigger is hidden when `lesson_agent_enabled` is OFF (server
passes the flag down — backend remains authoritative per D-03).

Scope guard: NO new route. The Phase 64 `LessonDraftReviewWorkspace` (diff review) is OUT OF SCOPE — this
plan only adds the trigger that produces the draft to be reviewed.

Purpose: DRAFT-01 — gives teachers the actual UI entry point to drive the run→persist closure.
Output: trigger button + inline panel wired to the server action.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/66-wire-ai-lessonagent-draft-loop-end-to-end-bridge-run-persist/66-UI-SPEC.md
@.planning/phases/66-wire-ai-lessonagent-draft-loop-end-to-end-bridge-run-persist/66-CONTEXT.md

<interfaces>
src/components/authoring/lesson-authoring-workspace.tsx — header action row (~295-303):
```tsx
<div className="flex flex-wrap items-center justify-end gap-2">
  <span ...>总时长约 {totalMinutes} 分钟</span>
  <span ...>{steps.length} 个活动</span>
  <span ...>{builtInStepCount} 个内置环节</span>
  <Button ... data-testid="lesson-flow-save-button" onClick={saveFlow}><Save .../>保存</Button>
</div>
```
Component already imports `Button` and lucide icons (e.g. `Save`). Add `Sparkles`.

Plan 03 server action:
```typescript
draftLessonWithAgentAction(input: { lessonId, stepType:"content"|"task"|"quiz", intent })
  : Promise<ActionResult<...>>   // { ok:true, data } | { ok:false, error:"AGENT_DISABLED"|..., message }
```

66-UI-SPEC mandates: Button variant="primary" gradient + Sparkles icon; inline panel with stepType
segmented control + intent text + 「生成草稿」; design-system tokens only (no 1px divider lines).
The `lessonId` is available in workspace props; the flag boolean (e.g. `lessonAgentEnabled`) is passed
from the server component that renders the workspace.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AI 起草 trigger + inline intent panel</name>
  <files>src/components/authoring/lesson-authoring-workspace.tsx</files>
  <action>Per 66-UI-SPEC: (1) import `Sparkles` from lucide-react. (2) In the header action row, add a primary
  gradient `Button` (Sparkles icon, label 「AI 起草」, data-testid="lesson-ai-draft-trigger") rendered only when
  the server-provided `lessonAgentEnabled` prop is true. (3) Add local UI state (useState) for panel open +
  selected stepType (default "content") + intent text + pending/feedback. (4) Render an inline panel/popover
  with a segmented control (内容→content / 任务→task / 测验→quiz), an intent textarea, and a 「生成草稿」 submit
  button (disabled while pending or intent empty). (5) On submit call `draftLessonWithAgentAction({ lessonId,
  stepType, intent })`; on `ok:true` show success feedback + close panel; on `ok:false` show `message`
  (AGENT_DISABLED handled gracefully). Use design tokens / tonal surfaces only — NO 1px divider lines, follow
  the existing rounded-full / surface-container styling. Do NOT add a route or touch LessonDraftReviewWorkspace.
  Thread the new `lessonAgentEnabled` prop through the workspace prop type (server passes it).</action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -c "lesson-authoring-workspace" || true</automated>
  </verify>
  <done>Trigger renders only when flag enabled; panel collects stepType+intent and calls draftLessonWithAgentAction; typecheck clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual + functional verification of AI 起草 trigger</name>
  <what-built>An 「AI 起草」 primary gradient button (Sparkles) in the lesson flow header that opens an
  inline panel (内容/任务/测验 segmented control + intent text + 生成草稿) wired to draftLessonWithAgentAction,
  hidden when the agent flag is off.</what-built>
  <how-to-verify>
    1. Seed the test DB agentRegistry LessonAgent row enabled=true (direct write — do NOT flip the seed default).
    2. Run the app, open a lesson authoring workspace as an active teacher.
    3. Confirm the 「AI 起草」 button appears in the flow header action row with gradient + Sparkles icon.
    4. Click it → inline panel opens; pick a stepType, type an intent, click 「生成草稿」.
    5. Confirm success feedback and that a draft was persisted (a new draft version exists for the lesson).
    6. Set the registry row enabled=false → reload → confirm the button is hidden.
    7. Confirm styling matches DESIGN.md (no 1px divider lines, tonal surfaces, Lexend).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → server action | the panel forwards teacher intent to the gated server action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-15 | EoP | client hiding bypass | mitigate | UI hide is convenience only; server action re-enforces lesson_agent_enabled (Plan 03), so a forged client call still returns AGENT_DISABLED |
| T-66-16 | Tampering | intent/stepType from UI | mitigate | server action validates intent/stepType with strict zod; client values are advisory |
| T-66-17 | Information disclosure | flag state in UI | accept | exposing only a boolean enabled flag to the teacher's own workspace is low-risk and required to render the trigger |
</threat_model>

<verification>
- `grep -n "draftLessonWithAgentAction\|Sparkles\|lesson-ai-draft-trigger" src/components/authoring/lesson-authoring-workspace.tsx` present
- Typecheck clean for the workspace file
- Human-verify checkpoint approved
</verification>

<success_criteria>
Teacher has a working 「AI 起草」 entry point that produces a persisted draft via the gated server action,
hidden when the agent is disabled, styled to DESIGN.md, with no new route and the review workspace untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/66-wire-ai-lessonagent-draft-loop-end-to-end-bridge-run-persist/66-05-SUMMARY.md`
</output>
