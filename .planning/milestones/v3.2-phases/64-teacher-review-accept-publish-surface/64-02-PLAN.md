---
phase: 64-teacher-review-accept-publish-surface
plan: 02
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - src/lib/dal/lesson-authoring.ts
  - src/lib/dal/lesson-authoring.test.ts
  - src/lib/dal/lesson-authoring.draft-review.test.ts
autonomous: true
requirements: [REVIEW-02, REVIEW-03]
must_haves:
  truths:
    - "D-01/D-02: getLessonDraftReviewDTO returns indexed diff rows comparing draft steps[n] ↔ live steps[n] per D-02 single-column index alignment"
    - "D-04/D-07: applyDraftToLiveLesson archives all active lessonSteps, inserts replacement rows with LexoRank ranks, marks draft status=applied, and sets lessons.aiDraftAppliedAt + lessons.latestDraftVersionId"
    - "D-08: discardDraftLessonVersion sets status=discarded + archivedAt=now, writes ZERO rows to lessonSteps, and preserves draft row for audit"
    - "REVIEW-03: DAL write tests prove apply archives/replaces active steps and discard leaves live steps untouched"
  artifacts:
    - path: "src/lib/dal/lesson-authoring.ts"
      provides: "Phase 64 DAL read/write methods for draft review lifecycle"
      contains: "applyDraftToLiveLesson"
    - path: "src/lib/dal/lesson-authoring.draft-review.test.ts"
      provides: "DAL-level tests for apply/discard/review-read behaviors"
      contains: "applyDraftToLiveLesson"
  key_links:
    - from: "applyDraftToLiveLesson"
      to: "lessons table (aiDraftAppliedAt, latestDraftVersionId)"
      via: "Drizzle update on lessons row"
      pattern: "db\\.update\\(lessons\\)"
    - from: "applyDraftToLiveLesson"
      to: "draftLessonVersions.status"
      via: "Drizzle update setting status 'applied'"
      pattern: "status.*applied"
    - from: "discardDraftLessonVersion"
      to: "draftLessonVersions (status, archivedAt)"
      via: "Drizzle update, zero lessonSteps writes"
      pattern: "discarded"
---

<objective>
Implement the Phase 64 DAL write functions and the draft review read function on top of Plan 64-01's schema and DTO contracts. This plan delivers `applyDraftToLiveLesson` (the core accept→live bridge per D-04/D-07), `discardDraftLessonVersion` (safe no-op discard per D-08), and `getLessonDraftReviewDTO` (the read side that feeds the UI diff view per D-01/D-02). All methods follow existing DAL patterns: `assertActiveTeacher`, scoped lesson lookup, DTO parse before return, explicit cache tag annotations in doc comments.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-teacher-review-accept-publish-surface/64-CONTEXT.md
@.planning/phases/64-teacher-review-accept-publish-surface/64-RESEARCH.md
@.planning/phases/64-teacher-review-accept-publish-surface/64-PATTERNS.md
@src/lib/dal/lesson-authoring.ts
@src/lib/dto/lesson-authoring.ts
@src/lib/ranking/lexorank.ts
@src/db/schema.ts

<interfaces>
<!-- Key DAL patterns the executor needs. Extracted from codebase. -->

From src/lib/dal/lesson-authoring.ts:
```typescript
// Auth guard pattern — call first in every public DAL function
import { assertActiveTeacher } from "@/lib/dal/lesson-authoring";
const scope = await assertActiveTeacher();

// Scoped lesson lookup pattern
import { getScopedLesson } from "@/lib/dal/lesson-authoring"; // check if exported
const { lesson, course } = await getScopedLesson(input.lessonId, scope);

// Step read pattern — non-archived only
await db.query.lessonSteps.findMany({
  where: and(eq(lessonSteps.lessonId, lesson.id), isNull(lessonSteps.archivedAt)),
  orderBy: (step, { asc }) => [asc(step.rank)],
});

// LexoRank pattern
import { createInitialRank } from "@/lib/ranking/lexorank";
// For sequential: first rank = createInitialRank(), subsequent = createRankAfter(prevRank)
// draft snapshotJson stores { steps: LessonStepPayload[] } — NO rank field
// on apply, insert step rows with computed LexoRank ranks from payload-only draft steps

// DTO parse pattern — always parse before return
return SomeDTOSchema.parse({ ... });
```

From src/db/schema.ts (after 64-01 migration):
```typescript
// draftLessonVersions gains: status, archivedAt
// lessons gains: aiDraftAppliedAt, latestDraftVersionId
```

From src/lib/dto/lesson-authoring.ts (after 64-01):
```typescript
// Available DTO schemas and types
export const LessonDraftReviewDTOSchema = z.object({ ... });
export type LessonDraftReviewDTO = z.infer<typeof LessonDraftReviewDTOSchema>;
export function buildLessonDraftDiffRows(live: LessonStepDTO[], draft: ...): LessonDraftDiffRow[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement getLessonDraftReviewDTO read DAL + tests</name>
  <read_first>
    - src/lib/dal/lesson-authoring.ts
    - src/lib/dto/lesson-authoring.ts
    - src/db/schema.ts
    - .planning/phases/64-teacher-review-accept-publish-surface/64-01-PLAN.md
  </read_first>
  <files>src/lib/dal/lesson-authoring.ts, src/lib/dal/lesson-authoring.draft-review.test.ts</files>
  <behavior>
    - Test 1: getLessonDraftReviewDTO returns draft metadata (draftVersionId, version, source, status, createdAt, stepCount) when a latest pending draft exists.
    - Test 2: getLessonDraftReviewDTO.liveSteps are non-archived lessonSteps ordered by rank ascending.
    - Test 3: getLessonDraftReviewDTO.draftSteps are parsed from draft.snapshotJson.steps array.
    - Test 4: diffRows are computed by buildLessonDraftDiffRows from the DTO module.
    - Test 5: hasPendingDraft is true when latest draft has status "pending", false otherwise.
    - Test 6: getLessonDraftReviewDTO returns null or throws DRAFT_NOT_FOUND when no pending draft exists (plan decision: return null with empty liveSteps).
  </behavior>
  <action>
Add `getLessonDraftReviewDTO(input: { lessonId: string })` to `src/lib/dal/lesson-authoring.ts`. Start with `assertActiveTeacher()` + `getScopedLesson()`. Query the latest `draftLessonVersions` row ordered by version DESC where `status = 'pending'`. If none found, return `{ hasPendingDraft: false, lesson, liveSteps, draftSteps: [], diffRows: [] }` parsed by `LessonDraftReviewDTOSchema`. If draft found, parse `snapshotJson` as `{ steps: LessonStepPayload[] }`, map draft steps to `LessonStepDTO`-compatible shapes with `deriveStepTitle()` and `getStepDescription()` helpers from the DTO module. Query live `lessonSteps` where `archivedAt IS NULL` ordered by `rank ASC`. Call `buildLessonDraftDiffRows(liveSteps, draftSteps)` from 64-01's DTO. Return full review DTO. Add comprehensive tests in `src/lib/dal/lesson-authoring.draft-review.test.ts` (create this file if absent — follow existing test patterns: mock/seed draft row, seed lesson with steps, call DAL, assert DTO shape).
  </action>
  <verify>
    <automated>pnpm test -- src/lib/dal/lesson-authoring.draft-review.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `getLessonDraftReviewDTO` is exported from `src/lib/dal/lesson-authoring.ts`.
    - Returns `hasPendingDraft: true` with diffRows when pending draft exists.
    - Returns `hasPendingDraft: false` with empty draftSteps/diffRows when no pending draft.
    - `diffRows[0]` has keys `index`, `state`, `liveStep`, `draftStep`.
    - grep `src/lib/dal/lesson-authoring.ts` for `getLessonDraftReviewDTO` returns the function definition.
  </acceptance_criteria>
  <done>Review read DTO is available for Server Components and downstream Server Actions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement applyDraftToLiveLesson write DAL + tests</name>
  <read_first>
    - src/lib/dal/lesson-authoring.ts
    - src/db/schema.ts
    - src/lib/ranking/lexorank.ts
    - .planning/phases/64-teacher-review-accept-publish-surface/64-CONTEXT.md
  </read_first>
  <files>src/lib/dal/lesson-authoring.ts, src/lib/dal/lesson-authoring.draft-review.test.ts</files>
  <behavior>
    - Test 1: applyDraftToLiveLesson archives ALL active lessonSteps (sets archivedAt = new Date() for every non-archived step) and inserts replacement steps.
    - Test 2: inserted steps match draft snapshotJson.steps content — title, type, payload preserved; new rows get sequential LexoRank ranks (createInitialRank for first, createRankAfter for subsequent).
    - Test 3: lessons row is updated: aiDraftAppliedAt = now, latestDraftVersionId = draftVersionId, revision incremented, updatedAt set.
    - Test 4: draftLessonVersions row status = 'applied'.
    - Test 5: returns ApplyDraftResultDTO with lessonId, courseId, draftVersionId, appliedStepCount.
    - Test 6: draft with status != 'pending' throws or returns error.
    - Test 7: (security) unauthorized teacher cannot apply draft for a lesson outside their scope.
    - Test 8: (regression) previously archived steps remain archived (double-archiving is a no-op).
  </behavior>
  <action>
Add `applyDraftToLiveLesson(input: { lessonId: string; draftVersionId: string; editedSteps?: EditableDraftStep[] })` to `src/lib/dal/lesson-authoring.ts`. Authorize via `assertActiveTeacher()` + `getScopedLesson()`. Load the draft row by `draftVersionId` — verify `status === 'pending'`, throw `DRAFT_NOT_PENDING` otherwise. Parse `snapshotJson.steps` as `LessonStepPayload[]`. If `editedSteps` provided, merge edits by index: for each index where editedSteps[i] exists, override draft step's title and content fields (per D-13: title, description, content only). Use a Drizzle transaction (via `db.batch` or `db.transaction`) to: (1) set `archivedAt = new Date()` on all `lessonSteps` where `lessonId = input.lessonId AND archivedAt IS NULL`, (2) insert new `lessonSteps` rows from merged steps with `createInitialRank()` for step 0 then `createRankAfter(prevRank)` for subsequent, each with `id = crypto.randomUUID()`, `type`, `title`, `rank`, `payload` from draft step, (3) update `lessons` row: `aiDraftAppliedAt = new Date()`, `latestDraftVersionId = input.draftVersionId`, `revision = lesson.revision + 1`, `updatedAt = new Date()`, (4) update `draftLessonVersions` row: `status = 'applied'`. Return `ApplyDraftResultDTOSchema.parse({ lessonId, courseId: course.id, draftVersionId, appliedStepCount })`. Test in draft-review test file.
  </action>
  <verify>
    <automated>pnpm test -- src/lib/dal/lesson-authoring.draft-review.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `applyDraftToLiveLesson` is exported from `src/lib/dal/lesson-authoring.ts`.
    - After apply, all active lessonSteps have non-null `archivedAt`.
    - After apply, new lessonSteps count equals draft step count.
    - After apply, `lessons.aiDraftAppliedAt` is non-null and `lessons.latestDraftVersionId` matches input draftVersionId.
    - After apply, `draftLessonVersions.status` = `'applied'`.
    - Calling apply on already-applied draft throws.
  </acceptance_criteria>
  <done>applyDraftToLiveLesson bridges AI draft to live lessonSteps per D-04/D-07, ready for teacher's publish step.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement discardDraftLessonVersion write DAL + tests</name>
  <read_first>
    - src/lib/dal/lesson-authoring.ts
    - src/db/schema.ts
    - .planning/phases/64-teacher-review-accept-publish-surface/64-CONTEXT.md
  </read_first>
  <files>src/lib/dal/lesson-authoring.ts, src/lib/dal/lesson-authoring.draft-review.test.ts</files>
  <behavior>
    - Test 1: discardDraftLessonVersion sets draft status = 'discarded' and archivedAt = timestamp.
    - Test 2: discard writes ZERO rows to lessonSteps (prove by counting lessonSteps before/after).
    - Test 3: discard writes ZERO to lessons table (revision unchanged, aiDraftAppliedAt unchanged).
    - Test 4: returns DiscardDraftResultDTO with lessonId, draftVersionId, discardedAt.
    - Test 5: draft with status != 'pending' throws.
    - Test 6: (security) unauthorized teacher cannot discard draft outside their scope.
    - Test 7: (regression) lessonSteps remain unchanged when discard is called.
  </behavior>
  <action>
Add `discardDraftLessonVersion(input: { lessonId: string; draftVersionId: string })` to `src/lib/dal/lesson-authoring.ts`. Authorize via `assertActiveTeacher()` + `getScopedLesson()`. Load the draft row — verify `status === 'pending'`, throw `DRAFT_NOT_PENDING` otherwise. Update `draftLessonVersions` row: `status = 'discarded'`, `archivedAt = new Date()`. Must NOT write to `lessonSteps` or `lessons` at all (D-08: discard is a pure draft-row metadata mutation). Return `DiscardDraftResultDTOSchema.parse({ lessonId, draftVersionId, discardedAt: toIso(now) })`. Test assertions must prove lessonSteps count unchanged and lessons row untouched.
  </action>
  <verify>
    <automated>pnpm test -- src/lib/dal/lesson-authoring.draft-review.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `discardDraftLessonVersion` is exported from `src/lib/dal/lesson-authoring.ts`.
    - After discard, `draftLessonVersions.status` = `'discarded'` and `archivedAt` is non-null.
    - grep `discardDraftLessonVersion` in src/lib/dal/lesson-authoring.ts verifies NO `lessonSteps` insert/update within the function body.
    - After discard, active lessonSteps count is identical to before-discard count.
    - After discard, `lessons.aiDraftAppliedAt` remains null (no accidental apply side-effect).
  </acceptance_criteria>
  <done>discardDraftLessonVersion safely closes the AI draft without any live-step mutation per D-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DAL ↔ database | applyDraftToLiveLesson is the sole destructive writer to active lessonSteps. Must be authorization-gated. |
| discardDraftLessonVersion ↔ lessonSteps | Trust boundary: discard must never cross into lessonSteps writes. Source assertions enforce this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| S64-02 | Tampering | applyDraftToLiveLesson | mitigate | Authorization via assertActiveTeacher + getScopedLesson before any write. |
| S64-03 | Information Disclosure | getLessonDraftReviewDTO | mitigate | Returns only DTO-parsed fields. snapshotJson raw is never passed to caller. |
| S64-06 | Denial of Service | discardDraftLessonVersion | mitigate | Test proves it never touches lessonSteps — prevents cascade data loss. |
| S64-07 | Elevation of Privilege | applyDraftToLiveLesson | mitigate | Scoped lesson ownership check ensures cross-school draft access is impossible. |
</threat_model>

<verification>
- `pnpm test -- src/lib/dal/lesson-authoring.draft-review.test.ts`
- `pnpm test -- src/lib/dal/lesson-authoring.test.ts` (regression — existing tests still pass)
- `pnpm typecheck`
</verification>

<success_criteria>
- REVIEW-03: applyDraftToLiveLesson archives active steps, inserts replacements with LexoRank, marks draft applied, and backlinks lesson per D-04/D-07.
- REVIEW-03: discardDraftLessonVersion marks draft discarded with zero live-step writes per D-08.
- REVIEW-01/02: getLessonDraftReviewDTO returns indexed diff rows for the UI via the 64-01 DTO helper.
- All three DAL functions have TDD-level test coverage proving the above behaviors.
</success_criteria>

<output>
完成后创建 `.planning/phases/64-teacher-review-accept-publish-surface/64-02-SUMMARY.md`
</output>
