---
phase: 62-lessonagent-typed-tool-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/platform-core/events/contracts.ts
  - src/features/platform-core/events/contracts.test.ts
autonomous: true
requirements: [AGENT-04]
must_haves:
  truths:
    - "三条 AI 域 typed events（lesson.draft.requested / lesson.tool.invoked / lesson.draft.produced）作为合法 PlatformEvent 成员存在，可被 discriminated union 解析"
    - "AI 域事件 payload 受 summary-only 守卫约束：字段名禁以 json 结尾、禁内嵌对象快照、.strict()"
    - "新增事件可被 PlatformCommandExecutionResult.emittedEvents 承载（并入 PlatformDomainEventSchema 联合）"
  artifacts:
    - path: "src/features/platform-core/events/contracts.ts"
      provides: "LessonDraft* 三个 AI 域 event schema + 并入 PlatformEventSchema / PlatformDomainEventSchema"
      contains: "lesson.draft.requested"
    - path: "src/features/platform-core/events/contracts.test.ts"
      provides: "AI 域事件契约单测（合法解析 + summary-only 拒绝 *Json）"
      contains: "lesson.draft.produced"
  key_links:
    - from: "src/features/platform-core/events/contracts.ts"
      to: "PlatformDomainEventSchema / PlatformEventSchema"
      via: "z.discriminatedUnion 追加成员"
      pattern: "PlatformEventSchema = z.discriminatedUnion"
---

<objective>
为 AGENT-04 落地 AI 域 typed events 的**契约层**：在 `platform-core/events/contracts.ts` 新增三条起草关键节点事件 schema（`lesson.draft.requested` / `lesson.tool.invoked` / `lesson.draft.produced`），照抄既有 `Plugin*EventSchema` 的 `.strict()` + summary-only 风格，并入 `PlatformEventSchema` 与 `PlatformDomainEventSchema` 两个联合类型，使其后续可经 handler 的 `emittedEvents` 落账。

Purpose: 事件落账无法脱离父 command，但事件 schema 是 handler / command / 测试三者共同依赖的**契约根**，须先于 command 子系统就位（interface-first）。本 plan 只动契约，不触发任何写库。
Output: 修改后的 `events/contracts.ts` + 契约单测，证明三事件合法、summary-only 合规。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-lessonagent-typed-tool-layer/62-CONTEXT.md
@.planning/phases/62-lessonagent-typed-tool-layer/62-PATTERNS.md

<interfaces>
<!-- 已读真实签名，executor 直接照抄，无需再探索 -->

既有 summary-only 守卫与事件结构（src/features/platform-core/events/contracts.ts）:
```typescript
// :5 SummaryRecordSchema —— 顶层及一层嵌套字段名禁以 "json" 结尾，违者报错
const SummaryRecordSchema = z.record(z.string(), z.unknown()).superRefine(/* ... */);

// :94-104 既有 domain 事件范本（照抄此结构）
export const PluginInstalledEventSchema = z.object({
  eventType: z.literal("plugin.installed"),
  category: z.literal("domain"),
  aggregateType: z.literal("plugin"),
  aggregateId: z.string().min(1),
  payload: PluginInstalledPayloadSchema,           // .strict()
  audit: PlatformAuditMetadataSchema.default({ delegatedActor: null, approval: null }),
}).strict();

// :130-147 三个联合（须追加新成员）
export const PlatformEventSchema = z.discriminatedUnion("eventType", [ /* ... */ ]);
export const PlatformDomainEventSchema = z.union([ /* ... */ ]);
export const PlatformSuccessOrDomainEventSchema = z.union([PlatformSuccessEventSchema, PlatformDomainEventSchema]);
```

ledger 持久化映射（src/features/platform-core/events/ledger.ts:47-48，已确认 aggregateType 通用落库，无需改动）:
```typescript
aggregateType: event.aggregateType,   // 接受 "lesson"，无字面量硬编码
aggregateId: event.aggregateId,
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为三条 AI 域事件写契约单测（RED）</name>
  <read_first>
    - src/features/platform-core/events/contracts.test.ts（既有契约测试骨架，照搬 describe/expect 风格）
    - src/features/platform-core/events/contracts.ts:5-27（SummaryRecordSchema 守卫语义）
    - src/features/platform-core/events/contracts.ts:94-147（既有 domain 事件 + 三联合）
  </read_first>
  <behavior>
    - Test 1（合法）: 一条形如 `{ eventType:"lesson.draft.requested", category:"domain", aggregateType:"lesson", aggregateId:"lesson_x", payload:{ stepType:"content", intentSummary:"导入" }, audit:{...} }` 经 `PlatformEventSchema.safeParse` → success。
    - Test 2（summary-only 拒绝）: payload 含字段 `stepPayloadJson`（以 Json 结尾）→ `safeParse` → success=false，issue message 含 "must not include object snapshots"。
    - Test 3（并入 domain 联合）: `lesson.draft.produced` 事件经 `PlatformDomainEventSchema.safeParse` 与 `PlatformSuccessOrDomainEventSchema.safeParse` 均 success（证可被 emittedEvents 承载）。
    - Test 4（strict）: payload 含未声明的多余字段 → safeParse 失败（.strict() 生效）。
    - 覆盖三个 eventType：requested / tool.invoked / produced。
  </behavior>
  <action>
    在 `src/features/platform-core/events/contracts.test.ts` 追加一个 `describe("AI 域 lesson draft 事件", ...)` 块，按上述 4 个 behavior 写断言。导入 `PlatformEventSchema` / `PlatformDomainEventSchema` / `PlatformSuccessOrDomainEventSchema`（以及待新增的 `LessonDraftRequestedEventSchema` 等具体 schema 用于直接 safeParse）。此刻这些新 schema 尚未导出 → 测试编译/运行 RED。事件命名严格对齐 D-03：`lesson.draft.requested` / `lesson.tool.invoked` / `lesson.draft.produced`，`aggregateType:"lesson"`，`category:"domain"`。payload 仅放摘要字段（如 `stepType`、`intentSummary`、`succeeded`、`toolName`、`tokenUsage`），**禁**整包 `lessonStepPayloadSchema` 快照。
  </action>
  <verify>
    <automated>pnpm vitest run src/features/platform-core/events/contracts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lesson.draft.requested\|lesson.tool.invoked\|lesson.draft.produced" src/features/platform-core/events/contracts.test.ts` ≥ 3
    - 运行测试此刻为 RED（新 describe 块失败，因 schema 未实现）—— 在 Task 2 前预期红
  </acceptance_criteria>
  <done>新 describe 块存在，断言覆盖合法解析 / summary-only 拒绝 / domain 联合 / strict 四类，运行处于 RED。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现三条 AI 域事件 schema 并并入联合（GREEN）</name>
  <read_first>
    - src/features/platform-core/events/contracts.ts:36-147（既有 payload schema + 事件 schema + 三联合，照抄结构）
    - src/features/platform-core/ai-contracts/delegation.ts:42（PlatformAuditMetadataSchema，audit 默认值来源）
    - src/features/platform-core/events/ledger.ts:38-52（确认 aggregateType 通用落库，无需改 ledger）
  </read_first>
  <action>
    在 `src/features/platform-core/events/contracts.ts` 中：
    1. 新增三个 `.strict()` payload schema（summary-only，字段名禁以 json/Json 结尾）：
       - `LessonDraftRequestedPayloadSchema`：`{ commandType: z.string().min(1), stepType: z.enum(["content","task","quiz"]), intentSummary: z.string().min(1) }`
       - `LessonToolInvokedPayloadSchema`：`{ toolName: z.string().min(1), stepType: z.enum(["content","task","quiz"]), attempt: z.number().int().positive() }`
       - `LessonDraftProducedPayloadSchema`：`{ stepType: z.enum(["content","task","quiz"]), title: z.string().min(1), succeeded: z.literal(true), tokenUsage: z.number().int().nonnegative().optional() }`
       （字段集为初值，executor 可按 handler 实际产出微调，但**必须** summary-only + `.strict()`）
    2. 新增三个事件 schema，照抄 `PluginInstalledEventSchema`（:94-104）结构，`category: z.literal("domain")`，`aggregateType: z.literal("lesson")`，`aggregateId: z.string().min(1)`，`audit` 用 `PlatformAuditMetadataSchema.default(...)`，整体 `.strict()`：`LessonDraftRequestedEventSchema` / `LessonToolInvokedEventSchema` / `LessonDraftProducedEventSchema`，`eventType` 分别为三个字面量。
    3. 把三个事件追加进 `PlatformEventSchema`（:130 discriminatedUnion）与 `PlatformDomainEventSchema`（:138 union）的成员数组。**不要**改动既有 `aggregateType: z.literal("plugin")` 字面量，也**不要**改 `PlatformSuccessEventSchema` / `PlatformFailureEventSchema`（失败节点复用既有 plugin 形 outcome 事件，见 Plan 62-03）。
    4. 导出三个事件 schema 与对应 `type`（`z.infer`），供 Plan 62-03 handler 与测试引用。
  </action>
  <verify>
    <automated>pnpm vitest run src/features/platform-core/events/contracts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' src/features/platform-core/events/contracts.ts | grep -c 'aggregateType: z.literal("lesson")'` == 3
    - `grep -c "LessonDraftRequestedEventSchema\|LessonToolInvokedEventSchema\|LessonDraftProducedEventSchema" src/features/platform-core/events/contracts.ts` ≥ 6（定义+联合引用）
    - Task 1 测试由 RED 转 GREEN
    - `pnpm vitest run src/features/platform-core/events/contracts.test.ts` 全绿，既有 plugin 事件断言不回归
  </acceptance_criteria>
  <done>三事件 schema 实现、并入两个联合、导出 type；契约测试全绿；既有 plugin 事件契约无回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler → event ledger | handler 产出的事件 payload 跨入持久化账本；若内嵌对象快照会泄漏/膨胀 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Information Disclosure | AI 域事件 payload | mitigate | payload schema `.strict()` + summary-only（字段名禁 json 结尾），步骤包整包**禁**入事件；单测断言含 `*Json` 字段被拒（Task 1 Test 2）|
| T-62-02 | Tampering | 事件 discriminated union | mitigate | 仅扩展联合成员，不放宽既有 `plugin` 字面量，既有契约测试防回归；新事件 `.strict()` 拒多余字段（Task 1 Test 4）|
</threat_model>

<verification>
- `pnpm vitest run src/features/platform-core/events/contracts.test.ts` 全绿
- `pnpm test` full suite 不因契约扩展回归（尤其 events/ledger.test.ts、commands/handlers/plugins.events.test.ts）
</verification>

<success_criteria>
三条 AI 域 typed events 作为合法 PlatformEvent / PlatformDomainEvent 存在，summary-only + strict 受测保障，可被下游 handler 的 emittedEvents 承载 —— 为 AGENT-04 事件落账提供契约根。
</success_criteria>

<output>
完成后创建 `.planning/phases/62-lessonagent-typed-tool-layer/62-01-SUMMARY.md`
</output>
