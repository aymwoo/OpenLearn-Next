---
phase: 62-lessonagent-typed-tool-layer
plan: 04
type: execute
wave: 3
depends_on: ["62-03"]
files_modified:
  - src/server/ai/agents/lesson-agent.ts
  - src/server/ai/agents/lesson-agent.test.ts
autonomous: true
requirements: [AGENT-03, AGENT-04]
must_haves:
  truths:
    - "lesson-agent.ts 暴露唯一 server-only 公共编排入口 draftLessonStep(...)，构造合法 command envelope（correlation 三字段 + scope.pluginId=core.lesson-agent sentinel + actor scope）后经 dispatchPlatformCommand 派发，绝不绕过 Command Bus 直接落事件"
    - "派发成功时从 PlatformCommandDispatchResult.resultSummary 取回生成的步骤包并回传调用方（SC3），失败时把 bus 的 failed 语义透传/抛出（不静默吞错）"
    - "集成测试经注入 persistPlatformEvents（PlatformCommandBusDependencies 可选注入点）断言三条 AI 域事件经真实 bus→handler 落账，且 events payload summary-only、step 仅在 resultSummary（SC4 + 信息泄露缓解端到端验证）"
    - "lesson-agent.ts 不导入 DB client / env key / ai 包 generateObject / eval，仅经 Plan 62-03 的 command 子系统与 Plan 62-01 的事件契约间接触达底层（server-only 边界完整）"
  artifacts:
    - path: "src/server/ai/agents/lesson-agent.ts"
      provides: "server-only 公共编排入口 draftLessonStep：构造 command envelope + dispatchPlatformCommand + 从 resultSummary 取回 step"
      contains: "dispatchPlatformCommand"
      min_lines: 30
    - path: "src/server/ai/agents/lesson-agent.test.ts"
      provides: "端到端集成测试：真实 bus + registry，注入 persistPlatformEvents 断言三事件落账 + step 形状 + summary-only"
      contains: "persistPlatformEvents"
  key_links:
    - from: "src/server/ai/agents/lesson-agent.ts"
      to: "dispatchPlatformCommand (commands/bus.ts)"
      via: "构造 envelope 后唯一派发路径，definitions 默认解析自 platformCommandRegistry"
      pattern: "dispatchPlatformCommand"
    - from: "src/server/ai/agents/lesson-agent.ts"
      to: "PlatformCommandDispatchResult.resultSummary"
      via: "从派发结果取回生成的步骤包回传调用方"
      pattern: "resultSummary"
    - from: "src/server/ai/agents/lesson-agent.test.ts"
      to: "lessonDraftCommandHandlers (经 platformCommandRegistry)"
      via: "真实 bus dispatch 触发 handler，注入 persistPlatformEvents 捕获 emittedEvents"
      pattern: "persistPlatformEvents"
---

<objective>
为 AGENT-03/04 落地 **公共编排入口**：新建 `src/server/ai/agents/lesson-agent.ts`，导出 server-only 函数 `draftLessonStep(...)`——它构造合法的 `lesson.draft.run` command envelope（`correlation` 三字段齐全、`scope.pluginId="core.lesson-agent"` sentinel、`actor` scope），经 `dispatchPlatformCommand` 派发（`definitions` 缺省自动解析自 `platformCommandRegistry`，故无需显式传入），并从 `PlatformCommandDispatchResult.resultSummary` 取回 Plan 62-03 handler 生成的步骤包回传调用方。配套 `lesson-agent.test.ts` 端到端集成测试：跑真实 bus + registry，注入 `persistPlatformEvents` 捕获并断言三条 AI 域事件落账、payload summary-only、step 仅在 resultSummary。

Purpose: Phase 62 的对外契约是「调用方给 lessonId/stepType/intent，拿回经校验的步骤包，并在事件账本留下可追溯轨迹」。Plan 62-02 建工具层、62-03 建 command/handler 落账轨道，本 plan 把二者收口成单一公共入口并端到端证明：调用方**只能**经 Command Bus 触达生成（满足事件落账硬约束 `appendPlatformEvents.commandId` FK notNull），三条成功事件真实落账（SC4），生成步骤经 resultSummary 回传（SC3），且 server-only 边界不泄露至 client/Edge/插件。此入口对应 `agents/registry.ts` 中 seed 的 LessonAgent（enabled=false / featureFlag `lesson_agent_enabled`），本 phase 不改其开关。
Output: 公共编排入口 `lesson-agent.ts` + 端到端集成测试，闭合 Success Criteria 3（步骤回传）与 4（三事件可追溯落账）。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-lessonagent-typed-tool-layer/62-CONTEXT.md
@.planning/phases/62-lessonagent-typed-tool-layer/62-PATTERNS.md
@.planning/phases/62-lessonagent-typed-tool-layer/62-03-PLAN.md

<interfaces>
<!-- 派发入口与依赖（执行者直接使用，无需再探索代码库） -->

dispatchPlatformCommand（src/features/platform-core/commands/bus.ts）:
```typescript
// 签名（contracts 引用见 bus.ts:241 / deps type :68）
async function dispatchPlatformCommand(
  command: PlatformCommandInput,          // { id, type, actor, scope, payload, correlation, audit?, dedupeKey? }
  dependencies: PlatformCommandBusDependencies,
): Promise<PlatformCommandDispatchResult>;

// PlatformCommandBusDependencies（bus.ts:68）
type PlatformCommandBusDependencies = {
  definitions?: Partial<Record<PlatformCommandType, PlatformCommandDefinition>>; // 缺省 → platformCommandRegistry
  store: PlatformCommandStore;
  publicationPort?: PlatformEventPublicationPort;
  persistPlatformEvents?: (input: {                  // ← 测试注入点：缺省 appendPlatformEvents
    commandId: string; attemptNumber: number; correlationId: string;
    causationId?: string | null; invalidationTags: string[];
    failureAttribution?: PlatformCommandExecutionError["failureAttribution"] | null;
    events: PlatformEvent[];                          // ← handler 的 emittedEvents 经此抵达落账
  }) => Promise<{ events: PersistedPlatformEventRecord[]; dispatches: PersistedPlatformEventDispatchRecord[] }>;
};

// readDefinition（bus.ts:125）：definitions?.[type] ?? platformCommandRegistry[type]
//   → 生产路径无需显式传 definitions；registry 已含 62-03 注册的 lesson.draft.run

// PlatformCommandDispatchResult（含回传通道）
//   { status, commandId, attemptNumber, resultSummary, invalidation, ... }
//   resultSummary ← 62-03 handler 的 successResult.resultSummary（含 step）
```

生产依赖装配范本（src/features/platform-core/commands/producers/plugin-governance.ts:248-265，照搬形态）:
```typescript
import { db } from "@/db";
import { defaultInProcessPlatformEventAdapter } from "@/features/platform-core/events/adapters/in-process";
const platformCommandStore: PlatformCommandStore = { /* createCommand/updateCommand/getCommand/listAttempts，经 db */ };
const result = await dispatchPlatformCommand(
  { id, type, actor, scope, payload, correlation, audit, dedupeKey },
  { store: platformCommandStore, publicationPort: defaultInProcessPlatformEventAdapter },
  // 注意：未传 definitions → 自动用 platformCommandRegistry；未传 persistPlatformEvents → 自动用 appendPlatformEvents
);
```

62-03 命令契约（本 plan 仅消费，不改）:
```typescript
type LessonDraftRunPayload = { lessonId: string; stepType: "content"|"task"|"quiz"; intent: string }; // .strict()
// scope = { schoolId, pluginId: "core.lesson-agent" }（sentinel，零改 scope schema）
// 成功：resultSummary = { stepType, title, succeeded: true, step }，emittedEvents = 3 条 lesson.* 域事件
// 失败：handler throw PlatformCommandExecutionError → bus 落唯一 generic platform.command.failed
```

测试 mock 范本（src/features/platform-core/commands/handlers/plugins.events.test.ts:1-58）:
```typescript
vi.mock("server-only", () => ({}));
// vi.hoisted 装配 mock；注入自定义 persistPlatformEvents 捕获 events 数组
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为 draftLessonStep 编排入口写端到端集成测试（RED）</name>
  <read_first>
    - src/features/platform-core/commands/handlers/plugins.events.test.ts:1-58（vi.mock("server-only")、vi.hoisted DAL/tools mock、注入式断言骨架，照搬）
    - src/features/platform-core/commands/producers/plugin-governance.ts:248-265（dispatchPlatformCommand 调用形态 + 依赖装配 + normalizeProducerResult 取 resultSummary）
    - .planning/phases/62-lessonagent-typed-tool-layer/62-03-PLAN.md（lesson.draft.run payload / resultSummary 形状 / 三事件 eventType）
    - src/features/platform-core/commands/bus.ts:68-100,125-136（deps type + readDefinition 默认 registry）
  </read_first>
  <behavior>
    - Test 1（端到端落账三事件 / SC4 / AGENT-04）: vi.hoisted mock `@/lib/dal/lesson-authoring` 的 `assertActiveTeacher` → `{ userId:"t1", schoolIds:["s1"] }`，mock `@/server/ai/tools` 的 `createDraftLessonStepTool` → `{ execute: async () => ({ type:"content", title:"导入", body:"…", teacherNotes:"…", materialRefs:[] }) }`。注入自定义 `persistPlatformEvents`（记录传入的 `events`）与 in-memory `store`。调用 `draftLessonStep({ schoolId:"s1", lessonId:"l1", stepType:"content", intent:"导入环节" }, { store, persistPlatformEvents })` → 捕获到的 `events` 长度==3，eventType 集合 == {lesson.draft.requested, lesson.tool.invoked, lesson.draft.produced}，三事件 aggregateType=="lesson"、aggregateId=="l1"、共享同一 correlationId。
    - Test 2（步骤经 resultSummary 回传 / SC3）: `draftLessonStep(...)` 返回值含可还原的步骤包（type=="content"、title=="导入"），即 dispatch 的 resultSummary.step 已透传回调用方。
    - Test 3（信息泄露缓解端到端 / T-62-09）: 捕获的三 `events` 的 payload **均不含**整包 step 字段（无 body/teacherNotes/materialRefs；仅 stepType/title/succeeded 类摘要），整包 step 仅出现在 resultSummary。
    - Test 4（envelope 合法性 / Spoofing 缓解）: 经 store.createCommand 捕获的 command，其 `scope.pluginId=="core.lesson-agent"`、`correlation` 三字段齐全、`type=="lesson.draft.run"`；teacherId 不出现在 command.payload（payload 仅 lessonId/stepType/intent）。
    - Test 5（失败透传 / D-53-08）: 令 mock tool.execute 抛错 → `draftLessonStep(...)` 结果为 failed 语义（返回 status=="failed" 或抛出，executor 定形），捕获的 `events` 不含任何 lesson.* domain 事件（失败走 bus generic 失败路径）。
  </behavior>
  <action>
    新建 `src/server/ai/agents/lesson-agent.test.ts`：首行 `vi.mock("server-only", () => ({}))`。用 `vi.hoisted` 装配 `@/lib/dal/lesson-authoring`（仅 `assertActiveTeacher`）与 `@/server/ai/tools`（`createDraftLessonStepTool`）的 mock（风格照搬 plugins.events.test.ts:1-58）。构造 in-memory `PlatformCommandStore`（createCommand 记录 envelope 供 Test 4 断言、updateCommand/getCommand/listAttempts 最小实现）与捕获型 `persistPlatformEvents`（push 入数组并返回 `{ events:[], dispatches:[] }`）。导入 `draftLessonStep`（尚未实现 → RED）。按 behavior 1-5 写断言；**不传 definitions**（验证默认解析 registry 的真实链路）。运行应 RED（模块不存在）。
  </action>
  <verify>
    <automated>pnpm vitest run src/server/ai/agents/lesson-agent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "draftLessonStep" src/server/ai/agents/lesson-agent.test.ts` ≥ 1
    - `grep -c "persistPlatformEvents" src/server/ai/agents/lesson-agent.test.ts` ≥ 1
    - `grep -c "lesson.draft.requested\|lesson.tool.invoked\|lesson.draft.produced" src/server/ai/agents/lesson-agent.test.ts` ≥ 3
    - `grep -c "core.lesson-agent" src/server/ai/agents/lesson-agent.test.ts` ≥ 1
    - 测试运行 RED（draftLessonStep 未实现）
  </acceptance_criteria>
  <done>端到端集成测试存在，覆盖三事件落账 / step 经 resultSummary 回传 / summary-only / envelope 合法性 / 失败透传，运行 RED。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 draftLessonStep 公共编排入口（GREEN）</name>
  <read_first>
    - src/features/platform-core/commands/producers/plugin-governance.ts:1-20,180-265（server-only 首行 + db import + store 装配 + dispatchPlatformCommand 调用 + normalizeProducerResult 从 resultSummary 取数）
    - src/server/ai/providers/facade.ts:1（`import "server-only"` 范式）
    - src/features/platform-core/commands/bus.ts:68-100,125-136（deps 可选注入 + definitions 默认 registry）
    - src/server/ai/agents/registry.ts（LessonAgent seed，确认 actor/scope 既有约定；本 plan 不改此文件）
    - src/features/platform-core/ai-contracts/delegation.ts:42（PlatformAuditMetadataSchema，audit 形状）
  </read_first>
  <action>
    新建 `src/server/ai/agents/lesson-agent.ts`：
    1. 首行 `import "server-only";`（server-only 边界，杜绝 client/Edge/插件触达）。
    2. import：`dispatchPlatformCommand`、`type PlatformCommandStore`（from commands/bus）；`defaultInProcessPlatformEventAdapter`（from events/adapters/in-process）；`db`（from @/db，仅用于装配生产 store）。**禁** import：DB schema 写入、env key、`ai` 包 `generateObject`、任何 eval/动态执行。
    3. 复用/装配生产 `platformCommandStore: PlatformCommandStore`（照搬 plugin-governance.ts 形态，经 `db` 实现 createCommand/updateCommand/getCommand/listAttempts）。
    4. 导出 `export async function draftLessonStep(input: { schoolId: string; lessonId: string; stepType: "content"|"task"|"quiz"; intent: string; actor?: PlatformCommand["actor"] }, deps?: Partial<Pick<PlatformCommandBusDependencies, "store"|"persistPlatformEvents"|"publicationPort">>)`:
       - 构造 `correlation`：生成 correlationId（如 `crypto.randomUUID()`），producer 字段固定 `"lesson-agent"`，causationId=null。
       - 构造 command envelope：`{ id: <稳定/随机 commandId>, type: "lesson.draft.run", actor: input.actor ?? <built-in system actor>, scope: { schoolId: input.schoolId, pluginId: "core.lesson-agent" }, payload: { lessonId: input.lessonId, stepType: input.stepType, intent: input.intent }, correlation, dedupeKey: <optional> }`。**teacherId 不进 payload**（由 handler 经 assertActiveTeacher 注入，缓解 Spoofing）。
       - 派发：`const result = await dispatchPlatformCommand(command, { store: deps?.store ?? platformCommandStore, publicationPort: deps?.publicationPort ?? defaultInProcessPlatformEventAdapter, persistPlatformEvents: deps?.persistPlatformEvents });`（**不传 definitions** → 默认解析 registry 的 lesson.draft.run）。
       - 回传：从 `result.resultSummary` 取回 step 并组装返回 `{ status: result.status, commandId: result.commandId, step: result.resultSummary?.step ?? null }`（失败时 status=="failed"、step==null，调用方据 status 处理；不静默吞错）。
    5. 末尾保持类型自洽（必要处 `satisfies`）。文件 ≥30 行。
  </action>
  <verify>
    <automated>pnpm vitest run src/server/ai/agents/lesson-agent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dispatchPlatformCommand" src/server/ai/agents/lesson-agent.ts` ≥ 1
    - `grep -c "core.lesson-agent" src/server/ai/agents/lesson-agent.ts` ≥ 1
    - `grep -c "resultSummary" src/server/ai/agents/lesson-agent.ts` ≥ 1
    - `head -1 src/server/ai/agents/lesson-agent.ts | grep -c 'import "server-only"'` == 1
    - `grep -v '^\s*//' src/server/ai/agents/lesson-agent.ts | grep -c "generateObject\|eval(\|process.env"` == 0
    - `grep -cE "definitions\s*:" src/server/ai/agents/lesson-agent.ts` == 0（验证依赖默认解析 registry）
    - `pnpm vitest run src/server/ai/agents/lesson-agent.test.ts` 全绿（GREEN）
  </acceptance_criteria>
  <done>draftLessonStep 实现完成：构造合法 envelope→dispatchPlatformCommand→从 resultSummary 取回 step；三事件经真实 bus 落账、summary-only、失败透传；server-only 边界完整；集成测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调用方 → lesson-agent（server-only） | 调用方传入 schoolId/lessonId/stepType/intent；teacherId 不可由调用方控制，必须 handler 经授权注入 |
| lesson-agent → Command Bus | 唯一派发路径；事件只能经 dispatchPlatformCommand→handler.emittedEvents 落账，禁绕过直写 ledger |
| server-only 边界 → client/Edge/插件 | 编排入口与底层工具/facade 绝不暴露给不可信运行时 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-10 | Spoofing | command.payload | mitigate | teacherId 不进 payload（不可由调用方伪造）；身份由 62-03 handler 经 `assertActiveTeacher` 注入（Task 1 Test 4 断言 payload 仅 lessonId/stepType/intent + scope.pluginId=="core.lesson-agent"）|
| T-62-11 | Tampering | 事件落账路径 | mitigate | 入口绝不直写 ledger；只经 `dispatchPlatformCommand`，事件必带父 commandId（FK notNull）；Task 1 经注入 persistPlatformEvents 验证事件全部源自 bus→handler |
| T-62-12 | Information Disclosure | server-only 边界 + 事件 payload | mitigate | 首行 `import "server-only"`（acceptance grep）；禁 import DB schema 写入/env key/generateObject（grep==0）；三事件 payload summary-only、整包 step 仅入 resultSummary（Task 1 Test 3 端到端断言）|
| T-62-13 | Repudiation | 起草轨迹 | mitigate | 三事件共享 correlationId + 同一 commandId，端到端可追溯（Task 1 Test 1 断言 correlation 串联）|
| T-62-14 | Denial of Service | LLM 工具调用 | accept | N=1 单链路、入口非公开触发（LessonAgent enabled=false / featureFlag）；速率/配额由 Phase 61 facade rate-limit 兜底，本 phase 不额外加固 |
</threat_model>

<verification>
- `pnpm vitest run src/server/ai/agents/lesson-agent.test.ts` 全绿（端到端三事件落账 + step 回传 + summary-only + 失败透传）。
- `pnpm vitest run src/server/ai/tools/no-leak.test.ts`（Plan 62-02）仍绿——lesson-agent.ts 不引入新泄露面。
- `pnpm test` 全套回归无新失败（commands/events/ai 域）。
- server-only 边界 grep 守卫：`lesson-agent.ts` 首行 server-only、无 generateObject/env/eval。
</verification>

<success_criteria>
- 公共入口 `draftLessonStep` 经 Command Bus 唯一合法路径派发 lesson.draft.run，端到端落账三条可追溯 AI 域事件（**SC4**）。
- 生成步骤包经 `resultSummary` 回传调用方（**SC3**），整包 step 不入事件 payload（T-62-09/12 端到端验证）。
- teacherId 由 handler 授权注入、不可由调用方伪造（T-62-10）；server-only 边界完整、无底层泄露（T-62-12）。
- 失败语义经 bus generic 失败路径透传、不静默吞错、不发 domain 事件（D-53-08）。
</success_criteria>

<output>
完成后创建 `.planning/phases/62-lessonagent-typed-tool-layer/62-04-SUMMARY.md`。
</output>
