---
phase: 65-eval-guardrails-verify-phase-close-gate
plan: 03
type: execute
wave: 2
depends_on: [65-01]
files_modified:
  - src/server/ai/tools/lesson-draft.eval.test.ts
autonomous: true
requirements: [EVAL-01]
must_haves:
  truths:
    - "A repeatable, deterministic eval suite replays the shared corpus through the draft tool's controlled generation channel (mock model, no network, no provider key) and asserts every produced step is schema-valid against lessonStepPayloadSchema."
    - "The same eval asserts the basic teaching-structure invariants (D-02) hold for content/task/quiz outputs: content has non-empty title+body; task has non-empty prompt + valid submissionType; quiz has >=2 options + an in-range correctOptionIndex."
    - "The eval is runnable in isolation via a single command and produces stable pass/fail with no flakiness."
  artifacts:
    - path: "src/server/ai/tools/lesson-draft.eval.test.ts"
      provides: "EVAL-01 schema + teaching-invariant eval over the shared corpus"
      contains: "lessonStepPayloadSchema"
  key_links:
    - from: "src/server/ai/tools/lesson-draft.eval.test.ts"
      to: "draftStepCorpus.valid"
      via: "replay through aiGenerateObject mock + lessonStepPayloadSchema.safeParse"
      pattern: "draftStepCorpus"
---

<objective>
Deliver the EVAL-01 eval suite: a deterministic, repeatable check that LessonAgent draft output is schema-valid and meets basic teaching structure (SC1).

Purpose: SC1 requires a repeatable eval verifying draft output on schema legality + teaching structure. Per locked decision D-01 this is a fixture-driven Vitest `*.eval.test.ts` replaying a mock model — NOT a tsx script, NOT LLM-as-judge. It reuses the single shared corpus (D-03) so the eval and the guardrail tests stay in lockstep.
Output: `src/server/ai/tools/lesson-draft.eval.test.ts`.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-CONTEXT.md
@.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-PATTERNS.md
@.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-VALIDATION.md

<interfaces>
<!-- Shared corpus (65-01). -->
draftStepCorpus.valid = { content, task, quiz }  // each must pass schema + teaching invariants

<!-- Mock model fixtures (src/server/ai/providers/__fixtures__/mock-model.ts — EXISTS, do not modify). -->
makeOkObjectModel(obj): MockLanguageModelV3  // doGenerate returns JSON.stringify(obj) for generateObject

<!-- Tool under eval (src/server/ai/tools/lesson-draft.ts). -->
createDraftLessonStepTool({ teacherId }) -> tool; execute({lessonId,stepType,intent}) -> LessonStepPayload
// execute calls aiGenerateObject({ teacherId, prompt, schema: lessonStepPayloadSchema }) and getTeacherLessonPreviewDTO

<!-- Schema (src/lib/dto/lesson-authoring.ts:154). -->
lessonStepPayloadSchema = z.discriminatedUnion("type",[content,task,quiz])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 65-03-01: Eval harness replaying corpus through the controlled channel</name>
  <read_first>
    - src/server/ai/tools/lesson-draft.test.ts (full) — ANALOG: how to mock `server-only`, `@/server/ai/providers` (aiGenerateObject) and `@/lib/dal/lesson-authoring` (getTeacherLessonPreviewDTO), and drive `createDraftLessonStepTool(...).execute`.
    - src/server/ai/providers/__fixtures__/mock-model.ts — `makeOkObjectModel` to produce deterministic JSON output (if eval goes through the real facade) OR simply resolve the aiGenerateObject mock with the corpus step (simpler, matches lesson-draft.test.ts).
    - src/server/ai/tools/__fixtures__/draft-step-corpus.ts (65-01) — `draftStepCorpus.valid`.
  </read_first>
  <action>
    Create `src/server/ai/tools/lesson-draft.eval.test.ts`. Mirror lesson-draft.test.ts mocking: `vi.mock("server-only", () => ({}))`; hoisted mock for `@/server/ai/providers` (`aiGenerateObject`) and `@/lib/dal/lesson-authoring` (`getTeacherLessonPreviewDTO` resolving a minimal valid preview like lesson-draft.test.ts `FAKE_PREVIEW`).
    Build a `describe("EVAL-01: draft output schema + teaching structure", ...)`. For EACH `stepType` in `["content","task","quiz"]`:
    - Set `aiGenerateObjectMock.mockResolvedValue(draftStepCorpus.valid[stepType])` (deterministic replay; no network, no provider key).
    - Call `await createDraftLessonStepTool({ teacherId: "t1" }).execute!({ lessonId: "l1", stepType, intent: "起草" }, {} as never)`.
    - Assert `lessonStepPayloadSchema.safeParse(result).success === true` (schema legality, EVAL-01 row).
    Run the assertions in a loop over the 3 types so coverage is corpus-driven.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/server/ai/tools/lesson-draft.eval.test.ts` passes.
    - `grep -n 'draftStepCorpus' src/server/ai/tools/lesson-draft.eval.test.ts` returns a match (uses the shared corpus, not inline literals).
    - `grep -nE 'process\.env|fetch\(|http' src/server/ai/tools/lesson-draft.eval.test.ts` — no network/provider-key usage (deterministic, per D-01).
  </acceptance_criteria>
  <done>Eval replays all 3 valid step types through the tool and asserts schema validity; runs offline.</done>
</task>

<task type="auto">
  <name>Task 65-03-02: Teaching-structure invariant assertions (D-02)</name>
  <read_first>
    - src/lib/dto/lesson-authoring.ts (lines 114-160) — the fields the invariants check.
    - src/features/platform-core/commands/handlers/lesson-draft.ts (lines 70-82, `deriveStepTitle`) — confirms quiz title field is `question`, task is `prompt`, content is `title`.
  </read_first>
  <action>
    Extend `lesson-draft.eval.test.ts` with an `it` per type (or a parametrized block) asserting the teaching invariants (D-02) on the produced step:
    - content: `step.title.trim().length > 0` AND `step.body.trim().length > 0`.
    - task: `step.prompt.trim().length > 0` AND `["text","image","file","link"].includes(step.submissionType)`.
    - quiz: `step.options.length >= 2` AND (`step.correctOptionIndex === undefined` OR `step.correctOptionIndex < step.options.length`).
    These are the "basic teaching structure 达标" criteria from SC1. Narrow `result` by `result.type` before asserting type-specific fields.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/server/ai/tools/lesson-draft.eval.test.ts` passes with the invariant assertions present.
    - `grep -cE 'submissionType|options\.length|\.body|\.prompt' src/server/ai/tools/lesson-draft.eval.test.ts` returns >= 3 (invariants for all three types present).
  </acceptance_criteria>
  <done>Eval asserts content/task/quiz teaching invariants in addition to schema validity; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test harness → AI facade | The eval replaces the live provider with a deterministic mock; no real model, key, or network is reached. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-EVAL-KEY | Information Disclosure | eval suite provider access | mitigate | Eval mocks `@/server/ai/providers` and resolves corpus steps directly; no `process.env` provider key read, no network call. Asserted by the grep acceptance check. |
</threat_model>

<verification>
- `npx vitest run src/server/ai/tools/lesson-draft.eval.test.ts` green, deterministic across repeated runs.
- Corpus-driven (uses `draftStepCorpus.valid`); offline.
- Asserts schema validity + content/task/quiz teaching invariants.
</verification>

<success_criteria>
A repeatable eval proves draft output is schema-valid and teaching-structure-sound for all three step types (SC1 / EVAL-01).
</success_criteria>

<output>
After completion, create `.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-03-SUMMARY.md`.
</output>
