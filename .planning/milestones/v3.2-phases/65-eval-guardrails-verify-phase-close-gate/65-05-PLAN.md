---
phase: 65-eval-guardrails-verify-phase-close-gate
plan: 05
type: execute
wave: 3
depends_on: [65-01, 65-02, 65-03, 65-04]
files_modified:
  - scripts/verify-phase65-eval-guardrails.ts
  - package.json
autonomous: true
requirements: [EVAL-03]
must_haves:
  truths:
    - "A single command (verify:phase65) runs `npm run build`, then a Phase 61–65 end-to-end Vitest regression subset (provider/tool/agent/draft-persist/review/eval/guardrail/events), and exits non-zero on any failure (D-08 a+b+c)."
    - "The close gate's static checks assert ALL FIVE v3.2 boundary invariants from D-10: (1) tool inputSchema excludes teacherId, (2) tool generates ONLY via aiGenerateObject, (3) no eval()/DB-client/provider-key import in the AI tool layer (+ no eval/provider-key in the agent layer), (4) guardrail layer exists, (5) lesson.draft.rejected contract exists — plus Phase 61/63/64 contract-existence checks (D-08 d)."
    - "An authoritative verify:phase alias resolves to the Phase 65 close gate so the milestone has one canonical 'is this phase done' command."
    - "The rejected-event field name is the canonical reasonCode (not reason), asserted by a close-gate grep, consistent with plans 01/04 (D-07)."
  artifacts:
    - path: "scripts/verify-phase65-eval-guardrails.ts"
      provides: "Phase 65 close-gate verifier (build + Phase 61–65 static checks + Phase 61–65 runVitest regression)"
      contains: "lesson.draft.rejected"
    - path: "package.json"
      provides: "verify:phase65 + verify:phase scripts"
      contains: "verify:phase65"
  key_links:
    - from: "package.json"
      to: "scripts/verify-phase65-eval-guardrails.ts"
      via: "verify:phase65 + verify:phase alias"
      pattern: "verify-phase65-eval-guardrails"
    - from: "scripts/verify-phase65-eval-guardrails.ts"
      to: "src/server/ai/tools/lesson-draft.ts"
      via: "D-10 static boundary assertions (inputSchema/aiGenerateObject/no-eval-db-key)"
      pattern: "draftStepInputSchema"
---

<objective>
Deliver the close gate (EVAL-03): a `verify:phase65` script that (a) runs `npm run build`, (b) runs the Phase 61–65 end-to-end Vitest regression subset, (c) includes the EVAL-01 eval suite, and (d) statically asserts the v3.2 boundary invariants (D-10's five + Phase 61/63/64 contract existence) — any failure → `process.exit(1)`. Plus an authoritative `verify:phase` alias.

Purpose: SC3 + D-08 require a single repeatable gate that does an **end-to-end v3.2 (Phase 61–65) regression**, not a Phase-65-only smoke test. Modeled on `scripts/verify-phase54-ai-contracts.ts` (same `read`/`listFiles`/`run`/`runVitest` helpers, `process.exit(1)` on static-check failure). D-10 mandates the static layer assert ALL FIVE boundaries against the real `src/server/ai/tools/lesson-draft.ts` source (the prior draft covered only 2 of 5). The alias gives the milestone one canonical command.
Output: `scripts/verify-phase65-eval-guardrails.ts` and two `package.json` scripts.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-CONTEXT.md
@.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-PATTERNS.md
@.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-VALIDATION.md

<interfaces>
<!-- ANALOG to copy verbatim for harness plumbing: scripts/verify-phase54-ai-contracts.ts -->
<!--   helpers: read(filePath), listFiles(root), run(cmd,args,label), runVitest(paths,label) -->
<!--   pattern: build StaticCheck[] → if any fails, print gaps + process.exit(1) → else run(npm build) → runVitest(subset) -->
<!--   runVitest resolves node_modules/vitest/vitest.mjs, falls back to .bin/vitest, then pnpm exec -->

<!-- D-10 GROUND TRUTH — src/server/ai/tools/lesson-draft.ts (verified, 66 lines): -->
<!--   const draftStepInputSchema = z.object({ lessonId, stepType, intent });   // NO teacherId -->
<!--   inputSchema: draftStepInputSchema,                                       // schema excludes teacherId -->
<!--   const step = await aiGenerateObject({ teacherId, prompt, schema });      // sole generation channel -->
<!--   import { tool } from "ai";   // NOT { generateObject, generateText } from "ai" -->
<!--   imports: @/lib/dal/lesson-authoring, @/lib/dto/lesson-authoring, @/server/ai/providers — NO @/db, NO process.env*KEY, NO eval -->

<!-- LAYER-SCOPING NOTE (avoids a false-positive RED — the T-65-GATE threat itself): -->
<!--   The no-DB-client-import ban from D-10(3) is the GENERATION-boundary ban → scope it to the TOOL layer -->
<!--   (src/server/ai/tools). The AGENT layer (src/server/ai/agents/lesson-agent.ts) LEGITIMATELY imports -->
<!--   `from "@/db"` to dispatch persisted platform commands — banning @/db there would red the gate on correct -->
<!--   code. So for the agent layer assert only no-eval + no-provider-key (both verified clean). -->
<!--   The no-direct-generateObject/Text ban is TOOL-only too: the Phase 61 facade (src/server/ai/providers/ -->
<!--   facade.ts) is the SANCTIONED place that imports `{ generateObject, generateText } from "ai"`. -->

<!-- Artifacts produced by 65-01..65-04 that this gate inspects: -->
src/lib/dto/draft-guardrails.ts                                   // enum + DraftGuardrailRejection (non-server-only)
src/server/ai/tools/guardrails.ts                                 // server-only assertStepWithinGuardrails + FORBIDDEN_MARKERS
src/server/ai/tools/lesson-draft.ts                               // calls assertStepWithinGuardrails(step); D-10 source-of-truth
src/server/ai/tools/lesson-draft.eval.test.ts                     // EVAL-01 suite
src/features/platform-core/events/contracts.ts                    // lesson.draft.rejected registered (reasonCode field)
src/features/platform-core/commands/handlers/lesson-draft.ts      // instanceof DraftGuardrailRejection branch

<!-- Phase 61–64 contract anchors (verified present in contracts.ts / providers): -->
src/server/ai/providers/index.ts          // export { aiGenerateText, aiGenerateObject } from "./facade"  (Phase 61)
src/server/ai/providers/facade.ts          // import "server-only" first line                              (Phase 61)
events/contracts.ts: lesson.draft.produced + lesson.draft.persisted                                        (Phase 63)
events/contracts.ts: lesson.draft.accepted + lesson.draft.applied + lesson.draft.discarded                 (Phase 64)

<!-- package.json scripts block. No existing verify:phase alias; "build":"next build --turbopack"; "test":"vitest" last. -->
<!-- Phase 65 invocation style matches verify:phase54: node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/<file>.ts -->
<!-- vitest.config.mts include globs cover *.test.ts AND *.test.tsx → the .tsx review-workspace test runs. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 65-05-01: Author verify-phase65 close-gate script (build + Phase 61–65 regression + D-10 five boundaries)</name>
  <read_first>
    - scripts/verify-phase54-ai-contracts.ts (full) — ANALOG to copy: `read`, `listFiles`, `run`, `runVitest`, `verifyPackageScript`, `main()` static-check-then-test structure, `process.exit(1)` on gap.
    - src/server/ai/tools/lesson-draft.ts (post-65-02, 66 lines) — D-10 GROUND TRUTH: confirm `draftStepInputSchema = z.object({ lessonId, stepType, intent })` (no teacherId), `aiGenerateObject({` call, `import { tool } from "ai"`, and the `assertStepWithinGuardrails(` call to grep.
    - src/server/ai/agents/lesson-agent.ts (lines 1-6) — confirms the agent legitimately `import { db } from "@/db"` (why the DB-ban is tool-scoped, not agent-scoped).
    - src/features/platform-core/events/contracts.ts — confirm the `lesson.draft.rejected` registration + `reasonCode` field, plus the Phase 63/64 event-type string literals to grep.
    - src/features/platform-core/commands/handlers/lesson-draft.ts (post-65-04) — confirm the `instanceof DraftGuardrailRejection` string to grep.
  </read_first>
  <action>
    Create `scripts/verify-phase65-eval-guardrails.ts`. Copy the `read`, `listFiles`, `run`, `runVitest` helpers from verify-phase54 verbatim (keep `runVitest` resolution order). Rename log/error strings to "Phase 65". Replace `verifyPackageScript` to assert `pkg.scripts["verify:phase65"] === "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase65-eval-guardrails.ts"` AND `pkg.scripts["verify:phase"] === "pnpm verify:phase65"`.

    Read sources once at the top of `main()`:
    - `const packageSource = read("package.json");`
    - `const lessonDraftSource = read("src/server/ai/tools/lesson-draft.ts");`
    - `const guardrailsSource = read("src/server/ai/tools/guardrails.ts");`
    - `const guardrailsDtoSource = read("src/lib/dto/draft-guardrails.ts");`
    - `const contractsSource = read("src/features/platform-core/events/contracts.ts");`
    - `const handlerSource = read("src/features/platform-core/commands/handlers/lesson-draft.ts");`
    - `const providersIndexSource = read("src/server/ai/providers/index.ts");`
    - `const facadeSource = read("src/server/ai/providers/facade.ts");`
    - Tool-layer combined source (EXCLUDES tests/fixtures): `const toolLayerSource = listFiles("src/server/ai/tools").filter(f => f.endsWith(".ts") && !f.endsWith(".test.ts") && !f.includes("__fixtures__")).map(read).join("\n");`
    - Agent-layer combined source: `const agentLayerSource = listFiles("src/server/ai/agents").filter(f => f.endsWith(".ts") && !f.endsWith(".test.ts")).map(read).join("\n");`
    - DB-schema combined source (scope guard): `const dbSource = listFiles("src/db").filter(f => f.endsWith(".ts") && !f.endsWith(".test.ts")).map(read).join("\n");`
    - Extract the input-schema block once: `const inputSchemaBlock = lessonDraftSource.match(/draftStepInputSchema\s*=\s*z\.object\(\{([\s\S]*?)\}\)/)?.[1] ?? "";`

    Build `staticChecks: StaticCheck[]` with these CONCRETE checks (each `passed` a boolean expression):

    1. "package.json exposes exact verify:phase65 + verify:phase alias" → `verifyPackageScript(packageSource)`.
    2. "[D-10.1] tool inputSchema excludes teacherId (closure-injected)" → `inputSchemaBlock.length > 0 && !/teacherId/.test(inputSchemaBlock)`.
    3. "[D-10.2] tool generates ONLY via aiGenerateObject (no direct ai generateObject/generateText)" → `lessonDraftSource.includes("aiGenerateObject(") && !/\b(generateObject|generateText)\s*\(/.test(lessonDraftSource) && !/import\s*\{[^}]*\b(generateObject|generateText)\b[^}]*\}\s*from\s*["']ai["']/.test(lessonDraftSource)`. (Note: the facade form `aiGenerateObject(` has a capital G so the lowercase pattern does not match it.)
    4. "[D-10.3a] AI tool layer has no eval / no DB-client import / no provider-key import" → `!/\beval\s*\(/.test(toolLayerSource) && !/from\s+["']@\/db["']/.test(toolLayerSource) && !/@libsql\/client|better-sqlite3/.test(toolLayerSource) && !/process\.env\.[A-Z0-9_]*KEY/.test(toolLayerSource)`.
    5. "[D-10.3b] AI agent layer has no eval / no provider-key import (DB-dispatch import allowed)" → `!/\beval\s*\(/.test(agentLayerSource) && !/process\.env\.[A-Z0-9_]*KEY/.test(agentLayerSource)`. Add an inline comment: agent layer legitimately imports `@/db` to dispatch persisted commands, so the DB-client ban is tool-scoped per D-10's generation-boundary intent.
    6. "[D-10.4] guardrail validator exists, is server-only and pure" → `guardrailsSource.includes("server-only") && guardrailsSource.includes("assertStepWithinGuardrails") && guardrailsSource.includes("FORBIDDEN_MARKERS")`.
    7. "guardrail reason-code contract is non-server-only and exports DraftGuardrailRejection" → `guardrailsDtoSource.includes("class DraftGuardrailRejection") && !guardrailsDtoSource.includes("server-only")`.
    8. "draft tool invokes the guardrail before returning the step" → `lessonDraftSource.includes("assertStepWithinGuardrails(")`.
    9. "[D-10.5] lesson.draft.rejected is registered summary-only with the canonical reasonCode field" → `contractsSource.includes("lesson.draft.rejected") && contractsSource.includes("LessonDraftRejectedEventSchema") && contractsSource.includes("summaryOnlyStrictPayload") && /reasonCode\s*:/.test(contractsSource)`. (WARNING-1: assert reasonCode, NOT reason.)
    10. "handler distinguishes guardrail rejection from real failure" → `handlerSource.includes("instanceof DraftGuardrailRejection")`.
    11. "[Phase 61] provider facade is the single server-only generation channel" → `/export\s*\{[^}]*aiGenerateObject[^}]*\}\s*from\s*["']\.\/facade["']/.test(providersIndexSource) && facadeSource.includes("server-only")`.
    12. "[Phase 63] draft-persist contracts present (produced + persisted)" → `contractsSource.includes("lesson.draft.produced") && contractsSource.includes("lesson.draft.persisted")`.
    13. "[Phase 64] accept→publish contracts present (accepted + applied + discarded)" → `contractsSource.includes("lesson.draft.accepted") && contractsSource.includes("lesson.draft.applied") && contractsSource.includes("lesson.draft.discarded")`.
    14. "[scope guard / T-65-GATE] phase 65 introduces NO guardrail persistence table" → `!/guardrailRejection|draftRejection/i.test(dbSource)`.

    On any failed check: print the gaps and `process.exit(1)`.

    THEN, after static checks pass, run the gate steps IN ORDER (D-08 a→b→c):
    - (a) `run("npm", ["run", "build"], "Phase 65 close-gate: npm run build");` — must run BEFORE the vitest subset; `run` throws (non-zero exit) on build failure.
    - (b)+(c) `runVitest([...], "Phase 61–65 v3.2 AI draft-chain regression suite")` over the full end-to-end subset (the eval suite at item c is one entry in this list):
      Phase 61 (provider facade):
        - src/server/ai/providers/facade.test.ts
        - src/server/ai/providers/no-leak.test.ts
        - src/server/ai/providers/registry.test.ts
        - src/server/ai/providers/rate-limit.test.ts
      Phase 62 (typed tool + agent):
        - src/server/ai/tools/lesson-draft.test.ts
        - src/server/ai/tools/no-leak.test.ts
        - src/server/ai/agents/lesson-agent.test.ts
      Phase 63 (draft persist):
        - src/lib/dal/lesson-authoring.draft-persist.test.ts
        - src/features/platform-core/commands/handlers/lesson-draft.persist.test.ts
        - src/features/platform-core/events/lesson-draft-persisted.contract.test.ts
      Phase 64 (review / accept-publish):
        - src/lib/dal/lesson-authoring.draft-review.test.ts
        - src/actions/lesson-authoring-draft-review-actions.test.ts
        - src/components/authoring/lesson-draft-review-workspace.test.tsx
      Phase 65 (eval / guardrail / events) — (c) EVAL-01 suite is `lesson-draft.eval.test.ts`:
        - src/lib/dto/draft-guardrails.test.ts
        - src/server/ai/tools/guardrails.test.ts
        - src/server/ai/tools/lesson-draft.eval.test.ts
        - src/features/platform-core/events/contracts.test.ts
        - src/features/platform-core/commands/handlers/lesson-draft.events.test.ts
    End with the success banner. Call `main()` at the bottom.
  </action>
  <acceptance_criteria>
    - `grep -n 'process.exit(1)' scripts/verify-phase65-eval-guardrails.ts` returns a match.
    - `grep -nE 'run\("npm", \[\s*"run",\s*"build"' scripts/verify-phase65-eval-guardrails.ts` returns a match positioned BEFORE the `runVitest(` call (build precedes the subset; verify with `grep -n` line ordering).
    - `grep -cE 'D-10\.[1-5]' scripts/verify-phase65-eval-guardrails.ts` returns >= 5 (all five D-10 boundary checks labelled).
    - `grep -n 'draftStepInputSchema' scripts/verify-phase65-eval-guardrails.ts` returns a match (inputSchema teacherId-exclusion assertion present).
    - `grep -n 'aiGenerateObject(' scripts/verify-phase65-eval-guardrails.ts` returns a match (sole-generation-channel assertion present).
    - `grep -n 'reasonCode' scripts/verify-phase65-eval-guardrails.ts` returns a match (WARNING-1 canonical field grep).
    - `grep -cE 'lesson.draft.(produced|persisted|accepted|applied|discarded)' scripts/verify-phase65-eval-guardrails.ts` returns >= 5 (Phase 61/63/64 contract checks present).
    - `grep -cE 'lesson.draft.rejected|assertStepWithinGuardrails|instanceof DraftGuardrailRejection' scripts/verify-phase65-eval-guardrails.ts` returns >= 3.
    - `grep -n 'lesson-draft.eval.test.ts' scripts/verify-phase65-eval-guardrails.ts` returns a match (EVAL-01 suite included).
    - `grep -cE 'providers/(facade|no-leak|registry|rate-limit).test.ts' scripts/verify-phase65-eval-guardrails.ts` returns >= 4 (Phase 61 regression spanned).
  </acceptance_criteria>
  <done>Close-gate script exists; static layer asserts all five D-10 boundaries + Phase 61/63/64 contracts; gate runs `npm run build` then the Phase 61–65 Vitest subset; non-zero exit on any failure.</done>
</task>

<task type="auto">
  <name>Task 65-05-02: Wire verify:phase65 + authoritative verify:phase, run the gate</name>
  <read_first>
    - package.json (scripts block) — insert after `verify:phase60:local`, keep `"test":"vitest"` last. Match the verify:phase54 invocation exactly. Confirm `"build": "next build --turbopack"` exists (the gate calls `npm run build`).
  </read_first>
  <action>
    In `package.json` scripts, add:
    - `"verify:phase65": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase65-eval-guardrails.ts"`
    - `"verify:phase": "pnpm verify:phase65"` (authoritative alias — the canonical close-gate command for this milestone; must resolve to the latest phase gate).
    Keep JSON valid (commas, ordering). Do not alter unrelated scripts.
  </action>
  <acceptance_criteria>
    - `node -e "const s=require('./package.json').scripts; process.exit(s['verify:phase65'] && s['verify:phase']==='pnpm verify:phase65' && s['build'] ? 0 : 1)"` exits 0.
    - `pnpm verify:phase65` runs the gate to completion and exits 0 (all static checks pass + `npm run build` green + Phase 61–65 subset green). Equivalently `npm run verify:phase65`.
    - `pnpm verify:phase` exits 0 (alias resolves to the Phase 65 gate).
  </acceptance_criteria>
  <done>verify:phase65 and verify:phase both registered; running the gate (build + Phase 61–65 regression) passes end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CI gate → source tree | The verifier reads files, runs `npm run build`, and runs the Phase 61–65 vitest subset; it executes no network calls and no provider model. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-GATE | Tampering / Elevation | close gate (false-green via build/types when live regression skipped) | mitigate | The gate does NOT stop at build/type success: after the static D-10 boundary layer passes, it runs `npm run build` AND a live Phase 61–65 vitest regression subset (provider/tool/agent/persist/review/eval/guardrail/events). A removed guardrail call, a leaked teacherId in inputSchema, a direct `ai` generateObject, or any broken contract trips a static check or a red test → `process.exit(1)`. Layer-scoping (DB-ban tool-only, generateObject-ban tool-only) prevents the inverse false-RED on legitimate agent/facade DB+`ai` usage. |
| T-65-GATE-SCOPE | Tampering | scope-guard static check (#14) | mitigate | Check 14 reads the real `src/db` schema sources and fails the gate if a guardrail/rejection persistence table is introduced (enforces the NOT-doing boundary: no new guardrail persistence table this phase). |
| T-65-GATE-NET | Information Disclosure | verifier process | accept | Verifier is local-only (read + build + vitest subset). The eval/guardrail suites it runs are themselves mocked (no key/network), so the gate inherits offline determinism. |
</threat_model>

<verification>
- `pnpm verify:phase65` exits 0 with all 14 static checks passing, `npm run build` green, and the Phase 61–65 subset green.
- `pnpm verify:phase` resolves to the Phase 65 gate and exits 0.
- Gate exits non-zero if: build fails; any subset test fails; or any static boundary is violated (teacherId in inputSchema, direct `ai` generateObject/Text in the tool, eval/DB/provider-key import in the tool layer, missing guardrail/contract/handler wiring, or a new guardrail persistence table).
- Maps to 65-VALIDATION.md EVAL-03 row (Wave 3, `npm run verify:phase`).
</verification>

<success_criteria>
A single authoritative command (verify:phase / verify:phase65) repeatably runs the v3.2 (Phase 61–65) end-to-end regression — build + AI draft-chain test subset + EVAL-01 eval suite + the five D-10 static boundary assertions + Phase 61/63/64 contract checks — and fails loudly on any regression (SC3 / EVAL-03 / D-08).
</success_criteria>

<output>
After completion, create `.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-05-SUMMARY.md`.
</output>
