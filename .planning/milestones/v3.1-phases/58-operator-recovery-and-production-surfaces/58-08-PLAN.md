---
phase: 58-operator-recovery-and-production-surfaces
plan: "08"
type: execute
wave: 7
depends_on:
  - "58-02"
  - "58-04"
  - "58-07"
files_modified:
  - src/features/runtime-platform/contracts/permissions.ts
  - src/features/platform-core/commands/handlers/plugins.ts
  - src/features/platform-core/commands/handlers/plugins.test.ts
  - src/actions/plugin-actions.ts
  - src/actions/plugin-actions.test.ts
  - src/actions/operator-posture-recovery-actions.ts
  - src/actions/operator-posture-recovery-actions.test.ts
  - scripts/verify-phase58-operator-recovery-and-surfaces.ts
  - scripts/verify-phase58-operator-recovery-and-surfaces.test.ts
autonomous: true
requirements:
  - OPS-03
  - PLUG-03
  - SAFE-02
  - OPS-01
gap_closure: true
must_haves:
  truths:
    - "operator/admin/developer 可以通过真正的 operator scope 执行 plugin `resume` / `suspend` / `fallback`。"
    - "`runOperatorPostureRecoveryAction()` 不再委托 teacher-scoped plugin mutation。"
    - "Phase 58 verifier 会对 teacher-only operator 恢复路径、route 未挂载、scope 漂移直接报错。"
  artifacts:
    - path: src/features/runtime-platform/contracts/permissions.ts
      provides: operator actor scope in command contract
    - path: src/features/platform-core/commands/handlers/plugins.ts
      provides: operator/admin/developer authorization for plugin governance commands
    - path: src/actions/plugin-actions.ts
      provides: operator-scoped plugin recovery actions
    - path: src/actions/operator-posture-recovery-actions.ts
      provides: high-risk operator recovery wrapper wired to operator-scoped mutations
    - path: scripts/verify-phase58-operator-recovery-and-surfaces.ts
      provides: hardened static gate for route mount and operator scope
  key_links:
    - from: src/actions/operator-posture-recovery-actions.ts
      to: src/actions/plugin-actions.ts
      via: operator-only plugin recovery entrypoints
      pattern: OperatorAction
    - from: src/actions/plugin-actions.ts
      to: src/features/platform-core/commands/handlers/plugins.ts
      via: actorScope: "operator"
      pattern: actorScope: "operator"
    - from: scripts/verify-phase58-operator-recovery-and-surfaces.ts
      to: src/actions/plugin-actions.ts
      via: static anti-pattern checks
      pattern: teacher-only
---

<objective>
闭合 Gap B：让高风险 plugin 恢复动作真正走 operator 可执行的 authorization path，并把 verifier 收紧到能挡回 teacher-only 回退。

Purpose: 让 Phase 58 不只是“看起来有按钮”，而是能被代码证明 admin/developer 复用的 support/operator scope 真能执行 `resume` / `suspend` / `fallback`，同时保持 audited、replay-safe 的 server-owned 恢复语义。
Output: operator actor scope 合同、plugin governance handler 授权扩展、operator-scoped plugin actions、rewired recovery wrapper、hardened verifier/self-test。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-CONTEXT.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-PATTERNS.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-VERIFICATION.md
@src/actions/plugin-actions.ts
@src/actions/operator-posture-recovery-actions.ts
@src/features/runtime-platform/contracts/permissions.ts
@src/features/platform-core/commands/handlers/plugins.ts
@scripts/verify-phase58-operator-recovery-and-surfaces.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 operator actor scope，并新增 operator-scoped plugin recovery actions</name>
  <read_first>
    - src/features/runtime-platform/contracts/permissions.ts
    - src/features/platform-core/commands/handlers/plugins.ts
    - src/actions/plugin-actions.ts
    - .planning/phases/58-operator-recovery-and-production-surfaces/58-VERIFICATION.md
    - .planning/phases/58-operator-recovery-and-production-surfaces/58-PATTERNS.md
  </read_first>
  <files>src/features/runtime-platform/contracts/permissions.ts, src/features/platform-core/commands/handlers/plugins.ts, src/features/platform-core/commands/handlers/plugins.test.ts, src/actions/plugin-actions.ts, src/actions/plugin-actions.test.ts</files>
  <action>在 `RuntimeActorScopeValues` 中新增 `operator`，并在 `authorizePluginGovernanceCommand()` 中为 plugin governance command 增加 operator 授权分支：system 仍直通；teacher 维持原路径；当 `command.actor.actorScope === "operator"` 时，必须校验当前登录用户拥有 active `admin` 或 `developer` membership，且目标 `schoolId` 在其 school scope 内。随后在 `src/actions/plugin-actions.ts` 中新增 operator 专用 entrypoints，例如 `transitionPluginLifecycleForOperatorAction()` 与 `setPluginKillSwitchForOperatorAction()`，它们必须显式用 `actorScope: "operator"` 调用 command bus，并用 admin/developer membership 解析 school scope，而不是复用 teacher-only 的 `resolvePluginSchoolId()`。测试要直接证明：admin 与 developer 能成功触发 `plugin.resume` / `plugin.suspend` / `plugin.kill_switch.set`；teacher-only operator 假冒路径与 foreign-school plugin 会被拒绝。</action>
  <verify>
    <automated>pnpm exec vitest --run src/features/platform-core/commands/handlers/plugins.test.ts src/actions/plugin-actions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/runtime-platform/contracts/permissions.ts` 包含 `"operator"` actor scope。
    - `src/features/platform-core/commands/handlers/plugins.ts` 明确处理 `command.actor.actorScope === "operator"`，并校验 active admin/developer school scope。
    - `src/actions/plugin-actions.ts` 新增 operator 专用 plugin recovery action，且这些 action 的 command dispatch 使用 `actorScope: "operator"`。
    - 测试覆盖 admin success、developer success、foreign-school deny、missing membership deny 四类结果。
  </acceptance_criteria>
  <done>高风险 plugin 恢复已拥有真正的 operator authz path，不再只能借 teacher scope 执行。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 重接 operator posture recovery 包装层，并把 verifier 收紧到阻断 teacher-only 回退</name>
  <read_first>
    - src/actions/operator-posture-recovery-actions.ts
    - src/actions/plugin-actions.ts
    - scripts/verify-phase58-operator-recovery-and-surfaces.ts
    - scripts/verify-phase58-operator-recovery-and-surfaces.test.ts
    - .planning/phases/58-operator-recovery-and-production-surfaces/58-VERIFICATION.md
  </read_first>
  <files>src/actions/operator-posture-recovery-actions.ts, src/actions/operator-posture-recovery-actions.test.ts, scripts/verify-phase58-operator-recovery-and-surfaces.ts, scripts/verify-phase58-operator-recovery-and-surfaces.test.ts</files>
  <action>把 `runOperatorPostureRecoveryAction()` 的 plugin 分支改为调用 Task 1 中新增的 operator-scoped plugin recovery actions，禁止继续直接委托 `transitionPluginLifecycleAction()` 与 `setPluginKillSwitchAction()` 这类 teacher-scoped entrypoints。`resume`、`suspend`、`fallback` 成功后必须继续更新 `cacheTags.pluginRegistry`、`cacheTags.plugin(pluginId)`，并 revalidate `/settings/labs`、`/settings/labs/plugins/[pluginId]`、`/settings/labs/plugins/[pluginId]/actions/[actionKey]` 等 operator route，保证 read-your-own-writes。随后收紧 `scripts/verify-phase58-operator-recovery-and-surfaces.ts`：新增静态检查，要求 plugin detail route 与 action detail route 存在且 page source 真正 render `PluginLifecycleOperatorSurface`；要求 operator recovery path 中存在 `actorScope: "operator"`、operator 专用 action 名称，以及 handler 的 operator authz 分支；并对“operator wrapper 仍调用 teacher-scoped plugin action”这一 anti-pattern 直接 fail。同步更新 verifier 自测，给出正例与反例样本，防止 Phase 58 再次出现 verifier 绿但 blocker 仍存在的情况。</action>
  <verify>
    <automated>pnpm exec vitest --run src/actions/operator-posture-recovery-actions.test.ts scripts/verify-phase58-operator-recovery-and-surfaces.test.ts</automated>
    <automated>pnpm verify:phase58</automated>
  </verify>
  <acceptance_criteria>
    - `src/actions/operator-posture-recovery-actions.ts` 的 plugin 分支不再直接调用 teacher-scoped `transitionPluginLifecycleAction` / `setPluginKillSwitchAction`。
    - verifier 静态检查会同时覆盖：plugin route existence、surface mounted、operator actor scope、operator wrapper 不再走 teacher-only path。
    - `pnpm verify:phase58` 只有在 Gap A route 挂载和 Gap B operator scope 都满足时才会通过。
  </acceptance_criteria>
  <done>operator recovery wrapper 与 verifier 都已对齐真实 operator scope，Phase 58 的两条 blocker 都会被 close gate 正确拦截或放行。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator surface → server action | 高风险恢复只能通过 audited server action 进入，不能伪装 teacher 身份或旁路写库 |
| server action → command handler | actor scope 与 school scope 必须在 handler 端再次验证，防止 client-side scope 假设 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-GB-01 | E | `plugin-actions.ts` operator recovery entrypoints | mitigate | 新增 operator-scoped action，并在 handler 端校验 active admin/developer membership + school scope |
| T-58-GB-02 | T | `operator-posture-recovery-actions.ts` wrapper | mitigate | wrapper 只调用 operator-scoped plugin mutations，继续执行 tag/path invalidation，禁止 teacher-only delegate |
| T-58-GB-03 | R | `verify-phase58-operator-recovery-and-surfaces.ts` | mitigate | verifier 新增 route mount + operator scope + anti-pattern checks，防止“验证绿但真实路径错”再次发生 |
</threat_model>

<verification>
  <automated>pnpm exec vitest --run src/features/platform-core/commands/handlers/plugins.test.ts src/actions/plugin-actions.test.ts src/actions/operator-posture-recovery-actions.test.ts scripts/verify-phase58-operator-recovery-and-surfaces.test.ts</automated>
  <automated>pnpm verify:phase58</automated>
</verification>

<success_criteria>
- Gap B 中 verifier 指出的 teacher-scoped recovery anti-pattern 被真实代码替换为 operator scope。
- admin/developer scope 的 success path 与 deny path 都有自动化测试证明。
- `pnpm verify:phase58` 现在能同时守住 route mount 与 operator authz 两类 blocker。
</success_criteria>

<decision_coverage>
- `D-58-01:` `58-05-PLAN.md` 保持 `runtime inspector`、`async operator`、`plugin governance` 为独立 next-hop surface，不做 mega dashboard。
- `D-58-02:` `58-01-PLAN.md`、`58-03-PLAN.md`、`58-05-PLAN.md`、`58-07-PLAN.md` 共同落实 classroom/session incident entry 与后续 runtime/plugin/command/task drill-down。
- `D-58-03:` `58-02-PLAN.md` 保留 classroom shell 的最小 recovery 与课堂内告警，不把诊断整体迁出课堂壳。
- `D-58-04:` `58-01-PLAN.md` 用 `classroomSessionId` 作为唯一排查锚点，避免退回 command/task/plugin-first 排障。
- `D-58-05:` `58-01-PLAN.md` 建立 authoritative classroom incident read model，收口 school、lesson version、runtime、plugin、action、command、task 关联状态。
- `D-58-06:` `58-01-PLAN.md` 与 `58-07-PLAN.md` 继续复用同一 DAL / governance truth，不维护 support/operator 与研发的双轨 read model。
- `D-58-07:` `58-02-PLAN.md`、`58-03-PLAN.md`、`58-07-PLAN.md` 让 command/task/plugin 保持 detail drill-down 节点，而不是首页主导航锚点。
- `D-58-08:` `58-01-PLAN.md`、`58-02-PLAN.md`、`58-03-PLAN.md`、`58-06-PLAN.md` 固定使用 trust-boundary honesty，而不是 badge-only / reason-code-only。
- `D-58-09:` `58-01-PLAN.md`、`58-02-PLAN.md`、`58-03-PLAN.md`、`58-06-PLAN.md` 都要求显式呈现 impact scope，并区分当前课堂与平台级影响。
- `D-58-10:` `58-01-PLAN.md`、`58-02-PLAN.md`、`58-03-PLAN.md`、`58-06-PLAN.md` 都要求说明仍可信什么、已不可信什么与下一步。
- `D-58-11:` `58-01-PLAN.md`、`58-02-PLAN.md`、`58-03-PLAN.md`、`58-04-PLAN.md`、`58-06-PLAN.md` 统一 classroom/runtime/async/plugin 的 honesty 口径。
- `D-58-12:` `58-01-PLAN.md`、`58-02-PLAN.md`、`58-03-PLAN.md`、`58-08-PLAN.md` 共同保证 operator surface 提供可执行恢复动作，而不是只给日志和跳转。
- `D-58-13:` `58-02-PLAN.md`、`58-03-PLAN.md`、`58-08-PLAN.md` 维持 `retry` / `reconcile` 轻确认、`resume` / `suspend` / `fallback` 强确认的风险分级。
- `D-58-14:` `58-03-PLAN.md`、`58-07-PLAN.md`、`58-08-PLAN.md` 要求 action availability reason-gated，并给出正确的下一步入口。
- `D-58-15:` `58-01-PLAN.md`、`58-02-PLAN.md`、`58-03-PLAN.md`、`58-04-PLAN.md`、`58-08-PLAN.md` 都要求所有 recovery 继续走 Server Actions / command bus / DAL / audited recovery services。
- `D-58-16:` `58-05-PLAN.md` 保留 `Settings Labs` 作为汇总索引入口，而不替代 classroom/session incident entry。
- `D-58-17:` `58-02-PLAN.md` 保证 classroom 页 operator 入口稳定存在，并在 degraded / failed / blocked posture 时升级为显眼 CTA。
- `D-58-18:` `58-05-PLAN.md` 让 `/settings/labs` 第一屏优先展示 classroom incident list。
- `D-58-19:` `58-01-PLAN.md` 与 `58-03-PLAN.md` 固定 incident detail 首屏为摘要优先，而不是 timeline-first / giant detail page。
- `D-58-20:` `58-01-PLAN.md` 与 `58-03-PLAN.md` 固定首屏按 current problem / incident cards 组织，而不是 Runtime / Plugin / Command / Task 平铺分区。
- `D-58-21:` `58-01-PLAN.md` 与 `58-03-PLAN.md` 将关联对象压缩到状态 + 原因/影响 + 下一跳粒度，把 attempt history / raw diagnostics 留在 detail surface。
- `D-58-22:` `58-01-PLAN.md`、`58-02-PLAN.md`、`58-03-PLAN.md`、`58-04-PLAN.md`、`58-06-PLAN.md` 共享固定三段 honesty 模板：`仍可信什么 / 已不可信什么 -> 影响范围 -> 推荐下一步`。
- `D-58-23:` `58-02-PLAN.md` 与 `58-03-PLAN.md` 将 quick path 严格限制为 `retry` / `reconcile`，不把 `resume` 留在课堂壳快路径。
- `D-58-24:` `58-02-PLAN.md`、`58-03-PLAN.md`、`58-07-PLAN.md`、`58-08-PLAN.md` 要求 `resume` / `suspend` / `fallback` 只能在相关 detail view 内完成强确认。
- `D-58-25:` `58-01-PLAN.md`、`58-02-PLAN.md`、`58-03-PLAN.md`、`58-07-PLAN.md` 保持不可执行动作可见，并以 disabled + reason + next step 解释为什么当前不能做。
</decision_coverage>

<output>
After completion, create `.planning/phases/58-operator-recovery-and-production-surfaces/58-08-SUMMARY.md`
</output>
