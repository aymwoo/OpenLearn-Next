---
phase: 58-operator-recovery-and-production-surfaces
plan: "05"
type: execute
wave: 4
depends_on:
  - "58-01"
  - "58-03"
files_modified:
  - src/app/settings/labs/page.tsx
  - src/app/settings/labs/incidents/page.tsx
  - src/components/surfaces/settings-surface.tsx
  - src/components/surfaces/classroom-incident-list-surface.tsx
  - src/components/surfaces/settings-surface.test.tsx
autonomous: true
requirements:
  - OPS-01
  - OPS-02
must_haves:
  truths:
    - "operator 从 `/settings/labs` 进入时第一屏先看到 classroom incidents，而不是工具目录。"
    - "incident list 保持 classroom-first stacked cards，而不是 dense admin table。"
  artifacts:
    - path: src/app/settings/labs/incidents/page.tsx
      provides: incident-first fallback entry route
    - path: src/components/surfaces/classroom-incident-list-surface.tsx
      provides: stacked incident cards surface
    - path: src/app/settings/labs/page.tsx
      provides: settings labs incident-first landing
  key_links:
    - from: src/app/settings/labs/page.tsx
      to: src/components/surfaces/classroom-incident-list-surface.tsx
      via: Settings Labs incident-first entry
      pattern: classroom incidents
---

<objective>
把 `Settings Labs` 调整为 incident-first fallback entry，并实现 classroom incident list surface。

Purpose: 在不扩大 58-03 范围的前提下，完成 Phase 58 的 fallback 入口与 incident list，保持 classroom-first operator 心智模型。
Output: incident-first Settings Labs、incident list route/surface、focused UI tests。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-CONTEXT.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-RESEARCH.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-UI-SPEC.md
@src/app/settings/labs/page.tsx
@src/components/surfaces/settings-surface.tsx
@src/lib/dto/classroom-incident-list.ts
@src/lib/dal/classroom-incident-list.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先用测试锁定 incident-first Settings Labs 入口与 list surface</name>
  <files>src/components/surfaces/settings-surface.test.tsx, src/app/settings/labs/page.tsx, src/app/settings/labs/incidents/page.tsx, src/components/surfaces/classroom-incident-list-surface.tsx</files>
  <behavior>
    - Test 1: `/settings/labs` 默认先呈现 classroom incident list，再呈现 Runtime Inspector / Async Operator / Plugin Governance next-hop cards。
    - Test 2: incident list 使用 stacked cards，不回退成 dense table；每张卡只显示 posture、原因/影响摘要、impact scope、updatedAt、主 CTA 和最多 2 个关联 chips。
    - Test 3: empty/error state copy 与 UI-SPEC 一致：先说明当前状态，再给 operator 去向。
  </behavior>
  <action>按 D-58-01、D-58-02、D-58-16、D-58-18 与 UI-SPEC 重做 `Settings Labs` 的首屏信息架构：新增 `/settings/labs/incidents` 与 `ClassroomIncidentListSurface`，并让 `/settings/labs` 在没有 classroom deep link 时优先渲染 incident-first entry，而不是工具宫格。保留 runtime inspector、async operator、plugin governance 作为 next-hop cards，不做 mega dashboard，不引入 data table。</action>
  <verify>
    <automated>pnpm exec vitest --run src/components/surfaces/settings-surface.test.tsx</automated>
  </verify>
  <done>`/settings/labs` 与 `/settings/labs/incidents` 已变成 incident-first fallback entry，且 card anatomy、empty state、tool next hops 都被测试锁定。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings Labs entry → incident list surface | 只能消费 authoritative list DTO，不允许 UI 端改写 posture 或 impact scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-10 | R | incident list UI | mitigate | 用测试锁定 stacked card anatomy、empty/error copy 与 next-hop cards，防止回退到 tool-first landing |

</threat_model>

<verification>
- `pnpm exec vitest --run src/components/surfaces/settings-surface.test.tsx`
- 静态检查：`ClassroomIncidentListSurface` 不引入 data table / dense grid / border-heavy layout。
</verification>

<success_criteria>
- `/settings/labs` 默认成为 incident-first fallback entry。
- `/settings/labs/incidents` 可展示 classroom-first incident list 与 tool next-hop cards。
</success_criteria>

<output>
After completion, create `.planning/phases/58-operator-recovery-and-production-surfaces/58-05-SUMMARY.md`
</output>
