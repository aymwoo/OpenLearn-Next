---
phase: 58-operator-recovery-and-production-surfaces
plan: "03"
type: execute
wave: 3
depends_on:
  - "58-01"
  - "58-02"
files_modified:
  - src/actions/operator-classroom-recovery-actions.ts
  - src/app/settings/labs/incidents/[sessionId]/page.tsx
  - src/components/surfaces/classroom-incident-operator-surface.tsx
  - src/components/surfaces/classroom-incident-operator-surface.test.tsx
  - src/actions/operator-classroom-recovery-actions.test.ts
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-03
  - PLUG-03
must_haves:
  truths:
    - "incident detail 首屏先回答这堂课现在发生了什么，并给出统一 honesty template 与 next-hop cards。"
    - "surface 上不可执行动作保持可见，并带 disabled reason 与 next step。"
  artifacts:
    - path: src/app/settings/labs/incidents/[sessionId]/page.tsx
      provides: classroom/session operator detail route
    - path: src/actions/operator-classroom-recovery-actions.ts
      provides: summary-surface retry/reconcile Server Action entrypoint
    - path: src/components/surfaces/classroom-incident-operator-surface.tsx
      provides: summary-first incident detail surface
  key_links:
    - from: src/components/surfaces/classroom-incident-operator-surface.tsx
      to: /settings/labs/runtime-inspector
      via: honesty next-step and related object cards
      pattern: Runtime Inspector
    - from: src/components/surfaces/classroom-incident-operator-surface.tsx
      to: /settings/labs/async-tasks/[taskId]
      via: problem task cards
      pattern: 查看任务详情
    - from: src/components/surfaces/classroom-incident-operator-surface.tsx
      to: /settings/labs
      via: plugin action / governance next hop
      pattern: actionId|pluginId|diagnostics
---

<objective>
把 Phase 58 的 classroom/session incident detail 与 light recovery path 落到正式 route 与 surface。

Purpose: 先把 incident detail、honesty posture 与 light recovery path 落稳，再由后续小计划承接 `Settings Labs` incident-first entry/list。
Output: incident detail route、summary-first detail surface、light recovery Server Action、focused UI tests。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-CONTEXT.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-RESEARCH.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-UI-SPEC.md
@src/components/surfaces/runtime-inspector-surface.tsx
@src/components/surfaces/async-task-operator-surface.tsx
@src/components/surfaces/async-task-operator-detail-surface.tsx
@src/components/surfaces/plugin-lifecycle-operator-surface.tsx
@src/lib/dto/classroom-incident-list.ts
@src/lib/dto/classroom-incident-operator.ts
@src/lib/dal/classroom-incident-list.ts
@src/lib/dal/classroom-incident-operator.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先用测试锁定 incident detail 的 summary-first IA 与 light actions</name>
  <files>src/actions/operator-classroom-recovery-actions.ts, src/actions/operator-classroom-recovery-actions.test.ts, src/app/settings/labs/incidents/[sessionId]/page.tsx, src/components/surfaces/classroom-incident-operator-surface.tsx, src/components/surfaces/classroom-incident-operator-surface.test.tsx</files>
  <behavior>
    - Test 1: detail 首屏顺序固定为 hero → metrics → honesty card → problem cards → related object cards → light actions，不渲染 full timeline / attempt history / raw diagnostics。
    - Test 2: honesty card 固定输出 `仍可信什么 / 已不可信什么 -> 影响范围 -> 推荐下一步` 三段，且不同 posture 只替换内容不改顺序。
    - Test 3: `retry` / `reconcile` 可在 summary 层通过 `operator-classroom-recovery-actions` Server Action 执行；`resume` / `suspend` / `fallback` 只以 disabled+reason+detail href 出现。
    - Test 4: relation cards 与 next-hop cards 必须显式覆盖 plugin action drill-down，而不是只有 plugin posture 标签。
  </behavior>
  <action>实现 `/settings/labs/incidents/[sessionId]`、`ClassroomIncidentOperatorSurface` 与 `operator-classroom-recovery-actions`。按 D-58-08、D-58-09、D-58-10、D-58-11、D-58-19、D-58-20、D-58-21、D-58-22 渲染 summary-first incident detail。按 D-58-12、D-58-13、D-58-14、D-58-15、D-58-24、D-58-25 只让 `retry`/`reconcile` 通过 Server Action 轻确认执行，且该 action 必须复用 58-01 的 server-owned recovery seam 并承担 `updateTag()` / `revalidatePath()`；其余 posture-changing actions 保持可见但 disabled，并给出 reason 与 next-step href 到 runtime/async/command/plugin/action detail。</action>
  <verify>
    <automated>pnpm exec vitest --run src/actions/operator-classroom-recovery-actions.test.ts src/components/surfaces/classroom-incident-operator-surface.test.tsx</automated>
  </verify>
  <done>incident detail 已满足 summary-first IA、honesty 三段模板、可执行 light actions 与 guarded high-risk actions 的显示规则；operator 可以从 incident surface 直接跳到 runtime / async / command / plugin / action 下钻页，且 `retry`/`reconcile` 已走正式 Server Action + audited recovery seam。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| incident detail route → summary surface | 只能消费 authoritative DTO，不允许 UI 端改写 risk posture |
| incident detail → recovery actions | 仅 light actions 在 summary 执行，高风险动作必须导向 detail confirmation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-07 | R | degraded honesty UI | mitigate | 静态与组件测试同时断言三段模板与 next-step 文案，防止页面各自发明 copy |
| T-58-08 | E | incident summary actions | mitigate | summary surface 只启用 `retry`/`reconcile`，其余动作 disabled + reason + detailHref |
| T-58-09 | I | related object cards | mitigate | 只展示 status / reason / next-hop 粒度，不暴露 raw diagnostics / hidden IDs |
</threat_model>

<verification>
- `pnpm exec vitest --run src/actions/operator-classroom-recovery-actions.test.ts src/components/surfaces/classroom-incident-operator-surface.test.tsx`
- 静态检查：`ClassroomIncidentOperatorSurface` 不包含 `timeline.map(`、`attemptGroups`、`raw diagnostics` 类 detail-only 内容。
</verification>

<success_criteria>
- `/settings/labs/incidents/[sessionId]` 可展示 summary-first incident detail、honesty posture、problem cards 与 next-hop cards。
- incident surface 上高风险动作只以 disabled + reason + next step 出现，不直接执行；`retry` / `reconcile` 走正式 Server Action 与缓存失效接线。
</success_criteria>

<output>
After completion, create `.planning/phases/58-operator-recovery-and-production-surfaces/58-03-SUMMARY.md`
</output>
