---
phase: 58-operator-recovery-and-production-surfaces
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dto/classroom-incident-list.ts
  - src/lib/dto/classroom-incident-operator.ts
  - src/lib/dal/classroom-incident-list.ts
  - src/lib/dal/classroom-incident-operator.ts
  - src/lib/dal/classroom-incident-operator-actions.ts
  - src/lib/dal/classroom-incident-operator.test.ts
autonomous: true
requirements:
  - OPS-01
  - SAFE-02
must_haves:
  truths:
    - "operator 可以从单个 classroom/session DTO 同时看到 school、lesson version、runtime、plugin、command、task 关联真相。"
    - "operator 可以从同一 DTO 与 relation cards 看到 plugin/action/command/task 的稳定关联与下钻入口，而不是只停留在 plugin posture。"
    - "support/operator 与研发读取同一 authoritative read model，而不是页面端拼接四份数据。"
  artifacts:
    - path: src/lib/dto/classroom-incident-list.ts
      provides: incident list contract
    - path: src/lib/dto/classroom-incident-operator.ts
      provides: classroom/session incident detail contract
    - path: src/lib/dal/classroom-incident-list.ts
      provides: incident list read model
    - path: src/lib/dal/classroom-incident-operator.ts
      provides: classroom/session correlation read model
    - path: src/lib/dal/classroom-incident-operator-actions.ts
      provides: summary-surface light recovery seam and action relation mapping
    - path: src/lib/dal/classroom-incident-operator.test.ts
      provides: scoped correlation regression coverage
  key_links:
    - from: src/lib/dal/classroom-incident-operator.ts
      to: src/lib/dal/runtime-inspector.ts
      via: runtimeSessionId correlation
      pattern: runtimeSessionId
    - from: src/lib/dal/classroom-incident-operator.ts
      to: src/features/platform-core/observability/operator-read-model.ts
      via: command correlation and detail hrefs
      pattern: commandId|correlationId
    - from: src/lib/dal/classroom-incident-operator.ts
      to: src/lib/dal/async-task-operator.ts
      via: problem task mapping
      pattern: taskId|detailHref
    - from: src/lib/dal/classroom-incident-operator.ts
      to: src/features/platform-core/actions/registry.ts
      via: plugin action correlation and action detail hrefs
      pattern: actionId|actionKey|recommendedRecoveryAction
---

<objective>
建立 Phase 58 的 authoritative classroom incident contracts 与 server-owned correlation read model。

Purpose: 先把“这堂课现在发生了什么”收敛成单一真相，再让后续 surface 直接消费，不再手工拼 runtime / plugin / command / task 多页信息。
Output: incident list/detail DTO、DAL read model、focused tests。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-CONTEXT.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-RESEARCH.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-VALIDATION.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-UI-SPEC.md
@.planning/phases/55-pilot-scope-and-acceptance-gate/55-FAILURE-RECOVERY-MATRIX.md
@src/lib/dal/runtime-inspector.ts
@src/lib/dal/async-task-operator.ts
@src/features/platform-core/observability/operator-read-model.ts
@src/lib/dto/runtime-inspector.ts
@src/lib/dto/async-task-operator.ts

<interfaces>
From `src/features/platform-core/observability/operator-read-model.ts`:

```ts
export async function listOperatorVisiblePlatformCommands(input: {
  schoolIds: string[];
  limit: number;
}): Promise<PlatformCommandOperatorSummaryDTO[]>;

export async function getPlatformCommandWithTimeline(input: {
  commandId: string;
  schoolIds: string[];
}): Promise<PlatformCommandOperatorDetailDTO>;
```

From `src/lib/dto/runtime-inspector.ts`:

```ts
export const RuntimeInspectorHealthSummaryDTOSchema = z.object({
  transportTopology: z.enum(["local_only", "redis_fanout", "degraded_local_fallback", "unknown"]),
  degraded: z.boolean(),
  degradedReason: z.string().nullable(),
});
```

From `src/lib/dto/async-task-operator.ts`:

```ts
export const AsyncTaskProblemTaskDTOSchema = z.object({
  taskId: z.string().min(1),
  detailHref: z.string().min(1),
  reason: z.string().min(1),
  latestError: z.string().nullable().default(null),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 固化 incident DTO 合同并先写失败测试</name>
  <files>src/lib/dto/classroom-incident-list.ts, src/lib/dto/classroom-incident-operator.ts, src/lib/dal/classroom-incident-operator-actions.ts, src/lib/dal/classroom-incident-operator.test.ts</files>
  <behavior>
    - Test 1: list DTO 只暴露 classroom-first 摘要：classroom/session、posture、原因/影响摘要、impact scope、更新时间、detailHref、最多 2 个关联 chips。
    - Test 2: detail DTO 只暴露 summary-first 信息：hero、3-4 metrics、honesty card、problem cards、related object cards、light actions、guarded high-risk actions；不含 full timeline / raw diagnostics。
    - Test 3: relation cards 必须显式覆盖 `plugin`、`action`、`command`、`task` 四类节点，并输出稳定 href / nextStepHref，而不是只保留 plugin posture。
    - Test 4: foreign-school classroom/session 不会泄漏到当前 actor scope。
    - Test 5: 不新增 `support` membership role；继续复用 admin/developer scope，并共享同一 truth source。
  </behavior>
  <action>新增 Zod contracts、summary-surface light recovery seam 与 failing tests，锁定 D-58-04、D-58-05、D-58-06、D-58-19、D-58-20、D-58-21、D-58-22。DTO 字段必须明确包含：classroom/session identity、lessonVersionLabel、runtimeSessionId、plugin posture summary、plugin action summary、latest command summary、problem task summaries、honesty sections、quick actions (`retry`/`reconcile`) 与 guarded actions (`resume`/`suspend`/`fallback`) 的 disabled+reason+nextStepHref。测试要直接断言“首屏不是 giant detail page”，并断言 action-level relation 与 recovery availability metadata 已落到 contract，防止后续 surface 漂移。</action>
  <verify>
    <automated>pnpm exec vitest --run src/lib/dal/classroom-incident-operator.test.ts</automated>
  </verify>
  <done>incident list/detail contract 已被测试锁定，且 scope、summary-first IA、honesty 模板顺序都在红灯测试中失败。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 classroom/session authoritative read model</name>
  <files>src/lib/dto/classroom-incident-list.ts, src/lib/dto/classroom-incident-operator.ts, src/lib/dal/classroom-incident-list.ts, src/lib/dal/classroom-incident-operator.ts, src/lib/dal/classroom-incident-operator-actions.ts, src/lib/dal/classroom-incident-operator.test.ts</files>
  <behavior>
    - Test 1: `getClassroomIncidentListDTO()` 返回 incident-first rows，且按 posture/updatedAt 输出可排序数据。
    - Test 2: `getClassroomIncidentOperatorDTO()` 通过 `classroomSessionId` 收口 school、lesson version、runtime、plugin、action、command、task 关联，不让页面自己补请求。
    - Test 3: summary-surface light recovery seam 明确提供 `retry` / `reconcile` 的 server-owned action metadata、reason gating 与 audited next hop，不停留在 UI metadata。
    - Test 4: degraded honesty 数据按 `仍可信什么 / 已不可信什么 -> 影响范围 -> 推荐下一步` 输出，不退化成 badge 或 reasonCode。
  </behavior>
  <action>实现三个 server-only seam：`getClassroomIncidentListDTO()`、`getClassroomIncidentOperatorDTO()` 与 `runClassroomIncidentLightRecovery()`。按 D-58-04、D-58-05、D-58-06 使用 `classroomSessionId` 作为唯一排查锚点，把 Phase 57 classroom truth、runtime inspector health、platform command observability、async problem tasks、plugin governance posture、plugin action correlation 收敛到同一 DTO。按 D-58-09、D-58-10、D-58-11、D-58-22 统一输出 honesty data；impact scope 至少区分“当前课堂受影响”与“平台/多课堂受影响”。`runClassroomIncidentLightRecovery()` 必须明确复用现有 server-owned seams 执行 `retry` / `reconcile`，写入 command/recovery audit/task history，并为 downstream surface 提供稳定 action target；禁止客户端旁路写入。继续复用 admin/developer scope，不扩角色模型。为 downstream surface 直接输出稳定 href：`/settings/labs/incidents/[sessionId]`、`/settings/labs/runtime-inspector?runtimeSessionId=...`、`/settings/labs/async-tasks/[taskId]`、`/settings/labs/commands/[commandId]`、plugin governance/action detail href。</action>
  <verify>
    <automated>pnpm exec vitest --run src/lib/dal/classroom-incident-operator.test.ts src/features/platform-core/observability/operator-read-model.test.ts</automated>
  </verify>
  <done>incident list/detail read model 已能在单次 server read 中返回 classroom-first authoritative truth、honesty posture 和 stable drill-down href，且 foreign-school 数据被拒绝。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator request → DAL correlation seam | 只能读取当前 actor school scope 内的 classroom incident truth |
| DAL correlation seam → downstream drill-down hrefs | 只能输出 server-owned、可审计的 next-hop，不允许浏览器本地拼接 truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | I | `getClassroomIncidentOperatorDTO` | mitigate | 按 active membership schoolIds 过滤 classroom/runtime/command/task/plugin 关联，测试覆盖 foreign-school rejection |
| T-58-02 | T | incident DTO assembly | mitigate | 所有关联状态在 server-owned DAL seam 中一次组装；页面只消费 DTO，不自行拼多请求 |
| T-58-03 | R | recovery affordance metadata | mitigate | 只返回 action availability、reason 与 nextStepHref，不在 read model 中执行任何 mutation |
</threat_model>

<verification>
- `pnpm exec vitest --run src/lib/dal/classroom-incident-operator.test.ts src/features/platform-core/observability/operator-read-model.test.ts`
- 确认新增 DAL 文件均含 `import "server-only";`，且未直接出现在 `src/app/**` UI 组件内。
</verification>

<success_criteria>
- `src/lib/dal/classroom-incident-list.ts`、`src/lib/dal/classroom-incident-operator.ts` 与 `src/lib/dal/classroom-incident-operator-actions.ts` 存在并导出可复用 read/mutation seam。
- incident detail DTO 已包含 classroom/session hero、summary metrics、honesty sections、problem cards、plugin/action/command/task related hrefs、light/guarded action metadata。
- scope 过滤、summary-first IA 与 honesty 模板由自动化测试锁定。
</success_criteria>

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | operator/support 可在不改库前提下定位并恢复课堂投票链路故障 | 01-04 | COVERED | 01 truth，02-03 surface/recovery，04 proof |
| REQ | OPS-01 | school/classroom/lesson/plugin/action/command/task 关联定位 | 01-03 | COVERED | 01 read model；02-03 UI drill-down |
| REQ | OPS-02 | degraded honesty 暴露 | 01-04 | COVERED | 01 DTO；02 surface；04 verifier/proof |
| REQ | OPS-03 | 无需 DB surgery 的恢复动作与 runbook | 02-04 | COVERED | 02-03 action placement；04 runbook/proof |
| REQ | PLUG-03 | plugin failure taxonomy + recovery actions | 01-04 | COVERED | incident DTO + plugin governance deep-link + proof |
| REQ | SAFE-02 | recovery audit-safe / replay-safe | 01-04 | COVERED | 01 metadata；02-03 server-owned seams；04 verifier |
| RESEARCH | — | classroom/session authoritative correlation read model | 01 | COVERED | primary recommendation |
| RESEARCH | — | incident-first Settings Labs entry | 03 | COVERED | `/settings/labs` first screen becomes incident-first |
| RESEARCH | — | risk-tiered recovery placement | 02-03 | COVERED | quick path only retry/reconcile |
| RESEARCH | — | phase verifier + support runbook excerpt | 04 | COVERED | proof inventory requirement |
| CONTEXT | D-58-01 | 不做 mega dashboard | 03 | COVERED | 保留独立 surfaces |
| CONTEXT | D-58-02 | 新增 classroom/session 起点 | 01,03 | COVERED | read model + incident entry |
| CONTEXT | D-58-03 | teacher shell 保留最小 recovery | 02 | COVERED | shell 仅保留最小入口 |
| CONTEXT | D-58-04 | 排查锚点固定 classroom/session | 01 | COVERED | read model anchored by classroomSessionId |
| CONTEXT | D-58-05 | authoritative read model 投影全部关联状态 | 01 | COVERED | single DTO seam |
| CONTEXT | D-58-06 | support/operator 与研发共享 truth source | 01 | COVERED | no split read models |
| CONTEXT | D-58-07 | command/task/plugin 保持下钻节点 | 02,03 | COVERED | relation cards only |
| CONTEXT | D-58-08 | degraded posture 强提示 + trust boundary | 01,03 | COVERED | DTO + UI |
| CONTEXT | D-58-09 | posture 要显示影响范围 | 01,03 | COVERED | impact scope in DTO and UI |
| CONTEXT | D-58-10 | 说明仍可信 / correctness boundary | 01,03 | COVERED | honesty sections |
| CONTEXT | D-58-11 | honesty 口径跨页一致 | 01,03,04 | COVERED | shared template + verifier |
| CONTEXT | D-58-12 | surface 提供可执行恢复动作 | 01-03 | COVERED | action metadata + surface wiring |
| CONTEXT | D-58-13 | 分级确认 | 02-03 | COVERED | quick vs detail-only |
| CONTEXT | D-58-14 | availability reason-gated | 01-03 | COVERED | disabled + reason |
| CONTEXT | D-58-15 | recovery 仅走 server-owned seams | 01-04 | COVERED | no DB surgery |
| CONTEXT | D-58-16 | Settings Labs 保留汇总索引入口 | 03 | COVERED | incident-first fallback index |
| CONTEXT | D-58-17 | classroom operator 入口稳定且可升格 | 02 | COVERED | shell CTA escalation |
| CONTEXT | D-58-18 | Settings Labs 第一屏优先 incident list | 03 | COVERED | incident-first entry |
| CONTEXT | D-58-19 | 首屏摘要优先 | 01,03 | COVERED | DTO + surface contract |
| CONTEXT | D-58-20 | 首屏按 current problem cards 组织 | 01,03 | COVERED | problem-first list/detail |
| CONTEXT | D-58-21 | 关联对象只到状态/原因/下一跳粒度 | 01,03 | COVERED | no raw diagnostics |
| CONTEXT | D-58-22 | honesty 固定三段模板 | 01,03,04 | COVERED | shared contract + verifier |
| CONTEXT | D-58-23 | quick path 只限 retry/reconcile | 02 | COVERED | shell + incident surface filtering |
| CONTEXT | D-58-24 | 高风险动作只在 detail view 确认 | 02-03 | COVERED | guarded actions route to detail |
| CONTEXT | D-58-25 | 不可执行动作 disabled + reason + next step | 01-03 | COVERED | DTO + surface rendering |

</source_audit>

<output>
After completion, create `.planning/phases/58-operator-recovery-and-production-surfaces/58-01-SUMMARY.md`
</output>
