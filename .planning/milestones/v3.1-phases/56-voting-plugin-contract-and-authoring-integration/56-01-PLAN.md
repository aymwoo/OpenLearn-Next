---
phase: 56-voting-plugin-contract-and-authoring-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dto/resource-ai.ts
  - src/lib/dal/plugins.ts
  - src/components/authoring/lesson-authoring-workspace.tsx
  - src/components/authoring/lesson-authoring-workspace.test.tsx
autonomous: true
requirements:
  - PLUG-01
  - PLUG-02
  - CHAIN-01
must_haves:
  truths:
    - "课堂投票插件必须复用现有 lesson step shell，不新增第四种 core step type。"
    - "教师只能看到当前学校可用、已启用、版本兼容的课堂投票插件模板或配置入口。"
    - "authoring config 必须有正式 schema 和默认值，而不是自由 JSON。"
---

<objective>
为课堂投票插件建立正式的 authoring contract，让它在现有 teacher editor 中可发现、可插入、可配置，并受学校范围与 lifecycle/compatibility 门禁约束。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-pilot-scope-and-acceptance-gate/55-PILOT-CONTRACT.md
@.planning/phases/56-voting-plugin-contract-and-authoring-integration/56-CONTEXT.md
@.planning/phases/56-voting-plugin-contract-and-authoring-integration/56-RESEARCH.md
@src/lib/dto/lesson-authoring.ts
@src/lib/dto/resource-ai.ts
@src/lib/dal/plugins.ts
@src/components/authoring/lesson-authoring-workspace.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the voting plugin authoring contract and inject it into the existing editor workspace</name>
  <files>src/lib/dto/resource-ai.ts, src/lib/dal/plugins.ts, src/components/authoring/lesson-authoring-workspace.tsx, src/components/authoring/lesson-authoring-workspace.test.tsx</files>
  <read_first>
    - src/lib/dto/lesson-authoring.ts
    - src/lib/dto/resource-ai.ts
    - src/lib/dal/plugins.ts
    - src/components/authoring/lesson-authoring-workspace.tsx
    - .planning/phases/56-voting-plugin-contract-and-authoring-integration/56-CONTEXT.md
  </read_first>
  <action>在不新增第四种 `lessonStep.type` 的前提下，为课堂投票插件定义正式的 authoring contract。优先沿用现有 built-in teaching template / lesson.sidebar integration 模式：为 classroom voting 样板增加明确的 template/config schema、默认值、teacher-facing summary，并让 `LessonAuthoringWorkspace` 只在 school-scoped enabled + lifecycle/compatibility 通过时显示它。不要把投票插件做成自由 JSON blob，不要只挂在 sidebar 不入 step chain，也不要把 plugin private config 直接污染核心 `lessonStep.payloadJson` 的通用结构。若需要在 step shell 中保留最小公开引用，只允许保留运行该样板所需的最小 public metadata；plugin-private config 应由 plugin-scoped contract 持有。补上 focused tests，证明该样板只在合格插件可用时出现，且插入 editor 的路径保留现有 built-in / generic step 心智。</action>
  <acceptance_criteria>
    - 课堂投票插件在 authoring 侧有正式 schema/default contract。
    - teacher editor 只在 plugin 可用时显示该样板入口。
    - 不新增新的 core step type。
    - focused tests 覆盖可见性门禁和插入 contract。
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/components/authoring/lesson-authoring-workspace.test.tsx src/lib/dal/plugins.builtins.test.ts</automated>
  </verify>
</task>

</tasks>

<output>
After completion, create `.planning/phases/56-voting-plugin-contract-and-authoring-integration/56-01-SUMMARY.md`
</output>
