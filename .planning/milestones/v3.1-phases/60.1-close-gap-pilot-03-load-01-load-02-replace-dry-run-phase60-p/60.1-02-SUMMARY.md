---
phase: 60.1-close-gap-pilot-03-load-01-load-02-replace-dry-run-phase60-p
plan: "02"
subsystem: local-proof-sqlite
tags: [phase60, sqlite, fixtures, blocker-removal]
completed: 2026-05-29
---

# Phase 60.1 Plan 02 Summary

## Accomplishments

- 在 `src/db/index.ts` 为本地 `file:` SQLite 路径新增 `ensureLocalSqliteConcurrencyPragmas()`，通过单例 promise 只执行一次 `PRAGMA journal_mode = WAL`、`PRAGMA busy_timeout = 5000`、`PRAGMA synchronous = NORMAL`，同时保留原有单一 `createClient()` 与 `db = drizzle(client, { schema })` 结构。
- 新增 `src/db/index.test.ts`，锁定本地 SQLite bootstrap、并发复用同一 promise、以及远端 `libsql://` 路径 no-op 这三条回归约束。
- 在 `scripts/load/phase60-fixtures.ts` 引入 `withSqliteBusyRetry()`，把 `upsertUser()`、membership/school/course/class/session/evidence 等高争用写路径改为仅对 `SQLITE_BUSY` 做有限退避重试。
- 当共享 SQLite 长时间被占用时，fixture seeding 现在抛出显式 blocker：`PHASE60_LOCAL_SQLITE_BUSY_BLOCKER`，并提示停止残留 `server.ts` / worker 持锁进程或改走 `pnpm verify:phase60:local` 隔离 lane。
- 新增 `scripts/load/phase60-fixtures.test.ts`，锁定 bootstrap 调用、busy-only retry、retry exhaustion blocker、以及 non-busy 错误不重试的行为。

## Verification

- `pnpm exec eslint src/db/index.ts src/db/index.test.ts scripts/load/phase60-fixtures.ts scripts/load/phase60-fixtures.test.ts`
- `pnpm exec vitest --run src/db/index.test.ts scripts/load/phase60-fixtures.test.ts`

## Notes

- 该计划只移除了 repo-local substitute lane 的 SQLite blocker；`PILOT-03` / `LOAD-01` / `LOAD-02` 仍未关闭，live evidence closeout 仍由 `60.1-01` 完成。
- 这里没有引入第二个 DB client，也没有改变 `ensurePhase60Fixtures()` 的单入口契约；修复目标仅限本地 proof 写锁姿态与显式 blocker 语义。
