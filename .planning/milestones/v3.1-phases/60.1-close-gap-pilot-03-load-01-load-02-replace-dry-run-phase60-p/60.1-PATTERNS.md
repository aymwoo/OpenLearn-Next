# Phase 60.1: close-gap-pilot-03-load-01-load-02-replace-dry-run-phase60-p - Pattern Map

**Mapped:** 2026-05-29
**Files analyzed:** 16
**Analogs found:** 16 / 16

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `scripts/verify-phase60-load-and-rehearsal.ts` | utility | batch | `scripts/verify-phase59-deploy-release.ts` | role-match |
| `scripts/verify-phase60-local.ts` | utility | file-I/O | `scripts/verify-phase59-deploy-release.ts` | role-match |
| `scripts/proof-phase60-load-smoke.ts` | utility | batch | `scripts/proof-phase58-operator-recovery.ts` | role-match |
| `scripts/rehearse-phase60-rollout-rollback.ts` | utility | batch | `scripts/rehearse-phase60-rollout-rollback.ts` | exact |
| `scripts/load/phase60-fixtures.ts` | service | CRUD | `scripts/proof-phase57-classroom-runtime.ts` | data-flow-match |
| `src/db/index.ts` | config | CRUD | `src/db/index.ts` | self-only |
| `package.json` (`verify:phase60:local`) | config | batch | `package.json` (`verify:phase60`) | exact-shape |
| `.planning/debug/phase60-local-sqlite-busy.md` | config | transform | `.planning/debug/phase54-delegated-audit-gap.md` | role-match |
| `ops/releases/evidence/phase60/smoke-result.json` | config | batch | `ops/releases/evidence/phase60/smoke-result.json` | exact |
| `ops/releases/evidence/phase60/capacity-result.json` | config | batch | `ops/releases/evidence/phase60/capacity-result.json` | exact |
| `ops/releases/evidence/phase60/drill-results.json` | config | batch | `ops/releases/evidence/phase60/drill-results.json` | exact |
| `ops/releases/evidence/phase60/rehearsal-summary.md` | config | batch | `ops/releases/evidence/phase60/rehearsal-summary.md` | exact |
| `ops/releases/evidence/phase60/rollout-notes.md` | config | batch | `ops/releases/evidence/phase60/rollout-notes.md` | exact |
| `ops/releases/evidence/phase60/rollback-notes.md` | config | batch | `ops/releases/evidence/phase60/rollback-notes.md` | exact |
| `ops/releases/evidence/phase60/transport-fallback-notes.md` | config | batch | `ops/releases/evidence/phase60/transport-fallback-notes.md` | exact |
| `.planning/v3.1-MILESTONE-AUDIT.md` | config | batch | `.planning/v3.1-MILESTONE-AUDIT.md` | exact |

## Pattern Assignments

### `scripts/verify-phase60-load-and-rehearsal.ts` (utility, batch)

**Analog:** `scripts/verify-phase59-deploy-release.ts`

**Imports + helper layout** (`scripts/verify-phase59-deploy-release.ts:1-18,49-86`):
```ts
import { execFileSync } from "node:child_process";
import { existsSync, readFileSync } from "node:fs";
import path from "node:path";

export const PHASE_59_VERIFY_SCRIPT =
  "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase59-deploy-release.ts";

export function read(filePath: string) {
  const absolutePath = path.join(process.cwd(), filePath);
  return existsSync(absolutePath) ? readFileSync(absolutePath, "utf8") : "";
}

function run(command: string, args: readonly string[], label: string) {
  try {
    const output = execFileSync(command, [...args], {
      stdio: "pipe",
      encoding: "utf8",
    });
```

**Static gate pattern** (`scripts/verify-phase59-deploy-release.ts:119-176`):
```ts
return [
  {
    label: "package.json exposes exact verify:phase59 script",
    passed: verifyPhase59PackageScripts(sources.packageSource),
  },
  {
    label: "phase59 required artifact list exists on disk",
    passed: requiredArtifacts.every((artifact) => sources.artifactPresence[artifact] === true),
  },
  {
    label: "verifier keeps helper-based implementation and avoids shell heredoc gates",
    passed:
      includesAll(sources.verifierSource, [
        "export function read(filePath: string)",
        "function run(command: string, args: readonly string[], label: string)",
        "function runVitest(paths: readonly string[], label: string)",
      ])
      && !containsShellHeredoc(sources.verifierSource),
  },
];
```

**Phase 60 exact stage order + live-result enforcement** (`scripts/verify-phase60-load-and-rehearsal.ts:58-67,238-254,361-396`):
```ts
export function getPhase60StageOrder() {
  return [
    "static",
    "sample smoke",
    "capacity",
    "drills",
    "rollout/rollback rehearsal",
    "summary",
  ] as const;
}

export function assertPhase60LiveResult(label: string, result: Phase60StageResult | null) {
  if (!result?.status) {
    throw new Error(`PHASE60_${label.toUpperCase().replace(/[^A-Z0-9]+/g, "_")}_RESULT_MISSING`);
  }
  if (result.status === "dry-run") {
    throw new Error(`PHASE60_${label.toUpperCase().replace(/[^A-Z0-9]+/g, "_")}_DRY_RUN_NOT_ALLOWED`);
  }
}

console.log('\n[2/6] sample smoke gate...');
await runPhase60SmokeStage();
console.log('\n[3/6] capacity gate...');
runPhase60K6Stage('scripts/load/phase60-capacity.k6.js', 'Phase 60 capacity gate');
```

**Testing pattern** (`scripts/verify-phase60-load-and-rehearsal.test.ts:21-135`):
```ts
describe("verify-phase60 load/degrade/rehearsal gate", () => {
  it("locks the required artifact list, focused suites, and hard-gate stage order", () => {
    expect(getPhase60StageOrder()).toEqual([
      "static",
      "sample smoke",
      "capacity",
      "drills",
      "rollout/rollback rehearsal",
      "summary",
    ]);
  });
});
```

---

### `scripts/verify-phase60-local.ts` (utility, file-I/O)

**Analog:** `scripts/verify-phase59-deploy-release.ts` for wrapper/runner shape, `scripts/prepare-dev-db.ts` for migration bridge call, `scripts/lib/sqlite-migration-proof.ts` for SQLite artifact lifecycle utilities.

**Plan 03 reference label:** `scripts/verify-phase60-local.ts`

**Imports + command-runner posture** (`scripts/verify-phase59-deploy-release.ts:1-18,49-64`):
```ts
import { execFileSync } from "node:child_process";
import { existsSync, readFileSync } from "node:fs";
import path from "node:path";

export const PHASE_59_VERIFY_SCRIPT =
  "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase59-deploy-release.ts";

function run(command: string, args: readonly string[], label: string) {
  try {
    const output = execFileSync(command, [...args], {
      stdio: "pipe",
      encoding: "utf8",
    });
```

**Copy the verifier-entry export shape** (`scripts/verify-phase60-load-and-rehearsal.ts:31-32`):
```ts
export const PHASE_60_VERIFY_SCRIPT =
  "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase60-load-and-rehearsal.ts";
```

**Migration bridge call to reuse** (`scripts/prepare-dev-db.ts:269-272`):
```ts
export async function prepareDevDb() {
  await bridgeExistingSchemaIfNeeded();
  await migrate(db, { migrationsFolder: MIGRATIONS_FOLDER });
}
```

**SQLite artifact lifecycle helper** (`scripts/lib/sqlite-migration-proof.ts:60-65`):
```ts
export function cleanupSqliteArtifacts(databasePath: string) {
  for (const filePath of [databasePath, `${databasePath}-shm`, `${databasePath}-wal`]) {
    if (existsSync(filePath)) {
      rmSync(filePath, { force: true });
    }
  }
}
```

**No exact analog guidance:**
- 仓库里没有“复制 repo-local SQLite 到 `/tmp/opencode/...` 后再转调 canonical verifier”的现成 wrapper。
- 规划时应保持“单 wrapper → 设置隔离 `DB_FILE_NAME` → 调 canonical verifier”这一单向链路，不要把 local lane 逻辑散落进 smoke/k6/rehearsal 脚本内部。
- 目标输出形状应与 Plan 03 一致：先准备 isolated proof DB，再以 `DB_FILE_NAME=file:/tmp/opencode/phase60-local-proof/local.db` 启动 `scripts/verify-phase60-load-and-rehearsal.ts`。

---

### `scripts/proof-phase60-load-smoke.ts` (utility, batch)

**Analog:** `scripts/proof-phase58-operator-recovery.ts`

**Runner shape** (`scripts/proof-phase58-operator-recovery.ts:49-86,95-157`):
```ts
function read(filePath: string) {
  const absolutePath = path.join(process.cwd(), filePath);
  return existsSync(absolutePath) ? readFileSync(absolutePath, "utf8") : "";
}

function run(command: string, args: readonly string[], label: string) {
  try {
    const output = execFileSync(command, [...args], {
      stdio: "pipe",
      encoding: "utf8",
    });
    if (output) process.stdout.write(output);
  } catch (error: unknown) {
    console.error(`Phase 58 proof failed while running: ${label}`);
    throw error;
  }
}

export async function runPhase58Proof() {
  console.log("Starting Phase 58 operator walkthrough proof...");
  runVitest(scenario.suites, `Phase 58 ${scenario.id}`);
}
```

**Phase 60 smoke contract + env gate** (`scripts/proof-phase60-load-smoke.ts:17-27,49-52,76-165`):
```ts
const PHASE60_SMOKE_REQUIRED_ENV = ["PHASE60_BASE_URL"] as const;

function getMissingPhase60SmokeEnv() {
  return PHASE60_SMOKE_REQUIRED_ENV.filter((envName) => !process.env[envName]?.trim());
}

function writeSmokeResult(payload: Record<string, unknown>) {
  mkdirSync(path.dirname(PHASE60_SMOKE_RESULT_PATH), { recursive: true });
  writeFileSync(PHASE60_SMOKE_RESULT_PATH, JSON.stringify(payload, null, 2));
}

const payload = {
  checkedAt: new Date().toISOString(),
  proofScopeId: fixtures.proofScopeId,
  classroomSessionId: primarySession,
  designPublish: { status: "passed", script: "pnpm verify:phase56" },
  browserProof: { status: "passed", reusedScript: "runPhase57BrowserProof" },
  status: "passed",
  blockingFailure: null,
  nextStep: "Capacity and drill gates may continue.",
};
```

**Shared browser proof reuse** (`scripts/proof-phase60-load-smoke.ts:54-74` + `scripts/proof-phase57-classroom-runtime.ts:801-832`):
```ts
const previousBaseUrl = process.env.PHASE57_PROOF_BASE_URL;
process.env.PHASE57_PROOF_BASE_URL = phase60BaseUrl;
try {
  await runPhase57BrowserProof();
} finally {
  if (previousBaseUrl === undefined) {
    delete process.env.PHASE57_PROOF_BASE_URL;
  }
}
```

---

### `scripts/rehearse-phase60-rollout-rollback.ts` (utility, batch)

**Analog:** `scripts/rehearse-phase60-rollout-rollback.ts`

**Canonical command builder pattern** (`scripts/rehearse-phase60-rollout-rollback.ts:69-152`):
```ts
export function buildPhase60DeployCommand(input: { ... }): RehearsalCommand {
  const args = [
    "ops/deploy/deploy.sh",
    "--environment", input.environment,
    "--actor", input.actor,
    "--shared-root", input.sharedRoot,
    "--current-root", input.currentRoot,
    "--base-url", input.baseUrl,
  ];

  if (input.dryRun) {
    args.push("--dry-run");
  }

  return { command: "bash", args };
}
```

**Rollback trigger / success rules** (`scripts/rehearse-phase60-rollout-rollback.ts:154-208`):
```ts
if (input.smokeResult?.status && input.smokeResult.status !== "passed" && input.smokeResult.status !== "dry-run") {
  return {
    kind: "sample-smoke-regression",
    reason: input.smokeResult.blockingFailure ?? "Phase 60 smoke result reported a sample-chain regression.",
  };
}

const ok = input.healthStatus === 200 && input.readyStatus === 200 && smokeOk;
```

**Artifact rendering pattern** (`scripts/rehearse-phase60-rollout-rollback.ts:234-270,348-383`):
```ts
function renderRolloutNotes(input: { ... }) {
  return `# Pilot rollout checklist

## Canonical release truth

- [x] Canonical release path stays on \`ops/deploy/deploy.sh\`.
`;
}

writeFileSync(PHASE60_ROLLOUT_NOTES_PATH, renderRolloutNotes({ ... }));
writeFileSync(PHASE60_ROLLBACK_NOTES_PATH, renderRollbackNotes({ ... }));
writeFileSync(PHASE60_TRANSPORT_FALLBACK_NOTES_PATH, renderTransportFallbackNotes());
writeFileSync(PHASE60_REHEARSAL_SUMMARY_PATH, renderRehearsalSummary({ ... }));
```

**Testing pattern** (`scripts/rehearse-phase60-rollout-rollback.test.ts:10-95`):
```ts
it("reuses only the canonical deploy and rollback scripts", () => {
  expect(deploy.command).toBe("bash");
  expect(deploy.args[0]).toBe("ops/deploy/deploy.sh");
  expect(rollback.args[0]).toBe("ops/deploy/rollback.sh");
});
```

---

### `scripts/load/phase60-fixtures.ts` (service, CRUD)

**Analog:** `scripts/proof-phase57-classroom-runtime.ts`

**Imports + schema wiring** (`scripts/proof-phase57-classroom-runtime.ts:1-30` and `scripts/load/phase60-fixtures.ts:1-28`):
```ts
import bcrypt from "bcryptjs";
import { randomUUID } from "node:crypto";
import { and, eq, inArray } from "drizzle-orm";
import { encode } from "next-auth/jwt";

import { db } from "@/db";
import {
  classMembers,
  classes,
  classroomEvidence,
  classroomEvents,
  classroomParticipants,
  classroomSessions,
  courseClasses,
  courseEnrollments,
  courses,
  lessons,
  lessonSteps,
  memberships,
  publishedLessonVersions,
  schools,
  users,
} from "@/db/schema";
```

**User upsert pattern to preserve** (`scripts/proof-phase57-classroom-runtime.ts:176-203` -> `scripts/load/phase60-fixtures.ts:241-273`):
```ts
const [existingUser] = await db
  .select({ id: users.id })
  .from(users)
  .where(eq(users.email, input.email))
  .limit(1);

if (existingUser) {
  await db.update(users).set({
    name: input.name,
    password: passwordHash,
    studentNumber: input.studentNumber ?? null,
  }).where(eq(users.id, existingUser.id));
  return existingUser.id;
}
```

**Session cookie seeding pattern** (`scripts/load/phase60-fixtures.ts:177-204,622-645` and `scripts/proof-phase57-classroom-runtime.ts:663-713`):
```ts
return encode({
  secret,
  salt: input.cookieName,
  token: {
    sub: input.actorId,
    id: input.actorId,
    name: input.name,
    email: input.email,
    roles: input.roles,
    workspaceRole: input.workspaceRole,
  },
  maxAge: 60 * 60,
});
```

**Fixture scope writer + classroom-affined seeding** (`scripts/load/phase60-fixtures.ts:236-239,429-671`):
```ts
function writeFixtureFile(scope: Phase60FixtureScope) {
  mkdirSync(path.dirname(PHASE60_FIXTURE_FILE_PATH), { recursive: true });
  writeFileSync(PHASE60_FIXTURE_FILE_PATH, JSON.stringify(scope, null, 2));
}

export async function ensurePhase60Fixtures(options?: { dryRun?: boolean; baseUrl?: string }) {
  if (options?.dryRun) {
    const dryRunScope = buildDryRunFixtures();
    writeFixtureFile(dryRunScope);
    return dryRunScope;
  }
  ...
  const teacherId = await upsertUser({ ... });
  ...
  await cleanupPhase60Scope();
  ...
  writeFixtureFile(scope);
  return scope;
}
```

**SQLite lock hotspot called out by debug note** (`scripts/load/phase60-fixtures.ts:241-300,429-474`):
- `upsertUser()` 是当前 `SQLITE_BUSY` 核心热点。
- 任何 closure 修复都应保持“单入口 seed + 单一 fixture 文件输出”形状，不要新增第二套散落写入路径。

---

### `src/db/index.ts` (config, CRUD)

**Analog:** `src/db/index.ts`

**Keep the singleton DB export shape** (`src/db/index.ts:1-9`):
```ts
import { drizzle } from 'drizzle-orm/libsql';
import { createClient } from '@libsql/client';
import * as schema from './schema';

const client = createClient({
  url: process.env.DB_FILE_NAME || "file:local.db",
});

export const db = drizzle(client, { schema });
```

**Planner note:** SQLite lock mitigation 必须在这个 `createClient(...)` 块内原位收紧；不要新建第二个 DB client，也不要让脚本绕开 `@/db`。

---

### `package.json` (`verify:phase60:local`) (config, batch)

**Analog:** existing Phase verifier script entries in `package.json`, plus exact-script assertion style from `scripts/verify-phase59-deploy-release.test.ts`.

**Plan 03 reference label:** `Package verifier script registration`

**Exact script-entry shape to copy** (`package.json:57-63`):
```json
"verify:phase56": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase56-voting-authoring.ts",
"verify:phase57": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase57-classroom-runtime.ts",
"verify:phase58": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase58-operator-recovery-and-surfaces.ts",
"verify:phase59": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase59-deploy-release.ts",
"verify:phase60": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase60-load-and-rehearsal.ts",
```

**Test lock pattern for exact script string** (`scripts/verify-phase59-deploy-release.test.ts:11-20`):
```ts
it("expects the dedicated verify:phase59 package script", () => {
  expect(
    verifyPhase59PackageScripts(
      JSON.stringify({
        scripts: {
          "verify:phase59": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase59-deploy-release.ts",
        },
      }),
    ),
  ).toBe(true);
});
```

**Planner guidance:**
- `verify:phase60:local` 应新增为并列脚本键，不要覆盖现有 `verify:phase60`。
- 命令格式继续使用 `node --require ./scripts/server-only-node-shim.cjs --import tsx ...`，不要退回裸 `tsx`。
- 精确字符串应直接指向 `scripts/verify-phase60-local.ts`，这样测试可以锁死 local-wrapper lane，而不是复用 raw verifier。

---

### `.planning/debug/phase60-local-sqlite-busy.md` (config, transform)

**Analog:** `.planning/debug/phase54-delegated-audit-gap.md`

**Plan 03 reference label:** `Debug-note resolution closure`

**Resolved debug-note frontmatter + focus shape** (`.planning/debug/phase54-delegated-audit-gap.md:1-13`):
```md
---
status: resolved
trigger: "..."
created: 2026-05-23T13:35:00Z
updated: 2026-05-23T22:25:00Z
---

## Current Focus

hypothesis: 已验证，根因是 ...
test: `verify:phase54` focused verifier + ... 回归测试。
expecting: ...
next_action: rerun milestone audit after ...
```

**Resolution section shape to copy** (`.planning/debug/phase54-delegated-audit-gap.md:52-68`):
```md
## Resolution

root_cause:
...
fix:
...
verification:
`node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase54-ai-contracts.ts` 通过；...
files_changed:
  - src/db/schema.ts
  - drizzle/0013_phase54_audit_summary_truth.sql
```

**Phase 60-specific evidence that must remain referenced** (`.planning/debug/phase60-local-sqlite-busy.md:27-40`):
```md
- timestamp: 2026-05-29T11:17:00+08:00
  finding: `.env.local` exposes `DB_FILE_NAME=file:local.db` and `PHASE60_BASE_URL=http://127.0.0.1:3000`, so local Phase 60 smoke and long-lived app processes target the same repo-local SQLite database.

- timestamp: 2026-05-29T11:19:00+08:00
  finding: `lsof local.db` shows multiple long-lived holders on the same file: two `server.ts` node processes and one `src/server/workers/async-task-worker.ts` process.
```

**Planner guidance:**
- 将 `status` 从 `investigating` 更新为 `resolved` 时，保留现有 Evidence 时间线，不要删掉锁冲突证据。
- `root_cause`、`fix`、`verification` 必须明确写出“localhost proof 不能共享 `file:local.db`，应改走 `pnpm verify:phase60:local` + isolated `/tmp/opencode/phase60-local-proof/local.db`”。
- `files_changed` 列表应至少覆盖 `package.json`、`scripts/verify-phase60-local.ts`、`scripts/verify-phase60-load-and-rehearsal.ts`、`.planning/debug/phase60-local-sqlite-busy.md`。

---

### `ops/releases/evidence/phase60/smoke-result.json` (config, batch)

**Analog:** `ops/releases/evidence/phase60/smoke-result.json`

**Current contract** (`ops/releases/evidence/phase60/smoke-result.json:2-16`):
```json
{
  "checkedAt": "...",
  "proofScopeId": "phase60-load-rehearsal",
  "classroomSessionId": "...",
  "designPublish": { "status": "failed", "script": "pnpm verify:phase56" },
  "browserProof": { "status": "failed", "reusedScript": "runPhase57BrowserProof" },
  "status": "close-blocker",
  "blockingFailure": "...",
  "nextStep": "Fix the real sample chain before continuing the Phase 60 rehearsal gate."
}
```

---

### `ops/releases/evidence/phase60/capacity-result.json` (config, batch)

**Analog:** `ops/releases/evidence/phase60/capacity-result.json`

**Current contract** (`ops/releases/evidence/phase60/capacity-result.json:1-51`):
```json
{
  "checkedAt": "...",
  "scenarioResults": [
    { "classroom": "phase60-load-rehearsal-class-1", "actors": 40, "iterations": 40, "checksRate": 1, "httpReqFailedRate": 0 }
  ],
  "thresholds": {
    "classrooms": 5,
    "studentsPerClassroom": 40,
    "reconnectRecoveryMs": 15000,
    "workerBacklogWindowMs": 120000,
    "partialFailureRatioMax": 0.02,
    "degradedDurationMs": 180000
  },
  "status": "dry-run",
  "blockingFailure": "Authoring mode only: dry-run capacity artifacts cannot satisfy the Phase 60 close gate."
}
```

---

### `ops/releases/evidence/phase60/drill-results.json` (config, batch)

**Analog:** `ops/releases/evidence/phase60/drill-results.json`

**Current contract** (`ops/releases/evidence/phase60/drill-results.json:1-31`):
```json
{
  "checkedAt": "...",
  "drills": {
    "redisDegraded": { "status": "dry-run", "reason": "authoring mode", "nextStep": "run live drill" },
    "workerBacklog": { "status": "dry-run", "reason": "authoring mode", "nextStep": "run live drill" },
    "reconnectRetry": { "status": "dry-run", "reason": "authoring mode", "nextStep": "run live drill" },
    "partialFailure": { "status": "dry-run", "reason": "authoring mode", "nextStep": "run live drill" }
  },
  "status": "dry-run",
  "manualRequired": { "transportFallback": true }
}
```

---

### `ops/releases/evidence/phase60/rehearsal-summary.md` (config, batch)

**Analog:** `ops/releases/evidence/phase60/rehearsal-summary.md`

**Current summary layout** (`ops/releases/evidence/phase60/rehearsal-summary.md:1-41`):
```md
# Phase 60 rehearsal summary

Machine-readable sources:
- `ops/releases/evidence/phase60/smoke-result.json`
- `ops/releases/evidence/phase60/capacity-result.json`
- `ops/releases/evidence/phase60/drill-results.json`

## Sample smoke
## Capacity gate
## Automated drills
## Shared stop rules
## Controlled rollout and rollback
## Go/No-Go
```

**Source writer pattern** (`scripts/rehearse-phase60-rollout-rollback.ts:261-270`):
```ts
return `# Phase 60 rehearsal summary

Machine-readable sources:
- \`ops/releases/evidence/phase60/smoke-result.json\`
- \`ops/releases/evidence/phase60/capacity-result.json\`
- \`ops/releases/evidence/phase60/drill-results.json\`
...
- Closeout note: ${input.dryRun
  ? "dry-run artifacts are authoring-only and cannot satisfy PILOT-03 / LOAD-01 / LOAD-02."
  : "live pilot-host evidence recorded on the canonical release path."}
`;
```

---

### `ops/releases/evidence/phase60/rollout-notes.md` (config, batch)

**Analog:** `ops/releases/evidence/phase60/rollout-notes.md`

**Checklist order to preserve** (`ops/releases/evidence/phase60/rollout-notes.md:1-26`):
```md
# Pilot rollout checklist

## Rollout block trigger alignment
## Canonical release truth
## Execution steps
## Post-deploy verification
## Failure posture
```

---

### `ops/releases/evidence/phase60/rollback-notes.md` (config, batch)

**Analog:** `ops/releases/evidence/phase60/rollback-notes.md`

**Checklist order to preserve** (`ops/releases/evidence/phase60/rollback-notes.md:1-27`):
```md
# Pilot rollback checklist

## Pilot rollback trigger alignment
## Canonical pointer and manifest checkpoints
## Execution steps
## Post-rollback verification
## Escalation
```

---

### `ops/releases/evidence/phase60/transport-fallback-notes.md` (config, batch)

**Analog:** `ops/releases/evidence/phase60/transport-fallback-notes.md`

**Manual-lane wording to preserve** (`ops/releases/evidence/phase60/transport-fallback-notes.md:1-8`):
```md
# Phase 60 transport fallback rehearsal notes

- Trigger: pending manual rehearsal
- Trust boundary: WebSocket-first transport with SSE only as rollback surface
- Conclusion: manual artifact required; this file must be updated during the live rehearsal
```

---

### `.planning/v3.1-MILESTONE-AUDIT.md` (config, batch)

**Analog:** `.planning/v3.1-MILESTONE-AUDIT.md`

**Requirement status row pattern** (`.planning/v3.1-MILESTONE-AUDIT.md:30-36,114-127`):
```md
- id: "PILOT-03"
  status: "unsatisfied"
  phase: "55,60"
  verification_status: "missing"
  evidence: "40/5 容量口径已定义，但 `ops/releases/evidence/phase60/*` 仍为 dry-run artifact，没有 live rehearsal proof。"

- id: "LOAD-01"
  status: "unsatisfied"
  ...
- id: "LOAD-02"
  status: "unsatisfied"
  ...
```

**Summary table pattern** (`.planning/v3.1-MILESTONE-AUDIT.md:203-219`):
```md
| REQ-ID | Phase | Final Status | Why It Failed |
| --- | --- | --- | --- |
| PILOT-03 | 55,60 | unsatisfied | 40/5 容量口径只有定义，没有 live rehearsal evidence。 |
| LOAD-01 | 60 | unsatisfied | 缺少 live 40x5 capacity proof。 |
| LOAD-02 | 60 | unsatisfied | 缺少 live degraded/reconnect/backlog/retry/partial-failure proof。 |
```

## Shared Patterns

### Script runner / error propagation
**Source:** `scripts/verify-phase59-deploy-release.ts:49-86`, `scripts/proof-phase58-operator-recovery.ts:49-86`
**Apply to:** `scripts/verify-phase60-local.ts`, `scripts/verify-phase60-load-and-rehearsal.ts`, `scripts/proof-phase60-load-smoke.ts`, `scripts/rehearse-phase60-rollout-rollback.ts`
```ts
function run(command: string, args: readonly string[], label: string) {
  try {
    const output = execFileSync(command, [...args], { stdio: "pipe", encoding: "utf8" });
    if (output) process.stdout.write(output);
  } catch (error: unknown) {
    console.error(`... failed while running: ${label}`);
    throw error;
  }
}
```

### Fixture seeding + cookie generation
**Source:** `scripts/load/phase60-fixtures.ts:177-204,241-300,429-671`, `scripts/proof-phase57-classroom-runtime.ts:663-713`
**Apply to:** `phase60-fixtures`, smoke proof, rehearsal script
```ts
return encode({
  secret,
  salt: input.cookieName,
  token: {
    sub: input.actorId,
    id: input.actorId,
    email: input.email,
    roles: input.roles,
    workspaceRole: input.workspaceRole,
  },
  maxAge: 60 * 60,
});
```

### Canonical deploy / rollback path reuse
**Source:** `scripts/rehearse-phase60-rollout-rollback.ts:69-152`, `ops/deploy/deploy.sh:10-32,120-140,407-439`, `ops/deploy/rollback.sh:8-24,82-95,213-238`
**Apply to:** rehearsal runner, rollout notes, rollback notes
```ts
args: [
  "ops/deploy/deploy.sh",
  "--shared-root", input.sharedRoot,
  "--current-root", input.currentRoot,
  "--base-url", input.baseUrl,
]
```

### Evidence contract + dry-run rejection
**Source:** `scripts/verify-phase60-load-and-rehearsal.ts:238-254,382-396`, `ops/releases/evidence/phase60/rehearsal-summary.md:1-41`
**Apply to:** all phase60 evidence writers and milestone audit updates
```ts
if (result.status === "dry-run") {
  throw new Error(`PHASE60_${label.toUpperCase().replace(/[^A-Z0-9]+/g, "_")}_DRY_RUN_NOT_ALLOWED`);
}

if (summarySource.includes('Mode: `dry-run`')) {
  throw new Error('PHASE60_REHEARSAL_DRY_RUN_NOT_ALLOWED');
}
```

### SQLite singleton boundary
**Source:** `src/db/index.ts:1-9`
**Apply to:** any lock mitigation touching local SQLite access, especially `scripts/verify-phase60-local.ts`
```ts
const client = createClient({
  url: process.env.DB_FILE_NAME || "file:local.db",
});

export const db = drizzle(client, { schema });
```

### Package verifier script registration
**Source:** `package.json:57-63`, `scripts/verify-phase59-deploy-release.test.ts:11-20`
**Apply to:** `package.json`, `scripts/verify-phase60-load-and-rehearsal.test.ts`
```ts
"verify:phase60": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase60-load-and-rehearsal.ts"

expect(pkg.scripts?.["verify:phase59"]).toBe(
  "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase59-deploy-release.ts",
);
```

### Debug-note resolution closure
**Source:** `.planning/debug/phase54-delegated-audit-gap.md:52-68`
**Apply to:** `.planning/debug/phase60-local-sqlite-busy.md`
```md
## Resolution

root_cause:
...
fix:
...
verification:
...
files_changed:
  - ...
```

## No Analog Found

| File | Role | Data Flow | Reason |
|---|---|---|---|
| `scripts/verify-phase60-local.ts` isolated DB copy choreography | utility | file-I/O | 没有现成脚本同时完成“复制 repo-local SQLite → 迁移桥接 → 转调 canonical Phase verifier”；需组合 `verify-phase59` 的 runner 形状、`prepareDevDb()` 的迁移调用、以及 `sqlite-migration-proof.ts` 的 SQLite artifact cleanup 思路。 |
| `src/db/index.ts` lock-handling details | config | CRUD | 仓库里没有第二个 libSQL client 配置 busy-timeout/WAL；语法需在保留现有 singleton 形状的前提下原位扩展。 |

## Plan 03 Mandatory References

- `scripts/verify-phase60-local.ts` — local wrapper lane must stay as a single wrapper that copies repo-local SQLite into `/tmp/opencode/phase60-local-proof/local.db`, then delegates to the canonical verifier with isolated `DB_FILE_NAME`.
- `Package verifier script registration` — `package.json` must add `verify:phase60:local` as a sibling exact script string; never overwrite `verify:phase60` and never downgrade to raw `tsx`.
- `Debug-note resolution closure` — `.planning/debug/phase60-local-sqlite-busy.md` must keep the evidence timeline, then append `root_cause`, `fix`, `verification`, and `files_changed` in the resolved-note shape.

## Metadata

**Analog search scope:** `scripts/`, `scripts/load/`, `src/db/`, `ops/deploy/`, `ops/releases/evidence/phase60/`, `.planning/`
**Files scanned:** 22+
**Pattern extraction date:** 2026-05-29
