---
phase: 57-classroom-runtime-sample-chain
plan: "05"
type: execute
wave: 5
depends_on:
  - 57-01
  - 57-02
  - 57-03
  - 57-04
files_modified:
  - scripts/verify-phase57-classroom-runtime.ts
  - scripts/verify-phase57-classroom-runtime.test.ts
  - scripts/proof-phase57-classroom-runtime.ts
  - package.json
autonomous: true
requirements:
  - CHAIN-03
  - CHAIN-04
  - CHAIN-05
  - PLUG-03
  - SAFE-01
  - SAFE-02
must_haves:
  truths:
    - "Phase 57 有 repo-local verify gate，能集中运行 launch/control/submit/result 关键回归。"
    - "验证脚本明确覆盖 sample chain proof，而不是只跑零散 suite。"
    - "执行人可以用单命令复查 Phase 57 的 must-haves。"
    - "browser/UAT proof 是硬门，不是可选附加项；`verify:phase57` 缺它即失败。"
  artifacts:
    - path: scripts/verify-phase57-classroom-runtime.ts
      provides: "Phase 57 focused verification gate"
    - path: scripts/verify-phase57-classroom-runtime.test.ts
      provides: "验证脚本自身的锁定测试"
    - path: scripts/proof-phase57-classroom-runtime.ts
      provides: "Phase 57 repo-local browser/UAT proof command"
    - path: package.json
      provides: "verify:phase57 / proof:phase57 script entries"
  key_links:
    - from: package.json
      to: scripts/verify-phase57-classroom-runtime.ts
      via: verify:phase57
      pattern: "verify:phase57"
    - from: scripts/verify-phase57-classroom-runtime.ts
      to: scripts/proof-phase57-classroom-runtime.ts
      via: proof:phase57
      pattern: "proof:phase57"
---

<objective>
给 Phase 57 增加一个 repo-local verify gate，把 launch → teacher round control → student completion → teacher result visibility 的关键回归收拢成单命令。

Purpose: 满足 proof inventory 的 automated gate 要求，避免 Phase 57 close 时再手工拼测试命令，并把 browser/UAT proof 纳入硬门。
Output: `verify:phase57` 脚本、`proof:phase57` browser/UAT proof、对应自测、聚合后的 focused suite 清单。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-pilot-scope-and-acceptance-gate/55-PROOF-INVENTORY.md
@.planning/phases/57-classroom-runtime-sample-chain/57-VALIDATION.md
@package.json
@scripts/verify-phase56-voting-authoring.ts
@scripts/verify-phase56-voting-authoring.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 先锁定 Phase 57 verifier 套件与脚本入口测试</name>
  <read_first>
    - package.json
    - scripts/verify-phase56-voting-authoring.ts
    - scripts/verify-phase56-voting-authoring.test.ts
    - .planning/phases/57-classroom-runtime-sample-chain/57-VALIDATION.md
    - .planning/phases/55-pilot-scope-and-acceptance-gate/55-PROOF-INVENTORY.md
  </read_first>
  <files>scripts/verify-phase57-classroom-runtime.test.ts</files>
  <action>仿照 Phase 56 verifier 的结构，先写 `scripts/verify-phase57-classroom-runtime.test.ts`，锁定：1) `package.json` 暴露精确 `verify:phase57` 与 `proof:phase57` script；2) verifier 跑的 focused suites 至少覆盖 `classroom-actions.test.ts`、`classroom-control-panel.test.tsx`、`runtime-session.test.ts`、`learning.test.ts`、`classroom-runtime-client.test.tsx`、`classroom.test.ts`、`classroom-roster-panel.test.tsx`；3) verifier 必须把 `proof:phase57` 作为强制步骤调用；4) 若 verifier 额外保留静态检查，它们只能围绕 Phase 57 的 script entry、关键 suite list 与 browser proof wiring，不能重新回退到大量 source-includes 门槛。</action>
  <acceptance_criteria>
    - `verify-phase57-classroom-runtime.test.ts` 对 script name、suite list、browser proof wiring、静态边界均有明确断言。
    - 测试失败能直接指出缺少 `verify:phase57` / `proof:phase57` 或 suite list 不完整。
    - 没有 watch-mode、模糊 grep gate 或 `== 0` 这类脆弱断言。
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run scripts/verify-phase57-classroom-runtime.test.ts</automated>
  </verify>
  <done>Phase 57 verifier 的入口与 suite 边界先被测试锁住。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 verify:phase57 聚合 gate</name>
  <read_first>
    - scripts/verify-phase56-voting-authoring.ts
    - package.json
    - .planning/phases/57-classroom-runtime-sample-chain/57-VALIDATION.md
    - .planning/phases/55-pilot-scope-and-acceptance-gate/55-PROOF-INVENTORY.md
  </read_first>
  <files>scripts/verify-phase57-classroom-runtime.ts, scripts/proof-phase57-classroom-runtime.ts, package.json</files>
  <action>新增 `scripts/verify-phase57-classroom-runtime.ts` 与 `scripts/proof-phase57-classroom-runtime.ts`。复用 Phase 56 verifier 风格，提供单命令聚合 gate：先做最小静态检查（仅验证 `verify:phase57` / `proof:phase57` script 与 focused suite list），再串行运行 Phase 57 的 focused Vitest suites，顺序至少为 launch/control → student submit/recovery → teacher aggregation → browser/UAT proof。把 package script 注册为 `verify:phase57 = node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase57-classroom-runtime.ts` 与 `proof:phase57 = node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/proof-phase57-classroom-runtime.ts`。如果仓库此时仍没有通用 Playwright config，就在 `scripts/proof-phase57-classroom-runtime.ts` 中实现 phase-scoped browser/UAT proof，不得把 browser gate 留成“未来补充”或可选步骤。输出日志要明确标出 `launch readiness`, `teacher round control`, `runtime submit policy`, `teacher result visibility`, `browser/UAT proof` 五组 gate，并在 browser proof 缺失或失败时整体失败。</action>
  <acceptance_criteria>
    - `package.json` 新增 `verify:phase57` 与 `proof:phase57`，且命令精确指向新脚本。
    - `verify-phase57-classroom-runtime.ts` 能独立运行并串起 Phase 57 focused suites + browser proof，不依赖手工拼命令。
    - verifier 输出阶段性日志，失败时能定位到是哪组 gate 或 browser/UAT proof 出错。
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run scripts/verify-phase57-classroom-runtime.test.ts && pnpm verify:phase57</automated>
  </verify>
  <done>执行人可通过 `pnpm verify:phase57` 一次性复查 Phase 57 关键 must-haves，且 browser/UAT proof 被硬性纳入 close gate。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-local verifier → phase close decision | close gate 不能依赖人工口头判断，必须收敛到单命令可复查证据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-10 | R | `verify:phase57` gate | mitigate | 用脚本固定 suite list 与 script entry，避免 close 时手工选择性运行测试。 |
| T-57-11 | D | verification drift | mitigate | verifier 输出按 launch/control/submit/result 分组，任一 gate 失败立即中止，防止带病 close。 |
</threat_model>

<verification>
- 运行 `pnpm vitest run scripts/verify-phase57-classroom-runtime.test.ts && pnpm verify:phase57`
</verification>

<success_criteria>
- 仓库存在 `verify:phase57` 单命令入口。
- 仓库存在 `proof:phase57` browser/UAT 单命令入口，并被 `verify:phase57` 强制调用。
- verifier 明确覆盖 launch、teacher control、student submit/recovery、teacher result 四组行为。
- 执行 Phase 57 后可用单命令复查关键回归，且 browser/UAT proof 不是可跳过项。
</success_criteria>

<output>
After completion, create `.planning/phases/57-classroom-runtime-sample-chain/57-05-SUMMARY.md`
</output>
