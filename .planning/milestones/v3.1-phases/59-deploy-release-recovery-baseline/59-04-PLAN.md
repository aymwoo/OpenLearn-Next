---
phase: 59-deploy-release-recovery-baseline
plan: "04"
type: execute
wave: 3
depends_on:
  - 59-01
  - 59-02
files_modified:
  - ops/deploy/deploy.sh
  - ops/deploy/rollback.sh
  - ops/systemd/openlearn-web.service
  - ops/systemd/openlearn-worker.service
  - ops/releases/checklists/rollout.md
  - ops/releases/checklists/rollback.md
autonomous: true
requirements:
  - ENVR-03
  - OPS-01
must_haves:
  truths:
    - "D-59-06: 每次 pilot release 都会写入绑定 Git SHA 的 immutable manifest，并以 release manifest 作为唯一主记录。"
    - "D-59-09: migration 或 ready gate 失败时默认动作是立即回滚到上一份 green release。"
    - "D-59-10: 当前 active release 与上一份 green release 由 canonical `current.json` / `green.json` pointer 管理，rollback 绑定上一份 green manifest 与 post-rollback verification。"
    - "D-59-01 / D-59-02 / D-59-04: single-node host 以复用 `pnpm start` + `pnpm worker:start` 的 dual-process 方式运行，并由 systemd + shell scripts 交付，而不是临时手工命令或 Docker 主路线。"
  artifacts:
    - path: "ops/deploy/deploy.sh"
      provides: "release orchestration, immutable manifest write, current/green pointer update"
      contains: "current.json"
    - path: "ops/deploy/rollback.sh"
      provides: "previous green release rollback and canonical pointer rewind"
      contains: "green.json"
    - path: "ops/systemd/openlearn-web.service"
      provides: "web long-running process unit"
      contains: "pnpm start"
    - path: "ops/systemd/openlearn-worker.service"
      provides: "worker long-running process unit"
      contains: "pnpm worker:start"
  key_links:
    - from: "ops/deploy/deploy.sh"
      to: "ops/deploy/rollback.sh"
      via: "failure-triggered rollback handoff"
      pattern: "rollback.sh"
    - from: "ops/deploy/deploy.sh"
      to: "src/app/api/release/route.ts"
      via: "current/green manifest pointers + operatorCorrelation"
      pattern: "current.json|green.json|operatorCorrelation"
    - from: "ops/systemd/openlearn-web.service"
      to: "server.ts"
      via: "pnpm start"
      pattern: "pnpm start"
    - from: "ops/systemd/openlearn-worker.service"
      to: "src/server/workers/async-task-worker.ts"
      via: "pnpm worker:start"
      pattern: "pnpm worker:start"
---

<objective>
Ship the official single-node deploy and rollback artifacts for the pilot host.

Purpose: satisfy D-59-01, D-59-02, D-59-04, D-59-06 through D-59-10 without inventing a new host model.
Output: deploy/rollback scripts, systemd units, rollout/rollback checklists.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-pilot-scope-and-acceptance-gate/55-FAILURE-RECOVERY-MATRIX.md
@.planning/phases/59-deploy-release-recovery-baseline/59-CONTEXT.md
@.planning/phases/59-deploy-release-recovery-baseline/59-RESEARCH.md
@package.json
@server.ts
@src/server/workers/async-task-worker.ts

<interfaces>
From `package.json`:

```json
{
  "start": "NODE_ENV=production node ... server.ts",
  "worker:start": "NODE_ENV=production node ... async-task-worker.ts",
  "db:migrate": "tsx --import ./scripts/node-shim.js scripts/prepare-dev-db.ts"
}
```

From `server.ts`:

```ts
const app = next({ dev, hostname, port, httpServer, turbopack: dev });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 deploy/rollback scripts 与 release manifest 写入</name>
  <read_first>
    .planning/phases/59-deploy-release-recovery-baseline/59-CONTEXT.md
    .planning/phases/55-pilot-scope-and-acceptance-gate/55-FAILURE-RECOVERY-MATRIX.md
    package.json
    ops/deploy/deploy.sh
    ops/deploy/rollback.sh
    src/app/api/health/route.ts
    src/app/api/ready/route.ts
  </read_first>
  <files>ops/deploy/deploy.sh, ops/deploy/rollback.sh</files>
  <action>创建 `ops/deploy/deploy.sh` 与 `ops/deploy/rollback.sh`。deploy 脚本按 D-59-01 / D-59-02 / D-59-04 固定 single-node dual-process：从当前 git worktree 生成 release 目录 `${OPENLEARN_SHARED_ROOT}/releases/${RELEASE_ID}`，`RELEASE_ID` 采用 `$(date -u +%Y%m%dT%H%M%SZ)_${GIT_SHA}`；脚本顺序固定为 `pnpm lint` → `pnpm typecheck` → `pnpm test --run` → `pnpm build` → `pnpm db:migrate` → `pnpm verify:phase57` → `pnpm verify:phase58` → `pnpm verify:phase59` → 切换 `${OPENLEARN_CURRENT_ROOT}` symlink → `systemctl daemon-reload` → `systemctl restart openlearn-web openlearn-worker` → `curl /api/health` → `curl /api/ready`。按 D-59-06 写 immutable manifest 到 `${OPENLEARN_RELEASE_MANIFESTS_DIR}/${RELEASE_ID}.json`，字段至少包含 `releaseId`、`gitSha`、`environment`、`actor`、`releaseDir`、`migrations.command`、`migrations.status`、`gates`、`releasedAt`、`rollbackTarget`、`systemdUnits`、`restoreDrill`、`operatorCorrelation`。`operatorCorrelation` 必须把 release/recovery posture 接到 OPS-01 七维：`schoolId`、`classroomSessionId`、`lessonVersionId`、`pluginId`、`actionKey`、`commandId`、`taskId`，以及指向 Phase 58 已存在 operator surfaces 的 href/hrefTemplate。deploy/rollback 共同维护 `${OPENLEARN_RELEASE_MANIFESTS_DIR}/current.json` 与 `${OPENLEARN_RELEASE_MANIFESTS_DIR}/green.json` 两个 canonical pointer：deploy 仅在 post-deploy `/api/health` 与 `/api/ready` 成功后同时更新 current+green；若 migrate 或 `/api/ready` 失败，则自动调用 `rollback.sh --release-id "$PREVIOUS_GREEN_RELEASE_ID" --reason migration_or_ready_failed`，并把 `current.json` 重置为 previous green，`green.json` 保持不变。rollback 脚本必须从 `green.json` 或指定 release manifest 读取 `releaseDir` 与 `gitSha`，重指 `${OPENLEARN_CURRENT_ROOT}` 后重启 web/worker，再执行 post-rollback `/api/health` 与 `/api/ready` 验证；成功后写回 `current.json`，并在该 release 重新成为 green baseline 时同步刷新 `green.json`。两个脚本都实现 `--dry-run`，并要求通过 flags 或等价 env 提供 operatorCorrelation 所需 sample-chain ids，禁止让 `/api/release` 自行猜测这些维度。</action>
  <acceptance_criteria>
    - `deploy.sh` 包含完整 gate 顺序、immutable manifest 字段写入、`current.json` / `green.json` 更新规则与 operatorCorrelation 写入逻辑。
    - `deploy.sh` 在 failure path 中显式调用 `rollback.sh`，而不是只打印提示。
    - `rollback.sh` 从 previous green manifest 或 `green.json` 读取 `releaseDir` / `gitSha` / `rollbackTarget`，执行 post-rollback health/ready 验证，并回写 `current.json`。
  </acceptance_criteria>
  <verify>
    <automated>bash ops/deploy/deploy.sh --dry-run --environment pilot-single-school --actor phase59-test --shared-root "/tmp/openlearn-phase59/shared" --current-root "/tmp/openlearn-phase59/current" --base-url "http://127.0.0.1:3300" --school-id school-1 --classroom-session-id session-1 --lesson-version-id lesson-v1 --plugin-id plugin-1 --action-key addStepSuggestion --command-id command-1 --task-id task-1 && bash ops/deploy/rollback.sh --dry-run --release-id previous-green --reason ready_failed --shared-root "/tmp/openlearn-phase59/shared" --current-root "/tmp/openlearn-phase59/current" --base-url "http://127.0.0.1:3300"</automated>
  </verify>
  <done>release/rollback 不再依赖口头步骤；manifest、failure-triggered rollback 与 previous-green binding 都有可执行脚本。</done>
</task>

<task type="auto">
  <name>Task 2: 提供 systemd units 与 rollout/rollback checklists</name>
  <read_first>
    ops/deploy/deploy.sh
    ops/deploy/rollback.sh
    ops/systemd/openlearn-web.service
    ops/systemd/openlearn-worker.service
    ops/releases/checklists/rollout.md
    ops/releases/checklists/rollback.md
    .planning/phases/55-pilot-scope-and-acceptance-gate/55-FAILURE-RECOVERY-MATRIX.md
  </read_first>
  <files>ops/systemd/openlearn-web.service, ops/systemd/openlearn-worker.service, ops/releases/checklists/rollout.md, ops/releases/checklists/rollback.md</files>
  <action>新增 `ops/systemd/openlearn-web.service` 与 `ops/systemd/openlearn-worker.service`，都使用 `Type=exec`、`Restart=on-failure`、`TimeoutStartSec=30s`、`TimeoutStopSec=30s`，并通过 `EnvironmentFile=/etc/openlearn/openlearn.env` 注入 `OPENLEARN_CURRENT_ROOT` 等变量。`ExecStart` 必须分别是 `/usr/bin/env bash -lc 'cd "$OPENLEARN_CURRENT_ROOT" && pnpm start'` 与 `/usr/bin/env bash -lc 'cd "$OPENLEARN_CURRENT_ROOT" && pnpm worker:start'`，落实 D-59-01 / D-59-02。同步编写 `ops/releases/checklists/rollout.md` 与 `ops/releases/checklists/rollback.md`，逐项绑定 `55-FAILURE-RECOVERY-MATRIX.md` 中的 rollout block trigger、pilot rollback trigger、post-rollback verification；清单里必须出现 immutable manifest 路径、`current.json` / `green.json` pointer 路径、systemd unit 名称、`/api/health`、`/api/ready`、上一份 green release id、operator actor 记录位置，以及 school/classroom/lesson version/plugin/action/command/task 七维 correlation 字段填写位，确保 operator 能把 release/recovery posture 回挂到既有 incident/runtime/plugin/command/task surface。</action>
  <acceptance_criteria>
    - `openlearn-web.service` 含 `pnpm start`，`openlearn-worker.service` 含 `pnpm worker:start`。
    - 两个 unit 都有 `Restart=on-failure` 与 `EnvironmentFile=/etc/openlearn/openlearn.env`。
    - rollout/rollback checklist 同时引用 `55-FAILURE-RECOVERY-MATRIX.md` 里的 trigger 与 post verification 语义，并显式列出 `current.json` / `green.json` 与 OPS-01 correlation 字段。
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'pnpm start' ops/systemd/openlearn-web.service && grep -q 'pnpm worker:start' ops/systemd/openlearn-worker.service && grep -q 'green.json' ops/releases/checklists/rollback.md && grep -q 'commandId' ops/releases/checklists/rollout.md && grep -q '/api/ready' ops/releases/checklists/rollout.md</automated>
  </verify>
  <done>pilot host 的正式运行方式、rollout checklist 与 rollback checklist 都已落地为仓库 artifacts。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release owner shell input → deploy scripts | shell 参数会驱动 symlink、systemctl 与 manifest 写入。 |
| previous green manifest → rollback action | rollback 依赖 manifest truth，若读取错误会回到错误版本。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-07 | Tampering | `ops/deploy/deploy.sh`, `ops/deploy/rollback.sh` | mitigate | 所有 shell 变量双引号包裹，使用固定命令路径与 manifest fields；禁止 `eval` 和未校验字符串拼接。 |
| T-59-08 | Repudiation | release manifest / checklists | mitigate | manifest 绑定 `gitSha`、`actor`、`releasedAt`、`rollbackTarget`，checklist 要求记录 releaseId 与上一份 green release id。 |
</threat_model>

<verification>
- `bash ops/deploy/deploy.sh --dry-run ...`
- `bash ops/deploy/rollback.sh --dry-run ...`
- `grep -q 'pnpm start' ops/systemd/openlearn-web.service`
</verification>

<success_criteria>
- single-node dual-process host baseline 由 systemd + shell scripts 正式化。
- release manifest + current/green pointers 成为 rollout/rollback 与 `/api/release` 的唯一主记录。
- migration/ready 失败自动触发 previous-green rollback，而不是留病运行。
</success_criteria>

<output>
After completion, create `.planning/phases/59-deploy-release-recovery-baseline/59-04-SUMMARY.md`
</output>
