---
phase: 59-deploy-release-recovery-baseline
plan: "03"
type: execute
wave: 3
depends_on:
  - 59-01
  - 59-02
files_modified:
  - package.json
  - scripts/verify-phase59-deploy-release.ts
  - scripts/verify-phase59-deploy-release.test.ts
  - .github/workflows/pilot-release.yml
autonomous: true
requirements:
  - ENVR-02
must_haves:
  truths:
    - "D-59-07: release hard gate 会在 lint/typecheck/test/build/migrate/health/ready 任一失败时中止。"
    - "D-59-08: migration 属于正式 release gate，不允许部署后补跑或改由 operator 手工兜底。"
    - "D-59-07 / D-59-08: 仓库有一个 phase59 repo-local verifier，固定 release baseline 的静态 contract。"
  artifacts:
    - path: "package.json"
      provides: "phase59 verifier script entry"
      contains: "verify:phase59"
    - path: "scripts/verify-phase59-deploy-release.ts"
      provides: "phase59 static and focused verification gate"
      exports: ["runPhase59Verification"]
    - path: ".github/workflows/pilot-release.yml"
      provides: "CI hard gate for pilot release baseline"
      contains: "pnpm typecheck"
  key_links:
    - from: ".github/workflows/pilot-release.yml"
      to: "package.json"
      via: "repo-local script calls"
      pattern: "pnpm lint|pnpm typecheck|pnpm build|pnpm db:migrate|pnpm verify:phase59"
---

<objective>
Create the repo-local and CI hard gates that make Phase 59 release discipline enforceable.

Purpose: satisfy ENVR-02 and lock D-59-07 / D-59-08 into executable checks rather than doc-only promises.
Output: `verify:phase59`, verifier self-tests, GitHub Actions workflow.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-pilot-scope-and-acceptance-gate/55-PROOF-INVENTORY.md
@.planning/phases/59-deploy-release-recovery-baseline/59-CONTEXT.md
@.planning/phases/59-deploy-release-recovery-baseline/59-RESEARCH.md
@package.json
@scripts/verify-phase58-operator-recovery-and-surfaces.ts
@scripts/verify-phase58-operator-recovery-and-surfaces.test.ts

<interfaces>
From `package.json`:

```json
{
  "lint": "eslint .",
  "typecheck": "tsc --noEmit",
  "build": "next build --turbopack",
  "db:migrate": "tsx --import ./scripts/node-shim.js scripts/prepare-dev-db.ts",
  "verify:phase57": "...",
  "verify:phase58": "..."
}
```

From `scripts/verify-phase58-operator-recovery-and-surfaces.ts`:

```ts
export function read(filePath: string) { ... }
function run(command: string, args: readonly string[], label: string) { ... }
function runVitest(paths: readonly string[], label: string) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 `verify:phase59` verifier、自测与 script entry</name>
  <read_first>
    package.json
    scripts/verify-phase58-operator-recovery-and-surfaces.ts
    scripts/verify-phase58-operator-recovery-and-surfaces.test.ts
    scripts/verify-phase59-deploy-release.ts
    scripts/verify-phase59-deploy-release.test.ts
  </read_first>
  <files>package.json, scripts/verify-phase59-deploy-release.ts, scripts/verify-phase59-deploy-release.test.ts</files>
  <action>参照 Phase 58 verifier 模式创建 `scripts/verify-phase59-deploy-release.ts` 与自测文件；在 `package.json` 注册 `verify:phase59`。verifier 的静态 contract 必须锁定以下 artifact 路径与 token：`.env.example`、`src/lib/ops/env.server.ts`、`src/app/api/health/route.ts`、`src/app/api/ready/route.ts`、`src/app/api/release/route.ts`、`ops/deploy/deploy.sh`、`ops/deploy/rollback.sh`、`ops/deploy/backup.sh`、`ops/deploy/restore.sh`、`ops/deploy/verify-restore.sh`、`ops/systemd/openlearn-web.service`、`ops/systemd/openlearn-worker.service`、`ops/releases/checklists/rollout.md`、`ops/releases/checklists/rollback.md`、`.github/workflows/pilot-release.yml`。按 D-59-07 / D-59-08，把 focused suite 列表固定为 `src/lib/ops/env.server.test.ts`、`src/lib/ops/release-status.test.ts`、`src/app/api/ops-routes.test.ts`、`scripts/verify-phase59-deploy-release.test.ts`，避免 verifier 退化成模糊 grep gate。</action>
  <acceptance_criteria>
    - `package.json` 包含精确 `verify:phase59` script entry。
    - verifier 自测文件显式断言 focused suite 列表与 required artifact 列表。
    - verifier 实现中使用 helper `read`/`run`/`runVitest` 风格，而不是散落 shell heredoc。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run scripts/verify-phase59-deploy-release.test.ts</automated>
  </verify>
  <done>Phase 59 有一个可复用的 repo-local close gate，后续 workflow 与 release script 都可以直接调用。</done>
</task>

<task type="auto">
  <name>Task 2: 用 GitHub Actions 固化 lint/typecheck/test/build/migrate/health/ready hard gate</name>
  <read_first>
    .planning/phases/59-deploy-release-recovery-baseline/59-RESEARCH.md
    package.json
    .github/workflows/pilot-release.yml
    src/app/api/health/route.ts
    src/app/api/ready/route.ts
  </read_first>
  <files>.github/workflows/pilot-release.yml</files>
  <action>新增 `.github/workflows/pilot-release.yml`。workflow 触发至少包含 `pull_request`、`push` 到 `main` 与 `workflow_dispatch`。在 job 内使用 `actions/checkout@v4`、`actions/setup-node@v4`、`pnpm/action-setup@v4`；提供 `redis:7-alpine` service，环境变量写死为 CI 安全占位值：`DB_FILE_NAME=file:ci.db`、`AUTH_SECRET=ci-auth-secret-0123456789`、`NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=ci-server-actions-key-0123456789abcdef`、`ASYNC_TASKS_ENABLED=true`、`BULLMQ_REDIS_URL=redis://127.0.0.1:6379`、`REDIS_FANOUT_ENABLED=false`、`OPENLEARN_DEPLOY_ENV=ci`、`OPENLEARN_SHARED_ROOT=${{ runner.temp }}/openlearn-shared`、`OPENLEARN_CURRENT_ROOT=${{ github.workspace }}`、`OPENLEARN_RUNTIME_ASSETS_ROOT=${{ runner.temp }}/openlearn-assets`、`OPENLEARN_RELEASE_MANIFESTS_DIR=${{ github.workspace }}/ops/releases/manifests`、`OPENLEARN_HEALTHCHECK_BASE_URL=http://127.0.0.1:3100`。步骤顺序固定为 `pnpm install --frozen-lockfile` → `pnpm lint` → `pnpm typecheck` → `pnpm test --run` → `pnpm build` → `pnpm db:migrate` → `pnpm verify:phase57` → `pnpm verify:phase58` → `pnpm verify:phase59` → 后台启动 `PORT=3100 pnpm start` 与 `pnpm worker:start` → `curl -fsS /api/health` → `curl -fsS /api/ready`。按 D-59-07，任一步失败即 job fail；按 D-59-08，不允许把 migrate 放到 workflow 之外。</action>
  <acceptance_criteria>
    - workflow 同时包含 `pnpm lint`、`pnpm typecheck`、`pnpm test --run`、`pnpm build`、`pnpm db:migrate`、`pnpm verify:phase59`、`curl -fsS http://127.0.0.1:3100/api/health`、`curl -fsS http://127.0.0.1:3100/api/ready`。
    - workflow 提供 Redis service，避免 worker ready gate 在 CI 中被误判为 unavailable。
    - workflow 中未把 fanout Redis 设成 blocking 开关；`REDIS_FANOUT_ENABLED=false` 明确保留 optional posture。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run scripts/verify-phase59-deploy-release.test.ts</automated>
  </verify>
  <done>CI hard gate 成为仓库内可执行事实，发布不再依赖口头约定或外部截图。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → release gate | CI 会执行真实 build/migrate/start commands，必须避免假绿。 |
| static verifier → deployment artifacts | verifier 是仓库 close gate，若过宽会放过 release regression。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-05 | Repudiation | `scripts/verify-phase59-deploy-release.ts` | mitigate | 固定 script entry、suite 列表、artifact 路径，使 close gate 可复查而不是口头解释。 |
| T-59-06 | Spoofing | `.github/workflows/pilot-release.yml` | mitigate | workflow 使用仓库内脚本和显式 env，占位 secrets 不复用真实凭证；失败即中止，不允许跳过 migrate/ready gate。 |
</threat_model>

<verification>
- `pnpm test --run scripts/verify-phase59-deploy-release.test.ts`
</verification>

<success_criteria>
- `verify:phase59` 成为 package script 并有自测锁边界。
- GitHub Actions 可以独立执行 Phase 59 gate，不依赖人工补跑。
- migration、health、ready 被正式纳入 CI/CD 硬门。
</success_criteria>

<output>
After completion, create `.planning/phases/59-deploy-release-recovery-baseline/59-03-SUMMARY.md`
</output>
