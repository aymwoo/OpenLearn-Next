---
phase: 60-load-degrade-pilot-rehearsal
plan: "03"
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - scripts/load/phase60-drill-classifier.js
  - scripts/load/phase60-drills.k6.js
  - scripts/load/phase60-drills.test.ts
  - ops/releases/evidence/phase60/drill-results.json
autonomous: true
requirements: [LOAD-02, OPS-02]
user_setup: []
must_haves:
  truths:
    - "D-60-05: Redis degraded, worker backlog, reconnect/retry, and partial failure are exercised by explicit automated drills."
    - "D-60-06 and D-60-07: drill results reuse the existing readiness honesty posture, keep transport fallback in the manual rehearsal lane, and preserve worker-blocking vs fanout-non-blocking semantics."
    - "D-60-08 through D-60-11: any threshold breach is classified as a close blocker, rollback-trigger candidate, or escalation condition instead of an advisory note."
  artifacts:
    - path: scripts/load/phase60-drill-classifier.js
      provides: "Shared blocker/escalation classification for failure-drill outputs"
    - path: scripts/load/phase60-drills.k6.js
      provides: "Automated degraded drill runner for the four required failure modes"
    - path: scripts/load/phase60-drills.test.ts
      provides: "Behavioral lock on honesty posture and stop-rule classification"
    - path: ops/releases/evidence/phase60/drill-results.json
      provides: "Structured drill outcomes consumed by the Phase 60 rehearsal summary"
  key_links:
    - from: scripts/load/phase60-drills.k6.js
      to: src/lib/ops/release-status.ts
      via: worker-blocking and fanout-non-blocking semantics
      pattern: blocking
    - from: scripts/load/phase60-drill-classifier.js
      to: scripts/load/phase60-thresholds.js
      via: shared stop-rule inputs
      pattern: degradedDurationMs
    - from: ops/releases/evidence/phase60/drill-results.json
      to: ops/releases/evidence/phase60/rehearsal-summary.md
      via: structured drill result contract
      pattern: drill-results.json
---

<objective>
Automate the non-happy-path proof for Phase 60 and make failure-drill outcomes machine-classified.

Purpose: Fulfill D-60-05 through D-60-11 and LOAD-02/OPS-02 without inventing new degraded semantics.
Output: automated drill script, pure classifier module, and tests locking honesty posture plus blocker classification.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-load-degrade-pilot-rehearsal/60-CONTEXT.md
@.planning/phases/60-load-degrade-pilot-rehearsal/60-RESEARCH.md
@.planning/phases/60-load-degrade-pilot-rehearsal/60-PATTERNS.md
@.planning/phases/55-pilot-scope-and-acceptance-gate/55-FAILURE-RECOVERY-MATRIX.md
@src/lib/ops/release-status.ts
@scripts/load/phase60-thresholds.js

<interfaces>
From `src/lib/ops/release-status.ts`:

```ts
type ReadyComponent = {
  posture: "green" | "degraded" | "failed";
  blocking: boolean;
  reason: string;
  nextStep: string;
};

export async function getReadyPayload();
```

Locked truth already in code:

```ts
worker.blocking === true
fanout.blocking === false
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the drill blocker classifier against Phase 58 and 59 honesty rules</name>
  <read_first>
    - scripts/load/phase60-thresholds.js
    - src/lib/ops/release-status.ts
    - .planning/phases/55-pilot-scope-and-acceptance-gate/55-FAILURE-RECOVERY-MATRIX.md
    - .planning/phases/60-load-degrade-pilot-rehearsal/60-RESEARCH.md
  </read_first>
  <files>scripts/load/phase60-drill-classifier.js, scripts/load/phase60-drills.test.ts</files>
  <behavior>
    - Test 1: worker backlog beyond 120000 ms becomes a close blocker because worker posture is blocking.
    - Test 2: reconnect beyond 15000 ms, partial failure at or above 0.02, and degraded duration beyond 180000 ms become blocker or escalation outputs with explicit reason strings.
    - Test 3: fanout-only degraded results remain visible but do not silently flip to ready-blocking unless worker or canonical truth seams also breach.
  </behavior>
  <acceptance_criteria>
    - `scripts/load/phase60-drill-classifier.js` exports pure helpers that turn raw drill measurements into `pass`, `close-blocker`, `rollback-trigger-candidate`, or `escalate` outcomes.
    - `scripts/load/phase60-drills.test.ts` proves the four D-60-10 thresholds and the existing worker-vs-fanout honesty semantics from `src/lib/ops/release-status.ts`.
    - The classifier never emits advisory-only output for breached thresholds covered by D-60-08 through D-60-11.
  </acceptance_criteria>
  <action>Create `scripts/load/phase60-drill-classifier.js` and its test first. Encode D-60-08 through D-60-11 as pure classification rules that consume the shared stop rules from `scripts/load/phase60-thresholds.js` and the existing posture split from `src/lib/ops/release-status.ts`: worker backlog is blocking, Redis fanout degraded remains visible but non-blocking unless a blocking seam also fails. The classifier must return structured status, reason, and recommended next step so the final summary can mark blocker vs rollback trigger vs escalation without free-form judgment.</action>
  <verify>
    <automated>pnpm exec vitest --run scripts/load/phase60-drills.test.ts</automated>
  </verify>
  <done>The Phase 60 drill classifier is tested and expresses blocker posture in code rather than meeting notes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement automated degraded drills for Redis, worker backlog, reconnect, and partial failure</name>
  <read_first>
    - scripts/load/phase60-drill-classifier.js
    - scripts/load/phase60-thresholds.js
    - src/lib/ops/release-status.ts
    - .planning/phases/60-load-degrade-pilot-rehearsal/60-CONTEXT.md
    - .planning/phases/55-pilot-scope-and-acceptance-gate/55-FAILURE-RECOVERY-MATRIX.md
  </read_first>
  <files>scripts/load/phase60-drills.k6.js, ops/releases/evidence/phase60/drill-results.json</files>
  <acceptance_criteria>
    - The k6 drill script covers all four automated failure classes from D-60-05: `Redis degraded`, `worker backlog`, `学生 reconnect/retry`, and `partial failure`.
    - Drill results invoke `scripts/load/phase60-drill-classifier.js` so threshold breaches exit as blocker or escalation results rather than raw metric dumps.
    - `ops/releases/evidence/phase60/drill-results.json` is the fixed drill output contract and contains top-level keys `checkedAt`, `drills`, `status`, `blockingFailure`, `manualRequired`, and `nextStep`; `drills` must include exactly `redisDegraded`, `workerBacklog`, `reconnectRetry`, and `partialFailure` objects.
    - The script explicitly records `transport fallback` as a manual rehearsal artifact required later per D-60-06 and does not pretend to automate it.
  </acceptance_criteria>
  <action>Implement `scripts/load/phase60-drills.k6.js` around the classifier from Task 1. Cover D-60-05 and D-60-07 by exercising the existing honesty seams instead of redefining them: a Redis degraded drill that proves fanout degradation remains visible, a worker backlog drill that proves backlog beyond 120000 ms blocks close, a reconnect/retry drill that measures recovery against the 15000 ms limit, and a partial-failure drill that measures failure ratio against 0.02. Emit structured drill results to `ops/releases/evidence/phase60/drill-results.json` with top-level keys `checkedAt`, `drills`, `status`, `blockingFailure`, `manualRequired`, and `nextStep`, where `drills` contains the four named drill objects and `manualRequired.transportFallback` is explicitly `true` so D-60-06 stays in the runbook lane rather than being silently omitted.</action>
  <verify>
    <automated>docker run --rm --network host -v "$PWD:/work" -w /work grafana/k6:latest run scripts/load/phase60-drills.k6.js && pnpm exec vitest --run scripts/load/phase60-drills.test.ts</automated>
  </verify>
  <done>All required automated drills exist, reuse the locked honesty vocabulary, and convert threshold breaches into blocker-grade outputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drill runner -> readiness truth | drill automation must read and classify existing operator posture honestly |
| classifier -> close decision | misclassification would let a blocked rehearsal look acceptable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-05 | Repudiation | `scripts/load/phase60-drill-classifier.js` | mitigate | Use explicit structured outcomes plus tests for blocker/escalation semantics; no prose-only summaries. |
| T-60-06 | Information Disclosure | `scripts/load/phase60-drills.k6.js` | accept | Drill logs may mention degraded causes, but they remain phase-scoped operational evidence with no new public surface. |
</threat_model>

<verification>
- `pnpm exec vitest --run scripts/load/phase60-drills.test.ts`
- `docker run --rm --network host -v "$PWD:/work" -w /work grafana/k6:latest run scripts/load/phase60-drills.k6.js`
</verification>

<success_criteria>
- Every required automated failure drill exists and is machine-verifiable.
- Worker blocking and fanout non-blocking semantics stay aligned with existing release truth.
- Breached drill thresholds become blocker-grade evidence rather than advisory prose.
</success_criteria>

<output>
After completion, create `.planning/phases/60-load-degrade-pilot-rehearsal/60-03-SUMMARY.md`.
</output>
