---
phase: 35-verification-baseline-convergence-and-milestone-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(teacher)/teacher/courses/[courseId]/page.tsx
  - src/app/(teacher)/teacher/courses/[courseId]/lessons/page.tsx
  - src/app/(teacher)/teacher/courses/import/[batchId]/page.tsx
  - src/components/courses/course-detail-form.tsx
  - src/components/surfaces/teacher-course-detail-surface.tsx
  - src/components/classroom/classroom-roster-panel.test.tsx
  - src/components/classroom/classroom-student-detail-panel.test.tsx
  - src/features/runtime-platform/classroom/runtime-session.test.ts
autonomous: true
requirements:
  - QUAL-01
must_haves:
  truths:
    - Milestone scope 内新触达的 course detail 和 verifier path 不能继续被 `react-hooks/error-boundaries` 或 `set-state-in-effect` 风格问题阻断。
    - `COURSE-07`、Phase 33/34 verifier 与相关 route surfaces 需要形成可重复 lint baseline，而不是继续依赖“先忽略当前错误”。
    - 对 runtime 或 classroom proof DTO 的测试修补只能对齐现有 contract，不得回退已收紧的字段要求。
  artifacts:
    - path: src/components/courses/course-detail-form.tsx
      provides: milestone-scoped course membership UI without effect-driven local resync lint blocker
    - path: src/app/(teacher)/teacher/courses/[courseId]/page.tsx
      provides: course detail route without JSX-in-try lint blocker
    - path: src/features/runtime-platform/classroom/runtime-session.test.ts
      provides: runtime proof seed test aligned to the current typed payload contract
---

<objective>
消除 active milestone 触达范围内最直接的 lint/typecheck blocker，先把 Phase 33/34 和课程成员闭环所依赖的 route、form、test surfaces 拉回到可持续基线。

Purpose: 如果 Phase 35 继续在一个连 milestone 自己改过的页面和测试都无法通过静态检查的状态下 close，close claim 就不诚实。
Output: targeted baseline fixes for milestone-scoped routes, forms, and tests.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: remove course-detail route and form lint blockers inside the active milestone scope</name>
  <files>src/app/(teacher)/teacher/courses/[courseId]/page.tsx, src/app/(teacher)/teacher/courses/[courseId]/lessons/page.tsx, src/app/(teacher)/teacher/courses/import/[batchId]/page.tsx, src/components/courses/course-detail-form.tsx, src/components/surfaces/teacher-course-detail-surface.tsx</files>
  <read_first>src/app/(teacher)/teacher/courses/[courseId]/page.tsx; src/app/(teacher)/teacher/courses/[courseId]/lessons/page.tsx; src/app/(teacher)/teacher/courses/import/[batchId]/page.tsx; src/components/courses/course-detail-form.tsx; src/components/surfaces/teacher-course-detail-surface.tsx</read_first>
  <action>Move route-level JSX returns outside `try/catch` in the three teacher course pages so Next or React lint no longer flags `react-hooks/error-boundaries`. In `CourseDetailForm`, remove the prop-to-state synchronization effect that triggered `set-state-in-effect`; keep the existing UX by letting the parent surface remount the form through a deterministic `key` derived from the refreshed course snapshot. Preserve current copy, current add/remove enrollment behavior, and all existing action contracts.</action>
  <acceptance_criteria>
    - `src/app/(teacher)/teacher/courses/[courseId]/page.tsx` no longer contains JSX inside the `try` block.
    - `src/app/(teacher)/teacher/courses/[courseId]/lessons/page.tsx` no longer contains JSX inside the `try` block.
    - `src/app/(teacher)/teacher/courses/import/[batchId]/page.tsx` no longer contains JSX inside the `try` block.
    - `src/components/courses/course-detail-form.tsx` no longer imports `useEffect`.
    - `src/components/surfaces/teacher-course-detail-surface.tsx` passes a deterministic `key` to `CourseDetailForm`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec eslint "src/app/(teacher)/teacher/courses/[courseId]/page.tsx" "src/app/(teacher)/teacher/courses/[courseId]/lessons/page.tsx" "src/app/(teacher)/teacher/courses/import/[batchId]/page.tsx" "src/components/courses/course-detail-form.tsx" "src/components/surfaces/teacher-course-detail-surface.tsx"</automated>
  </verify>
  <done>Active milestone course detail surfaces no longer rely on known lint blockers.</done>
</task>

<task type="auto">
  <name>Task 2: align classroom and runtime proof tests to the current typed DTO contracts</name>
  <files>src/components/classroom/classroom-roster-panel.test.tsx, src/components/classroom/classroom-student-detail-panel.test.tsx, src/features/runtime-platform/classroom/runtime-session.test.ts</files>
  <read_first>src/components/classroom/classroom-roster-panel.test.tsx; src/components/classroom/classroom-student-detail-panel.test.tsx; src/features/runtime-platform/classroom/runtime-session.test.ts; src/lib/dto/classroom.ts; scripts/bootstrap-dev-db.ts</read_first>
  <action>Update milestone-touched tests so they satisfy the current `ClassroomParticipantMonitoringDTO` and runtime proof payload contracts. Add `runtimeProof: null` where the DTO now requires it, align submitted-proof fixtures to the current flattened summary fields, and in the runtime proof seed test assert the typed `runtime` payload through a local cast rather than assuming every bootstrap step payload is structurally narrowed by TypeScript. Do not weaken the DTO or remove required proof fields just to satisfy tests.</action>
  <acceptance_criteria>
    - `src/components/classroom/classroom-roster-panel.test.tsx` includes `runtimeProof: null` for non-runtime participant fixtures.
    - `src/components/classroom/classroom-student-detail-panel.test.tsx` uses the current `runtimeProof` fields `runtimeInstanceId`, `summaryTitle`, and `summaryLabel`.
    - `src/features/runtime-platform/classroom/runtime-session.test.ts` no longer accesses `.payload.runtime` without narrowing.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Milestone-touched classroom/runtime proof tests match the actual typed contracts and stop blocking the phase baseline.</done>
</task>

</tasks>

<verification>
- `pnpm exec eslint "src/app/(teacher)/teacher/courses/[courseId]/page.tsx" "src/app/(teacher)/teacher/courses/[courseId]/lessons/page.tsx" "src/app/(teacher)/teacher/courses/import/[batchId]/page.tsx" "src/components/courses/course-detail-form.tsx" "src/components/surfaces/teacher-course-detail-surface.tsx"`
- `pnpm typecheck`
</verification>

<output>
After completion, create `.planning/phases/35-verification-baseline-convergence-and-milestone-close/35-01-SUMMARY.md`
</output>
