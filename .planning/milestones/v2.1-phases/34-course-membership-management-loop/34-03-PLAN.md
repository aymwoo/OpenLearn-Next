---
phase: 34-course-membership-management-loop
plan: 03
type: execute
wave: 3
depends_on:
  - 34-01
  - 34-02
files_modified:
  - src/components/courses/course-detail-form.tsx
  - src/components/courses/course-detail-form.test.tsx
  - src/components/surfaces/teacher-course-detail-surface.tsx
  - src/app/(teacher)/teacher/courses/[courseId]/page.tsx
  - scripts/verify-phase34-course-membership.ts
  - package.json
autonomous: true
requirements:
  - COURSE-07
must_haves:
  truths:
    - D-07/D-08/D-09: 成员管理继续内嵌在课程详情页，采用名单式成员区，不新建独立 route，archived 课程固定只读禁改。
    - UI-SPEC Interaction contract: add/remove 后在 section 内保留页内反馈，当前成员区和可添加候选区同步更新，hero `学生数` 也同步刷新。
    - Success Criteria 4: touched course-management surfaces 需要有 focused 回归和单独 `verify:phase34`，以便后续 Phase 35 收敛 lint/typecheck 与功能验证。
  artifacts:
    - path: src/components/courses/course-detail-form.tsx
      provides: in-page course membership management loop with same-route feedback and archived read-only posture
      contains: 课程成员管理
    - path: src/components/courses/course-detail-form.test.tsx
      provides: component-level proof for add, remove, duplicate, and archived read-only behavior
      contains: 加入课程
    - path: scripts/verify-phase34-course-membership.ts
      provides: dedicated phase verifier for route posture, DTO wiring, and membership interaction invariants
      contains: verify:phase34
  key_links:
    - from: src/components/surfaces/teacher-course-detail-surface.tsx
      to: src/components/courses/course-detail-form.tsx
      via: same-page course detail workflow remains the only course membership operating surface
      pattern: CourseDetailForm
    - from: package.json
      to: scripts/verify-phase34-course-membership.ts
      via: dedicated phase verification command
      pattern: verify:phase34
---

<objective>
把课程成员 loop 产品化到现有课程详情体验中，并补齐 focused UI regressions 与 Phase 34 verifier，确保 `COURSE-07` 不是“后端已支持、页面却仍不可用”的半闭环状态。

Purpose: Phase 34 的用户价值落在课程详情页里真实可见的 add/remove/member-feedback loop；没有 UI productization 和 verifier，这一 phase 无法作为 milestone 的可交付能力被证明。
Output: membership section inside course detail, focused component regressions, `verify:phase34`.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-course-membership-management-loop/34-CONTEXT.md
@.planning/phases/34-course-membership-management-loop/34-RESEARCH.md
@.planning/phases/34-course-membership-management-loop/34-UI-SPEC.md
@.planning/phases/34-course-membership-management-loop/34-01-PLAN.md
@.planning/phases/34-course-membership-management-loop/34-02-PLAN.md
@src/app/(teacher)/teacher/courses/[courseId]/page.tsx
@src/components/courses/course-detail-form.tsx
@src/components/courses/course-detail-form.test.tsx
@src/components/surfaces/teacher-course-detail-surface.tsx
@package.json

<interfaces>
User-visible loop finalized in this plan:
```ts
GET /teacher/courses/[courseId]
POST addCourseEnrollmentAction(...)
POST removeCourseEnrollmentAction(...)
```

UI section copy required by this plan includes at least:
```ts
"课程成员管理"
"加入课程"
"移出课程"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the in-page course membership section inside the detail form</name>
  <files>src/components/courses/course-detail-form.tsx, src/components/courses/course-detail-form.test.tsx, src/components/surfaces/teacher-course-detail-surface.tsx, src/app/(teacher)/teacher/courses/[courseId]/page.tsx</files>
  <read_first>src/components/courses/course-detail-form.tsx; src/components/courses/course-detail-form.test.tsx; src/components/surfaces/teacher-course-detail-surface.tsx; src/app/(teacher)/teacher/courses/[courseId]/page.tsx; .planning/phases/34-course-membership-management-loop/34-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: the detail page renders a `课程成员管理` section between class association and dangerous actions, with current members and eligible-student add controls inside the same page.
    - Test 2: adding a student updates the current members list, success feedback region, and course `学生数` metric through the refreshed detail DTO.
    - Test 3: removing a student updates the list and keeps the workflow on the same route.
    - Test 4: archived courses still render members but disable all add or remove controls and show read-only guidance.
    - Test 5: duplicate or stale-candidate failures remain visible in-page instead of collapsing into a generic toast-only flow.
  </behavior>
  <action>Update `src/components/courses/course-detail-form.tsx` to add a new membership section after the class association block and before dangerous actions. Reuse the existing success or error feedback pattern, but ensure membership actions can show explicit section-level messages such as `已将 {学生名} 加入当前课程。`, `已将 {学生名} 移出当前课程。`, and the locked duplicate or read-only failure copy from Plan 02. Render current members as compact tonal cards or chips with `studentName`, `studentNumber`, and `classLabels`, plus an inline `移出课程` action. Add a lightweight search input or filter over the server-provided `eligibleStudents` DTO list and wire `加入课程` to the new actions without introducing a new route or a global roster table. When the course is archived, keep the section visible but disable add or remove controls and show the approved read-only explanation. Update `src/components/surfaces/teacher-course-detail-surface.tsx` only as needed so the hero metric `学生数` and surrounding copy remain aligned with the refreshed detail DTO, and keep `src/app/(teacher)/teacher/courses/[courseId]/page.tsx` on the same route posture.</action>
  <acceptance_criteria>
    - `src/components/courses/course-detail-form.tsx` contains `课程成员管理`, `加入课程`, and `移出课程`.
    - `src/components/courses/course-detail-form.tsx` renders membership UI before the dangerous actions block.
    - `src/components/courses/course-detail-form.test.tsx` contains add, remove, duplicate or error, and archived read-only assertions for course members.
    - `src/components/surfaces/teacher-course-detail-surface.tsx` still reads `course.enrollmentCount` for the `学生数` metric.
    - No new `/teacher/courses/[courseId]/members` route is introduced.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/courses/course-detail-form.test.tsx</automated>
  </verify>
  <done>The course detail page now contains a complete, same-route course membership management loop that teachers can actually use.</done>
</task>

<task type="auto">
  <name>Task 2: Add a dedicated Phase 34 verifier for membership route, DTO, and interaction invariants</name>
  <files>scripts/verify-phase34-course-membership.ts, package.json, src/components/courses/course-detail-form.test.tsx, src/lib/dal/course-authoring.test.ts, src/actions/course-authoring-actions.test.ts</files>
  <read_first>package.json; scripts/verify-phase15-course-import.ts; scripts/verify-phase21-contracts.ts; src/components/courses/course-detail-form.tsx; src/lib/dal/course-authoring.ts; src/actions/course-authoring-actions.ts</read_first>
  <action>Create `scripts/verify-phase34-course-membership.ts` and register `verify:phase34` in `package.json`. The verifier must statically and behaviorally guard the locked Phase 34 invariants: (1) the detail DTO still contains membership slices such as `members` and `eligibleStudents`; (2) enrollment actions still parse `courseId` and `studentId` through Zod; (3) membership reads still derive eligibility from linked classes rather than a same-school global student scan; (4) archived courses still present read-only membership posture; (5) the detail form still contains `课程成员管理`, `加入课程`, and `移出课程`; (6) the workflow stays on `/teacher/courses/[courseId]` without a new members route; and (7) no touched page or surface introduced `eval(`, `dangerouslySetInnerHTML`, or direct `@/db` imports. Pair the verifier with the focused DAL, action, and component suites already expanded in Plans 01 and 02 so Phase 34 has one executable regression command.</action>
  <acceptance_criteria>
    - `package.json` contains `verify:phase34`.
    - `scripts/verify-phase34-course-membership.ts` contains checks for `members`, `eligibleStudents`, `courseId`, and `studentId`.
    - `scripts/verify-phase34-course-membership.ts` contains guards against a new members route and unsafe page or surface shortcuts.
    - The verification command references the focused DAL, action, and component tests touched by Phase 34.
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase34</automated>
  </verify>
  <done>Phase 34 ends with a single command that protects the membership workflow, route posture, and DTO or action safety guarantees from future drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detail page UI -> course membership actions | Teacher-facing controls must not widen roster scope or bypass the server action boundary |
| repository sources -> phase verifier | Static and focused-test checks decide whether the membership loop still honors the locked workflow posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07 | I | `src/components/courses/course-detail-form.tsx` | mitigate | Keep membership candidate rendering tied to server DTOs and never expose same-school global roster data. |
| T-34-08 | D | `src/components/surfaces/teacher-course-detail-surface.tsx` | mitigate | Preserve the same route and same-page feedback loop so course-member cleanup remains connected to delete blockers and course metrics. |
| T-34-09 | R | `scripts/verify-phase34-course-membership.ts` | mitigate | Add deterministic guards for route posture, DTO shape, action parsing, and unsafe rendering shortcuts. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/course-authoring.test.ts src/actions/course-authoring-actions.test.ts src/components/courses/course-detail-form.test.tsx`
- `pnpm verify:phase34`
</verification>

<success_criteria>
- Teachers can view, add, and remove course members inside the existing course detail experience.
- Archived courses remain readable but non-mutable for membership management.
- Phase 34 has a dedicated automated verification command guarding its route, DTO, action, and UI invariants.
</success_criteria>

<output>
After completion, create `.planning/phases/34-course-membership-management-loop/34-03-SUMMARY.md`
</output>
