---
phase: 34-course-membership-management-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 34-01
files_modified:
  - src/db/schema.ts
  - src/lib/dto/course-authoring.ts
  - src/lib/dal/course-authoring.ts
  - src/lib/dal/course-authoring.test.ts
  - src/actions/course-authoring-actions.ts
  - src/actions/course-authoring-actions.test.ts
autonomous: true
requirements:
  - COURSE-07
must_haves:
  truths:
    - D-04/D-05/D-06: 成员管理只做单个学生 add/remove，成功后返回刷新后的课程详情 DTO，重复添加必须显式拦截或幂等处理。
    - D-09/D-10: archived 课程成员管理为只读禁改，且写入继续复用 `course-authoring` 的 DAL + Server Actions + cache contract。
    - Success Criteria 2/3: enrollment 写路径必须有 duplicate guard、failure semantics 和 read-your-writes，不新增 route handler shortcut 或 UI-side DB access。
  artifacts:
    - path: src/db/schema.ts
      provides: durable duplicate-prevention constraint for course memberships if needed
      contains: courseEnrollment
    - path: src/lib/dal/course-authoring.ts
      provides: teacher-scoped add or remove enrollment helpers that return refreshed detail DTOs
      contains: addCourseEnrollmentForTeacherScoped
    - path: src/actions/course-authoring-actions.ts
      provides: parsed enrollment mutation actions with tag invalidation and user-facing error mapping
      contains: addCourseEnrollmentAction
  key_links:
    - from: src/actions/course-authoring-actions.ts
      to: src/lib/dal/course-authoring.ts
      via: validated enrollment mutation boundary using the existing course-authoring action pattern
      pattern: CourseEnrollmentInputSchema|addCourseEnrollmentAction|removeCourseEnrollmentAction
    - from: src/lib/dal/course-authoring.ts
      to: src/db/schema.ts
      via: unique or idempotent enrollment write path backed by `courseEnrollment`
      pattern: courseEnrollments
---

<objective>
实现课程成员 add or remove 写路径，把单个学生 enrollment 变更收口到现有 `course-authoring` 安全边界中，并提供重复拦截、只读归档语义和 tag-safe read-your-writes。

Purpose: 没有诚实的 mutation contract，Phase 34 会沦为“详情页能看 roster，但真正加减人还要靠隐藏捷径或脆弱客户端状态”。
Output: enrollment DAL writes, strict action schemas, explicit error semantics, focused write regressions.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-course-membership-management-loop/34-CONTEXT.md
@.planning/phases/34-course-membership-management-loop/34-RESEARCH.md
@.planning/phases/34-course-membership-management-loop/34-01-PLAN.md
@src/db/schema.ts
@src/lib/dto/course-authoring.ts
@src/lib/dal/course-authoring.ts
@src/lib/dal/course-authoring.test.ts
@src/actions/course-authoring-actions.ts
@src/actions/course-authoring-actions.test.ts

<interfaces>
Mutation contracts added in this plan:
```ts
type CourseEnrollmentInput = {
  courseId: string;
  studentId: string;
};
```

Action result error vocabulary introduced or extended in this plan:
```ts
"COURSE_ENROLLMENT_EXISTS"
"STUDENT_NOT_ELIGIBLE"
"COURSE_MEMBERSHIP_READ_ONLY"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement teacher-scoped enrollment add or remove helpers with duplicate and scope guards</name>
  <files>src/db/schema.ts, src/lib/dto/course-authoring.ts, src/lib/dal/course-authoring.ts, src/lib/dal/course-authoring.test.ts</files>
  <read_first>src/db/schema.ts; src/lib/dto/course-authoring.ts; src/lib/dal/course-authoring.ts; src/lib/dal/course-authoring.test.ts; .planning/phases/34-course-membership-management-loop/34-RESEARCH.md</read_first>
  <behavior>
    - Test 1: add enrollment succeeds only when the student is inside the linked-class eligible pool for the teacher-owned course.
    - Test 2: duplicate add returns a structured domain error or idempotent success without creating a second enrollment record.
    - Test 3: remove enrollment deletes the association if present, returns the refreshed detail DTO, and reintroduces the student into the eligible pool.
    - Test 4: archived courses reject add or remove writes with an explicit read-only error.
  </behavior>
  <action>Add `CourseEnrollmentInputSchema` to `src/lib/dto/course-authoring.ts`, then implement `addCourseEnrollmentForTeacherScoped({ courseId, studentId })` and `removeCourseEnrollmentForTeacherScoped({ courseId, studentId })` in `src/lib/dal/course-authoring.ts`. Both helpers must reuse `getScopedOwnedCourse()` and the linked-class eligibility derivation from Plan 01. On add, check whether the course is archived, verify the student appears in the eligible linked-class pool, then block duplicates via an application guard and, if the schema lacks it, add a unique `(courseId, studentId)` constraint to `courseEnrollment` in `src/db/schema.ts`. On remove, delete the targeted enrollment row if it exists, keep the operation idempotent, and return `getTeacherCourseDetailDTO({ courseId })` so member count, eligibility, and delete blockers refresh together. Extend DAL tests for eligible add, duplicate rejection, out-of-scope student rejection, archived-course read-only failure, and remove rehydration of the eligible pool.</action>
  <acceptance_criteria>
    - `src/lib/dto/course-authoring.ts` contains `CourseEnrollmentInputSchema`.
    - `src/lib/dal/course-authoring.ts` contains `addCourseEnrollmentForTeacherScoped` and `removeCourseEnrollmentForTeacherScoped`.
    - `src/lib/dal/course-authoring.ts` returns the refreshed detail DTO after add or remove enrollment writes.
    - `src/lib/dal/course-authoring.test.ts` contains cases for duplicate guard, linked-class eligibility, archived-course read-only, and removal success.
    - `src/db/schema.ts` documents or enforces duplicate prevention for `courseEnrollment` writes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/course-authoring.test.ts</automated>
  </verify>
  <done>Course membership writes now exist as teacher-scoped DAL helpers instead of ad hoc UI behavior, and duplicates or archived-course writes are handled explicitly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add parsed course-membership actions with cache invalidation and domain error mapping</name>
  <files>src/actions/course-authoring-actions.ts, src/actions/course-authoring-actions.test.ts</files>
  <read_first>src/actions/course-authoring-actions.ts; src/actions/course-authoring-actions.test.ts; src/lib/dto/course-authoring.ts; src/lib/dal/course-authoring.ts</read_first>
  <behavior>
    - Test 1: add or remove actions validate `courseId` and `studentId` through Zod before touching the DAL.
    - Test 2: successful add or remove actions invalidate both `teacherCourses(actorId)` and `course(courseId)`.
    - Test 3: duplicate, stale-candidate, and archived read-only failures map to explicit user-facing messages instead of generic `ACTION_FAILED`.
  </behavior>
  <action>Extend `src/actions/course-authoring-actions.ts` with a helper parallel to the class-association runner, then add `addCourseEnrollmentAction()` and `removeCourseEnrollmentAction()`. Parse input with `CourseEnrollmentInputSchema`, call `assertActiveTeacher()`, delegate to the new DAL helpers, and invalidate `teacherCourses(actorId)` plus `course(courseId)` via `updateTag()`. Expand `handleActionError()` so `COURSE_ENROLLMENT_EXISTS` maps to the exact duplicate copy `该学生已经在当前课程中，无需重复添加。`, `STUDENT_NOT_ELIGIBLE` maps to `你当前无法管理这名学生的课程关联。`, and `COURSE_MEMBERSHIP_READ_ONLY` maps to `归档课程仅支持查看成员，暂不支持修改。`. Add action tests for validation failure, duplicate mapping, read-only mapping, and success invalidation.</action>
  <acceptance_criteria>
    - `src/actions/course-authoring-actions.ts` contains `addCourseEnrollmentAction` and `removeCourseEnrollmentAction`.
    - `src/actions/course-authoring-actions.ts` contains mappings for `COURSE_ENROLLMENT_EXISTS`, `STUDENT_NOT_ELIGIBLE`, and `COURSE_MEMBERSHIP_READ_ONLY`.
    - `src/actions/course-authoring-actions.test.ts` asserts tag invalidation and explicit error messages for enrollment actions.
    - No direct `db` import is introduced into the action layer.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/actions/course-authoring-actions.test.ts</automated>
  </verify>
  <done>The enrollment loop now has a complete server-action boundary with predictable cache and failure semantics that the detail page can consume directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detail page mutation input -> course-authoring actions | Untrusted client selections become authoritative enrollment writes only after Zod parsing |
| course-authoring DAL -> courseEnrollment table | Server-owned scope checks decide whether a write can create or remove a membership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04 | T | `src/db/schema.ts` | mitigate | Add or preserve duplicate-prevention protection on `(courseId, studentId)` so repeated add clicks cannot create silent duplicates. |
| T-34-05 | E | `src/lib/dal/course-authoring.ts` | mitigate | Recompute eligibility from linked classes before each add or remove write and block archived-course mutations. |
| T-34-06 | I | `src/actions/course-authoring-actions.ts` | mitigate | Map domain failures to explicit user-facing messages instead of leaking raw DAL or database errors. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/course-authoring.test.ts`
- `pnpm test --run src/actions/course-authoring-actions.test.ts`
</verification>

<success_criteria>
- Single-student add or remove enrollment writes are available through the existing course-authoring action boundary.
- Duplicate adds, archived-course writes, and stale or out-of-scope students are handled explicitly.
- Successful writes immediately refresh the course detail read model through tag invalidation and DTO return.
</success_criteria>

<output>
After completion, create `.planning/phases/34-course-membership-management-loop/34-02-SUMMARY.md`
</output>
