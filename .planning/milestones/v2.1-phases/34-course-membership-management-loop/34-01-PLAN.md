---
phase: 34-course-membership-management-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dto/course-authoring.ts
  - src/lib/dal/course-authoring.ts
  - src/lib/dal/course-authoring.test.ts
autonomous: true
requirements:
  - COURSE-07
must_haves:
  truths:
    - D-01/D-02/D-03: 课程成员 read model 只来自当前课程已关联班级中的学生，并继续复用 teacher-owned course scope，不暴露同校其它 roster。
    - D-05/D-10/D-12: 课程详情 DTO 必须直接输出成员列表、可添加候选和删除阻断联动所需摘要，保持 read-your-writes 与 delete eligibility 同步。
    - UI-SPEC Membership read-model contract: UI 只消费 sanitized `current members` 与 `eligible students` 两类 DTO slice，不拼接 raw rows。
  artifacts:
    - path: src/lib/dto/course-authoring.ts
      provides: membership-aware teacher course detail DTO slices and strict enrollment input schema
      contains: TeacherCourseMemberDTOSchema
    - path: src/lib/dal/course-authoring.ts
      provides: linked-class-scoped membership read model inside the existing course detail DTO
      contains: getTeacherCourseDetailDTO
    - path: src/lib/dal/course-authoring.test.ts
      provides: focused proof that membership reads stay owner-scoped and linked-class-scoped
      contains: eligibleStudents
  key_links:
    - from: src/lib/dal/course-authoring.ts
      to: src/db/schema.ts
      via: courseClasses -> classMembers -> users -> courseEnrollments linked-class roster derivation
      pattern: courseClasses|classMembers|courseEnrollments
    - from: src/lib/dal/course-authoring.ts
      to: src/lib/dto/course-authoring.ts
      via: detail DTO shaping for current members and eligible students
      pattern: TeacherCourseDetailDTOSchema
---

<objective>
扩展 `course-authoring` 详情读模型，让课程详情页能在单一 DTO 中拿到当前课程成员和可添加学生候选，且候选来源严格锁定在当前课程已关联班级。

Purpose: 如果 Phase 34 先从 UI 或新 helper 临时拼 roster，后续 add/remove 写路径就会建立在漂移的作用域事实上，直接违背 Phase 33 刚收紧的 auth 或 DTO contract。
Output: membership-aware detail DTO, linked-class-scoped roster derivation, focused DAL regressions.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-course-membership-management-loop/34-CONTEXT.md
@.planning/phases/34-course-membership-management-loop/34-RESEARCH.md
@.planning/phases/34-course-membership-management-loop/34-UI-SPEC.md
@.planning/phases/33-project-level-auth-data-and-classroom-durability-closure/33-CONTEXT.md
@.planning/phases/14-course-lifecycle-and-associations/14-02-SUMMARY.md
@src/db/schema.ts
@src/lib/dto/course-authoring.ts
@src/lib/dto/class-management.ts
@src/lib/dal/course-authoring.ts
@src/lib/dal/class-management.ts
@src/lib/dal/course-authoring.test.ts

<interfaces>
DTO slices added in this plan:
```ts
type TeacherCourseMemberDTO = {
  studentId: string;
  studentName: string;
  studentNumber: string;
  classLabels: string[];
  enrollmentStatus: "active";
};

type TeacherCourseEligibleStudentDTO = {
  studentId: string;
  studentName: string;
  studentNumber: string;
  classLabels: string[];
  isAlreadyEnrolled: boolean;
};
```

`TeacherCourseDetailDTO` must be extended to include at least:
```ts
members: TeacherCourseMemberDTO[]
eligibleStudents: TeacherCourseEligibleStudentDTO[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the course-authoring DTO contract with membership-aware detail slices</name>
  <files>src/lib/dto/course-authoring.ts, src/lib/dal/course-authoring.ts, src/lib/dal/course-authoring.test.ts</files>
  <read_first>src/lib/dto/course-authoring.ts; src/lib/dal/course-authoring.ts; src/lib/dal/course-authoring.test.ts; src/lib/dto/class-management.ts; .planning/phases/34-course-membership-management-loop/34-CONTEXT.md; .planning/phases/34-course-membership-management-loop/34-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: `TeacherCourseDetailDTO` returns `members` and `eligibleStudents` as sanitized DTO arrays rather than raw joined rows.
    - Test 2: `members` include only currently enrolled students for the course, while `eligibleStudents` include only students reachable through the course's linked classes.
    - Test 3: same-school foreign courses and out-of-scope schools still resolve to `COURSE_NOT_FOUND` instead of exposing roster details.
  </behavior>
  <action>Add `TeacherCourseMemberDTOSchema` and `TeacherCourseEligibleStudentDTOSchema` to `src/lib/dto/course-authoring.ts`, then extend `TeacherCourseDetailDTOSchema` with `members` and `eligibleStudents`. Update `src/lib/dal/course-authoring.ts` so `getCachedTeacherCourseDetailDTO()` derives a membership read model from the exact join chain `courseClasses -> classMembers -> users`, filtered to `role === "student"`, then merges it with `courseEnrollments` to produce current enrolled members and not-yet-enrolled eligible candidates. Include `studentId`, `studentName`, `studentNumber`, and sorted `classLabels`; do not expose raw membership rows, timestamps, or unrelated user fields. Update `src/lib/dal/course-authoring.test.ts` with focused cases that prove the DTO only returns linked-class students, excludes same-school foreign courses, and keeps `enrollmentCount` aligned with the membership slices.</action>
  <acceptance_criteria>
    - `src/lib/dto/course-authoring.ts` contains `TeacherCourseMemberDTOSchema` and `TeacherCourseEligibleStudentDTOSchema`.
    - `src/lib/dto/course-authoring.ts` extends `TeacherCourseDetailDTOSchema` with `members` and `eligibleStudents`.
    - `src/lib/dal/course-authoring.ts` references `classMembers`, `users`, and `courseEnrollments` when building the course detail DTO.
    - `src/lib/dal/course-authoring.test.ts` contains assertions for linked-class-only eligibility and membership DTO contents.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/course-authoring.test.ts</automated>
  </verify>
  <done>The course detail read path now has a single server-owned truth for current members and eligible students, ready for add or remove mutations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Couple the membership read model to delete eligibility and archived-course detail posture</name>
  <files>src/lib/dal/course-authoring.ts, src/lib/dal/course-authoring.test.ts</files>
  <read_first>src/lib/dal/course-authoring.ts; src/lib/dal/course-authoring.test.ts; .planning/phases/14-course-lifecycle-and-associations/14-02-SUMMARY.md; .planning/phases/34-course-membership-management-loop/34-CONTEXT.md</read_first>
  <behavior>
    - Test 1: `deleteEligibility` still includes `COURSE_HAS_ENROLLMENTS` when current members exist, and the count matches the membership read model.
    - Test 2: archived courses remain readable through `getTeacherCourseDetailDTO()` and still expose current members, but the DTO can signal read-only membership posture to the UI.
  </behavior>
  <action>Keep `buildCourseDeleteEligibility()` as the single delete-block source, but ensure the membership-aware detail DTO keeps `enrollmentCount`, `members.length`, and the `COURSE_HAS_ENROLLMENTS` blocker aligned. Add a lightweight DTO signal for the UI's archived-course read-only behavior if needed, such as reusing `status === "archived"` or adding a boolean `membershipReadOnly`; do not create a separate page-level permission system. Extend DAL tests so an archived course still returns detail data, member lists, and truthful delete blockers without exposing any write shortcut.</action>
  <acceptance_criteria>
    - `src/lib/dal/course-authoring.ts` keeps `COURSE_HAS_ENROLLMENTS` tied to the same enrollment truth used by `members`.
    - `src/lib/dal/course-authoring.test.ts` asserts enrollment blockers and archived-course readability after the membership DTO expansion.
    - No new route handler or feature root is introduced for course members in this plan.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/course-authoring.test.ts</automated>
  </verify>
  <done>Delete blocking and archived-course read behavior remain tied to the same membership truth, so later UI work can explain enrollment blockers honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| course detail DAL -> membership read model | Server joins derive roster data that must stay teacher-owned and linked-class-scoped |
| membership DTO -> detail page UI | Only sanitized student summaries cross into the client surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | I | `src/lib/dal/course-authoring.ts` | mitigate | Build eligibility strictly from linked classes, not same-school global membership scans. |
| T-34-02 | E | `src/lib/dto/course-authoring.ts` | mitigate | Return only stable student summary fields and class labels; do not expose raw membership or user internals. |
| T-34-03 | T | `src/lib/dal/course-authoring.test.ts` | mitigate | Add regressions for foreign-course rejection and linked-class-only candidates before mutation work begins. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/course-authoring.test.ts`
</verification>

<success_criteria>
- Course detail DTO owns both current members and eligible students.
- Eligibility is provably limited to linked classes under the existing teacher-owned course scope.
- Enrollment blockers, member counts, and archived-course readability stay aligned.
</success_criteria>

<output>
After completion, create `.planning/phases/34-course-membership-management-loop/34-01-SUMMARY.md`
</output>
