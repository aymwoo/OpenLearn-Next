---
phase: 33-project-level-auth-data-and-classroom-durability-closure
plan: 04
type: execute
wave: 4
depends_on: [33-01, 33-02, 33-03]
files_modified:
  - scripts/verify-phase33-auth-data-durability.ts
  - package.json
  - src/lib/auth/auth.test.ts
  - src/lib/auth/auth.config.test.ts
  - src/lib/dal/classroom.test.ts
  - src/lib/dal/course-authoring.test.ts
  - src/actions/auth-actions.test.ts
  - src/actions/classroom-actions.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, CLASS-05]
must_haves:
  truths:
    - "Phase 33 拥有单一 canonical gate，统一验证 auth entry、proxy protection、DAL-only boundary、DTO sanitation、schema posture 与 classroom durability。"
    - "`verify:phase33` 明确证明它建立在 Phase 27-32 runtime foundations 之上，但不会把 close claim 扩展到 runtime expansion。"
    - "milestone-scoped lint or typecheck 噪音在 touched auth/data/classroom surfaces 上继续收敛，不引入新回归。"
  artifacts:
    - path: "scripts/verify-phase33-auth-data-durability.ts"
      provides: "canonical phase 33 verifier"
    - path: "package.json"
      provides: "verify:phase33 script registration"
    - path: "src/lib/dal/classroom.test.ts"
      provides: "classroom durability regression coverage"
  key_links:
    - from: "scripts/verify-phase33-auth-data-durability.ts"
      to: "src/proxy.ts"
      via: "protected-family drift guard"
      pattern: "teacher|student|classroom|admin"
    - from: "scripts/verify-phase33-auth-data-durability.ts"
      to: "src/lib/dal/classroom.ts"
      via: "classroom durability and DTO drift guard"
      pattern: "runtimeSessionId|DTO"
    - from: "scripts/verify-phase33-auth-data-durability.ts"
      to: "src/db/schema.ts"
      via: "schema posture drift guard"
      pattern: "onDelete|index|uniqueIndex"
---

<objective>
建立 Phase 33 的单一验证入口，把 auth、data、classroom durability closure 收口成可信 gate，
并同步控制 touched surfaces 的 lint or typecheck 回归。

Purpose: 没有单一 `verify:phase33`，本阶段很容易变成“修了很多点，但 requirement 仍缺一条明确 close 证据”。
Output: phase-specific verifier, focused regression suite bundle, milestone-scoped baseline repair.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-project-level-auth-data-and-classroom-durability-closure/33-CONTEXT.md
@scripts/verify-phase27-runtime-platform.ts
@scripts/verify-phase28-runtime-bridge.ts
@scripts/verify-phase29-runtime-host.ts
@scripts/verify-phase30-governance.ts
@scripts/verify-phase31-transport-inspector.ts
@scripts/verify-phase32-end-to-end.ts
@package.json
@src/proxy.ts
@src/db/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 `verify:phase33` 单一总闸门</name>
  <files>scripts/verify-phase33-auth-data-durability.ts, package.json</files>
  <behavior>
    - Test 1: verifier 明确检查 auth drift、proxy drift、DAL boundary drift、DTO leakage drift、schema posture drift、classroom durability drift 六类问题。
    - Test 2: verifier 输出会说明它建立在 Phase 27-32 foundation 之上，但不把 runtime expansion 误纳入 close claim。
  </behavior>
  <action>新增 `scripts/verify-phase33-auth-data-durability.ts` 并注册 `verify:phase33`。脚本应复用现有 phase verifier 结构，必要时先重确认 `verify:phase32` 仍通过，以证明 runtime platform foundations 没被 Phase 33 改坏；然后执行真正属于本阶段的静态 guards 与 focused suites。guards 至少覆盖：`auth drift`、`proxy drift`、`dal boundary drift`、`dto leakage drift`、`schema posture drift`、`classroom durability drift`。输出要显式说明本阶段没有做 PostgreSQL、Redis、WebSocket 或多 runtime expansion。
</action>
  <verify>
    <automated>pnpm verify:phase33</automated>
  </verify>
  <done>Phase 33 有单一对外 gate，可直接支撑 requirement close claim。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 清理 touched auth or data or classroom surfaces 的 baseline regressions</name>
  <files>src/lib/auth/auth.test.ts, src/lib/auth/auth.config.test.ts, src/lib/dal/classroom.test.ts, src/lib/dal/course-authoring.test.ts, src/actions/auth-actions.test.ts, src/actions/classroom-actions.test.ts</files>
  <behavior>
    - Test 1: touched focused suites 在 lint or typecheck 维度不引入新回归。
    - Test 2: phase verifier 依赖的测试文件能稳定通过，不被历史噪音掩盖。
  </behavior>
  <action>围绕本阶段 touched files 与 focused suites，顺手消除新增的 lint 或 typecheck regression。目标不是一次性清仓全 repo，而是保证 auth、data、classroom 与 verifier surfaces 具备诚实的 phase-level baseline。若全量 `pnpm lint` 或 `pnpm typecheck` 仍受 repo 其他历史问题阻塞，需要在 summary 中明确隔离 active-scope blocker 与 out-of-scope noise。</action>
  <verify>
    <automated>pnpm test --run src/lib/auth/auth.test.ts src/lib/auth/auth.config.test.ts src/actions/auth-actions.test.ts src/lib/dal/classroom.test.ts src/lib/dal/course-authoring.test.ts src/actions/classroom-actions.test.ts</automated>
  </verify>
  <done>Phase 33 的 close 证据不被 touched-surface baseline regressions 污染。</done>
</task>

</tasks>

<verification>
- `pnpm verify:phase33`
- `pnpm test --run src/lib/auth/auth.test.ts src/lib/auth/auth.config.test.ts src/actions/auth-actions.test.ts src/lib/dal/classroom.test.ts src/lib/dal/course-authoring.test.ts src/actions/classroom-actions.test.ts`
</verification>

<risks>
- 如果直接把 `verify:phase32` 当作 Phase 33 close 证明，会掩盖项目级 auth/data requirement 未被验证的问题。
- 历史 lint or typecheck 噪音较多，必须明确区分 active-scope regression 与 repo-wide pre-existing blocker。
</risks>

<dependencies>
- 依赖 33-01 到 33-03 已把 auth、DAL、schema、classroom durability contract 落地。
- 为 Phase 34 提供可信的 auth/data baseline，避免 course membership loop 继续叠在未验证基础上。
</dependencies>

<output>
After completion, create `.planning/phases/33-project-level-auth-data-and-classroom-durability-closure/33-04-SUMMARY.md`
</output>
