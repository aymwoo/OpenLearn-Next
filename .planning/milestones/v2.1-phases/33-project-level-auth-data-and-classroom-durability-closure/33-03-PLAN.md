---
phase: 33-project-level-auth-data-and-classroom-durability-closure
plan: 03
type: execute
wave: 3
depends_on: [33-01, 33-02]
files_modified:
  - src/db/schema.ts
  - src/db/schema.learning.test.ts
  - src/features/runtime-platform/classroom/runtime-session.ts
  - src/features/runtime-platform/classroom/runtime-session.test.ts
  - src/lib/dal/classroom.ts
  - src/lib/dal/classroom.test.ts
  - src/lib/dal/runtime-inspector.test.ts
  - src/lib/dto/classroom.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-05, CLASS-05]
must_haves:
  truths:
    - "SQLite-first Drizzle schema 继续覆盖 auth、schools、courses、lessons、progress、submissions、classroom、runtime、plugins、themes 等表组。"
    - "parent-owned child records 的 cascade behavior、high-frequency indexes or unique constraints、classroom durable truth 都有可验证证据。"
    - "classroom session state、current step、lock mode、participants、events 与 runtime-session durable truth 对齐，不再只依赖 SSE memory。"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "documented cascade and index posture for phase-33-critical tables"
    - path: "src/lib/dal/classroom.ts"
      provides: "classroom durable snapshot and participant truth aligned with SQLite"
    - path: "src/features/runtime-platform/classroom/runtime-session.ts"
      provides: "runtime-session durability reused instead of parallel truth path"
    - path: "src/lib/dal/classroom.test.ts"
      provides: "focused proof for classroom durability and reconnect-safe truth"
  key_links:
    - from: "src/lib/dal/classroom.ts"
      to: "src/db/schema.ts"
      via: "classroom session or participant or event durable reads"
      pattern: "classroom"
    - from: "src/lib/dal/classroom.ts"
      to: "src/features/runtime-platform/classroom/runtime-session.ts"
      via: "shared durable truth between classroom and runtime proof"
      pattern: "runtimeSessionId"
---

<objective>
把项目级 schema、cascade、index 与 classroom durability requirement 收成可证明的 SQLite truth，
尤其是让 `CLASS-05` 与 Phase 28-32 已有 runtime-session durability 真正对齐。

Purpose: 当前仓库已经有大量持久化表和索引，但 requirement 仍把 `DATA-01`、`DATA-02`、`DATA-05`、`CLASS-05`
标为 pending，说明缺的不是“有没有表”，而是“是否形成了可验证、可共享、无第二真相源的 durability contract”。
Output: schema audit fixes, index or uniqueness proof, classroom durability alignment, focused tests.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/28-runtime-bridge-contracts-and-session-persistence/28-VERIFICATION.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-CONTEXT.md
@.planning/phases/33-project-level-auth-data-and-classroom-durability-closure/33-CONTEXT.md
@src/db/schema.ts
@src/lib/dal/classroom.ts
@src/features/runtime-platform/classroom/runtime-session.ts
@src/lib/dto/classroom.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 审计并补齐 phase-critical schema groups、cascade 与 index posture</name>
  <files>src/db/schema.ts, src/db/schema.learning.test.ts, src/features/runtime-platform/classroom/runtime-session.test.ts</files>
  <behavior>
    - Test 1: auth、membership、course、lesson、progress、submission、classroom、runtime 等 phase-critical 表组的 cascade behavior 与 high-frequency unique or index posture 有 focused proof。
    - Test 2: lesson step order、progress identity、latest submissions、classroom sessions 与 scoped permissions 的关键索引或唯一约束可被回归守住。
  </behavior>
  <action>围绕 `DATA-01`、`DATA-02`、`DATA-05` 做 system-level schema audit。优先检查 Phase 33 直接涉及的表组：Auth.js tables、memberships、course enrollments、lesson steps、lesson step progress、task submissions、quiz attempts、classroom sessions、classroom participants、classroom events、runtime step sessions 与相关审计表。必要时补齐缺失的 cascade、index 或 unique constraint，并通过 focused schema tests 或 verifier static guards 形成证据。不要把 audit 扩展成 PostgreSQL 兼容性工作。</action>
  <verify>
    <automated>pnpm test --run src/db/schema.learning.test.ts src/features/runtime-platform/classroom/runtime-session.test.ts</automated>
  </verify>
  <done>项目级数据层 requirement 的关键 schema truth 已从“存在部分实现”升级为“可验证 closure”。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把 classroom 主真相与 runtime-session durable truth 对齐</name>
  <files>src/lib/dal/classroom.ts, src/lib/dto/classroom.ts, src/features/runtime-platform/classroom/runtime-session.ts, src/lib/dal/classroom.test.ts, src/lib/dal/runtime-inspector.test.ts</files>
  <behavior>
    - Test 1: reconnect or late join 读到的 current step、lock mode、participants、events 来自 SQLite durable truth，而不是只依赖 in-memory SSE state。
    - Test 2: classroom monitoring、recap 或 inspector 共享同一 classroom or runtime durable evidence，不出现第二真相源。
  </behavior>
  <action>复用 `classroom.ts` 与 `runtime-session.ts` 现有 durable contracts，把 classroom session state、active step、lock mode、participants 与 canonical event history 的读取和证明统一到 SQLite truth 上。重点不是新建 transport，而是让 current classroom read models、reconnect path、teacher monitoring 与 runtime proof 继续沿用 durable tables。必要时补 DTO 字段、测试与少量 schema 连接点，但避免把 classroom 重新拆成新 feature root。</action>
  <verify>
    <automated>pnpm test --run src/lib/dal/classroom.test.ts src/lib/dal/runtime-inspector.test.ts src/features/runtime-platform/classroom/runtime-session.test.ts</automated>
  </verify>
  <done>`CLASS-05` 有 focused proof，可证明 classroom truth 已落在 SQLite 并与 runtime durability 共用事实链。</done>
</task>

</tasks>

<verification>
- `pnpm test --run src/db/schema.learning.test.ts src/features/runtime-platform/classroom/runtime-session.test.ts src/lib/dal/classroom.test.ts src/lib/dal/runtime-inspector.test.ts`
</verification>

<risks>
- schema 变更若超出 phase-critical 表组，容易扩大 blast radius；应优先守住 requirement 直接点名的高频读写。
- classroom durability 若引入第二套 snapshot persistence，会直接违背 Phase 28-32 已锁定的 truth ownership。
</risks>

<dependencies>
- 依赖 33-02 已收紧的 classroom or learning or course-authoring 数据边界，避免 durability 证明建立在不可信读写之上。
- 为 33-04 的 phase verifier 提供可检查的 schema and durability truth。
</dependencies>

<output>
After completion, create `.planning/phases/33-project-level-auth-data-and-classroom-durability-closure/33-03-SUMMARY.md`
</output>
