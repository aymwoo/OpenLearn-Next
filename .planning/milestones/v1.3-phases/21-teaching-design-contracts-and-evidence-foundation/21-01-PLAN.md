---
phase: 21-teaching-design-contracts-and-evidence-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dto/lesson-authoring.ts
  - src/lib/dal/lesson-authoring.ts
  - src/lib/dal/lesson-authoring.test.ts
  - src/lib/dto/classroom.ts
  - src/lib/dal/classroom.ts
  - src/lib/dal/classroom.test.ts
autonomous: true
requirements:
  - ORCH-01
must_haves:
  truths:
    - 现有 content/task/quiz 步骤可以携带结构化 teaching-design 元数据，而不是另起分支步骤模型。
    - 已发布 lesson snapshot 和 classroom launch preview 在 teaching metadata 缺失时仍能稳定工作。
    - lesson preview 与 launch contract 都能输出“显式配置 / 默认推断 / 待完善”标记，供教师侧后续 surface 直接消费。
  artifacts:
    - path: src/lib/dto/lesson-authoring.ts
      provides: teaching-design payload schema and preview DTO contract
      contains: teachingDesign
    - path: src/lib/dal/lesson-authoring.ts
      provides: backward-safe parsing and preview/readiness mapping
      contains: getTeacherLessonPreviewDTO
    - path: src/lib/dto/classroom.ts
      provides: launch preview step contract enriched with teaching metadata and fallback status
      contains: ClassroomLaunchPreviewStepDTOSchema
  key_links:
    - from: src/lib/dal/lesson-authoring.ts
      to: src/lib/dto/lesson-authoring.ts
      via: lesson payload parsing and preview DTO generation share the same teaching-design defaults and inference markers
      pattern: lessonStepPayloadSchema|TeacherLessonPreviewDTOSchema
    - from: src/lib/dal/classroom.ts
      to: src/lib/dto/classroom.ts
      via: published snapshot launch preview reuses typed teaching-design fields and fallback markers
      pattern: buildLaunchPreview|ClassroomLaunchPreviewStepDTOSchema
---

<objective>
在现有线性 lesson step 模型上补齐 teaching-design contract，让 authoring、preview
和 class launch 都能理解“教学意图、时长、活动方式、证据预期”，同时保持旧课时向后兼容。

Purpose: 先锁定 ORCH-01 的 typed contract，避免后续 launch、student runtime 和
analytics 各自发明一套教学元数据。
Output: teaching-design payload schema、backward-safe preview mapping、launch preview
contract 更新。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-teacher-flow-editor-enhancement/17-01-SUMMARY.md
@.planning/phases/17-teacher-flow-editor-enhancement/17-03-SUMMARY.md
@src/lib/dto/lesson-authoring.ts
@src/lib/dal/lesson-authoring.ts
@src/lib/dto/classroom.ts
@src/lib/dal/classroom.ts

<interfaces>
From `src/lib/dto/lesson-authoring.ts`:
```ts
export const contentStepPayloadSchema = z.object({
  type: z.literal("content"),
  title: z.string().min(1),
  body: z.string().min(1),
  teacherNotes: z.string().optional(),
  materialRefs: z.array(materialRefSchema).default([]),
  markdown: markdownStepConfigSchema.optional(),
  builtInSource: builtInSourceSchema.optional(),
})

export const taskStepPayloadSchema = z.object({
  type: z.literal("task"),
  prompt: z.string().min(1),
  submissionType: z.enum(["text", "image", "file", "link"]).default("text"),
  successCriteria: z.string().optional(),
  materialRefs: z.array(materialRefSchema).default([]),
  builtInSource: builtInSourceSchema.optional(),
})
```

From `src/lib/dto/classroom.ts`:
```ts
export const ClassroomLaunchPreviewStepDTOSchema = z.object({
  id: z.string(),
  order: z.number().int().positive(),
  title: z.string(),
  family: z.string(),
  summary: z.string(),
  estimatedMinutes: z.number().int().nonnegative(),
  materialCues: z.array(z.string()).default([]),
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为 lesson step payload 定义 teaching-design contract 与向后兼容默认值</name>
  <files>src/lib/dto/lesson-authoring.ts, src/lib/dal/lesson-authoring.ts, src/lib/dal/lesson-authoring.test.ts</files>
  <behavior>
    - Test 1: 旧 content/task/quiz payload 没有 `teachingDesign` 时仍能 safe-parse。
    - Test 2: preview DTO 会输出 teaching-design 字段或稳定默认值，而不是返回 `undefined` 漏洞。
    - Test 3: built-in provenance 与 teaching-design 可以共存，不互相覆盖。
  </behavior>
  <action>在 `src/lib/dto/lesson-authoring.ts` 为 `contentStepPayloadSchema`、`taskStepPayloadSchema`、`quizStepPayloadSchema` 增加统一 `teachingDesign` 子对象，严格按 D-02 / D-03 / D-04 命名为 `activityIntent`、`estimatedMinutes`、`activityMode`、`evidenceExpectation`。其中 `activityIntent` 与 `activityMode` 都使用稳定枚举，`estimatedMinutes` 固定为单个正整数分钟数，`evidenceExpectation` 至少包含 `evidenceType`、`prompt`、`required`，并预留 D-05 / D-08 允许的轻量扩展字段（例如 `checklist`、`tags`、`studentVisibility`，默认 `teacher-only` 以满足 D-10）。在 `src/lib/dal/lesson-authoring.ts` 中把旧 payload 默认映射写死到服务端：content 默认 `explain/mini-lecture/12`，task 默认 `practice/independent/15`，quiz 默认 `check/assessment/8`；同时让 `getLessonEditorDTO()`、`getTeacherLessonPreviewDTO()` 和发布前解析都消费同一个默认化 helper，并额外返回 `teachingDesignStatus` / `needsTeachingDesignRefinement` / `teachingDesignFallbackReason` 之类的稳定标记，供后续教师 surface 按 D-17 / D-18 / D-19 / D-20 呈现“默认推断、可继续、待完善”。不要创建新的 step type，也不要要求历史 lesson 先迁移后才能读取。</action>
  <verify>
    <automated>pnpm test --run src/lib/dal/lesson-authoring.test.ts</automated>
  </verify>
  <done>教师编辑器和 preview DTO 已经能稳定读写 teaching-design 元数据，且历史课时不补字段也不会损坏现有 authoring/publish 路径。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 让课堂 launch preview 使用结构化 teaching-design contract</name>
  <files>src/lib/dto/classroom.ts, src/lib/dal/classroom.ts, src/lib/dal/classroom.test.ts</files>
  <behavior>
    - Test 1: launch preview 有 teaching-design 时优先显示结构化 family、estimatedMinutes 与 evidence cue。
    - Test 2: 已发布 snapshot 缺少 teaching-design 时，launch preview 继续回退到旧启发式，不报错。
    - Test 3: launch preview 不读取草稿态 lesson，而继续只读 published snapshot。
  </behavior>
  <action>扩展 `src/lib/dto/classroom.ts` 的 `ClassroomLaunchPreviewStepDTOSchema`，加入可供后续 `/teacher/launch` 直接消费的 teaching-design 字段：`activityIntent`、`activityMode`、`estimatedMinutes`、`evidenceSummary`，以及与 lesson preview 对齐的 `teachingDesignStatus`、`needsTeachingDesignRefinement`、`teachingDesignFallbackReason`。在 `src/lib/dal/classroom.ts` 的 `buildLaunchPreview()` 中优先读取 snapshot step payload 内的 `teachingDesign`，并把 family/summary/estimatedMinutes/evidence summary 统一从结构化字段生成；只有 teaching-design 缺失时才回退到当前 `STEP_DEFAULT_MINUTES` 和摘要启发式。保持 D-22：`launchClassroomSession()`、published snapshot scope 和现有 teacher-owned 读取边界不变，不要把草稿 lesson 或 UI 本地推断接入 launch contract。</action>
  <verify>
    <automated>pnpm test --run src/lib/dal/classroom.test.ts</automated>
  </verify>
  <done>class launch contract 已经理解结构化教学设计元数据，且旧 published lesson snapshot 仍可直接发起课堂。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authoring payload -> lesson DTOs | 教师输入的 step payload 进入服务端解析与 published snapshot |
| published snapshot -> classroom launch preview | 课堂启动预览消费已发布 JSON snapshot，而不是直接读草稿态 UI 数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | T | `src/lib/dto/lesson-authoring.ts` | mitigate | 用 Zod enum + positive int 限定 teaching-design 字段，拒绝任意结构漂移。 |
| T-21-02 | D | `src/lib/dal/lesson-authoring.ts` | mitigate | 为缺失 teaching-design 的历史 payload 提供服务端默认值，避免旧 lesson 因新增字段失效。 |
| T-21-03 | I | `src/lib/dal/classroom.ts` | mitigate | launch preview 继续只读取 published snapshot，不引入草稿 lesson 或客户端状态泄漏。 |
| T-21-04 | R | `src/lib/dal/classroom.test.ts` | accept | 测试仅验证 contract 与 fallback，不处理敏感数据。 |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/lesson-authoring.test.ts`
- `pnpm test --run src/lib/dal/classroom.test.ts`
</verification>

<success_criteria>
- step payload 拥有统一 teaching-design contract。
- 历史 lesson 与 published snapshot 在 metadata 缺失时仍保持可读、可预览、可开课。
- launch preview 与 teacher preview contract 都具备稳定 fallback 标记，供后续教师侧 surface 明示“默认推断 / 待完善”。
</success_criteria>

<output>
After completion, create `.planning/phases/21-teaching-design-contracts-and-evidence-foundation/21-01-SUMMARY.md`
</output>
