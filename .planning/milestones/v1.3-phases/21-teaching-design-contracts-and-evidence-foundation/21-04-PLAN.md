---
phase: 21-teaching-design-contracts-and-evidence-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 21-02
  - 21-03
files_modified:
  - src/lib/dto/classroom.ts
  - src/lib/dal/classroom.ts
  - src/lib/dal/classroom.test.ts
  - src/components/surfaces/classroom-console-surface.tsx
  - src/components/classroom/classroom-control-panel.tsx
  - src/components/classroom/classroom-timeline-panel.tsx
  - src/components/classroom/classroom-timeline-panel.test.tsx
autonomous: true
gap_closure: true
requirements:
  - EVAL-03
must_haves:
  truths:
    - 教师在 `/classroom` 记录 intervention 后，能在稳定独立的 timeline panel 里看到标题、正文、目标范围和记录时间，而不是挤进现有控制区。
    - intervention 继续保持 session-owned、teacher-only 过程记录，不泄漏到学生侧 runtime。
    - classroom runtime snapshot 或等价 teacher runtime DTO 能提供 typed timeline read model，UI 不再依赖 ad hoc payload 读取。
  artifacts:
    - path: src/lib/dto/classroom.ts
      provides: teacher timeline read model contract for runtime rendering
      contains: teacherTimeline
    - path: src/components/classroom/classroom-timeline-panel.tsx
      provides: dedicated intervention timeline panel
      contains: 干预记录时间线
    - path: src/components/classroom/classroom-timeline-panel.test.tsx
      provides: regression coverage for timeline layout contract
      contains: targetScope
  key_links:
    - from: src/lib/dal/classroom.ts
      to: src/lib/dto/classroom.ts
      via: snapshot hydration maps classroomTimeline rows into typed teacher timeline entries
      pattern: teacherTimeline
    - from: src/components/classroom/classroom-control-panel.tsx
      to: src/components/classroom/classroom-timeline-panel.tsx
      via: teacher runtime right-rail or dedicated section renders stable timeline container
      pattern: ClassroomTimelinePanel
---

<objective>
修复教师 intervention 在 `/classroom` 里的“布局混乱”问题：把仅有写链路的 classroom timeline 补成教师可读、可布局的专用 read model 和独立 panel。

Purpose: 闭合 UAT Test 5 的 gap，落实 D-12 / D-14 / D-15 / D-16，确保 intervention 不只是持久化成功，还能在教师运行台以稳定的产品级布局被消费。
Output: typed teacher timeline read model、snapshot/runtime DTO 暴露、独立 timeline panel、对应回归测试。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-teaching-design-contracts-and-evidence-foundation/21-UAT.md
@.planning/phases/21-teaching-design-contracts-and-evidence-foundation/21-02-SUMMARY.md
@.planning/phases/21-teaching-design-contracts-and-evidence-foundation/21-03-SUMMARY.md
@.planning/debug/intervention-timeline-layout.md
@src/lib/dto/classroom.ts
@src/lib/dal/classroom.ts
@src/components/surfaces/classroom-console-surface.tsx
@src/components/classroom/classroom-control-panel.tsx

<interfaces>
From `src/lib/dto/classroom.ts`:
```ts
export const ClassroomTimelineEntryDTOSchema = z.object({
  id: z.string(),
  sessionId: z.string(),
  studentId: z.string().nullable(),
  stepId: z.string().nullable(),
  entryType: ClassroomTimelineEntryTypeSchema,
  actorId: z.string(),
  payload: z.record(z.string(), z.unknown()),
  createdAt: z.string(),
});

export const ClassroomSnapshotDTOSchema = z.object({
  sessionId: z.string(),
  participants: z.array(ClassroomParticipantDTOSchema),
  steps: z.array(ClassroomStepDTOSchema),
  slideState: ClassroomSlideStateDTOSchema.nullable().default(null),
});
```

From `src/lib/dal/classroom.ts`:
```ts
export async function recordClassroomIntervention(input: unknown) {
  return createClassroomTimelineEntry({
    entryType: "intervention_noted",
    payload: { title, body, targetScope, visibility: "teacher-only" },
  });
}

export async function getClassroomSnapshotDTO(input: { sessionId: string }) {
  // current snapshot has participants, steps, slideState, but no timeline read model yet
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 补齐教师 timeline read model，并把 intervention 注入 runtime snapshot</name>
  <files>src/lib/dto/classroom.ts, src/lib/dal/classroom.ts, src/lib/dal/classroom.test.ts</files>
  <behavior>
    - Test 1: `getClassroomSnapshotDTO()` 对教师调用时返回 typed 的 `teacherTimeline`（或同等命名的 teacher-only runtime timeline 字段），每条 entry 至少包含 `title`、`body`、`targetScope`、`createdAt`，必要时带 `studentName` / `stepTitle` 辅助文案。
    - Test 2: timeline read path 只消费本 session 的 `intervention_noted` 记录，不把别的 session 或学生侧不该见的数据混进来。
    - Test 3: 非教师上下文不会拿到 teacher-only intervention 正文；如果沿用通用 snapshot DTO，则该字段对非教师返回空数组而不是泄漏 payload。
  </behavior>
  <action>按 D-12 / D-14 / D-15 / D-16 修改 `src/lib/dto/classroom.ts` 与 `src/lib/dal/classroom.ts`：把当前仅有 raw `ClassroomTimelineEntryDTO` 的写模型，收敛为教师运行台首发可直接消费的 typed read model。优先为 `/classroom` 补 `teacherTimeline`（或等价的 teacher runtime DTO 字段），只映射 `intervention_noted` entries，并把 raw payload 规范化成稳定字段：`title`、`body`、`targetScope`、`visibility`、`createdAt`，可附加 `studentName`、`stepTitle`、`targetLabel` 这类 UI 直接需要的字符串。读取时必须严格按 `sessionId` 过滤，并保持 teacher-only 语义：不要把 intervention 正文泄漏给学生侧 snapshot consumer。同步扩展 `src/lib/dal/classroom.test.ts`，新增针对 teacher timeline hydration、session scoping、teacher-only 边界的回归测试；不要只做源码字符串断言。</action>
  <verify>
    <automated>pnpm test --run src/lib/dal/classroom.test.ts</automated>
  </verify>
  <done>教师 runtime 已拥有稳定的 intervention timeline 读模型；后续 UI 可以直接渲染明确字段，而不是在组件中解析自由 payload。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新增独立 timeline panel，并把 intervention 从拥挤控制区移到稳定容器</name>
  <files>src/components/surfaces/classroom-console-surface.tsx, src/components/classroom/classroom-control-panel.tsx, src/components/classroom/classroom-timeline-panel.tsx, src/components/classroom/classroom-timeline-panel.test.tsx</files>
  <behavior>
    - Test 1: `/classroom` 教师运行台出现独立的 timeline/intervention panel，而不是把 intervention 文本散落在 hero、控制按钮区或 roster 区。
    - Test 2: 每条 intervention entry 稳定呈现 `title`、`body`、`targetScope`、`createdAt`，长正文换行后仍保持卡片边界和阅读节奏。
    - Test 3: 空 timeline 时显示中文 empty state；有 timeline 时保留单一 gradient 主舞台，新增 panel 仍回落到 tonal card，不引入第二个 hero。
  </behavior>
  <action>新增 `src/components/classroom/classroom-timeline-panel.tsx`，为教师运行台提供专门的 `干预记录时间线` panel。组件必须直接消费 Task 1 产出的 typed read model，而不是自己解析 raw payload。每条 entry 固定展示 `title`、`body`、`targetScope`、`createdAt`，必要时补 `studentName` / `stepTitle` 辅助标签；正文容器需要明确支持多行换行和长文本，不要继续复用按钮、metric orb 或 roster cell 这种不适合富文本记录的结构。同步修改 `src/components/classroom/classroom-control-panel.tsx` 与 `src/components/surfaces/classroom-console-surface.tsx`，在保持“单一渐变主舞台，其余模块都是 tonal cards”的既有壳层决策下，把 timeline panel 放到教师运行台稳定的独立版块中（优先右侧次级栏 stack，若空间不足可改成主列下方独立 section），不要新增第二个 hero，也不要把 timeline 挤进现有控制按钮卡片。新增 `src/components/classroom/classroom-timeline-panel.test.tsx` 覆盖 empty state、entry field rendering、长正文与目标范围标签的布局合同。</action>
  <verify>
    <automated>pnpm test --run src/components/classroom/classroom-timeline-panel.test.tsx</automated>
  </verify>
  <done>教师在 `/classroom` 能以独立、稳定、可读的 panel 查看 intervention timeline；界面不再因缺少容器合同而显得混乱。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DAL -> teacher runtime DTO | session-owned timeline facts 被映射成教师 UI 可直接消费的 read model |
| teacher-only timeline -> classroom surface | 过程记录正文只能进入教师运行台，不能泄漏到学生或公共 snapshot consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-13 | I | `src/lib/dal/classroom.ts` | mitigate | timeline read path 按 `sessionId` 与 teacher scope 过滤，非教师 consumer 返回空 timeline 或不含正文字段。 |
| T-21-14 | T | `src/components/classroom/classroom-timeline-panel.tsx` | mitigate | 只消费 typed read model 字段，不在组件里信任任意 raw payload keys 或未验证 HTML。 |
| T-21-15 | R | `src/lib/dal/classroom.test.ts` | mitigate | 用行为测试证明 intervention timeline 可读模型来自 durable classroomTimeline，而非临时 client state。 |
| T-21-16 | D | `src/components/classroom/classroom-control-panel.tsx` | accept | 长正文可能拉高卡片高度，但 panel 采用独立 scroll/stack 容器即可控制影响，不需要牺牲正文可读性。 |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/classroom.test.ts`
- `pnpm test --run src/components/classroom/classroom-timeline-panel.test.tsx`
</verification>

<success_criteria>
- teacher intervention 不再只有写链路，教师 runtime 也有稳定 read model。
- `/classroom` 有独立的 intervention timeline panel，能清晰展示 title/body/targetScope/createdAt。
- teacher-only 边界仍成立，timeline 正文不会泄漏到学生侧。
- classroom runtime 继续保持单一渐变主舞台，其余新增 panel 使用 tonal cards。
</success_criteria>

<output>
After completion, create `.planning/phases/21-teaching-design-contracts-and-evidence-foundation/21-04-SUMMARY.md`
</output>
