---
phase: 15-batch-course-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/lib/dto/course-import.ts
  - src/lib/dal/course-import.ts
  - src/lib/dal/course-import.test.ts
  - src/actions/course-import-actions.ts
  - src/actions/course-import-actions.test.ts
  - src/app/(teacher)/teacher/courses/import/template/route.ts
  - src/app/(teacher)/teacher/courses/import/template/route.test.ts
autonomous: true
requirements:
  - COURSE-08
  - COURSE-09
must_haves:
  truths:
    - D-01/D-02/D-03/D-04: 首发导入固定为 CSV，模板字段固定为标题、学科、年级、课程状态，且不包含班级关联列。
    - D-05/D-06/D-08: 预览阶段必须做同校范围匹配与同批次重复检测，不能依赖数据库唯一约束，也不能让批内撞车靠顺序隐式覆盖。
    - D-13/D-14/D-21/D-22: 上传后只生成课程导入草稿批次与行级状态，所有逻辑仍走 teacher-owned、school-scoped 的 DAL + Server Actions 边界。
  artifacts:
    - path: src/lib/dto/course-import.ts
      provides: batch import input, row classification, review DTOs, and result enums
      contains: CourseImportDraftInputSchema
    - path: src/lib/dal/course-import.ts
      provides: persisted draft-import pipeline and preview classification
      contains: draftCourseImport
    - path: src/app/(teacher)/teacher/courses/import/template/route.ts
      provides: downloadable CSV template route for the fixed import contract
      contains: text/csv
  key_links:
    - from: src/actions/course-import-actions.ts
      to: src/lib/dal/course-import.ts
      via: validated draft import action
      pattern: draftCourseImportAction|draftCourseImport
    - from: src/lib/dal/course-import.ts
      to: src/db/schema.ts
      via: persisted batch plus row staging tables
      pattern: courseImportBatch|courseImportRow
---

<objective>
建立 Phase 15 的批量导入基础 contract：固定 CSV 模板、行级校验 DTO、持久化草稿批次与预览分类管线。

Purpose: 先把“上传后先审核、再应用”的数据真相源搭起来，避免后续 preview/apply 继续依赖 modal 本地状态或临时对象。
Output: course import schema、DTO、draft action、template route、duplicate-aware preview classifier。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-batch-course-import/15-CONTEXT.md
@.planning/phases/15-batch-course-import/15-RESEARCH.md
@src/db/schema.ts
@src/lib/dto/course-authoring.ts
@src/lib/dal/course-authoring.ts
@src/actions/course-authoring-actions.ts
@src/features/schedule/import/server.ts
@src/features/schedule/import/actions.ts
@src/features/schedule/import/template.ts

<interfaces>
New import contracts introduced in this plan:
```ts
type CourseImportDraftInput = {
  sourceLabel: string;
  rows: Array<{
    title: string;
    subject: string;
    grade: string;
    status: "draft" | "published" | "archived";
  }>;
}

type CourseImportBatchStatus = "draft" | "in_review" | "ready_to_apply" | "applied" | "partially_applied";
type CourseImportRowStatus = "ready_to_create" | "matched_existing" | "same_file_conflict" | "invalid" | "blocked";
```

Template route introduced in this plan:
```ts
GET /teacher/courses/import/template
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add course import staging schema, DTOs, and draft classification pipeline</name>
  <files>src/db/schema.ts, src/lib/dto/course-import.ts, src/lib/dal/course-import.ts, src/lib/dal/course-import.test.ts</files>
  <read_first>src/db/schema.ts; src/lib/dto/course-authoring.ts; src/lib/dal/course-authoring.ts; src/lib/dal/course-authoring.test.ts; src/features/schedule/import/server.ts; .planning/phases/15-batch-course-import/15-CONTEXT.md</read_first>
  <behavior>
    - Test 1: persisted draft import creates one batch plus row records instead of writing directly into `courses`.
    - Test 2: row classifier distinguishes same-file duplicate keys from same-school existing-course matches.
    - Test 3: matching uses `title + subject + grade` within school scope and foreign-owned matches become explicit blocking rows rather than silent creates or unauthorized updates.
  </behavior>
  <action>Create `courseImportBatch` and `courseImportRow` tables in `src/db/schema.ts` with `schoolId`, `actorId`, batch status, row status, decision, result, validation JSON, and `onDelete: cascade` ownership. Then add `src/lib/dto/course-import.ts` with strict Zod schemas for the fixed CSV row (`title`, `subject`, `grade`, `status`), draft input, row review state, apply decisions (`update` or `skip`), and result enums (`created`, `updated`, `skipped`, `failed`). Implement `draftCourseImport()` in `src/lib/dal/course-import.ts` so it: validates rows; normalizes the exact match key `title + subject + grade`; detects duplicate keys inside the same CSV; queries same-school courses for existing matches; classifies each row as `ready_to_create`, `matched_existing`, `same_file_conflict`, `invalid`, or `blocked`; and persists batch plus row truth without mutating `courses`. Add DAL tests in `src/lib/dal/course-import.test.ts` covering same-file conflict detection, same-school existing match detection, and foreign-owned match blocking.</action>
  <acceptance_criteria>
    - `src/db/schema.ts` contains `courseImportBatch` and `courseImportRow`.
    - `src/lib/dto/course-import.ts` contains `CourseImportDraftInputSchema` and `CourseImportRowStatus`.
    - `src/lib/dal/course-import.ts` contains `draftCourseImport` and logic referencing `title`, `subject`, and `grade` as the match key.
    - `src/lib/dal/course-import.test.ts` contains assertions for same-file duplicate and same-school match classification.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/course-import.test.ts</automated>
  </verify>
  <done>Phase 15 has a persisted import truth source and deterministic preview classifier before any teacher-facing review page is added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the CSV template route and draft import server action boundary</name>
  <files>src/actions/course-import-actions.ts, src/actions/course-import-actions.test.ts, src/app/(teacher)/teacher/courses/import/template/route.ts, src/app/(teacher)/teacher/courses/import/template/route.test.ts</files>
  <read_first>src/actions/course-authoring-actions.ts; src/actions/course-authoring-actions.test.ts; src/features/schedule/import/actions.ts; src/features/schedule/import/template.ts; src/lib/cache-policy.ts; src/lib/dto/course-import.ts</read_first>
  <behavior>
    - Test 1: template route returns a CSV file with the exact fixed headers and sample rows for the Phase 15 contract.
    - Test 2: draft import action parses `FormData | Record<string, unknown>`, validates with Zod, and returns structured validation errors instead of throwing raw exceptions.
    - Test 3: successful draft import invalidates course-import and teacher-course cache tags for read-your-writes navigation into the review page.
  </behavior>
  <action>Create `src/app/(teacher)/teacher/courses/import/template/route.ts` to return a downloadable CSV with the exact Chinese headers `标题,学科,年级,课程状态` and at least one sample row. Add `src/actions/course-import-actions.ts` with `draftCourseImportAction(input)` that normalizes `FormData`, parses JSON rows, validates through `CourseImportDraftInputSchema`, calls `assertActiveTeacher()`, then delegates to `draftCourseImport()` and uses `updateTag()` for the teacher's course-center tag plus the new import-batch tag family. Add `src/actions/course-import-actions.test.ts` covering validation failure, unauthorized handling, and successful draft action wiring. Keep the action contract parallel to existing course-authoring actions; do not import `db` directly into the action layer.</action>
  <acceptance_criteria>
    - `src/app/(teacher)/teacher/courses/import/template/route.ts` contains `标题,学科,年级,课程状态`.
    - `src/actions/course-import-actions.ts` contains `draftCourseImportAction` and `updateTag(`.
    - `src/actions/course-import-actions.ts` contains `assertActiveTeacher`.
    - `src/actions/course-import-actions.test.ts` covers validation failure and successful draft action behavior.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/actions/course-import-actions.test.ts src/app/(teacher)/teacher/courses/import/template/route.test.ts</automated>
  </verify>
  <done>Teachers can download the fixed CSV template and submit import drafts through a server-owned boundary that already preserves Phase 15's cache and auth rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser CSV rows -> draft import action | Untrusted file data crosses into server-owned validation and classification logic |
| draft import DAL -> staged batch tables | Persisted preview truth becomes the source for later review/apply decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | T | `src/lib/dal/course-import.ts` | mitigate | Recompute the exact match key on the server and classify same-file duplicates before any row can be marked ready. |
| T-15-02 | E | `src/actions/course-import-actions.ts` | mitigate | Keep draft import behind `assertActiveTeacher()` and school-scoped DAL queries; no direct DB writes from UI or action code. |
| T-15-03 | R | `src/db/schema.ts` | mitigate | Persist batch and row truth with timestamps and actor IDs so review/apply decisions remain auditable. |
| T-15-04 | D | `src/app/(teacher)/teacher/courses/import/template/route.ts` | accept | Template download is read-only and exposes only fixed headers plus sample rows. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/course-import.test.ts`
- `pnpm test --run src/actions/course-import-actions.test.ts src/app/(teacher)/teacher/courses/import/template/route.test.ts`
</verification>

<success_criteria>
- Phase 15 has persisted batch plus row staging instead of modal-only preview state.
- The fixed CSV contract and server-owned validation path are executable.
- Same-file duplicates and same-school matches are already classified before review UI work starts.
</success_criteria>

<output>
After completion, create `.planning/phases/15-batch-course-import/15-01-SUMMARY.md`
</output>
