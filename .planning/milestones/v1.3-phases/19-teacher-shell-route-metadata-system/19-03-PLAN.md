---
phase: 19-teacher-shell-route-metadata-system
plan: 03
type: execute
wave: 3
depends_on:
  - 19-01
  - 19-02
files_modified:
  - src/components/shell/teacher-sidebar-shell.test.tsx
  - src/lib/theme-layout/shell-surface-resolver.test.ts
  - scripts/verify-phase19-shell-route-metadata.ts
  - package.json
autonomous: true
requirements:
  - Extension phase — teacher shell architecture hardening and future layout-variant expansion.
must_haves:
  truths:
    - D-09/D-12: regression coverage proves `/teacher` still renders the current square/full-width shell after the metadata migration
    - D-11/D-12: route metadata resolution, theme coupling, and sidebar/top-nav coupling are locked by automated checks
    - D-10: the phase ships with a dedicated verification command that can be rerun before future shell expansions
  artifacts:
    - path: src/lib/theme-layout/shell-surface-resolver.test.ts
      provides: resolver contract tests for current and future-safe shell variants
      contains: immersive
    - path: src/components/shell/teacher-sidebar-shell.test.tsx
      provides: shell source and invariant regression checks
      contains: full-width
    - path: scripts/verify-phase19-shell-route-metadata.ts
      provides: dedicated phase verification guard
      contains: verify:phase19
  key_links:
    - from: src/lib/theme-layout/shell-surface-resolver.test.ts
      to: src/lib/theme-layout/shell-surface-resolver.ts
      via: tests assert typed resolver output for current and future-safe variants
      pattern: getShellSurfaceConfig
    - from: package.json
      to: scripts/verify-phase19-shell-route-metadata.ts
      via: dedicated phase verification command
      pattern: verify:phase19
---

<objective>
Lock the new shell metadata architecture with targeted regression coverage and a dedicated verification command.

Purpose: Prevent future route additions or shell variants from reintroducing route-string branching, visual drift, or theme/shell coupling regressions.
Output: Resolver tests, shell regression tests, and a reusable `verify:phase19` script.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-teacher-shell-route-metadata-system/19-CONTEXT.md
@.planning/phases/19-teacher-shell-route-metadata-system/19-RESEARCH.md
@.planning/phases/19-teacher-shell-route-metadata-system/19-UI-SPEC.md
@.planning/phases/19-teacher-shell-route-metadata-system/19-01-PLAN.md
@.planning/phases/19-teacher-shell-route-metadata-system/19-02-PLAN.md
@scripts/verify-phase16-theme-layout.ts
@src/components/shell/teacher-sidebar-shell.test.tsx

<interfaces>
From src/lib/theme-layout/shell-surface-resolver.ts (created by 19-01):
```ts
export function getShellSurfaceConfig(input: { routeKey: TeacherThemeRouteKey; layoutRuntime: ThemeLayoutRuntime }): {
  shellVariant: string;
  shellConfig: {
    mode: ThemeShellMode;
    radius: "rounded" | "square";
    width: "default" | "full-width";
    chrome: "default" | "immersive" | "minimal" | "presentation" | "fullscreen" | "focus";
  };
  surfaceMetadata: { routeKey: TeacherThemeRouteKey; label: string; summary: ThemeLayoutSummary };
};
```

Verification pattern to reuse from scripts/verify-phase16-theme-layout.ts:
```ts
type StaticCheck = { label: string; passed: boolean };
runPnpm(["test", "--run", ...], "phase regression suite");
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add resolver and shell regression tests for current and future-safe variants</name>
  <files>src/components/shell/teacher-sidebar-shell.test.tsx, src/lib/theme-layout/shell-surface-resolver.test.ts</files>
  <read_first>src/components/shell/teacher-sidebar-shell.test.tsx, src/lib/theme-layout/shell-surface-resolver.ts, .planning/phases/19-teacher-shell-route-metadata-system/19-UI-SPEC.md, .planning/quick/260511-bbe-teacher-main-main-div-100/260511-bbe-SUMMARY.md, .planning/quick/260511-bn1-teacher-data-region-page-header-page-hea/260511-bn1-SUMMARY.md</read_first>
  <behavior>
    - Test 1: `/teacher` resolves to `square`, `full-width`, and `immersive` without any route-string render checks in the shell source.
    - Test 2: `/settings` and `/resources` continue to resolve through the shared teacher shell path.
    - Test 3: future-safe enums such as `presentation`, `focus`, `fullscreen`, and `minimal` produce structured resolver output but do not require visible UI expansion in this phase.
  </behavior>
  <action>Replace the current source guard in `src/components/shell/teacher-sidebar-shell.test.tsx` per D-09, D-11, and D-12 so it asserts the absence of `routeKey === "/teacher"`, the presence of resolver-driven `shellConfig` usage, and the preservation of `/teacher` square/full-width invariants. Add `src/lib/theme-layout/shell-surface-resolver.test.ts` covering `getShellSurfaceConfig()` and `resolveShellVariant()` for `/teacher`, `/settings`, and at least one future-safe metadata combination using `presentation`, `focus`, `fullscreen`, or `minimal` chrome. Keep the tests focused on compile/resolve/render boundaries; do not add snapshot-only assertions or manual visual checklists.</action>
  <acceptance_criteria>
    - `src/components/shell/teacher-sidebar-shell.test.tsx` does not assert `routeKey === "/teacher"` and does assert resolver-driven shell config usage.
    - `src/lib/theme-layout/shell-surface-resolver.test.ts` contains `presentation`, `focus`, `fullscreen`, or `minimal` in structured resolver assertions.
    - Both test files contain `getShellSurfaceConfig` or `resolveShellVariant` references.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/theme-layout/shell-surface-resolver.test.ts src/components/shell/teacher-sidebar-shell.test.tsx</automated>
  </verify>
  <done>The metadata contract is guarded by focused tests that lock current behavior and future-safe shell variant expansion boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Add a dedicated Phase 19 verification command for metadata-driven shell safety</name>
  <files>scripts/verify-phase19-shell-route-metadata.ts, package.json</files>
  <read_first>scripts/verify-phase16-theme-layout.ts, package.json, src/components/shell/teacher-sidebar-shell.tsx, src/lib/theme-layout/route-surface-registry.ts, src/lib/theme-layout/shell-surface-resolver.ts</read_first>
  <action>Create `scripts/verify-phase19-shell-route-metadata.ts` and register `verify:phase19` in `package.json` per D-10, D-11, and D-12. The script must statically verify: (1) `teacher-sidebar-shell.tsx` does not contain `routeKey === "/teacher"` or `pathname.startsWith(` business route checks, (2) `route-surface-registry.ts` defines shell metadata for `/teacher`, `/settings`, and `/resources`, (3) `shell-surface-resolver.ts` exports `resolveShellVariant` and `getShellSurfaceConfig`, (4) the shell source still contains square/full-width handling through `shellConfig`, and (5) the targeted Vitest suite for the resolver and shell source passes. Reuse the `StaticCheck + runPnpm()` structure from `scripts/verify-phase16-theme-layout.ts`; do not build a manual checklist or a grep gate that can self-invalidate on comment text.</action>
  <acceptance_criteria>
    - `package.json` contains `verify:phase19`.
    - `scripts/verify-phase19-shell-route-metadata.ts` contains checks for `routeKey === "/teacher"`, `pathname.startsWith(`, `getShellSurfaceConfig`, and `/teacher` route metadata.
    - `scripts/verify-phase19-shell-route-metadata.ts` runs the resolver + shell Vitest files.
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase19</automated>
  </verify>
  <done>Phase 19 has a dedicated, rerunnable verification command that catches route-string regressions, metadata omissions, and shell/resolver coupling issues before future expansion work lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolver contract -> regression suite | Tests and verify script determine whether future shell variants remain safe |
| repository sources -> verify script | Static checks gate shipping by scanning route metadata and shell implementation invariants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-09 | R | `src/components/shell/teacher-sidebar-shell.test.tsx` | mitigate | Replace stale source guards with assertions for resolver-driven config and absence of route-string business logic. |
| T-19-10 | T | `scripts/verify-phase19-shell-route-metadata.ts` | mitigate | Fail verification when route-string checks or missing route metadata entries reappear in the shell path. |
| T-19-11 | D | `src/lib/theme-layout/shell-surface-resolver.test.ts` | mitigate | Cover future-safe chrome enums structurally so later additions cannot bypass the typed resolver contract. |
| T-19-12 | I | `scripts/verify-phase19-shell-route-metadata.ts` | accept | The verify script reads source code only and does not expose user or school data. |
</threat_model>

<verification>
- `pnpm test --run src/lib/theme-layout/shell-surface-resolver.test.ts src/components/shell/teacher-sidebar-shell.test.tsx`
- `pnpm verify:phase19`
</verification>

<success_criteria>
- Route metadata resolution and shell rendering are protected by targeted tests.
- A dedicated `verify:phase19` command catches route-string regressions and metadata drift.
- Future shell variant expansion can extend enums and resolver logic without touching UI route branching.
</success_criteria>

<output>
After completion, create `.planning/phases/19-teacher-shell-route-metadata-system/19-03-SUMMARY.md`
</output>
