---
phase: 20-help-center-and-developer-guides
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/theme-layout/route-surface-registry.ts
  - src/app/help/layout.tsx
  - src/app/help/page.tsx
  - src/app/help/plugins/page.tsx
  - src/app/help/themes/page.tsx
  - src/app/help/actions-interfaces/page.tsx
  - src/components/surfaces/help-center-overview-surface.tsx
  - src/components/surfaces/help-guide-detail-surface.tsx
  - src/lib/help/help-center-content.ts
autonomous: true
requirements:
  - Extension phase — product help center and developer-facing implementation guidance.
must_haves:
  truths:
    - D-01/D-02/D-03/D-04/D-05: `/help` 是正式产品页，首页先做教师/开发者分流；教师帮助保留为简洁、实用、面向产品使用的说明层，开发者指南仍留在同一帮助体系下并拆出 3 个详细子页
    - D-06/D-19/D-20: `/help` 与所有子页继续走 teacher shell 和现有 tonal visual language，而不是 docs-only layout
    - D-13/D-14: 本期只保留侧边栏 `/help` 单入口，不新增其他产品入口
    - D-21: 先完成 `/help` 接入 teacher shell 与 route metadata 的方案，再进入详细内容实现
  artifacts:
    - path: src/lib/theme-layout/route-surface-registry.ts
      provides: help route metadata and shell allowlist
      contains: /help
    - path: src/app/help/layout.tsx
      provides: shell-compatible help route wrapper
      contains: TeacherSidebarShell
    - path: src/components/surfaces/help-center-overview-surface.tsx
      provides: `/help` overview hero, audience split, and teacher/developer entry cards
      contains: 我是教师
    - path: src/components/surfaces/help-guide-detail-surface.tsx
      provides: shared developer detail-page template with return row and aside summary
      contains: 返回帮助中心
  key_links:
    - from: src/app/help/layout.tsx
      to: src/lib/theme-layout/route-surface-registry.ts
      via: `/help` routes resolve through allowlisted teacher shell metadata instead of shell special-casing
      pattern: resolveTeacherThemeRouteSurface
    - from: src/app/help/page.tsx
      to: src/components/surfaces/help-center-overview-surface.tsx
      via: overview route renders the product-native help home surface
      pattern: HelpCenterOverviewSurface
    - from: src/app/help/plugins/page.tsx
      to: src/components/surfaces/help-guide-detail-surface.tsx
      via: developer subpages share one detail-page template
      pattern: HelpGuideDetailSurface
---

<objective>
先把 `/help` 接成 teacher-shell-compatible 的正式产品路线，并落下首页 + 三个子页的
信息架构骨架与共享 surface 模板。

Purpose: 解决当前“侧边栏已有 `/help` 入口但没有真实页面”的接入缺口，同时把帮助中心
定型为产品内双层 IA，而不是后续临时拼接的 docs 页面。
Output: route metadata allowlist、`/help` layout、overview surface、detail template、以及
首页与 3 个子页的最小结构化页面骨架。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-help-center-and-developer-guides/20-CONTEXT.md
@.planning/phases/20-help-center-and-developer-guides/20-RESEARCH.md
@.planning/phases/20-help-center-and-developer-guides/20-UI-SPEC.md
@.planning/phases/19-teacher-shell-route-metadata-system/19-CONTEXT.md
@src/lib/theme-layout/route-surface-registry.ts
@src/app/(teacher)/teacher/layout.tsx
@src/app/settings/layout.tsx
@src/components/shell/teacher-sidebar-shell.tsx
@src/components/shell/sidebar.tsx
@src/components/surfaces/settings-surface.tsx
@src/components/surfaces/plugin-marketplace-surface.tsx

<interfaces>
From src/lib/theme-layout/route-surface-registry.ts:
```ts
export const TEACHER_THEME_ROUTE_KEYS = [
  "/teacher",
  "/settings",
  "/resources",
] as const;

export function resolveTeacherThemeRouteSurface(pathname: string | null | undefined): TeacherThemeRouteKey;
```

Route identities to add for this phase:
```ts
"/help"
"/help/plugins"
"/help/themes"
"/help/actions-interfaces"
```

Shell integration target:
```tsx
<TeacherSidebarShell routeKey="/help" activePath="/help" headerTitle="帮助中心">
  {children}
</TeacherSidebarShell>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend route metadata and create the shell-compatible `/help` route family</name>
  <files>src/lib/theme-layout/route-surface-registry.ts, src/app/help/layout.tsx, src/app/help/page.tsx, src/app/help/plugins/page.tsx, src/app/help/themes/page.tsx, src/app/help/actions-interfaces/page.tsx</files>
  <read_first>src/lib/theme-layout/route-surface-registry.ts, src/app/(teacher)/teacher/layout.tsx, src/app/settings/layout.tsx, src/components/shell/sidebar.tsx, .planning/phases/20-help-center-and-developer-guides/20-CONTEXT.md, .planning/phases/20-help-center-and-developer-guides/20-UI-SPEC.md, .planning/phases/19-teacher-shell-route-metadata-system/19-01-PLAN.md, .planning/phases/19-teacher-shell-route-metadata-system/19-02-PLAN.md</read_first>
  <action>Update `src/lib/theme-layout/route-surface-registry.ts` per D-04, D-05, D-06, D-19, D-21 and the UI-SPEC route architecture contract so `TEACHER_THEME_ROUTE_KEYS` and `TEACHER_THEME_ROUTE_SURFACES` explicitly include `/help`, `/help/plugins`, `/help/themes`, and `/help/actions-interfaces`. Give all four routes teacher-facing labels, rounded/default shell defaults, and allowlisted module identities that fit the help center surface. Extend `resolveTeacherThemeRouteSurface()` to resolve these paths without adding shell-specific hacks in `TeacherSidebarShell`. Then create `src/app/help/layout.tsx` that wraps the entire route family in `TeacherSidebarShell`, keeps the route key source of truth at the layout boundary, and preserves `/help` sidebar highlighting across child pages through `activePath`. Create page files for `/help`, `/help/plugins`, `/help/themes`, and `/help/actions-interfaces` that render stubbed-but-real product pages through the shared help surfaces rather than placeholder text, and do not introduce a separate markdown/docs layout, top-level marketing nav, or external redirect.</action>
  <acceptance_criteria>
    - `src/lib/theme-layout/route-surface-registry.ts` contains `"/help"`, `"/help/plugins"`, `"/help/themes"`, and `"/help/actions-interfaces"`.
    - `src/app/help/layout.tsx` contains `TeacherSidebarShell` and a `/help` route key.
    - `src/app/help/page.tsx`, `src/app/help/plugins/page.tsx`, `src/app/help/themes/page.tsx`, and `src/app/help/actions-interfaces/page.tsx` all exist.
    - No new file under `src/app/help/**` imports a docs-only layout component or redirects to `docs/` or GitHub URLs.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const reg=fs.readFileSync('src/lib/theme-layout/route-surface-registry.ts','utf8');const layout=fs.readFileSync('src/app/help/layout.tsx','utf8');const pages=['src/app/help/page.tsx','src/app/help/plugins/page.tsx','src/app/help/themes/page.tsx','src/app/help/actions-interfaces/page.tsx'];if(!reg.includes('\"/help\"')||!reg.includes('\"/help/plugins\"')||!reg.includes('\"/help/themes\"')||!reg.includes('\"/help/actions-interfaces\"')||!layout.includes('TeacherSidebarShell')||pages.some((path)=>!fs.existsSync(path)))process.exit(1)"</automated>
  </verify>
  <done>`/help` 从一个空入口变成了 teacher-shell 内的正式路线家族，且与 Phase 19 route metadata 体系兼容。</done>
</task>

<task type="auto">
  <name>Task 2: Build the overview IA and the shared developer detail-page template</name>
  <files>src/components/surfaces/help-center-overview-surface.tsx, src/components/surfaces/help-guide-detail-surface.tsx, src/lib/help/help-center-content.ts, src/app/help/page.tsx, src/app/help/plugins/page.tsx, src/app/help/themes/page.tsx, src/app/help/actions-interfaces/page.tsx</files>
  <read_first>src/components/surfaces/settings-surface.tsx, src/components/surfaces/plugin-marketplace-surface.tsx, .planning/phases/20-help-center-and-developer-guides/20-UI-SPEC.md, .planning/phases/20-help-center-and-developer-guides/20-RESEARCH.md</read_first>
  <action>Create `src/components/surfaces/help-center-overview-surface.tsx` and `src/components/surfaces/help-guide-detail-surface.tsx` per D-02, D-03, D-09, D-10, D-19, D-20 and the UI-SPEC overview/detail template contracts. The overview surface must render: (1) page header and hero using the copy contract for `帮助中心`, (2) a visible teacher/developer split with `我是教师` and `我是开发者`, (3) 3 to 4 lightweight teacher-help modules with no code blocks, (4) three developer guide entry cards for 插件开发 / 主题开发 / Actions / Interfaces, and (5) a trust note that explicitly differentiates `当前可用`, `使用边界`, and `后续扩展`. The detail template must render: a return row back to `/help`, intro card, main narrative sections, optional code-example blocks for developer pages only, and a right-side aside with `本页覆盖` plus boundary notes. Add `src/lib/help/help-center-content.ts` as the centralized source for section titles, summaries, badges, and route card metadata so the four routes reuse one structured content map instead of copying labels inline. Keep the UI in tonal product surfaces, not markdown article styling or 1px-divider docs chrome.</action>
  <acceptance_criteria>
    - `src/components/surfaces/help-center-overview-surface.tsx` contains `我是教师`, `我是开发者`, `当前可用`, `使用边界`, and `后续扩展`.
    - `src/components/surfaces/help-guide-detail-surface.tsx` contains `返回帮助中心` and `本页覆盖`.
    - `src/lib/help/help-center-content.ts` contains entries for `/help/plugins`, `/help/themes`, and `/help/actions-interfaces`.
    - `src/app/help/page.tsx` renders the overview surface, and the three detail pages render the shared detail template.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const overview=fs.readFileSync('src/components/surfaces/help-center-overview-surface.tsx','utf8');const detail=fs.readFileSync('src/components/surfaces/help-guide-detail-surface.tsx','utf8');const content=fs.readFileSync('src/lib/help/help-center-content.ts','utf8');if(!overview.includes('我是教师')||!overview.includes('我是开发者')||!overview.includes('当前可用')||!overview.includes('使用边界')||!overview.includes('后续扩展')||!detail.includes('返回帮助中心')||!detail.includes('本页覆盖')||!content.includes('/help/plugins')||!content.includes('/help/themes')||!content.includes('/help/actions-interfaces'))process.exit(1)"</automated>
  </verify>
  <done>帮助中心的双层 IA 与共享页面模板已经落地，后续内容实现只需往既定产品结构中填充真实指南内容。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route metadata -> help route family | allowlisted route identities decide whether `/help` renders inside the teacher shell |
| centralized help content map -> help surfaces | structured content definitions become visible product guidance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | T | `src/lib/theme-layout/route-surface-registry.ts` | mitigate | Add `/help` routes to the allowlisted metadata registry so shell integration stays declarative and future-safe. |
| T-20-02 | D | `src/app/help/layout.tsx` | mitigate | Force the entire `/help` route family through `TeacherSidebarShell` instead of allowing an isolated root page or external redirect. |
| T-20-03 | S | `src/components/surfaces/help-center-overview-surface.tsx` | mitigate | Keep teacher and developer content visibly separated on the overview page so users do not misread technical docs as teacher-facing instructions. |
| T-20-04 | R | `src/lib/help/help-center-content.ts` | accept | The centralized content map is product copy only and does not cross a privileged runtime boundary. |
</threat_model>

<verification>
- `pnpm typecheck`
- `node -e "const fs=require('fs');const reg=fs.readFileSync('src/lib/theme-layout/route-surface-registry.ts','utf8');if(!reg.includes('/help/actions-interfaces'))process.exit(1)"`
</verification>

<success_criteria>
- `/help` 和所有 `/help/*` 页面都通过 teacher shell 渲染。
- 首页首屏先完成教师 / 开发者分流，而不是直接进入长文。
- 三个开发者详细页和共享 detail template 已就位，后续内容可以在稳定结构上继续填充。
</success_criteria>

<output>
After completion, create `.planning/phases/20-help-center-and-developer-guides/20-01-SUMMARY.md`
</output>
