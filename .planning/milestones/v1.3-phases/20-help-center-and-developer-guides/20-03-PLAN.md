---
phase: 20-help-center-and-developer-guides
plan: 03
type: execute
wave: 3
depends_on:
  - 20-01
  - 20-02
files_modified:
  - src/lib/help/help-center-content.ts
  - src/app/help/actions-interfaces/page.tsx
  - src/app/help/page.tsx
  - src/components/surfaces/help-center-overview-surface.tsx
  - src/components/surfaces/help-center-overview-surface.test.tsx
  - scripts/verify-phase20-help-center.ts
  - package.json
autonomous: true
requirements:
  - Extension phase — product help center and developer-facing implementation guidance.
must_haves:
  truths:
    - D-09/D-15/D-18: actions/interfaces 文档只覆盖当前已开放的插件、主题与 schedule 扩展边界，且 schedule 必须明确是 proposal-only
    - D-10: 教师帮助区只留在 `/help` 首页，不放代码
    - D-13/D-19/D-20: 产品入口维持侧边栏 `/help` 单入口，并继续使用 teacher-facing visual language
    - Phase goal / success criteria 4: 帮助中心必须带回归或验证护栏，避免后续代码变化把页面内容悄悄写旧
  artifacts:
    - path: src/app/help/actions-interfaces/page.tsx
      provides: schedule proposal-only extensions guide and current actions/interfaces documentation
      contains: proposal-only
    - path: src/app/help/page.tsx
      provides: finalized overview page with lightweight teacher help and developer entry cards
      contains: 帮助中心
    - path: scripts/verify-phase20-help-center.ts
      provides: dedicated phase verifier for help-route existence, shell metadata, teacher/dev content boundaries, and schedule proposal-only wording
      contains: verify:phase20
  key_links:
    - from: src/app/help/actions-interfaces/page.tsx
      to: src/server/plugins/registry.ts
      via: actions/interfaces guide is grounded in the current schedule assistant hook and allowlisted proposal actions
      pattern: schedule.assistant|createScheduleOverrideProposal|createScheduleReminderDraft
    - from: package.json
      to: scripts/verify-phase20-help-center.ts
      via: phase-specific verification command keeps help-center routing and content boundaries from drifting
      pattern: verify:phase20
---

<objective>
完成 `/help/actions-interfaces`、收口首页教师帮助区，并为整个帮助中心建立专用验证护栏，防止后续 drift。

Purpose: 让 Phase 20 不只是“有页面”，而是有可复跑的真实性校验；同时把 schedule 扩展边界讲清楚。
Output: actions/interfaces 指南、最终 overview teacher-help 区、overview 回归测试、`verify:phase20` 命令。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-help-center-and-developer-guides/20-CONTEXT.md
@.planning/phases/20-help-center-and-developer-guides/20-RESEARCH.md
@.planning/phases/20-help-center-and-developer-guides/20-UI-SPEC.md
@.planning/phases/20-help-center-and-developer-guides/20-01-PLAN.md
@.planning/phases/20-help-center-and-developer-guides/20-02-PLAN.md
@src/lib/dto/resource-ai.ts
@src/server/plugins/registry.ts
@src/lib/dal/plugins.ts
@src/actions/plugin-actions.ts
@src/components/shell/sidebar.tsx
@src/components/surfaces/help-center-overview-surface.tsx
@src/components/surfaces/plugin-marketplace-surface.tsx
@scripts/verify-phase19-shell-route-metadata.ts
@package.json

<interfaces>
Current schedule extension surface to document:
```ts
PluginHookAnchorSchema = z.enum(["dashboard.widget", "lesson.sidebar", "schedule.assistant"])

PluginActionSchema = z.enum([
  "createScheduleOverrideProposal",
  "createScheduleReminderDraft",
  "annotateScheduleConflict",
])
```

Verification command pattern to reuse:
```ts
type StaticCheck = { label: string; passed: boolean }
runPnpm(["test", "--run", ...], "phase regression suite")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finish the actions/interfaces guide and the lightweight teacher-help overview content</name>
  <files>src/lib/help/help-center-content.ts, src/app/help/actions-interfaces/page.tsx, src/app/help/page.tsx, src/components/surfaces/help-center-overview-surface.tsx</files>
  <read_first>src/lib/help/help-center-content.ts, src/app/help/actions-interfaces/page.tsx, src/app/help/page.tsx, src/lib/dto/resource-ai.ts, src/server/plugins/registry.ts, src/actions/plugin-actions.ts, src/components/shell/sidebar.tsx, .planning/phases/20-help-center-and-developer-guides/20-UI-SPEC.md</read_first>
  <action>Populate the `/help/actions-interfaces` guide and finalize the overview teacher-help content per D-09, D-10, D-15, D-18, D-19, and the UI-SPEC teacher/developer rules. In `src/lib/help/help-center-content.ts`, add an actions/interfaces guide that explicitly states why this page only covers plugin/theme/schedule extension boundaries, lists the current `schedule.assistant` hook and the current schedule proposal actions (`createScheduleOverrideProposal`, `createScheduleReminderDraft`, `annotateScheduleConflict`), explains proposal/result semantics, includes a minimum payload/result example, and separates `当前可用`, `使用边界`, and `后续扩展`. The `使用边界` section must explicitly say `proposal-only`, no direct runtime schedule write, and no all-system DAL/Server Action API coverage. Then finalize `/help` homepage teacher-help modules so they explain where teachers go for settings, plugin marketplace, themes, and schedule pages, while keeping the teacher area code-free and technical-jargon-light. Do not move teacher help into a third detail page and do not add code blocks to the teacher overview area.</action>
  <acceptance_criteria>
    - `src/lib/help/help-center-content.ts` contains `schedule.assistant`, `createScheduleOverrideProposal`, `createScheduleReminderDraft`, `annotateScheduleConflict`, and `proposal-only` in the actions/interfaces guide section.
    - `src/app/help/actions-interfaces/page.tsx` renders the actions/interfaces guide through the shared detail template.
    - `src/components/surfaces/help-center-overview-surface.tsx` does not contain fenced code blocks and does contain teacher-facing references such as `/settings`, `/settings/plugins`, or `/teacher/schedule`.
    - The overview page still contains the teacher/developer split and does not introduce a separate teacher detail route.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const content=fs.readFileSync('src/lib/help/help-center-content.ts','utf8');const overview=fs.readFileSync('src/components/surfaces/help-center-overview-surface.tsx','utf8');if(!content.includes('schedule.assistant')||!content.includes('createScheduleOverrideProposal')||!content.includes('createScheduleReminderDraft')||!content.includes('annotateScheduleConflict')||!content.includes('proposal-only')||overview.includes('```')||(!overview.includes('/settings')&&!overview.includes('/teacher/schedule')))process.exit(1)"</automated>
  </verify>
  <done>schedule 扩展边界已经被准确写清楚，教师帮助区也被收口为轻量、无代码、面向产品使用的首页内容。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add overview regression tests and a dedicated `verify:phase20` safety command</name>
  <files>src/components/surfaces/help-center-overview-surface.test.tsx, scripts/verify-phase20-help-center.ts, package.json</files>
  <read_first>src/components/surfaces/help-center-overview-surface.tsx, src/components/surfaces/help-guide-detail-surface.test.tsx, scripts/verify-phase19-shell-route-metadata.ts, package.json, src/lib/theme-layout/route-surface-registry.ts</read_first>
  <behavior>
    - Test 1: overview renders the teacher/developer split and teacher help area without code examples.
    - Test 2: verification script fails if `/help` route metadata disappears or if the schedule guide loses `proposal-only` framing.
    - Test 3: verification script checks that the three developer pages exist and the help route remains product-native inside the shell path.
  </behavior>
  <action>Create `src/components/surfaces/help-center-overview-surface.test.tsx` to assert the overview renders `我是教师`, `我是开发者`, the three developer guide entry labels, and teacher-help copy without developer code examples. Then create `scripts/verify-phase20-help-center.ts` and register `verify:phase20` in `package.json`. Reuse the `StaticCheck + runPnpm()` pattern from `scripts/verify-phase19-shell-route-metadata.ts`. The verifier must statically check: (1) `route-surface-registry.ts` still contains `/help`, `/help/plugins`, `/help/themes`, and `/help/actions-interfaces`; (2) `src/app/help/layout.tsx` still wraps the route family in `TeacherSidebarShell`; (3) the three detail pages exist; (4) overview content contains both `我是教师` and `我是开发者`; (5) developer guide content contains `当前可用` and `使用边界`; (6) actions/interfaces guide contains `proposal-only`; and (7) the targeted Vitest files for help overview/detail surfaces pass. Do not implement this as a manual checklist or an external browser-only test gate.</action>
  <acceptance_criteria>
    - `src/components/surfaces/help-center-overview-surface.test.tsx` contains assertions for `我是教师`, `我是开发者`, and at least one developer guide entry label.
    - `package.json` contains `verify:phase20`.
    - `scripts/verify-phase20-help-center.ts` contains checks for `/help/actions-interfaces`, `TeacherSidebarShell`, and `proposal-only`.
    - `scripts/verify-phase20-help-center.ts` runs `pnpm test --run` against the help surface test files.
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase20</automated>
  </verify>
  <done>Phase 20 拥有了专用验证命令，后续 shell/contract 变化一旦让帮助中心走样，仓库会第一时间报错。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| help content -> product guidance | action/interface documentation can mislead users if it overstates current capabilities |
| repository sources -> verify script | static checks and tests decide whether the help center still matches route and content invariants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-09 | I | `src/app/help/actions-interfaces/page.tsx` | mitigate | Keep the schedule guide strictly proposal-only and tied to the current allowlisted actions so users are not misled into assuming direct runtime writes. |
| T-20-10 | T | `src/components/surfaces/help-center-overview-surface.tsx` | mitigate | Keep teacher help code-free and product-facing so the overview page does not leak developer-only contracts into teacher guidance. |
| T-20-11 | T | `scripts/verify-phase20-help-center.ts` | mitigate | Fail verification when route metadata, required detail pages, or proposal-only wording drift out of the help center implementation. |
| T-20-12 | R | `src/components/surfaces/help-center-overview-surface.test.tsx` | accept | UI tests are local safety checks and do not expose privileged data. |
</threat_model>

<verification>
- `pnpm test --run src/components/surfaces/help-center-overview-surface.test.tsx src/components/surfaces/help-guide-detail-surface.test.tsx`
- `pnpm verify:phase20`
</verification>

<success_criteria>
- `/help/actions-interfaces` 准确表达当前 schedule 扩展边界且明确 proposal-only。
- 首页教师帮助区轻量、无代码，并保留开发者 3 个详细页入口。
- Phase 20 拥有专用验证命令，能在后续实现变化时发现 route/content drift。
</success_criteria>

<output>
After completion, create `.planning/phases/20-help-center-and-developer-guides/20-03-SUMMARY.md`
</output>
