---
phase: 23-student-in-class-activity-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dto/learning.ts
  - src/lib/dal/learning.ts
  - src/lib/dal/learning.test.ts
  - src/components/learning/classroom-runtime-client.tsx
  - src/components/learning/task-step-card.tsx
  - src/components/learning/quiz-step-card.tsx
autonomous: true
requirements:
  - ACT-01
must_haves:
  truths:
    - 学生端继续保持 `shell -> Suspense -> personal state` 架构，不把 player 读路径回退成单次大 DTO。
    - 当前步骤始终通过统一课堂活动壳先展示“现在做什么、交什么、何时算完成”，再进入具体动作区。
    - `content`、`task`、`quiz` 都继续复用现有提交/进度边界，只是改成课堂友好的统一叙事。
  artifacts:
    - path: src/lib/dto/learning.ts
      provides: typed student activity guidance contract on the player DTO
      contains: activityGuidance
    - path: src/lib/dal/learning.ts
      provides: server-side mapping from teachingDesign and progress into classroom-friendly student copy
      contains: getStudentPlayerPersonalDTO
    - path: src/components/learning/classroom-runtime-client.tsx
      provides: unified activity shell for the current step
      contains: 当前活动
  key_links:
    - from: src/lib/dal/learning.ts
      to: src/lib/dto/learning.ts
      via: player shell/personal DTOs share one typed activity guidance contract
      pattern: StudentPlayerPersonalDTOSchema|activityGuidance
    - from: src/components/learning/classroom-runtime-client.tsx
      to: src/components/learning/task-step-card.tsx
      via: runtime shell renders one activity wrapper around task-specific action content
      pattern: TaskStepCard|QuizStepCard
---

<objective>
把学生端当前步骤从“泛化步骤卡”升级成“课堂活动卡”，让学生一眼知道现在该做什么、
要交什么、何时算完成，同时继续保持现有 player cache boundary、SSE runtime、task/quiz
写入边界不变。

Purpose: 先锁定 ACT-01 的服务端 guidance contract 和统一活动壳，避免后续 quick
response、live monitoring、analytics 各自发明一套学生端文案与布局。
Output: player DTO activity guidance fields、runtime unified activity shell、task/quiz
card convergence.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-student-in-class-activity-flow/23-CONTEXT.md
@.planning/phases/23-student-in-class-activity-flow/23-RESEARCH.md
@.planning/phases/23-student-in-class-activity-flow/23-UI-SPEC.md
@src/app/(student)/student/player/page.tsx
@src/lib/dto/learning.ts
@src/lib/dal/learning.ts
@src/lib/dto/lesson-authoring.ts
@src/lib/teaching-design.ts
@src/components/learning/classroom-runtime-client.tsx
@src/components/learning/task-step-card.tsx
@src/components/learning/quiz-step-card.tsx
@src/components/surfaces/player-surface.tsx

<interfaces>
Current player boundary that must remain intact:
```ts
const shell = await getStudentPlayerShellDTO({ lessonId, scope })
const personal = await getStudentPlayerPersonalDTO({ lessonId, selectedStepId, forcedStepId: null, scope })
return <ClassroomRuntimeClient shell={shell} personal={personal} />
```

Current teaching-design source of truth already available on step payloads:
```ts
teachingDesign: {
  activityIntent,
  estimatedMinutes,
  activityMode,
  evidenceExpectation: { evidenceType, prompt, required, studentVisibility }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a server-owned student activity guidance contract to player DTOs</name>
  <files>src/lib/dto/learning.ts, src/lib/dal/learning.ts, src/lib/dal/learning.test.ts</files>
  <read_first>src/lib/dto/learning.ts, src/lib/dal/learning.ts, src/lib/dal/learning.test.ts, src/lib/dto/lesson-authoring.ts, src/lib/teaching-design.ts, src/lib/dal/classroom.ts, .planning/phases/23-student-in-class-activity-flow/23-CONTEXT.md, .planning/phases/23-student-in-class-activity-flow/23-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: player DTO now exposes typed student-facing activity fields derived from `teachingDesign`, progress state, and step family instead of relying on client heuristics.
    - Test 2: missing or teacher-only evidence metadata still produces stable student copy rather than leaking raw field names or `teacher-only` strings.
    - Test 3: shell/personal read split remains intact and `getStudentPlayerPersonalDTO()` stays dynamic and student-scoped.
  </behavior>
  <action>Extend `src/lib/dto/learning.ts` with a typed student activity contract that every rendered step can consume. The contract must include concrete fields for `activityGuidance`, `expectedOutput`, `evidenceExpectationSummary`, `completionStateCopy`, `activityModeLabel`, and `estimatedMinutesLabel`. In `src/lib/dal/learning.ts`, compute those values inside the existing player read path from `payload.teachingDesign`, step type, latest task/quiz attempt presence, and progress state. Translate `activityIntent` / `activityMode` into direct Chinese action copy such as `先阅读并抓住重点`、`完成本次课堂作答`、`独立完成`、`两人讨论`、`全班跟随`; translate `evidenceExpectation.required=true` into explicit submit guidance and `required=false` into explicit no-submit guidance. Keep `getPublishedStudentPlayerShellDTO()` cached and free of auth/session reads, and keep all request-specific work inside `getStudentPlayerPersonalDTO()`.</action>
  <acceptance_criteria>
    - `src/lib/dto/learning.ts` contains typed fields named `activityGuidance`, `expectedOutput`, `evidenceExpectationSummary`, and `completionStateCopy` on the player-facing step contract.
    - `src/lib/dal/learning.ts` contains server-side mapping logic that reads `payload.teachingDesign` and emits Chinese student-facing guidance.
    - `src/lib/dal/learning.ts` does not contain new client-side-only fallback comments or raw `teacher-only` copy in the output contract.
    - `src/lib/dal/learning.test.ts` contains assertions for the new activity guidance fields and still asserts the cached shell / dynamic personal split.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/learning.test.ts</automated>
  </verify>
  <done>player DTOs now expose a stable student activity contract that future runtime UI can render directly without re-deriving classroom copy in the client.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the current step through one unified classroom activity shell</name>
  <files>src/components/learning/classroom-runtime-client.tsx, src/components/learning/task-step-card.tsx, src/components/learning/quiz-step-card.tsx</files>
  <read_first>src/components/learning/classroom-runtime-client.tsx, src/components/learning/task-step-card.tsx, src/components/learning/quiz-step-card.tsx, src/components/surfaces/player-surface.tsx, src/components/classroom/classroom-launch-preview.tsx, .planning/phases/23-student-in-class-activity-flow/23-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: current-step UI foregrounds one unified activity card with guidance, expected output, evidence summary, completion copy, and one primary action area.
    - Test 2: task and quiz still keep append-only latest/history semantics, but their history is visually demoted behind the main activity focus.
    - Test 3: lock/recommend guidance remains clear and non-hostile, with recommended-step CTA staying secondary to the current-step primary action.
  </behavior>
  <action>Refactor `src/components/learning/classroom-runtime-client.tsx` so the current step is always rendered inside one classroom-first activity shell before delegating to step-specific content. The shell must render the exact six content blocks from the UI contract: step title, activity guidance, expected output, evidence expectation summary, current completion state, and one step-specific action area. Update `src/components/learning/task-step-card.tsx` and `src/components/learning/quiz-step-card.tsx` so they stop owning a conflicting top-level narrative and instead fit inside that shell: latest attempt stays nearest to the input/result area, full history remains append-only but moves to a secondary tonal layer, and primary CTA wording remains direct. Keep existing task/quiz submission actions and retry/reveal policies intact. Do not add a second hero, tabbed runtime shell, or admin-style checklist sidebar.</action>
  <acceptance_criteria>
    - `src/components/learning/classroom-runtime-client.tsx` contains a unified current-step section with explicit guidance and expected-output rendering before task/quiz-specific content.
    - `src/components/learning/task-step-card.tsx` and `src/components/learning/quiz-step-card.tsx` still contain their existing submit actions and append-only latest/history concepts.
    - `src/components/learning/classroom-runtime-client.tsx` still contains `teacherRecommendedStepId`, `locked`, and `前往老师推荐步骤`, but does not render the recommendation CTA as a co-equal primary submit action.
    - none of the modified player components contain raw `teacher-only`, `readiness`, or `run sheet` student-facing copy.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/learning/student-step-cards.test.ts src/components/surfaces/student-player-surfaces.test.ts</automated>
  </verify>
  <done>students now see one consistent classroom activity surface across content, task, and quiz steps without breaking existing runtime behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teaching-design payload -> player DTO | student-facing guidance must come from server-owned translation, not ad hoc client inference |
| player shell -> personal runtime state | Phase 23 must preserve the cached shell and Suspense-streamed personal boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | T | `src/lib/dal/learning.ts` | mitigate | Derive activity guidance server-side from typed `teachingDesign` and step state so copy cannot drift per client render path. |
| T-23-02 | I | `src/lib/dto/learning.ts` | mitigate | Translate `teacher-only` evidence metadata into natural student copy instead of leaking teacher-facing fields. |
| T-23-03 | D | `src/app/(student)/student/player/page.tsx` | mitigate | Keep the split shell/personal read model so runtime enrichment does not regress cache and Suspense behavior. |
| T-23-04 | E | runtime UI | accept | UI refactor changes presentation only and continues to use the existing task/quiz action boundaries. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/learning.test.ts`
- `pnpm test --run src/components/learning/student-step-cards.test.ts src/components/surfaces/student-player-surfaces.test.ts`
</verification>

<success_criteria>
- player DTOs expose classroom-friendly activity guidance derived server-side.
- students see one unified activity shell for the current step.
- task, quiz, lock/unlock, and Suspense streaming semantics remain compatible with the current runtime.
</success_criteria>

<output>
After completion, create `.planning/phases/23-student-in-class-activity-flow/23-01-SUMMARY.md`
</output>
