---
phase: 17-teacher-flow-editor-enhancement
plan: 03
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/app/(teacher)/teacher/editor/preview/page.tsx
  - src/app/(teacher)/teacher/editor/preview/page.test.tsx
  - src/components/surfaces/teacher-lesson-preview-surface.tsx
  - src/components/surfaces/lesson-editor-surface.tsx
autonomous: true
requirements:
  - LESSON-03
  - LESSON-04
  - PLUGIN-05
must_haves:
  truths:
    - D-07: teacher 现在可以打开真实 preview route 预览当前草稿 lesson flow，而不是只看到占位 CTA
    - D-03/D-07/D-10: preview 继续使用 teacher-owned course/lesson 参数，不复用 student runtime 或绕过 DAL
    - D-05: preview 可见步骤类型、顺序、built-in teaching-step 来源和引用材料摘要
  artifacts:
    - path: src/app/(teacher)/teacher/editor/preview/page.tsx
      provides: teacher preview route
      contains: getTeacherLessonPreviewDTO
    - path: src/components/surfaces/teacher-lesson-preview-surface.tsx
      provides: draft lesson preview surface
      contains: 课堂预览
    - path: src/components/surfaces/lesson-editor-surface.tsx
      provides: editor preview entry wiring
      contains: /teacher/editor/preview
  key_links:
    - from: src/app/(teacher)/teacher/editor/preview/page.tsx
      to: src/lib/dal/lesson-authoring.ts
      via: explicit courseId + lessonId drive teacher preview DTO loading
      pattern: getTeacherLessonPreviewDTO
    - from: src/components/surfaces/lesson-editor-surface.tsx
      to: src/app/(teacher)/teacher/editor/preview/page.tsx
      via: preview CTA and panel point to the new route
      pattern: /teacher/editor/preview
---

<objective>
为 `/teacher/editor` 建立真实课堂预览能力：新增 teacher preview route，并把编辑页的预览入口
从占位按钮升级为真实可执行预览链路。

Purpose: 满足“可以预览”的明确用户目标，让教师在发布前就能看到当前草稿流程的课堂呈现顺序。
Output: teacher preview route、preview surface、editor 内预览入口与摘要面板。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-teacher-flow-editor-enhancement/17-CONTEXT.md
@.planning/phases/17-teacher-flow-editor-enhancement/17-RESEARCH.md
@.planning/phases/17-teacher-flow-editor-enhancement/17-UI-SPEC.md
@.planning/phases/17-teacher-flow-editor-enhancement/17-01-PLAN.md
@src/app/(teacher)/teacher/editor/page.tsx
@src/components/surfaces/lesson-editor-surface.tsx
@src/components/surfaces/player-surface.tsx
@src/app/(student)/student/player/page.tsx

<interfaces>
From src/app/(teacher)/teacher/editor/page.tsx:
```ts
type TeacherEditorPageProps = {
  searchParams?: Promise<{ courseId?: string; lessonId?: string }>;
};
```

From src/components/surfaces/player-surface.tsx:
```ts
export function PlayerSurface({ shell, personalSlot }: PlayerSurfaceProps)
```

From Plan 17-01 expected output:
```ts
export async function getTeacherLessonPreviewDTO(input: { lessonId: string }): Promise<...>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a dedicated teacher preview route for draft lesson flows</name>
  <files>src/app/(teacher)/teacher/editor/preview/page.tsx, src/app/(teacher)/teacher/editor/preview/page.test.tsx, src/components/surfaces/teacher-lesson-preview-surface.tsx</files>
  <read_first>src/app/(student)/student/player/page.tsx, src/components/surfaces/player-surface.tsx, src/app/(teacher)/teacher/editor/page.tsx, .planning/phases/17-teacher-flow-editor-enhancement/17-RESEARCH.md</read_first>
  <behavior>
    - Test 1: preview route requires explicit `courseId` and `lessonId` and does not fall back to student dashboard or global lesson selection per D-01 and D-07.
    - Test 2: preview surface renders ordered active steps, materials, and built-in source labels from the teacher preview DTO.
    - Test 3: preview route stays teacher-owned and draft-based instead of reusing student progress/runtime state.
  </behavior>
  <action>Create `src/app/(teacher)/teacher/editor/preview/page.tsx` and `src/components/surfaces/teacher-lesson-preview-surface.tsx`. The route must accept `courseId` and `lessonId` query params, verify the lesson belongs to the same course-aware editor context, and load `getTeacherLessonPreviewDTO()` from Plan 17-01. Render a teacher-facing preview that shows lesson title/objective, ordered active steps, built-in source badges, and material reference summaries. Use the visual rhythm of the player/stage surfaces as inspiration, but do not import classroom runtime, SSE, student progress, or `getStudentPlayerPersonalDTO`; this route previews draft authoring state only.</action>
  <acceptance_criteria>
    - `src/app/(teacher)/teacher/editor/preview/page.tsx` exists and contains `courseId` and `lessonId` branches.
    - `src/app/(teacher)/teacher/editor/preview/page.tsx` contains `getTeacherLessonPreviewDTO` and does not contain `getStudentPlayerPersonalDTO`.
    - `src/components/surfaces/teacher-lesson-preview-surface.tsx` contains `课堂预览` and built-in/source rendering.
    - `src/app/(teacher)/teacher/editor/preview/page.test.tsx` asserts explicit-param behavior.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run "src/app/(teacher)/teacher/editor/preview/page.test.tsx"</automated>
  </verify>
  <done>Teachers can open a real preview route for the current draft lesson flow and inspect ordered steps, materials, and built-in source information before publishing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the editor shell to the real preview route and inline preview summary</name>
  <files>src/components/surfaces/lesson-editor-surface.tsx</files>
  <read_first>src/components/surfaces/lesson-editor-surface.tsx, src/app/(teacher)/teacher/editor/page.tsx, src/components/surfaces/teacher-lesson-preview-surface.tsx, .planning/phases/17-teacher-flow-editor-enhancement/17-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: the existing `预览课堂` CTA now targets the new preview route with explicit `courseId` and `lessonId`.
    - Test 2: editor shell includes a preview summary panel that reflects the current draft step count, built-in step count, and material count per D-07.
    - Test 3: preview entry remains inside the current teacher editor shell and does not degrade the course-aware layout.
  </behavior>
  <action>Update `src/components/surfaces/lesson-editor-surface.tsx` so the existing `预览课堂` action becomes a real link/button to `/teacher/editor/preview?courseId={lesson.course.id}&lessonId={lesson.lesson.id}` (per D-07). Add an inline preview summary card/panel in the editor shell that summarizes the current draft flow using values already present in the loaded editor DTO: active step count, built-in sourced step count, and material count. Keep the current teacher shell structure and Chinese tonal styling; do not turn preview into a modal requiring client-side data refetch.</action>
  <acceptance_criteria>
    - `src/components/surfaces/lesson-editor-surface.tsx` contains `/teacher/editor/preview`.
    - `src/components/surfaces/lesson-editor-surface.tsx` no longer renders `预览课堂` as a dead button with no route target.
    - `src/components/surfaces/lesson-editor-surface.tsx` contains preview summary copy tied to active steps / built-in steps / materials.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/components/surfaces/lesson-editor-surface.tsx','utf8');if(!s.includes('/teacher/editor/preview')||!s.includes('预览课堂'))process.exit(1)"</automated>
  </verify>
  <done>The editor no longer advertises fake preview capability; it offers both an inline draft preview summary and a working teacher preview route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor query params → preview route | Untrusted `courseId` / `lessonId` params determine preview target |
| teacher preview DTO → preview surface | Draft lesson content becomes teacher-visible preview UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | E | `src/app/(teacher)/teacher/editor/preview/page.tsx` | mitigate | Require explicit `courseId` and `lessonId`, and validate preview lesson ownership against the teacher-scoped DAL before rendering. |
| T-17-10 | I | `src/components/surfaces/teacher-lesson-preview-surface.tsx` | accept | Preview exposes only draft content already visible in the same teacher-owned editor context. |
| T-17-11 | T | `src/components/surfaces/lesson-editor-surface.tsx` | mitigate | Link preview through the fixed teacher route instead of a client-side freeform preview state that could drift from server data. |
| T-17-12 | D | `src/app/(teacher)/teacher/editor/preview/page.tsx` | mitigate | Keep preview isolated from student runtime/SSE so preview failures do not pull in unrelated classroom dependencies. |
</threat_model>

<verification>
- `pnpm test --run "src/app/(teacher)/teacher/editor/preview/page.test.tsx"`
- `node -e "const fs=require('fs');const s=fs.readFileSync('src/app/(teacher)/teacher/editor/preview/page.tsx','utf8');if(!s.includes('getTeacherLessonPreviewDTO')||s.includes('getStudentPlayerPersonalDTO'))process.exit(1)"`
</verification>

<success_criteria>
- `/teacher/editor` has a real draft preview route.
- Teachers can understand the current lesson flow before publish without entering student runtime.
- Preview keeps built-in step provenance and material summaries visible.
</success_criteria>

<output>
After completion, create `.planning/phases/17-teacher-flow-editor-enhancement/17-03-SUMMARY.md`
</output>
