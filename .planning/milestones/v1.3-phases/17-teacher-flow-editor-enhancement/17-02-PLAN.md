---
phase: 17-teacher-flow-editor-enhancement
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/components/authoring/lesson-authoring-workspace.tsx
  - src/components/authoring/lesson-authoring-workspace.test.tsx
  - src/components/authoring/lesson-step-editor.tsx
  - src/components/authoring/lesson-step-editor.test.tsx
autonomous: true
requirements:
  - LESSON-03
  - LESSON-04
  - LESSON-07
  - PLUGIN-05
must_haves:
  truths:
    - D-04/D-06: 教师能在统一 flow composition workspace 里组合普通步骤与 built-in teaching-step plugins，而不是仅靠分散按钮
    - D-05: 已插入的 built-in teaching steps 在流程轨道和属性编辑器中保留来源可见性
    - D-13: 编辑器交互升级但仍保持中文、tonal surfaces、无 1px divider 的设计语言
  artifacts:
    - path: src/components/authoring/lesson-authoring-workspace.tsx
      provides: integrated flow composition workspace
      contains: builtInSource
    - path: src/components/authoring/lesson-step-editor.tsx
      provides: structured step property editor with built-in metadata affordances
      contains: 步骤来源
  key_links:
    - from: src/components/authoring/lesson-authoring-workspace.tsx
      to: src/actions/lesson-authoring-actions.ts
      via: add/reorder/archive/duplicate stay on current Server Actions
      pattern: addLessonStepAction|reorderLessonStepAction
    - from: src/components/authoring/lesson-step-editor.tsx
      to: src/lib/dto/lesson-authoring.ts
      via: built-in provenance and structured payload fields are visible in the editor
      pattern: builtInSource|autosaveLessonStepAction
---

<objective>
把现有 authoring workspace 升级为更清晰的一体化课堂流程编辑器，并让步骤属性编辑器理解
built-in teaching-step 的来源与结构化字段。

Purpose: 兑现“灵活、功能强大、可组合编辑上课流程环节”的核心体验，而不是停留在基础卡片堆叠。
Output: integrated flow composition workspace、来源可见的步骤轨道、增强的 step property editor。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-teacher-flow-editor-enhancement/17-CONTEXT.md
@.planning/phases/17-teacher-flow-editor-enhancement/17-RESEARCH.md
@.planning/phases/17-teacher-flow-editor-enhancement/17-UI-SPEC.md
@.planning/phases/17-teacher-flow-editor-enhancement/17-01-PLAN.md
@src/components/authoring/lesson-authoring-workspace.tsx
@src/components/authoring/lesson-step-editor.tsx

<interfaces>
From src/components/authoring/lesson-authoring-workspace.tsx:
```ts
type LessonAuthoringWorkspaceProps = {
  overview: TeacherAuthoringOverviewDTO;
  lesson: LessonEditorDTO | null;
  builtInTemplates: BuiltInTemplateForAuthoring[];
};
```

From src/components/authoring/lesson-step-editor.tsx:
```ts
export function LessonStepEditor({ step }: { step: LessonStepDTO | null })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor the workspace into an integrated flow-composition surface</name>
  <files>src/components/authoring/lesson-authoring-workspace.tsx, src/components/authoring/lesson-authoring-workspace.test.tsx</files>
  <read_first>src/components/authoring/lesson-authoring-workspace.tsx, src/components/authoring/lesson-authoring-workspace.test.tsx, src/components/surfaces/lesson-editor-surface.tsx, src/actions/lesson-authoring-actions.ts, .planning/phases/17-teacher-flow-editor-enhancement/17-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: built-in teaching-step buttons and ordinary step actions render in one composition area and keep their quick-add affordance per D-04.
    - Test 2: flow cards show step type, ordering, and built-in source badge when `builtInSource` exists per D-05.
    - Test 3: reorder/duplicate/archive still call the existing Server Actions while the upgraded UI remains source-aware per D-06.
  </behavior>
  <action>Refactor `src/components/authoring/lesson-authoring-workspace.tsx` so the left/composer rail, flow canvas, and selected-step summary read as one integrated classroom-flow editor instead of separate utility cards (per D-02, D-04, and D-06). Keep the existing `新增内容` / `新增任务` / `新增测验` and built-in template insertion actions, but surface them as one flow-composition contract with clearer grouping, step source badges, and richer step summaries. When a step payload includes `builtInSource`, render a stable source label such as `内置环节 · 教师讲授`; when absent, render the existing ordinary type label only. Preserve current `addLessonStepAction`, `duplicateLessonStepAction`, `archiveLessonStepAction`, and `reorderLessonStepAction` usage; do not introduce a new client-side persistence layer or direct DB access.</action>
  <acceptance_criteria>
    - `src/components/authoring/lesson-authoring-workspace.tsx` contains built-in source rendering such as `内置环节` or `步骤来源`.
    - `src/components/authoring/lesson-authoring-workspace.tsx` still contains `addLessonStepAction` and `reorderLessonStepAction`.
    - `src/components/authoring/lesson-authoring-workspace.test.tsx` asserts built-in source badges and reorder behavior.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/authoring/lesson-authoring-workspace.test.tsx</automated>
  </verify>
  <done>The workspace now reads like a real flow editor: ordinary steps and built-in teaching-step plugins are composed in one place, with ordering and source visibility intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the structured step editor for richer payload and source editing context</name>
  <files>src/components/authoring/lesson-step-editor.tsx, src/components/authoring/lesson-step-editor.test.tsx</files>
  <read_first>src/components/authoring/lesson-step-editor.tsx, src/components/authoring/lesson-step-editor.test.tsx, src/lib/dto/lesson-authoring.ts, src/actions/lesson-authoring-actions.ts</read_first>
  <behavior>
    - Test 1: content/task/quiz payload editing still persists through `autosaveLessonStepAction`.
    - Test 2: built-in sourced steps render read-only source metadata in the editor without losing existing field editing behavior.
    - Test 3: material reference editing remains structured and stays inside the same save payload per LESSON-04.
  </behavior>
  <action>Upgrade `src/components/authoring/lesson-step-editor.tsx` so it remains the structured property editor for `content`, `task`, and `quiz`, but also exposes non-editable built-in source context from `payload.builtInSource` when present (per D-05). Keep the existing form sections for body/prompt/question/material references, but reorganize labels and summaries so the editor clearly communicates the current step type, source, and save state. Continue to save through `autosaveLessonStepAction`, and keep the saved payload shape aligned with the extended schema from Plan 01; do not create an uncontrolled secondary form state outside the component.</action>
  <acceptance_criteria>
    - `src/components/authoring/lesson-step-editor.tsx` contains built-in source display text such as `步骤来源` or `内置环节`.
    - `src/components/authoring/lesson-step-editor.tsx` still calls `autosaveLessonStepAction`.
    - `src/components/authoring/lesson-step-editor.test.tsx` covers built-in source visibility plus save payload assertions.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/authoring/lesson-step-editor.test.tsx</automated>
  </verify>
  <done>The step editor remains schema-driven and saveable, while teachers can now tell whether a selected flow item comes from an ordinary step or a built-in teaching-step plugin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server DTOs → client authoring UI | Teacher-scoped lesson and plugin metadata become interactive composition controls |
| client step editor → Server Actions | Edited payloads cross from client form state into server-side persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | T | `src/components/authoring/lesson-authoring-workspace.tsx` | mitigate | Keep all mutations on existing Server Actions so richer composition UI cannot bypass server validation. |
| T-17-06 | I | `src/components/authoring/lesson-step-editor.tsx` | accept | Built-in source labels expose only plugin name and key already visible in the teacher editor context. |
| T-17-07 | E | `src/components/authoring/lesson-step-editor.tsx` | mitigate | Persist through `autosaveLessonStepAction` and the lesson step payload schema instead of creating raw client-side mutation paths. |
| T-17-08 | D | `src/components/authoring/lesson-authoring-workspace.tsx` | mitigate | Preserve current reorder/archive/duplicate action wiring so UI refactor does not break LexoRank-based flow editing. |
</threat_model>

<verification>
- `pnpm test --run src/components/authoring/lesson-authoring-workspace.test.tsx src/components/authoring/lesson-step-editor.test.tsx`
- `node -e "const fs=require('fs');const s=fs.readFileSync('src/components/authoring/lesson-step-editor.tsx','utf8');if(!s.includes('autosaveLessonStepAction'))process.exit(1)"`
</verification>

<success_criteria>
- Ordinary steps and built-in teaching-step plugins compose inside one coherent flow editor.
- Step cards and property editor both expose built-in provenance when available.
- Existing structured payload editing and material reference persistence remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/17-teacher-flow-editor-enhancement/17-02-SUMMARY.md`
</output>
