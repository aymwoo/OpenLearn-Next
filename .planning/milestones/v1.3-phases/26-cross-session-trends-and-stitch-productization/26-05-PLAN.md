---
phase: 26-cross-session-trends-and-stitch-productization
plan: 05
type: execute
wave: 4
depends_on:
  - 26-03
files_modified:
  - src/components/surfaces/classroom-console-surface.tsx
  - src/components/surfaces/classroom-console-surface.test.tsx
  - src/components/learning/teacher-review-surface.tsx
  - src/components/learning/teacher-review-surface.test.ts
autonomous: true
requirements:
  - UI-05
must_haves:
  truths:
    - `/classroom` 继续是 live runtime 与 single-session recap 的主域，同时视觉上属于同一教师产品链。
    - `/teacher/review` 继续是 lesson-level feedback 工作台，同时视觉上不再像独立后台工具。
    - classroom / review surfaces 保持 mobile 单列优先、共享 width/rhythm contract，且不引入横向滚动主卡轨。
  artifacts:
    - path: src/components/surfaces/classroom-console-surface.tsx
      provides: runtime and recap home aligned to the shared teacher product skeleton
      contains: ClassroomSessionRecapSurface
    - path: src/components/learning/teacher-review-surface.tsx
      provides: lesson-level review posture preserved inside the same teacher product language
      contains: teacherSurfaceRhythm
  key_links:
    - from: src/components/surfaces/classroom-console-surface.tsx
      to: src/components/classroom/classroom-session-recap-surface.tsx
      via: classroom runtime and recap keep one shell language while retaining session-first ownership
      pattern: ClassroomSessionRecapSurface
    - from: src/components/learning/teacher-review-surface.tsx
      to: /teacher/review
      via: review stays lesson-level and does not become the analytics primary route
      pattern: /teacher/review
---

<objective>
产品化 classroom / review，同时保留它们各自的主域职责与已有工作流边界。

Purpose: 让 classroom 和 review 进入同一教师产品语言，但不把它们混成 trends 的替代入口。
Output: updated classroom/review surfaces and focused regressions for their shared skeleton.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-cross-session-trends-and-stitch-productization/26-CONTEXT.md
@.planning/phases/26-cross-session-trends-and-stitch-productization/26-RESEARCH.md
@.planning/phases/26-cross-session-trends-and-stitch-productization/26-UI-SPEC.md
@src/components/surfaces/classroom-console-surface.tsx
@src/components/learning/teacher-review-surface.tsx
@src/components/surfaces/teacher-surface-rhythm.ts
@src/components/surfaces/surface-widths.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Productize classroom runtime and recap while preserving `/classroom` ownership</name>
  <files>src/components/surfaces/classroom-console-surface.tsx, src/components/surfaces/classroom-console-surface.test.tsx</files>
  <read_first>src/components/surfaces/classroom-console-surface.tsx, src/components/surfaces/classroom-console-surface.test.tsx, src/components/surfaces/teacher-surface-rhythm.ts, src/components/surfaces/surface-widths.ts, .planning/phases/26-cross-session-trends-and-stitch-productization/26-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - `src/components/surfaces/classroom-console-surface.tsx` continues to reference `teacherSurfaceRhythm` and `surfaceWidths`.
    - `src/components/surfaces/classroom-console-surface.tsx` keeps `/classroom` as runtime + single-session recap home and does not position trends as a replacement home.
    - the surface keeps mobile single-column posture before existing `md:` / `xl:` splits and does not introduce `overflow-x-auto` on the main content rails.
    - `src/components/surfaces/classroom-console-surface.test.tsx` asserts the shared skeleton plus responsive/no-horizontal-scroll contract.
  </acceptance_criteria>
  <action>Refactor `classroom-console-surface.tsx` so it visibly reuses the shared teacher product skeleton from `teacherSurfaceRhythm` and `surfaceWidths` while preserving `/classroom` ownership. Keep the current gradient runtime stage for live class and the session-first recap hero for ended sessions, but align section labels, metric card rhythm, and CTA wording with the Phase 26 skeleton rather than bespoke per-page phrasing. Keep mobile single-column flow first, only promote to existing `md:` / `xl:` split layouts at larger breakpoints, continue wrapping copy within `surfaceWidths.heroBody`, and do not introduce `overflow-x-auto` or new `min-w` card rails. Update `src/components/surfaces/classroom-console-surface.test.tsx` so it explicitly covers shared skeleton reuse and the responsive/no-horizontal-scroll contract.</action>
  <verify>
    <automated>pnpm test --run src/components/surfaces/classroom-console-surface.test.tsx</automated>
  </verify>
  <done>The classroom runtime and recap home now feel like part of the same teacher product without losing their single-session ownership.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Productize review while preserving lesson-level feedback ownership</name>
  <files>src/components/learning/teacher-review-surface.tsx, src/components/learning/teacher-review-surface.test.ts</files>
  <read_first>src/components/learning/teacher-review-surface.tsx, src/components/learning/teacher-review-surface.test.ts, src/components/surfaces/teacher-surface-rhythm.ts, src/components/surfaces/surface-widths.ts, .planning/phases/26-cross-session-trends-and-stitch-productization/26-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - `src/components/learning/teacher-review-surface.tsx` continues to reference `teacherSurfaceRhythm` and `surfaceWidths`.
    - `src/components/learning/teacher-review-surface.tsx` remains lesson-level feedback workflow and does not present itself as the trends primary route.
    - the surface keeps mobile single-column posture before existing `md:` / `xl:` splits and does not introduce `overflow-x-auto` on the main content rails.
    - `src/components/learning/teacher-review-surface.test.ts` asserts the shared skeleton plus responsive/no-horizontal-scroll contract.
  </acceptance_criteria>
  <action>Refactor `teacher-review-surface.tsx` so it visibly reuses the same product skeleton from `teacherSurfaceRhythm` and `surfaceWidths` while preserving lesson-level ownership. Keep review posture per D-04, but normalize hero metrics, filter rail, detail panel density, and CTA grammar to match the shared teacher product language instead of a detached admin workflow. Keep mobile single-column flow first, only promote to existing `md:` / `xl:` split layouts at larger breakpoints, continue wrapping copy within `surfaceWidths.heroBody`, and do not introduce `overflow-x-auto` or new `min-w` card rails. Update `src/components/learning/teacher-review-surface.test.ts` so it explicitly covers shared skeleton reuse and the responsive/no-horizontal-scroll contract.</action>
  <verify>
    <automated>pnpm test --run src/components/learning/teacher-review-surface.test.ts</automated>
  </verify>
  <done>The review surface now shares the teacher product language without losing its lesson-level feedback role.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classroom runtime -> recap ownership | classroom productization must not turn trends into the new single-session home |
| review workflow -> analytics workflow | review must stay lesson-level feedback follow-up rather than becoming the primary analytics entry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-13 | T | classroom surface productization | mitigate | Reuse `teacherSurfaceRhythm` and `surfaceWidths` directly while preserving `/classroom` ownership language. |
| T-26-14 | I | review route posture | mitigate | Keep review lesson-level and forbid analytics-first copy or CTA posture. |
| T-26-15 | R | classroom/review responsive drift | mitigate | Assert mobile-first width/rhythm rules and no-horizontal-scroll posture in focused tests. |
</threat_model>

<verification>
- `pnpm test --run src/components/surfaces/classroom-console-surface.test.tsx src/components/learning/teacher-review-surface.test.ts`
</verification>

<success_criteria>
- Classroom remains the single-session runtime/recap home while sharing the teacher product language.
- Review remains lesson-level feedback follow-up while sharing the same product skeleton.
- Classroom and review remain responsive and free of horizontal-scroll drift.
</success_criteria>

<output>
After completion, create `.planning/phases/26-cross-session-trends-and-stitch-productization/26-05-SUMMARY.md`
</output>
