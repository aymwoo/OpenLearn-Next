---
phase: 26-cross-session-trends-and-stitch-productization
plan: 04
type: execute
wave: 3
depends_on:
  - 26-02
files_modified:
  - src/components/surfaces/teacher-trends-surface.tsx
  - src/components/surfaces/teacher-trends-surface.test.tsx
  - src/components/surfaces/teacher-dashboard-surface.tsx
  - src/components/surfaces/teacher-dashboard-surface.test.tsx
  - src/components/surfaces/help-center-overview-surface.tsx
  - src/components/surfaces/help-center-overview-surface.test.tsx
  - src/components/surfaces/settings-surface.tsx
  - src/components/surfaces/settings-surface.test.tsx
autonomous: true
requirements:
  - UI-05
must_haves:
  truths:
    - trends、dashboard、help、settings 继续属于同一教师产品系统，而不是 analytics 页面一套、支持页面一套。
    - `teacher dashboard` 明确成为教师工作链入口，并把 trends 作为可见下一步动作。
    - 次级教师页面继续遵守 shared width / rhythm / responsive / no-horizontal-scroll contract。
  artifacts:
    - path: src/components/surfaces/teacher-trends-surface.tsx
      provides: trends surface aligned to the shared product skeleton
      contains: xl:grid-cols-[minmax(0,1fr)_minmax(280px,0.4fr)]
    - path: src/components/surfaces/teacher-dashboard-surface.test.tsx
      provides: dashboard-to-trends product entry regression coverage
      contains: /teacher/trends
    - path: src/components/surfaces/settings-surface.tsx
      provides: secondary teacher page posture aligned to shared width and rhythm rules
      contains: surfaceWidths.workspace
  key_links:
    - from: src/components/surfaces/teacher-dashboard-surface.tsx
      to: /teacher/trends
      via: dashboard exposes trends as the visible next teacher action
      pattern: /teacher/trends
    - from: src/components/surfaces/teacher-trends-surface.tsx
      to: src/components/surfaces/help-center-overview-surface.tsx
      via: primary analytics and secondary support pages must reuse the same width/rhythm contract
      pattern: surfaceWidths.workspace
---

<objective>
对 trends、dashboard、help、settings 执行同一轮 Stitch-aligned productization pass，
把 analytics 入口与次级支持页拉回同一教师产品系统。

Purpose: 完成 UI-05 的另一半，把 trends 与 dashboard / help / settings 统一到相同的 teacher shell product language。
Output: updated trends/dashboard/help/settings surfaces and regression tests for their shared skeleton.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-cross-session-trends-and-stitch-productization/26-CONTEXT.md
@.planning/phases/26-cross-session-trends-and-stitch-productization/26-RESEARCH.md
@.planning/phases/26-cross-session-trends-and-stitch-productization/26-UI-SPEC.md
@src/components/surfaces/teacher-trends-surface.tsx
@src/components/surfaces/teacher-dashboard-surface.tsx
@src/components/surfaces/help-center-overview-surface.tsx
@src/components/surfaces/settings-surface.tsx
@src/components/surfaces/teacher-surface-rhythm.ts
@src/components/surfaces/surface-widths.ts
</context>

<interfaces>
Shared contracts already available:
```ts
export const teacherSurfaceRhythm = {
  stack: "space-y-6",
  shell: "rounded-[var(--radius-shell)] shadow-ambient",
  cardInset: "rounded-[var(--radius-card)] bg-surface-container-lowest shadow-ambient",
  hero: "rounded-[var(--radius-shell)] bg-surface-container-low p-6 shadow-ambient sm:p-8",
  section: "rounded-[var(--radius-shell)] bg-surface-container-low p-5 shadow-ambient sm:p-6",
  sectionCompact: "rounded-[var(--radius-shell)] bg-surface-container-low p-5 shadow-ambient",
  gradientHeroContent: "px-6 py-6 sm:px-8 sm:py-8",
} as const;

export const surfaceWidths = {
  workspace: "mx-auto w-full max-w-[1360px]",
  heroTitle: "max-w-[52rem]",
  heroBody: "max-w-[44rem]",
} as const;
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Productize trends and dashboard as the visible analytics entry of the teacher workflow</name>
  <files>src/components/surfaces/teacher-trends-surface.tsx, src/components/surfaces/teacher-trends-surface.test.tsx, src/components/surfaces/teacher-dashboard-surface.tsx, src/components/surfaces/teacher-dashboard-surface.test.tsx</files>
  <read_first>src/components/surfaces/teacher-trends-surface.tsx, src/components/surfaces/teacher-trends-surface.test.tsx, src/components/surfaces/teacher-dashboard-surface.tsx, src/components/surfaces/teacher-dashboard-surface.test.tsx, src/components/surfaces/teacher-surface-rhythm.ts, src/components/surfaces/surface-widths.ts, .planning/phases/26-cross-session-trends-and-stitch-productization/26-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: trends surface keeps class-first posture, inline detail, desktop split, and classroom-first CTA hierarchy while adopting the shared product skeleton.
    - Test 2: dashboard exposes `/teacher/trends` as a visible next action and keeps dashboard as the teacher workflow entry rather than a competing analytics home.
  </behavior>
  <acceptance_criteria>
    - `src/components/surfaces/teacher-trends-surface.tsx` continues to contain `grid gap-4 md:grid-cols-2`, contains `xl:grid-cols-[minmax(0,1fr)_minmax(280px,0.4fr)]`, and does not contain `overflow-x-auto`.
    - dashboard tests assert `/teacher/trends` is present.
    - `src/components/surfaces/teacher-trends-surface.test.tsx` and `src/components/surfaces/teacher-dashboard-surface.test.tsx` assert shared `teacherSurfaceRhythm` / `surfaceWidths` usage and no detached analytics-first drift.
    - dashboard remains the teacher workflow entry while trends is the visible analytics next action.
  </acceptance_criteria>
  <action>Refine `teacher-trends-surface.tsx` and `teacher-dashboard-surface.tsx` so the analytics entry of the teacher workflow uses one consistent product skeleton. In `teacher-trends-surface.tsx`, preserve class-first posture, inline detail, and the classroom-first primary CTA, but normalize hero spacing, section density, metric rhythm, and split layout around `teacherSurfaceRhythm` and `surfaceWidths`; keep `grid gap-4 md:grid-cols-2`, keep `xl:grid-cols-[minmax(0,1fr)_minmax(280px,0.4fr)]`, and do not introduce `overflow-x-auto`. In `teacher-dashboard-surface.tsx`, keep dashboard as the workflow entry while surfacing a visible `查看班级趋势` action to `/teacher/trends`, and align hero rhythm, quick actions, and support rails to the same shared skeleton instead of a detached home-page language. Update the two tests so they assert `/teacher/trends`, shared width/rhythm usage, responsive split classes, and the absence of detached analytics-first drift.</action>
  <verify>
    <automated>pnpm test --run src/components/surfaces/teacher-trends-surface.test.tsx src/components/surfaces/teacher-dashboard-surface.test.tsx</automated>
  </verify>
  <done>Trends and dashboard now look and behave like adjacent surfaces in the same teacher workflow.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Productize help and settings as secondary teacher pages inside the same system</name>
  <files>src/components/surfaces/help-center-overview-surface.tsx, src/components/surfaces/help-center-overview-surface.test.tsx, src/components/surfaces/settings-surface.tsx, src/components/surfaces/settings-surface.test.tsx</files>
  <read_first>src/components/surfaces/help-center-overview-surface.tsx, src/components/surfaces/help-center-overview-surface.test.tsx, src/components/surfaces/settings-surface.tsx, src/components/surfaces/settings-surface.test.tsx, src/components/surfaces/teacher-surface-rhythm.ts, src/components/surfaces/surface-widths.ts, .planning/phases/26-cross-session-trends-and-stitch-productization/26-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - `src/components/surfaces/help-center-overview-surface.tsx` and `src/components/surfaces/settings-surface.tsx` continue to reference `surfaceWidths.workspace` and the shared teacher rhythm tokens.
    - help/settings stay mobile-first, keep wrapped hero copy inside shared width rules, and do not introduce `overflow-x-auto` card rails.
    - `src/components/surfaces/help-center-overview-surface.test.tsx` and `src/components/surfaces/settings-surface.test.tsx` assert `/teacher/trends`-era teacher product language, shared width tokens, and the responsive/no-horizontal-scroll posture.
  </acceptance_criteria>
  <action>Update `help-center-overview-surface.tsx` and `settings-surface.tsx` so both pages visibly reuse the same teacher product skeleton as the primary chain: shared hero rhythm, tonal sections, `surfaceWidths.workspace`, wrapped long-form copy, and the same Chinese CTA language family as the rest of the teacher system. Keep both pages mobile-first, allow denser grids only at `md:` / `xl:` breakpoints, and do not introduce `overflow-x-auto` or page-specific max-width recipes. Update the two tests so they assert shared width tokens, responsive/no-horizontal-scroll posture, and that these routes still read like teacher-facing product surfaces instead of utilitarian docs/admin fallbacks.</action>
  <verify>
    <automated>pnpm test --run src/components/surfaces/help-center-overview-surface.test.tsx src/components/surfaces/settings-surface.test.tsx</automated>
  </verify>
  <done>Help and settings now behave like secondary teacher product pages, not detached utility shells.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analytics entry -> support surfaces | dashboard/help/settings must inherit the same product posture without turning into a second shell system |
| shared design contracts -> touched surfaces | trends/dashboard/help/settings must reuse existing width and rhythm tokens instead of inventing ad-hoc layout rules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-10 | T | analytics/support surface productization | mitigate | Reuse `teacherSurfaceRhythm` and `surfaceWidths` directly, and assert the resulting posture in focused tests. |
| T-26-11 | I | dashboard analytics entry | mitigate | Keep `/teacher/trends` as a visible next action from dashboard without turning dashboard into a competing analytics-only home. |
| T-26-12 | R | support-page responsive drift | mitigate | Assert mobile-first width/rhythm rules and no-horizontal-scroll posture in help/settings/trends tests. |
</threat_model>

<verification>
- `pnpm test --run src/components/surfaces/teacher-trends-surface.test.tsx src/components/surfaces/teacher-dashboard-surface.test.tsx src/components/surfaces/help-center-overview-surface.test.tsx src/components/surfaces/settings-surface.test.tsx`
</verification>

<success_criteria>
- Trends, dashboard, help, and settings visibly belong to the same Stitch-aligned teacher product system.
- Dashboard becomes the clear analytics entry while help/settings inherit the same support-page skeleton.
- The touched secondary surfaces remain responsive and free of horizontal-scroll drift.
</success_criteria>

<output>
After completion, create `.planning/phases/26-cross-session-trends-and-stitch-productization/26-04-SUMMARY.md`
</output>
