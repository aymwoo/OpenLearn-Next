---
phase: 26-cross-session-trends-and-stitch-productization
plan: 03
type: execute
wave: 3
depends_on:
  - 26-02
files_modified:
  - src/components/classroom/classroom-session-recap-surface.tsx
  - src/components/classroom/classroom-session-recap-surface.test.tsx
  - src/components/surfaces/lesson-editor-surface.tsx
  - src/components/surfaces/lesson-editor-surface.test.tsx
  - src/components/surfaces/classroom-launch-surface.tsx
  - src/components/classroom/classroom-launch-panel.test.tsx
autonomous: true
requirements:
  - ANALYTICS-02
  - UI-05
must_haves:
  truths:
    - `/classroom` 的 single-session recap 继续是主域，但会提供回到同一 `/teacher/trends` 的次级探索入口。
    - `editor -> launch` 共享同一套 teacher hero/section/CTA 语法，明显属于同一教师产品链起点。
    - planning/launch surfaces 保持 mobile 单列优先、共享 width/rhythm contract，且不引入横向滚动卡轨。
  artifacts:
    - path: src/components/classroom/classroom-session-recap-surface.tsx
      provides: recap-to-trends dual entry that preserves class and session context
      contains: /teacher/trends?classId=
    - path: src/components/surfaces/lesson-editor-surface.tsx
      provides: planning surface aligned to the shared teacher product skeleton
      contains: teacherSurfaceRhythm
    - path: src/components/surfaces/classroom-launch-surface.tsx
      provides: launch surface aligned to the same teacher chain start
      contains: 开启新课堂
  key_links:
    - from: src/components/classroom/classroom-session-recap-surface.tsx
      to: /teacher/trends
      via: ended-session recap exposes the same trends route as a secondary exploration entry per D-02
      pattern: /teacher/trends\?classId=
    - from: src/components/surfaces/lesson-editor-surface.tsx
      to: src/components/surfaces/classroom-launch-surface.tsx
      via: planning and launch surfaces share one teacher product grammar before classroom runtime begins
      pattern: teacherSurfaceRhythm|surfaceWidths
---

<objective>
补上 recap-to-trends 双入口，并把 editor / launch 收束成同一条教师主链起点。

Purpose: 让 single-session recap 能顺滑进入 trends，同时让 planning 和 launch 不再像两套独立产品。
Output: recap deep-link wiring plus a unified planning-to-launch product entry.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-cross-session-trends-and-stitch-productization/26-CONTEXT.md
@.planning/phases/26-cross-session-trends-and-stitch-productization/26-RESEARCH.md
@.planning/phases/26-cross-session-trends-and-stitch-productization/26-UI-SPEC.md
@src/components/classroom/classroom-session-recap-surface.tsx
@src/components/surfaces/lesson-editor-surface.tsx
@src/components/surfaces/classroom-launch-surface.tsx
@src/components/surfaces/teacher-surface-rhythm.ts
@src/components/surfaces/surface-widths.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the recap-to-trends dual entry without replacing `/classroom` as the recap home</name>
  <files>src/components/classroom/classroom-session-recap-surface.tsx, src/components/classroom/classroom-session-recap-surface.test.tsx</files>
  <read_first>src/components/classroom/classroom-session-recap-surface.tsx, src/components/classroom/classroom-session-recap-surface.test.tsx, .planning/phases/26-cross-session-trends-and-stitch-productization/26-CONTEXT.md, .planning/phases/26-cross-session-trends-and-stitch-productization/26-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - `src/components/classroom/classroom-session-recap-surface.tsx` contains `/teacher/trends?classId=` plus `lessonId=`, `sessionId=`, and `view=sessions` in a recap-to-trends CTA.
    - the CTA is labeled `查看班级趋势` and appears as a secondary exploration action, not as the hero's primary classroom action.
    - the recap copy still states that `/classroom` remains the single-session recap home.
    - `src/components/classroom/classroom-session-recap-surface.test.tsx` asserts the trends deep-link and the retained classroom-home posture.
  </acceptance_criteria>
  <action>In `src/components/classroom/classroom-session-recap-surface.tsx`, add a secondary CTA labeled `查看班级趋势` inside the teacher follow-up / recap action area that links to `/teacher/trends?classId={recap.session.classId}&lessonId={recap.session.lessonId}&sessionId={recap.session.id}&view=sessions`. Keep the existing `/classroom` recap hero and student follow-up posture as the primary single-session workflow; do not move the hero copy or primary action to trends. Update `src/components/classroom/classroom-session-recap-surface.test.tsx` so it proves the deep-link exists and that recap copy still frames `/classroom` as the single-session home.</action>
  <verify>
    <automated>pnpm test --run src/components/classroom/classroom-session-recap-surface.test.tsx</automated>
  </verify>
  <done>Teachers can move from a finished classroom recap into trends without losing the classroom-first ownership model.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Productize editor and launch as the planning-to-runtime start of the teacher chain</name>
  <files>src/components/surfaces/lesson-editor-surface.tsx, src/components/surfaces/lesson-editor-surface.test.tsx, src/components/surfaces/classroom-launch-surface.tsx, src/components/classroom/classroom-launch-panel.test.tsx</files>
  <read_first>src/components/surfaces/lesson-editor-surface.tsx, src/components/surfaces/lesson-editor-surface.test.tsx, src/components/surfaces/classroom-launch-surface.tsx, src/components/classroom/classroom-launch-panel.test.tsx, src/components/surfaces/teacher-surface-rhythm.ts, src/components/surfaces/surface-widths.ts, .planning/phases/26-cross-session-trends-and-stitch-productization/26-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - `lesson-editor-surface.tsx` and `classroom-launch-surface.tsx` continue to reference `teacherSurfaceRhythm` and `surfaceWidths`.
    - `lesson-editor-surface.tsx` keeps the workspace-first posture and does not introduce analytics-first copy or `overflow-x-auto` rails.
    - `classroom-launch-surface.tsx` keeps `开启新课堂` as the single primary action and keeps live-session recovery in a tonal secondary rail.
    - the two touched surfaces keep mobile single-column posture before existing `md:` / `xl:` splits.
    - `src/components/surfaces/lesson-editor-surface.test.tsx` and `src/components/classroom/classroom-launch-panel.test.tsx` assert the shared skeleton plus responsive/no-horizontal-scroll contract.
  </acceptance_criteria>
  <action>Refactor `lesson-editor-surface.tsx` and `classroom-launch-surface.tsx` so they visibly reuse the same product skeleton from `teacherSurfaceRhythm` and `surfaceWidths`. In `lesson-editor-surface.tsx`, keep the workspace-first posture and compact authoring summary, but normalize hero/header spacing, tonal cards, and CTA hierarchy so it reads as the planning start of the same teacher workflow instead of a standalone admin tool. In `classroom-launch-surface.tsx`, preserve `开启新课堂` as the single primary action and keep live-session recovery in the tonal secondary rail. Across both surfaces, keep mobile single-column flow first, only promote to existing `md:` / `xl:` split layouts at larger breakpoints, continue wrapping copy within `surfaceWidths.heroBody`, and do not introduce `overflow-x-auto` or new `min-w` card rails. Update the corresponding tests so editor/launch assertions explicitly cover shared skeleton reuse and the responsive/no-horizontal-scroll contract.</action>
  <verify>
    <automated>pnpm test --run src/components/surfaces/lesson-editor-surface.test.tsx src/components/classroom/classroom-launch-panel.test.tsx</automated>
  </verify>
  <done>The editor and launch surfaces now read as consecutive stages in the same teacher workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recap home -> trends exploration | recap may expose trends as secondary exploration, but cannot surrender `/classroom` as the single-session owner |
| shared design contracts -> planning/launch surfaces | the teacher chain start must reuse existing surface tokens instead of inventing ad-hoc layout rules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07 | T | recap-to-trends wiring | mitigate | Add the deep-link only as a secondary CTA and keep recap copy plus primary classroom actions intact. |
| T-26-08 | I | planning and launch posture | mitigate | Keep `editor` workspace-first and `launch` action-first while adjusting only shared hero/section grammar. |
| T-26-09 | R | planning-to-launch productization | mitigate | Reuse `teacherSurfaceRhythm` and `surfaceWidths` directly; forbid new page-specific shell recipes or horizontal card rails. |
</threat_model>

<verification>
- `pnpm test --run src/components/classroom/classroom-session-recap-surface.test.tsx src/components/surfaces/lesson-editor-surface.test.tsx src/components/classroom/classroom-launch-panel.test.tsx`
</verification>

<success_criteria>
- Teachers can move from single-session recap into trends without losing classroom ownership.
- Editor and launch visibly share one Stitch-aligned teacher product language.
- The teacher chain start remains mobile-first, responsive, and free of horizontal-scroll drift.
</success_criteria>

<output>
After completion, create `.planning/phases/26-cross-session-trends-and-stitch-productization/26-03-SUMMARY.md`
</output>
