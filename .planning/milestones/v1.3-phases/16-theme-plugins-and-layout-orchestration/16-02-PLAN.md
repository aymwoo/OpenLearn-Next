---
phase: 16-theme-plugins-and-layout-orchestration
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - src/server/themes/registry.ts
  - src/lib/dal/themes.ts
  - src/actions/theme-actions.ts
  - src/components/theme/theme-injector.tsx
  - src/lib/dal/themes.test.ts
  - src/actions/theme-actions.test.ts
autonomous: true
requirements:
  - PLUGIN-06
must_haves:
  truths:
    - D-16/D-18: 已启用主题继续通过既有 `activeThemeId` 与 actor/school scope 校验进入运行时，不新增平行主题系统
    - D-08/D-15/D-17: 主题运行时同时提供 CSS variables 与编译后的 layout metadata，并保持声明式 JSON 编译边界
    - D-13/D-14/D-18/D-19: 缺失或非法 layout 数据时系统稳定回退到当前默认教师布局并维持既有设计边界
  artifacts:
    - path: src/lib/dal/themes.ts
      provides: Actor-scoped active theme runtime access
      contains: getActiveThemeRuntimeForCurrentActor
    - path: src/components/theme/theme-injector.tsx
      provides: Theme runtime injection boundary
      contains: theme-layout-runtime
    - path: src/actions/theme-actions.ts
      provides: Theme switch and registration actions on the same runtime path
      contains: revalidatePath("/", "layout")
  key_links:
    - from: src/server/themes/registry.ts
      to: src/lib/dal/themes.ts
      via: persisted parsed theme tokens become runtime DTOs
      pattern: registerThemeTokens|getActiveThemeRuntimeForCurrentActor
    - from: src/lib/dal/themes.ts
      to: src/components/theme/theme-injector.tsx
      via: active theme runtime resolution
      pattern: getActiveThemeRuntimeForCurrentActor|ThemeInjector
---

<objective>
Extend the existing theme registry and injection path so one resolved runtime payload drives
both visual tokens and layout orchestration.

Purpose: Preserve D-16 through D-19 while making the compiled layout runtime available to
teacher shells and settings surfaces without creating a parallel theme system.
Output: Active-theme runtime resolver, registry persistence updates, injector metadata, and
regression tests for the single runtime path.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-theme-plugins-and-layout-orchestration/16-CONTEXT.md
@.planning/phases/16-theme-plugins-and-layout-orchestration/16-RESEARCH.md
@.planning/phases/16-theme-plugins-and-layout-orchestration/16-PATTERNS.md
@.planning/phases/16-theme-plugins-and-layout-orchestration/16-01-PLAN.md
@src/server/themes/registry.ts
@src/lib/dal/themes.ts
@src/actions/theme-actions.ts
@src/components/theme/theme-injector.tsx

<interfaces>
From src/lib/dal/themes.ts:
```ts
export async function getThemeRegistryDTO(themeId: string): Promise<ThemeRegistryDTO | null>;
export async function getValidThemesForSchool(schoolId: string): Promise<ThemeRegistryDTO[]>;
export async function getActiveThemeForCurrentActor(themeId: string): Promise<ThemeRegistryDTO | null>;
```

From src/actions/theme-actions.ts:
```ts
export async function setActiveThemeAction(input: FormData | Record<string, unknown>);
export async function registerThemeTokensAction(input: FormData | Record<string, unknown>);
```

From src/components/theme/theme-injector.tsx:
```tsx
export async function ThemeInjector();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Surface a compiled active-theme runtime through registry and DAL</name>
  <files>src/server/themes/registry.ts, src/lib/dal/themes.ts, src/lib/dal/themes.test.ts</files>
  <read_first>src/server/themes/registry.ts, src/lib/dal/themes.ts, src/lib/dal/themes.test.ts, src/server/themes/tokens.ts, .planning/phases/16-theme-plugins-and-layout-orchestration/16-01-PLAN.md</read_first>
  <behavior>
    - Test 1: actor-scoped active theme lookup returns compiled layout runtime only for valid themes within the actor's school membership.
    - Test 2: invalid or missing layout data produces default shell runtime instead of `null` layout metadata.
    - Test 3: school theme listings expose summary-ready runtime metadata for settings cards.
  </behavior>
  <action>Extend `src/server/themes/registry.ts` and `src/lib/dal/themes.ts` per D-13, D-14, D-15, D-16, and D-18 so persisted theme records expose a compiled runtime object built from `compileThemeLayoutRuntime()`. Add a DAL function such as `getActiveThemeRuntimeForCurrentActor(themeId)` that returns the validated theme DTO plus compiled layout runtime and settings-summary data after actor/school scope checks. Keep `getValidThemesForSchool()` school-scoped, but enrich the returned DTOs with runtime summary fields rather than guessing from theme names. When theme layout data is missing or invalid, return the current default teacher shell runtime instead of removing the theme entirely. Update `src/lib/dal/themes.test.ts` to assert actor scope, valid-theme guard, and default-runtime fallback behavior.</action>
  <acceptance_criteria>
    - `src/lib/dal/themes.ts` exports `getActiveThemeRuntimeForCurrentActor`.
    - `src/lib/dal/themes.ts` contains `getValidThemesForSchool` logic that exposes summary-ready runtime data.
    - `src/lib/dal/themes.test.ts` contains `getActiveThemeRuntimeForCurrentActor` and a fallback assertion string for default runtime behavior.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/themes.test.ts</automated>
  </verify>
  <done>The active theme runtime is resolved through the existing school-scoped DAL path, and settings consumers can read structured shell summaries without using theme-name heuristics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inject runtime metadata through the existing theme switch path</name>
  <files>src/actions/theme-actions.ts, src/components/theme/theme-injector.tsx, src/actions/theme-actions.test.ts</files>
  <read_first>src/actions/theme-actions.ts, src/components/theme/theme-injector.tsx, src/actions/theme-actions.test.ts, src/lib/theme-cookie.ts, src/lib/dal/themes.ts</read_first>
  <behavior>
    - Test 1: theme switching continues to use `activeThemeId` and `revalidatePath("/", "layout")`.
    - Test 2: `ThemeInjector` emits CSS variables plus serialized runtime metadata for layout consumers.
    - Test 3: when no valid active theme exists, injector returns a safe default runtime marker rather than unsafe arbitrary payloads.
  </behavior>
  <action>Update `src/actions/theme-actions.ts` and `src/components/theme/theme-injector.tsx` per D-15, D-16, D-17, and D-18. Keep `setActiveThemeAction()` and `registerThemeTokensAction()` as the only switch/registration entry points, but make `ThemeInjector` read the compiled runtime from `getActiveThemeRuntimeForCurrentActor()` and expose it to downstream shells through a stable DOM contract such as `data-theme-layout-runtime` or a serialized JSON script tag with a fixed id. Continue to sanitize CSS variable output; do not inject arbitrary layout HTML, arbitrary CSS property maps, or a second theme cookie. Update `src/actions/theme-actions.test.ts` so it asserts the same runtime path plus the new layout-runtime injection contract.</action>
  <acceptance_criteria>
    - `src/components/theme/theme-injector.tsx` contains `getActiveThemeRuntimeForCurrentActor`.
    - `src/components/theme/theme-injector.tsx` contains `theme-layout-runtime`.
    - `src/actions/theme-actions.ts` still contains `revalidatePath("/", "layout")` and does not declare a second theme cookie name.
    - `src/actions/theme-actions.test.ts` asserts the new runtime metadata contract.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/actions/theme-actions.test.ts src/lib/dal/themes.test.ts</automated>
  </verify>
  <done>The runtime injection boundary stays on the existing theme chain, and downstream shells can consume compiled layout metadata without bypassing actor/school scoping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted theme record → DAL runtime DTO | Stored manifest data becomes actor-visible runtime metadata |
| DAL runtime DTO → DOM injection | Validated theme output is emitted into the rendered document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | S | `src/lib/dal/themes.ts` | mitigate | Reuse current user + membership checks so only active school members can resolve a theme runtime. |
| T-16-06 | T | `src/components/theme/theme-injector.tsx` | mitigate | Emit only sanitized CSS variables plus fixed-shape runtime metadata; do not allow arbitrary style/html injection. |
| T-16-07 | I | `src/actions/theme-actions.ts` | mitigate | Keep a single `activeThemeId` cookie and existing action entry points to avoid leaking theme selection through parallel runtime paths. |
| T-16-08 | D | `src/lib/dal/themes.ts` | mitigate | Return default runtime fallback when layout data is invalid so broken themes do not blank the teacher shell. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/themes.test.ts src/actions/theme-actions.test.ts`
- `node -e "const fs=require('fs');const s=fs.readFileSync('src/components/theme/theme-injector.tsx','utf8');if(!s.includes('theme-layout-runtime'))process.exit(1)"`
</verification>

<success_criteria>
- The active theme runtime is resolved from the existing plugin/theme registry path.
- Theme switching keeps the same cookie + revalidation contract.
- Layout metadata reaches the DOM through a fixed safe boundary that later plans can consume.
</success_criteria>

<output>
After completion, create `.planning/phases/16-theme-plugins-and-layout-orchestration/16-02-SUMMARY.md`
</output>
