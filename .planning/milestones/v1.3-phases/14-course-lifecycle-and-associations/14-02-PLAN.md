---
phase: 14-course-lifecycle-and-associations
plan: 02
status: complete
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dto/course-authoring.ts
  - src/lib/dal/course-authoring.ts
  - src/lib/dal/course-authoring.test.ts
  - src/actions/course-authoring-actions.ts
  - src/actions/course-authoring-actions.test.ts
  - src/components/courses/course-detail-form.tsx
  - src/components/courses/course-detail-form.test.tsx
  - src/components/surfaces/teacher-course-detail-surface.tsx
autonomous: false
requirements:
  - COURSE-06
must_haves:
  truths:
    - 教师能在自己的 school scope 内把 class 关联到课程，或从课程中移除 class。
    - class association 只能复用既有 teacher/school-scoped DAL 和 DTO 边界，不能让 UI 直连 DB。
    - 本计划已执行完成；实现结果见 `14-02-SUMMARY.md`。
    - `COURSE-07` 学生 enrollment management 明确后置，不并入本计划。
  artifacts:
    - path: src/lib/dal/course-authoring.ts
      provides: teacher-owned and school-scoped course-class association read/write contract
    - path: src/actions/course-authoring-actions.ts
      provides: class association add/remove server actions
    - path: src/components/courses/course-detail-form.tsx
      provides: in-page class association management UI
  key_links:
    - src/components/courses/course-detail-form.tsx -> src/actions/course-authoring-actions.ts via class association submit
    - src/actions/course-authoring-actions.ts -> src/lib/dal/course-authoring.ts via scoped association mutation
    - src/lib/dal/course-authoring.ts -> school-scoped class lookup via existing teacher scope rules
---

<objective>
实现 Phase 14-02 的课程与班级关联管理，固定 school-scoped association contract、
入口位置与验证范围，并保持与学生 enrollment 明确分离。

Purpose: 完成 COURSE-06，并继续把范围限制在 class association add/remove，
而不是扩成 roster import、student enrollment 或额外课程管理页面。
Output: class association DTO/DAL/action contract、课程详情页关联管理入口、focused tests 与 phase summary。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-course-center-foundation/13-01-PLAN.md
@.planning/phases/13-course-center-foundation/13-02-PLAN.md
@src/lib/dto/course-authoring.ts
@src/lib/dal/course-authoring.ts
@src/actions/course-authoring-actions.ts
@src/lib/dto/class-management.ts
@src/lib/dal/class-management.ts
@src/components/surfaces/teacher-course-detail-surface.tsx

<interfaces>
Existing school-scoped domains that this plan should reuse:
```ts
getTeacherCourseDetailDTO({ courseId })
createCourseAction(input)
updateCourseAction(input)
```

Related class-management boundary to reference, not bypass:
```ts
// class-management scoped reads/writes already own class domain rules
```

Explicitly out of scope:
```ts
COURSE-07 // course-student enrollment associations
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Skeleton for course-class association contracts and scope checks</name>
  <files>src/lib/dto/course-authoring.ts, src/lib/dal/course-authoring.ts, src/actions/course-authoring-actions.ts, src/lib/dto/class-management.ts, src/lib/dal/class-management.ts</files>
  <read_first>src/lib/dto/course-authoring.ts; src/lib/dal/course-authoring.ts; src/actions/course-authoring-actions.ts; src/lib/dto/class-management.ts; src/lib/dal/class-management.ts; .planning/ROADMAP.md; .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Only classes inside the teacher's school scope are available for association.
    - Association add/remove reuses server-owned contracts and explicit cache invalidation.
    - Class association never mutates or implies student enrollment changes.
  </behavior>
  <action>在 `course-authoring` 边界内实现 class association contract，补齐 DTO 字段、course/class scope 校验、add/remove 操作语义、失败反馈和 cache invalidation 触点。实现保持与 `class-management` 的 teacher scope 规则一致，但不新增独立 association 子系统。</action>
  <acceptance_criteria>
    - 计划明确 add/remove class association 都是 teacher/school-scoped mutation
    - 计划明确 class lookup 应复用现有 class-management 范围边界
    - 计划明确写出 “关联班级 != 管理学生 enrollment” 的范围隔离
  </acceptance_criteria>
  <verify>
    <automated>./node_modules/.bin/vitest run src/actions/course-authoring-actions.test.ts src/lib/dal/course-authoring.test.ts</automated>
  </verify>
  <done>course-class association 的 contract、scope 和非目标边界已落地，不会误把 COURSE-07 提前混入。</done>
</task>

<task type="auto">
  <name>Task 2: Implement course detail association UI and success/failure feedback</name>
  <files>src/components/courses/course-detail-form.tsx, src/components/courses/course-detail-form.test.tsx, src/components/surfaces/teacher-course-detail-surface.tsx</files>
  <read_first>src/components/courses/course-detail-form.tsx; src/components/surfaces/teacher-course-detail-surface.tsx; .planning/phases/13-course-center-foundation/13-02-PLAN.md</read_first>
  <action>在课程详情页内实现班级关联管理，保持它是 course workflow 的一部分，而不是新增独立页面。实现覆盖可关联班级列表、已关联班级列表、移除动作、blocked/error feedback、read-your-writes 反馈，以及与现有课程详情信息区的关系。</action>
  <acceptance_criteria>
    - 计划明确 class association UI 主入口位于课程详情工作流内
    - 计划明确 success / blocked / empty-state feedback 需要页内可见证据
    - 计划再次声明 `COURSE-07` enrollment management 后置
  </acceptance_criteria>
  <verify>
    <manual>Review course detail page for add/remove class affordances and page-level feedback behavior</manual>
  </verify>
  <done>班级关联入口、反馈形式与页面边界都已在课程详情页内落地。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teacher association request -> server action | 未信任的 classId/courseId 请求进入 course-class mutation |
| course association -> class-management scope | 课程域必须复用班级域的 school-scope 真值，而不是自行猜测 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | E | course-class association | mitigate | add/remove 操作都必须检查 teacher school scope，跨 school class 一律拒绝 |
| T-14-06 | T | association semantics | mitigate | 在 skeleton 中显式写清 add/remove 仅影响 course-class link，不修改 enrollment |
| T-14-07 | I | course detail feedback | mitigate | 后续实现要求页内 success/blocked feedback，避免 silent failure |
| T-14-08 | S | scope creep | mitigate | 明确 `COURSE-07` deferred，association 计划不包含 student enrollment 管理 |
</threat_model>

<verification>
- Focused review: confirm scope is limited to `COURSE-06`
- Focused review: confirm class association and student enrollment are explicitly separated
- Automated: `./node_modules/.bin/vitest run src/components/courses/course-detail-form.test.tsx src/actions/course-authoring-actions.test.ts src/lib/dal/course-authoring.test.ts`
- Automated: `./node_modules/.bin/next build`
</verification>

<success_criteria>
- 14-02 只覆盖 class association add/remove 与 school-scoped guardrails
- 实现继续复用 `course-authoring` 现有 DTO、DAL 与 Server Actions 边界，并保持与 teacher school scope 规则一致
- 课程详情页内已提供 add/remove 入口与页内反馈，且未把 `COURSE-07` enrollment management 混入
</success_criteria>

<output>
Created `.planning/phases/14-course-lifecycle-and-associations/14-02-SUMMARY.md`
</output>
