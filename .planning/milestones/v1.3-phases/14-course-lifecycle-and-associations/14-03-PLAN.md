---
phase: 14-course-lifecycle-and-associations
plan: 03
status: complete
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dto/course-authoring.ts
  - src/lib/dal/course-authoring.ts
  - src/lib/dal/course-authoring.test.ts
  - src/actions/course-authoring-actions.ts
  - src/actions/course-authoring-actions.test.ts
  - src/components/surfaces/teacher-course-detail-surface.tsx
  - src/components/courses/course-detail-form.tsx
  - src/components/courses/course-detail-form.test.tsx
autonomous: false
requirements:
  - COURSE-05
must_haves:
  truths:
    - 教师只能删除满足条件的课程，且删除必须通过显式确认路径完成。
    - 当课程因 lessons、associations 或其他 guardrail 被阻止删除时，教师能看到清晰 failure feedback。
    - 本计划已执行完成；实现结果见 `14-03-SUMMARY.md`。
    - `COURSE-07` 学生 enrollment management 明确后置，不并入本计划。
  artifacts:
    - path: src/lib/dal/course-authoring.ts
      provides: delete eligibility and blocked-delete guardrails
    - path: src/actions/course-authoring-actions.ts
      provides: explicit delete confirmation action
    - path: src/components/surfaces/teacher-course-detail-surface.tsx
      provides: guarded delete entry
  key_links:
    - src/components/surfaces/teacher-course-detail-surface.tsx -> src/actions/course-authoring-actions.ts via guarded delete submit
    - src/actions/course-authoring-actions.ts -> src/lib/dal/course-authoring.ts via delete eligibility check and delete mutation
    - src/lib/dal/course-authoring.ts -> src/lib/dto/course-authoring.ts via blocked-delete result contract
---

<objective>
实现 Phase 14-03 的课程删除 guardrail：delete eligibility、blocked reasons、显式确认路径与页内反馈。

Purpose: 完成 COURSE-05，而不把范围扩散到班级关联、enrollment cleanup 或其他 phase。
Output: delete guardrail contract、blocked-delete result、课程详情删除入口与确认交互。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-course-center-foundation/13-01-PLAN.md
@.planning/phases/13-course-center-foundation/13-02-PLAN.md
@src/lib/dto/course-authoring.ts
@src/lib/dal/course-authoring.ts
@src/actions/course-authoring-actions.ts
@src/actions/course-authoring-actions.test.ts
@src/components/surfaces/teacher-course-detail-surface.tsx
@src/components/courses/course-detail-form.tsx

<interfaces>
Delete flow extends the current teacher course workflow instead of adding a side-channel:
```ts
getTeacherCourseDetailDTO({ courseId })
updateCourseAction(input)
```

Delete-specific contract implemented in this plan:
```ts
deleteCourseAction(input)
getCourseDeleteEligibility({ courseId })
```

Explicitly out of scope:
```ts
COURSE-07 // enrollment cleanup or student reassignment workflow
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement delete eligibility, blocked reasons, and confirmation contract</name>
  <files>src/lib/dto/course-authoring.ts, src/lib/dal/course-authoring.ts, src/actions/course-authoring-actions.ts, src/actions/course-authoring-actions.test.ts</files>
  <read_first>src/lib/dto/course-authoring.ts; src/lib/dal/course-authoring.ts; src/actions/course-authoring-actions.ts; src/actions/course-authoring-actions.test.ts; .planning/ROADMAP.md; .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Only eligible teacher-scoped courses can be deleted.
    - Blocked deletes return explicit machine-readable reasons and teacher-facing feedback copy.
    - Delete remains distinct from archive, and cannot silently degrade into status mutation.
  </behavior>
  <action>在课程 authoring DTO、DAL 和 action 边界内实现删除 eligibility、blocked reason contract、显式确认输入、成功后的 cache invalidation，以及课程详情页所需的结果 DTO。保持 `archive != delete`，blocked state 必须有明确页内反馈。</action>
  <acceptance_criteria>
    - 计划明确 delete 与 archive 是两条不同语义路径
    - 计划明确 blocked-delete 至少需要结构化 reason 和 teacher-facing feedback
    - 计划明确 delete flow 需要显式确认，而不是单击即删
  </acceptance_criteria>
  <verify>
      <automated>./node_modules/.bin/vitest run src/actions/course-authoring-actions.test.ts src/lib/dal/course-authoring.test.ts</automated>
  </verify>
  <done>后续执行时，课程删除 guardrail、blocked reasons 和确认 contract 都已有骨架，不会被 archive 语义替代。</done>
</task>

<task type="auto">
  <name>Task 2: Implement guarded delete UI and blocked-delete evidence</name>
  <files>src/components/surfaces/teacher-course-detail-surface.tsx, src/components/courses/course-detail-form.tsx</files>
  <read_first>src/components/surfaces/teacher-course-detail-surface.tsx; src/components/courses/course-detail-form.tsx; .planning/phases/13-course-center-foundation/13-02-PLAN.md</read_first>
  <action>在课程详情页内实现 guarded delete 入口，提供确认文案、disabled/blocked 状态、成功后返回课程中心的行为，以及页内可见的失败证据。删除 guardrail 只覆盖课程实体本身，不额外展开 student enrollment 清理流程，`COURSE-07` 仍后置。</action>
  <acceptance_criteria>
    - 计划明确 delete 入口位于课程详情工作流内，并需要显式确认交互
    - 计划明确 blocked-delete 反馈必须页内可见，不能只依赖瞬时 toast
    - 计划再次声明 `COURSE-07` enrollment management 后置
  </acceptance_criteria>
  <verify>
      <manual>Review detail page for explicit confirmation path and blocked-delete feedback evidence</manual>
  </verify>
  <done>后续实现时，删除入口、确认步骤和失败反馈形式都已预先框定。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teacher delete request -> server action | 未信任的删除请求进入高风险 mutation |
| delete guardrail -> teacher feedback | 被阻止的删除必须返回结构化理由和可见证据，避免误删或误判 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | T | delete eligibility | mitigate | 预先定义 eligibility contract 和 blocked reasons，避免 UI 自行推断能否删除 |
| T-14-10 | R | explicit confirmation | mitigate | 要求删除路径有显式确认，不允许单击直接删除 |
| T-14-11 | I | teacher feedback | mitigate | blocked-delete 必须返回页内可见证据，而不是静默失败或通用 error |
| T-14-12 | S | scope creep | mitigate | 明确 `COURSE-07` deferred，删除 guardrail 不展开 enrollment cleanup 工作流 |
</threat_model>

<verification>
- `./node_modules/.bin/vitest run src/components/courses/course-detail-form.test.tsx src/actions/course-authoring-actions.test.ts src/lib/dal/course-authoring.test.ts`
- `./node_modules/.bin/next build`
</verification>

<success_criteria>
- 14-03 只覆盖 delete guardrails、显式确认和 blocked-delete feedback
- delete 与 archive 已在 contract 和 UI 上明确区分
- 课程详情页已提供显式确认删除路径与页内 blocked feedback
</success_criteria>

<output>
Implementation summary: `.planning/phases/14-course-lifecycle-and-associations/14-03-SUMMARY.md`
</output>
