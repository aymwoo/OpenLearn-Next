---
phase: 24-live-classroom-operations-and-formative-evaluation
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - src/lib/dto/classroom.ts
  - src/lib/dal/classroom.ts
  - src/actions/classroom-actions.ts
  - src/components/classroom/classroom-student-evaluation-form.tsx
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
must_haves:
  truths:
    - 过程评价模型固定为 `3 档参与度 + 标签 + 观察记录`，不能滑向分数制、rubric 或可配置等级系统。
    - 所有评价写入必须继续走 teacher-scoped、durable、auditable 的 classroom domain path，而不是新增平行 review backend。
    - 本阶段优先复用现有 `classroomEvidence` / `classroomTimeline` 边界，除非确有必要，不引入新的评价表。
  artifacts:
    - path: src/lib/dto/classroom.ts
      provides: formative evaluation input and read DTO contracts
      contains: ClassroomParticipationLevelSchema
    - path: src/lib/dal/classroom.ts
      provides: teacher-scoped evaluation write/read helpers
      contains: recordStudentFormativeEvaluation
    - path: src/actions/classroom-actions.ts
      provides: server action for participation tier, tags, and observation note writes
      contains: recordStudentFormativeEvaluationAction
  key_links:
    - from: src/actions/classroom-actions.ts
      to: src/lib/dal/classroom.ts
      via: server action writes evaluation through the classroom DAL and invalidates classroom cache
      pattern: recordStudentFormativeEvaluationAction|updateTag
    - from: src/lib/dto/classroom.ts
      to: src/components/classroom/classroom-student-evaluation-form.tsx
      via: fixed tiers and tag allowlist power the teacher form
      pattern: ClassroomParticipationLevelSchema|ClassroomEvaluationTagSchema
---

<objective>
为 `/classroom` 单学生评价建立可落地的写入与读取契约，固定实现“积极参与 / 正常参与 / 需要关注 + 标签 + 观察记录”，并保持 teacher-only、session-scoped、可审计。

Purpose: 先把 formative evaluation 变成稳定的数据契约，后续 detail panel 只负责展示与交互，不再临时设计评价模型。
Output: DTO schemas, DAL write/read helpers, server action, and the basic teacher evaluation form component.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-live-classroom-operations-and-formative-evaluation/24-CONTEXT.md
@src/lib/dto/classroom.ts
@src/lib/dal/classroom.ts
@src/actions/classroom-actions.ts
@src/db/schema.ts
@src/components/classroom/classroom-control-panel.tsx
@src/components/classroom/classroom-timeline-panel.tsx

<interfaces>
Locked evaluation model for Phase 24:
```ts
type ClassroomParticipationLevel = "active" | "normal" | "attention"

type StudentFormativeEvaluationInput = {
  sessionId: string
  studentId: string
  participationLevel: ClassroomParticipationLevel
  tags: Array<
    | "主动发言"
    | "专注跟进"
    | "协作支持"
    | "表达清晰"
    | "需要提醒"
    | "需要跟进"
  >
  observationNote: string
}
```

Persistence rule:
```ts
recordClassroomEvidence({
  sourceType: "teacher-observation",
  evidenceType: "observation",
  payload: {
    kind: "formative-evaluation",
    participationLevel,
    tags,
    observationNote,
  },
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define fixed formative evaluation schemas and DAL write/read helpers</name>
  <files>src/lib/dto/classroom.ts, src/lib/dal/classroom.ts</files>
  <read_first>src/lib/dto/classroom.ts, src/lib/dal/classroom.ts, src/db/schema.ts, src/actions/classroom-actions.ts, .planning/phases/24-live-classroom-operations-and-formative-evaluation/24-CONTEXT.md</read_first>
  <behavior>
    - Test 1: DTOs lock participation to exactly 3 tiers and keep tags inside a finite allowlist.
    - Test 2: DAL writes remain teacher-scoped and reuse the classroom evidence path with an explicit formative-evaluation payload marker.
    - Test 3: DAL can read back session-scoped evaluation history for a single student without querying another route domain.
  </behavior>
  <action>In `src/lib/dto/classroom.ts`, add `ClassroomParticipationLevelSchema` with exact values `active`, `normal`, and `attention`, a `ClassroomEvaluationTagSchema` enum with exact values `主动发言`, `专注跟进`, `协作支持`, `表达清晰`, `需要提醒`, and `需要跟进`, plus `RecordStudentFormativeEvaluationInputSchema` and `StudentFormativeEvaluationEntryDTOSchema`. In `src/lib/dal/classroom.ts`, create `recordStudentFormativeEvaluation(input)` that calls `getTeacherSessionScope`, verifies the target student is a participant, and persists the evaluation through `recordClassroomEvidence` using `sourceType: "teacher-observation"`, `evidenceType: "observation"`, and payload fields `kind: "formative-evaluation"`, `participationLevel`, `tags`, and `observationNote`. Also add `listStudentFormativeEvaluationEntries({ sessionId, studentId })` that filters session evidence to the same `kind: "formative-evaluation"` marker and returns newest-first DTOs with captured teacher id and createdAt.</action>
  <acceptance_criteria>
    - `src/lib/dto/classroom.ts` contains `ClassroomParticipationLevelSchema` with `active`, `normal`, `attention`.
    - `src/lib/dto/classroom.ts` contains all six fixed tags `主动发言`, `专注跟进`, `协作支持`, `表达清晰`, `需要提醒`, `需要跟进`.
    - `src/lib/dal/classroom.ts` contains `recordStudentFormativeEvaluation` and `listStudentFormativeEvaluationEntries`.
    - `src/lib/dal/classroom.ts` persists payload field `kind: "formative-evaluation"` through `recordClassroomEvidence`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/classroom.test.ts</automated>
  </verify>
  <done>Phase 24 gets a durable and locked formative evaluation data model without introducing a new gradebook subsystem.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add teacher-only server action and input form for the fixed evaluation model</name>
  <files>src/actions/classroom-actions.ts, src/components/classroom/classroom-student-evaluation-form.tsx</files>
  <read_first>src/actions/classroom-actions.ts, src/lib/dto/classroom.ts, src/lib/dal/classroom.ts, src/components/ui/button.tsx, src/components/ui/card.tsx, .planning/phases/24-live-classroom-operations-and-formative-evaluation/24-CONTEXT.md</read_first>
  <behavior>
    - Test 1: teacher writes validate fixed tiers and tags before touching DAL.
    - Test 2: classroom cache invalidates after a successful evaluation write.
    - Test 3: the basic form exposes the exact three tier labels and finite tag chips needed by the detail panel.
  </behavior>
  <action>Create `recordStudentFormativeEvaluationAction` in `src/actions/classroom-actions.ts`, validate with `RecordStudentFormativeEvaluationInputSchema.safeParse`, call `recordStudentFormativeEvaluation`, and run `updateTag(cacheTags.classroom(parsed.data.sessionId))` on success. Map authorization failures to the existing structured classroom action error shape. Create `src/components/classroom/classroom-student-evaluation-form.tsx` as a small client form component that submits the exact three Chinese tier labels `积极参与`, `正常参与`, and `需要关注`, binds to the six fixed tags above as toggle chips, and sends `observationNote` as a textarea with teacher-only copy explaining that this is a process evaluation note rather than a score entry.</action>
  <acceptance_criteria>
    - `src/actions/classroom-actions.ts` contains `recordStudentFormativeEvaluationAction` and `RecordStudentFormativeEvaluationInputSchema.safeParse`.
    - `src/actions/classroom-actions.ts` invalidates `cacheTags.classroom(parsed.data.sessionId)` after successful evaluation writes.
    - `src/components/classroom/classroom-student-evaluation-form.tsx` contains `积极参与`, `正常参与`, `需要关注`.
    - `src/components/classroom/classroom-student-evaluation-form.tsx` contains the six fixed tag labels and `observationNote`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/actions/classroom-actions.test.ts src/components/classroom/classroom-student-evaluation-form.test.tsx</automated>
  </verify>
  <done>The write path is fully teacher-scoped and ready to plug into the `/classroom` student detail panel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teacher runtime form -> server action | only an authenticated teacher for the current session may create evaluation records |
| classroom DAL -> stored evidence payload | evaluation records must keep the fixed model and stay auditable inside classroom evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | S | `recordStudentFormativeEvaluationAction` | mitigate | Reuse `getTeacherSessionScope` via DAL and reject non-teacher writes before persistence. |
| T-24-05 | T | evaluation payload schema | mitigate | Lock participation levels and tags in `z.enum` schemas so the write contract cannot silently drift into a generic grading payload. |
| T-24-06 | R | classroom evidence audit trail | mitigate | Persist evaluation through `recordClassroomEvidence` and keep `capturedById` plus `createdAt` on every entry. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/classroom.test.ts src/actions/classroom-actions.test.ts src/components/classroom/classroom-student-evaluation-form.test.tsx`
</verification>

<success_criteria>
- teachers can record fixed 3-tier participation, tags, and observation notes without leaving the classroom domain.
- evaluation writes remain explicit, durable, and cache-safe.
- Phase 24 does not introduce scores, rubrics, or a second grading backend.
</success_criteria>

<output>
After completion, create `.planning/phases/24-live-classroom-operations-and-formative-evaluation/24-02-SUMMARY.md`
</output>
