---
phase: 24-live-classroom-operations-and-formative-evaluation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dto/classroom.ts
  - src/lib/dal/classroom.ts
  - src/components/classroom/classroom-roster-panel.tsx
  - src/components/classroom/classroom-control-panel.tsx
autonomous: true
requirements:
  - ACT-03
must_haves:
  truths:
    - Phase 24 的主工作流必须继续留在 `/classroom`，运行态 roster monitoring 不能拆到新页面或独立 dashboard。
    - 名册监控必须同时覆盖 presence、progress、submission summary 与需要关注的学生，不能只显示在线状态。
    - 监控 read model 必须直接建立在当前 classroom session 的 durable data 上，不能改成 client 侧拼接或临时状态统计。
  artifacts:
    - path: src/lib/dto/classroom.ts
      provides: live classroom roster monitoring DTO contract
      contains: ClassroomRosterSummaryDTOSchema
    - path: src/lib/dal/classroom.ts
      provides: session-scoped roster monitoring aggregation
      contains: submissionCount
    - path: src/components/classroom/classroom-roster-panel.tsx
      provides: roster summary and attention-first student rows
      contains: 需要关注
  key_links:
    - from: src/lib/dal/classroom.ts
      to: src/components/classroom/classroom-roster-panel.tsx
      via: snapshot adds monitoring summary and per-student attention fields
      pattern: monitoringSummary|needsAttention|submissionCount
    - from: src/components/classroom/classroom-control-panel.tsx
      to: src/components/classroom/classroom-roster-panel.tsx
      via: control panel surfaces the upgraded roster panel inside `/classroom`
      pattern: ClassroomRosterPanel
---

<objective>
把 live classroom 的名册区从“仅在线状态”提升为可运营的 runtime monitoring 面板，
让教师在 `/classroom` 里直接看到学生 presence、progress、submission summary 和需优先关注对象。

Purpose: 把 Phase 24 的监控闭环先落在 snapshot/read model，后续 detail 与评价面板才能复用同一事实源。
Output: expanded classroom DTO/DAL aggregation plus roster monitoring UI.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-live-classroom-operations-and-formative-evaluation/24-CONTEXT.md
@src/app/(classroom)/classroom/page.tsx
@src/components/surfaces/classroom-console-surface.tsx
@src/components/classroom/classroom-control-panel.tsx
@src/components/classroom/classroom-roster-panel.tsx
@src/lib/dto/classroom.ts
@src/lib/dal/classroom.ts
@src/db/schema.ts

<interfaces>
Roster monitoring additions to land in the classroom snapshot:
```ts
type ClassroomRosterSummaryDTO = {
  connectedCount: number
  reconnectingCount: number
  offlineCount: number
  needsAttentionCount: number
  submittedCount: number
}

type ClassroomParticipantDTO = {
  studentId: string
  studentName: string
  connectionState: "connected" | "reconnecting" | "offline"
  currentStepId: string
  lastSeenAt: string
  progressLabel: string
  submissionCount: number
  needsAttention: boolean
  attentionReasons: string[]
}
```

Expected derivation rules:
```ts
needsAttention = connectionState !== "connected" || progressLabel !== "跟随当前环节" || submissionCount === 0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend classroom snapshot DTOs and DAL aggregation for runtime monitoring</name>
  <files>src/lib/dto/classroom.ts, src/lib/dal/classroom.ts</files>
  <read_first>src/lib/dto/classroom.ts, src/lib/dal/classroom.ts, src/db/schema.ts, .planning/phases/24-live-classroom-operations-and-formative-evaluation/24-CONTEXT.md, .planning/phases/23-student-in-class-activity-flow/23-CONTEXT.md</read_first>
  <behavior>
    - Test 1: classroom snapshot exposes a roster-level monitoring summary instead of forcing UI code to count everything client-side.
    - Test 2: each participant row includes progress/submission/attention fields derived from the current session.
    - Test 3: attention derivation remains session-scoped and teacher-readable without creating a new gradebook table.
  </behavior>
  <action>Update `src/lib/dto/classroom.ts` to add `ClassroomRosterSummaryDTOSchema` plus participant fields `progressLabel`, `submissionCount`, `needsAttention`, and `attentionReasons`. In `src/lib/dal/classroom.ts`, keep `getClassroomSnapshotDTO({ sessionId })` as the single read entry and compute these fields from existing `classroomParticipants`, `classroomEvidence`, and published step ordering. Use student evidence rows whose `sourceType` is `student-quick-response` or `student-submission` as submission facts. Derive `progressLabel` with exact states `跟随当前环节` / `落后于当前环节` / `已进入后续环节`, and mark `needsAttention` when the participant is offline or reconnecting, or when the participant is behind the active step, or when the participant has `submissionCount === 0` while the current step type is `task` or `quiz`. Add `monitoringSummary` onto `ClassroomSnapshotDTO` so the UI reads one stable DTO instead of recounting raw arrays in the client.</action>
  <acceptance_criteria>
    - `src/lib/dto/classroom.ts` contains `ClassroomRosterSummaryDTOSchema` and participant fields `progressLabel`, `submissionCount`, `needsAttention`, `attentionReasons`.
    - `src/lib/dal/classroom.ts` contains `monitoringSummary` and counts `needsAttentionCount` plus `submittedCount`.
    - `src/lib/dal/classroom.ts` derives submissions from `student-quick-response` or `student-submission` evidence rows.
    - `src/lib/dal/classroom.ts` contains the exact progress labels `跟随当前环节`, `落后于当前环节`, and `已进入后续环节`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/classroom.test.ts</automated>
  </verify>
  <done>Classroom snapshot becomes the authoritative monitoring read model for roster operations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade `/classroom` roster UI to show attention-first monitoring cards and student row signals</name>
  <files>src/components/classroom/classroom-roster-panel.tsx, src/components/classroom/classroom-control-panel.tsx</files>
  <read_first>src/components/classroom/classroom-roster-panel.tsx, src/components/classroom/classroom-control-panel.tsx, src/components/surfaces/classroom-console-surface.tsx, src/lib/dto/classroom.ts, .planning/phases/24-live-classroom-operations-and-formative-evaluation/24-CONTEXT.md</read_first>
  <behavior>
    - Test 1: roster top cards show connection and submission monitoring, not only online counts.
    - Test 2: rows make attention status visually explicit and keep the teacher on the current `/classroom` runtime surface.
    - Test 3: control panel summary copy reflects that roster now supports intervention triage.
  </behavior>
  <action>Update `src/components/classroom/classroom-roster-panel.tsx` so the top metrics display `已连接`, `重连中`, `需要关注`, and `已提交`. For each participant row, render `progressLabel`, `submissionCount`, and an attention chip that uses the exact label `需要关注` when `needsAttention` is true; otherwise show `状态稳定`. Render every `attentionReason` as inline tonal tags, and keep the panel subtitle focused on `课堂名册、进度与课堂回应概览`. In `src/components/classroom/classroom-control-panel.tsx`, replace the current `completionRate` hero metric wording with monitoring language that references roster progress and student attention rather than generic活跃度 percentage.</action>
  <acceptance_criteria>
    - `src/components/classroom/classroom-roster-panel.tsx` contains `已提交`, `需要关注`, `状态稳定`, and `课堂名册、进度与课堂回应概览`.
    - `src/components/classroom/classroom-roster-panel.tsx` renders `progressLabel`, `submissionCount`, and `attentionReasons`.
    - `src/components/classroom/classroom-control-panel.tsx` no longer labels the third hero orb as `课堂活跃度` and instead references roster monitoring or intervention priority.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/classroom.test.ts src/components/classroom/classroom-roster-panel.test.tsx</automated>
  </verify>
  <done>The roster panel becomes a live operations surface instead of a passive attendance list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable classroom evidence -> monitoring summary | roster metrics must reflect session-scoped facts rather than client-side guesses |
| snapshot DTO -> runtime UI | attention indicators drive teacher intervention priority and must not be silently omitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | T | `src/lib/dal/classroom.ts` | mitigate | Derive monitoring fields from `classroomParticipants` and `classroomEvidence` only, and keep the logic in the server-side snapshot builder. |
| T-24-02 | I | `src/components/classroom/classroom-roster-panel.tsx` | mitigate | Render explicit `needsAttention` and `attentionReasons` labels so critical runtime signals are not hidden behind generic badges. |
| T-24-03 | D | snapshot aggregation | accept | Session-scoped evidence reads may grow, but Phase 24 keeps them limited to the active classroom session only. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/classroom.test.ts src/components/classroom/classroom-roster-panel.test.tsx`
</verification>

<success_criteria>
- `/classroom` roster shows live presence, progress, submission, and attention summaries.
- teachers can identify students needing intervention without opening another page.
- monitoring remains backed by the session-scoped classroom snapshot contract.
</success_criteria>

<output>
After completion, create `.planning/phases/24-live-classroom-operations-and-formative-evaluation/24-01-SUMMARY.md`
</output>
