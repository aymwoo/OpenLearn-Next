---
phase: 18-teaching-schedule-os
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - src/lib/dal/schedule-runtime.ts
  - src/lib/dal/schedule-runtime.test.ts
  - src/components/surfaces/teacher-schedule-surface.tsx
  - src/components/surfaces/teacher-schedule-surface.test.tsx
  - src/app/(teacher)/teacher/schedule/page.tsx
autonomous: true
requirements:
  - SCHEDULE-02
  - SCHEDULE-03
  - SCHEDULE-09
must_haves:
  truths:
    - D-04/D-05: 教师个人日程是首个主 runtime surface，卡片第一层固定显示时间、班级、地点、状态。
    - D-06: 课程/教案联动只作为次级信息，不压过 runtime scheduling information。
    - D-13/D-14: daily agenda 由 teacher/class scoped DAL deterministic 生成，并带 explicit cache tags。
  artifacts:
    - path: src/lib/dal/schedule-runtime.ts
      provides: deterministic daily agenda engine
      contains: getTeacherDailyAgendaDTO
    - path: src/components/surfaces/teacher-schedule-surface.tsx
      provides: teacher-first agenda UI
      contains: 今天还没有生成可执行课表
    - path: src/app/(teacher)/teacher/schedule/page.tsx
      provides: teacher schedule entry route
      contains: TeacherScheduleSurface
  key_links:
    - from: src/lib/dal/schedule-runtime.ts
      to: src/components/surfaces/teacher-schedule-surface.tsx
      via: one runtime DTO powers the teacher surface
      pattern: TeacherDailyAgendaDTOSchema
    - from: src/lib/dal/schedule-runtime.ts
      to: src/lib/cache-policy.ts
      via: cached agenda reads use explicit schedule tags
      pattern: teacherScheduleAgenda|classScheduleAgenda
---

<objective>
实现真正的 runtime daily agenda engine，并让教师端先拥有一个可解释、可缓存、可回归的个人日程主视图。

Purpose: 兑现 D-04/D-05/D-06，让课表系统先服务“教师今天要给谁上课、在哪里上、有没有变更”。
Output: runtime agenda DAL、teacher daily agenda surface、`/teacher/schedule` route。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-teaching-schedule-os/18-CONTEXT.md
@.planning/phases/18-teaching-schedule-os/18-RESEARCH.md
@.planning/phases/18-teaching-schedule-os/18-UI-SPEC.md
@.planning/phases/18-teaching-schedule-os/18-PATTERNS.md
@.planning/phases/18-teaching-schedule-os/18-01-PLAN.md
@src/lib/dal/classroom.ts
@src/components/surfaces/teacher-dashboard-surface.tsx

<interfaces>
Use a runtime DTO shape analogous to existing classroom snapshots:
```ts
type TeacherDailyAgendaCardDTO = {
  id: string;
  timeLabel: string;
  classLabel: string;
  locationLabel: string;
  status: "正常" | "进行中" | "即将开始" | "已变更" | "代课" | "停课";
  courseTitle: string;
  overrideSummary: string | null;
};
```

The page should stay thin like `src/app/(teacher)/teacher/page.tsx`: load DTO, render surface.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the deterministic daily agenda engine for teacher and class views</name>
  <files>src/lib/dal/schedule-runtime.ts, src/lib/dal/schedule-runtime.test.ts</files>
  <read_first>src/lib/dto/schedule.ts, src/lib/dal/classroom.ts, src/lib/cache-policy.ts, .planning/phases/18-teaching-schedule-os/18-RESEARCH.md</read_first>
  <behavior>
    - Test 1: per D-04/D-05, teacher agenda for a day returns cards with `timeLabel`, `classLabel`, `locationLabel`, and `status` as the first information layer.
    - Test 2: per D-07/D-09, a single-instance override changes only the targeted date and preserves base recurring entries for other dates.
    - Test 3: per SCHEDULE-03/SCHEDULE-05, holidays and non-teaching days participate in agenda generation and can cancel agenda cards deterministically.
  </behavior>
  <action>Create `src/lib/dal/schedule-runtime.ts` with cached, server-only read functions `getTeacherDailyAgendaDTO({ date, teacherId? })` and `getClassDailyAgendaDTO({ date, classId })`. The engine must read only normalized schedule tables, holiday dates, and single-instance overrides; do not read `scheduleImportRow` for runtime output. Apply an explicit precedence order: holiday/non-teaching day blocks base recurring entries, override replaces or cancels only the targeted service date, otherwise recurring schedule wins. Use `cacheTag(cacheTags.teacherScheduleAgenda(actorId,dateKey))` and `cacheTag(cacheTags.classScheduleAgenda(classId,dateKey))` on cached reads. Keep lesson/course links and materials as secondary metadata only per D-06, not the primary status block.</action>
  <acceptance_criteria>
    - `src/lib/dal/schedule-runtime.ts` contains `getTeacherDailyAgendaDTO` and `getClassDailyAgendaDTO`.
    - `src/lib/dal/schedule-runtime.ts` does not reference `scheduleImportRow` in runtime output assembly.
    - `src/lib/dal/schedule-runtime.ts` contains `teacherScheduleAgenda` and `classScheduleAgenda` cache tag usage.
    - `src/lib/dal/schedule-runtime.test.ts` covers holiday and override precedence.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/schedule-runtime.test.ts</automated>
  </verify>
  <done>The system can deterministically materialize daily teacher and class agendas from normalized schedule data without leaking import-layer details.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship the teacher-first daily agenda surface and route</name>
  <files>src/components/surfaces/teacher-schedule-surface.tsx, src/components/surfaces/teacher-schedule-surface.test.tsx, src/app/(teacher)/teacher/schedule/page.tsx</files>
  <read_first>src/components/surfaces/teacher-dashboard-surface.tsx, src/app/(teacher)/teacher/page.tsx, src/lib/dto/schedule.ts, .planning/phases/18-teaching-schedule-os/18-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: per D-04/D-05, the surface uses a single agenda hero and renders timeline cards whose first visible fields are time, class, location, and status.
    - Test 2: per D-06, course/lesson links appear only as secondary actions inside cards.
    - Test 3: empty and error states use the exact Chinese copy from the UI-SPEC.
  </behavior>
  <action>Create `/teacher/schedule/page.tsx` as a thin page that loads the teacher daily agenda DTO and renders `TeacherScheduleSurface`. In `src/components/surfaces/teacher-schedule-surface.tsx`, follow the UI-SPEC exactly: one agenda hero with date/week summary/next class countdown, a vertical timeline or card flow for today's agenda, and a secondary support region for changes/reminders/AI suggestions. Each agenda card must show `时间 / 班级 / 地点 / 状态` before any lesson/course linkage. Use the exact empty-state heading/body `今天还没有生成可执行课表` and `先导入并审核课表数据，或检查今天是否被设置为节假日/非教学日。`. Do not render this page as a spreadsheet-style operations dashboard, and do not create multiple competing hero sections.</action>
  <acceptance_criteria>
    - `src/components/surfaces/teacher-schedule-surface.tsx` contains `今天还没有生成可执行课表`.
    - `src/components/surfaces/teacher-schedule-surface.tsx` contains agenda card fields for time, class, location, and status.
    - `src/app/(teacher)/teacher/schedule/page.tsx` imports `TeacherScheduleSurface`.
    - `src/components/surfaces/teacher-schedule-surface.test.tsx` asserts the empty state and teacher-first card hierarchy.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/surfaces/teacher-schedule-surface.test.tsx</automated>
  </verify>
  <done>Teachers have a first-class daily agenda page that centers today's schedule runtime instead of hiding it behind generic dashboard widgets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| normalized schedule model → runtime engine | Trusted schedule data is materialized into teacher/class agendas |
| runtime DTO → teacher schedule surface | UI must display deterministic, sanitized agenda cards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-09 | T | `src/lib/dal/schedule-runtime.ts` | mitigate | Centralize precedence logic for holidays and overrides in one runtime engine instead of per-surface branching. |
| T-18-10 | I | `src/components/surfaces/teacher-schedule-surface.tsx` | mitigate | Render only DTO fields; keep import rows and internal lineage out of the teacher-facing surface. |
| T-18-11 | D | `src/lib/dal/schedule-runtime.ts` | mitigate | Cache per teacher/class/date tags so expensive agenda reads are explicit and refreshable after writes. |
| T-18-12 | E | `src/app/(teacher)/teacher/schedule/page.tsx` | mitigate | Route remains teacher-scoped and server-rendered via existing auth boundary, not a public runtime endpoint. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/schedule-runtime.test.ts`
- `pnpm test --run src/components/surfaces/teacher-schedule-surface.test.tsx`
</verification>

<success_criteria>
- The daily agenda engine deterministically combines recurring schedule, holidays, and overrides.
- Teachers can open `/teacher/schedule` and immediately understand today's schedule from the first information layer.
- Runtime agendas are cached and invalidated explicitly through schedule-specific tags.
</success_criteria>

<output>
After completion, create `.planning/phases/18-teaching-schedule-os/18-03-SUMMARY.md`
</output>
