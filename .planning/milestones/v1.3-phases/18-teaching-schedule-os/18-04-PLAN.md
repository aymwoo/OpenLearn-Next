---
phase: 18-teaching-schedule-os
plan: 04
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-03
files_modified:
  - src/lib/dal/schedule-operations.ts
  - src/lib/dal/schedule-operations.test.ts
  - src/actions/schedule-operations-actions.ts
  - src/components/surfaces/schedule-operations-surface.tsx
  - src/components/surfaces/schedule-operations-surface.test.tsx
  - src/app/(teacher)/teacher/schedule/changes/page.tsx
autonomous: true
requirements:
  - SCHEDULE-04
  - SCHEDULE-05
  - SCHEDULE-09
must_haves:
  truths:
    - D-07/D-08: 调课首发只做单次覆盖，并且动作只限代课、停课、换时间/教室。
    - D-09: override 永远保留原始 recurring lineage、生效日期与审计记录。
    - SCHEDULE-05/D-14: holiday / non-teaching day 保存后立即参与 agenda generation，并显式失效相关 tags。
  artifacts:
    - path: src/lib/dal/schedule-operations.ts
      provides: override + holiday audited mutations
      contains: createScheduleOverride
    - path: src/actions/schedule-operations-actions.ts
      provides: teacher-scoped mutation entrypoints
      contains: SCHEDULE_OVERRIDE_BLOCKED
    - path: src/components/surfaces/schedule-operations-surface.tsx
      provides: override and holiday management UI
      contains: 生效日期
  key_links:
    - from: src/actions/schedule-operations-actions.ts
      to: src/lib/dal/schedule-operations.ts
      via: validated override/holiday server actions
      pattern: createScheduleOverride|saveHolidayCalendar
    - from: src/lib/dal/schedule-operations.ts
      to: src/lib/cache-policy.ts
      via: mutation invalidates affected agenda/calendar tags
      pattern: teacherScheduleAgenda|classScheduleAgenda|scheduleCalendar
---

<objective>
把 Phase 18 的运行时变更能力收敛成 audited single-instance override 与 holiday management：既能调课，也不破坏基础 recurring schedule。

Purpose: 兑现 D-07/D-08/D-09 和 SCHEDULE-04/SCHEDULE-05，而不是让课表一有变更就重写基表。
Output: override/holiday DAL、server actions、teacher changes surface、`/teacher/schedule/changes` route。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-teaching-schedule-os/18-CONTEXT.md
@.planning/phases/18-teaching-schedule-os/18-RESEARCH.md
@.planning/phases/18-teaching-schedule-os/18-UI-SPEC.md
@.planning/phases/18-teaching-schedule-os/18-03-PLAN.md
@src/actions/lesson-authoring-actions.ts
@src/lib/dal/classroom.ts
@src/lib/dto/schedule.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement audited single-instance overrides and holiday mutations</name>
  <files>src/lib/dal/schedule-operations.ts, src/lib/dal/schedule-operations.test.ts, src/actions/schedule-operations-actions.ts</files>
  <read_first>src/lib/dto/schedule.ts, src/lib/cache-policy.ts, src/actions/lesson-authoring-actions.ts, .planning/phases/18-teaching-schedule-os/18-RESEARCH.md</read_first>
  <behavior>
    - Test 1: per D-07/D-08, override creation accepts only `substitute`, `cancel`, and `move`-style single-instance actions for one service date.
    - Test 2: per D-09, saving an override stores original recurring lineage and does not mutate the base recurring entry for other dates.
    - Test 3: per SCHEDULE-05, holiday/non-teaching/make-up day mutations update runtime generation inputs and invalidate affected agenda tags.
  </behavior>
  <action>Create `src/lib/dal/schedule-operations.ts` with teacher-scoped mutation functions for: `createScheduleOverride`, `updateScheduleOverride`, `revokeScheduleOverride`, `saveHolidayCalendarDate`, and `removeHolidayCalendarDate`. The override input must require `effectiveDate`, `reason`, and one of the exact action families from D-08: substitute teacher, cancel class, or move time/room. Persist original lineage fields such as `recurringEntryId`, `originalTeacherId`, `originalBellSlotId`, `originalRoomLabel`, and `effectiveDate` on the override record, and append a `scheduleMutationAudit` entry for every create/update/revoke. Then add `src/actions/schedule-operations-actions.ts` using `safeParse + assertActiveTeacher + updateTag()` for all affected agenda/calendar tags. When the mutation would break scope, target date, or recurrence rules, return a structured error such as `SCHEDULE_OVERRIDE_BLOCKED`; do not fall back to silent no-op writes.</action>
  <acceptance_criteria>
    - `src/lib/dal/schedule-operations.ts` contains `effectiveDate` and `recurringEntryId` lineage handling.
    - `src/lib/dal/schedule-operations.ts` contains override actions limited to `substitute`, `cancel`, and `move`.
    - `src/actions/schedule-operations-actions.ts` contains `SCHEDULE_OVERRIDE_BLOCKED` and `updateTag(`.
    - `src/lib/dal/schedule-operations.test.ts` asserts the base recurring record is unchanged after a single-instance override.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/schedule-operations.test.ts</automated>
  </verify>
  <done>Schedule changes are now modeled as audited per-date overlays instead of destructive edits to the base timetable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the changes and holiday management surface with inline read-your-writes feedback</name>
  <files>src/components/surfaces/schedule-operations-surface.tsx, src/components/surfaces/schedule-operations-surface.test.tsx, src/app/(teacher)/teacher/schedule/changes/page.tsx</files>
  <read_first>src/components/surfaces/teacher-schedule-surface.tsx, src/lib/dto/schedule.ts, .planning/phases/18-teaching-schedule-os/18-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: the override form shows the original schedule summary, action type, effective date, and audit explanation before save.
    - Test 2: the surface exposes only the three allowed single-instance actions: `代课`, `停课`, `换时间/教室`.
    - Test 3: holiday updates and override saves show inline success/error regions instead of toast-only feedback.
  </behavior>
  <action>Create `/teacher/schedule/changes/page.tsx` to render a combined `ScheduleOperationsSurface` for single-instance override management and holiday calendar management. The override form must pin the original schedule summary at the top with original teacher/time/room, then expose only the three exact actions from D-08: `代课`, `停课`, and `换时间/教室`. Every form must display `生效日期` and an audit explanation block. In the same surface, add a term-aware holiday/non-teaching calendar section that shows whether a date is teaching, make-up, or non-teaching and saves into runtime inputs rather than UI-only annotations. After save, show an inline success/error region inside the current surface and refresh the summary cards immediately; do not rely on ephemeral toast-only confirmation.</action>
  <acceptance_criteria>
    - `src/components/surfaces/schedule-operations-surface.tsx` contains `代课`, `停课`, and `换时间/教室`.
    - `src/components/surfaces/schedule-operations-surface.tsx` contains `生效日期` and an audit/record explanation block.
    - `src/app/(teacher)/teacher/schedule/changes/page.tsx` imports `ScheduleOperationsSurface`.
    - `src/components/surfaces/schedule-operations-surface.test.tsx` asserts inline success/error rendering after save attempts.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/surfaces/schedule-operations-surface.test.tsx</automated>
  </verify>
  <done>Authorized teachers can change one class occurrence or mark holiday exceptions without losing the original schedule truth or context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teacher mutation input → override/holiday DAL | High-impact schedule changes cross into trusted runtime state |
| override/holiday mutations → runtime agenda engine | Per-date changes affect daily agenda materialization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-13 | T | `src/lib/dal/schedule-operations.ts` | mitigate | Model changes as per-date override records with original lineage instead of rewriting recurring entries. |
| T-18-14 | R | `src/lib/dal/schedule-operations.ts` | mitigate | Write `scheduleMutationAudit` records on create/update/revoke of overrides and holiday dates. |
| T-18-15 | E | `src/actions/schedule-operations-actions.ts` | mitigate | Gate all operations behind teacher scope checks and structured blocked errors. |
| T-18-16 | D | `src/components/surfaces/schedule-operations-surface.tsx` | mitigate | Show exact action scope and effective date before confirmation to reduce accidental destructive changes. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/schedule-operations.test.ts`
- `pnpm test --run src/components/surfaces/schedule-operations-surface.test.tsx`
</verification>

<success_criteria>
- Overrides are single-instance only and keep original schedule lineage.
- Holiday/non-teaching changes participate in agenda generation instead of living only in UI state.
- Teachers receive inline read-your-writes feedback for schedule change operations.
</success_criteria>

<output>
After completion, create `.planning/phases/18-teaching-schedule-os/18-04-SUMMARY.md`
</output>
