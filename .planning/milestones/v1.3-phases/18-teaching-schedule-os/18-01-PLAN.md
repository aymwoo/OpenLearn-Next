---
phase: 18-teaching-schedule-os
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dto/schedule.ts
  - src/db/schema.ts
  - src/lib/cache-policy.ts
autonomous: true
requirements:
  - SCHEDULE-02
  - SCHEDULE-09
must_haves:
  truths:
    - D-01/D-03: 原始导入数据只能停留在 staging/import contracts 中，runtime agenda 与 surface 不直接消费 raw rows。
    - D-13/D-14: schedule domain 从第一天起就有 DTO、DAL cache tags 与 explicit invalidation contract。
    - D-15: schedule reminder / assistant / plugin 扩展只能沿 allowlisted、audited contract 前进。
  artifacts:
    - path: src/lib/dto/schedule.ts
      provides: 统一 schedule 输入和 DTO contract
      contains: ScheduleImportRowStatusSchema
    - path: src/db/schema.ts
      provides: import layer + normalized model + runtime support tables
      contains: scheduleImportBatch
    - path: src/lib/cache-policy.ts
      provides: schedule cache tags
      contains: teacherScheduleAgenda
  key_links:
    - from: src/lib/dto/schedule.ts
      to: src/actions/schedule-import-actions.ts
      via: import/review/apply inputs reuse one contract
      pattern: ScheduleImportDraftInputSchema
    - from: src/db/schema.ts
      to: src/lib/dal/schedule-runtime.ts
      via: runtime engine reads recurring entries, overrides, holidays
      pattern: scheduleRecurringEntry|scheduleOverride|scheduleHolidayDate
---

<objective>
先把课表系统的共享 contract 锁死：统一 DTO / input schema、统一 cache tags、统一 Drizzle schema，确保后续 5 个 plans 都围绕同一套三层边界实现。

Purpose: 让 Phase 18 从基础层就满足 D-01/D-03/D-13/D-15，而不是后面再补“边界修复”。
Output: `src/lib/dto/schedule.ts`、扩展后的 `src/db/schema.ts`、扩展后的 `src/lib/cache-policy.ts`。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-teaching-schedule-os/18-CONTEXT.md
@.planning/phases/18-teaching-schedule-os/18-RESEARCH.md
@.planning/phases/18-teaching-schedule-os/18-PATTERNS.md
@src/db/schema.ts
@src/lib/cache-policy.ts

<interfaces>
From `src/lib/dto/course-authoring.ts`:
```ts
export const CourseCreateInputSchema = z.object({ ... }).strict();
export const TeacherCourseCenterDTOSchema = z.object({ ... });
```

From `src/lib/dto/classroom.ts`:
```ts
export const ClassroomSnapshotDTOSchema = z.object({ ... });
export const ClassroomActionResultDTOSchema = z.object({ ... });
```

Use the same pattern in `src/lib/dto/schedule.ts`: small schemas first, composed DTOs second, exported types last.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the full shared schedule contract file first</name>
  <files>src/lib/dto/schedule.ts</files>
  <read_first>src/lib/dto/course-authoring.ts, src/lib/dto/classroom.ts, .planning/phases/18-teaching-schedule-os/18-CONTEXT.md, .planning/phases/18-teaching-schedule-os/18-RESEARCH.md</read_first>
  <action>Create `src/lib/dto/schedule.ts` as the single shared contract file for the whole phase. Export exact schemas and types for: `ScheduleImportBatchStatusSchema`, `ScheduleImportRowStatusSchema`, `ScheduleOverrideActionSchema`, `ScheduleReminderTypeSchema`, `ScheduleReminderStatusSchema`, `ScheduleAssistantProposalStatusSchema`, `ScheduleImportDraftInputSchema`, `ApproveScheduleImportInputSchema`, `TeacherDailyAgendaCardDTOSchema`, `TeacherDailyAgendaDTOSchema`, `ClassDailyAgendaCardDTOSchema`, `ScheduleOverrideInputSchema`, `ScheduleHolidayDateInputSchema`, `ScheduleReminderRuleInputSchema`, and `ScheduleAssistantProposalDTOSchema`. Per D-01/D-03, include row-level review fields such as `validationIssues`, `mappingSummary`, `conflictSummary`, `approvalState`, and `sourceRowKey`; do not define any DTO that exposes raw import payloads as teacher-facing runtime data. Per D-07/D-08, lock override action values to `substitute`, `cancel`, and `move`. Per D-10/D-11, lock assistant proposals to proposal/draft states only, not applied runtime writes.</action>
  <acceptance_criteria>
    - `src/lib/dto/schedule.ts` contains `ScheduleImportRowStatusSchema`.
    - `src/lib/dto/schedule.ts` contains `pending_review`, `validation_failed`, `conflict_review`, `ready_to_apply`, `approved`, and `rejected`.
    - `src/lib/dto/schedule.ts` contains `TeacherDailyAgendaDTOSchema` and `ClassDailyAgendaCardDTOSchema`.
    - `src/lib/dto/schedule.ts` contains `ScheduleAssistantProposalDTOSchema` and does not contain a direct `applyScheduleChange` action schema.
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>The schedule phase has a single source-of-truth DTO/input contract that downstream plans can implement against without reopening interface design.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the SQLite schema and cache tags for the three-layer schedule architecture</name>
  <files>src/db/schema.ts, src/lib/cache-policy.ts</files>
  <read_first>src/db/schema.ts, src/lib/cache-policy.ts, .planning/phases/18-teaching-schedule-os/18-CONTEXT.md, .planning/phases/18-teaching-schedule-os/18-RESEARCH.md</read_first>
  <action>Extend `src/db/schema.ts` with the exact schedule table set required by the research baseline: `scheduleImportBatch`, `scheduleImportRow`, `scheduleTerm`, `scheduleWeekPattern`, `scheduleBellSlot`, `scheduleTeachingAssignment`, `scheduleRecurringEntry`, `scheduleOverride`, `scheduleHolidayCalendar`, `scheduleHolidayDate`, `scheduleReminderRule`, `scheduleMutationAudit`, and `scheduleAssistantProposal`. All parent-owned foreign keys must use `{ onDelete: "cascade" }` per project constraints. Add indexes/unique indexes for high-frequency lookups: `(batchId,status)`, `(schoolId,effectiveDate)`, `(teacherId,date)`-supporting fields, `(classId,date)`-supporting fields, and uniqueness for recurring entry identity within `(assignmentId, weekPatternId, weekday, bellSlotId)`. Then extend `src/lib/cache-policy.ts` with exact tag helpers for `scheduleImportBatch`, `scheduleImportSchool`, `teacherScheduleAgenda`, `classScheduleAgenda`, `scheduleCalendar`, `scheduleReminder`, and `scheduleAssistantProposal` per D-13/D-14. Do not add implicit caching or client-owned state contracts here.</action>
  <acceptance_criteria>
    - `src/db/schema.ts` contains `scheduleImportBatch`, `scheduleImportRow`, and `scheduleRecurringEntry`.
    - `src/db/schema.ts` contains `scheduleOverride`, `scheduleHolidayDate`, `scheduleReminderRule`, and `scheduleAssistantProposal`.
    - `src/lib/cache-policy.ts` contains `teacherScheduleAgenda` and `classScheduleAgenda`.
    - `src/lib/cache-policy.ts` contains `scheduleImportBatch` and `scheduleReminder`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>The database and cache layer can now represent import, normalized schedule, overrides, holidays, reminders, and assistant proposals without inventing parallel infrastructure later.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push the new Drizzle schema before downstream implementation</name>
  <files>src/db/schema.ts</files>
  <read_first>src/db/schema.ts, package.json</read_first>
  <action>After Tasks 1 and 2 are complete, run `npx drizzle-kit push` from the repository root so the local SQLite schema matches the new schedule tables before any DAL or action code is implemented. This task is mandatory because later verification can false-pass against TypeScript-only contracts if the live database has not been updated. If the command reports a blocking interactive prompt, resolve it without using destructive shortcuts beyond the documented Drizzle push flow.</action>
  <acceptance_criteria>
    - `npx drizzle-kit push` exits with status 0.
    - The push command completes after the schedule tables were added to `src/db/schema.ts`.
  </acceptance_criteria>
  <verify>
    <automated>npx drizzle-kit push</automated>
  </verify>
  <done>The live local database schema matches the code contract, so downstream schedule DAL/tests verify against real tables instead of stale SQLite state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import staging → normalized model | Untrusted timetable rows cross into trusted schedule tables only after review |
| server DTO/cache layer → UI surfaces | Teacher-facing schedule UI must consume sanitized DTOs, not raw import payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | T | `src/db/schema.ts` | mitigate | Keep raw import rows in dedicated staging tables and add lineage-specific normalized tables instead of reusing one mutable record shape. |
| T-18-02 | I | `src/lib/dto/schedule.ts` | mitigate | Omit raw import payload DTOs from teacher runtime exports; expose review summaries instead. |
| T-18-03 | E | `src/lib/cache-policy.ts` | mitigate | Define explicit schedule tags so only server actions can invalidate trusted schedule reads. |
| T-18-04 | R | `src/db/schema.ts` | mitigate | Add `scheduleMutationAudit` and proposal tables so later writes keep actor/date/reason lineage. |
</threat_model>

<verification>
- `pnpm typecheck`
- `npx drizzle-kit push`
</verification>

<success_criteria>
- Phase 18 has one shared schedule contract file that all later plans can import.
- The database contains explicit staging, normalized, override, holiday, reminder, and proposal tables.
- Cache tags exist for import batches, agendas, calendars, reminders, and assistant proposals.
</success_criteria>

<output>
After completion, create `.planning/phases/18-teaching-schedule-os/18-01-SUMMARY.md`
</output>
