---
phase: 18-teaching-schedule-os
plan: 05
type: execute
wave: 4
depends_on:
  - 18-03
  - 18-04
files_modified:
  - src/lib/dal/schedule-reminders.ts
  - src/lib/dal/schedule-reminders.test.ts
  - src/actions/schedule-reminder-actions.ts
  - src/server/schedule/reminder-dispatch.ts
  - src/components/surfaces/schedule-reminder-surface.tsx
  - src/app/(teacher)/teacher/schedule/reminders/page.tsx
autonomous: true
requirements:
  - SCHEDULE-06
  - SCHEDULE-09
must_haves:
  truths:
    - D-12: 首发 reminder 只覆盖开课前提醒和调课变更提醒，不扩到家长/学生外发。
    - D-13/D-14: reminder rule 保存、dispatch 状态变化和 agenda-related change 都通过 server-only boundary 与 explicit tag invalidation 生效。
    - SCHEDULE-06: 系统能为 upcoming classes 和 schedule changes 产出可跟踪的发送计划与发送结果状态。
  artifacts:
    - path: src/lib/dal/schedule-reminders.ts
      provides: reminder rule + delivery state orchestration
      contains: getScheduleReminderCenterDTO
    - path: src/server/schedule/reminder-dispatch.ts
      provides: notification boundary adapter
      contains: pre_class
    - path: src/components/surfaces/schedule-reminder-surface.tsx
      provides: reminder configuration UI
      contains: 开课前提醒
  key_links:
    - from: src/actions/schedule-reminder-actions.ts
      to: src/server/schedule/reminder-dispatch.ts
      via: saved reminder rules trigger allowlisted dispatch planning
      pattern: pre_class|schedule_change
    - from: src/lib/dal/schedule-reminders.ts
      to: src/lib/cache-policy.ts
      via: reminder writes invalidate reminder and agenda summaries
      pattern: scheduleReminder
---

<objective>
把课表提醒从“概念字段”落成真正可配置、可发送、可回溯的 reminder orchestration，并严格限制首发只做开课前和调课变更两类提醒。

Purpose: 兑现 D-12 与 SCHEDULE-06，同时守住通知边界，不把 phase 范围扩展到家长/学生广播系统。
Output: reminder DAL、dispatch adapter、server actions、reminder surface、`/teacher/schedule/reminders` route。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-teaching-schedule-os/18-CONTEXT.md
@.planning/phases/18-teaching-schedule-os/18-RESEARCH.md
@.planning/phases/18-teaching-schedule-os/18-UI-SPEC.md
@src/server/mcp/registry.ts
@src/lib/dal/mcp.ts
@src/lib/dto/schedule.ts
@src/lib/dto/resource-ai.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement reminder rules and allowlisted dispatch orchestration</name>
  <files>src/lib/dal/schedule-reminders.ts, src/lib/dal/schedule-reminders.test.ts, src/actions/schedule-reminder-actions.ts, src/server/schedule/reminder-dispatch.ts</files>
  <read_first>src/server/mcp/registry.ts, src/lib/dal/mcp.ts, src/lib/dto/schedule.ts, src/lib/cache-policy.ts, .planning/phases/18-teaching-schedule-os/18-RESEARCH.md</read_first>
  <behavior>
    - Test 1: per D-12, only `pre_class` and `schedule_change` reminder types are eligible in the first release.
    - Test 2: dispatch attempts transition through `planned`, `sent`, `failed`, and `retry_required` instead of pretending every reminder was delivered.
    - Test 3: reminder dispatch uses the existing notification boundary rather than embedding provider secrets or direct network calls in UI/action code.
  </behavior>
  <action>Create `src/lib/dal/schedule-reminders.ts` to manage reminder rules, eligible reminder instances, delivery state, and center DTO reads. Add a server-only adapter `src/server/schedule/reminder-dispatch.ts` that accepts allowlisted channels only and routes them through the existing notification boundary patterns from the MCP registry; do not place provider secrets in schedule DTOs or server action inputs. Lock the reminder types exactly to `pre_class` and `schedule_change` per D-12, and lock delivery states to `planned`, `sent`, `failed`, and `retry_required`. In `src/actions/schedule-reminder-actions.ts`, use `safeParse + assertActiveTeacher + updateTag()` and return structured errors when a rule targets an unsupported audience/channel. Do not add parent/student broadcast flows or morning digest reminders; those are explicitly deferred.</action>
  <acceptance_criteria>
    - `src/lib/dal/schedule-reminders.ts` contains `pre_class` and `schedule_change`.
    - `src/lib/dal/schedule-reminders.ts` contains `planned`, `sent`, `failed`, and `retry_required`.
    - `src/server/schedule/reminder-dispatch.ts` exists and references allowlisted provider/capability routing instead of secret material.
    - `src/lib/dal/schedule-reminders.test.ts` asserts unsupported reminder types are rejected.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/schedule-reminders.test.ts</automated>
  </verify>
  <done>The schedule system can produce and track reminder deliveries for upcoming classes and schedule changes without escaping the existing notification boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the reminder center surface with explicit delivery-state feedback</name>
  <files>src/components/surfaces/schedule-reminder-surface.tsx, src/app/(teacher)/teacher/schedule/reminders/page.tsx</files>
  <read_first>src/components/surfaces/teacher-schedule-surface.tsx, src/lib/dto/schedule.ts, .planning/phases/18-teaching-schedule-os/18-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: the surface presents only the two first-release reminder categories and their trigger/object/channel configuration.
    - Test 2: status chips distinguish `已计划`, `发送成功`, `发送失败`, and `需重试`.
    - Test 3: saves show visible success/error feedback inside the surface, not only a toast.
  </behavior>
  <action>Create `/teacher/schedule/reminders/page.tsx` as a thin page that renders `ScheduleReminderSurface`. In `src/components/surfaces/schedule-reminder-surface.tsx`, implement the card-based reminder center from the UI-SPEC: cards for `开课前提醒` and `调课变更提醒`, each showing trigger offset/time, target audience scope, channel, and latest execution state. Use the exact Chinese state labels `已计划`, `发送成功`, `发送失败`, and `需重试`. Keep the layout tonal and secondary, not as a competing hero. After save, render a success/error region in the current surface and refresh summary cards immediately.</action>
  <acceptance_criteria>
    - `src/components/surfaces/schedule-reminder-surface.tsx` contains `开课前提醒` and `调课变更提醒`.
    - `src/components/surfaces/schedule-reminder-surface.tsx` contains `已计划`, `发送成功`, `发送失败`, and `需重试`.
    - `src/app/(teacher)/teacher/schedule/reminders/page.tsx` imports `ScheduleReminderSurface`.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Wave 4 must create src/components/surfaces/schedule-reminder-surface.test.tsx first if UI coverage is required beyond DAL tests</automated>
  </verify>
  <done>Teachers can configure first-release schedule reminders and see an honest delivery state instead of fake “all sent” copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schedule change / upcoming class → reminder dispatcher | Trusted schedule events are translated into outbound notification attempts |
| reminder configuration UI → server action | Teacher-controlled reminder settings cross into server-only delivery logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-17 | I | `src/server/schedule/reminder-dispatch.ts` | mitigate | Reuse allowlisted provider/capability routing and forbid secret material in schedule-layer inputs. |
| T-18-18 | R | `src/lib/dal/schedule-reminders.ts` | mitigate | Persist delivery state transitions so the UI can distinguish planned/sent/failed/retry states. |
| T-18-19 | E | `src/actions/schedule-reminder-actions.ts` | mitigate | Keep reminder rule mutations teacher-scoped and reject unsupported audiences/channels. |
| T-18-20 | D | `src/components/surfaces/schedule-reminder-surface.tsx` | accept | Reminder center is teacher-only; low-value denial risk is acceptable if server writes remain scoped and recoverable. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/schedule-reminders.test.ts`
- If UI coverage is added in-task: `pnpm test --run src/components/surfaces/schedule-reminder-surface.test.tsx`
</verification>

<success_criteria>
- Reminder rules are limited to the two first-release types from D-12.
- The system tracks and displays honest delivery states for schedule reminders.
- Reminder orchestration stays inside the existing notification boundary and explicit cache invalidation rules.
</success_criteria>

<output>
After completion, create `.planning/phases/18-teaching-schedule-os/18-05-SUMMARY.md`
</output>
