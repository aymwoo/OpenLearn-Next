---
phase: 25-teaching-data-capture-and-session-analytics
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - src/components/surfaces/classroom-console-surface.tsx
  - src/components/classroom/classroom-control-panel.tsx
  - src/components/classroom/classroom-student-detail-panel.tsx
  - src/components/classroom/classroom-session-history-panel.tsx
  - src/components/classroom/classroom-session-recap-surface.tsx
autonomous: true
requirements:
  - ANALYTICS-01
must_haves:
  truths:
    - ended session 的主舞台必须在 `/classroom` 内被 recap takeover，不得继续把 live controls 留在首屏主叙事里。
    - student-first recap 是主钻取路径；step diagnostics 只能作为次级诊断区。
    - `待跟进课堂信号` 与 `待反馈提交` 必须在 UI 上拆成两个可行动的工作量卡片，而不是混成抽象 analytics 总数。
  artifacts:
    - path: src/components/surfaces/classroom-console-surface.tsx
      provides: recap-aware classroom main-stage selection
      contains: ClassroomSessionRecapSurface
    - path: src/components/classroom/classroom-session-recap-surface.tsx
      provides: recap hero, workload split, student-first drill-down, step diagnostics
      contains: 查看学生复盘
    - path: src/components/classroom/classroom-session-history-panel.tsx
      provides: same-domain live/ended/history session entry list
      contains: 课堂记录
  key_links:
    - from: src/components/surfaces/classroom-console-surface.tsx
      to: src/components/classroom/classroom-session-recap-surface.tsx
      via: ended session main-stage takeover inside the existing classroom console shell
      pattern: recap|ClassroomSessionRecapSurface
    - from: src/components/classroom/classroom-session-history-panel.tsx
      to: src/components/classroom/classroom-session-recap-surface.tsx
      via: history entry selection reopens recap in the same `/classroom` domain
      pattern: sessionId|课堂记录
---

<objective>
把 Phase 25 的 teacher-facing recap surface 落到 `/classroom` 现有 console shell 中，
实现 ended-state 主舞台 takeover、课堂记录列表、student-first drill-down、step diagnostics 与 workload split 的 Stitch-aligned 产品面。

Purpose: 将 25-01 的 recap DTO 变成教师可直接行动的复盘界面，而不是一组裸数据。
Output: recap-aware classroom surfaces and same-domain history reopen UX.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-teaching-data-capture-and-session-analytics/25-CONTEXT.md
@.planning/phases/25-teaching-data-capture-and-session-analytics/25-RESEARCH.md
@.planning/phases/25-teaching-data-capture-and-session-analytics/25-UI-SPEC.md
@src/components/surfaces/classroom-console-surface.tsx
@src/components/classroom/classroom-control-panel.tsx
@src/components/classroom/classroom-student-detail-panel.tsx
@src/components/learning/teacher-review-surface.tsx
@src/lib/dto/classroom.ts

<interfaces>
Ended-state surface handoff:
```tsx
type ClassroomConsoleSurfaceProps = {
  consoleData: ClassroomConsoleDTO
  initialSnapshot?: ClassroomSnapshotDTO | null
  recap?: ClassroomSessionRecapDTO | null
}
```

Student-first recap IA:
```ts
type ClassroomSessionRecapStudentSummaryDTO = {
  studentId: string
  studentName: string
  completionLabel: string
  participationLabel: "积极参与" | "正常参与" | "需要关注" | "未评价"
  evidenceCount: number
  needsFollowUp: boolean
  pendingFeedbackCount: number
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ended-state recap main stage and same-domain session history list</name>
  <files>src/components/surfaces/classroom-console-surface.tsx, src/components/classroom/classroom-control-panel.tsx, src/components/classroom/classroom-session-history-panel.tsx, src/components/classroom/classroom-session-recap-surface.tsx</files>
  <read_first>src/components/surfaces/classroom-console-surface.tsx, src/components/classroom/classroom-control-panel.tsx, src/lib/dto/classroom.ts, .planning/phases/25-teaching-data-capture-and-session-analytics/25-UI-SPEC.md, .planning/phases/25-teaching-data-capture-and-session-analytics/25-CONTEXT.md</read_first>
  <behavior>
    - Test 1: ended session switches the main stage from live runtime to recap hero and metrics without leaving `/classroom`.
    - Test 2: the secondary rail exposes a `课堂记录` module that can reopen recent ended sessions in the same route context.
    - Test 3: recap top-level summary presents workload split as two explicit action cards rather than a single analytics total.
  </behavior>
  <action>Create a dedicated recap surface component, for example `src/components/classroom/classroom-session-recap-surface.tsx`, and render it from `src/components/surfaces/classroom-console-surface.tsx` whenever a recap DTO is present. The recap main stage must follow the locked order from `25-UI-SPEC.md`: recap hero, headline metrics, workload split, then student-first recap. Add a `课堂记录` panel in the secondary rail via `src/components/classroom/classroom-session-history-panel.tsx`, listing live and recent ended sessions with current selection state and same-route reopen behavior driven by `sessionId`. Update `src/components/classroom/classroom-control-panel.tsx` so live runtime controls do not remain the dominant ended-state hero when recap is active.</action>
  <acceptance_criteria>
    - `src/components/surfaces/classroom-console-surface.tsx` renders a recap surface path and does not keep live controls as the ended-state primary hero.
    - `src/components/classroom/classroom-session-history-panel.tsx` exists and contains `课堂记录` plus current-session selection behavior.
    - `src/components/classroom/classroom-session-recap-surface.tsx` contains `待跟进课堂信号`, `待反馈提交`, and `查看学生复盘`.
    - no new analytics dashboard route or `/teacher/review`-first entry is introduced.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/surfaces/classroom-console-surface.test.tsx src/components/classroom/classroom-session-history-panel.test.tsx src/components/classroom/classroom-session-recap-surface.test.tsx</automated>
  </verify>
  <done>The classroom console becomes recap-aware and preserves one same-domain teacher workflow before and after class ends.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build student-first recap drill-down and secondary step diagnostics</name>
  <files>src/components/classroom/classroom-session-recap-surface.tsx, src/components/classroom/classroom-student-detail-panel.tsx</files>
  <read_first>src/components/classroom/classroom-session-recap-surface.tsx, src/components/classroom/classroom-student-detail-panel.tsx, src/lib/dto/classroom.ts, .planning/phases/25-teaching-data-capture-and-session-analytics/25-UI-SPEC.md, .planning/phases/25-teaching-data-capture-and-session-analytics/25-RESEARCH.md</read_first>
  <behavior>
    - Test 1: recap defaults to a student-first list/detail structure rather than a step-first or timeline-first landing view.
    - Test 2: selected student shows summary first, then grouped evidence sections in the exact four-group posture.
    - Test 3: step diagnostics render after student recap as a secondary tonal section and do not replace the primary drill-down path.
  </behavior>
  <action>Use the recap DTO from 25-01 to render a student summary list and selected student detail area. Reuse or extend `src/components/classroom/classroom-student-detail-panel.tsx` so the selected student recap shows summary first and then grouped evidence sections named exactly `完成情况`, `提交与反馈`, `过程评价`, and `课堂时间线`. Render participation labels with the explicit four-state contract `积极参与 / 正常参与 / 需要关注 / 未评价`. Below the student-first area, render a secondary `环节诊断` section for step summaries with subdued tonal treatment and copy that positions it as auxiliary diagnosis only.</action>
  <acceptance_criteria>
    - the recap surface contains the exact grouped evidence labels `完成情况`, `提交与反馈`, `过程评价`, and `课堂时间线`.
    - the selected student summary exposes `未评价` as an explicit participation state when applicable.
    - the recap surface contains `环节诊断` after the student-first section and uses copy that frames it as secondary support.
    - student-first list items include teacher-action cues such as follow-up or pending feedback state rather than only names and counts.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/classroom/classroom-session-recap-surface.test.tsx src/components/classroom/classroom-student-detail-panel.test.tsx</automated>
  </verify>
  <done>Teachers can move from recap headline metrics to concrete student evidence and only then to step diagnostics, preserving the intended action hierarchy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recap DTO -> teacher-facing UI | the surface must preserve semantics such as `未评价`, workload split, and same-domain session scope without visual shortcutting |
| history session selector -> main-stage takeover | teacher clicks determine which ended/live session data is shown and must not route away or mix sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | I | recap surface hierarchy | mitigate | Keep ended recap as the only main-stage hero, move live controls out of the primary story when recap is active, and preserve the classroom domain shell. |
| T-25-05 | T | workload cards | mitigate | Render `待跟进课堂信号` and `待反馈提交` as separate cards with explicit explanations, never as a merged analytics badge or chart. |
| T-25-06 | I | student/step drill-down posture | mitigate | Default to student-first list/detail and render step diagnostics only after the main recap path as a secondary tonal section. |
</threat_model>

<verification>
- `pnpm test --run src/components/surfaces/classroom-console-surface.test.tsx src/components/classroom/classroom-session-history-panel.test.tsx src/components/classroom/classroom-session-recap-surface.test.tsx src/components/classroom/classroom-student-detail-panel.test.tsx`
</verification>

<success_criteria>
- ended sessions in `/classroom` display a recap-first main stage instead of the live runtime hero.
- teachers can reopen recent ended sessions, review workload, and drill into student evidence without leaving the classroom domain.
- step diagnostics remain available but clearly secondary to the student-first recap flow.
</success_criteria>

<output>
After completion, create `.planning/phases/25-teaching-data-capture-and-session-analytics/25-02-SUMMARY.md`
</output>
