---
phase: 25-teaching-data-capture-and-session-analytics
plan: 03
type: execute
wave: 3
depends_on:
  - 25-01
  - 25-02
files_modified:
  - src/lib/dal/classroom.test.ts
  - src/lib/dal/learning.test.ts
  - src/components/surfaces/classroom-console-surface.test.tsx
  - src/components/classroom/classroom-session-recap-surface.test.tsx
  - scripts/verify-phase25-session-analytics.ts
  - package.json
autonomous: true
requirements:
  - ANALYTICS-01
must_haves:
  truths:
    - Phase 25 需要专属 verifier，不能只靠人工点击或 prose checklist 证明 recap 边界正确。
    - 验证必须同时覆盖聚合口径、teacher/session scoping、same-domain route posture、empty-state behavior 和 anti-pattern guard。
    - verifier 要显式防止第二真相源、未评价静默归类、以及 recap 主入口漂移到 `/teacher/review` 或新 analytics route。
  artifacts:
    - path: scripts/verify-phase25-session-analytics.ts
      provides: phase-specific static guards plus focused test runner
      contains: verify:phase25
    - path: src/lib/dal/classroom.test.ts
      provides: recap aggregation and scoping regression coverage
      contains: getClassroomSessionRecapDTO
    - path: src/components/classroom/classroom-session-recap-surface.test.tsx
      provides: ended recap UI hierarchy and empty-state regression coverage
      contains: 未评价
  key_links:
    - from: package.json
      to: scripts/verify-phase25-session-analytics.ts
      via: `verify:phase25` package script
      pattern: verify:phase25
    - from: scripts/verify-phase25-session-analytics.ts
      to: focused test files
      via: static guards run first, then targeted test suite proves behavior
      pattern: pnpm test --run
---

<objective>
为 Phase 25 补齐验证闭环，固定 session recap 的聚合语义、同域入口、空态与 anti-pattern 边界，
确保后续 Phase 26 的趋势与产品化工作不会静默破坏 recap contract。

Purpose: 用 phase-specific verifier 和 focused regressions 把本阶段的关键事实收口为可重复执行的证明。
Output: `verify:phase25`, targeted DAL/UI tests, and static invariant guards.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-teaching-data-capture-and-session-analytics/25-CONTEXT.md
@.planning/phases/25-teaching-data-capture-and-session-analytics/25-RESEARCH.md
@.planning/phases/25-teaching-data-capture-and-session-analytics/25-UI-SPEC.md
@src/lib/dal/classroom.ts
@src/lib/dal/learning.ts
@src/components/surfaces/classroom-console-surface.tsx
@src/components/classroom/classroom-session-recap-surface.tsx
@package.json

<interfaces>
Verifier entry:
```json
{
  "scripts": {
    "verify:phase25": "tsx scripts/verify-phase25-session-analytics.ts"
  }
}
```

Focused proof surface:
```bash
pnpm test --run \
  src/lib/dal/classroom.test.ts \
  src/lib/dal/learning.test.ts \
  src/components/surfaces/classroom-console-surface.test.tsx \
  src/components/classroom/classroom-session-recap-surface.test.tsx
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused DAL and surface regressions for recap aggregation, scoping, and empty states</name>
  <files>src/lib/dal/classroom.test.ts, src/lib/dal/learning.test.ts, src/components/surfaces/classroom-console-surface.test.tsx, src/components/classroom/classroom-session-recap-surface.test.tsx</files>
  <read_first>src/lib/dal/classroom.test.ts, src/lib/dal/learning.test.ts, src/components/surfaces/classroom-console-surface.test.tsx, src/components/classroom/classroom-session-recap-surface.test.tsx, src/lib/dal/classroom.ts, .planning/phases/25-teaching-data-capture-and-session-analytics/25-RESEARCH.md</read_first>
  <behavior>
    - Test 1: recap aggregation preserves `未评价` as a separate participation bucket and keeps pending feedback/follow-up signals separated.
    - Test 2: recap reads stay teacher- and session-scoped, including ended/history reopen contexts.
    - Test 3: ended recap surface renders the expected hierarchy and empty-state copy when no ended sessions or no student evidence exists.
  </behavior>
  <action>Extend existing DAL and surface tests to cover `getClassroomSessionRecapDTO`, history session selection, participation bucket semantics, bridged `待反馈提交`, and `待跟进课堂信号`. Add UI coverage ensuring recap surfaces display explicit `未评价`, `待跟进课堂信号`, `待反馈提交`, and the empty-state copy defined in `25-UI-SPEC.md`, while step diagnostics stay secondary to the student-first section.</action>
  <acceptance_criteria>
    - DAL tests assert `未评价` remains explicit and missing evaluations are not coerced into another participation bucket.
    - DAL or page tests assert ended/history recap stays inside `/classroom` scope and does not use `/teacher/review` as the primary read path.
    - surface tests assert the recap hierarchy includes workload split and student-first content before `环节诊断`.
    - empty-state tests cover the no-history/no-recap guidance copy in Chinese.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/classroom.test.ts src/lib/dal/learning.test.ts src/components/surfaces/classroom-console-surface.test.tsx src/components/classroom/classroom-session-recap-surface.test.tsx</automated>
  </verify>
  <done>Phase 25 behavior is protected by targeted regression tests instead of manual recap checking.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add `verify:phase25` static guards and focused runner</name>
  <files>scripts/verify-phase25-session-analytics.ts, package.json</files>
  <read_first>package.json, scripts/verify-phase18-schedule.ts, scripts/verify-phase16-theme-layout.ts, .planning/phases/25-teaching-data-capture-and-session-analytics/25-CONTEXT.md, .planning/phases/25-teaching-data-capture-and-session-analytics/25-RESEARCH.md</read_first>
  <behavior>
    - Test 1: verifier checks required files and fails fast when recap scripts/tests are missing.
    - Test 2: static guards catch forbidden route or persistence drift before tests even run.
    - Test 3: verifier runs the focused recap suite as the final proof step.
  </behavior>
  <action>Create `scripts/verify-phase25-session-analytics.ts` following the repo verifier pattern: required-file checks, source guard helpers, and then a focused `pnpm test --run ...` invocation. Static guards must fail if recap code introduces a new analytics route family, uses `/teacher/review` as the recap primary entry, creates an analytics snapshot/materialized persistence path, or hides the explicit `未评价` participation state. Add a `verify:phase25` script in `package.json` that points to this verifier.</action>
  <acceptance_criteria>
    - `package.json` contains `verify:phase25`.
    - `scripts/verify-phase25-session-analytics.ts` exists and checks for required files before running tests.
    - the verifier contains guard logic against new analytics primary routes, `/teacher/review` takeover, second-source-of-truth persistence, and missing `未评价` semantics.
    - the verifier runs the focused test suite rather than printing a prose checklist only.
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase25</automated>
  </verify>
  <done>The phase has a repeatable verification command that protects recap boundaries against future drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implementation -> verifier | the proof command must detect recap-boundary regressions before future productization or trend work lands |
| static source guards -> targeted tests | static checks catch forbidden architecture drift; tests prove actual behavior and copy hierarchy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07 | T | verifier coverage drift | mitigate | Add a dedicated `verify:phase25` script and keep required-file plus focused-test checks in one committed entry point. |
| T-25-08 | I | route/persistence anti-patterns | mitigate | Fail verification if a new analytics route family, `/teacher/review` takeover, or snapshot persistence layer appears. |
| T-25-09 | I | participation/workload semantic erosion | mitigate | Assert `未评价` and split workload semantics in tests and static guards so UI polish cannot silently merge or relabel them. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/classroom.test.ts src/lib/dal/learning.test.ts src/components/surfaces/classroom-console-surface.test.tsx src/components/classroom/classroom-session-recap-surface.test.tsx`
- `pnpm verify:phase25`
</verification>

<success_criteria>
- Phase 25 ships with a dedicated verification command matching repo conventions.
- recap aggregation, same-domain route posture, workload split, and empty states are all covered by focused tests.
- future changes cannot silently reintroduce a second analytics source of truth or hide explicit `未评价` semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/25-teaching-data-capture-and-session-analytics/25-03-SUMMARY.md`
</output>
