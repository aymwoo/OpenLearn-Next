---
phase: 38-cutover-verification-fallback-and-operational-hardening
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - .planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-CLOSEOUT.md
  - .planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-FALLBACK-MATRIX.md
  - .planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-DEMO-RUNBOOK.md
autonomous: true
requirements: [RTPX-02, RTPX-03]
must_haves:
  truths:
    - "Phase 38 必须把 fallback / rollback posture 讲清楚：哪些情况下是 websocket success、哪些是 Redis optional disabled、哪些是 Redis degraded local-only、哪些仍依赖 snapshot/SSE rollback。"
    - "本地开发与演示路径必须 repo-local、可重复、可诚实跳过；没有 Redis capability 时不能假装多实例已验证。"
    - "milestone close artifact 必须显式写出 delivered scope 与 deferred scope，防止 `RTPX-02` 被误扩展为 BullMQ 或 broader async worker close。"
    - "operator visibility 只需要被解释和归档，不默认扩成新的产品 surface 开发。"
  artifacts:
    - path: ".planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-FALLBACK-MATRIX.md"
      provides: "explicit fallback and rollback posture across websocket, Redis, and operator surfaces"
    - path: ".planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-DEMO-RUNBOOK.md"
      provides: "repo-local local-dev, dual-instance, and Redis smoke guidance"
    - path: ".planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-CLOSEOUT.md"
      provides: "milestone close artifact with scope, exclusions, and proof chain"
  key_links:
    - from: ".planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-FALLBACK-MATRIX.md"
      to: "scripts/verify-phase36-websocket-cutover.ts"
      via: "Phase 36 remains source of websocket rollback truth"
      pattern: "SSE rollback surface"
    - from: ".planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-FALLBACK-MATRIX.md"
      to: "scripts/verify-phase37-redis-fanout.ts"
      via: "Phase 37 remains source of optional Redis and degraded local-only truth"
      pattern: "local_only"
    - from: ".planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-DEMO-RUNBOOK.md"
      to: "scripts/bootstrap-dev-db.ts"
      via: "bootstrap posture must match human demo instructions"
      pattern: "local_only"
---

<objective>
把 fallback / rollback posture、本地 bootstrap 与 demo 路径、operator observability
说明，以及最终 milestone close artifact 收口成诚实、可传递、可重复执行的 closeout 面。

Purpose: milestone close 不只是机器可验证，还必须让后续开发者或 reviewer 能在 repo
内复现实情，而不是依赖作者口头说明。
Output: fallback matrix、demo runbook、milestone close artifact。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-CONTEXT.md
@.planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-RESEARCH.md
@.planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-PATTERNS.md
@.planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-VERIFICATION.md
@.planning/phases/36-websocket-classroom-transport-cutover/36-VERIFICATION.md
@.planning/phases/37-redis-fanout-and-multi-instance-delivery-convergence/37-VERIFICATION.md
@scripts/bootstrap-dev-db.ts
@scripts/verify-phase36-websocket-cutover.ts
@scripts/verify-phase37-redis-fanout.ts
@src/components/surfaces/settings-surface.tsx
@src/components/surfaces/runtime-inspector-surface.tsx
@src/components/classroom/classroom-control-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 发布 fallback / rollback matrix，明确 websocket、Redis、snapshot/SSE 与 operator visibility 的关系</name>
  <read_first>
    - .planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-RESEARCH.md
    - .planning/phases/36-websocket-classroom-transport-cutover/36-VERIFICATION.md
    - .planning/phases/37-redis-fanout-and-multi-instance-delivery-convergence/37-VERIFICATION.md
    - src/components/surfaces/settings-surface.tsx
    - src/components/surfaces/runtime-inspector-surface.tsx
    - src/components/classroom/classroom-control-panel.tsx
  </read_first>
  <files>.planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-FALLBACK-MATRIX.md</files>
  <behavior>
    - Test 1: matrix 至少区分 `ws cutover success`、`Redis optional disabled`、`Redis enabled and healthy`、`Redis degraded local-only`、`snapshot/SSE rollback posture` 五类状态。
    - Test 2: matrix 会明确每种状态下 teacher、student、settings、runtime inspector 分别能看到什么，不会混成一句泛泛的“系统会自动回退”。
    - Test 3: matrix 会说明哪些路径仍是 durable truth，哪些只是 delivery layer，避免把 Redis 或 websocket 写成新的持久真相源。
  </behavior>
  <action>新增 `38-FALLBACK-MATRIX.md`，把当前仓库里已经成立但分散的 fallback semantics 收口成一张可读矩阵。至少要列出：触发条件、effective transport posture、cross-instance 是否保证、publisher 实例是否仍 local-only 可达、operator surface 是否可见、student-facing 是否暴露 Redis-specific copy、durable truth 是否仍在 SQLite + DAL。矩阵必须直接引用并承认 Phase 36 的 SSE rollback truth 和 Phase 37 的 Redis degraded truth，不另发明新机制。不要把它写成模糊 FAQ；必须是表格化、可审阅的 closeout artifact。</action>
  <acceptance_criteria>
    - `38-FALLBACK-MATRIX.md` 至少覆盖五种固定状态，并分别列出 transport、operator visibility、student impact、durable truth。
    - 文档明确写出 Redis degraded 时是 `local-only` fallback + cross-instance failure，而不是“自动透明恢复成功”。
    - 文档明确写出 SSE rollback 仍然是 Phase 36 保留的设计内事实。
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase38</automated>
  </verify>
  <done>fallback / rollback posture 已从隐含知识收口为显式 closeout matrix。</done>
</task>

<task type="auto">
  <name>Task 2: 发布本地 demo/bootstrap runbook 与 milestone close artifact</name>
  <read_first>
    - scripts/bootstrap-dev-db.ts
    - scripts/verify-phase37-redis-fanout.ts
    - .planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-VERIFICATION.md
    - .planning/phases/32-end-to-end-hardening-and-milestone-proof/32-DEMO-HANDOFF.md
  </read_first>
  <files>.planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-DEMO-RUNBOOK.md, .planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-CLOSEOUT.md</files>
  <behavior>
    - Test 1: runbook 会写清本地默认 `local_only` 开发路径、Redis smoke 所需环境变量、双实例启动方式和预期验证结果。
    - Test 2: closeout artifact 会列出 proof chain：`verify:phase36`、`verify:phase37`、`verify:phase38`、fallback matrix、demo runbook、operator visibility surfaces。
    - Test 3: closeout artifact 会显式列出 out-of-scope：PostgreSQL、BullMQ、Redis Streams、第二 runtime、第三方 runtime/package、AI runtime expansion。
  </behavior>
  <action>新增 `38-DEMO-RUNBOOK.md` 与 `38-CLOSEOUT.md`。`38-DEMO-RUNBOOK.md` 负责讲清楚本地如何复现：默认 local-only 开发、显式启用 Redis 的环境变量、双实例或等价 smoke 路径、应该从 `/settings`、runtime inspector、teacher `/classroom` 看什么结果、无 Redis 时为什么 honest skip 合法。`38-CLOSEOUT.md` 则负责 milestone 级归档结论：goal、delivered scope、proof chain、fallback posture summary、operator visibility summary、known exclusions、deferred next steps。不要把 closeout artifact 写成 roadmap 重复文本；它应该是这次 cutover 的最终归档页。</action>
  <acceptance_criteria>
    - `38-DEMO-RUNBOOK.md` 包含 Redis 相关环境变量、local-only 默认姿态、Redis smoke 入口和 operator observation points。
    - `38-CLOSEOUT.md` 包含 delivered scope、proof chain、explicit exclusions、fallback summary。
    - 两个文档都不把 BullMQ 或 broader async worker close 混入本 milestone 结论。
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase38</automated>
  </verify>
  <done>milestone close 已具备面向人类的演示与归档能力，不再只依赖脚本输出或作者口头说明。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fallback docs → real runtime semantics | 文档如果弱化 degraded truth，会误导 operator 和 reviewer |
| runbook → bootstrap scripts | 人类 runbook 如果和实际 bootstrap posture 不一致，会造成假复现路径 |
| closeout artifact → requirements scope | 如果 closeout 不写 exclusions，`RTPX-02` 很容易被误解为 broader Redis/BullMQ close |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02-01 | T | fallback matrix | mitigate | 固定列出 degraded local-only、cross-instance failed、operator-visible surfaces，不允许模糊化 |
| T-38-02-02 | R | demo runbook | mitigate | 明确 local-only 默认姿态、Redis env prerequisites、honest skip 条件，与 bootstrap/verifier 输出保持一致 |
| T-38-02-03 | I | closeout scope | mitigate | 在 `38-CLOSEOUT.md` 中显式写出 deferred scope，避免把 BullMQ、PostgreSQL、第二 runtime 混入已完成结论 |
</threat_model>

<verification>
- `pnpm verify:phase38`
</verification>

<success_criteria>
- 仓库存在可读的 fallback matrix、demo runbook 与 milestone close artifact。
- 本地开发、Redis smoke 与 operator observation 路径都已被 repo-local 文档化。
- milestone close 口径已明确锁定在 `ws + ioredis` classroom transport，不扩张到其他 runtime-platform deferred scope。
</success_criteria>

<output>
After completion, create `.planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-02-SUMMARY.md`
</output>
