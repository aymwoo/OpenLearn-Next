---
phase: 52-action-registry-plugin-lifecycle-governance
plan: "05"
type: execute
wave: 4
depends_on:
  - 52-04
files_modified:
  - src/features/platform-core/actions/registry.ts
  - src/components/surfaces/settings-surface.tsx
  - src/components/surfaces/plugin-lifecycle-operator-surface.tsx
  - src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx
  - scripts/verify-phase52-action-registry-and-lifecycle.ts
autonomous: true
gap_closure: true
requirements:
  - ACTN-03
  - ACTN-04
  - LIFE-01
  - LIFE-03
  - LIFE-04
  - LIFE-06
must_haves:
  truths:
    - "settings/operator surface 直接消费 executableActionCatalog、blockedActionDiagnostics 与 governance lifecycle read model，不再用原始 plugin DTO 本地推断。"
    - "普通首屏仍只展示 executable catalog；blocked diagnostics 继续只在显式治理诊断入口中可见。"
    - "五态 lifecycle、reason code、recovery action、retain/cleanup 预检摘要都来自统一 read model，而不是组件内部 hardcode。"
  artifacts:
    - "src/features/platform-core/actions/registry.ts 提供 UI 可直接消费的 governance dashboard bundle。"
    - "src/components/surfaces/settings-surface.tsx 与 src/components/surfaces/plugin-lifecycle-operator-surface.tsx 完成统一读模型接线。"
    - "scripts/verify-phase52-action-registry-and-lifecycle.ts 静态守卫 UI 不再回退到 raw plugin 推断。"
  key_links:
    - "settings-surface.tsx -> registry.ts governance dashboard bundle -> plugin-lifecycle-operator-surface.tsx"
    - "registry blocked diagnostics -> operator diagnostics view only"
---

<objective>
把已经修好的治理真相接到 UI：让 settings/operator surface 真正消费统一 read model，并更新 verifier 防止后续再回退到 raw plugin DTO 本地推断。

Purpose: Phase 52 当前最大的“看起来完成”问题在 UI。这个计划让 operator surface 与 server read model 对齐，避免再出现前端自己发明 lifecycle / reason / catalog 语义。
Output: governance dashboard read bundle、settings/operator surface rewiring、UI-aware phase verifier。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-action-registry-plugin-lifecycle-governance/52-CONTEXT.md
@.planning/phases/52-action-registry-plugin-lifecycle-governance/52-UI-SPEC.md
@.planning/phases/52-action-registry-plugin-lifecycle-governance/52-VERIFICATION.md
@.planning/phases/52-action-registry-plugin-lifecycle-governance/52-04-PLAN.md
@src/features/platform-core/actions/registry.ts
@src/components/surfaces/settings-surface.tsx
@src/components/surfaces/plugin-lifecycle-operator-surface.tsx

<interfaces>
From `src/features/platform-core/actions/registry.ts`:

```ts
export async function readExecutableActionCatalog(input: { actorId: string; schoolId: string })
export async function readBlockedActionDiagnostics(input: { actorId: string; schoolId: string })
export async function readPluginGovernanceLifecycle(input: { actorId: string; schoolId: string; pluginId: string })
```

From `src/components/surfaces/plugin-lifecycle-operator-surface.tsx`:

```ts
type Props = {
  schoolId: string | null;
  plugins: PluginRegistrationDTO[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 让 settings/operator surface 直接吃 governance read model</name>
  <files>src/features/platform-core/actions/registry.ts, src/components/surfaces/settings-surface.tsx, src/components/surfaces/plugin-lifecycle-operator-surface.tsx, src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx</files>
  <read_first>src/features/platform-core/actions/registry.ts, src/components/surfaces/settings-surface.tsx, src/components/surfaces/plugin-lifecycle-operator-surface.tsx, .planning/phases/52-action-registry-plugin-lifecycle-governance/52-UI-SPEC.md, .planning/phases/52-action-registry-plugin-lifecycle-governance/52-VERIFICATION.md</read_first>
  <acceptance_criteria>
    - `src/features/platform-core/actions/registry.ts` 新增单次读取的 governance dashboard bundle，至少返回 `executableActionCatalog`、`blockedActionDiagnostics`、`pluginLifecycleRows` 三个字段。
    - `src/components/surfaces/settings-surface.tsx` 不再导入 `listPluginsAction`，而是由 server component 直接读取 governance dashboard bundle 并传给 operator surface。
    - `src/components/surfaces/plugin-lifecycle-operator-surface.tsx` 不再导入 `PluginRegistrationDTO`，也不再定义 `lifecycleCopyMap`、`isExecutablePlugin`、`getDiagnosticReason` 这类本地推断 helper。
    - executable 首屏 action badge、diagnostic reason code、recovery action、retain/cleanup preflight summary 都来自 props 内的 registry read model，而不是 `plugin.manifestJson.actions` / `plugin.lifecycleState` 本地计算。
    - `src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx` 覆盖“普通视图只显示 executable catalog”“治理诊断视图显示 blocked diagnostics”“cleanup 仍需显式确认”三条行为。
  </acceptance_criteria>
  <action>按 D-52-01 / D-52-02 / D-52-03 / D-52-05，在 `src/features/platform-core/actions/registry.ts` 增加单次读取的 governance dashboard bundle，字段固定为 `executableActionCatalog`、`blockedActionDiagnostics`、`pluginLifecycleRows`。每个 lifecycle row 至少包含 `pluginId`、`pluginKey`、`name`、`sourceType`、`lifecycleState`、`reasonCode`、`recommendedRecoveryAction`、`uninstall.preflightSummary`、`uninstall.cleanupConfirmationToken`。随后重写 `src/components/surfaces/settings-surface.tsx` 的 labs 分支：删除 `listPluginsAction` 调用，改为在 server component 中读取 governance dashboard bundle，再把它作为 props 传给 `PluginLifecycleOperatorSurface`。最后重构 `src/components/surfaces/plugin-lifecycle-operator-surface.tsx`：删掉 `PluginRegistrationDTO` 输入和所有 lifecycle/reason/action 本地推断 helper，首屏 executable cards 只渲染 `executableActionCatalog`，治理诊断页只渲染 `blockedActionDiagnostics` 与 `pluginLifecycleRows`，所有 badge/copy 统一消费五态 lifecycle、stable reason code、recommendedRecoveryAction、retain/cleanup preflight summary。</action>
  <verify>
    <automated>pnpm exec vitest run src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx</automated>
  </verify>
  <done>operator surface 不再持有第二份治理真相，UI 与 host/server read model 完全对齐。</done>
</task>

<task type="auto">
  <name>Task 2: 更新 Phase 52 verifier，阻止 UI 回退到 raw plugin 推断</name>
  <files>scripts/verify-phase52-action-registry-and-lifecycle.ts</files>
  <read_first>scripts/verify-phase52-action-registry-and-lifecycle.ts, src/components/surfaces/settings-surface.tsx, src/components/surfaces/plugin-lifecycle-operator-surface.tsx</read_first>
  <acceptance_criteria>
    - verifier 静态检查 `settings-surface.tsx` 不再包含 `listPluginsAction` / `plugins={plugins}` 这类 raw DTO wiring。
    - verifier 静态检查 `plugin-lifecycle-operator-surface.tsx` 不再包含 `PluginRegistrationDTO`、`lifecycleCopyMap`、`getDiagnosticReason`。
    - `pnpm run verify:phase52` 会同时覆盖 Task 1 的 UI wiring 与 Plan 04 的恢复/cleanup 行为。
  </acceptance_criteria>
  <action>更新 `scripts/verify-phase52-action-registry-and-lifecycle.ts` 的静态 guards：新增对 `src/components/surfaces/settings-surface.tsx` 的检查，要求它不再导入 `listPluginsAction`，并直接消费 governance dashboard bundle；新增对 `src/components/surfaces/plugin-lifecycle-operator-surface.tsx` 的检查，明确禁止 `PluginRegistrationDTO`、`lifecycleCopyMap`、`isExecutablePlugin`、`getDiagnosticReason` 继续存在。保留 focused Vitest 运行，并把 Phase 52 close gate 扩展为“写路径 + UI 读路径”双重回归，确保后续修改不能再把 blocked diagnostics 或 lifecycle semantics 回退成本地猜测。</action>
  <verify>
    <automated>pnpm run verify:phase52</automated>
  </verify>
  <done>Phase 52 close gate 能自动阻止 settings/operator surface 再次偏离统一治理 contract。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server governance bundle -> client operator surface | UI 只能展示 server read model 的结果，不能再自行推断 blocked reason 或 lifecycle |
| executable catalog -> diagnostics tab | blocked diagnostics 必须继续留在 operator-only 入口，避免普通首屏泄露治理内部状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-05-01 | I | `settings-surface.tsx` | mitigate | Server component 直接消费 governance bundle，移除 raw plugin DTO wiring。 |
| T-52-05-02 | T | `plugin-lifecycle-operator-surface.tsx` | mitigate | 删除本地 lifecycle/reason 推断 helper，只展示 registry 输出。 |
| T-52-05-03 | I | diagnostics view | mitigate | 保持 executable 首屏与 diagnostics 次级入口分流，blocked rows 不回流到普通视图。 |
</threat_model>

<verification>
- `pnpm exec vitest run src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx`
- `pnpm run verify:phase52`
</verification>

<success_criteria>
- settings/operator surface 直接消费统一治理读模型。
- blocked diagnostics 不再通过 raw plugin 数据在客户端重算。
- Phase 52 verifier 对 UI drift 有明确静态守卫。
</success_criteria>

<output>
After completion, create `.planning/phases/52-action-registry-plugin-lifecycle-governance/52-action-registry-plugin-lifecycle-governance-05-SUMMARY.md`
</output>
