---
phase: 52-action-registry-plugin-lifecycle-governance
plan: "07"
type: execute
wave: 5
depends_on:
  - 52-05
files_modified:
  - src/features/platform-core/commands/contracts.ts
  - src/features/platform-core/commands/registry.ts
  - src/features/platform-core/commands/handlers/plugins.ts
  - src/features/platform-core/commands/handlers/plugins.test.ts
  - src/features/platform-core/commands/producers/plugin-governance.ts
  - src/actions/plugin-actions.ts
  - src/actions/plugin-actions.test.ts
  - src/features/runtime-platform/host-actions/plugin-host.ts
  - src/features/runtime-platform/host-actions/plugin-host.phase52.test.ts
autonomous: true
gap_closure: true
requirements:
  - ACTN-03
  - LIFE-02
  - LIFE-03
must_haves:
  truths:
    - "`plugin.reconcile` 成为真实可执行的治理命令，而不是只存在于 diagnostic 文案中的假恢复路径。"
    - "host 和 server action 都能显式触发 `plugin.reconcile`，dependency_missing / dependency_cycle 不再只剩 blanket deny。"
    - "reconcile 与 retry/enable/resume 一样走 Command Bus、handler、audit、invalidation 的同一治理边界。"
  artifacts:
    - "src/features/platform-core/commands/contracts.ts / registry.ts 新增 `plugin.reconcile` command family。"
    - "src/features/platform-core/commands/handlers/plugins.ts 实现 reconcile handler，并复用现有 dependency activation chain。"
    - "src/actions/plugin-actions.ts` 与 `src/features/runtime-platform/host-actions/plugin-host.ts` 暴露 reconcile 入口。"
  key_links:
    - "plugin-host.ts / plugin-actions.ts -> dispatchPluginGovernanceCommand(type='plugin.reconcile')"
    - "plugin.reconcile -> installOrReconcilePluginWithTx + activation chain helper -> transitionPluginLifecycleWithTx"
---

<objective>
把 `reconcile` 从推荐动作升级为真实治理命令：补齐 command contract、producer、handler、server/host adapter，让 dependency 问题拥有明确可执行的恢复路径。

Purpose: 当前 Phase 52 最关键的剩余 gap 不是提示词，而是 recover path 名义存在、执行入口不存在。这个计划把 `plugin.reconcile` 做成真正的 command surface。
Output: `plugin.reconcile` command family、server/host adapter、focused regression tests。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-action-registry-plugin-lifecycle-governance/52-CONTEXT.md
@.planning/phases/52-action-registry-plugin-lifecycle-governance/52-RESEARCH.md
@.planning/phases/52-action-registry-plugin-lifecycle-governance/52-VERIFICATION.md
@.planning/phases/52-action-registry-plugin-lifecycle-governance/52-action-registry-plugin-lifecycle-governance-04-SUMMARY.md
@src/features/platform-core/commands/contracts.ts
@src/features/platform-core/commands/registry.ts
@src/features/platform-core/commands/handlers/plugins.ts
@src/features/platform-core/commands/producers/plugin-governance.ts
@src/actions/plugin-actions.ts
@src/features/runtime-platform/host-actions/plugin-host.ts

<interfaces>
From `src/features/platform-core/commands/contracts.ts`:

```ts
export const PlatformPluginGovernanceCommandTypes = [
  "plugin.install",
  "plugin.enable",
  "plugin.disable",
  "plugin.retry",
  "plugin.suspend",
  "plugin.resume",
  "plugin.uninstall.preflight",
  "plugin.uninstall",
  "plugin.kill_switch.set",
] as const;
```

From `src/features/platform-core/commands/producers/plugin-governance.ts`:

```ts
export async function dispatchPluginGovernanceCommand(
  input: DispatchPluginGovernanceCommandInput,
): Promise<GovernanceProducerResult>;
```

From `src/features/runtime-platform/host-actions/plugin-host.ts`:

```ts
function isReasonMatchedRecoveryAction(
  action: PluginHostRequest["action"],
  reasonCode: string | null,
) {
  // maps reasonCode -> allowed recovery action
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 `plugin.reconcile` command contract、registry 与 handler</name>
  <files>src/features/platform-core/commands/contracts.ts, src/features/platform-core/commands/registry.ts, src/features/platform-core/commands/handlers/plugins.ts, src/features/platform-core/commands/handlers/plugins.test.ts</files>
  <read_first>src/features/platform-core/commands/contracts.ts, src/features/platform-core/commands/registry.ts, src/features/platform-core/commands/handlers/plugins.ts, src/features/platform-core/commands/handlers/plugins.test.ts, src/features/platform-core/commands/producers/plugin-governance.ts, .planning/phases/52-action-registry-plugin-lifecycle-governance/52-VERIFICATION.md</read_first>
  <acceptance_criteria>
    - `PlatformPluginGovernanceCommandTypes`、`PlatformCommandPayloadSchemas`、`PlatformCommandSchema` 与 `platformCommandRegistry` 都包含 `plugin.reconcile`。
    - `plugin.reconcile` payload 固定包含 `schoolId`、`pluginId`、`reason`，并支持 `targetState?: "enabled" | "mounted" | "ready"`。
    - handler 会先读取当前注册记录，再调用 `installOrReconcilePluginWithTx()` 并传入 `existingRegistrationId`、当前 `name`、当前 `manifestJson`、当前 `installSource`；随后复用 activation chain helper，把 dependencies resolved 后的链路按拓扑顺序推进到 `targetState`（默认 `enabled`）。
    - 当 reconcile 后仍存在 `missingDependencies` 或 `cycles` 时，handler 抛 `PLUGIN_RECONCILE_BLOCKED:*`，而不是静默成功或隐式恢复。
    - `src/features/platform-core/commands/handlers/plugins.test.ts` 覆盖 `plugin.reconcile` 的成功路径与依赖阻断路径。
  </acceptance_criteria>
  <action>按 D-52-09、D-52-10、D-52-11，在 command family 中新增显式 `plugin.reconcile`。先在 `src/features/platform-core/commands/contracts.ts` 追加命令类型与 payload schema：`reason` 必填，`targetState` 允许 `enabled | mounted | ready`，默认由 handler 回落到 `enabled`。随后更新 `src/features/platform-core/commands/registry.ts` 注册该命令。接着在 `src/features/platform-core/commands/handlers/plugins.ts` 实现 reconcile handler：读取当前学校插件记录，使用该记录的 `id/name/manifestJson/installSource/enabled/killSwitchEnabled/lifecycleState` 调用 `installOrReconcilePluginWithTx()`，显式传入 `existingRegistrationId` 与 `commandContext`；然后复用现有 dependency activation chain helper，对已解析依赖的链路按拓扑顺序执行 `transitionPluginLifecycleWithTx()`，让 reconcile 成为真正的“重新计算依赖与激活路径”的显式治理动作。若 reconcile 后依旧存在 missing dependency 或 cycle，则抛 `PLUGIN_RECONCILE_BLOCKED:${details}`，禁止把失败伪装成成功。</action>
  <verify>
    <automated>pnpm exec vitest run src/features/platform-core/commands/handlers/plugins.test.ts</automated>
  </verify>
  <done>`plugin.reconcile` 成为真实 command，而不是只写在 diagnostic 文案里的名词。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 暴露 reconcile 到 producer、server action 与 host recovery gate</name>
  <files>src/features/platform-core/commands/producers/plugin-governance.ts, src/actions/plugin-actions.ts, src/actions/plugin-actions.test.ts, src/features/runtime-platform/host-actions/plugin-host.ts, src/features/runtime-platform/host-actions/plugin-host.phase52.test.ts</files>
  <read_first>src/features/platform-core/commands/producers/plugin-governance.ts, src/actions/plugin-actions.ts, src/actions/plugin-actions.test.ts, src/features/runtime-platform/host-actions/plugin-host.ts, src/features/runtime-platform/host-actions/plugin-host.phase52.test.ts, .planning/phases/52-action-registry-plugin-lifecycle-governance/52-VERIFICATION.md</read_first>
  <acceptance_criteria>
    - `dispatchPluginGovernanceCommand` 的输入 union 与 helper exports 包含 `plugin.reconcile`。
    - `src/actions/plugin-actions.ts` 新增显式 server action（例如 `reconcilePluginAction`），它会 dispatch `type: "plugin.reconcile"`，并保留 `updateTag()` 在入口层的约束。
    - `src/features/runtime-platform/host-actions/plugin-host.ts` 的 action enum 和 `dispatchGovernanceFromHost()` 包含 `plugin.reconcile`。
    - `isReasonMatchedRecoveryAction()` 将 `dependency_missing` / `dependency_cycle` 精确映射到 `plugin.reconcile`，不再返回 `false`。
    - `src/actions/plugin-actions.test.ts` 与 `src/features/runtime-platform/host-actions/plugin-host.phase52.test.ts` 覆盖 reconcile dispatch，且 dependency-blocked host request 只有 `plugin.reconcile` 被放行。
  </acceptance_criteria>
  <action>把 `plugin.reconcile` 贯穿到所有显式 producer surface。先在 `src/features/platform-core/commands/producers/plugin-governance.ts` 的 `DispatchPluginGovernanceCommandInput` union、`buildCommandId()` 及导出 helper 中加入 `plugin.reconcile`。再在 `src/actions/plugin-actions.ts` 增加独立 server action（命名为 `reconcilePluginAction`），输入固定为 `schoolId`、`pluginId`、`reason`、可选 `targetState`，内部通过 `dispatchPluginGovernanceCommand({ type: "plugin.reconcile", ... })` 触发，并继续在入口层调用 `updateTag()`。最后更新 `src/features/runtime-platform/host-actions/plugin-host.ts`：把 `plugin.reconcile` 加入请求枚举与 dispatch switch，并把 `isReasonMatchedRecoveryAction()` 的 dependency_missing / dependency_cycle 分支改为 `return action === "plugin.reconcile"`。同步更新两个测试文件，保证 server/host 两条入口都能实际触发 reconcile，且 dependency-blocked 场景不再被 blanket deny。</action>
  <verify>
    <automated>pnpm exec vitest run src/actions/plugin-actions.test.ts src/features/runtime-platform/host-actions/plugin-host.phase52.test.ts</automated>
  </verify>
  <done>dependency 问题的推荐恢复动作有了真实 server/host 执行入口。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host/server recovery request -> command bus | `reconcile` 必须通过同一 Command Bus 治理面进入，不得新增绕过 DAL/审计的直达恢复路径 |
| dependency diagnostic -> reconcile mutation | dependency 问题只能显式 reconcile，不得因为按钮存在而恢复为隐式 auto-recovery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-07-01 | E | `plugin.reconcile` command surface | mitigate | 新命令沿用现有 command registry、authorize、ledger、audit 机制，不创建旁路 mutation seam。 |
| T-52-07-02 | T | `isReasonMatchedRecoveryAction()` | mitigate | dependency_missing / dependency_cycle 只放行 `plugin.reconcile`，保持 reason-code 精确恢复。 |
| T-52-07-03 | D | reconcile activation path | mitigate | reconcile 后仍用 activation chain fail-fast；missing/cycle 继续抛 `PLUGIN_RECONCILE_BLOCKED:*`。 |
</threat_model>

<verification>
- `pnpm exec vitest run src/features/platform-core/commands/handlers/plugins.test.ts src/actions/plugin-actions.test.ts src/features/runtime-platform/host-actions/plugin-host.phase52.test.ts`
</verification>

<success_criteria>
- `plugin.reconcile` 成为真实 command family。
- host/server 都能显式执行 reconcile。
- dependency-blocked 恢复不再依赖不存在的命令入口。
</success_criteria>

<output>
After completion, create `.planning/phases/52-action-registry-plugin-lifecycle-governance/52-action-registry-plugin-lifecycle-governance-07-SUMMARY.md`
</output>
