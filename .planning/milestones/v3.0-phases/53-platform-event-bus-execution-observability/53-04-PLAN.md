---
phase: 53-platform-event-bus-execution-observability
plan: "04"
type: execute
wave: 3
depends_on:
  - "53-02"
  - "53-03"
files_modified:
  - src/features/platform-core/commands/producers/plugin-governance.ts
  - src/features/platform-core/commands/producers/plugin-governance.test.ts
  - src/features/platform-core/observability/dto.ts
  - src/features/platform-core/observability/operator-read-model.ts
  - src/features/platform-core/observability/operator-read-model.test.ts
  - src/components/surfaces/settings-surface.tsx
  - src/components/surfaces/settings-surface.test.tsx
  - scripts/verify-phase53-platform-events.ts
  - package.json
autonomous: true
requirements:
  - EVNT-06
  - EVNT-07
user_setup: []
must_haves:
  truths:
    - "operator 可以先看 command summary，再下钻 event timeline，而不是被迫阅读 raw event stream。"
    - "command summary 直接暴露 status、result summary、failure attribution、invalidation intent；event timeline 只补充 after-fact facts。"
    - "Phase 53 具备 focused regression gate，能自动阻止 runtime truth reuse、noisy invalidation event family、以及 Phase 54 descriptor scope 漂移。"
  artifacts:
    - path: "src/features/platform-core/commands/producers/plugin-governance.ts"
      provides: "real producer-side composition that injects the persisted-event publication port into `dispatchPlatformCommand()`"
    - path: "src/features/platform-core/observability/operator-read-model.ts"
      provides: "command-first operator execution summary and event timeline loader"
    - path: "src/features/platform-core/observability/dto.ts"
      provides: "operator-facing DTOs for summary rows and timeline entries"
    - path: "src/components/surfaces/settings-surface.tsx"
      provides: "existing `/settings/labs` operator entrypoint integration for the minimal execution summary panel"
    - path: "scripts/verify-phase53-platform-events.ts"
      provides: "phase regression gate for event-truth and scope-boundary invariants"
    - path: "package.json"
      provides: "verify:phase53 script entry"
  key_links:
    - from: "src/features/platform-core/observability/operator-read-model.ts"
      to: "src/db/schema.ts"
      via: "platformCommands summary joined with platformEvents timeline"
      pattern: "platformCommands|platformEvents|invalidation|failureAttribution"
    - from: "src/components/surfaces/settings-surface.tsx"
      to: "src/features/platform-core/observability/operator-read-model.ts"
      via: "server-side labs operator surface consumes command-first execution summary and timeline without building a new console route"
      pattern: "settings/labs|execution summary|operator read model"
    - from: "src/features/platform-core/commands/producers/plugin-governance.ts"
      to: "src/features/platform-core/events/bus.ts"
      via: "producer composition injects the concrete persisted-event publication port so real command dispatches reach the platform event bus"
      pattern: "dispatchPlatformCommand|publicationPort|publishPersistedDispatches"
    - from: "scripts/verify-phase53-platform-events.ts"
      to: "src/features/platform-core/events"
      via: "static guards for truth ownership, event-family drift, and phase-scope leakage"
      pattern: "runtimeEventOutbox|platform\.cache\.invalidation|descriptor|capability"
---

<objective>
收口 Phase 53：交付 command-first operator execution summary + event timeline 读模，并加上 `verify:phase53` 回归闸门，确保本阶段边界不会在后续实现中漂移。

Purpose: 53-02 与 53-03 已经建立 event truth 和 delivery seam；现在需要让 operator 能看懂执行结果，并把关键边界变成自动化校验而不是口头约束。
Output: operator DTO/read model、focused read-model tests、`verify:phase53` script 与 package.json 脚本入口。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-platform-event-bus-execution-observability/53-CONTEXT.md
@.planning/phases/53-platform-event-bus-execution-observability/53-RESEARCH.md
@.planning/phases/53-platform-event-bus-execution-observability/53-02-PLAN.md
@.planning/phases/53-platform-event-bus-execution-observability/53-03-PLAN.md
@src/features/async-tasks/server/operator-read-model.ts
@package.json

<interfaces>
From `src/features/async-tasks/server/operator-read-model.ts`:

```ts
export async function getAsyncTaskWithEvents(taskId: string): Promise<{
  task: AsyncTaskOperatorRow | null;
  events: AsyncTaskOperatorEventRow[];
}>;
```

Use this list/detail shape as the structural reference only. Per D-53-12/D-53-13, the platform surface must remain command-summary-first, not raw-event-first.
</interfaces>
</context>

<tasks>

  <task type="auto" tdd="true">
    <name>Task 1: Compose real command-path publication wiring and operator-visible summary integration</name>
  <read_first>
    - src/features/async-tasks/server/operator-read-model.ts
    - src/features/platform-core/events/ledger.ts
    - src/features/platform-core/commands/contracts.ts
    - .planning/phases/53-platform-event-bus-execution-observability/53-CONTEXT.md
  </read_first>
   <files>src/features/platform-core/commands/producers/plugin-governance.ts, src/features/platform-core/commands/producers/plugin-governance.test.ts, src/features/platform-core/observability/dto.ts, src/features/platform-core/observability/operator-read-model.ts, src/features/platform-core/observability/operator-read-model.test.ts, src/components/surfaces/settings-surface.tsx, src/components/surfaces/settings-surface.test.tsx</files>
   <acceptance_criteria>
     - `src/features/platform-core/commands/producers/plugin-governance.ts` 明确把 53-03 的 persisted-event bus publication port 注入 `dispatchPlatformCommand()`，形成真实 producer -> command bus -> persisted-event bus 接线，而不是只在测试里 mock optional port。
     - 新增 `src/features/platform-core/commands/producers/plugin-governance.test.ts` 覆盖至少一条真实 plugin governance command dispatch 会触发 publication-port handoff。
     - summary DTO 明确区分 command row 与 event timeline row，不让 operator 直接依赖 raw SQL/event table 字段。
     - command summary 直接暴露 status、result summary、failure attribution、invalidation intent，满足 D-53-12/D-53-13/D-53-14。
     - detail loader 返回 command-first summary + ordered event timeline；timeline 可引用 invalidation intent，但 invalidation 不单列成 event family。
     - 现有 `/settings/labs` operator entrypoint 会挂载最小 execution summary / timeline drill-down，而不是新建独立 raw event console 路由。
     - `operator-read-model.test.ts` 覆盖 success summary、failure attribution、timeline ordering、empty timeline 兼容、invalidation visibility。
   </acceptance_criteria>
  <behavior>
    - Test 1: loading one command returns a primary summary object plus an ordered event timeline array.
    - Test 2: failure summaries expose failure attribution even when the timeline contains only one generic failure event.
    - Test 3: invalidation intent appears on the command summary and is not represented as a standalone event type.
  </behavior>
   <action>First, finish the real composition gap left intentionally between 53-02 and 53-03: update `src/features/platform-core/commands/producers/plugin-governance.ts` so the repo's canonical plugin-governance producer injects the concrete persisted-event publication port implemented by `src/features/platform-core/events/bus.ts` into `dispatchPlatformCommand()`. Add `src/features/platform-core/commands/producers/plugin-governance.test.ts` to prove a real producer dispatch hands persisted dispatch ids into the platform event bus rather than relying on mocked optional-port behavior only. Then create `src/features/platform-core/observability/dto.ts` and `src/features/platform-core/observability/operator-read-model.ts` as the minimal operator-visible execution surface for Phase 53. Follow D-53-12/D-53-13/D-53-14 exactly: the primary read is the command row (status, result summary, invalidation intent, failure attribution, actor/scope/correlation metadata), and the secondary read is the related platform event timeline ordered by attempt/event creation. Reuse server-side DAL/Drizzle patterns only; UI components must not read DB directly. Finally integrate this read model into the existing `/settings/labs` operator entrypoint by extending `src/components/surfaces/settings-surface.tsx` (and its test) with a minimal execution-summary section or panel that links command summaries to timeline drill-down, reusing the established governance/operator surface instead of creating a separate raw-event console route. Do not build a raw stream console, do not create analytics aggregates, and do not add Phase 54 AI descriptor fields into this read model. Add `src/features/platform-core/observability/operator-read-model.test.ts` to prove the command-first posture.</action>
   <verify>
     <automated>pnpm vitest run src/features/platform-core/commands/producers/plugin-governance.test.ts src/features/platform-core/observability/operator-read-model.test.ts src/components/surfaces/settings-surface.test.tsx</automated>
   </verify>
  <done>Operator consumers have a stable command-summary-first read model with timeline drill-down, and invalidation/failure signals are directly visible without inventing extra event families.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add verify:phase53 regression gate for truth and scope boundaries</name>
  <read_first>
    - package.json
    - .planning/phases/53-platform-event-bus-execution-observability/53-RESEARCH.md
    - .planning/phases/53-platform-event-bus-execution-observability/53-CONTEXT.md
  </read_first>
  <files>scripts/verify-phase53-platform-events.ts, package.json</files>
   <acceptance_criteria>
     - `package.json` 新增 `verify:phase53` 脚本入口。
     - `scripts/verify-phase53-platform-events.ts` 至少自动检查 4 件事：不得 import `runtimeEventOutbox` 作为 platform truth、不得引入 invalidation noisy event family、不得把 Redis/WebSocket 标成 truth owner、不得吸入 Phase 54 descriptor/capability surface。
     - 脚本还能串起 focused Vitest suites：event contracts、event ledger、handlers events、command bus、producer publication wiring、persisted-event bus、event adapters、operator read model。
     - 该闸门不要求外部 Redis/WebSocket 环境。
   </acceptance_criteria>
  <behavior>
    - Test 1: verify script fails if platform event files import runtime outbox or runtime bus truth modules.
    - Test 2: verify script fails if new event types include cache invalidation family names or Phase 54 descriptor/capability vocabulary.
    - Test 3: verify script passes after running the focused Phase 53 Vitest suite.
  </behavior>
   <action>Create `scripts/verify-phase53-platform-events.ts` and wire `package.json#verify:phase53` so Phase 53 gets one focused regression gate. The script must statically guard the locked boundaries from D-53-10/D-53-14 and the deferred wall: no `runtimeEventOutbox` truth reuse, no `platform.cache.invalidation.*` family, no Redis/WebSocket truth ownership claims, no AI-native descriptor/delegation/capability contract leakage from Phase 54. After static checks, run the full focused Phase 53 Vitest set: `src/features/platform-core/events/contracts.test.ts`, `src/features/platform-core/events/ledger.test.ts`, `src/features/platform-core/commands/handlers/plugins.events.test.ts`, `src/features/platform-core/commands/bus.test.ts`, `src/features/platform-core/commands/producers/plugin-governance.test.ts`, `src/features/platform-core/events/bus.test.ts`, `src/features/platform-core/events/adapters.test.ts`, and `src/features/platform-core/observability/operator-read-model.test.ts`. Keep the gate repo-local and deterministic; do not depend on browser checks or external infrastructure.</action>
  <verify>
    <automated>pnpm verify:phase53</automated>
  </verify>
  <done>Phase 53 has an automated safety gate that preserves truth ownership, keeps invalidation summary-only, and blocks accidental scope creep into runtime transport or Phase 54 AI contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command/event persistence → operator surface | operator summaries must expose enough attribution without leaking unrelated detail or changing truth semantics |
| repo changes → verify gate | future edits can silently reintroduce runtime truth reuse or phase-scope creep unless guarded automatically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-13 | I | operator summary DTOs | mitigate | Surface summary-only result, failure attribution, and invalidation intent; keep detailed payload lookups in ledger/timeline reads. |
| T-53-14 | T/R | `operator-read-model.ts` | mitigate | Build a command-first read model over persisted `platformCommands` + `platformEvents` so operator diagnostics remain traceable. |
| T-53-15 | T | `verify-phase53-platform-events.ts` | mitigate | Statically block runtime truth reuse, noisy invalidation event families, and Phase 54 descriptor/capability leakage. |
| T-53-16 | D | verification workflow | mitigate | Keep `verify:phase53` local and dependency-light so the regression gate runs in CI/dev without Redis/WebSocket infrastructure. |
</threat_model>

<verification>
- `pnpm vitest run src/features/platform-core/observability/operator-read-model.test.ts`
- `pnpm verify:phase53`
</verification>

<success_criteria>
- Operator surfaces can load command summaries first and event timelines second.
- Invalidation intent and failure attribution are directly visible on command summaries.
- `verify:phase53` protects the locked Phase 53 boundaries from later drift.
- Phase 53 closes with one deterministic regression command and no dependency on external delivery infrastructure.
</success_criteria>

<output>
After completion, create `.planning/phases/53-platform-event-bus-execution-observability/53-04-SUMMARY.md`
</output>
