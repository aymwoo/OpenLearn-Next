---
phase: 54-ai-native-contract-exposure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/platform-core/ai-contracts/contracts.ts
  - src/features/platform-core/ai-contracts/contracts.test.ts
  - src/lib/dto/resource-ai.ts
autonomous: true
requirements:
  - AINT-01
  - AINT-02
must_haves:
  truths:
    - "Phase 54 的 shared descriptor contract 必须是 machine-readable surface，不是新的 execution authority。"
    - "descriptor 必须显式表达 input schema、required capability、side-effect class、stability/version metadata。"
    - "action descriptor 应复用 Phase 52 的 code-owned truth，而不是复制一套平行 schema。"
---

<objective>
定义 Phase 54 的 shared AI-native descriptor contract 与 outward DTO shell，为 command/action/capability discovery 建立统一结构。
</objective>

<tasks>
  <task type="auto" tdd="true">
    <name>Task 1: Define shared machine-readable descriptor schemas</name>
    <files>src/features/platform-core/ai-contracts/contracts.ts, src/features/platform-core/ai-contracts/contracts.test.ts, src/lib/dto/resource-ai.ts</files>
    <acceptance_criteria>
      - 新增 shared descriptor shell，至少覆盖 command/action/capability 三类 kind。
      - descriptor 显式包含 schema key、required capabilities、side-effect class、stability/version、implementation source、delegation/approval posture。
      - action descriptor 复用既有 `ActionDescriptorSchema` 字段语义，不重复发明冲突字段名。
      - `resource-ai` DTO 层导出 outward-facing Phase 54 descriptor DTO。
    </acceptance_criteria>
    <verify>
      <automated>pnpm vitest run src/features/platform-core/ai-contracts/contracts.test.ts</automated>
    </verify>
  </task>
</tasks>
