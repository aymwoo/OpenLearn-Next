---
phase: 01-application-foundation-and-design-shell
plan: 05
type: execute
wave: 3
depends_on:
  - 03
  - 04
files_modified:
  - package.json
  - src/lib/cache-policy.ts
  - src/app/(public)/loading.tsx
  - src/app/(teacher)/teacher/loading.tsx
  - src/app/(student)/student/loading.tsx
  - src/app/(classroom)/classroom/loading.tsx
  - src/app/(library)/courses/loading.tsx
  - src/app/(library)/resources/loading.tsx
  - src/app/(admin)/admin/loading.tsx
  - scripts/verify-phase1-shell.ts
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06
must_haves:
  truths:
    - "开发者可以查看每个 route group 的 static shell、Suspense region 和 cache tag 规则。"
    - "每个 required route 有 localized tonal loading shell，不使用全页 spinner-only 等待。"
    - "自动验证脚本检查 route 文件、设计禁忌、Cache Components 和 required copy。"
  artifacts:
    - path: "src/lib/cache-policy.ts"
      provides: "Completed route cache boundary registry"
      contains: "routeCacheBoundaries"
    - path: "scripts/verify-phase1-shell.ts"
      provides: "Automated Phase 1 shell verification"
      contains: "FOUND-06"
    - path: "package.json"
      provides: "Verification script entry"
      contains: "verify:phase1"
  key_links:
    - from: "scripts/verify-phase1-shell.ts"
      to: "src/lib/cache-policy.ts"
      via: "file content checks"
      pattern: "routeCacheBoundaries"
    - from: "package.json"
      to: "scripts/verify-phase1-shell.ts"
      via: "verify:phase1 script"
      pattern: "tsx scripts/verify-phase1-shell.ts"
---

<objective>
Finalize PPR/cache boundaries and add automated guardrails for Phase 1 shell quality.

Purpose: FOUND-06 must be discoverable and testable before later phases add auth, progress, and classroom runtime. This plan also verifies FOUND-02 through FOUND-05 route and design coverage after all surfaces exist.
Output: loading shells, completed cache policy registry, and phase verification script.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-application-foundation-and-design-shell/01-CONTEXT.md
@.planning/phases/01-application-foundation-and-design-shell/01-RESEARCH.md
@.planning/phases/01-application-foundation-and-design-shell/01-PATTERNS.md
@.planning/phases/01-application-foundation-and-design-shell/01-UI-SPEC.md
@DESIGN.md

<interfaces>
Extend the existing `cache-policy.ts` contract, do not replace it:

```ts
export const routeCacheBoundaries = [
  {
    route: '/student/player',
    staticShell: ['lesson chrome', 'step navigation'],
    suspenseRegions: ['personal progress', 'latest submission', 'classroom runtime state'],
    tagNotes: ['lesson:${lessonId}', 'steps:${lessonId}', 'progress:${lessonId}:${userId}', 'classroom:${sessionId}'],
  },
] as const
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add route loading shells and complete cache boundary registry</name>
  <files>src/lib/cache-policy.ts, src/app/(public)/loading.tsx, src/app/(teacher)/teacher/loading.tsx, src/app/(student)/student/loading.tsx, src/app/(classroom)/classroom/loading.tsx, src/app/(library)/courses/loading.tsx, src/app/(library)/resources/loading.tsx, src/app/(admin)/admin/loading.tsx</files>
  <read_first>
    - src/lib/cache-policy.ts
    - src/app/(public)/loading.tsx (if present)
    - src/app/(teacher)/teacher/loading.tsx (if present)
    - src/app/(student)/student/loading.tsx (if present)
    - src/app/(classroom)/classroom/loading.tsx (if present)
    - src/app/(library)/courses/loading.tsx (if present)
    - src/app/(library)/resources/loading.tsx (if present)
    - src/app/(admin)/admin/loading.tsx (if present)
    - src/components/ui/skeleton.tsx
    - .planning/PROJECT.md
    - .planning/phases/01-application-foundation-and-design-shell/01-RESEARCH.md
    - .planning/phases/01-application-foundation-and-design-shell/01-UI-SPEC.md
    - DESIGN.md
  </read_first>
  <action>Update `src/lib/cache-policy.ts` so `routeCacheBoundaries` includes exact routes `/`, `/teacher`, `/teacher/editor`, `/student`, `/student/player`, `/classroom`, `/courses`, `/resources`, and `/admin`. Each entry must include `staticShell`, `suspenseRegions`, `cacheTags`, and `rules`. Use exact Suspense region labels where applicable: `auth toolbar`, `teacher save status`, `student progress`, `latest submission`, `classroom live state`, and `resource filters`. Add loading shell files for public, teacher, student, classroom, courses, resources, and admin routes. Each loading file must render tonal skeleton blocks with `bg-surface`, `bg-surface-container-low`, `bg-surface-container-lowest`, `rounded-[var(--radius-shell)]`, and copy `页面外壳正在加载`. Do not use full-page spinner-only UI, runtime APIs, or uncached fetches in loading files.</action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/cache-policy.ts` contains exact strings `/teacher/editor`, `/student/player`, `/classroom`, `/resources`, and `/admin`.
    - `src/lib/cache-policy.ts` contains exact strings `auth toolbar`, `teacher save status`, `student progress`, `latest submission`, and `classroom live state`.
    - Every `src/app/**/loading.tsx` added in this task contains exact string `页面外壳正在加载`.
    - Every `src/app/**/loading.tsx` added in this task contains exact string `bg-surface-container-low`.
    - `grep -R "spinner\|Date.now\|Math.random\|cookies()\|headers()\|fetch(" src/app/*/loading.tsx src/app/\(*\)/*/loading.tsx 2>/dev/null` returns no matches.
  </acceptance_criteria>
  <done>FOUND-06 PPR/cache boundaries and localized loading shells are explicit for all route groups.</done>
</task>

<task type="auto">
  <name>Task 2: Add automated Phase 1 shell verification</name>
  <files>package.json, scripts/verify-phase1-shell.ts</files>
  <read_first>
    - package.json
    - scripts/verify-phase1-shell.ts (if present)
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-application-foundation-and-design-shell/01-CONTEXT.md
    - .planning/phases/01-application-foundation-and-design-shell/01-UI-SPEC.md
    - DESIGN.md
    - src/lib/cache-policy.ts
  </read_first>
  <action>Add dev dependency `tsx` if it is not already present, and add package script `verify:phase1` with value `tsx scripts/verify-phase1-shell.ts`. Create `scripts/verify-phase1-shell.ts` as a Node script that reads files with `node:fs` and fails with `process.exit(1)` when required files or required strings are missing. It must check: `next.config.ts` contains `cacheComponents: true`; route files exist for `/`, `/teacher`, `/teacher/editor`, `/student`, `/student/player`, `/classroom`, `/courses`, `/resources`, `/admin`; `src/app/layout.tsx` contains `lang="zh-CN"` and `Lexend`; `src/lib/demo-data.ts` contains `初中信息科技`, `编程基础：让角色动起来`, `导入`, `讲授`, `练习`, `总结`; `src/lib/cache-policy.ts` contains `routeCacheBoundaries`, `public:shell`, `progress:${lessonId}:${userId}`, and `classroom:${sessionId}`; source files under `src` do not contain forbidden visible implementation terms `mock`, `seed`, `database`, or `占位符`; source files under `src` do not contain `border-b`, `border-t`, `border-l`, `border-r`, `divide-`, `#000000`, or `text-black`. Print `Phase 1 shell verification passed` on success.</action>
  <verify>
    <automated>pnpm verify:phase1 && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains exact string `"verify:phase1": "tsx scripts/verify-phase1-shell.ts"`.
    - `package.json` contains exact string `"tsx"`.
    - `scripts/verify-phase1-shell.ts` contains exact string `FOUND-06`.
    - `scripts/verify-phase1-shell.ts` contains exact string `Phase 1 shell verification passed`.
    - `scripts/verify-phase1-shell.ts` contains exact string `cacheComponents: true`.
    - `scripts/verify-phase1-shell.ts` contains exact string `progress:${lessonId}:${userId}`.
    - `scripts/verify-phase1-shell.ts` contains exact string `border-b`.
    - Running `pnpm verify:phase1` exits with status 0 after all Phase 1 plans are complete.
  </acceptance_criteria>
  <done>Phase 1 has an automated quality gate covering route coverage, design constraints, demo copy, and cache/PPR discoverability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future user/live regions→Cached shell | Later dynamic data must not be placed into static cached shells. |
| Verification script→Source tree | Local script reads code and fails on unsafe patterns. |
| Loading shell→User perception | Loading UI may imply unavailable runtime data if copy is wrong. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17 | Information Disclosure | `src/lib/cache-policy.ts` | mitigate | Explicitly list user/live Suspense regions and scoped tags for progress, latest submission, and classroom state. |
| T-01-18 | Tampering | `scripts/verify-phase1-shell.ts` | mitigate | Fail builds when shippable source contains known design/safety anti-patterns or missing required route/cache strings. |
| T-01-19 | Denial of Service | `loading.tsx` files | mitigate | Use light deterministic skeleton shells only; no runtime APIs, fetches, random values, or spinner-only route blocking. |
| T-01-20 | Elevation of Privilege | route cache docs | mitigate | Cache policy must state auth toolbar, student progress, latest submission, and classroom live state are Suspense regions, not static shell content. |
</threat_model>

<verification>
Run `pnpm typecheck`, `pnpm lint`, `pnpm verify:phase1`, and `pnpm build`. Inspect failure output from `scripts/verify-phase1-shell.ts` if any required route, copy, cache, or visual invariant is missing.
</verification>

<success_criteria>
Every Phase 1 route has explicit loading/PPR behavior, cache boundaries are discoverable in code, and an automated verification command blocks missing routes, unsafe visible terms, and design-system violations.
</success_criteria>

<output>
After completion, create `.planning/phases/01-application-foundation-and-design-shell/01-05-SUMMARY.md`.
</output>
