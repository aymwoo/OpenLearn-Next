---
phase: 01-application-foundation-and-design-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - postcss.config.mjs
  - eslint.config.mjs
  - src/app/layout.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - FOUND-01
  - FOUND-03
  - FOUND-05
  - FOUND-06
must_haves:
  truths:
    - "开发者可以运行 Next.js 16 / React 19.2 / TypeScript / Turbopack 应用。"
    - "所有页面继承 Lexend、简体中文 lang、Tailwind v4 token 和 no-line 视觉约束。"
    - "Next.js Cache Components 在第一阶段即显式启用。"
  artifacts:
    - path: "package.json"
      provides: "Next.js 16.2.4、React 19.2.5、Tailwind v4.2.4 和脚本入口"
      contains: "\"next\": \"16.2.4\""
    - path: "next.config.ts"
      provides: "Cache Components 开关"
      contains: "cacheComponents: true"
    - path: "src/app/layout.tsx"
      provides: "根布局、Lexend 字体、zh-CN 语言"
      contains: "Lexend"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @theme 设计 token"
      contains: "--color-surface: #f5f6f7"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import"
      pattern: "import './globals.css'"
    - from: "src/app/globals.css"
      to: "DESIGN.md"
      via: "token values"
      pattern: "--color-primary: #005da7"
---

<objective>
Create the runnable application baseline and binding visual token layer for Phase 1.

Purpose: FOUND-01 needs a real Next.js 16 foundation before route shells and surfaces can be built; FOUND-03/05 require Lexend, Simplified Chinese, Tailwind v4 tokens, tonal layers, glass, gradients, and accessible focus states to be encoded globally rather than repeated per page.
Output: framework config, root layout, global CSS tokens, scripts, and Cache Components configuration.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-application-foundation-and-design-shell/01-CONTEXT.md
@.planning/phases/01-application-foundation-and-design-shell/01-RESEARCH.md
@.planning/phases/01-application-foundation-and-design-shell/01-PATTERNS.md
@.planning/phases/01-application-foundation-and-design-shell/01-UI-SPEC.md
@DESIGN.md

<interfaces>
Greenfield repository: no application source files exist yet. Create these first contracts exactly:

```ts
// next.config.ts
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  cacheComponents: true,
}

export default nextConfig
```

```tsx
// src/app/layout.tsx
import type { Metadata } from 'next'
import { Lexend } from 'next/font/google'
import './globals.css'

const lexend = Lexend({ subsets: ['latin'], variable: '--font-lexend' })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js 16 project configuration</name>
  <files>package.json, next.config.ts, tsconfig.json, postcss.config.mjs, eslint.config.mjs</files>
  <read_first>
    - package.json (if present)
    - next.config.ts (if present)
    - tsconfig.json (if present)
    - postcss.config.mjs (if present)
    - eslint.config.mjs (if present)
    - .planning/phases/01-application-foundation-and-design-shell/01-RESEARCH.md
    - .planning/phases/01-application-foundation-and-design-shell/01-PATTERNS.md
    - AGENTS.md
  </read_first>
  <action>Create the framework baseline with concrete dependencies: `next@16.2.4`, `react@19.2.5`, `react-dom@19.2.5`, `typescript@6.0.3`, `tailwindcss@4.2.4`, `@tailwindcss/postcss@4.2.4`, `lucide-react@1.14.0`, `@radix-ui/react-slot@1.2.4`, `clsx@2.1.1`, `tailwind-merge@3.5.0`, `vitest@4.1.5`, `@testing-library/react@16.3.2`, and `playwright@1.59.1`. Add scripts: `dev: next dev --turbopack`, `build: next build --turbopack`, `start: next start`, `typecheck: tsc --noEmit`, `lint: eslint .`, and `test: vitest`. Create `next.config.ts` with `cacheComponents: true`. Use `@/*` alias in `tsconfig.json`. Configure Tailwind v4 through `postcss.config.mjs` using `@tailwindcss/postcss`. Use flat ESLint config with `next/core-web-vitals` and `next/typescript`. Do not add `middleware.ts`, DB clients, Auth.js config, shadcn files, or third-party registry UI blocks in this phase.</action>
  <verify>
    <automated>pnpm install && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains exact string `"next": "16.2.4"`.
    - `package.json` contains exact string `"react": "19.2.5"`.
    - `package.json` contains exact string `"dev": "next dev --turbopack"`.
    - `next.config.ts` contains exact string `cacheComponents: true`.
    - `tsconfig.json` contains exact string `"@/*": ["./src/*"]`.
    - `postcss.config.mjs` contains exact string `"@tailwindcss/postcss"`.
    - `eslint.config.mjs` contains exact string `next/typescript`.
  </acceptance_criteria>
  <done>FOUND-01 baseline exists and Cache Components are explicitly enabled for FOUND-06.</done>
</task>

<task type="auto">
  <name>Task 2: Create root layout and Tailwind v4 design tokens</name>
  <files>src/app/layout.tsx, src/app/globals.css</files>
  <read_first>
    - src/app/layout.tsx (if present)
    - src/app/globals.css (if present)
    - DESIGN.md
    - .planning/phases/01-application-foundation-and-design-shell/01-UI-SPEC.md
    - .planning/phases/01-application-foundation-and-design-shell/01-CONTEXT.md
    - .planning/phases/01-application-foundation-and-design-shell/01-PATTERNS.md
  </read_first>
  <action>Create `src/app/layout.tsx` with `html lang="zh-CN"`, `Lexend` from `next/font/google`, `className={lexend.variable}`, metadata title `OpenLearn Next`, description `面向未来教育的 AI 原生开源课堂操作系统`, and no auth or runtime API calls. Create `src/app/globals.css` with `@import "tailwindcss";` and an `@theme` block defining exact tokens: `--font-sans: var(--font-lexend), sans-serif;`, `--color-surface: #f5f6f7;`, `--color-surface-container-low: #eff1f2;`, `--color-surface-container-lowest: #ffffff;`, `--color-primary: #005da7;`, `--color-primary-container: #68abff;`, `--color-on-primary: #ffffff;`, `--color-on-surface: #2c2f30;`, `--color-on-surface-variant: #595c5d;`, `--color-outline-variant: #abadae;`, `--color-tertiary: #386700;`, `--color-tertiary-container: #a4fd4c;`, `--color-error: #b31b25;`, `--shadow-ambient: 0 16px 48px rgba(44, 47, 48, 0.06);`, `--radius-shell: 2rem;`, and spacing tokens `--spacing-xs: 4px;`, `--spacing-sm: 8px;`, `--spacing-md: 16px;`, `--spacing-lg: 24px;`, `--spacing-xl: 32px;`, `--spacing-2xl: 48px;`, `--spacing-3xl: 64px;`. Set `body` background to `var(--color-surface)`, text to `var(--color-on-surface)`, and include `:focus-visible` with `outline: 2px solid color-mix(in srgb, var(--color-primary) 40%, transparent);` and `outline-offset: 4px`. Do not use pure black text or 1px solid section dividers.</action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` contains exact string `lang="zh-CN"`.
    - `src/app/layout.tsx` contains exact string `Lexend`.
    - `src/app/layout.tsx` contains exact string `import './globals.css'`.
    - `src/app/globals.css` contains exact string `@import "tailwindcss";`.
    - `src/app/globals.css` contains exact string `--color-surface: #f5f6f7;`.
    - `src/app/globals.css` contains exact string `--color-primary: #005da7;`.
    - `src/app/globals.css` contains exact string `--color-primary-container: #68abff;`.
    - `src/app/globals.css` contains exact string `:focus-visible`.
    - `grep -R "#000000\|text-black\|border-b\|border-t\|border-l\|border-r" src/app src/components 2>/dev/null | grep -v "outline"` returns no matches after this plan.
  </acceptance_criteria>
  <done>FOUND-03 and FOUND-05 global design constraints are available to all route shells.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→Next.js shell | Public users request static route shells with no real session. |
| Design config→Rendered UI | CSS tokens control visual output across all route groups. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | `src/app/layout.tsx` | mitigate | Do not add fake auth, session text, cookies, headers, or protected route logic in Phase 1 root layout. |
| T-01-02 | Information Disclosure | `next.config.ts` / route shell caching | mitigate | Enable `cacheComponents: true` and keep Phase 1 data deterministic; route-specific Suspense/cache policy is added in later plans of this phase. |
| T-01-03 | Tampering | `src/app/globals.css` | mitigate | Use local Tailwind v4 theme variables only; no remote CSS, script tags, shadcn registry output, or third-party UI blocks. |
| T-01-04 | Elevation of Privilege | project config | mitigate | Do not add DB clients, Server Actions, plugin execution, or core API access in this plan. |
</threat_model>

<verification>
Run `pnpm install`, `pnpm typecheck`, and `pnpm lint`. Inspect config files for exact versions and `cacheComponents: true`.
</verification>

<success_criteria>
The app foundation builds from pinned package/config files, applies Lexend and zh-CN globally, exposes Tailwind v4 tokens from DESIGN.md, and has explicit Cache Components configuration.
</success_criteria>

<output>
After completion, create `.planning/phases/01-application-foundation-and-design-shell/01-01-SUMMARY.md`.
</output>
