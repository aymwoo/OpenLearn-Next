---
phase: 01-application-foundation-and-design-shell
plan: 06
type: execute
wave: 5
depends_on:
  - 03
  - 04
  - 05
files_modified:
  - src/components/surfaces/home-surface.tsx
  - src/components/surfaces/teacher-dashboard-surface.tsx
  - src/components/surfaces/lesson-editor-surface.tsx
  - src/components/shell/glass-nav.tsx
  - src/components/shell/sidebar.tsx
  - src/components/shell/route-shell.tsx
  - src/lib/navigation.ts
  - src/lib/demo-data.ts
  - scripts/verify-phase1-shell.ts
autonomous: true
gap_closure: true
requirements:
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
must_haves:
  truths:
    - "首页视觉密度、字体层级、组件布局和 teacher-first CTA 更接近 Stitch `首页 - OpenLear-Next (一屏精简版)`，关闭当前模板感缺口。"
    - "首页与 dashboard 的主导航、CTA、移动端横向导航、focus 和 touch 状态均按 Stitch 项目 `5322129002350954765` 与 `DESIGN.md` 重新校准。"
    - "自动验证脚本新增 gap-closure guard，防止首页、导航和 CTA 回退到松散模板化布局。"
  artifacts:
    - path: "src/components/surfaces/home-surface.tsx"
      provides: "Stitch-aligned compact public home surface"
      contains: "学生登录"
    - path: "src/components/shell/glass-nav.tsx"
      provides: "Compact glass top navigation with 44px touch targets"
      contains: "min-h-11"
    - path: "scripts/verify-phase1-shell.ts"
      provides: "Automated UAT gap closure guard"
      contains: "Home visual density and navigation alignment verified"
  key_links:
    - from: "src/components/surfaces/home-surface.tsx"
      to: "/teacher/editor"
      via: "primary teacher CTA"
      pattern: "href=\"/teacher/editor\""
    - from: "src/components/shell/glass-nav.tsx"
      to: "src/lib/navigation.ts"
      via: "navigationItems map"
      pattern: "navigationItems\.map"
    - from: "scripts/verify-phase1-shell.ts"
      to: "src/components/surfaces/home-surface.tsx"
      via: "file content checks"
      pattern: "Home visual density and navigation alignment verified"
---

<objective>
Close the Phase 1 Human UAT gaps by recalibrating the public home, primary navigation, and CTA surfaces against the binding Stitch design source.

Purpose: Automated verification passed, but human UAT failed visual fidelity and navigation/CTA alignment. This plan fixes the gap without changing Phase 1 scope: routes remain public, data remains static, and no auth/database/runtime behavior is introduced.
Output: A denser Stitch-aligned home surface, recalibrated shell navigation/CTA states, and source-level verification guards for the UAT fixes.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-application-foundation-and-design-shell/01-CONTEXT.md
@.planning/phases/01-application-foundation-and-design-shell/01-RESEARCH.md
@.planning/phases/01-application-foundation-and-design-shell/01-PATTERNS.md
@.planning/phases/01-application-foundation-and-design-shell/01-UI-SPEC.md
@.planning/phases/01-application-foundation-and-design-shell/01-HUMAN-UAT.md
@.planning/phases/01-application-foundation-and-design-shell/01-03-SUMMARY.md
@.planning/phases/01-application-foundation-and-design-shell/01-04-SUMMARY.md
@.planning/phases/01-application-foundation-and-design-shell/01-05-SUMMARY.md
@DESIGN.md

<interfaces>
Use the Stitch project and exact screen identity during implementation:

```text
Project: projects/5322129002350954765
Home screen: projects/5322129002350954765/screens/aca6ee2b673749c5838f4eaa621806bd
Home label: 首页 - OpenLear-Next (一屏精简版)
Reference home copy visible from Stitch HTML: OpenLear-Next, 课程, 探索, 关于, 登录, 立即加入, 开启智慧学习新篇章, 学生登录, 教师登录, 10W+ 活跃学生, 500+ 精品课程, 98% 好评率, 推荐课程.
```

Existing contracts the executor must preserve:

```ts
// src/lib/navigation.ts
export type NavigationItem = {
  label: string
  href: string
  emphasis?: 'low'
}

export const navigationItems: readonly NavigationItem[] = [
  { label: '首页', href: '/' },
  { label: '教师工作台', href: '/teacher' },
  { label: '学生空间', href: '/student' },
  { label: '课堂运行', href: '/classroom' },
  { label: '课程中心', href: '/courses' },
  { label: '资源中心', href: '/resources' },
  { label: '管理后台', href: '/admin', emphasis: 'low' },
] as const
```

```tsx
// src/components/shell/glass-nav.tsx
export function GlassNav() {
  const pathname = usePathname()
  // maps navigationItems and must keep aria-label="主导航"
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild the public home as a compact Stitch-aligned landing shell</name>
  <files>src/components/surfaces/home-surface.tsx, src/lib/demo-data.ts, scripts/verify-phase1-shell.ts</files>
  <read_first>
    - src/components/surfaces/home-surface.tsx
    - src/lib/demo-data.ts
    - scripts/verify-phase1-shell.ts
    - .planning/phases/01-application-foundation-and-design-shell/01-HUMAN-UAT.md
    - .planning/phases/01-application-foundation-and-design-shell/01-UI-SPEC.md
    - DESIGN.md
  </read_first>
  <behavior>
    - Test 1: `pnpm verify:phase1` fails if `src/components/surfaces/home-surface.tsx` does not contain `开启智慧学习新篇章`, `学生登录`, `教师登录`, `10W+`, `500+`, `98%`, and `推荐课程`.
    - Test 2: `pnpm verify:phase1` fails if the home surface does not contain compact density markers `lg:pt-8`, `gap-4`, `rounded-[calc(var(--radius-shell)-0.75rem)]`, and `grid-cols-[0.92fr_1.08fr]`.
    - Test 3: `pnpm verify:phase1` still fails on forbidden visible terms `mock`, `seed`, `database`, and `占位符`.
  </behavior>
  <action>First update `scripts/verify-phase1-shell.ts` with a new check group named `Home visual density and navigation alignment verified`. The group must read `src/components/surfaces/home-surface.tsx` and require exact strings `开启智慧学习新篇章`, `学生登录`, `教师登录`, `10W+`, `500+`, `98%`, `推荐课程`, `lg:pt-8`, `gap-4`, `rounded-[calc(var(--radius-shell)-0.75rem)]`, and `grid-cols-[0.92fr_1.08fr]`. Then rewrite `HomeSurface` to close Gap 1: use the Stitch home screen as the visual reference, make the top section one-screen and tighter, use a left compact login/action card plus a right hero/course column, and keep the teacher-first product direction by routing `教师登录` and the primary `开始备课`/teacher CTA to `/teacher/editor` per D-07. Include `学生登录` routing to `/student`; keep `管理后台` low-emphasis and do not add fake auth. Use only Simplified Chinese copy; preserve 信息科技 / 编程基础 demo content from D-09 through D-12. Add typed home metric/course constants to `src/lib/demo-data.ts` only if needed to avoid scattering repeated copy. Avoid borders, dividers, pure black, default shadows, fake session state, DB/DAL imports, Server Actions, `cookies()`, `headers()`, and `fetch(`.</action>
  <verify>
    <automated>pnpm verify:phase1 && pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-phase1-shell.ts` contains exact string `Home visual density and navigation alignment verified`.
    - `scripts/verify-phase1-shell.ts` contains exact strings `开启智慧学习新篇章`, `学生登录`, `教师登录`, `10W+`, `500+`, `98%`, `推荐课程`, `lg:pt-8`, and `grid-cols-[0.92fr_1.08fr]`.
    - `src/components/surfaces/home-surface.tsx` contains exact string `开启智慧学习新篇章`.
    - `src/components/surfaces/home-surface.tsx` contains exact string `学生登录`.
    - `src/components/surfaces/home-surface.tsx` contains exact string `教师登录`.
    - `src/components/surfaces/home-surface.tsx` contains exact string `href="/teacher/editor"`.
    - `src/components/surfaces/home-surface.tsx` contains exact string `href="/student"`.
    - `src/components/surfaces/home-surface.tsx` contains exact string `grid-cols-[0.92fr_1.08fr]`.
    - `pnpm verify:phase1` exits 0.
  </acceptance_criteria>
  <done>Gap 1 is closed: the home page is compact, Stitch-referential, teacher-first, and guarded by an automated source check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recalibrate navigation, CTA, and dashboard interaction density</name>
  <files>src/components/shell/glass-nav.tsx, src/components/shell/sidebar.tsx, src/components/shell/route-shell.tsx, src/lib/navigation.ts, src/components/surfaces/teacher-dashboard-surface.tsx, src/components/surfaces/lesson-editor-surface.tsx, scripts/verify-phase1-shell.ts</files>
  <read_first>
    - src/components/shell/glass-nav.tsx
    - src/components/shell/sidebar.tsx
    - src/components/shell/route-shell.tsx
    - src/lib/navigation.ts
    - src/components/surfaces/teacher-dashboard-surface.tsx
    - src/components/surfaces/lesson-editor-surface.tsx
    - scripts/verify-phase1-shell.ts
    - .planning/phases/01-application-foundation-and-design-shell/01-HUMAN-UAT.md
    - .planning/phases/01-application-foundation-and-design-shell/01-UI-SPEC.md
    - DESIGN.md
  </read_first>
  <behavior>
    - Test 1: `pnpm verify:phase1` fails if `GlassNav` loses `aria-label="主导航"`, `overflow-x-auto`, `overscroll-x-contain`, `min-h-11`, or `backdrop-blur-xl`.
    - Test 2: `pnpm verify:phase1` fails if teacher/dashboard/editor CTA surfaces do not retain `开始备课`, `进入课堂`, and `/teacher/editor`.
    - Test 3: `pnpm verify:phase1` fails if source files contain `border-b`, `border-t`, `border-l`, `border-r`, `divide-`, `#000000`, or `text-black`.
  </behavior>
  <action>Extend the new `Home visual density and navigation alignment verified` check group in `scripts/verify-phase1-shell.ts` so it also validates navigation and CTA alignment: `src/components/shell/glass-nav.tsx` must contain `aria-label="主导航"`, `overflow-x-auto`, `overscroll-x-contain`, `min-h-11`, `backdrop-blur-xl`, and `focus-visible:outline-2`; `src/components/shell/sidebar.tsx` must contain `min-h-11`, `rounded-full`, and `focus-visible:outline-2`; `src/components/surfaces/teacher-dashboard-surface.tsx` must contain `开始备课`, `进入课堂`, and `href="/teacher/editor"`. Then update `GlassNav`, `Sidebar`, and `RouteShell` for Gap 2: make top navigation visually closer to the Stitch glass header by tightening widths/padding, preserving horizontal overflow on mobile, adding explicit focus-visible classes on links, and keeping every interactive target at least 44px high. Update `teacher-dashboard-surface.tsx` and `lesson-editor-surface.tsx` only where needed to reduce template-like looseness: use tighter `gap-4`/`p-5` clusters, keep rounded tonal cards, preserve `开始备课` and `进入课堂`, and do not add dense tables or divider lines. Keep all route labels from `navigationItems` unchanged unless changing presentation only; do not add fake login/session behavior despite Stitch login wording.</action>
  <verify>
    <automated>pnpm verify:phase1 && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/shell/glass-nav.tsx` contains exact string `aria-label="主导航"`.
    - `src/components/shell/glass-nav.tsx` contains exact string `overflow-x-auto`.
    - `src/components/shell/glass-nav.tsx` contains exact string `overscroll-x-contain`.
    - `src/components/shell/glass-nav.tsx` contains exact string `min-h-11`.
    - `src/components/shell/glass-nav.tsx` contains exact string `focus-visible:outline-2`.
    - `src/components/shell/sidebar.tsx` contains exact string `min-h-11`.
    - `src/components/shell/sidebar.tsx` contains exact string `focus-visible:outline-2`.
    - `src/components/surfaces/teacher-dashboard-surface.tsx` contains exact string `开始备课`.
    - `src/components/surfaces/teacher-dashboard-surface.tsx` contains exact string `进入课堂`.
    - `src/components/surfaces/teacher-dashboard-surface.tsx` contains exact string `href="/teacher/editor"`.
    - `pnpm build` exits 0 and prerenders `/`, `/teacher`, `/teacher/editor`, `/student`, `/student/player`, `/classroom`, `/courses`, `/resources`, and `/admin`.
  </acceptance_criteria>
  <done>Gap 2 is closed: navigation, CTA, mobile overflow, focus, and touch states are explicitly recalibrated and regression-guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User perception→Static demo shell | Stitch login labels may be mistaken for real authentication if copy or routing implies sessions. |
| Mobile touch navigation→Route changes | Public route navigation is interactive and must remain reachable without hidden overflow or broken focus states. |
| Verification script→Source tree | Local guard reads source and blocks regressions in design/copy invariants. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-21 | Spoofing | `home-surface.tsx` | mitigate | Keep `学生登录`/`教师登录` as demo route CTAs only, route them to public shells, and do not introduce auth/session/cookies/headers. |
| T-01-22 | Tampering | `scripts/verify-phase1-shell.ts` | mitigate | Add exact source checks for Stitch home copy, compact density markers, navigation overflow, focus-visible, and CTA routes. |
| T-01-23 | Denial of Service | `glass-nav.tsx` mobile navigation | mitigate | Preserve `overflow-x-auto`, `overscroll-x-contain`, `min-h-11`, and visible focus classes so navigation remains usable on narrow screens. |
| T-01-24 | Information Disclosure | home/dashboard surfaces | accept | Phase 1 uses deterministic non-sensitive demo data only; no DB, DAL, API, credentials, or user session data is introduced. |
</threat_model>

<verification>
Run `pnpm verify:phase1`, `pnpm typecheck`, `pnpm lint`, and `pnpm build`. Then open `/`, `/teacher`, and `/teacher/editor` locally for a visual spot-check against Stitch project `5322129002350954765`, focusing on home density, top glass navigation, teacher-first CTA clarity, 44px touch targets, and no-line tonal layering.
</verification>

<success_criteria>
Phase 1 no longer fails the diagnosed Human UAT gaps: the home page is compact and Stitch-aligned, navigation and CTA interactions remain reachable and visually aligned, and automated guards prevent the same density/navigation regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-application-foundation-and-design-shell/01-06-SUMMARY.md`.
</output>
