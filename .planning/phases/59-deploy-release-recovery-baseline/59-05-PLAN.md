---
phase: 59-deploy-release-recovery-baseline
plan: "05"
type: execute
wave: 4
depends_on:
  - 59-01
  - 59-02
  - 59-04
files_modified:
  - ops/deploy/backup.sh
  - ops/deploy/restore.sh
  - ops/deploy/verify-restore.sh
  - .planning/phases/59-deploy-release-recovery-baseline/59-RESTORE-DRILL.md
autonomous: true
requirements:
  - SAFE-03
  - ENVR-03
must_haves:
  truths:
    - "D-59-15: 备份对象同时覆盖 SQLite、runtime assets 与 env template，不只是一份 DB 文件，且真实 secrets 不进入备份快照。"
    - "D-59-16: restore 只有在 integrity_check、health/ready、sample smoke 全绿时才算成功。"
    - "D-59-17: 至少完成一次真实 restore drill；失败会被记录为 release blocker，而不是仅作为备注。"
    - "D-59-18: backup / restore 以 SQLite + DAL 为 canonical truth，Redis、WebSocket、BullMQ 只在恢复后重新附着。"
  artifacts:
    - path: "ops/deploy/backup.sh"
      provides: "SQLite + assets + env template backup baseline"
      contains: "VACUUM INTO"
    - path: "ops/deploy/restore.sh"
      provides: "truth-first restore script"
      contains: "backup-manifest.json"
    - path: "ops/deploy/verify-restore.sh"
      provides: "post-restore validation gate"
      contains: "PRAGMA integrity_check"
    - path: ".planning/phases/59-deploy-release-recovery-baseline/59-RESTORE-DRILL.md"
      provides: "executed restore drill outcome artifact"
      contains: "Sample-chain smoke"
  key_links:
    - from: "ops/deploy/backup.sh"
      to: "src/db/index.ts"
      via: "DB_FILE_NAME-backed SQLite snapshot"
      pattern: "VACUUM INTO"
    - from: "ops/deploy/verify-restore.sh"
      to: "src/app/api/ready/route.ts"
      via: "post-restore readiness gate"
      pattern: "/api/ready"
    - from: "ops/deploy/verify-restore.sh"
      to: "package.json"
      via: "sample-chain smoke command"
      pattern: "verify:phase57"
---

<objective>
Establish the pilot backup/restore baseline and the scriptable restore drill contract.

Purpose: satisfy SAFE-03 and D-59-15 through D-59-18 with truth-first recovery semantics.
Output: backup/restore/verify scripts plus one executed restore drill artifact.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-pilot-scope-and-acceptance-gate/55-PROOF-INVENTORY.md
@.planning/phases/55-pilot-scope-and-acceptance-gate/55-FAILURE-RECOVERY-MATRIX.md
@.planning/phases/59-deploy-release-recovery-baseline/59-CONTEXT.md
@.planning/phases/59-deploy-release-recovery-baseline/59-RESEARCH.md
@package.json
@src/db/index.ts
@drizzle.config.ts

<interfaces>
From `src/db/index.ts`:

```ts
const client = createClient({
  url: process.env.DB_FILE_NAME || "file:local.db",
});
```

From `package.json`:

```json
{
  "verify:phase57": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase57-classroom-runtime.ts"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 SQLite + assets + env template backup / restore scripts</name>
  <read_first>
    .planning/phases/59-deploy-release-recovery-baseline/59-CONTEXT.md
    .planning/phases/59-deploy-release-recovery-baseline/59-RESEARCH.md
    src/db/index.ts
    drizzle.config.ts
    ops/deploy/backup.sh
    ops/deploy/restore.sh
  </read_first>
  <files>ops/deploy/backup.sh, ops/deploy/restore.sh</files>
  <action>创建 `ops/deploy/backup.sh` 与 `ops/deploy/restore.sh`。`backup.sh` 按 D-59-15 / D-59-18 从 `DB_FILE_NAME` 指向的 SQLite truth 生成一致性快照：优先 `sqlite3 "$LIVE_DB_PATH" "VACUUM INTO '$SNAPSHOT_DB'"`；同时把 `${OPENLEARN_RUNTIME_ASSETS_ROOT}` 打成 `runtime-assets.tar.gz`，把 `.env.example` 或 `${OPENLEARN_SHARED_ROOT}/shared/.env.template` 复制成 `env.template`，并写 `backup-manifest.json`，字段包含 `backupId`、`createdAt`、`dbSnapshot`、`assetsArchive`、`envTemplate`、`sourceReleaseId`、`gitSha`。`restore.sh` 必须先读取 `backup-manifest.json`，再恢复 SQLite snapshot 与 runtime assets，不得把 Redis/BullMQ 当 truth source；真实 secrets 不从备份包恢复，只恢复 env template。两个脚本都提供 `--backup-dir`、`--db-path`、`--runtime-assets-root`、`--restore-root`、`--dry-run` 参数，禁止硬编码本机绝对路径。</action>
  <acceptance_criteria>
    - `backup.sh` 同时生成 SQLite snapshot、runtime assets archive、env template copy 与 `backup-manifest.json`。
    - `restore.sh` 以 `backup-manifest.json` 为入口，不直接猜测文件名。
    - 脚本中显式出现 `VACUUM INTO`，且没有把 Redis/BullMQ 当 restore success 条件。
  </acceptance_criteria>
  <verify>
    <automated>bash -n ops/deploy/backup.sh && bash -n ops/deploy/restore.sh && grep -q 'VACUUM INTO' ops/deploy/backup.sh && grep -q 'backup-manifest.json' ops/deploy/restore.sh</automated>
  </verify>
  <done>pilot 恢复所需的 truth objects 已经固定为 SQLite + runtime assets + env template，脚本参数可在单机环境复用。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 post-restore verification gate 与 restore drill artifact</name>
  <read_first>
    ops/deploy/backup.sh
    ops/deploy/restore.sh
    ops/deploy/verify-restore.sh
    .planning/phases/55-pilot-scope-and-acceptance-gate/55-PROOF-INVENTORY.md
    .planning/phases/55-pilot-scope-and-acceptance-gate/55-FAILURE-RECOVERY-MATRIX.md
    .planning/phases/59-deploy-release-recovery-baseline/59-RESTORE-DRILL.md
  </read_first>
  <files>ops/deploy/verify-restore.sh, .planning/phases/59-deploy-release-recovery-baseline/59-RESTORE-DRILL.md</files>
  <action>新增 `ops/deploy/verify-restore.sh`，执行顺序必须固定为：`sqlite3 "$DB_PATH" "PRAGMA integrity_check;"` → `sqlite3 "$DB_PATH" "PRAGMA foreign_key_check;"` → `curl -fsS "$BASE_URL/api/health"` → `curl -fsS "$BASE_URL/api/ready"` → `pnpm verify:phase57`。按 D-59-16 与 D-59-17，任一失败即非零退出，并输出 `RESTORE_BLOCKER` 标记。同步创建 `.planning/phases/59-deploy-release-recovery-baseline/59-RESTORE-DRILL.md` 的执行记录骨架，字段必须包含 `backupId`、`source release`、`restored target`、`integrity_check result`、`foreign_key_check result`、`health result`、`ready result`、`sample-chain smoke`、`release blocker? yes/no`、`operator notes`；这些字段在下一个 task 中必须被真实执行结果覆盖，不能保留模板占位。</action>
  <acceptance_criteria>
    - `verify-restore.sh` 同时包含 `PRAGMA integrity_check`、`PRAGMA foreign_key_check`、`/api/health`、`/api/ready`、`pnpm verify:phase57`。
    - 失败分支输出 `RESTORE_BLOCKER`，不允许“记录风险继续放行”。
    - `59-RESTORE-DRILL.md` 模板含 `Sample-chain smoke` 与 `release blocker` 字段。
  </acceptance_criteria>
  <verify>
    <automated>bash -n ops/deploy/verify-restore.sh && grep -q 'PRAGMA integrity_check' ops/deploy/verify-restore.sh && grep -q 'pnpm verify:phase57' ops/deploy/verify-restore.sh && grep -q 'Sample-chain smoke' .planning/phases/59-deploy-release-recovery-baseline/59-RESTORE-DRILL.md</automated>
  </verify>
  <done>restore success 的定义已经固定为 DB check + app check + sample smoke，且 drill 结果有标准化 artifact 容器可落盘。</done>
</task>

<task type="auto">
  <name>Task 3: 真实执行一次 restore drill 并把结果写入 `59-RESTORE-DRILL.md`</name>
  <read_first>
    ops/deploy/backup.sh
    ops/deploy/restore.sh
    ops/deploy/verify-restore.sh
    .planning/phases/59-deploy-release-recovery-baseline/59-RESTORE-DRILL.md
    .planning/phases/55-pilot-scope-and-acceptance-gate/55-FAILURE-RECOVERY-MATRIX.md
  </read_first>
  <files>.planning/phases/59-deploy-release-recovery-baseline/59-RESTORE-DRILL.md</files>
  <action>在执行阶段必须真实跑一次 drill，而不是只生成模板：创建独立 drill workspace（例如 `${OPENLEARN_SHARED_ROOT}/restore-drills/${DRILL_ID}` 或等价临时目录），先运行 `backup.sh` 生成真实 `backupId`，再用 `restore.sh` 把备份恢复到 throwaway restore root，然后以恢复出来的 truth 启动 web/worker 或把 `verify-restore.sh` 指向该 restore target 对应的 `BASE_URL`，最后执行 `verify-restore.sh`。把每一步真实输出、时间戳、`backupId`、source release、restore target、integrity/foreign-key 结果、`/api/health` 结果、`/api/ready` 结果、sample-chain smoke 结果、overall pass/fail 与 operator notes 写入 `59-RESTORE-DRILL.md`。若任何一步失败，必须在文档中明确写 `Release blocker: yes` 与失败原因，并让任务以非零退出终止，不允许把失败 drill 当作“后续再处理”的备注（D-59-16, D-59-17）。</action>
  <acceptance_criteria>
    - `59-RESTORE-DRILL.md` 填入真实 `backupId`、实际执行时间、restore target 路径、health/ready/sample smoke 结果，而非模板占位。
    - 文档中显式出现 `Release blocker: yes|no`，且失败时为 `yes` 并附失败命令/输出摘要。
    - 自动化命令真实执行 `backup.sh -> restore.sh -> verify-restore.sh` 链路，而不是只做 `bash -n` 或 grep。
  </acceptance_criteria>
  <verify>
    <automated>DRILL_ROOT="$(mktemp -d)" && bash ops/deploy/backup.sh --backup-dir "$DRILL_ROOT/backup" --db-path "${DB_FILE_NAME:-file:local.db}" --runtime-assets-root "${OPENLEARN_RUNTIME_ASSETS_ROOT:-./runtime-assets}" && bash ops/deploy/restore.sh --backup-dir "$DRILL_ROOT/backup" --restore-root "$DRILL_ROOT/restore" --db-path "$DRILL_ROOT/restore/local.db" --runtime-assets-root "$DRILL_ROOT/restore/runtime-assets" && bash ops/deploy/verify-restore.sh --db-path "$DRILL_ROOT/restore/local.db" --base-url "${OPENLEARN_HEALTHCHECK_BASE_URL:-http://127.0.0.1:3000}" && pnpm verify:phase59</automated>
  </verify>
  <done>Phase 59 至少有一份真实 restore drill 证据，且 SAFE-03 的 release-blocker 规则已通过实际执行路径固化。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backup snapshot → restored environment | 备份包进入目标环境时可能携带损坏或不完整 truth。 |
| restore verification script → release decision | verify 结果会直接决定是否继续放行 pilot baseline。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-09 | Tampering | `ops/deploy/backup.sh`, `ops/deploy/restore.sh` | mitigate | 使用 SQLite `VACUUM INTO` 和 manifest-driven restore；不直接复制活跃 DB 文件，不跳过 asset/env template。 |
| T-59-10 | Denial of Service | `ops/deploy/verify-restore.sh` | mitigate | 任一 integrity/health/ready/sample-smoke 失败立即退出并标记 `RESTORE_BLOCKER`，防止损坏基线继续对外接流量。 |
</threat_model>

<verification>
- `bash -n ops/deploy/backup.sh`
- `bash -n ops/deploy/restore.sh`
- `bash -n ops/deploy/verify-restore.sh`
</verification>

<success_criteria>
- 备份对象范围符合 D-59-15，不再只有 SQLite 单文件。
- restore 成功定义符合 D-59-16，不再停留在文件恢复成功。
- 至少完成一次真实 restore drill，结果可以作为 release blocker artifact 直接复查。
</success_criteria>

<output>
After completion, create `.planning/phases/59-deploy-release-recovery-baseline/59-05-SUMMARY.md`
</output>
