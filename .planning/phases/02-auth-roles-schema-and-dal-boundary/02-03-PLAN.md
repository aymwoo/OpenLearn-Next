---
phase: "02"
plan: "03"
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - src/lib/dto/user.ts
  - src/lib/dto/membership.ts
  - src/lib/dal/auth.ts
  - src/lib/dal/membership.ts
  - src/actions/auth-actions.ts
  - src/app/(teacher)/teacher/layout.tsx
  - src/app/(student)/student/layout.tsx
  - src/app/(admin)/admin/layout.tsx
autonomous: true
requirements:
  - AUTH-05
  - AUTH-06
  - DATA-03
  - DATA-04
  - AUTH-02
must_haves:
  truths:
    - "UI components receive only sanitized DTOs, not raw DB records"
    - "Protected workspaces verify memberships before rendering"
    - "Database logic is isolated behind `server-only` DAL modules"
  artifacts:
    - path: "src/lib/dal/auth.ts"
      provides: "Server-only logic for fetching current user DTO"
    - path: "src/lib/dto/membership.ts"
      provides: "Zod types and sanitization schemas for memberships"
    - path: "src/actions/auth-actions.ts"
      provides: "Server Actions enforcing boundaries for write operations"
  key_links:
    - from: "src/app/(teacher)/teacher/layout.tsx"
      to: "src/lib/dal/membership.ts"
      via: "membership verification check"
      pattern: "getUserMembershipsDTO\\("
---

<objective>
Implement the Data Access Layer (DAL) boundary, enforce Data Transfer Object (DTO) sanitization with Zod, and apply role verification in protected route layouts.

Purpose: Prevent UI from ever seeing raw database rows, securing sensitive fields, and ensuring that access to a workspace (`/teacher`, `/student`, `/admin`) is verified against real role memberships, not just an active session.
Output: Strongly typed DAL functions, Zod validation schemas, and secured route layouts.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-roles-schema-and-dal-boundary/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Zod DTOs and Schemas</name>
  <files>src/lib/dto/user.ts, src/lib/dto/membership.ts</files>
  <action>
Create `src/lib/dto/user.ts` defining `UserDTO` with Zod: `z.object({ id: z.string(), name: z.string().nullable(), email: z.string().nullable() })`. Omit password hashes, verification tokens, etc.
Create `src/lib/dto/membership.ts` defining `MembershipDTO`: `z.object({ id: z.string(), schoolId: z.string(), role: z.enum(['admin', 'teacher', 'student', 'parent', 'developer', 'ai_agent']), status: z.string() })`.
Export these schemas and infer TypeScript types for components to consume.
  </action>
  <verify>
    <automated>grep -q "z.object(" src/lib/dto/user.ts</automated>
  </verify>
  <done>User and Membership DTO schemas exist and explicitly drop sensitive fields.</done>
</task>

<task type="auto">
  <name>Task 2: Implement server-only DAL functions</name>
  <files>src/lib/dal/auth.ts, src/lib/dal/membership.ts</files>
  <action>
Create `src/lib/dal/auth.ts`. Add `import "server-only";` at the top.
Implement `getCurrentUserDTO()` that calls `auth()` to check session, fetches full user from `db`, parses it through `UserDTO`, and returns the safe object.
Create `src/lib/dal/membership.ts` with `import "server-only";`. Implement `getUserMembershipsDTO(userId)` that queries the `memberships` table for the user and parses them through `MembershipDTO`.
  </action>
  <verify>
    <automated>grep -q 'import "server-only"' src/lib/dal/auth.ts</automated>
  </verify>
  <done>DAL functions fetch raw rows, parse through Zod DTOs, and enforce server-only.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate DAL in Route Layouts and Actions</name>
  <files>src/app/(teacher)/teacher/layout.tsx, src/app/(student)/student/layout.tsx, src/app/(admin)/admin/layout.tsx, src/actions/auth-actions.ts</files>
  <action>
Update `src/app/(teacher)/teacher/layout.tsx`, `src/app/(student)/student/layout.tsx`, and `src/app/(admin)/admin/layout.tsx`.
Make them async Server Components. Call `getCurrentUserDTO()` and `getUserMembershipsDTO()`.
Verify that the user has a membership with the appropriate `role` ('teacher' for `/teacher`, etc.). If not, `redirect('/unauthorized')`.
Create `src/actions/auth-actions.ts` exporting safe Server Actions (`signInAction`, `signOutAction`) wrapping Auth.js. Apply Zod input validation to credentials before passing them to the adapter. Use `revalidateTag` or `redirect` appropriately.
  </action>
  <verify>
    <automated>grep -q "getUserMembershipsDTO" src/app/(teacher)/teacher/layout.tsx</automated>
  </verify>
  <done>Layouts successfully restrict access by actual membership roles using the DAL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DAL to UI | Server-only modules converting DB payloads to strict DTOs. |
| Client to Server Action | Action validating input via Zod. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | src/lib/dal/*.ts | mitigate | Enforce Zod DTO parsing to strip passwords/tokens before sending to UI. |
| T-02-04 | Privilege Elevation | src/app/(teacher)/* | mitigate | Explicit RBAC check against DB memberships in layouts, preventing IDOR. |
</threat_model>

<verification>
- Navigating to `/teacher` with an active session but no teacher membership redirects to `/unauthorized`.
- API endpoints/Actions validate via Zod schemas.
</verification>

<success_criteria>
Users are contained in their role-appropriate workspaces; UI code exclusively uses DTO types; Server actions guard all writes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-roles-schema-and-dal-boundary/02-03-SUMMARY.md`
</output>