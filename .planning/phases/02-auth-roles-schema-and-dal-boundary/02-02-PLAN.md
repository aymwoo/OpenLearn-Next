---
phase: "02"
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/lib/auth/auth.config.ts
  - src/lib/auth/auth.ts
  - src/app/api/auth/[...nextauth]/route.ts
  - src/proxy.ts
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/unauthorized/page.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-04
  - AUTH-02
must_haves:
  truths:
    - "Unauthenticated users hitting protected routes are redirected to login by `proxy.ts`"
    - "Auth.js can successfully initialize, sign in, and persist sessions to Drizzle"
  artifacts:
    - path: "src/lib/auth/auth.config.ts"
      provides: "Edge-safe authentication config"
    - path: "src/lib/auth/auth.ts"
      provides: "Full Node.js authentication config with DrizzleAdapter"
    - path: "src/proxy.ts"
      provides: "Next.js middleware route protection"
  key_links:
    - from: "src/proxy.ts"
      to: "src/lib/auth/auth.config.ts"
      via: "NextAuth Edge initialization"
      pattern: "authConfig"
---

<objective>
Setup Auth.js v5, split configurations (edge vs node), protect routes using Next.js Proxy (middleware), and create the initial login page and unauthorized error boundary.

Purpose: Implement robust, edge-compatible authentication checks that redirect unauthenticated traffic and support email/password sign-in.
Output: Working Auth.js endpoint, configured proxy, and functional login flow UI.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-roles-schema-and-dal-boundary/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup split Auth.js configuration</name>
  <files>src/lib/auth/auth.config.ts, src/lib/auth/auth.ts</files>
  <action>
Create `src/lib/auth/auth.config.ts` exporting an `authConfig` object with an empty `providers` array (to be filled later) and callbacks. Do NOT import DrizzleAdapter here.
Create `src/lib/auth/auth.ts` that imports `authConfig`, `db`, and `DrizzleAdapter`. Set up `NextAuth()` using `CredentialsProvider` (with dummy password hashing for now or `bcryptjs` if available) to allow login via email/password. Set `session.strategy = "database"`. Ensure `.env.local` contains `AUTH_SECRET`.
  </action>
  <verify>
    <automated>grep "DrizzleAdapter" src/lib/auth/auth.ts</automated>
  </verify>
  <done>auth.config.ts and auth.ts exist and correctly split edge/node concerns.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Next.js Proxy for route protection</name>
  <files>src/proxy.ts, src/app/api/auth/[...nextauth]/route.ts</files>
  <action>
Create `src/proxy.ts` (Next.js 16 terminology for middleware). Import `authConfig` from `src/lib/auth/auth.config.ts` (NOT `auth.ts` to avoid Edge runtime crashes).
Protect `/teacher`, `/student`, `/classroom`, and `/admin` routes. If `!session`, redirect to `/login` with `callbackUrl`. Leave `/` and `/courses` unprotected.
Create the NextAuth API route `src/app/api/auth/[...nextauth]/route.ts` importing the handlers from `src/lib/auth/auth.ts`.
  </action>
  <verify>
    <automated>grep -q "authConfig" src/proxy.ts</automated>
  </verify>
  <done>proxy.ts is edge-safe, correctly protecting workspace routes.</done>
</task>

<task type="auto">
  <name>Task 3: Create Login and Unauthorized UI pages</name>
  <files>src/app/(auth)/login/page.tsx, src/app/(auth)/unauthorized/page.tsx, src/app/page.tsx</files>
  <action>
Create `src/app/(auth)/login/page.tsx`. Implement a sign-in form using Server Actions (`signIn("credentials")`). Apply UI specs: Lexend font, tonal surfaces, gradient CTA for "登录". Handle `roleIntent` search param if present to display helper text.
Create `src/app/(auth)/unauthorized/page.tsx` showing "暂无权限访问此工作区，请切换角色或返回。". Provide a button to return to home or default workspace.
Update `src/app/page.tsx` CTA links (教师登录, 学生登录) to point to `/login?roleIntent=teacher` and `?roleIntent=student`.
  </action>
  <verify>
    <automated>grep -q "roleIntent" src/app/page.tsx</automated>
  </verify>
  <done>Login page authenticates users via Server Actions; CTA links pass role intent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client to Server | `proxy.ts` intercepting incoming navigation to protected routes |
| Edge to DB | `proxy.ts` strictly isolated from Node-based DB adapters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Spoofing | src/proxy.ts | mitigate | Use standard Auth.js session cookie verification before allowing access to route groups. |
</threat_model>

<verification>
- Visiting `/teacher` without session redirects to `/login`.
- Visiting `/login` rendering without runtime errors.
</verification>

<success_criteria>
`proxy.ts` guards the application boundaries and Auth.js correctly handles sign-ins using the Drizzle database.
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-roles-schema-and-dal-boundary/02-02-SUMMARY.md`
</output>