---
phase: 63-ai-draft-chain-into-draft-lesson-version
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/platform-core/commands/contracts.ts
  - src/features/platform-core/events/contracts.ts
  - src/lib/cache-policy.ts
autonomous: true
requirements: [DRAFT-01, DRAFT-03]
must_haves:
  truths:
    - "lesson.draft.persist 命令类型已在 contracts 四处平行登记，payload .strict() 且不含 teacherId"
    - "payload.steps 复用 lessonStepPayloadSchema（不造第二套 step schema）"
    - "lesson.draft.persisted summary-only 事件已在三处 union 登记，payload 不含 snapshotJson"
    - "cacheTags.draftLesson 产出 draft:${lessonId} tag"
  artifacts:
    - path: "src/features/platform-core/commands/contracts.ts"
      provides: "lesson.draft.persist 命令契约"
      contains: "lesson.draft.persist"
    - path: "src/features/platform-core/events/contracts.ts"
      provides: "lesson.draft.persisted 事件契约"
      contains: "lesson.draft.persisted"
    - path: "src/lib/cache-policy.ts"
      provides: "draftLesson cache tag"
      contains: "draftLesson"
  key_links:
    - from: "LessonDraftPersistPayloadSchema.steps"
      to: "lessonStepPayloadSchema"
      via: "z.array 复用既有 step schema"
      pattern: "z\\.array\\(lessonStepPayloadSchema\\)"
    - from: "lesson.draft.persisted 事件"
      to: "PlatformDomainEventSchema union"
      via: "discriminatedUnion 登记"
      pattern: "LessonDraftPersistedEventSchema"
---

<objective>
为 `lesson.draft.persist` 写型命令铺设契约层：在 `commands/contracts.ts` 四处平行登记命令类型与 `.strict()` payload（复用 `lessonStepPayloadSchema` 承载整课 steps），在 `events/contracts.ts` 三处 union 登记 `lesson.draft.persisted` summary-only 事件，并在 `cache-policy.ts` 新增 `draftLesson` tag。本 plan 为纯 schema/config 层，**不含任何 DB 逻辑**，故与 Plan 01 并行（Wave 1）。

Purpose: 提供 Plan 04 handler 所需的命令 payload 类型、事件契约与缓存 tag。契约层先行，handler/registry 后续在 Plan 04 消费。
Output: 命令契约 + 事件契约 + cache tag + 契约单测。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-ai-draft-chain-into-draft-lesson-version/63-RESEARCH.md
@.planning/phases/63-ai-draft-chain-into-draft-lesson-version/63-PATTERNS.md

<interfaces>
<!-- contracts.ts 现状（已读源码核验）-->
<!-- :28 命令类型数组 -->
```typescript
export const LessonDraftCommandTypes = ["lesson.draft.run"] as const;
```
<!-- :136-140 run payload 先例（.strict，teacherId 绝不入 payload）-->
```typescript
const LessonDraftRunPayloadSchema = z.object({
  lessonId: z.string().min(1),
  stepType: z.enum(["content", "task", "quiz"]),
  intent: z.string().min(1),
}).strict();
```
<!-- :142-154 PlatformCommandPayloadSchemas 注册表；:156-201 PlatformCommandSchema discriminated union（含 PlatformCommandEnvelopeSchema.extend 变体） -->
<!-- envelope 已内建 dedupeKey（contracts.ts:60: dedupeKey: z.string().min(1).optional()）— idempotencyKey 经此注入，payload 无需重复 -->

<!-- events/contracts.ts:215-225 事件模板 LessonDraftProducedEventSchema -->
```typescript
export const LessonDraftProducedEventSchema = z.object({
  eventType: z.literal("lesson.draft.produced"),
  category: z.literal("domain"),
  aggregateType: z.literal("lesson"),
  aggregateId: z.string().min(1),
  payload: LessonDraftProducedPayloadSchema,
  audit: PlatformAuditMetadataSchema.default({ delegatedActor: null, approval: null }),
}).strict();
```
<!-- 三处 union 登记点：PlatformEventSchema（:227-236 discriminatedUnion）/ PlatformDomainEventSchema（:238-245 union）/ PlatformSuccessOrDomainEventSchema（:247-250 自动含 domain） -->

<!-- lessonStepPayloadSchema 来源：src/lib/dto/lesson-authoring.ts:154-158（content/task/quiz discriminated union），可从 @/lib/dto/lesson-authoring 导入 -->

<!-- cache-policy.ts:6-7 tag 先例 -->
```typescript
lesson: (lessonId: string) => `lesson:${lessonId}`,
steps: (lessonId: string) => `steps:${lessonId}`,
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: contracts.ts 四处登记 lesson.draft.persist 命令 + payload</name>
  <files>src/features/platform-core/commands/contracts.ts</files>
  <action>
仿 `lesson.draft.run` 走位（PATTERNS Pattern 2），四处平行登记 `lesson.draft.persist`：

1. **命令类型**（:28）扩数组：
```typescript
export const LessonDraftCommandTypes = ["lesson.draft.run", "lesson.draft.persist"] as const;
```

2. **payload schema**（紧邻 `LessonDraftRunPayloadSchema` :136-140 之后新增，必须 `.strict()`，**teacherId/source 绝不入 payload**）。从 `@/lib/dto/lesson-authoring` 导入 `lessonStepPayloadSchema`，承载整课 steps（D-02 整课多步，A1/A2：调用方传入已校验包，persist 不内部生成）：
```typescript
const LessonDraftPersistPayloadSchema = z.object({
  lessonId: z.string().min(1),
  steps: z.array(lessonStepPayloadSchema).min(1),   // 复用既有 step schema，不造第二套
}).strict();
```
注：idempotencyKey 经 envelope `dedupeKey`（contracts.ts:60）注入，payload **不**重复携带。

3. **payload 注册表**（:142-154 `PlatformCommandPayloadSchemas`）追加：
```typescript
"lesson.draft.persist": LessonDraftPersistPayloadSchema,
```

4. **discriminated union 变体**（:156-201 `PlatformCommandSchema`，仿 run 的 `PlatformCommandEnvelopeSchema.extend`）追加：
```typescript
PlatformCommandEnvelopeSchema.extend({
  type: z.literal("lesson.draft.persist"),
  payload: LessonDraftPersistPayloadSchema,
}),
```

若 contracts.ts 已对 `lessonStepPayloadSchema` 有导入则复用；否则在文件顶部 import 区新增 `import { lessonStepPayloadSchema } from "@/lib/dto/lesson-authoring";`。`PlatformCommandType` 联合类型会自动纳入新成员，下游 `satisfies Record<PlatformCommandType, ...>`（registry，Plan 04）将强制登记完整性。
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>contracts.ts 四处含 `lesson.draft.persist`；payload `.strict()` 且 steps 复用 lessonStepPayloadSchema、无 teacherId 字段；`pnpm typecheck` 零错误。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: events/contracts.ts 新增 lesson.draft.persisted 事件（三处 union）+ cache tag</name>
  <files>src/features/platform-core/events/contracts.ts, src/lib/cache-policy.ts, src/features/platform-core/events/lesson-draft-persisted.contract.test.ts</files>
  <behavior>
    - Test 1: LessonDraftPersistedEventSchema 解析含 {draftVersionId, version, stepCount, source:'ai'} 的合法事件成功
    - Test 2: payload 含 snapshotJson 或任意 `*Json` 键时 `.strict()` 解析失败（summary-only 守卫，T-63-04）
    - Test 3: PlatformDomainEventSchema 能解析 lesson.draft.persisted 事件（三处 union 登记到位）
    - Test 4: cacheTags.draftLesson('L1') === 'draft:L1'
  </behavior>
  <action>
**events/contracts.ts**（仿 LessonDraftProducedEventSchema :215-225）新增 payload + 事件 schema：
```typescript
const LessonDraftPersistedPayloadSchema = z.object({
  draftVersionId: z.string().min(1),
  version: z.number().int().positive(),
  stepCount: z.number().int().nonnegative(),
  source: z.literal("ai"),
}).strict();                                  // summary-only：绝不含 snapshotJson

export const LessonDraftPersistedEventSchema = z.object({
  eventType: z.literal("lesson.draft.persisted"),
  category: z.literal("domain"),
  aggregateType: z.literal("lesson"),
  aggregateId: z.string().min(1),
  payload: LessonDraftPersistedPayloadSchema,
  audit: PlatformAuditMetadataSchema.default({ delegatedActor: null, approval: null }),
}).strict();
```
**三处 union 必须全加**（否则 bus 无法承载该事件）：
- `PlatformEventSchema` discriminatedUnion（:227-236）追加 `LessonDraftPersistedEventSchema`
- `PlatformDomainEventSchema` union（:238-245）追加 `LessonDraftPersistedEventSchema`
- 导出 payload type（仿 :266-268）：`export type LessonDraftPersistedPayload = z.infer<typeof LessonDraftPersistedPayloadSchema>;`

**cache-policy.ts**（:7 `steps` 之后追加）：
```typescript
draftLesson: (lessonId: string) => `draft:${lessonId}`,
```

**测试** `lesson-draft-persisted.contract.test.ts`：覆盖上述 behavior 四点（合法解析、summary-only 拒绝 snapshotJson、domain union 含该事件、cache tag 字面值）。
  </action>
  <verify>
    <automated>pnpm test -- src/features/platform-core/events/lesson-draft-persisted.contract.test.ts && pnpm typecheck</automated>
  </verify>
  <done>事件三处 union 登记到位；payload `.strict()` 拒绝快照泄漏；cacheTags.draftLesson 产出 `draft:${lessonId}`；测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调用方/LLM → command payload | 命令输入跨此边界，须 `.strict()` 校验且禁止携带 teacherId/source |
| handler → event ledger | 事件 payload 跨此边界进入审计账，须 summary-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-06 | Tampering | command payload（注入越权字段/畸形 step）| mitigate | `LessonDraftPersistPayloadSchema.strict()` 拒绝额外键；steps 经 `lessonStepPayloadSchema` 校验；teacherId/source 不在 payload（authorize 阶段闭包注入，见 Plan 04）|
| T-63-04 | Information Disclosure | lesson.draft.persisted 事件 payload 泄漏整包 snapshot | mitigate | payload `.strict()` 仅 id/version/stepCount/source；测试断言含 `*Json` 即拒绝（沿用 Phase 62 summary-only 守卫）|
</threat_model>

<verification>
- `pnpm typecheck` 零错误
- `pnpm test -- src/features/platform-core/events/lesson-draft-persisted.contract.test.ts` 绿
- `pnpm test -- src/features/platform-core/commands`（契约相关回归）绿
</verification>

<success_criteria>
- lesson.draft.persist 命令契约四处登记完整，payload .strict() 复用 step schema（DRAFT-01）
- lesson.draft.persisted summary-only 事件三处 union 登记，拒绝快照泄漏（DRAFT-03 provenance 事件）
- draftLesson cache tag 就位，供 Plan 04 invalidation 消费
</success_criteria>

<output>
完成后创建 `.planning/phases/63-ai-draft-chain-into-draft-lesson-version/63-03-SUMMARY.md`
</output>
