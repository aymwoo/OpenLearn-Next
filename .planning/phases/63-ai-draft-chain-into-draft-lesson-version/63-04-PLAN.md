---
phase: 63-ai-draft-chain-into-draft-lesson-version
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - src/features/platform-core/commands/handlers/lesson-draft.ts
  - src/features/platform-core/commands/registry.ts
  - src/features/platform-core/commands/handlers/lesson-draft.persist.test.ts
autonomous: true
requirements: [DRAFT-01, DRAFT-02, DRAFT-03]
must_haves:
  truths:
    - "lesson.draft.persist 在 registry 经 createPlatformCommandDefinition 注册，dedupe='required'"
    - "executeLessonDraftPersist 签名 ExecutionInput<LessonDraftPersistCommand>；授权经 authorizeLessonDraftCommand(command) 校验 schoolId（T-63-01），actor 取自 assertActiveTeacher().userId"
    - "handler 注入 sourceCommandId=command.id 与 createdById=teacherId（均闭包注入，不入 payload）调 DAL persistDraftLessonVersion"
    - "handler 经 successResult 返回 invalidation.tags 含 draft:${lessonId} 与 lesson:${lessonId}，emittedEvents 含 withAudit 包裹的 lesson.draft.persisted"
    - "lessonDraftCommandHandlers 导出对象含 lesson.draft.persist 键，satisfies 类型扩为 Record<\"lesson.draft.run\" | \"lesson.draft.persist\", ...>"
    - "双 dispatch（模拟 pending 崩溃重放）后 draftLessonVersions 仅 1 行（幂等双层兜底）"
  artifacts:
    - path: "src/features/platform-core/commands/handlers/lesson-draft.ts"
      provides: "executeLessonDraftPersist handler + lessonDraftCommandHandlers 扩 persist 键"
      contains: "executeLessonDraftPersist"
    - path: "src/features/platform-core/commands/registry.ts"
      provides: "lesson.draft.persist 注册项 dedupe:required"
      contains: "lesson.draft.persist"
    - path: "src/features/platform-core/commands/handlers/lesson-draft.persist.test.ts"
      provides: "handler 单测 + 幂等集成测试"
      contains: "persist"
  key_links:
    - from: "executeLessonDraftPersist"
      to: "persistDraftLessonVersion (DAL)"
      via: "授权后调用，闭包注入 command.id 作 sourceCommandId、teacherId 作 createdById"
      pattern: "persistDraftLessonVersion"
    - from: "registry lesson.draft.persist"
      to: "lessonDraftCommandHandlers[\"lesson.draft.persist\"]"
      via: "createPlatformCommandDefinition + dedupe:required 登记"
      pattern: "dedupe:\\s*\"required\""
---

<objective>
收口写链：在 `handlers/lesson-draft.ts` 新增 `executeLessonDraftPersist`（仿 `executeLessonDraftRun` 范式，签名 `ExecutionInput<LessonDraftPersistCommand>` → 复用 `authorizeLessonDraftCommand(command)` 走 `assertActiveTeacher` + schoolId 校验、`assertActiveTeacher().userId` 取 actor → 调 Plan 02 的 DAL → 经 `successResult` 返回 invalidation tags + `withAudit` 包裹的 `lesson.draft.persisted` 事件），把 `lesson.draft.persist` 登记进 `lessonDraftCommandHandlers` 导出对象（类型扩为联合），在 `registry.ts` 经 `createPlatformCommandDefinition` 以 `dedupe:"required"` 注册。以集成测试证明幂等双层：command-bus dedupe 短路终态 + 表层唯一约束兜 pending-崩溃-重放，双 dispatch 后 draft 仅 1 行。

Purpose: 把命令契约（Plan 03）与 DAL 写入（Plan 02）接通成可经 bus 调用的幂等写命令，完成 DRAFT-01/02/03 端到端闭环。
Output: handler + handlers 导出登记 + registry 注册 + handler 单测 + 幂等集成测试。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-ai-draft-chain-into-draft-lesson-version/63-RESEARCH.md
@.planning/phases/63-ai-draft-chain-into-draft-lesson-version/63-PATTERNS.md
@.planning/phases/63-ai-draft-chain-into-draft-lesson-version/63-02-PLAN.md
@.planning/phases/63-ai-draft-chain-into-draft-lesson-version/63-03-PLAN.md

<interfaces>
<!-- ===== handlers/lesson-draft.ts 现状真实契约（执行者必须照此对齐，勿凭记忆改形）===== -->

<!-- ExecutionInput / ExecutionResult（:34-39）—— handler 签名形状 -->
```typescript
type ExecutionInput<TCommand extends PlatformCommand = PlatformCommand> = {
  command: TCommand;
  attemptNumber: number;
};
type ExecutionResult = Awaited<ReturnType<PlatformCommandDefinition["execute"]>>;
```

<!-- 授权助手（:112-118）—— 返回 Promise<void>、单参数（command），不返回 actor -->
```typescript
async function authorizeLessonDraftCommand(command: PlatformCommand): Promise<void> {
  const scope = await assertActiveTeacher();
  if (!scope.schoolIds.includes(command.scope.schoolId)) {
    throw new Error("TEACHER_AUTH_REQUIRED");
  }
}
```

<!-- successResult 助手（:52-64）—— 返回形状唯一权威：字段名 resultSummary / invalidation / emittedEvents -->
```typescript
function successResult(input: {
  resultSummary: Record<string, unknown> | null;
  invalidation: { tags: string[] };
  emittedEvents: PlatformSuccessOrDomainEvent[];
}): ExecutionResult {
  return { resultSummary: input.resultSummary, invalidation: input.invalidation,
    emittedEvents: input.emittedEvents, failureEvent: null, failureAttribution: null };
}
```

<!-- 事件须 withAudit 包裹（:45-50, :152-184）；actor 取值范式（:126）-->
```typescript
function withAudit<TEvent>(event: TEvent, audit: PlatformAuditMetadata) { return { ...event, audit }; }
// actor：const { userId: teacherId } = await assertActiveTeacher();  // 绝不取自 payload/LLM
```

<!-- 导出登记现状（:189-194）—— Task 1 必须把 persist 追加进此对象并扩联合类型 -->
```typescript
export const lessonDraftCommandHandlers = {
  "lesson.draft.run": {
    authorize: ({ command }) => authorizeLessonDraftCommand(command),
    execute: (input) => executeLessonDraftRun(input as ExecutionInput<LessonDraftRunCommand>),
  },
} satisfies Record<"lesson.draft.run", Pick<PlatformCommandDefinition, "authorize" | "execute">>;
```

<!-- ===== registry.ts 现状真实契约（:9-13, :86-93）===== -->
<!-- 登记必须经 createPlatformCommandDefinition({ commandType, payloadSchema, dedupe, authorize, execute })
     authorize/execute 经 lessonDraftCommandHandlers["..."] 取；整对象 satisfies Record<PlatformCommandType, PlatformCommandDefinition> -->
```typescript
"lesson.draft.run": createPlatformCommandDefinition({
  commandType: "lesson.draft.run",
  payloadSchema: PlatformCommandPayloadSchemas["lesson.draft.run"],
  dedupe: "optional",
  authorize: lessonDraftCommandHandlers["lesson.draft.run"].authorize,
  execute: lessonDraftCommandHandlers["lesson.draft.run"].execute,
}),
```

<!-- ===== 依赖 plan 契约 ===== -->
<!-- Plan 02 DAL（63-02 :88-92）：persistDraftLessonVersion(input: { lessonId, steps, sourceCommandId, createdById })
       → { draftVersionId, version, stepCount }。注：DAL 不吞唯一约束冲突——冲突向上抛（63-02 :113），由本 plan handler 幂等策略处理。 -->
<!-- Plan 03 事件：LessonDraftPersistedEventSchema，payload {draftVersionId, version, stepCount, source:'ai'}（summary-only，无 snapshotJson） -->
<!-- Plan 03 cache tag：cacheTags.draftLesson(lessonId) = `draft:${lessonId}`；cacheTags.lesson(lessonId) = `lesson:${lessonId}`（@/lib/cache-policy） -->
<!-- bus.ts:266 dedupe 仅对“终态”命令短路；pending 崩溃会重放 → 故需表层唯一约束兜底（RESEARCH Pitfall 1） -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 executeLessonDraftPersist handler 并登记进 lessonDraftCommandHandlers</name>
  <files>src/features/platform-core/commands/handlers/lesson-draft.ts</files>
  <behavior>
    - Test 1: 合法 teacher（schoolId 命中 scope）→ authorizeLessonDraftCommand 通过 → 调 DAL，resultSummary 含 draftVersionId/version/stepCount
    - Test 2: command.scope.schoolId ∉ teacher scope → authorizeLessonDraftCommand 抛 TEACHER_AUTH_REQUIRED，DAL 未被调用（T-63-01）
    - Test 3: 返回 invalidation.tags 含 `draft:${lessonId}` 与 `lesson:${lessonId}`
    - Test 4: emittedEvents 含一条 withAudit 包裹的 lesson.draft.persisted，payload summary-only（无 snapshotJson / 无 *Json 键）
    - Test 5: 传入 command.id 作 sourceCommandId、assertActiveTeacher().userId 作 createdById 流入 DAL（mock 断言入参，二者均不来自 payload）
  </behavior>
  <action>
仿 `executeLessonDraftRun`（:120-187）新增导出 handler。**签名是 `ExecutionInput<LessonDraftPersistCommand>`，不是 `(ctx, command)`**；actor 经 `assertActiveTeacher()` 解构 `userId`，**不**从授权助手取（authorizeLessonDraftCommand 返回 void）。

```typescript
type LessonDraftPersistCommand = Extract<PlatformCommand, { type: "lesson.draft.persist" }>;

async function executeLessonDraftPersist(
  input: ExecutionInput<LessonDraftPersistCommand>,
): Promise<ExecutionResult> {
  const { command } = input;
  const lessonId = command.payload.lessonId;

  // 1) 授权：schoolId 越权写他校 draft 拦截（T-63-01）。单参数、返回 void。
  await authorizeLessonDraftCommand(command);

  // 2) actor 取自已鉴权闭包（绝不取自 payload/LLM），仿 :126。
  const { userId: teacherId } = await assertActiveTeacher();

  // 3) 调 Plan 02 DAL；sourceCommandId/createdById 均闭包注入，不入 payload。
  const { draftVersionId, version, stepCount } = await persistDraftLessonVersion({
    lessonId,
    steps: command.payload.steps,
    sourceCommandId: command.id,   // provenance + 表层幂等键
    createdById: teacherId,        // 起草教师 id → 表 createdById（FK→users）
  });

  // 4) 返回 bus 约定形状：successResult，事件 withAudit 包裹（仿 :142-186）。
  return successResult({
    resultSummary: { draftVersionId, version, stepCount },
    invalidation: { tags: [cacheTags.draftLesson(lessonId), cacheTags.lesson(lessonId)] },
    emittedEvents: [
      withAudit({
        eventType: "lesson.draft.persisted",
        category: "domain",
        aggregateType: "lesson",
        aggregateId: lessonId,
        payload: { draftVersionId, version, stepCount, source: "ai" },
      }, command.audit),
    ],
  });
}
```

随后把 persist 登记进导出对象并**扩联合类型**（:189-194 → 否则 registry 引用编译失败）：

```typescript
export const lessonDraftCommandHandlers = {
  "lesson.draft.run": {
    authorize: ({ command }) => authorizeLessonDraftCommand(command),
    execute: (input) => executeLessonDraftRun(input as ExecutionInput<LessonDraftRunCommand>),
  },
  "lesson.draft.persist": {
    authorize: ({ command }) => authorizeLessonDraftCommand(command),
    execute: (input) => executeLessonDraftPersist(input as ExecutionInput<LessonDraftPersistCommand>),
  },
} satisfies Record<"lesson.draft.run" | "lesson.draft.persist", Pick<PlatformCommandDefinition, "authorize" | "execute">>;
```

约束：
- **不**在 execute 内调用 `updateTag()`（写后失效由触发起草的 Server Action 依据返回的 invalidation.tags 处理，符合 STACK caching 约定 / RESEARCH Anti-Pattern）。
- **不**在 persist handler 内调用 `lesson.draft.run` / AI 生成（D-05 解耦）。
- teacherId/source 绝不来自 payload；source 已在 DAL 硬编码 'ai'。
- import：`persistDraftLessonVersion`（@/lib/dal/lesson-authoring，同文件已 import `assertActiveTeacher`）、`cacheTags`（@/lib/cache-policy）。
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>executeLessonDraftPersist 已定义、签名为 ExecutionInput&lt;LessonDraftPersistCommand&gt;；授权经 authorizeLessonDraftCommand(command) 单参数；actor 取 assertActiveTeacher().userId；注入 command.id 作 sourceCommandId、teacherId 作 createdById；经 successResult 返回 invalidation tags + withAudit 包裹 summary-only 事件；lessonDraftCommandHandlers 含 persist 键且 satisfies 联合类型；不在 execute 内 updateTag；typecheck 绿。</done>
</task>

<task type="auto">
  <name>Task 2: registry.ts 经 createPlatformCommandDefinition 注册 lesson.draft.persist（dedupe:required）</name>
  <files>src/features/platform-core/commands/registry.ts</files>
  <action>
在 `platformCommandRegistry` 对象内、`"lesson.draft.run"` 项之后追加 `"lesson.draft.persist"` 项，**照搬 `:86-92` 结构**（经 `createPlatformCommandDefinition`，authorize/execute 经 `lessonDraftCommandHandlers["lesson.draft.persist"]` 取），唯一差异 `dedupe:"required"`：

```typescript
"lesson.draft.persist": createPlatformCommandDefinition({
  commandType: "lesson.draft.persist",
  payloadSchema: PlatformCommandPayloadSchemas["lesson.draft.persist"],
  dedupe: "required",     // 命令层幂等：bus 对同 dedupeKey 复用同一 command 记录（幂等双层之第一层）
  authorize: lessonDraftCommandHandlers["lesson.draft.persist"].authorize,
  execute: lessonDraftCommandHandlers["lesson.draft.persist"].execute,
}),
```

整个映射仍 `satisfies Record<PlatformCommandType, PlatformCommandDefinition>`（:93）——Plan 03 已把 `lesson.draft.persist` 纳入 `PlatformCommandType`，故缺登记会 typecheck 失败（登记完整性由类型兜底）。无需新增 import：`lessonDraftCommandHandlers` 与 `createPlatformCommandDefinition`、`PlatformCommandPayloadSchemas` 均已在文件顶部 import（:1-7）。
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>registry 含 lesson.draft.persist，经 createPlatformCommandDefinition、dedupe:"required"、authorize/execute 指向 lessonDraftCommandHandlers["lesson.draft.persist"]；satisfies Record&lt;PlatformCommandType, ...&gt; 通过；typecheck 绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: handler 单测 + 幂等双层集成测试</name>
  <files>src/features/platform-core/commands/handlers/lesson-draft.persist.test.ts</files>
  <behavior>
    - 单测：覆盖 Task 1 behavior Test 1-5（授权通过/拒绝、invalidation tags、withAudit summary-only 事件、sourceCommandId=command.id 与 createdById=userId 注入）
    - 集成（幂等核心）：经 bus dispatch 同一 dedupeKey 两次 → draftLessonVersions 仅 1 行（终态 dedupe 短路），两次返回同 draftVersionId
    - 集成（重放兜底）：模拟 pending-崩溃-重放（同 command.id 二次进入执行，绕过终态短路）→ 表层唯一约束 (lessonId, sourceCommandId) 触发，最终仍仅 1 行，无未捕获异常逃逸
  </behavior>
  <action>
新建 co-located 测试。两段：

1. **handler 单测**（mock 夹具仿 `lesson-draft.events.test.ts:1-24`）：`vi.mock("server-only")`；`vi.hoisted` mock `assertActiveTeacher` + DAL `persistDraftLessonVersion`；`import { lessonDraftCommandHandlers } from "./lesson-draft"`，经 `lessonDraftCommandHandlers["lesson.draft.persist"].execute({ command, attemptNumber })` 驱动。验证：
   - 授权分支：assertActiveTeacher 返回命中 schoolId → 调 DAL；返回 scope 不含 command.scope.schoolId → execute 抛 TEACHER_AUTH_REQUIRED 且 DAL `not.toHaveBeenCalled()`（T-63-01）。
   - DAL 入参：`sourceCommandId === command.id`、`createdById === <mock userId>`、`lessonId/steps` 来自 payload。
   - 返回：`invalidation.tags` 集合等于 `[draft:${lessonId}, lesson:${lessonId}]`；`emittedEvents[0].eventType === "lesson.draft.persisted"` 且 `audit` 存在（withAudit）；断言事件 payload 无任何 `*Json` 键（summary-only）。

2. **幂等集成测试**（真 DB 夹具，参照仓库既有 bus/handler 集成测试搭法 + `bus.test.ts:279-298` 行数断言夹具）：
   - seed lesson + steps。先 `grep` 既有 bus 集成测试确认 dispatch API 与 dedupeKey 注入方式，照搬夹具。
   - **用例 A（dedupe 短路）**：经 command bus 以相同 `dedupeKey` dispatch persist 两次 → 查 `draftLessonVersions where lessonId` 断言 `count === 1`，且两次返回同 draftVersionId（bus 复用终态结果，invalidation 短路返回空 tags 属正常）。
   - **用例 B（pending 重放兜底，RESEARCH Pitfall 1）**：构造“同 command.id 二次进入执行”的场景（直接二次调用同 command.id，或在 bus 上模拟 pending 重放），断言**固化终态语义**：DAL 不吞约束冲突（63-02 :113），故第二次 INSERT 撞 (lessonId, sourceCommandId) 唯一约束。**期望终态二选一并在测试旁注明确**——
     - 期望 A（受控错误且不增行）：第二次执行抛受控错误（约束冲突），`draftLessonVersions where lessonId` 终态 `count === 1`，无新增行、无未捕获异常逃逸到测试框架之外。
     - 期望 B（幂等返回既有版本）：handler/DAL 捕获约束冲突并返回既有 draftVersionId，终态 `count === 1`。
     执行者按本 plan 实际 handler 行为选定其一并在断言旁注明所固化语义（注释写明“pending-replay 终态：受控错误 / 幂等返回，count 恒 1”），不得让行数 >1 或异常裸逃。
  </action>
  <verify>
    <automated>pnpm test -- src/features/platform-core/commands/handlers/lesson-draft.persist.test.ts</automated>
  </verify>
  <done>单测覆盖授权/注入（sourceCommandId+createdById）/withAudit summary-only 事件/失效 tags；集成用例 A、B 双 dispatch 后 draftLessonVersions 行数恒为 1（dedupe + 唯一约束双层证明），用例 B 旁注明固化的 pending-replay 终态语义；测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端/调用方 → command bus | 命令入口；授权（assertActiveTeacher + schoolId）必须在 handler 内，不依赖 proxy |
| bus 重试/重放 → DAL 写入 | pending 崩溃可重放同 command；写入须幂等 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Elevation of Privilege | executeLessonDraftPersist 越权写他校 draft | mitigate | authorizeLessonDraftCommand(command) 校验 command.scope.schoolId ∈ assertActiveTeacher().schoolIds；Task 3 单测断言不匹配即拒绝、DAL 未调用 |
| T-63-02 | Replay (Spoofing/Tampering) | bus 重放同 command 致重复 draft 行 | mitigate | 双层幂等：registry dedupe:"required"（终态短路）+ 表层唯一约束 (lessonId, sourceCommandId)（pending 重放兜底）；Task 3 集成用例 A/B 断言 count==1 |
| T-63-05 | Tampering | 绕 bus 直写 draft 表 | mitigate | 写逻辑收敛于 server-only DAL，仅经 handler 调用；sourceCommandId 来自真实 command.id；DAL 不导出给客户端 |
| T-63-04 | Information Disclosure | persisted 事件泄漏整快照 | mitigate | 事件 payload 由 Plan 03 `.strict()` summary-only；Task 3 断言无 `*Json` 键 |
| T-63-03 | Tampering | teacherId/source 由调用方伪造入 payload | mitigate | createdById 取 assertActiveTeacher().userId、source DAL 硬编码 'ai'；payload 经 `.strict()` 不含二者；Task 3 断言入参来自闭包非 payload |
| T-63-06 | Repudiation | draft 写入无审计回链 | mitigate | sourceCommandId 落库回链产出命令；emittedEvents 经 withAudit(command.audit) 承载审计元数据 |
| T-63-07 | Denial of Service | 重放风暴产生大量 draft 行 | accept | dedupe:"required" + 唯一约束天然抑制同命令重复；命令入口受 assertActiveTeacher 限于活跃教师，低风险 |
</threat_model>

<verification>
- `pnpm test -- src/features/platform-core/commands/handlers/lesson-draft.persist.test.ts` 绿
- `pnpm test -- src/features/platform-core/commands`（命令域回归）绿
- `pnpm typecheck` 零错误
- `pnpm lint`（改动文件）通过
</verification>

<success_criteria>
- lesson.draft.persist 可经 bus dispatch，授权 + DAL 写入 + 事件 + 失效闭环（DRAFT-01/02/03）
- 幂等双层证明：双 dispatch / pending 重放后 draftLessonVersions 恒 1 行，用例 B 终态语义已固化
- 事件 summary-only、teacherId/createdById 不入 payload、不在 execute 内 updateTag —— 全部不变式守住
</success_criteria>

<output>
完成后创建 `.planning/phases/63-ai-draft-chain-into-draft-lesson-version/63-04-SUMMARY.md`
</output>
