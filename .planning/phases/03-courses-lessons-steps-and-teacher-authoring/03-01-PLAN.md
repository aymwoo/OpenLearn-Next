---
phase: 03-courses-lessons-steps-and-teacher-authoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/lib/dto/lesson-authoring.ts
  - src/lib/ranking/lexorank.ts
  - src/lib/ranking/lexorank.test.ts
autonomous: true
requirements:
  - LESSON-01
  - LESSON-02
  - LESSON-03
  - LESSON-04
  - LESSON-06
  - LESSON-07
must_haves:
  truths:
    - "D-01: Teacher authoring data has SQLite tables for courses, enrollments, lessons, ordered steps, materials, and published lesson versions."
    - "D-03: Draft steps support exactly content, task, and quiz payload contracts."
    - "D-04: Step ordering uses sortable rank strings instead of integer positions."
    - "D-02: Published lesson versions are represented as stable snapshots separate from mutable drafts."
    - "D-07: DTO contracts define sanitized UI-facing authoring shapes."
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Phase 3 Drizzle schema for course/lesson/step authoring"
      contains: "publishedLessonVersions"
    - path: "src/lib/dto/lesson-authoring.ts"
      provides: "Zod DTO and payload schemas for teacher authoring"
      exports: ["CourseDTOSchema", "LessonEditorDTOSchema", "lessonStepPayloadSchema"]
    - path: "src/lib/ranking/lexorank.ts"
      provides: "Rank string helper for insert before/after/between"
      exports: ["createInitialRank", "createRankBefore", "createRankAfter", "createRankBetween"]
  key_links:
    - from: "src/db/schema.ts"
      to: "src/lib/dto/lesson-authoring.ts"
      via: "DTO field names mirror persisted authoring records"
      pattern: "lessonStepPayloadSchema"
    - from: "src/lib/ranking/lexorank.ts"
      to: "src/db/schema.ts"
      via: "lessonSteps.rank stores sortable strings"
      pattern: "rank.*text"
---

<objective>
Create the Phase 3 authoring data contracts: Drizzle tables, DTO schemas, step
payload validation, and rank-string ordering.

Purpose: This establishes the stable data shape for every downstream DAL,
Server Action, and UI plan while honoring D-01: teacher-owned authoring data,
D-02: separate published snapshots, D-03: content/task/quiz payload contracts,
D-04: LexoRank-style rank strings, and D-07: sanitized DTO boundaries.
Output: Schema, DTOs, rank helper, and rank tests ready for DAL implementation.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-courses-lessons-steps-and-teacher-authoring/03-CONTEXT.md
@.planning/phases/03-courses-lessons-steps-and-teacher-authoring/03-RESEARCH.md
@DESIGN.md

<interfaces>
Existing schema exports from `src/db/schema.ts` include `users`, `schools`,
`memberships`, `classes`, and `classMembers`. Follow the existing SQLite style:

```typescript
export const classes = sqliteTable("class", {
  id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
  schoolId: text("schoolId").notNull().references(() => schools.id, { onDelete: "cascade" }),
  name: text("name").notNull(),
});
```

Existing DTO pattern from `src/lib/dto/membership.ts`:

```typescript
export const MembershipDTOSchema = z.object({
  id: z.string(),
  schoolId: z.string(),
  role: z.enum(['admin', 'teacher', 'student', 'parent', 'developer', 'ai_agent']),
  status: z.string(),
});
export type MembershipDTO = z.infer<typeof MembershipDTOSchema>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend authoring schema and DTO contracts</name>
  <files>src/db/schema.ts, src/lib/dto/lesson-authoring.ts</files>
  <read_first>
    - src/db/schema.ts
    - src/lib/dto/membership.ts
    - .planning/phases/03-courses-lessons-steps-and-teacher-authoring/03-CONTEXT.md
    - .planning/phases/03-courses-lessons-steps-and-teacher-authoring/03-RESEARCH.md
  </read_first>
  <behavior>
    - Zod accepts valid `content`, `task`, and `quiz` payloads and rejects unknown step types.
    - Drizzle schema contains no `position` column for lesson steps.
    - Every parent-owned Phase 3 foreign key uses `{ onDelete: "cascade" }`.
  </behavior>
  <action>Add Phase 3 tables to `src/db/schema.ts` per D-01, D-02, D-03, and D-04: `courses`, `courseClasses`, `courseEnrollments`, `lessons`, `lessonSteps`, `lessonMaterials`, and `publishedLessonVersions`. Use existing `sqliteTable`, `text`, `integer`, `index`, and `primaryKey` imports. Required fields: `courses.id`, `courses.schoolId`, `courses.ownerId`, `courses.title`, `courses.subject`, `courses.grade`, `courses.status`, `courses.createdAt`, `courses.updatedAt`; `lessons.id`, `lessons.courseId`, `lessons.createdById`, `lessons.title`, `lessons.objective`, `lessons.status`, `lessons.revision`, `lessons.publishedVersionId`, `lessons.createdAt`, `lessons.updatedAt`; `lessonSteps.id`, `lessonSteps.lessonId`, `lessonSteps.type`, `lessonSteps.title`, `lessonSteps.rank`, `lessonSteps.payloadJson`, `lessonSteps.archivedAt`, `lessonSteps.createdAt`, `lessonSteps.updatedAt`; `publishedLessonVersions.id`, `publishedLessonVersions.lessonId`, `publishedLessonVersions.version`, `publishedLessonVersions.snapshotJson`, `publishedLessonVersions.publishedById`, `publishedLessonVersions.publishedAt`. Add indexes named `courses_schoolId_idx`, `courses_ownerId_idx`, `courseEnrollments_courseId_idx`, `courseEnrollments_studentId_idx`, `lessons_courseId_idx`, `lessonSteps_lessonId_rank_idx`, and `publishedLessonVersions_lessonId_version_idx`. Do not add an integer `position` column.

Create `src/lib/dto/lesson-authoring.ts` per D-03 and D-07. Export `contentStepPayloadSchema`, `taskStepPayloadSchema`, `quizStepPayloadSchema`, `lessonStepPayloadSchema`, `CourseDTOSchema`, `ClassRosterDTOSchema`, `LessonSummaryDTOSchema`, `LessonStepDTOSchema`, `LessonMaterialDTOSchema`, `LessonEditorDTOSchema`, `AutosaveResultDTOSchema`, and `PublishResultDTOSchema`, plus inferred TypeScript types. Payload schemas must include `type: z.literal("content" | "task" | "quiz")`; the discriminated union must be `z.discriminatedUnion("type", [...])`.</action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/schema.ts` contains `export const courses = sqliteTable`.
    - `src/db/schema.ts` contains `export const lessonSteps = sqliteTable`.
    - `src/db/schema.ts` contains `lessonSteps_lessonId_rank_idx`.
    - `src/db/schema.ts` contains `export const publishedLessonVersions = sqliteTable`.
    - `src/db/schema.ts` has zero non-comment matches for `position` in the lesson step table section.
    - `src/lib/dto/lesson-authoring.ts` contains `z.discriminatedUnion("type"`.
    - `src/lib/dto/lesson-authoring.ts` contains `z.literal("content")`, `z.literal("task")`, and `z.literal("quiz")`.
  </acceptance_criteria>
  <done>Authoring schema and DTO contracts exist, typecheck passes, and step ordering is represented by `rank` only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement rank-string helper and tests</name>
  <files>src/lib/ranking/lexorank.ts, src/lib/ranking/lexorank.test.ts</files>
  <read_first>
    - package.json
    - src/lib/utils.ts
    - .planning/phases/03-courses-lessons-steps-and-teacher-authoring/03-CONTEXT.md
  </read_first>
  <behavior>
    - `createInitialRank()` returns a non-empty sortable string.
    - `createRankBefore(rank)` returns a rank that sorts before `rank`.
    - `createRankAfter(rank)` returns a rank that sorts after `rank`.
    - `createRankBetween(left, right)` returns a rank where `left < rank < right`.
  </behavior>
  <action>Create `src/lib/ranking/lexorank.ts` per D-04. Export `createInitialRank()`, `createRankBefore(rank: string)`, `createRankAfter(rank: string)`, and `createRankBetween(leftRank: string, rightRank: string)`. Implement deterministic sortable string ranks using a base-62 alphabet such as `0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz`; reject invalid inputs with `throw new Error("Invalid rank")`. Do not introduce integer `position` semantics. Create `src/lib/ranking/lexorank.test.ts` with Vitest tests for before, after, between, repeated between insertions, and invalid-order rejection.</action>
  <verify>
    <automated>pnpm test -- src/lib/ranking/lexorank.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/ranking/lexorank.ts` exports all four required functions.
    - `src/lib/ranking/lexorank.ts` contains `Invalid rank`.
    - `src/lib/ranking/lexorank.test.ts` contains `createRankBetween`.
    - `pnpm test -- src/lib/ranking/lexorank.test.ts` exits 0.
  </acceptance_criteria>
  <done>Rank helper supports stable sortable insertion without changing unrelated step records.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push updated Drizzle schema</name>
  <files>local.db</files>
  <read_first>
    - drizzle.config.ts
    - src/db/schema.ts
  </read_first>
  <action>Run the mandatory SQLite schema push after schema edits: `pnpm exec drizzle-kit push`. This is required because Phase 3 modifies `src/db/schema.ts`; TypeScript checks alone do not prove the live SQLite schema has the new authoring tables.</action>
  <verify>
    <automated>pnpm exec drizzle-kit push</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec drizzle-kit push` exits 0.
    - `local.db` exists after the command.
  </acceptance_criteria>
  <done>SQLite schema is pushed locally with Phase 3 authoring tables available.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema -> DAL | Persistent authoring records later cross into sanitized DTOs. |
| Draft -> Published snapshot | Mutable teacher drafts become stable student/classroom-readable versions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `lessonSteps.rank` | mitigate | Use rank-string helper and `(lessonId, rank)` index; do not use integer positions that update unrelated rows. |
| T-03-02 | Information Disclosure | `lessons` vs `publishedLessonVersions` | mitigate | Store mutable drafts separately from stable published snapshots so downstream phases read published versions only. |
| T-03-03 | Denial of Service | malformed step payloads | mitigate | Define Zod discriminated union payload schemas for `content`, `task`, and `quiz`. |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit`
- `pnpm test -- src/lib/ranking/lexorank.test.ts`
- `pnpm exec drizzle-kit push`
</verification>

<success_criteria>
Phase 3 has schema, DTO, and rank contracts that cover LESSON-01, LESSON-02,
LESSON-03, LESSON-04, LESSON-06, and LESSON-07 without exposing raw DB rows to UI.
</success_criteria>

<output>
After completion, create `.planning/phases/03-courses-lessons-steps-and-teacher-authoring/03-01-SUMMARY.md`
</output>
