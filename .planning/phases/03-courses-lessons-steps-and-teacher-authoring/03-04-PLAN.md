---
phase: 03-courses-lessons-steps-and-teacher-authoring
plan: 04
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - src/app/(teacher)/teacher/editor/page.tsx
  - src/components/surfaces/lesson-editor-surface.tsx
  - src/components/authoring/lesson-authoring-workspace.tsx
  - src/components/authoring/lesson-step-editor.tsx
  - src/components/authoring/authoring-status-panel.tsx
autonomous: true
requirements:
  - LESSON-01
  - LESSON-02
  - LESSON-03
  - LESSON-04
  - LESSON-05
  - LESSON-06
  - LESSON-07
  - LESSON-08
must_haves:
  truths:
    - "D-01: Teacher can see course/class context, lesson list, and enrolled-student context in the editor."
    - "D-02: Teacher sees draft-hidden and published-version visibility states in the editor."
    - "D-03: Teacher can create/edit/duplicate/archive/publish lessons and manage content/task/quiz steps from the UI."
    - "D-04: Teacher can reorder steps with rank-backed actions and visible move controls."
    - "D-05: Teacher sees autosave, publish, conflict, and cache freshness feedback."
    - "D-06: Authoring UI uses Simplified Chinese and follows DESIGN.md no-line tonal surface rules."
    - "D-07: Authoring UI receives DTO props and does not import the database."
    - "D-08: Authoring UI closes the `pnpm verify:phase3` verification gate."
  artifacts:
    - path: "src/app/(teacher)/teacher/editor/page.tsx"
      provides: "Server route loading authoring DTO through DAL"
    - path: "src/components/surfaces/lesson-editor-surface.tsx"
      provides: "Data-backed teacher editor surface"
    - path: "src/components/authoring/lesson-authoring-workspace.tsx"
      provides: "Interactive client authoring controls"
    - path: "src/components/authoring/lesson-step-editor.tsx"
      provides: "Content/task/quiz step editor controls"
    - path: "src/components/authoring/authoring-status-panel.tsx"
      provides: "Autosave, publish, conflict, freshness feedback"
  key_links:
    - from: "src/app/(teacher)/teacher/editor/page.tsx"
      to: "src/lib/dal/lesson-authoring.ts"
      via: "getTeacherAuthoringOverview/getLessonEditorDTO"
      pattern: "getTeacherAuthoringOverview"
    - from: "src/components/authoring/lesson-authoring-workspace.tsx"
      to: "src/actions/lesson-authoring-actions.ts"
      via: "Server Action form/action props"
      pattern: "publishLessonAction"
---

<objective>
Replace the static lesson editor with a data-backed teacher authoring workspace
that exposes course/class, lesson, step, autosave, reorder, and publish flows.

Purpose: This plan delivers the user-visible Phase 3 authoring experience while
honoring D-01: teacher-owned authoring, D-02: draft/published visibility,
D-03: content/task/quiz editing, D-04: rank-backed reorder controls,
D-05: autosave/publish freshness feedback, D-06: Chinese `DESIGN.md` UI,
D-07: DTO-only UI props, and D-08: `pnpm verify:phase3` closure.
Output: RSC route, authoring surface, client workspace, step editor, status
panel, and passing `pnpm verify:phase3`.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-courses-lessons-steps-and-teacher-authoring/03-CONTEXT.md
@.planning/phases/03-courses-lessons-steps-and-teacher-authoring/03-RESEARCH.md
@.planning/phases/03-courses-lessons-steps-and-teacher-authoring/03-UI-SPEC.md
@.planning/phases/03-courses-lessons-steps-and-teacher-authoring/03-03-SUMMARY.md
@DESIGN.md

<interfaces>
Use `Button`, `Card`, and `Badge` from existing UI primitives. They already
encode gradient CTA, tonal cards, and rounded labels:

```typescript
export function Button({ asChild, variant = 'primary', className, ...props }: ButtonProps)
export function Card({ className, ...props }: ComponentPropsWithoutRef<'section'>)
export function Badge({ variant = 'default', className, ...props }: BadgeProps)
```

Do not import `@/db`, `src/db`, or schema tables in any UI component.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load authoring DTOs in the teacher editor route</name>
  <files>src/app/(teacher)/teacher/editor/page.tsx, src/components/surfaces/lesson-editor-surface.tsx</files>
  <read_first>
    - src/app/(teacher)/teacher/editor/page.tsx
    - src/components/surfaces/lesson-editor-surface.tsx
    - src/lib/dal/lesson-authoring.ts
    - src/lib/dto/lesson-authoring.ts
    - .planning/phases/03-courses-lessons-steps-and-teacher-authoring/03-UI-SPEC.md
  </read_first>
  <action>Update `src/app/(teacher)/teacher/editor/page.tsx` into an async Server Component. Import `getTeacherAuthoringOverview` and optionally `getLessonEditorDTO` from `@/lib/dal/lesson-authoring`; call the DAL and pass DTOs into `LessonEditorSurface`. Update `src/components/surfaces/lesson-editor-surface.tsx` to accept props `{ overview, lesson }` typed from `src/lib/dto/lesson-authoring.ts`; remove imports from `@/lib/demo-data`. Keep the three-pane layout from the existing surface and replace static demo values with DTO values. Include Chinese labels `课程 / 班级`, `课时列表`, `步骤编排`, `草稿仅教师可见`, `学生将读取已发布版本`, and `发布课时` per D-06.</action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(teacher)/teacher/editor/page.tsx` contains `async function TeacherEditorPage` or `export default async function TeacherEditorPage`.
    - `src/app/(teacher)/teacher/editor/page.tsx` contains `getTeacherAuthoringOverview`.
    - `src/components/surfaces/lesson-editor-surface.tsx` no longer contains `demo-data`.
    - `src/components/surfaces/lesson-editor-surface.tsx` contains `草稿仅教师可见` and `发布课时`.
  </acceptance_criteria>
  <done>Teacher editor route loads DAL DTOs and the surface renders real authoring context instead of demo data.</done>
</task>

<task type="auto">
  <name>Task 2: Add interactive authoring workspace and step editor</name>
  <files>src/components/authoring/lesson-authoring-workspace.tsx, src/components/authoring/lesson-step-editor.tsx, src/components/surfaces/lesson-editor-surface.tsx</files>
  <read_first>
    - src/components/surfaces/lesson-editor-surface.tsx
    - src/actions/lesson-authoring-actions.ts
    - src/lib/dto/lesson-authoring.ts
    - src/components/ui/button.tsx
    - src/components/ui/card.tsx
    - src/components/ui/badge.tsx
    - DESIGN.md
  </read_first>
  <action>Create `src/components/authoring/lesson-authoring-workspace.tsx` as a client component with `"use client"`. It receives DTO props and Server Action references imported from `src/actions/lesson-authoring-actions.ts`. Render a step rail with each step type label as `内容`, `任务`, or `测验`; include buttons `新增内容`, `新增任务`, `新增测验`, `复制`, `归档`, `上移`, and `下移`. `上移`/`下移` must call `reorderLessonStepAction` with neighboring step IDs/ranks so keyboard users can reorder without drag. Create `src/components/authoring/lesson-step-editor.tsx` for the selected `content`, `task`, or `quiz` payload form. Use Chinese labels: `标题`, `正文`, `教师提示`, `任务说明`, `提交要求`, `题目`, `选项`, `答案说明`, and `引用材料`. Wire the workspace into `LessonEditorSurface` center pane. Follow D-06: tonal backgrounds only, no divider borders, gradient primary CTA through `Button`.</action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/authoring/lesson-authoring-workspace.tsx` contains `"use client"`.
    - `src/components/authoring/lesson-authoring-workspace.tsx` contains `reorderLessonStepAction`.
    - `src/components/authoring/lesson-authoring-workspace.tsx` contains `上移` and `下移`.
    - `src/components/authoring/lesson-step-editor.tsx` contains `新增内容`, `新增任务`, or `新增测验` usage through props or labels.
    - New authoring components contain zero imports from `@/db` or `@/db/schema`.
  </acceptance_criteria>
  <done>Teacher can manage content/task/quiz steps and rank-backed reorder controls from the UI.</done>
</task>

<task type="auto">
  <name>Task 3: Add authoring status panel and close verification</name>
  <files>src/components/authoring/authoring-status-panel.tsx, src/components/surfaces/lesson-editor-surface.tsx</files>
  <read_first>
    - src/components/surfaces/lesson-editor-surface.tsx
    - src/components/authoring/lesson-authoring-workspace.tsx
    - src/actions/lesson-authoring-actions.ts
    - scripts/verify-phase3-authoring.ts
    - .planning/phases/03-courses-lessons-steps-and-teacher-authoring/03-UI-SPEC.md
  </read_first>
  <action>Create `src/components/authoring/authoring-status-panel.tsx` and wire it into the right inspector of `LessonEditorSurface`. It must display all required Phase 3 feedback strings per D-05, D-06, and D-08: `已自动保存`, `正在保存...`, `检测到更新冲突`, `可发布`, `发布课时`, `学生将读取已发布版本`, and `缓存已刷新`. Connect `publishLessonAction` to the `发布课时` CTA. Display publish readiness based on DTO fields: title present, objective present, at least one non-archived valid step. Preserve the no-line tonal design: use `bg-surface-container-low`, `bg-surface-container-lowest`, rounded shells, whitespace, and no `border` utility for section separation.</action>
  <verify>
    <automated>pnpm verify:phase3 && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/authoring/authoring-status-panel.tsx` contains `已自动保存`.
    - `src/components/authoring/authoring-status-panel.tsx` contains `检测到更新冲突`.
    - `src/components/authoring/authoring-status-panel.tsx` contains `缓存已刷新`.
    - `src/components/authoring/authoring-status-panel.tsx` contains `publishLessonAction` or receives a publish action prop from the workspace.
    - `pnpm verify:phase3` exits 0.
    - `pnpm exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Authoring UI surfaces autosave, publish, conflict, and cache freshness feedback and Phase 3 verification passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC -> Client component | Sanitized DTOs and Server Action references cross into interactive UI. |
| Client form -> Server Action | Teacher input crosses back to validated server mutations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Information Disclosure | authoring UI props | mitigate | Route loads DTOs through DAL; UI components must not import DB modules. |
| T-03-13 | Tampering | reorder controls | mitigate | UI calls rank-backed `reorderLessonStepAction`; it never sends integer positions. |
| T-03-14 | Repudiation | autosave/publish status | mitigate | Render explicit Chinese status strings for saved, saving, conflict, publish-ready, and cache-refreshed states. |
</threat_model>

<verification>
- `pnpm verify:phase3`
- `pnpm exec tsc --noEmit`
</verification>

<success_criteria>
Teacher authoring is visible and actionable from `/teacher/editor`: course/class
context, lesson management, validated step editing, rank reorder controls,
autosave feedback, conflict feedback, and stable publish CTA all work through
DAL and Server Actions.
</success_criteria>

<output>
After completion, create `.planning/phases/03-courses-lessons-steps-and-teacher-authoring/03-04-SUMMARY.md`
</output>
