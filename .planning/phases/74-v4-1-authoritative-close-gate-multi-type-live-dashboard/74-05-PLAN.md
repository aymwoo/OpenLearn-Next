---
phase: 74-v4-1-authoritative-close-gate-multi-type-live-dashboard
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md
  - .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md
  - .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-VALIDATION.md
  - .planning/STATE.md
  - package.json
autonomous: true
requirements: [QUIZ-EXT-CLOSE-02, QUIZ-EXT-CLOSE-03]
must_haves:
  truths:
    - "v4.1 两条 manual rows 被真实观察结果回填为 `status: passed`，并具备 `executed_by` / `executed_at` / `evidence note`（per D-05/D-06）"
    - "`73-CLOSEOUT.md` 必须先以 evidence-first 形态存在，随后才允许执行包含 closeout existence 的完整 D-04 cutover 判定；final operational verdict 只能在最终 alias verify 之后写回（per D-04/D-09）"
    - "`verify:phase` 只有在 fast static preflight + smoke readiness + 完整 D-04 predicates 全满足时才切到 `pnpm verify:phase72 && pnpm verify:phase73-v41-close-gate`；若任一 predicate 缺失则必须升级为 `revision required` 并停止 phase 完成，而不是把 alias 保持 phase72 当作成功结案（per D-04）"
    - "`.planning/STATE.md` 在最终 cutover 通过后同步到 v4.1 close-gate complete / 100% posture，不再停留在 Phase 73 执行中（per QUIZ-EXT-CLOSE-03）"
    - "`73-CLOSEOUT.md` 显式写出 AGENTS SSE baseline 与本 phase 锁定 WebSocket-first path 的治理关系，并说明不会新增第二条 transport runtime"
  artifacts:
    - path: ".planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md"
      provides: "final passed v4.1 manual ledger rows"
      contains: "status: passed"
    - path: ".planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md"
      provides: "evidence-first closeout artifact with final operational verdict completed after alias verification"
      contains: "Proof chain summary"
    - path: ".planning/STATE.md"
      provides: "milestone state synced to completed v4.1 close gate"
      contains: "Current focus: v4.1 authoritative close gate complete"
    - path: "package.json"
      provides: "conditional verify:phase alias cutover"
      contains: "verify:phase"
  key_links:
    - from: "73-PROOF-MAPPING.md"
      to: "scripts/verify-phase73-v41-close-gate.ts"
      via: "manual ledger parser token `status: passed`"
      pattern: "| status | `status: passed` |"
    - from: "73-CLOSEOUT.md"
      to: "package.json#verify:phase"
      via: "closeout documents cutover-ready or blocked alias posture"
      pattern: "verify:phase"
    - from: "package.json#verify:phase"
      to: "verify:phase72 && verify:phase73-v41-close-gate"
      via: "final conditional cutover per D-04"
      pattern: "verify:phase73-v41-close-gate"
    - from: ".planning/STATE.md"
      to: "package.json#verify:phase"
      via: "close-gate completion posture recorded only after final alias verify succeeds"
      pattern: "Current focus: v4.1 authoritative close gate complete"
---

<objective>
用真人回传的 sign-off payload 回填 `73-PROOF-MAPPING.md` 两条 v4.1 rows，先落地 evidence-first `73-CLOSEOUT.md` 让 D-04 三件套真实存在，再执行包含 `73-CLOSEOUT.md` 的完整 readiness/cutover 与最终 `pnpm verify:phase`，最后把 closeout 与 `.planning/STATE.md` 补全为真实 alias verdict；若 readiness 不满足，则写明 `revision required` 并停止 phase 完成，而不是把 phase72 alias posture 当作成功结案。

Purpose: 完成 authoritative close 的最后三步：真实 manual evidence 落账 + evidence-first closeout artifact 落地 + 完整 D-04 cutover 判定 / STATE sync / 最终 alias verify。
Output: final `73-PROOF-MAPPING.md`, `73-CLOSEOUT.md`, `.planning/STATE.md`, and conditional `package.json#verify:phase`.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-CONTEXT.md
@.planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md
@.planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-VERIFICATION.md
@.planning/milestones/v4.0-phases/72.1-close-gap-gate-01-authoritative-milestone-close-gate/72.1-CLOSEOUT.md
@package.json
@scripts/verify-phase73-v41-close-gate.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 用 74-04 的真人 payload 回填 73-PROOF-MAPPING.md 两条 v4.1 manual rows</name>
  <read_first>
    - .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md
    - .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-CONTEXT.md
    - .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-OBSERVATION-TARGETS.md
    - .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md
  </read_first>
  <files>.planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md</files>
  <action>
    只读取 `.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md` 中持久化保存的两组真实 payload，**逐字**回填 `73-PROOF-MAPPING.md` 里的 v4.1 row 3 / row 4：
    - 把 `status: pending-human-signoff` 改成精确 `status: passed`
    - 将 `executed_by` 写成真人提供的名字
    - 将 `executed_at` 写成真人提供的 ISO timestamp
    - 将 `evidence note` 写成真人提供的一句话观察

    不要修改 carried-forward 的 v4.0 两行，不要捏造任何值，不要把 sign-off 信息改写到 `73-VERIFICATION.md`。如果 `74-MANUAL-SIGNOFF.md` 不存在、没有给齐两组 payload、缺少 `session_id` / `observed_url`、`executed_by` 为空、`executed_at` 不是可解析 ISO 时间、`evidence_note` 为空，或者 payload 与 `74-OBSERVATION-TARGETS.md` 中的预生成 URL 不一致，就停止并报错，不得自行补全，也不得继续 Task 2/3。
  </action>
  <verify>
    <automated>grep -c '^| status | `status: passed` |' .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md | grep -q '4'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md` 存在，并同时包含 `## LIVE_ANSWER_SIGNOFF` 与 `## RECAP_SIGNOFF`
    - 两个 sign-off section 都包含非空 `executed_by`、可解析 ISO 时间的 `executed_at`、非空 `evidence_note`，且与 observation targets 的 `session_id` / `observed_url` 对齐
    - `73-PROOF-MAPPING.md` 中 `| status | \`status: passed\` |` 总数为 4（v4.0 两行 + v4.1 两行）
    - v4.1 两行分别包含 `/classroom` + `live-answer` 与 `classroom-session-recap-surface.tsx`
    - 四行都包含 `| executed_by |`、`| executed_at |`、`| evidence note |`
    - 文档中不再存在 `status: pending-human-signoff`
  </acceptance_criteria>
  <done>两条 v4.1 manual rows 已被真实观察结果回填，proof mapping 进入 passed-ready 状态。</done>
</task>

<task type="auto">
  <name>Task 2: 先写 evidence-first `73-CLOSEOUT.md`，让 D-04 所需三件套在 cutover 前真实存在</name>
  <read_first>
    - .planning/milestones/v4.0-phases/72.1-close-gap-gate-01-authoritative-milestone-close-gate/72.1-CLOSEOUT.md
    - .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md
    - .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-VERIFICATION.md
    - package.json
    - .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md (create/update this file)
  </read_first>
  <files>.planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md</files>
  <action>
    参照 `72.1-CLOSEOUT.md` 先创建/更新 `73-CLOSEOUT.md` 的 **evidence-first draft**，以满足 D-04 要求的 artifact existence，但不得让结论先于证据（per D-04/D-09）。固定包含这些标题：
    - `## Close conclusion`
    - `## Delivered scope`
    - `## Proof chain summary`
    - `## Operational summary`
    - `## Explicit exclusions`
    - `## Deferred next steps`

    本任务只写已经成立的证据链：
    - `Delivered scope`、`Proof chain summary`、`Explicit exclusions`、`Deferred next steps` 必须基于现有 `73-PROOF-MAPPING.md` 与 `73-VERIFICATION.md` 落笔
    - `Proof chain summary` 必须显式点名：`verify:phase67`、`verify:phase68`、`verify:phase69`、`verify:phase70`、`verify:phase71`、`verify:phase72`、`verify:phase73`、`73-PROOF-MAPPING.md`、`73-VERIFICATION.md`、`73-CLOSEOUT.md`
    - `Explicit exclusions` 必须再次写清：不重开 quiz scope、不新增 dashboard write behaviors、不新增第二 transport runtime

    `Operational summary` 在本任务只能写成 provisional 语句：`Alias cutover status: pending final D-04 evaluation`，并明确说明最终 operational verdict 要等 Task 3 完成 **精确 D-04 判定 + 最终 `pnpm verify:phase`** 后再回填。
    同一 section 或 `Explicit exclusions` 中必须补一段显式治理说明：`AGENTS.md` 把课堂广播写成 SSE baseline，但本 phase 依据锁定 scope 只验证既有的 WebSocket-first teacher-only transport；这次 close 只是收口现有 WS proof chain，不会引入第二条 transport runtime。`

    本任务 **不得**：
    - 把状态写成 `ready and applied` 或 `blocked`
    - 改 `package.json`
    - 虚构 `pnpm verify:phase` 已通过或 cutover 已完成
  </action>
  <verify>
    <automated>grep -q "## Close conclusion" .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md && grep -q "Alias cutover status: pending final D-04 evaluation" .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md && grep -q "verify:phase73" .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md && grep -q "SSE" .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md && grep -q "WebSocket" .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md && grep -q "no second transport runtime" .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md && ! grep -q "Alias cutover status: ready and applied" .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md && ! grep -q "Alias cutover status: blocked" .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md</automated>
  </verify>
  <acceptance_criteria>
    - `73-CLOSEOUT.md` 在 Task 3 之前就已存在，从而满足 D-04 的三件套 existence 前提
    - 文档含上述 6 个固定 section 标题
    - `Proof chain summary` 显式点名 `verify:phase67`、`verify:phase68`、`verify:phase72`、`verify:phase73`
    - `Operational summary` 只允许出现 `Alias cutover status: pending final D-04 evaluation`，不得提前宣称 `ready and applied` 或 `blocked`
    - `Explicit exclusions` 明确包含 `no new dashboard write behaviors` 和 `no second transport runtime` 等同义字面
    - 文档显式写出 `AGENTS.md` 的 SSE baseline 与本 phase 锁定 WebSocket-first path 的关系
  </acceptance_criteria>
  <done>evidence-first closeout artifact 已先于 alias cutover 判定落地，D-04 所需三件套现已真实存在。</done>
</task>

<task type="auto">
  <name>Task 3: 先跑 fast preflight，再做唯一成功分支的 D-04 cutover、final verify 与 STATE sync</name>
  <read_first>
    - package.json
    - .planning/STATE.md
    - .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md
    - .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-VERIFICATION.md
    - .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md
    - scripts/verify-phase73-quiz-ext.ts
    - scripts/verify-phase73-v41-close-gate.ts
  </read_first>
  <files>package.json, .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md, .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-VALIDATION.md, .planning/STATE.md</files>
  <action>
    先执行 **fast static/readiness preflight**，这一步必须比重型 verify 更快，先快速证明 cutover 输入齐备，再决定是否进入 smoke/full gate：
    1. `test -f scripts/verify-phase73-quiz-ext.ts`
    2. `test -f scripts/verify-phase73-v41-close-gate.ts`
    3. `test -f .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-VERIFICATION.md`
    4. `test -f .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md`
    5. `test -f .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md`
    6. `grep -Eq '"verify:phase73": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase73-quiz-ext.ts"' package.json`
    7. `grep -Eq '"verify:phase73-v41-close-gate": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase73-v41-close-gate.ts"' package.json`
    8. `grep -c '^| status | `status: passed` |' .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md`

     只有 fast preflight 通过后，才运行 smoke readiness：`pnpm verify:phase73-v41-close-gate --smoke`。这个 smoke 仍然属于 preflight，不是最终 authoritative verify。manual sign-off 不是只数 token：4 行 passed rows 要逐行满足 `executed_by` 非空、`executed_at` 可解析、`evidence note` 非空；2 条 v4.1 rows 还要绑定正确的 `session_id` / `observed_url`。

     在唯一成功分支里，还必须同步更新 `.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-VALIDATION.md`，消除 competing authoritative entrypoints：
     - `Full suite command` 改成 `pnpm verify:phase`
     - phase-gate / final-gate 指引改成 `pnpm verify:phase`
     - `After every task commit` 改成一个 sub-30s fast lane，只保留 parser/static/readiness checks，不再复用 180s 级重型链路
     - `Max feedback latency` 改成与该 fast lane 一致的快速阈值（sub-30s），明确把 `pnpm verify:phase` 保留到 wave merge / final gate
     - 若保留 pre-cutover 历史说明，必须明确标注为 historical / superseded，不得继续与新的 single authoritative gate 并列为当前官方入口
     - 在同一文件中补一个 sub-30s fast lane 说明（仅 parser/static/readiness preflight），把重型 `pnpm verify:phase` 保留为 wave-merge / final gate

    然后用 **精确** D-04 predicate 做最终判断（per D-04）：
    1. `scripts/verify-phase73-quiz-ext.ts` 存在且 `package.json` 有 `verify:phase73`
    2. `scripts/verify-phase73-v41-close-gate.ts` 存在且 `package.json` 有 `verify:phase73-v41-close-gate`
    3. `73-VERIFICATION.md` 存在
    4. `73-PROOF-MAPPING.md` 存在
    5. `73-CLOSEOUT.md` 存在
    6. `73-PROOF-MAPPING.md` 里两条 v4.1 manual rows 已真实 `status: passed`，并且 4 条 manual rows 全部满足：`executed_by` 非空、`executed_at` 为可解析 ISO 时间、`evidence note` 非空；两条 v4.1 rows 还保留与 `74-MANUAL-SIGNOFF.md` 对齐的 `session_id` / `observed_url`

    **唯一成功分支（1/2/3/4/5/6 全满足）：**
    - 把 `package.json` 中 `verify:phase` 改成精确字符串：`"verify:phase": "pnpm verify:phase72 && pnpm verify:phase73-v41-close-gate"`
    - 运行最终权威入口：`pnpm verify:phase`
    - 仅在该命令真实通过后，把 Task 2 的 provisional wording 改写为 `Alias cutover status: ready and applied`
    - 在 `73-CLOSEOUT.md` 中明确记录最终 alias 是 `pnpm verify:phase72 && pnpm verify:phase73-v41-close-gate`，且最终 authoritative entrypoint 已由 `pnpm verify:phase` 验证通过
    - 同步 `.planning/STATE.md`，至少更新以下目标：
      - `last_updated` / `last_activity`
      - `Current focus: v4.1 authoritative close gate complete`
      - `Phase: 74 (v4-1-authoritative-close-gate-multi-type-live-dashboard) — COMPLETE`
      - `Status: Completed Phase 74 authoritative close gate`
      - `progress.total_plans: 7`
      - `progress.completed_plans: 7`
      - `progress.completed_phases: 2`
      - `progress.percent: 100`
      - `By v4.1 Phase` 表中 73 / 74 都标为 complete
      - 移除或更新 `Add pnpm verify:phase73-v41-close-gate` 这一 pending todo，避免 STATE 继续宣称 cutover 未接线

    **revision-required 分支（1/2/3/4/5/6 任一不满足，或最终 `pnpm verify:phase` 失败）：**
    - 立即把 `package.json` 中 `verify:phase` 保持/恢复为精确字符串：`"verify:phase": "pnpm verify:phase72"`
    - 把 `73-CLOSEOUT.md` 的 provisional wording 改写为 `Alias cutover status: revision required (D-04 unmet)`
    - 在 `73-CLOSEOUT.md` 中逐条列出未满足的 D-04 predicates 或最终 `pnpm verify:phase` 失败点，并明确写出 alias 仍保持 `pnpm verify:phase72`
    - **停止执行并返回 incomplete**：不要更新 `.planning/STATE.md` 到 completed posture，不要把 phase 当作完成，也不要把保持 phase72 alias 当作成功 closeout

    不允许第三种 alias 状态，不允许把 `verify:phase73-v41-close-gate` 单独设为 alias，也不允许删掉 phase72 这层串联。`73-CLOSEOUT.md` 的最终 operational verdict 与 `.planning/STATE.md` 的 completed posture 都只能在 `pnpm verify:phase` 真实通过后写回，从而保持 D-09 的 evidence-first discipline。
  </action>
  <verify>
    <automated>test -f scripts/verify-phase73-quiz-ext.ts && test -f scripts/verify-phase73-v41-close-gate.ts && test -f .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-VERIFICATION.md && test -f .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md && test -f .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md && grep -Eq '"verify:phase73": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase73-quiz-ext.ts"' package.json && grep -Eq '"verify:phase73-v41-close-gate": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase73-v41-close-gate.ts"' package.json && pnpm verify:phase73-v41-close-gate --smoke && pnpm verify:phase && grep -Eq '"verify:phase": "pnpm verify:phase72 && pnpm verify:phase73-v41-close-gate"' package.json && grep -q 'Alias cutover status: ready and applied' .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md && ! grep -q 'pending final D-04 evaluation' .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md && ! grep -q 'revision required' .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md && grep -q 'Current focus: v4.1 authoritative close gate complete' .planning/STATE.md && grep -q 'total_plans: 7' .planning/STATE.md && grep -q 'completed_plans: 7' .planning/STATE.md && grep -q 'completed_phases: 2' .planning/STATE.md && grep -q 'percent: 100' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Task 3 在最终 `pnpm verify:phase` 之前必须先跑 fast static/readiness preflight（`test -f` + exact `grep` checks），然后才允许进入 `pnpm verify:phase73-v41-close-gate --smoke`
    - D-04 predicate 明确包含 `73-CLOSEOUT.md` existence；缺少 closeout 时不允许 alias cutover
    - `package.json` 的 `verify:phase` 最终成功态只能是 `pnpm verify:phase72 && pnpm verify:phase73-v41-close-gate`
    - 若进入唯一成功分支，则组合 alias 已设置，action 内运行的 `pnpm verify:phase` exit 0，`73-CLOSEOUT.md` 明确记录 `Alias cutover status: ready and applied`，`.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-VALIDATION.md` 已把 full suite / final gate 收口到 `pnpm verify:phase`，且 `.planning/STATE.md` 已同步到 completed posture（包含 `total_plans: 7`、`completed_plans: 7`、`completed_phases: 2`、`percent: 100`）
    - 若任一 D-04 predicate 未满足或最终 `pnpm verify:phase` 失败，则必须写成 `Alias cutover status: revision required (D-04 unmet)` 并停止 phase 完成；这不是可接受的成功结案路径
    - 文档含上述 6 个固定 section 标题
    - `Proof chain summary` 显式点名 `verify:phase67`、`verify:phase68`、`verify:phase72`、`verify:phase73`
    - `Operational summary` 在成功态只允许记录 `Alias cutover status: ready and applied`；失败态只能进入 `revision required` 并停止，不得再使用 `blocked` 作为 phase-complete outcome
    - `Explicit exclusions` 明确包含 `no new dashboard write behaviors` 和 `no second transport runtime` 等同义字面
    - `.planning/STATE.md` 不再停留在 `Phase 73 — EXECUTING` / `progress.percent: 50` 这类旧 posture，且 `total_plans` / `completed_plans` 已同步到 roadmap 一致的最终 7/7
    - `.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-VALIDATION.md` 不再把 `pnpm verify:phase73 && pnpm verify:phase73-v41-close-gate` 作为当前 full suite / final gate，而是明确收口到 `pnpm verify:phase`
    - `.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-VALIDATION.md` 的 `After every task commit` 与 `Max feedback latency` 已改成 sub-30s fast lane，而不是继续沿用 ~180s 的重型链路
  </acceptance_criteria>
  <done>完整 D-04 predicate 已在唯一成功分支下被执行，最终 alias posture 已经过 fast preflight + authoritative `pnpm verify:phase` 验证，closeout 与 STATE 也已补全为真实最终 verdict。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human checkpoint payload → proof ledger | user-provided observation data becomes authoritative close evidence |
| final proof artifacts → package alias | closeout state controls whether the global alias may move |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-05-01 | Repudiation | v4.1 manual sign-off rows | mitigate | Copy observer-supplied `executed_by` / `executed_at` / `evidence note` verbatim; abort if either payload is missing |
| T-74-05-02 | Tampering | `package.json#verify:phase` alias | mitigate | Evaluate exact D-04 predicates and allow only two legal alias states; never point alias directly to outer gate alone |
| T-74-05-03 | Tampering | `73-CLOSEOUT.md` | mitigate | Closeout must exist before D-04 cutover evaluation, but its final ready-vs-revision-required verdict can only be written after the real final `pnpm verify:phase` result |
| T-74-05-04 | Repudiation | proof-chain summary | mitigate | `73-CLOSEOUT.md` explicitly names `verify:phase67` through `verify:phase73` plus the three final artifacts |
| T-74-05-05 | Repudiation | `.planning/STATE.md` | mitigate | STATE is updated only after successful final verify; incomplete D-04 path must not fake a completed posture |
</threat_model>

<verification>
- `test -f scripts/verify-phase73-quiz-ext.ts && test -f scripts/verify-phase73-v41-close-gate.ts && test -f .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-VERIFICATION.md && test -f .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md && test -f .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md`
- `grep -c '^| status | `status: passed` |' .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md`
- `test -f .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md`
- `pnpm verify:phase73-v41-close-gate --smoke`
- `pnpm verify:phase`
- `grep -Eq '"verify:phase": "pnpm verify:phase72 && pnpm verify:phase73-v41-close-gate"' package.json`
- `grep -q 'Alias cutover status: ready and applied' .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CLOSEOUT.md`
- `grep -q 'Current focus: v4.1 authoritative close gate complete' .planning/STATE.md`
</verification>

<success_criteria>
- v4.1 manual rows are backed by real human observations
- `73-CLOSEOUT.md` exists before alias cutover evaluation, and its final operational verdict is only completed after the real final verify result
- D-04 readiness/cutover has been executed with `73-CLOSEOUT.md` included in the exact predicate, then validated through fast static preflight + smoke readiness + the final authoritative entrypoint `pnpm verify:phase`
- `verify:phase` is successfully cut over only when D-04 is fully satisfied; otherwise the phase stops as incomplete and requests revision instead of claiming success
- `.planning/STATE.md` is synchronized with the final v4.1 close posture after successful cutover
</success_criteria>

<output>
After completion, create `.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-05-SUMMARY.md`
</output>
