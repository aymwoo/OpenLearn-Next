---
phase: 74-v4-1-authoritative-close-gate-multi-type-live-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md
  - scripts/verify-phase73-quiz-ext.ts
  - package.json
autonomous: true
requirements: [QUIZ-EXT-CLOSE-01, QUIZ-EXT-CLOSE-02]
must_haves:
  truths:
    - "73-PROOF-MAPPING.md 先于 closeout 存在，并把 recap chain、live dashboard chain、proof chain、manual ledger 全部显式落到 Phase 73 目录（per D-09）"
    - "`verify:phase73` 成为独立可重跑的产品 truth lane，显式证明 `plugin_owned_quiz_questions.questionType`、5-type enum / DTO union / allowlist、`cacheTags.quizStats` invalidation，以及 Phase 73 的多题型 recap / live dashboard / zero-write / transport seams（per D-01）"
    - "`verify:phase` 仍保持 `pnpm verify:phase72`，不发生任何提前 alias cutover（per D-03）"
  artifacts:
    - path: ".planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md"
      provides: "Phase 73 authoritative proof-mapping skeleton with 4-row manual ledger"
      contains: "Manual Surface Sign-Off Ledger"
    - path: "scripts/verify-phase73-quiz-ext.ts"
      provides: "Independent Phase 73 product verifier"
      contains: "runPhase73Verification"
    - path: "package.json"
      provides: "Exact verify:phase73 script wiring while keeping verify:phase on phase72"
      contains: "verify:phase73"
  key_links:
    - from: "package.json"
      to: "scripts/verify-phase73-quiz-ext.ts"
      via: "verify:phase73 exact script entry"
      pattern: "verify:phase73"
    - from: ".planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md"
      to: "src/components/classroom/live-answer-dashboard-surface.tsx"
      via: "v4.1 live-answer manual sign-off row"
      pattern: "作答实时"
    - from: ".planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md"
      to: "src/components/classroom/classroom-session-recap-surface.tsx"
      via: "v4.1 multi-type recap manual sign-off row"
      pattern: "题目复盘"
---

<objective>
先按 D-09 把 `73-PROOF-MAPPING.md` 落到位，再补一个可独立复跑的 `verify:phase73` 产品 verifier，收口多题型 recap 与 live dashboard 的产品事实，但不碰外层 close gate、不提前切换 `verify:phase`。

Purpose: 建立 Phase 74 的第一性证据层：proof mapping 先行、inner verifier 单独成型、global alias 保持冻结。
Output: `73-PROOF-MAPPING.md`、`scripts/verify-phase73-quiz-ext.ts`、`package.json#verify:phase73`。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-CONTEXT.md
@.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-RESEARCH.md
@.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-PATTERNS.md
@.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-UI-SPEC.md
@.planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-01-SUMMARY.md
@.planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-02-SUMMARY.md
@.planning/milestones/v4.0-phases/72-end-to-end-verify-phase-close-gate/72-VERIFICATION.md
@.planning/milestones/v4.0-phases/72.1-close-gap-gate-01-authoritative-milestone-close-gate/72.1-PROOF-MAPPING.md
@scripts/verify-phase71-marketplace-lifecycle.ts
@scripts/verify-phase72-close-gate.ts
@scripts/verify-live-answer-zero-write.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 先写 73-PROOF-MAPPING.md，固定 4-row ledger 和 proof-chain 骨架</name>
  <read_first>
    - .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-01-SUMMARY.md
    - .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-02-SUMMARY.md
    - .planning/milestones/v4.0-phases/72.1-close-gap-gate-01-authoritative-milestone-close-gate/72.1-PROOF-MAPPING.md
    - .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-CONTEXT.md
    - .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-RESEARCH.md
    - .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md (create this file)
  </read_first>
  <files>.planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md</files>
  <action>
    创建 `73-PROOF-MAPPING.md`，文件必须写回 **Phase 73 目录**（不是 Phase 74 目录），并严格按 D-09 先于 gate wiring 落地。
    正文至少包含这 5 个区块，标题文字按这里写：
    1. `## Requirement → Flow Segment → Proof Files / Scripts / Tests`
    2. `## User Flow → Code Seam → Proof`
    3. `## Archival Artifact → Purpose → Dependency`
    4. `## Final Proof Chain`
    5. `## Manual Surface Sign-Off Ledger`

    其中 `Final Proof Chain` 必须显式列出：`verify:phase67`、`verify:phase68`、`verify:phase69`、`verify:phase70`、`verify:phase71`、`verify:phase72`、`verify:phase73`、`73-VERIFICATION.md`、`73-PROOF-MAPPING.md`、`73-CLOSEOUT.md`。

    `Requirement → Flow Segment → Proof Files / Scripts / Tests` 区块必须逐条显式收录 roadmap success criteria 要求的全部 sub-ID，不能只写父级 REQ：
    - `QUIZ-EXT-01-A` / `QUIZ-EXT-01-B` / `QUIZ-EXT-01-C` / `QUIZ-EXT-01-D` / `QUIZ-EXT-01-E`
    - `QUIZ-EXT-02-A` / `QUIZ-EXT-02-B` / `QUIZ-EXT-02-C` / `QUIZ-EXT-02-D` / `QUIZ-EXT-02-E`
    - `QUIZ-EXT-CLOSE-01` / `QUIZ-EXT-CLOSE-02` / `QUIZ-EXT-CLOSE-03`
    对每个 sub-ID 都要写清：对应 flow segment、proof files / scripts / tests、以及它在 `Final Proof Chain` / archival artifacts 里的落点。不要把这些条目折叠成 `QUIZ-EXT-01`、`QUIZ-EXT-02`、`QUIZ-EXT-CLOSE` 三个宏观标题，这一条是为满足 roadmap success criteria 4 与 D-09 的显式追踪要求。

    `Manual Surface Sign-Off Ledger` 必须在本计划里一次性落出 **4 行**（研究建议沿用单文件 parser）：
    - v4.0 carried-forward row 1：`/settings/plugins` lifecycle surface
    - v4.0 carried-forward row 2：ended classroom recap baseline surface
    - v4.1 row 3：`/classroom` live-answer tab
    - v4.1 row 4：multi-type ended-session recap surface

    所有行都必须使用锁定 schema 字段名：`proof artifact`、`status`、`executed_by`、`executed_at`、`evidence note`。
    **关键约束：** 仅 carried-forward 的 v4.0 两行允许保留已完成值；v4.1 两行在本任务内只能写成待人工填写的占位，不得写 `status: passed`，建议字面值用 `status: pending-human-signoff`，并在相邻说明中点名 D-05 / D-06。不要发明新字段，不要把 manual rows 拆到第二个文件。
  </action>
  <verify>
    <automated>for token in QUIZ-EXT-01-A QUIZ-EXT-01-B QUIZ-EXT-01-C QUIZ-EXT-01-D QUIZ-EXT-01-E QUIZ-EXT-02-A QUIZ-EXT-02-B QUIZ-EXT-02-C QUIZ-EXT-02-D QUIZ-EXT-02-E QUIZ-EXT-CLOSE-01 QUIZ-EXT-CLOSE-02 QUIZ-EXT-CLOSE-03; do grep -q "$token" .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md || exit 1; done && grep -q "Requirement → Flow Segment → Proof Files / Scripts / Tests" .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md && grep -q "Manual Surface Sign-Off Ledger" .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md && grep -q "verify:phase73" .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md && grep -c '^| status | `status: passed` |' .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md | grep -q '2' && grep -c 'status: pending-human-signoff' .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md | grep -q '2' && grep -c '^| proof artifact |' .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md | grep -q '4' && grep -c '^| executed_by |' .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md | grep -q '4' && grep -c '^| executed_at |' .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md | grep -q '4' && grep -c '^| evidence note |' .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md | grep -q '4'</automated>
  </verify>
  <acceptance_criteria>
    - `73-PROOF-MAPPING.md` 存在于 `.planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/`
    - `Requirement → Flow Segment → Proof Files / Scripts / Tests` 区块显式出现 `QUIZ-EXT-01-A..E`、`QUIZ-EXT-02-A..E`、`QUIZ-EXT-CLOSE-01/02/03` 全部 sub-ID，而不是只写父级 REQ
    - 文件内同时出现 `verify:phase67`、`verify:phase68`、`verify:phase72`、`verify:phase73`
    - 文件内出现 4 组完整 ledger schema 字段：`| proof artifact |`、`| status |`、`| executed_by |`、`| executed_at |`、`| evidence note |`
    - carried-forward 的 v4.0 行中 `| status | \`status: passed\` |` 总数为 2
    - v4.1 两行明确包含 `/classroom` 与 `live-answer`，以及 `classroom-session-recap-surface.tsx`
    - v4.1 两行 **不** 含 `| status | \`status: passed\` |`，而是 2 处 `status: pending-human-signoff`
    - 文档正文出现 `conclusion never leads evidence` 或 `D-09` 的顺序纪律说明
  </acceptance_criteria>
  <done>Phase 73 proof mapping 骨架就位，4-row ledger 单文件结构与全部 QUIZ-EXT-01/02/CLOSE sub-ID trace 已固定，且 v4.1 两行未被自动伪造为 passed。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 verify:phase73 inner verifier，并保持 verify:phase 继续指向 phase72</name>
  <read_first>
    - scripts/verify-phase71-marketplace-lifecycle.ts
    - scripts/verify-phase72-close-gate.ts
    - scripts/verify-live-answer-zero-write.ts
    - package.json
    - src/db/schema/generated/plugin-owned/quiz.ts
    - src/db/schema/generated/plugin-owned/data-access-allowlist.ts
    - src/lib/dto/plugin-data-model.ts
    - src/lib/dto/classroom.ts
    - src/features/platform-core/plugin-data-access/allowlist.ts
    - src/actions/classroom-actions.ts
    - src/lib/dal/classroom.ts
    - src/features/runtime-platform/seams/transport/ws-envelope.ts
    - src/features/runtime-platform/seams/transport/ws-connection-registry.ts
    - src/features/runtime-platform/seams/transport/ws-auth.ts
    - src/components/classroom/live-answer-dashboard-surface.tsx
    - src/components/classroom/classroom-control-panel.tsx
    - src/app/(classroom)/classroom/page.tsx
    - src/features/platform-core/plugin-data-access/quiz-data-access.test.ts
    - src/features/platform-core/plugin-data-access/allowlist.test.ts
    - src/components/classroom/classroom-session-recap-surface.test.tsx
    - tests/classroom/ws-events.test.ts
    - tests/classroom/fanout.test.ts
    - tests/classroom/dashboard.test.ts
    - scripts/verify-phase73-quiz-ext.ts (create this file)
  </read_first>
  <files>scripts/verify-phase73-quiz-ext.ts, package.json</files>
  <behavior>
    - Test 1: `pnpm verify:phase73 --smoke` exits 0 and reports only product-truth stages, including schema / enum / DTO-union / allowlist / cache-tag static assertions, not artifact/manual/alias-close stages
    - Test 2: `package.json` adds exact `verify:phase73` entry pointing to `scripts/verify-phase73-quiz-ext.ts`
    - Test 3: `package.json` keeps `verify:phase` exactly `pnpm verify:phase72` per D-03
    - Test 4: full `verify:phase73` runs `pnpm test:live-answer-zero-write` plus the focused Vitest set that explicitly includes `src/features/platform-core/plugin-data-access/quiz-data-access.test.ts`, `src/features/platform-core/plugin-data-access/allowlist.test.ts`, `src/components/classroom/classroom-session-recap-surface.test.tsx`, `tests/classroom/ws-events.test.ts`, `tests/classroom/fanout.test.ts`, and `tests/classroom/dashboard.test.ts`
    - Test 5: `verify:phase73` explicitly proves the live dashboard remains teacher-only by checking the real teacher-scope filter / access-control seams rather than only checking WS event existence or tab presence
  </behavior>
  <action>
    新建 `scripts/verify-phase73-quiz-ext.ts`，复用 `verify-phase71-marketplace-lifecycle.ts` 的 smoke/full split 和 `verify-phase72-close-gate.ts` 的 `StaticCheck` / `Stage` / `run()` 风格。
    这个 inner verifier 只证明 **产品 truth**，不要读取 `73-PROOF-MAPPING.md`、`73-VERIFICATION.md`、`73-CLOSEOUT.md`，也不要做 alias cutover 判定。固定包含这些 stage：
    1. `Static product seam checks` — 除了现有 recap / dashboard / transport seams 外，必须把多题型 close-proof truth lane 补全为显式静态断言：
       - `src/db/schema/generated/plugin-owned/quiz.ts` 含 `plugin_owned_quiz_questions.questionType` 5 题型 enum（`single_choice` / `multi_choice` / `true_false` / `fill_blank` / `ordering`）
       - `src/lib/dto/plugin-data-model.ts` 含 `QuestionTypeSchema`
       - `src/lib/dto/classroom.ts` 含 5-branch `z.discriminatedUnion("questionType", ...)`，证明 recap DTO union 真正覆盖 5 题型
       - `src/db/schema/generated/plugin-owned/data-access-allowlist.ts` 与 `src/features/platform-core/plugin-data-access/allowlist.ts` 仍允许 `quiz.questions` / `quiz.responses` 读取 `questionType`
       - `src/actions/classroom-actions.ts` 仍含 `updateTag(cacheTags.quizStats(parsed.data.sessionId))`，证明 quiz stats cache-tag read-your-writes 合同没有丢失
       - `src/lib/dal/classroom.ts` 含 `buildQuizSampleRecapStats`、`produceQuizAnswerReceived(`；`src/features/runtime-platform/seams/transport/ws-envelope.ts` 含 `quiz.answer.received`；`src/components/classroom/live-answer-dashboard-surface.tsx` 含 `useLiveAnswerStore`；`src/app/(classroom)/classroom/page.tsx` 或 `classroom-control-panel.tsx` 含 `live-answer`
       - `src/features/runtime-platform/seams/transport/ws-connection-registry.ts` 明确保留 `connection.owner.actorScope !== "teacher"` 的 teacher-only 过滤
       - `src/features/runtime-platform/seams/transport/ws-auth.ts` 明确保留 active membership / classroom ownership 推导，不允许学生或非拥有教师绕过 live dashboard transport path
       - focused tests / page tests 必须继续证明 `/classroom` live-answer surface 只挂在真实 teacher control-room route 上，而不是新建第二 route 或放宽 access control
    2. `Zero-write guard` — 调用 `pnpm test:live-answer-zero-write`
    3. `Focused Phase 73 suites` — 在 full 模式运行这条固定命令：
       `pnpm vitest run src/features/platform-core/plugin-data-access/quiz-data-access.test.ts src/features/platform-core/plugin-data-access/allowlist.test.ts src/components/classroom/classroom-session-recap-surface.test.tsx src/components/classroom/live-answer-dashboard-store.test.ts src/components/classroom/live-answer-dashboard-surface.test.tsx src/features/runtime-platform/seams/transport/ws-envelope.test.ts src/features/runtime-platform/seams/transport/ws-adapter.test.ts src/features/runtime-platform/seams/transport/redis-fanout-manager.test.ts tests/classroom/ws-events.test.ts tests/classroom/fanout.test.ts tests/classroom/dashboard.test.ts tests/classroom/live-view.test.ts "src/app/(classroom)/classroom/page.test.tsx" src/components/classroom/classroom-control-panel.test.tsx src/lib/dal/classroom.test.ts`

    同时修改 `package.json`：
    - 新增精确脚本：`"verify:phase73": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase73-quiz-ext.ts"`
    - **不要**修改 `verify:phase`
    - **不要**添加 `verify:phase73-v41-close-gate`（那是下一个 plan）
  </action>
  <verify>
    <automated>pnpm verify:phase73 --smoke</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` 出现精确字符串 `"verify:phase73": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase73-quiz-ext.ts"`
    - `package.json` 仍保留精确字符串 `"verify:phase": "pnpm verify:phase72"`
    - `scripts/verify-phase73-quiz-ext.ts` 导出或定义 `runPhase73Verification`
    - `pnpm verify:phase73 --smoke` exit 0
    - 脚本源码 **不** 包含 `73-PROOF-MAPPING.md`、`73-CLOSEOUT.md`、`verify:phase72` 这些 outer-close 依赖
     - smoke/static stage 明确断言 `src/db/schema/generated/plugin-owned/quiz.ts` 的 `plugin_owned_quiz_questions.questionType` 5 题型 enum、`src/lib/dto/plugin-data-model.ts` 的 `QuestionTypeSchema`、`src/lib/dto/classroom.ts` 的 5-branch DTO union、`src/db/schema/generated/plugin-owned/data-access-allowlist.ts` / `src/features/platform-core/plugin-data-access/allowlist.ts` 的 `questionType` allowlist，以及 `src/actions/classroom-actions.ts` 的 `updateTag(cacheTags.quizStats(parsed.data.sessionId))`
     - smoke/static stage 明确断言 teacher-only / access-control seam：`src/features/runtime-platform/seams/transport/ws-connection-registry.ts` 包含 `connection.owner.actorScope !== "teacher"`，`src/features/runtime-platform/seams/transport/ws-auth.ts` 保留 active membership / teacher ownership 推导，且 `src/app/(classroom)/classroom/page.tsx` / `src/components/classroom/classroom-control-panel.tsx` 继续把 `live-answer` 约束在真实 teacher control-room surface
     - full-mode focused suite命令显式包含 `src/features/platform-core/plugin-data-access/quiz-data-access.test.ts`、`src/features/platform-core/plugin-data-access/allowlist.test.ts`、`src/components/classroom/classroom-session-recap-surface.test.tsx`、`tests/classroom/ws-events.test.ts`、`tests/classroom/fanout.test.ts`、`tests/classroom/dashboard.test.ts`，且继续覆盖 `live-answer-dashboard-store.test.ts`、`live-answer-dashboard-surface.test.tsx`、`classroom-control-panel.test.tsx`、`src/lib/dal/classroom.test.ts`
     - focused tests / acceptance 明确覆盖 teacher-only / access-control proof，而不只是 dashboard 存在性
  </acceptance_criteria>
  <done>`verify:phase73` 已成为独立产品 truth lane，显式覆盖多题型 schema / enum / DTO union / allowlist / cache-tag / recap / WS / dashboard 证明链；`verify:phase` 仍冻结在 phase72。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| product proof → inner verifier | `verify:phase73` must consume real product seams/tests, not stale summaries |
| proof artifact authoring → close artifact chain | `73-PROOF-MAPPING.md` becomes a downstream parser target for manual ledger enforcement |
| package.json script wiring → global milestone alias | Adding `verify:phase73` must not mutate `verify:phase` prematurely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01-01 | Tampering | `package.json` script wiring | mitigate | Add only `verify:phase73`; leave `verify:phase` exact `pnpm verify:phase72` per D-03 and assert exact-string equality in task acceptance criteria |
| T-74-01-02 | Repudiation | `73-PROOF-MAPPING.md` manual ledger | mitigate | Create the locked five-field ledger schema now, but keep v4.1 rows in `status: pending-human-signoff` until a real observer completes Plan 74-04/05 |
| T-74-01-03 | Repudiation | `verify:phase73` evidence source | mitigate | Inner verifier reads real product seams/tests only (`classroom.ts`, `ws-envelope.ts`, live dashboard tests) and must not cite summaries as proof |
| T-74-01-04 | Information Disclosure | carried-forward proof rows | accept | v4.0 carried-forward rows only reference already-archived proof artifacts; no new secret or student payload is introduced in this plan |
</threat_model>

<verification>
- `pnpm verify:phase73 --smoke`
- `grep -q "Manual Surface Sign-Off Ledger" .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md`
- `grep -q '"verify:phase": "pnpm verify:phase72"' package.json`
</verification>

<success_criteria>
- `73-PROOF-MAPPING.md` exists before any closeout doc and carries the 4-row ledger scaffold
- `verify:phase73` can be run independently in smoke mode
- `verify:phase` remains unchanged on phase72 after this plan
</success_criteria>

<output>
After completion, create `.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-01-SUMMARY.md`
</output>
