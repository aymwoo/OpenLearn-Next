---
phase: 74-v4-1-authoritative-close-gate-multi-type-live-dashboard
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - scripts/lib/phase74-observation-auth-stub.ts
  - scripts/prepare-phase74-observation-targets.ts
  - .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-OBSERVATION-TARGETS.md
  - .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md
autonomous: false
requirements: [QUIZ-EXT-CLOSE-02]
must_haves:
  truths:
    - "在真人 checkpoint 之前，必须先用真实 DAL launch/end 机制准备出 1 个 teacher-owned live session 与 1 个 ended session，并产出精确 `/classroom?...` 观察 URL（per D-05/D-06）"
    - "`/classroom` live-answer tab 必须由真人在真实 teacher route 上观察后才能记为 passed（per D-05/D-06）"
    - "multi-type ended-session recap surface 必须由真人在真实 product route 上观察后才能记为 passed（per D-05/D-06）"
    - "观察目标必须先由准备任务写入精确 `/classroom?sessionId=...&tab=live-answer` 与 `/classroom?sessionId=...`；checkpoint 不再允许人工从 session list 现找目标，也不得使用占位 `<teacherLiveSessionId>` / `<endedSessionId>`（per D-05/D-06）"
  artifacts:
    - path: ".planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-OBSERVATION-TARGETS.md"
      provides: "deterministic live/ended classroom observation URLs for the human checkpoint"
      contains: "live_url: /classroom?sessionId="
    - path: ".planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md"
      provides: "durable handoff artifact carrying LIVE_ANSWER_SIGNOFF and RECAP_SIGNOFF payloads into Plan 74-05"
      contains: "## LIVE_ANSWER_SIGNOFF"
    - path: ".planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md"
      provides: "manual ledger target to be updated after checkpoint"
      contains: "Manual Surface Sign-Off Ledger"
  key_links:
    - from: "scripts/prepare-phase74-observation-targets.ts"
      to: ".planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-OBSERVATION-TARGETS.md"
      via: "real DAL launch/end preparation emits exact /classroom URLs"
      pattern: "live_url: /classroom?sessionId="
    - from: "real /classroom live route"
      to: "73-PROOF-MAPPING.md v4.1 live-answer row"
      via: "human-observed executed_by/executed_at/evidence_note"
      pattern: "live-answer"
    - from: "real ended-session recap route"
      to: "73-PROOF-MAPPING.md v4.1 recap row"
      via: "human-observed executed_by/executed_at/evidence_note"
      pattern: "题目复盘"
    - from: "scripts/seed-test-accounts.ts"
      to: "real /classroom sessionEntries list"
      via: "deterministic teacher login before human sign-off"
      pattern: "teacher@example.com"
    - from: ".planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md"
      to: ".planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md"
      via: "durable LIVE_ANSWER_SIGNOFF / RECAP_SIGNOFF handoff into the final proof-mapping fill step"
      pattern: "## LIVE_ANSWER_SIGNOFF"
---

<objective>
先自动准备出可观察的真实 live / ended classroom targets，再暂停自动执行，要求真人在真实 `/classroom` 产品路径上完成两条 v4.1 surface 的最终观察，并把可审计的 sign-off 数据带回后续计划。这个 plan 不允许自动把 manual rows 标成 passed。

Purpose: 满足 D-05 / D-06 的真实人工签核要求。
Output: `74-OBSERVATION-TARGETS.md` + `74-MANUAL-SIGNOFF.md`（持久化保存两组可审计 sign-off payload），供下个 plan 回填 proof mapping。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-CONTEXT.md
@.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-UI-SPEC.md
@.planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md
@scripts/seed-test-accounts.ts
    @scripts/verify-phase69-quiz-sample.ts
    @scripts/prepare-dev-db.ts
    @scripts/lib/phase69-auth-stub.ts
    @.planning/milestones/v4.0-phases/72.1-close-gap-gate-01-authoritative-milestone-close-gate/72.1-PROOF-MAPPING.md
@src/app/(classroom)/classroom/page.tsx
@src/app/(classroom)/classroom/page.test.tsx
@src/lib/dal/classroom.ts
@src/components/classroom/classroom-control-panel.tsx
@src/components/classroom/live-answer-dashboard-surface.tsx
@src/components/classroom/classroom-session-recap-surface.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 先准备真实 live / ended observation targets，并输出精确 `/classroom` URL</name>
  <read_first>
    - scripts/seed-test-accounts.ts
    - scripts/verify-phase69-quiz-sample.ts
    - scripts/lib/phase69-auth-stub.ts
    - src/lib/dal/classroom.ts
    - src/app/(classroom)/classroom/page.tsx
    - src/app/(classroom)/classroom/page.test.tsx
    - scripts/prepare-phase74-observation-targets.ts (create this file)
    - scripts/lib/phase74-observation-auth-stub.ts (create this file)
    - .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-OBSERVATION-TARGETS.md (create this file)
    - .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md (create this file later in Task 2)
  </read_first>
  <files>scripts/lib/phase74-observation-auth-stub.ts, scripts/prepare-phase74-observation-targets.ts, .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-OBSERVATION-TARGETS.md</files>
  <action>
    先创建一个**确定性 observation-target preparation lane**，让人工 checkpoint 只消费已准备好的真实 URL，而不是自己从 session list 猜目标。

    具体要求：
    1. 复用 `scripts/lib/phase69-auth-stub.ts` 的模式创建 `scripts/lib/phase74-observation-auth-stub.ts`，让 CLI 脚本能够以 seeded teacher / student 身份调用真实 DAL（不是 mock route，不是手填 sessionId）。准备脚本的 artifact 写法同时参考 `scripts/prepare-dev-db.ts`；目标 markdown 字段表模式参考 `72.1-PROOF-MAPPING.md` 的固定 field/value 呈现方式。
    2. 新建 `scripts/prepare-phase74-observation-targets.ts`，固定通过真实 repo 机制准备或定位 observation targets：
       - 先调用 `seedTestAccounts()`（或等价复用 `pnpm seed:test-accounts` 的逻辑），确保 `teacher@example.com` / `password` 存在
       - 优先 locate 现有 teacher-owned live / ended sessions；但只有在它们都走真实 `/classroom` route shape、并且 ended session 对应的是可展示多题型 recap 的真实会话时才允许复用
       - 若现有目标不满足，按 `scripts/verify-phase69-quiz-sample.ts` 的真实 DAL 模式补齐 fixture：准备 teacher-owned published lesson / class roster，调用 `launchClassroomSession(...)` 产生 live session；再用同一 lesson 再起一个 session，并在需要时用 `recordClassroomVotingRoundControl(...)` / `submitQuizSampleAnswer(...)` / `endClassroomSession(...)` 形成一个 ended session，使 `/classroom?sessionId=<ended>` 真正能显示 multi-type recap surface，而不是空占位
       - live 与 ended 两个 session 都必须出现在 `src/app/(classroom)/classroom/page.tsx` 所使用的同一 `/classroom` + `sessionEntries` route shape 上
    3. 脚本成功后写出 `.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-OBSERVATION-TARGETS.md`，至少包含这些精确字段：
       - `teacher_login: teacher@example.com / password`
       - `live_session_id: <id>`
       - `live_url: /classroom?sessionId=<id>&tab=live-answer`
       - `ended_session_id: <id>`
       - `ended_url: /classroom?sessionId=<id>`
       - `preparation_source: launchClassroomSession(...) / endClassroomSession(...) / sessionEntries`

    失败语义必须收紧：如果准备步骤无法产出这两个真实 URL，就让脚本 exit 1，并把错误写成 `OBSERVATION_PREPARATION_FAILED`；不要把 `OBSERVATION_TARGETS_MISSING` 留给人工 checkpoint 作为常规分支。
  </action>
  <verify>
    <automated>pnpm seed:test-accounts && node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/prepare-phase74-observation-targets.ts && grep -q 'teacher_login: teacher@example.com / password' .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-OBSERVATION-TARGETS.md && grep -q 'live_url: /classroom?sessionId=' .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-OBSERVATION-TARGETS.md && grep -q '&tab=live-answer' .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-OBSERVATION-TARGETS.md && grep -q 'ended_url: /classroom?sessionId=' .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-OBSERVATION-TARGETS.md</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/prepare-phase74-observation-targets.ts` 明确复用 `launchClassroomSession(...)` 与 `endClassroomSession(...)` 的真实 DAL 机制；必要时可复用 `recordClassroomVotingRoundControl(...)` / `submitQuizSampleAnswer(...)` 来让 ended target 具备真实 recap 数据
    - `.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-OBSERVATION-TARGETS.md` 存在，且显式写出 `teacher_login: teacher@example.com / password`
    - artifact 内显式写出一条 `live_url: /classroom?sessionId=...&tab=live-answer` 与一条 `ended_url: /classroom?sessionId=...`
    - live / ended 两条 target 都是 teacher-owned real sessions，并来自 `sessionEntries` 同一 `/classroom` route shape，不是 mock route / invented ID / placeholder
    - `OBSERVATION_TARGETS_MISSING` 不再是人工 checkpoint 的常规分支；缺少 target 只允许作为 Task 1 的 `OBSERVATION_PREPARATION_FAILED` 错误返回
  </acceptance_criteria>
  <done>真人 checkpoint 所需的 live / ended observation targets 已被确定性准备并写成精确 `/classroom` URL。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 真人观察 live-answer tab 与 multi-type recap surface，并回传审计字段</name>
  <read_first>
    - .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-UI-SPEC.md
    - .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-OBSERVATION-TARGETS.md
    - .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md
    - src/components/classroom/live-answer-dashboard-surface.tsx
    - src/components/classroom/classroom-session-recap-surface.tsx
    - src/app/(classroom)/classroom/page.tsx
  </read_first>
  <files>.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md</files>
  <action>
    本 checkpoint **不再负责找 target**。先读取 `74-OBSERVATION-TARGETS.md`，只使用其中已经准备好的精确 URL：
    1. 启动应用并使用 `teacher@example.com` / `password` 登录真实教师端。
    2. 运行 `pnpm verify:phase73 --smoke`。
    3. 运行 `pnpm verify:phase73-v41-close-gate --smoke`。
    4. 打开 `74-OBSERVATION-TARGETS.md` 中的 `live_url`（必须是精确 `/classroom?sessionId=...&tab=live-answer`），确认看到 `作答实时` tab、连接状态 badge、按题分布/作答流水双视图、`5/20/50` recent limit，且没有任何评分/改答/删除类写入口。
    5. 打开 `74-OBSERVATION-TARGETS.md` 中的 `ended_url`（必须是精确 `/classroom?sessionId=...`），确认看到 `题目复盘` 区块、5 题型统计块、无 1px divider、无新增排行榜/评分/竞争机制。

    如果 Task 1 已经成功但这里的 URL 无法打开、未落到真实 `/classroom` 路径、或 surface 与准备结果不一致，就按错误处理上报 `OBSERVATION_PREPARATION_FAILED`，说明准备产物与真实页面不一致；不要退回到人工现找 sessionId 的旧流程。

    观察完成后，先把两组 payload **持久化写入** `.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md`，再在恢复消息中原样回传，避免后续上下文切换丢失。`74-MANUAL-SIGNOFF.md` 必须使用固定 field/value table 模式（参考 `72.1-PROOF-MAPPING.md`），并至少包含两个 section：`## LIVE_ANSWER_SIGNOFF`、`## RECAP_SIGNOFF`。

    按下面 **原样** 写入文件并同步回传（下一 plan 只从 `74-MANUAL-SIGNOFF.md` 读取并回填 proof mapping）：

    `## LIVE_ANSWER_SIGNOFF`
    `| field | value |`
    `|-------|-------|`
    `| session_id | <74-OBSERVATION-TARGETS.md 中的 live_session_id> |`
    `| observed_url | <74-OBSERVATION-TARGETS.md 中的 live_url> |`
    `| executed_by | <真实观察人> |`
    `| executed_at | <ISO timestamp> |`
    `| evidence_note | <一句话描述你实际看到的 live-answer surface> |`

    `## RECAP_SIGNOFF`
    `| field | value |`
    `|-------|-------|`
    `| session_id | <74-OBSERVATION-TARGETS.md 中的 ended_session_id> |`
    `| observed_url | <74-OBSERVATION-TARGETS.md 中的 ended_url> |`
    `| executed_by | <真实观察人> |`
    `| executed_at | <ISO timestamp> |`
    `| evidence_note | <一句话描述你实际看到的 recap surface> |`
  </action>
  <verify>
    <automated>test -f .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-OBSERVATION-TARGETS.md && test -f .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md && grep -q '^## LIVE_ANSWER_SIGNOFF' .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md && grep -q '^## RECAP_SIGNOFF' .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md && grep -q '^| session_id |' .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md && grep -q '^| observed_url |' .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md && grep -q '^| executed_by |' .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md && grep -q '^| executed_at |' .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md && grep -q '^| evidence_note |' .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md && pnpm verify:phase73 --smoke && pnpm verify:phase73-v41-close-gate --smoke</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md` 已落地，且同时包含标题 `## LIVE_ANSWER_SIGNOFF` 与 `## RECAP_SIGNOFF`
    - 恢复消息同时包含标题 `LIVE_ANSWER_SIGNOFF` 与 `RECAP_SIGNOFF`
    - 每个 sign-off section 都采用固定 `| field | value |` 表格，且包含 `session_id` 与 `observed_url`，URL 必须与 `74-OBSERVATION-TARGETS.md` 中预先生成的真实 `/classroom?...` 路径一致
    - 每个标题下都包含 `executed_by`、`executed_at`、`evidence_note` 三个字段
    - `executed_at:` 使用 ISO 时间格式（示例：`2026-06-08T12:34:56Z`）
    - 两条观察都发生在真实 `/classroom` 路径，不是 mock page / debug page / 设计稿
  </acceptance_criteria>
  <how-to-verify>
    1. 打开 `74-OBSERVATION-TARGETS.md`，确认里面已有 `teacher_login`、`live_url`、`ended_url`。
    2. 登录 `teacher@example.com` / `password`。
    3. 用 artifact 中的 `live_url` 打开 `/classroom?sessionId=<...>&tab=live-answer`。
    4. 确认可见 `作答实时`、`按题分布`、`作答流水`、`5`/`20`/`50` 切换，且看不到评分/改答/删除按钮。
    5. 用 artifact 中的 `ended_url` 打开 `/classroom?sessionId=<...>`。
    6. 确认可见 `题目复盘` 和 5 题型统计块，且没有排行/评分/竞争入口。
    7. 先把两组 payload 写入 `74-MANUAL-SIGNOFF.md`，再按上面的 payload 模板回复两组真实观察结果。
  </how-to-verify>
  <resume-signal>回复包含 `LIVE_ANSWER_SIGNOFF` 与 `RECAP_SIGNOFF` 两组字段，且 `74-MANUAL-SIGNOFF.md` 已写入；若预生成 URL 与真实页面不一致，则回复 `OBSERVATION_PREPARATION_FAILED` 并附上问题。</resume-signal>
  <done>已收到并持久化两组真实 human sign-off payload，且每组都绑定到确定的 `/classroom` sessionId，可以进入 proof mapping 回填与最终 closeout。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preparation script → human checkpoint | automated target generation must emit the exact teacher-owned `/classroom` URLs that the observer will use |
| automated smoke proof → human sign-off | automation may prove wiring but cannot replace real human observation |
| human observer → signoff artifact → proof ledger | observer identity and timestamp become auditable close evidence only after being persisted to a durable phase artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-04-01 | Tampering | observation-target preparation | mitigate | Use real DAL `launchClassroomSession(...)` / `endClassroomSession(...)` preparation and fail Task 1 if exact `live_url` + `ended_url` cannot be emitted into `74-OBSERVATION-TARGETS.md` |
| T-74-04-02 | Repudiation | manual sign-off rows | mitigate | Require real observer name, ISO timestamp, and evidence note for both surfaces before any row can be marked passed, and persist them in `74-MANUAL-SIGNOFF.md` before plan handoff |
| T-74-04-03 | Information Disclosure | live-answer teacher surface | mitigate | Human observation must happen on the real teacher-owned `/classroom` route to confirm teacher-only visibility is preserved |
| T-74-04-04 | Tampering | checkpoint bypass | mitigate | This plan is blocking (`autonomous: false`) and the next plan may not proceed without the exact `LIVE_ANSWER_SIGNOFF` and `RECAP_SIGNOFF` payloads |
</threat_model>

<verification>
- `pnpm seed:test-accounts && node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/prepare-phase74-observation-targets.ts`
- `grep -q 'live_url: /classroom?sessionId=' .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-OBSERVATION-TARGETS.md`
- `pnpm verify:phase73 --smoke && pnpm verify:phase73-v41-close-gate --smoke`
- `test -f .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md`
- Human resume payload contains both sign-off blocks exactly as requested and includes concrete `session_id` / `observed_url`
</verification>

<success_criteria>
- A deterministic preparation step has produced one real teacher-owned live session and one real teacher-owned ended session, with exact `/classroom?...` URLs emitted before the checkpoint
- A real human has observed both surfaces on the production route shape
- The live and ended observation targets came from the preparation artifact, not ad hoc manual target hunting
- Two auditable sign-off payloads are persisted in `74-MANUAL-SIGNOFF.md` and are available for proof-mapping update
</success_criteria>

<output>
After completion, create `.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-04-SUMMARY.md`
</output>
