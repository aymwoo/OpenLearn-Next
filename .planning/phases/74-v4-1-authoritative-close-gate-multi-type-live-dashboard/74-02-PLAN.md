---
phase: 74-v4-1-authoritative-close-gate-multi-type-live-dashboard
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scripts/verify-phase73-v41-close-gate.ts
  - package.json
autonomous: true
requirements: [QUIZ-EXT-CLOSE-01, QUIZ-EXT-CLOSE-03]
must_haves:
  truths:
    - "outer `verify:phase73-v41-close-gate` 只消费 `pnpm verify:phase73` 作为上游 proof lane，不复制 Phase 73 产品断言（per D-02）"
    - "outer gate 保留 v4.0 authoritative gate 的 5-stage baseline（含 lifecycle bridge / recap bridge regression），再叠加 v4.1 的 multi-type 与 live-dashboard 两个 close stages；在 final cutover 前继续把 `verify:phase` 保持到 phase72（per D-03 / D-72.1-08）"
    - "`verify:phase73-v41-close-gate` full mode 保留 baseline bridge regression，并在缺少 `73-PROOF-MAPPING.md` / `73-VERIFICATION.md` / `73-CLOSEOUT.md`、proof-chain wording、4 条 manual ledger passed rows 任一时 hard-fail；`--smoke` 只验证 wiring/parser/readiness 并以 blocked report exit 0"
  artifacts:
    - path: "scripts/verify-phase73-v41-close-gate.ts"
      provides: "Thin authoritative close gate for v4.1"
      contains: "verifyManualSignOffLedger"
    - path: "package.json"
      provides: "verify:phase73-v41-close-gate script registration while alias remains phase72"
      contains: "verify:phase73-v41-close-gate"
  key_links:
    - from: "scripts/verify-phase73-v41-close-gate.ts"
      to: "package.json#verify:phase73-v41-close-gate"
      via: "exact script entry"
      pattern: "verify:phase73-v41-close-gate"
    - from: "scripts/verify-phase73-v41-close-gate.ts"
      to: "pnpm verify:phase73"
      via: "upstream product proof lane"
      pattern: "verify:phase73"
    - from: "scripts/verify-phase73-v41-close-gate.ts"
      to: ".planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md"
      via: "manual ledger parser and proof-chain wording checks"
      pattern: "Manual Surface Sign-Off Ledger"
---

<objective>
在 proof mapping 已存在的前提下，补一个 **薄 outer gate**：`verify:phase73-v41-close-gate` 只做 close truth（artifact dependencies / manual sign-off / proof-chain wording / alias readiness），不重跑 inner verifier 之外的产品断言，也不提前切换 `verify:phase`。

Purpose: 把两层职责切开，确保 v4.1 close gate authoritative 但不与产品 truth 重叠。
Output: `scripts/verify-phase73-v41-close-gate.ts` + `package.json#verify:phase73-v41-close-gate`。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-CONTEXT.md
@.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-RESEARCH.md
@.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-PATTERNS.md
@.planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md
@scripts/verify-phase72-close-gate.ts
@scripts/verify-phase73-quiz-ext.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 从 verify-phase72-close-gate.ts 复制 skeleton，做 thin outer gate</name>
  <read_first>
    - scripts/verify-phase72-close-gate.ts
    - scripts/verify-phase73-quiz-ext.ts
    - .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-CONTEXT.md
    - .planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md
    - scripts/verify-phase73-v41-close-gate.ts (create this file)
  </read_first>
  <files>scripts/verify-phase73-v41-close-gate.ts</files>
  <behavior>
    - Test 1: `pnpm verify:phase73-v41-close-gate --smoke` exits 0, prints 7-stage close-truth/readiness output, and reports future artifacts/manual rows as `blocked` instead of hard-failing wave 2
    - Test 2: full mode runs `pnpm verify:phase73` as the single upstream product proof lane
    - Test 3: full mode preserves v4.0 stage 3 `Lifecycle milestone-bridge static seams` and stage 4 `Recap / stats milestone-bridge static seams` regression checks from `scripts/verify-phase72-close-gate.ts`
    - Test 4: full mode requires 4 passed rows total in the single proof-mapping ledger and separately proves the 2 v4.1 surfaces are the newly-added rows counted toward that total, using exact field tokens `| status | \`status: passed\` |`, `| executed_by |`, `| executed_at |`, `| evidence note |`
    - Test 5: full mode parses each passed proof-mapping row and rejects empty `executed_by`, non-ISO `executed_at`, or empty `evidence note`; the 2 v4.1 rows must additionally cross-check non-empty `session_id` / `observed_url` against `.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md`, not against new fields invented inside `73-PROOF-MAPPING.md`
  </behavior>
  <action>
    新建 `scripts/verify-phase73-v41-close-gate.ts`，直接复用 `verify-phase72-close-gate.ts` 的 `StaticCheck` / `Stage` / `run()` / `countOccurrences()` 结构，并明确保留 **v4.0 5-stage baseline + v4.1 2 stages = 7 stages**：
    1. `Static script wiring` — `package.json` 存在 `verify:phase73` 与 `verify:phase73-v41-close-gate` 精确 entry；outer gate 对 `verify:phase` 只接受两种合法 posture：pre-cutover 的 `pnpm verify:phase72`，或 final-cutover 成功后的 `pnpm verify:phase72 && pnpm verify:phase73-v41-close-gate`。outer gate 不负责决定何时切换 alias，只负责把当前 alias 判定为 `pre-cutover valid` / `post-cutover valid` / `invalid alias state`
    2. `Upstream product proof lane` — full mode 执行 `pnpm verify:phase73`，smoke mode 执行 `pnpm verify:phase73 --smoke`
    3. `Lifecycle milestone-bridge static seams` — 保留 `scripts/verify-phase72-close-gate.ts` 的 lifecycle bridge regression，确保 `/settings/plugins` / phase71 marketplace lifecycle baseline 仍被 authoritative gate 回归覆盖（D-72.1-08）
    4. `Recap / stats milestone-bridge static seams` — 保留 `scripts/verify-phase72-close-gate.ts` 的 recap bridge regression，确保 v4.0 recap/stats baseline 在 v4.1 close gate 下不退化
    5. `Final-artifact dependencies + manual sign-off ledger` — full mode hard-check `.planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-PROOF-MAPPING.md`、`73-VERIFICATION.md`、`73-CLOSEOUT.md` 三件套存在，并要求单一 Phase 73 proof-mapping ledger 中 4 条 passed rows 逐行满足：`status: passed`、`executed_by` 非空、`executed_at` 为可解析 ISO 时间、`evidence note` 非空；其中 2 条 v4.1 rows 的 `session_id` / `observed_url` 则必须从 `.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-MANUAL-SIGNOFF.md` 读取并验证非空，再与对应 surface 身份交叉核对。smoke mode 只验证这些 exact path constants、ledger parser 与 readiness reporting 已接线，文件缺失时报告 `blocked` 但不 exit 1
    6. `Multi-type close-proof crosswalk` — full mode 校验 `73-PROOF-MAPPING.md` / `73-VERIFICATION.md` 已按上游文档计划显式收录 `QUIZ-EXT-01-A..E`、`QUIZ-EXT-02-A..E`、`QUIZ-EXT-CLOSE-01/02/03`、`verify:phase73`、`scripts/verify-phase73-quiz-ext.ts`、`src/lib/dal/classroom.ts`、`src/components/classroom/classroom-session-recap-surface.tsx` 等 proof structure / requirement trace；不要额外 hard-grep 上游计划未承诺必须落文的 `QuestionTypeSchema` / `cacheTags.quizStats` 等 token。smoke mode 只验证这些 proof-structure hooks 已定义
    7. `Live-dashboard close-proof crosswalk + alias readiness` — full mode 校验 `73-PROOF-MAPPING.md` / `73-VERIFICATION.md` / `73-CLOSEOUT.md` 显式点名 `/classroom` live-answer surface、`quiz.answer.received`、teacher-only posture、`no second transport runtime`，并输出 D-04 predicate-by-predicate readiness；smoke mode 只证明 crosswalk/parser/readiness machinery 已就绪

    明确禁止在 outer gate 里再次 grep/断言 `quiz.answer.received`、`buildQuizSampleRecapStats`、`useLiveAnswerStore` 这类 inner-product seams；outer gate 只能调用 `pnpm verify:phase73` 获取这些 truths。
  </action>
  <verify>
    <automated>node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase73-v41-close-gate.ts --smoke</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-phase73-v41-close-gate.ts` 存在并定义 manual ledger parser，且在 full mode 里同时读取 `73-PROOF-MAPPING.md` 与 `74-MANUAL-SIGNOFF.md`
    - 脚本源码包含 `73-PROOF-MAPPING.md`、`73-VERIFICATION.md`、`73-CLOSEOUT.md` 三个精确路径
    - 脚本源码包含 `pnpm verify:phase73`，且 **不** 直接包含 `buildQuizSampleRecapStats` / `quiz.answer.received` / `useLiveAnswerStore` 这类 inner seam token 作为 close checks
    - `node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase73-v41-close-gate.ts --smoke` exit 0，并给出 7-stage close-truth + readiness 输出；未来 artifacts/manual rows 未齐时只能报告 `blocked`，不能让 wave 2 verify fail
    - full-mode 源码路径明确要求 4 条 passed rows 逐行解析并校验 `executed_by` 非空、`executed_at` 可解析、`evidence note` 非空；v4.1 两条 rows 的 `session_id` / `observed_url` 非空校验来自 `74-MANUAL-SIGNOFF.md`，并单独识别 `/classroom` live-answer` 与 `classroom-session-recap-surface.tsx`
    - full-mode 的 multi-type crosswalk 检查只验证上游计划明确承诺的 requirement trace / proof structure（`QUIZ-EXT-01-A..E`、`QUIZ-EXT-02-A..E`、`QUIZ-EXT-CLOSE-*`、`verify:phase73`、proof files/scripts/tests），不再硬-grep 上游未承诺的 token
    - full-mode 计划文本明确保留 v4.0 stage 3 `Lifecycle milestone-bridge static seams` 与 stage 4 `Recap / stats milestone-bridge static seams` 回归，不允许在 v4.1 close gate 中删除这两条 baseline bridge regression
  </acceptance_criteria>
  <done>outer gate 脚本成型且职责边界清晰：product truth inward, close truth outward。</done>
</task>

<task type="auto">
  <name>Task 2: 注册 verify:phase73-v41-close-gate，且 verify:phase 继续冻结到 phase72</name>
  <read_first>
    - package.json
    - scripts/verify-phase73-v41-close-gate.ts
    - .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-CONTEXT.md
  </read_first>
  <files>package.json</files>
  <action>
    修改 `package.json` 的 scripts：
    - 新增精确 entry：`"verify:phase73-v41-close-gate": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase73-v41-close-gate.ts"`
    - 保留 `"verify:phase73": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase73-quiz-ext.ts"`
    - 保留 `"verify:phase": "pnpm verify:phase72"`

    不要把 alias 提前改成 `verify:phase72 && verify:phase73-v41-close-gate`；cutover 留给最终条件任务（D-03 / D-04）。
  </action>
  <verify>
    <automated>pnpm verify:phase73-v41-close-gate --smoke</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` 出现精确字符串 `"verify:phase73-v41-close-gate": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase73-v41-close-gate.ts"`
     - `package.json` 在 wave 2 仍出现精确字符串 `"verify:phase": "pnpm verify:phase72"`，同时计划文本明确声明 outer gate 最终也接受 post-cutover 的合法组合 alias
    - `pnpm verify:phase73-v41-close-gate --smoke` exit 0，可执行并输出 close-truth stages + readiness report
    - 未来 artifacts/manual rows 未完成时只能以 readiness `blocked` 形式出现，不得因为它们尚未生成而让 wave 2 package wiring verify 失败
  </acceptance_criteria>
  <done>outer gate 已注册且 global alias 仍冻结到 phase72。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inner verifier → outer gate | outer gate must trust `verify:phase73` for product truth and not fork a second proof grammar |
| proof artifacts → parser | close gate treats markdown artifacts and ledger rows as executable inputs |
| package script wiring → alias cutover | package.json controls the single milestone entrypoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-02-01 | Tampering | `scripts/verify-phase73-v41-close-gate.ts` | mitigate | Outer gate may only run `pnpm verify:phase73` for product truth; task acceptance explicitly forbids direct product-seam duplication in close checks |
| T-74-02-02 | Tampering | final-artifact dependency stage | mitigate | Hard-check exact existence of `73-PROOF-MAPPING.md`, `73-VERIFICATION.md`, and `73-CLOSEOUT.md`; missing any file fails the gate |
| T-74-02-03 | Repudiation | manual ledger parser | mitigate | Use deterministic field tokens and require passed-row plus `executed_by` / `executed_at` / `evidence note` counts before readiness can pass |
| T-74-02-04 | Elevation of Privilege | `package.json#verify:phase` | mitigate | Keep alias unchanged on `pnpm verify:phase72`; move cutover to a separate final conditional task guarded by D-04 |
</threat_model>

<verification>
- `pnpm verify:phase73-v41-close-gate --smoke`
- `grep -Eq '"verify:phase": "pnpm verify:phase72( && pnpm verify:phase73-v41-close-gate)?"' package.json`
- `grep -q 'pnpm verify:phase73' scripts/verify-phase73-v41-close-gate.ts`
- `grep -q 'Lifecycle milestone-bridge static seams' scripts/verify-phase73-v41-close-gate.ts && grep -q 'Recap / stats milestone-bridge static seams' scripts/verify-phase73-v41-close-gate.ts`
</verification>

<success_criteria>
- Outer gate exists and only consumes `verify:phase73` for product proof
- Outer gate preserves the v4.0 lifecycle / recap bridge regression baseline while adding v4.1 close-proof stages
- Wave-local smoke (`pnpm verify:phase73-v41-close-gate --smoke`) exits 0 by proving wiring/parser/readiness, while full mode remains strict on artifact/manual/proof-chain readiness
- Global alias is still unchanged after this plan
</success_criteria>

<output>
After completion, create `.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-02-SUMMARY.md`
</output>
