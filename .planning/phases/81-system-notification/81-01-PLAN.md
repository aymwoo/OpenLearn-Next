---
phase: 81-system-notification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/lib/dto/resource-ai.ts
  - src/lib/dto/notification.ts
  - src/features/runtime-platform/contracts/permissions.ts
  - src/features/platform-core/commands/contracts.ts
autonomous: true
requirements:
  - NOTIF-05
  - NOTIF-07
  - SYS-06
must_haves:
  truths:
    - "pluginNotifications 表存在于 Drizzle schema 中，字段包含 id/pluginId/schoolId/recipientUserId/notificationType/title/body/readAt/createdAt"
    - "manifest 中 system.notification 在 SystemCommandDiscriminatedSchema 中为合法 variant，包含 notificationTypes 白名单字段"
    - "PlatformCommandType 联合类型包含 system.notification.send"
    - "GovernanceDeniedReasonValues 包含 notification 相关拒因码"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "pluginNotifications 表定义 + 索引"
      contains: "export const pluginNotifications"
    - path: "src/lib/dto/resource-ai.ts"
      provides: "SystemCommandNotificationSchema + discriminatedUnion variant"
      contains: "SystemCommandNotificationSchema"
    - path: "src/lib/dto/notification.ts"
      provides: "NotificationDTO + insert/query schemas"
      contains: "NotificationInsertSchema"
    - path: "src/features/platform-core/commands/contracts.ts"
      provides: "SystemNotificationSendPayloadSchema + SystemCommandTypes 条目 + PlatformCommandPayloadSchemas 条目 + PlatformCommandSchema variant"
      contains: "system.notification.send"
    - path: "src/features/runtime-platform/contracts/permissions.ts"
      provides: "新增拒因码"
      contains: "notification_type_not_allowed"
  key_links:
    - from: "src/lib/dto/resource-ai.ts SystemCommandDiscriminatedSchema"
      to: "SystemCommandNotificationSchema"
      via: "z.discriminatedUnion variant"
      pattern: "z.strictObject.*command.*system.notification.*merge.*SystemCommandNotificationSchema"
    - from: "src/db/schema.ts pluginNotifications"
      to: "pluginRegistrations/schools/users"
      via: "foreign key references"
      pattern: "references.*pluginRegistrations"
---

<objective>
建立 Phase 81 的数据模型和类型契约基础层：新增 pluginNotifications 表（Drizzle schema）、定义 manifest SystemCommandNotificationSchema（Zod discriminatedUnion 第 4 variant）、新增 notification DTO schemas、扩展 PlatformCommandType 类型系统、新增通知相关拒因码。

Purpose: 为 Wave 2 的 Command Bus handler 和用户读路径提供类型安全保障，确保后续实现可以编译通过。
Output: pluginNotifications 表定义 + manifest schema variant + DTO schemas + 命令类型枚举扩展 + 拒因码扩展
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/wuxf/Develop/OpenLearn-Next/.planning/phases/81-system-notification/81-RESEARCH.md
@/home/wuxf/Develop/OpenLearn-Next/.planning/phases/81-system-notification/81-CONTEXT.md
@/home/wuxf/Develop/OpenLearn-Next/.planning/ROADMAP.md
@/home/wuxf/Develop/OpenLearn-Next/.planning/REQUIREMENTS.md

<interfaces>
从 RESEARCH.md 和现有代码库提取的契约规范：

**pluginNotifications 表结构（RESEARCH.md Code Examples）:**
- id: text PK, $defaultFn crypto.randomUUID()
- pluginId: text NOT NULL, FK → pluginRegistrations.id, onDelete cascade
- schoolId: text NOT NULL, FK → schools.id, onDelete cascade
- recipientUserId: text NOT NULL, FK → users.id, onDelete cascade
- notificationType: text NOT NULL
- title: text NOT NULL, max 256
- body: text NOT NULL, max 2048
- readAt: integer timestamp_ms, NULL = unread（per D-03）
- createdAt: integer timestamp_ms, $defaultFn new Date()
- 索引: (recipientUserId, readAt), (recipientUserId, createdAt), (readAt, createdAt)

**SystemCommandNotificationSchema 形状（RESEARCH.md Pattern 2）:**
- notificationTypes: z.array(z.string().min(1).max(64).regex(/^[a-z][a-z0-9_]*(?:\.[a-z][a-z0-9_]*)*$/)).min(1)

**SystemCommandDiscriminatedSchema 当前状态:**
```typescript
// src/lib/dto/resource-ai.ts lines 882-892
export const SystemCommandDiscriminatedSchema = z.discriminatedUnion("command", [
  z.strictObject({ command: z.literal("system.http.request") }).merge(SystemCommandHttpRequestSchema),
  z.strictObject({ command: z.literal("system.config") }).merge(SystemCommandConfigSchema),
  z.strictObject({ command: z.literal("system.file") }).merge(SystemCommandFileSchema),
]);
```

**SystemCommandTypes 当前状态:**
```typescript
// src/features/platform-core/commands/contracts.ts lines 39-43
export const SystemCommandTypes = [
  "system.http.request",
  "system.config.set",
  "system.file.upload",
  "system.file.delete",
] as const;
```

**GovernanceDeniedReasonValues 当前状态:**
```typescript
// src/features/runtime-platform/contracts/permissions.ts lines 32-47
// 现有 17 个值：not_allowlisted ~ quota_exceeded
```

**SystemNotificationSendPayloadSchema 形状（RESEARCH.md Code Examples）:**
- recipientUserId: z.string().min(1)
- notificationType: z.string().min(1).max(64)
- title: z.string().min(1).max(256)
- body: z.string().min(1).max(2048)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 pluginNotifications Drizzle 表定义</name>
  <files>src/db/schema.ts</files>
  <read_first>
src/db/schema.ts — 查看现有 pluginFiles 表定义作为模式参考（约 line 1917-1945），查看 pluginRegistrations, schools, users 表确认 FK 引用列名和类型
  </read_first>
  <action>
    在 src/db/schema.ts 中新增 pluginNotifications 表定义，放置在 pluginFiles 表之后（约 line 1945 之后）。表定义要求：

    1. 表名：sqliteTable("pluginNotification", ...)
    2. 字段（全部 NOT NULL 除非注明）：
       - id: text primaryKey, $defaultFn(() => crypto.randomUUID())
       - pluginId: text, FK → pluginRegistrations.id onDelete cascade
       - schoolId: text, FK → schools.id onDelete cascade
       - recipientUserId: text, FK → users.id onDelete cascade
       - notificationType: text
       - title: text
       - body: text
       - readAt: integer({ mode: "timestamp_ms" }), 可 NULL（未读）
       - createdAt: integer({ mode: "timestamp_ms" }), $defaultFn(() => new Date())

    3. 三个复合索引（在第二个参数 table creator 中声明）：
       - index("pluginNotif_user_read_idx").on(table.recipientUserId, table.readAt)
       - index("pluginNotif_user_created_idx").on(table.recipientUserId, table.createdAt)
       - index("pluginNotif_cleanup_idx").on(table.readAt, table.createdAt)

    不要使用 @deprecated 标签或字段。不要修改现有表定义。遵循项目现有 Drizzle sqliteTable 模式（使用 .notNull()、.references()、.$defaultFn()）。
  </action>
  <verify>
    <automated>grep -c "export const pluginNotifications" src/db/schema.ts | grep -q "^[1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - src/db/schema.ts 中包含 `export const pluginNotifications = sqliteTable("pluginNotification", ...)` 定义
    - 所有 9 个字段均已定义且有正确类型（id/pluginId/schoolId/recipientUserId/notificationType/title/body/readAt/createdAt）
    - pluginId 引用 pluginRegistrations.id，schoolId 引用 schools.id，recipientUserId 引用 users.id
    - 三个索引已声明：pluginNotif_user_read_idx、pluginNotif_user_created_idx、pluginNotif_cleanup_idx
    - `pnpm typecheck` 在本次所有修改完成后通过（跨 task 验证）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 新增 manifest schema + DTO + 命令类型契约</name>
  <files>
src/lib/dto/resource-ai.ts,
src/lib/dto/notification.ts,
src/features/platform-core/commands/contracts.ts,
src/features/runtime-platform/contracts/permissions.ts
  </files>
  <read_first>
src/lib/dto/resource-ai.ts (lines 800-892) — 现有 SystemCommandHttpRequestSchema/SystemCommandConfigSchema/SystemCommandFileSchema 模式 + SystemCommandDiscriminatedSchema discriminatedUnion
src/features/platform-core/commands/contracts.ts (lines 39-43, 295-334, 414-429) — SystemCommandTypes / PlatformCommandPayloadSchemas / PlatformCommandSchema discriminatedUnion
src/features/runtime-platform/contracts/permissions.ts (lines 32-47) — GovernanceDeniedReasonValues 现有值列表
  </read_first>
  <action>
    需要修改 4 个文件：

    **(A) src/lib/dto/resource-ai.ts**
    1. 在 SystemCommandFileSchema 之后（约 line 871 之后）新增 SystemCommandNotificationSchema：
       - 使用 z.strictObject({ notificationTypes: ... })
       - notificationTypes: z.array(z.string().min(1).max(64).regex(/^[a-z][a-z0-9_]*(?:\.[a-z][a-z0-9_]*)*$/)).min(1)
       - 错误消息使用 "SYSTEM_COMMAND_NOTIFICATION_TYPE_INVALID"
    2. 在 SystemCommandDiscriminatedSchema discriminatedUnion 数组中新增第 4 variant：
       - z.strictObject({ command: z.literal("system.notification") }).merge(SystemCommandNotificationSchema)
       - 放在 "system.file" variant 之后

    **(B) src/lib/dto/notification.ts（新建文件）**
    1. 创建 NotificationInsertSchema（handler.execute 使用）：
       - z.strictObject({ recipientUserId: z.string().min(1), notificationType: z.string().min(1).max(64), title: z.string().min(1).max(256), body: z.string().min(1).max(2048) })
    2. 导出 NotificationInsertInput = z.infer<typeof NotificationInsertSchema>
    3. 创建 NotificationDTOSchema（API 响应使用）：
       - 包含 id/pluginId/schoolId/recipientUserId/notificationType/title/body/readAt/createdAt
       - readAt 可选（z.number().nullable() 或 timestamp|null）
       - 从 db select 行映射到 DTO 的类型
    4. 创建 MarkReadSchema：z.discriminatedUnion("markAll", [
         z.strictObject({ markAll: z.literal(true) }),  → 全部已读
         z.strictObject({ notificationId: z.string().min(1) }), → 单条已读
       ])
    5. 导出相关类型

    **(C) src/features/platform-core/commands/contracts.ts**
    1. 在 SystemCommandTypes 数组中新增 "system.notification.send"（放在 "system.file.delete" 之后）
    2. 新增 SystemNotificationSendPayloadSchema（位于 SystemFileDeletePayloadSchema 之后）：
       - z.strictObject({ recipientUserId: z.string().min(1), notificationType: z.string().min(1).max(64), title: z.string().min(1).max(256), body: z.string().min(1).max(2048) })
    3. 在 PlatformCommandPayloadSchemas 中新增 key "system.notification.send" → SystemNotificationSendPayloadSchema
    4. 在 PlatformCommandSchema discriminatedUnion 中新增 variant：
       - type: z.literal("system.notification.send"), payload: SystemNotificationSendPayloadSchema
       - 放在 "system.file.delete" variant 之后

    **(D) src/features/runtime-platform/contracts/permissions.ts**
    1. 在 GovernanceDeniedReasonValues 数组中新增 3 个拒因码：
       - "notification_type_not_allowed"
       - "rate_limit_exceeded"
       - "recipient_not_in_school"
       放在 "quota_exceeded" 之后

    所有修改遵循现有模式（参照 SystemFileSchema 和 SystemFileUploadPayloadSchema）。不要引入新的依赖或修改无关代码。
  </action>
  <verify>
    <automated>
grep -c "SystemCommandNotificationSchema" src/lib/dto/resource-ai.ts | grep -q "^[1-9]" &&
grep -c "system.notification.send" src/features/platform-core/commands/contracts.ts | grep -q "^[2-9]" &&
grep -c "notification_type_not_allowed" src/features/runtime-platform/contracts/permissions.ts | grep -q "^[1-9]" &&
grep -c "NotificationInsertSchema" src/lib/dto/notification.ts | grep -q "^[1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - SystemCommandDiscriminatedSchema 包含 4 个 variant（http.request, config, file, notification）
    - PlatformCommandType 联合类型中包含 "system.notification.send"
    - PlatformCommandPayloadSchemas 包含 "system.notification.send" key
    - PlatformCommandSchema discriminatedUnion 包含 system.notification.send variant
    - GovernanceDeniedReasonValues 包含 notification_type_not_allowed / rate_limit_exceeded / recipient_not_in_school
    - src/lib/dto/notification.ts 文件存在并导出 NotificationInsertSchema / NotificationDTOSchema / MarkReadSchema
    - `pnpm typecheck` 通过
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| plugin→CommandBus | 插件经 Command Bus 发送通知，payload 经 Zod schema 校验后进入 handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-01 | Tampering | pluginNotifications 表 | mitigate | Drizzle ORM 类型安全 insert，payload 经 Zod strict schema 校验（max 256 title + max 2048 body） |
| T-81-02 | Elevation of Privilege | SystemCommandTypes 枚举 | mitigate | 仅 "system.notification.send" 为注册命令类型；读操作不走 Command Bus |
| T-81-03 | Information Disclosure | manifest notificationTypes 声明 | mitigate | notificationTypes 正则 ^[a-z][a-z0-9_]*(?:\.[a-z][a-z0-9_]*)*$ 防注入 |
| T-81-SC | Tampering | npm 依赖 | mitigate | 本 phase 零新依赖，所有包已在 RESEARCH.md Package Legitimacy Audit 中验证为 [OK] |
</threat_model>

<verification>
1. `pnpm typecheck` 通过（所有类型定义一致）
2. `grep -A5 "export const pluginNotifications" src/db/schema.ts` 确认表定义存在
3. `grep -A5 "system.notification.send" src/features/platform-core/commands/contracts.ts` 确认命令类型注册
4. `grep "notification_type_not_allowed\|rate_limit_exceeded\|recipient_not_in_school" src/features/runtime-platform/contracts/permissions.ts` 确认拒因码已添加
</verification>

<success_criteria>
- pluginNotifications 表在 Drizzle schema 中定义，包含所有 9 个字段和 3 个索引
- manifold 中 system.notification 可通过 Zod discriminatedUnion 解析为合法 variant
- SystemCommandTypes 包含 "system.notification.send"
- 3 个通知相关拒因码已注册
- Notification DTO schemas 文件存在并导出完整类型
- `pnpm typecheck` 零错误
</success_criteria>

<output>
Create .planning/phases/81-system-notification/81-01-SUMMARY.md when done
</output>
