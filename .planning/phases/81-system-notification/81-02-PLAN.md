---
phase: 81-system-notification
plan: 02
type: execute
wave: 2
depends_on:
  - 81-01
files_modified:
  - src/features/system-commands/handler.ts
  - src/features/system-commands/facade.ts
  - src/features/system-commands/audit.ts
  - src/features/platform-core/commands/registry.ts
  - src/lib/dal/notification.ts
  - src/app/api/notification/list/route.ts
  - src/app/api/notification/mark-read/route.ts
  - src/app/api/notification/unread-count/route.ts
  - src/features/system-commands/rate-limiter.ts
  - src/lib/cache-policy.ts
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04
  - NOTIF-05
  - NOTIF-06
  - NOTIF-07
  - SYS-06
must_haves:
  truths:
    - "插件经 dispatchSystemCommand 调用 system.notification.send 能写入通知到 pluginNotifications 表且写入 audit 记录"
    - "manifest notificationTypes 白名单生效：未声明的 notificationType 被拒绝并写 denial audit"
    - "recipientUserId 经 memberships 表校验归属 schoolId，跨校写入被拒绝"
    - "频率限制：每插件每分钟 >60 条被拒绝，每用户每小时 >30 条被拒绝"
    - "用户可通过 GET /api/notification/list 分页查看自己的通知（倒序）"
    - "用户可通过 POST /api/notification/mark-read 标记单条/全部已读"
    - "用户可通过 GET /api/notification/unread-count 获取未读计数"
    - "所有拒绝路径先写 audit 再抛 PlatformCommandExecutionError"
  artifacts:
    - path: "src/features/system-commands/handler.ts"
      provides: "notificationSendAuthorize + notificationSendExecute handler"
      contains: "system.notification.send"
    - path: "src/features/system-commands/facade.ts"
      provides: "dispatchSystemCommand 新增 system.notification.send 分支"
      contains: "notification.send"
    - path: "src/features/system-commands/audit.ts"
      provides: "writeSystemCommandAudit commandType 联合类型新增 system.notification.send"
      contains: "system.notification.send"
    - path: "src/features/platform-core/commands/registry.ts"
      provides: "platformCommandRegistry 注册 system.notification.send"
      contains: "system.notification.send"
    - path: "src/lib/dal/notification.ts"
      provides: "insertNotification / getNotifications / markNotificationRead / markAllNotificationsRead / getUnreadCount"
      contains: "export async function insertNotification"
    - path: "src/features/system-commands/rate-limiter.ts"
      provides: "Redis INCR+EXPIRE 原子化限流函数"
      contains: "checkPluginRateLimit"
    - path: "src/app/api/notification/list/route.ts"
      provides: "GET 通知列表 API"
      contains: "getNotifications"
    - path: "src/app/api/notification/mark-read/route.ts"
      provides: "POST 标记已读 API"
      contains: "markAll"
    - path: "src/app/api/notification/unread-count/route.ts"
      provides: "GET 未读计数 API"
      contains: "getUnreadCount"
    - path: "src/lib/cache-policy.ts"
      provides: "notification cache tags"
      contains: "notification"
  key_links:
    - from: "src/features/system-commands/facade.ts dispatchSystemCommand"
      to: "dispatchPlatformCommand"
      via: "envelope type: system.notification.send"
      pattern: "input.commandType.*system.notification.send"
    - from: "src/features/system-commands/handler.ts notificationSendAuthorize"
      to: "memberships 表"
      via: "db.query.memberships.findFirst"
      pattern: "eq\(memberships\.userId"
    - from: "src/features/system-commands/rate-limiter.ts checkPluginRateLimit"
      to: "Redis INCR+EXPIRE"
      via: "ioredis Lua script"
      pattern: "redis.call.*INCR.*EXPIRE"
    - from: "src/app/api/notification/list/route.ts GET handler"
      to: "DAL getNotifications"
      via: "Auth.js session → userId"
      pattern: "auth\(\)"
---

<objective>
实现 Phase 81 的 Command Bus 写入路径（system.notification.send）和用户读路径（notification API Routes）。插件经 dispatchSystemCommand facade → Command Bus → handler authorize/execute 写入通知，走完整三段式链路（manifest 声明 → governance gate → audit）。用户通过独立 API Routes 验证 Auth.js session 后直连 DAL 查询/标记/计数。

Purpose: 通知写入的安全模型（manifest 白名单 + membership 归属校验 + 频率限制 + audit）和通知读取的用户体验（列表/已读/计数）同时建立。
Output: 完整通知系统后端（Command Bus handler + DAL + API Routes + 频率限制 + audit 扩展）
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/wuxf/Develop/OpenLearn-Next/.planning/phases/81-system-notification/81-RESEARCH.md
@/home/wuxf/Develop/OpenLearn-Next/.planning/phases/81-system-notification/81-CONTEXT.md
@/home/wuxf/Develop/OpenLearn-Next/.planning/REQUIREMENTS.md

<interfaces>
**本 Plan 的关键现存代码接口（从代码库提取，执行前必须了解）：**

来自 src/features/system-commands/facade.ts:
```typescript
// dispatchSystemCommand 函数签名（lines 174-196）
export async function dispatchSystemCommand(input: {
  commandType: string;
  pluginKey: string;
  actorId: string;
  configKey?: string;
  configValue?: unknown;
  fileId?: string;
  fileMeta?: { sha256: string; fileName: string; mimeType: string; sizeBytes: number; diskPath: string; fileId: string };
})

// 需要在 input 中新增字段：
notifPayload?: { recipientUserId: string; notificationType: string; title: string; body: string }
```

来自 src/features/system-commands/handler.ts:
```typescript
// system.config.set handler 的 authorize/execute 模式（lines 938-1068）
// system.file.upload handler 的 authorize/execute 模式（lines 1326-1561）
// denySystemConfig / denySystemFile helper 模式：先 writeSystemCommandAudit 再 throw PlatformCommandExecutionError

// systemConfigSetAuthorize 签名
async function systemConfigSetAuthorize({ command }: { command: PlatformCommand }): Promise<void>
// 本 Plan 需要 mirror 此模式创建 notificationSendAuthorize

// systemConfigSetExecute 签名
async function systemConfigSetExecute({ command, attemptNumber }: { command: PlatformCommand; attemptNumber: number }): Promise<PlatformCommandExecutionResult>
// 返回 successResult({ ... })
```

来自 src/features/system-commands/audit.ts:
```typescript
// writeSystemCommandAudit 的 commandType 参数联合类型（line 18）
commandType: "system.http.request" | "system.config.set" | "system.config.get" | "system.file.upload" | "system.file.delete";
// 需要新增 "system.notification.send"
```

来自 src/features/platform-core/commands/registry.ts:
```typescript
// 现有 system.file.delete 注册模式（lines 180-188）
"system.file.delete": createPlatformCommandDefinition({
  commandType: "system.file.delete",
  payloadSchema: PlatformCommandPayloadSchemas["system.file.delete"],
  dedupe: "required",
  authorize: async (input) => { await systemFileHandler["system.file.delete"].authorize(input); },
  execute: systemFileHandler["system.file.delete"].execute,
}),
// 需要新增 system.notification.send 注册，模式与 system.file.delete 一致

// importer 需要新增 systemNotificationHandler
import { systemHttpRequestHandler, systemConfigHandler, systemFileHandler } from "@/features/system-commands/handler";
```

来自 src/db/schema.ts (81-01 已创建):
```typescript
export const pluginNotifications = sqliteTable("pluginNotification", {
  id, pluginId, schoolId, recipientUserId, notificationType, title, body, readAt, createdAt
})
```

来自 src/features/async-tasks/infra/connection.ts:
```typescript
// Redis 连接工具
export async function getBullmqProducerConnection() // 返回 ioredis Redis 实例
export function getBullmqEnvironmentCapability() // 返回 { asyncTasksEnabled, redisUrl, ... }
```

来自 src/lib/dto/notification.ts (81-01 已创建):
```typescript
export const NotificationInsertSchema // { recipientUserId, notificationType, title, body }
export const MarkReadSchema // discriminatedUnion markAll | notificationId
```

来自 src/db/schema.ts memberships 表（lines 65-84）:
```typescript
export const memberships = sqliteTable("membership", {
  id, userId, schoolId, role, status
})
// 查询: WHERE userId=? AND schoolId=? AND status='active'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 DAL + 频率限制器</name>
  <files>
src/lib/dal/notification.ts,
src/features/system-commands/rate-limiter.ts,
src/lib/cache-policy.ts
  </files>
  <read_first>
src/db/schema.ts (lines 1945+) — pluginNotifications 表定义（81-01 创建）
src/lib/dal/files.ts — cursor-based 分页 DAL 模式参考
src/lib/dto/notification.ts — NotificationInsertSchema / MarkReadSchema（81-01 创建）
src/features/async-tasks/infra/connection.ts — getBullmqProducerConnection / getBullmqEnvironmentCapability
src/lib/cache-policy.ts — 现有 cache tags 模式
  </read_first>
  <behavior>
    - Test 1: insertNotification({schoolId, pluginId, recipientUserId, notificationType, title, body}) → 插入一行到 pluginNotifications，返回插入行
    - Test 2: getNotifications({userId, cursor: null, limit: 20}) → 返回该用户最近 20 条通知，按 createdAt DESC
    - Test 3: getNotifications({userId, cursor: base64(cutoffDate)}) → 返回 cutoffDate 之前的 20 条通知
    - Test 4: markNotificationRead({userId, notificationId}) → 单条通知 readAt 变为非 null
    - Test 5: markAllNotificationsRead({userId}) → 该用户所有未读通知 readAt 变为非 null
    - Test 6: getUnreadCount({userId}) → 返回正确的未读通知数量
    - Test 7: checkPluginRateLimit(pluginId) → 首次返回 true（<60），第61次返回 false
    - Test 8: checkUserRateLimit(userId) → 首次返回 true（<30），第31次返回 false
    - Test 9: Redis 不可达时 rate limiter 返回 true（FAIL-OPEN）并记录警告日志
  </behavior>
  <action>
    **(A) src/lib/dal/notification.ts（新建文件）**
    实现五个 DAL 函数：
    1. insertNotification(input: NotificationInsertInput & { schoolId: string, pluginId: string }) — 插入通知记录，返回插入行
    2. getNotifications(input: { userId: string, cursor?: string, limit?: number }) — cursor-based 分页查询。cursor 是 base64 编码的 ISO 日期字符串（decoded 后作为 createdAt 的 lt 条件）。limit 默认 20，最大 100。返回 { items: NotificationDTO[], nextCursor: string | null }
    3. markNotificationRead(input: { userId: string, notificationId: string }) — 单条 UPDATE readAt = now() WHERE id=? AND recipientUserId=?（确保 owner 校验）
    4. markAllNotificationsRead(input: { userId: string }) — 批量 UPDATE readAt = now() WHERE recipientUserId=? AND readAt IS NULL
    5. getUnreadCount(input: { userId: string }) — SELECT COUNT(*) WHERE recipientUserId=? AND readAt IS NULL，返回 number

    cursor 分页使用 createdAt 值而非 id，对齐 pluginFiles DAL 模式。notificationId 参数经 zod string min(1) 校验。DAL 函数接受 unknown input 并 parse 后使用（遵循项目 DTO + Zod 模式）。createAt 使用整数 timestamp_ms（对齐 Drizzle mode: "timestamp_ms"）。
    返回的 NotificationDTO 中 readAt 为 number | null。

    **(B) src/features/system-commands/rate-limiter.ts（新建文件）**
    实现两个频率限制函数 + Lua script：
    1. Lua 脚本常量 RATE_LIMIT_LUA（原子化 INCR + EXPIRE）：
       ```
       local count = redis.call('INCR', KEYS[1])
       if count == 1 then redis.call('EXPIRE', KEYS[1], ARGV[1]) end
       return count
       ```
    2. checkPluginRateLimit(pluginId: string): Promise<boolean>
       - key: `notif:plugin:{pluginId}:{Math.floor(Date.now() / 60000)}`
       - TTL: 65s
       - 上限 60
       - Redis 不可达 → FAIL-OPEN（返回 true + console.warn）
    3. checkUserRateLimit(userId: string): Promise<boolean>
       - key: `notif:user:{userId}:{Math.floor(Date.now() / 3600000)}`
       - TTL: 3660s
       - 上限 30
       - Redis 不可达 → FAIL-OPEN

    使用 getBullmqProducerConnection() 获取 ioredis 连接。Lua 脚本用 `redis.eval(RATE_LIMIT_LUA, 1, key, ttl)` 执行。
    Redis 不可达的判断：try/catch 包裹整个 eval 调用，catch 中 console.warn("[notification-rate-limit] redis unavailable") + return true。

    **(C) src/lib/cache-policy.ts**
    新增 cache tag `notifications:userId:${userId}` 到 cacheTags 对象中，供 API Routes 在通知变更后 revalidate 使用。
  </action>
  <verify>
    <automated>npx vitest run src/lib/dal/notification.test.ts src/features/system-commands/rate-limiter.test.ts 2>/dev/null || echo "MISSING — 测试文件将在 TDD RED 阶段创建"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/dal/notification.ts 包含 insertNotification / getNotifications / markNotificationRead / markAllNotificationsRead / getUnreadCount 五个导出函数
    - cursor-based 分页正确：nextCursor 为 null 当 items.length <= limit
    - markNotificationRead 校验 userId 与 notification 的 recipientUserId 一致后才更新
    - src/features/system-commands/rate-limiter.ts 包含 checkPluginRateLimit 和 checkUserRateLimit 两个导出函数
    - Redis key 命名正确（notif:plugin:{id}:{minute} / notif:user:{id}:{hour}）
    - FAIL-OPEN 逻辑：Redis 不可达时返回 true + console.warn 日志
    - `pnpm typecheck` 通过
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 notification handler + facade 分支 + registry + audit 扩展</name>
  <files>
src/features/system-commands/handler.ts,
src/features/system-commands/facade.ts,
src/features/system-commands/audit.ts,
src/features/platform-core/commands/registry.ts
  </files>
  <read_first>
src/features/system-commands/handler.ts — systemConfigHandler (authorize/execute 模式, lines 938-1068) + systemFileHandler (lines 1326-1561) + denySystemConfig/denySystemFile helper 模式
src/features/system-commands/facade.ts — dispatchSystemCommand 函数（lines 174-484），system.config.set 分支模式（lines 231-293），system.file.upload 分支模式（lines 330-397）
src/features/system-commands/audit.ts — writeSystemCommandAudit 函数签名（lines 6-19）
src/features/platform-core/commands/registry.ts — 现有 registry 注册模式（lines 153-188）
src/features/platform-core/commands/contracts.ts — PlatformCommandExecutionError 构造函数签名
src/lib/dal/notification.ts — insertNotification 函数（Task 1 创建）
src/features/system-commands/rate-limiter.ts — checkPluginRateLimit / checkUserRateLimit（Task 1 创建）
  </read_first>
  <behavior>
    - Test 1: handler 无 matching manifest entry → denySystemNotification 被调用 → audit 写 "notification_type_not_allowed"
    - Test 2: handler recipientUserId 不在 schoolId 下 → denySystemNotification → audit 写 "recipient_not_in_school"
    - Test 3: handler 插件频率超限 → denySystemNotification → audit 写 "rate_limit_exceeded"
    - Test 4: handler 用户频率超限 → denySystemNotification → audit 写 "rate_limit_exceeded"
    - Test 5: handler 所有条件通过 → execute 写入 pluginNotifications + audit 写 "allowed"
    - Test 6: facade dispatchSystemCommand 收到 commandType="system.notification.send" → 构造 envelope → 调用 dispatchPlatformCommand
    - Test 7: registry 中 system.notification.send commandType 正确注册
    - Test 8: audit.ts writeSystemCommandAudit 接受 commandType="system.notification.send"
  </behavior>
  <action>
    **(A) src/features/system-commands/handler.ts**
    新增 systemNotificationHandler（放置 systemFileHandler 之后，约 line 1569 之后）：

    1. 辅助函数 resolveSystemNotificationManifestEntry(pluginId, schoolId)：
       - mirror resolveSystemConfigManifestEntry 模式
       - 查 pluginRegistrations WHERE id=? AND schoolId=?
       - PluginManifestSchema.parse(row.manifestJson)
       - filter systemCommands WHERE command === "system.notification"
       - 返回 { row, manifest, notificationEntries }

    2. 辅助函数 denySystemNotification(params: { pluginId, schoolId, commandId, actorId, actorScope, lifecycleState, correlationId, reasonCode, notificationType, commandType, recommendedRecoveryAction })：
       - mirror denySystemFile 模式
       - 先 writeSystemCommandAudit（decision: "denied"）
       - 再 throw PlatformCommandExecutionError
       - commandType 参数类型为 "system.notification.send"

    3. notificationSendAuthorize({ command }: { command: PlatformCommand })：
       - command as SystemNotificationSendCommand（type: Extract<PlatformCommand, { type: "system.notification.send" }>）
       - 步骤1：resolveSystemNotificationManifestEntry → 获取 notificationEntries
       - 如果 notificationEntries.length === 0 → deny(notification_type_not_allowed)
       - 步骤2：遍历 notificationEntries，检查 notificationTypes.includes(payload.notificationType)
         - 不匹配 → deny(notification_type_not_allowed)
       - 步骤3：查 memberships 表 WHERE userId=? AND schoolId=? AND status='active'
         - 不存在 → deny(recipient_not_in_school)
       - 步骤4：checkPluginRateLimit(pluginId) → false → deny(rate_limit_exceeded)
       - 步骤5：checkUserRateLimit(payload.recipientUserId) → false → deny(rate_limit_exceeded)
       - 全部通过 → return（void）

    4. notificationSendExecute({ command, attemptNumber })：
       - command as SystemNotificationSendCommand
       - 调用 insertNotification({ schoolId, pluginId, recipientUserId, notificationType, title, body })
       - 写 writeSystemCommandAudit（decision: "allowed", commandType: "system.notification.send"）
       - 返回 successResult({ notificationId: ... })

    5. Export systemNotificationHandler：
       ```
       export const systemNotificationHandler = {
         "system.notification.send": { authorize: notificationSendAuthorize, execute: notificationSendExecute },
       };
       ```

    **(B) src/features/system-commands/facade.ts**
    1. 在 dispatchSystemCommand input 参数中新增 notifPayload 字段：
       ```
       notifPayload?: { recipientUserId: string; notificationType: string; title: string; body: string }
       ```
    2. 在 dispatchSystemCommand 函数体中，新增 system.notification.send 分支（放在 system.file.delete 分支之后，约 line 459 之前）：
       - mirror system.config.set 分支模式
       - commandId = buildSystemCommandId({commandType: "system.notification.send", correlationId})
       - dedupeKey = buildSystemCommandDedupeKey 类似模式，使用 `${commandType}:${schoolId}:${pluginId}:${recipientUserId}:${notificationType}` hash
       - envelope 结构：type "system.notification.send", scope {schoolId, pluginId}, payload {recipientUserId, notificationType, title, body}
       - await dispatchPlatformCommand(envelope, { definitions: platformCommandRegistry, store: systemCommandStore, publicationPort: defaultInProcessPlatformEventAdapter })
       - 返回 { success: result.status === "succeeded", data: result.resultSummary, commandId, attemptNumber }

    **(C) src/features/system-commands/audit.ts**
    在 SystemCommandAuditInput.commandType 联合类型中新增 "system.notification.send"：
    ```
    commandType: "system.http.request" | "system.config.set" | "system.config.get" | "system.file.upload" | "system.file.delete" | "system.notification.send"
    ```

    **(D) src/features/platform-core/commands/registry.ts**
    1. 新增 import：
       ```
       import { systemHttpRequestHandler, systemConfigHandler, systemFileHandler, systemNotificationHandler } from "@/features/system-commands/handler";
       ```
    2. 在 "system.file.delete" 注册之后新增 system.notification.send 注册：
       ```
       "system.notification.send": createPlatformCommandDefinition({
         commandType: "system.notification.send",
         payloadSchema: PlatformCommandPayloadSchemas["system.notification.send"],
         dedupe: "required",
         authorize: async (input: { command: PlatformCommand }) => {
           await systemNotificationHandler["system.notification.send"].authorize(input);
         },
         execute: systemNotificationHandler["system.notification.send"].execute,
       }),
       ```
  </action>
  <verify>
    <automated>
grep -c "systemNotificationHandler" src/features/system-commands/handler.ts | grep -q "^[1-9]" &&
grep -c "system.notification.send" src/features/system-commands/handler.ts | grep -q "^[2-9]" &&
grep -c "notifPayload" src/features/system-commands/facade.ts | grep -q "^[1-9]" &&
grep -c "system.notification.send" src/features/system-commands/audit.ts | grep -q "^[1-9]" &&
grep -c "system.notification.send" src/features/platform-core/commands/registry.ts | grep -q "^[1-9]" &&
npx vitest run src/features/system-commands/handler.test.ts 2>/dev/null || echo "测试文件待创建"</automated>
  </verify>
  <acceptance_criteria>
    - handler.ts 包含 notificationSendAuthorize + notificationSendExecute 函数，遵循 denySystemNotification 先 audit 后 throw 模式
    - handler.ts authorize 阶段完成：manifest re-parse + notificationTypes 白名单匹配 + memberships 归属校验 + 双层频率限制
    - facade.ts dispatchSystemCommand 新增 system.notification.send 分支，构造正确 envelope 并调用 dispatchPlatformCommand
    - audit.ts writeSystemCommandAudit commandType 联合类型包含 "system.notification.send"
    - registry.ts 注册 system.notification.send commandType 且 dedupe 为 "required"
    - 所有拒绝路径先 writeSystemCommandAudit(decision: "denied") 再 throw PlatformCommandExecutionError
    - `pnpm typecheck` 通过
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 创建用户端 notification API Routes</name>
  <files>
src/app/api/notification/list/route.ts,
src/app/api/notification/mark-read/route.ts,
src/app/api/notification/unread-count/route.ts
  </files>
  <read_first>
src/app/api/system/file/download/route.ts — Phase 80 API Route 模式参考（auth() + DAL 直调）
src/lib/dal/notification.ts — getNotifications / markNotificationRead / markAllNotificationsRead / getUnreadCount（Task 1 创建）
src/lib/dto/notification.ts — MarkReadSchema（81-01 创建）
src/features/system-commands/handler.ts — systemConfigGetAuthorize 的 facade 直调模式（line 1079-1149）
  </read_first>
  <behavior>
    - Test 1: GET /api/notification/list (无 auth) → 401 Unauthorized
    - Test 2: GET /api/notification/list (有 session) → 200 + { items: [...], nextCursor: ... }
    - Test 3: GET /api/notification/list?cursor=xxx&limit=10 → 200 应用 cursor 和 limit
    - Test 4: POST /api/notification/mark-read { notificationId: "xxx" } → 200 标记单条已读
    - Test 5: POST /api/notification/mark-read { markAll: true } → 200 批量标记已读
    - Test 6: POST /api/notification/mark-read (无 auth) → 401
    - Test 7: GET /api/notification/unread-count (有 session) → 200 + { count: N }
    - Test 8: POST /api/notification/mark-read { notificationId: "other-user-notif" } → DAL 层不应更新不属于该用户的 notification
  </behavior>
  <action>
    **(A) src/app/api/notification/list/route.ts（新建 + GET handler）**
    1. export async function GET(request: NextRequest)
    2. const session = await auth() — 获取 Auth.js session
    3. if (!session?.user?.id) → NextResponse.json({ error: "Unauthorized" }, { status: 401 })
    4. 解析 searchParams：cursor (string|undefined), limit (parseInt 默认 20, Math.min 最大 100)
    5. 调用 getNotifications({ userId: session.user.id, cursor, limit })
    6. 返回 NextResponse.json(result) — result 结构 { items: NotificationDTO[], nextCursor: string | null }

    **(B) src/app/api/notification/mark-read/route.ts（新建 + POST handler）**
    1. export async function POST(request: NextRequest)
    2. const session = await auth() — 401 如果未认证
    3. 从 request body 解析 JSON，用 MarkReadSchema.parse() 校验
    4. 如果 markAll === true → markAllNotificationsRead({ userId: session.user.id })
    5. 否则 → markNotificationRead({ userId: session.user.id, notificationId: ... })
    6. 返回 NextResponse.json({ success: true })
    7. Zod 校验失败 → NextResponse.json({ error: "Invalid request" }, { status: 400 })
    8. MarkReadSchema 已在 81-01 中定义：z.discriminatedUnion("markAll", [
         z.strictObject({ markAll: z.literal(true) }),
         z.strictObject({ notificationId: z.string().min(1) }),
       ])

    **(C) src/app/api/notification/unread-count/route.ts（新建 + GET handler）**
    1. export async function GET(_request: NextRequest)
    2. const session = await auth() — 401 如果未认证
    3. 调用 getUnreadCount({ userId: session.user.id })
    4. 返回 NextResponse.json({ count })
    5. 设置 Cache-Control: no-store（不缓存未读计数）

    所有三个路由使用 "server-only" 导入。userId 从 auth() session 派生，绝不从 query param/body 获取（per 安全要求 V2/V4）。
    不要在这些 API Routes 中创建 Command Bus envelope —— 它们是纯读/写 DAL 操作。
  </action>
  <verify>
    <automated>npx vitest run src/app/api/notification/list/route.test.ts src/app/api/notification/mark-read/route.test.ts src/app/api/notification/unread-count/route.test.ts 2>/dev/null || echo "MISSING — TDD RED 阶段创建测试文件"</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/notification/list 返回用户通知列表（分页、倒序），未认证返回 401
    - POST /api/notification/mark-read 支持单条已读（notificationId）和全部已读（markAll: true）
    - GET /api/notification/unread-count 返回正确的未读计数（非负整数）
    - 所有 userId 从 auth() session.user.id 派生，不从请求参数获取
    - 所有三个文件存在且包含正确的 route handler 导出
    - `pnpm typecheck` 通过
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| plugin→CommandBus | 插件经 dispatchSystemCommand → Command Bus → handler.authorize 写入通知 |
| client→API Route | 用户经 Auth.js session 认证后访问通知读 API |
| handler→DB (write) | handler.execute 写入 pluginNotifications 表 |
| handler→Redis | 频率限制器读写 Redis 计数器 |
| API Route→DAL | API Route 经 session userId 调用 DAL 函数 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-04 | Elevation of Privilege | handler.notificationSendAuthorize | mitigate | manifest notificationTypes 白名单匹配：未声明类型 → deny(notification_type_not_allowed) + audit |
| T-81-05 | Information Disclosure | handler.notificationSendAuthorize | mitigate | memberships 表校验 recipientUserId 归属 schoolId：跨校 → deny(recipient_not_in_school) + audit |
| T-81-06 | Denial of Service | handler.notificationSendAuthorize | mitigate | 双层频率限制：每插件 60/min + 每用户 30/hr（Redis INCR+EXPIRE Lua 原子化） |
| T-81-07 | Denial of Service | rate-limiter.ts | accept | Redis 不可达时 FAIL-OPEN（允许发送 + 告警日志）。风险受控：通知系统非计费功能，短期放宽不影响安全边界 |
| T-81-08 | Spoofing | API Routes (list/mark-read/unread-count) | mitigate | Auth.js auth() wrapper 验证 session；userId 仅从 session.user.id 派生 |
| T-81-09 | Tampering | API Route mark-read | mitigate | markNotificationRead DAL 函数校验 notificationId 对应的 recipientUserId === userId 后才更新 readAt |
| T-81-10 | Information Disclosure | API Route list | mitigate | getNotifications 查询仅 WHERE recipientUserId = session.user.id |
| T-81-11 | Tampering | facade.ts envelope construction | mitigate | schoolId 由 assertActionExecutable 治理门派生注入（Phase 79 不变式 T-79-04），facade 不接受 schoolId 参数 |
| T-81-SC | Tampering | npm 依赖 | mitigate | 本 plan 零新依赖；所有包已在 RESEARCH.md Package Legitimacy Audit 中验证为 [OK] |
</threat_model>

<verification>
1. `pnpm typecheck` 通过（所有类型定义一致、import 路径正确）
2. `grep -A20 "system.notification.send" src/features/system-commands/handler.ts` 确认 handler 实现存在
3. `grep "notifPayload" src/features/system-commands/facade.ts` 确认 facade 分支存在
4. `grep "system.notification.send" src/features/system-commands/audit.ts` 确认 audit commandType 已扩展
5. `grep "system.notification.send" src/features/platform-core/commands/registry.ts` 确认 registry 已注册
6. `ls -la src/app/api/notification/list/route.ts src/app/api/notification/mark-read/route.ts src/app/api/notification/unread-count/route.ts` 确认 API Route 文件存在
7. 拒绝路径验证：确认每个 denySystemNotification 调用发生在 PlatformCommandExecutionError throw 之前
</verification>

<success_criteria>
- dispatchSystemCommand 接受 system.notification.send 命令类型并正确派发到 Command Bus
- pluginNotifications 表中可写入通知记录（经 handler.execute）
- manifest notificationTypes 白名单生效：白名单外类型被拒绝并写 audit
- memberships 归属校验生效：跨校 recipient 被拒绝
- 频率限制生效：超限被拒绝（Redis 可用时），Redis 不可用时 FAIL-OPEN
- GET /api/notification/list 返回分页通知列表
- POST /api/notification/mark-read 支持单条/全部已读
- GET /api/notification/unread-count 返回未读计数
- 所有拒绝路径先写 system command audit 再抛错
- `pnpm typecheck` 零错误
</success_criteria>

<output>
Create .planning/phases/81-system-notification/81-02-SUMMARY.md when done
</output>
