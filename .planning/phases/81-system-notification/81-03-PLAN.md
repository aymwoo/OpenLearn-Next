---
phase: 81-system-notification
plan: 03
type: execute
wave: 3
depends_on:
  - 81-02
files_modified:
  - src/components/notification/notification-bell.tsx
  - src/components/notification/notification-dropdown.tsx
  - src/components/notification/notification-item.tsx
  - src/components/notification/notification-list.tsx
  - src/components/notification/empty-state.tsx
  - src/app/notifications/page.tsx
  - src/hooks/use-unread-count.ts
  - src/hooks/use-notifications.ts
  - src/lib/relative-time.ts
  - src/server/workers/notification-cleanup.ts
  - src/features/async-tasks/worker/bootstrap.ts
  - src/db/schema.ts
autonomous: true
requirements:
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04
  - NOTIF-08
must_haves:
  truths:
    - "顶部导航栏 Bell icon 显示未读计数 badge，有未读时 icon 变 primary 色"
    - "点击 Bell icon 展开 dropdown，显示最近 5 条通知预览 + 查看全部链接"
    - "/notifications 页面显示完整通知列表，支持分页（加载更多）"
    - "/notifications 页面支持单条标记已读 + 全部标为已读"
    - "未读计数每 10s 轮询 GET /api/notification/unread-count"
    - "超过 90 天的已读通知每天凌晨 3:00 自动删除"
    - "通知时间显示为'刚刚'/'N分钟前'/'N小时前'/'N天前'格式"
  artifacts:
    - path: "src/components/notification/notification-bell.tsx"
      provides: "Bell icon + badge + dropdown trigger 组件"
      contains: "useUnreadCount"
    - path: "src/components/notification/notification-dropdown.tsx"
      provides: "通知 dropdown 面板（最近 5 条）"
      contains: "查看全部"
    - path: "src/components/notification/notification-item.tsx"
      provides: "单条通知行组件（复用于 dropdown + page）"
      contains: "relativeTime"
    - path: "src/components/notification/notification-list.tsx"
      provides: "带分页的通知列表组件"
      contains: "加载更多"
    - path: "src/app/notifications/page.tsx"
      provides: "/notifications 通知中心页面"
      contains: "通知中心"
    - path: "src/hooks/use-unread-count.ts"
      provides: "SWR-based 未读计数轮询 hook"
      contains: "useSWR"
    - path: "src/hooks/use-notifications.ts"
      provides: "cursor-based 通知列表 fetcher hook"
      contains: "getNotifications"
    - path: "src/lib/relative-time.ts"
      provides: "相对时间格式化工具函数"
      contains: "刚刚"
    - path: "src/server/workers/notification-cleanup.ts"
      provides: "BullMQ 通知清理 job scheduler + processor"
      contains: "upsertJobScheduler"
    - path: "src/features/async-tasks/worker/bootstrap.ts"
      provides: "在 worker start() 中注册通知清理 scheduler"
      contains: "notification-cleanup"
  key_links:
    - from: "src/components/notification/notification-bell.tsx"
      to: "/api/notification/unread-count"
      via: "useUnreadCount hook 10s polling"
      pattern: "fetch.*api/notification/unread-count"
    - from: "src/app/notifications/page.tsx"
      to: "/api/notification/list"
      via: "useNotifications hook"
      pattern: "fetch.*api/notification/list"
    - from: "src/server/workers/notification-cleanup.ts BullMQ scheduler"
      to: "pluginNotifications 表"
      via: "Drizzle DELETE query"
      pattern: "delete.*pluginNotifications"
---

<objective>
完成 Phase 81 的用户界面（Bell icon + badge + dropdown + /notifications 通知中心页面）+ BullMQ 通知清理 worker + 数据库 schema push。实现完整的通知读写闭环体验。

Purpose: 用户可看到通知、标记已读、查看历史；系统自动清理过期已读通知。UI 严格遵循 81-UI-SPEC.md 设计合同。
Output: 完整通知 UI 组件套件 + 通知清理 worker + DB schema 同步
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/wuxf/Develop/OpenLearn-Next/.planning/phases/81-system-notification/81-UI-SPEC.md
@/home/wuxf/Develop/OpenLearn-Next/.planning/phases/81-system-notification/81-RESEARCH.md
@/home/wuxf/Develop/OpenLearn-Next/.planning/phases/81-system-notification/81-CONTEXT.md
@/home/wuxf/Develop/OpenLearn-Next/.planning/REQUIREMENTS.md

<interfaces>
**UI 设计合同中提取的关键规范（81-UI-SPEC.md）：**

**Bell Icon + Badge 状态（Interaction Contract lines 133-143）:**
- Loading: 骨架 pulse on badge area
- No unread: Bell icon `on-surface-variant`, no badge
- Has unread (1-9): Bell icon `primary`, badge pill `primary` bg with count
- Has unread (10+): "9+" overflow capped
- Polling: 每 10s GET /api/notification/unread-count
- Tooltip: "通知"

**Dropdown Panel 规格（lines 146-166）:**
- Width: 360px, Max height: 480px
- Position: Right-aligned below bell, 8px gap
- Items: 最近 5 条（倒序）
- Footer: "查看全部" link → /notifications
- 点击 item → mark as read + dismiss dropdown

**/notifications Page 规格（lines 169-188）:**
- Route: /notifications（无 route group wrapper）
- Layout: 中心 content, max-width 680px
- Header: "通知中心" heading + "全部标为已读" button
- List: cursor-based, 20 per page
- "加载更多" button（infinite scroll pattern）
- 空状态: "暂无通知" + "系统通知将在此处显示"

**Copywriting (lines 104-120):**
- "标记已读", "查看全部", "暂无通知", "系统通知将在此处显示"
- "加载失败", "通知暂时无法加载，请稍后重试"
- "全部标为已读", "最近通知", "通知中心"
- 相对时间: "刚刚" / "N分钟前" / "N小时前" / "N天前"

**Color Tokens (lines 76-99):**
- Bell default: on-surface-variant (#595c5e)
- Bell has unread: primary (#0050d4)
- Badge: primary bg + on-primary text
- Dropdown: surface-container-lowest + shadow-lg + backdrop-blur (no border)
- Unread item: 8px primary dot at left
- Timer label: on-surface-variant

**Spacing:**
- Dropdown gap: 8px, item min-height: 56px, padding: md (16px)
- Badge dot: 8x8 absolute positioned

**现有代码接口：**
```typescript
// 来自 src/app/(teacher)/teacher/layout.tsx (line 12, 63)
import { Bell, Search, CalendarDays } from "lucide-react";
// 当前 Bell 是静态图标 <Bell className="size-5" />，需要替换为 NotificationBell 组件

// 来自 src/features/async-tasks/worker/bootstrap.ts (lines 115-127)
// startDueDispatchSweepLoop 模式：在 start() 中注册扫尾循环
private startDueDispatchSweepLoop() {
  void enqueueDueScheduleReminderDispatches().catch(...);
  setInterval(runSweep, 5_000);
}
// 通知清理应在 start() 中类似注册（但使用 upsertJobScheduler 而非 setInterval）

// 来自 src/db/schema.ts
export const pluginNotifications // 81-01 创建的表

// 来自 src/server/workers/async-task-worker.ts
import { startAsyncTaskWorker } from "@/features/async-tasks/worker/bootstrap";
// 通知清理是 worker 的一部分，不需要独立进程

// API Routes（81-02 创建）:
// GET /api/notification/list?cursor=&limit=20
// POST /api/notification/mark-read { notificationId } | { markAll: true }
// GET /api/notification/unread-count → { count: number }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建前端组件 + hooks + relative-time 工具</name>
  <files>
src/lib/relative-time.ts,
src/hooks/use-unread-count.ts,
src/hooks/use-notifications.ts,
src/components/notification/notification-item.tsx,
src/components/notification/notification-list.tsx,
src/components/notification/empty-state.tsx,
src/components/notification/notification-dropdown.tsx,
src/components/notification/notification-bell.tsx,
src/app/notifications/page.tsx
  </files>
  <read_first>
src/app/(teacher)/teacher/layout.tsx — 现有 Bell icon 位置和样式（line 12, 63），需要替换
DESIGN.md — 项目设计系统规范（Lexend 字体、surface 层叠、无 1px border、gradient CTA）
81-UI-SPEC.md — 完整 UI 设计合同（color tokens, typography, spacing, copywriting, interaction contract）
src/components/ui/badge.tsx — 现有 Badge 组件
src/components/ui/button.tsx — 现有 Button 组件
src/components/ui/skeleton.tsx — 现有 Skeleton 组件
src/components/ui/card.tsx — 现有 Card 组件
src/lib/utils.ts — cn() class name 合并工具
  </read_first>
  <action>
    **(A) src/lib/relative-time.ts（新建文件）**
    实现函数 formatRelativeTime(date: Date | number | string): string
    - < 60s → "刚刚"
    - 1-59 min → "N分钟前"
    - 1-23 hr → "N小时前"
    - 1-30 days → "N天前"
    - > 30 days → 返回 YYYY-MM-DD 格式（new Date().toISOString().slice(0, 10)）
    - 输入 number 类型视为毫秒时间戳（timestamp_ms，对齐 Drizzle mode）
    - 未来时间 → "刚刚"
    - 导出为命名导出

    **(B) src/hooks/use-unread-count.ts（新建文件）**
    使用 SWR（项目已有）实现轮询 hook：
    - fetcher: () => fetch("/api/notification/unread-count").then(r => r.json()).then(d => d.count as number)
    - refreshInterval: 10000（10s 轮询，per CONTEXT.md Claude's Discretion）
    - 不可见时暂停：监听 document.visibilitychange 事件，hidden 时暂停 SWR，visible 时恢复
    - 导出 useUnreadCount(): { count: number | undefined, isLoading: boolean, error: Error | undefined }

    **(C) src/hooks/use-notifications.ts（新建文件）**
    实现 cursor-based 通知列表 hook：
    - fetcher: (cursor?: string) → fetch(`/api/notification/list?limit=20${cursor ? `&cursor=${cursor}` : ''}`).then(r => r.json())
    - 状态：items: NotificationDTO[], nextCursor: string | null, isLoading: boolean
    - loadMore(): 用 nextCursor 加载下一页并 append 到 items
    - 导出 useNotifications(): { items, nextCursor, isLoading, loadMore }

    **(D) src/components/notification/notification-item.tsx（新建文件）**
    单条通知行组件：
    - Props: { notification: { id, title, body, notificationType, readAt, createdAt }, onMarkRead?: (id: string) => void }
    - 布局: min-height 56px, padding md (16px)
    - 未读状态：readAt === null → 左侧 8x8 primary dot、背景微亮
    - 已读状态：左侧无 dot、正常背景
    - 点击：调用 onMarkRead?.(id)（下拉中标记已读并关闭；页面中标记已读）
    - 时间：notification-item 右侧显示 formatRelativeTime(createdAt)
    - 标题：text-base font-lexend 400, line-clamp-2
    - 正文：text-sm font-lexend 400, line-clamp-1（下拉）/ line-clamp-2（页面）

    **(E) src/components/notification/notification-list.tsx（新建文件）**
    通知列表组件（用于 /notifications 页面）：
    - 使用 useNotifications hook
    - 渲染 notification-item 列表
    - 底部 "加载更多" Button variant="secondary"（仅当 nextCursor 不为 null 时显示）
    - loading 状态：3 个 Skeleton 条
    - 空状态：EmptyState 组件（"暂无通知" / "系统通知将在此处显示"）

    **(F) src/components/notification/empty-state.tsx（新建文件）**
    空状态组件：
    - Props: { title: string, body: string, icon?: ReactNode }
    - 默认 icon: Inbox (lucide-react)
    - 居中布局，flex flex-col items-center gap-2
    - title: text-lg font-semibold, body: text-sm text-on-surface-variant

    **(G) src/components/notification/notification-dropdown.tsx（新建文件）**
    Dropdown 面板组件：
    - Props: { isOpen, onClose, notifications: NotificationDTO[], onMarkRead }
    - 使用 shadcn Card 包裹，width 360px, max-height 480px overflow-y-auto
    - glassmorphism: bg-surface-container-lowest, shadow-lg, backdrop-blur
    - 标题行："最近通知"（text-sm font-semibold）
    - 列表：最近 5 条通知，每行 notification-item
    - Footer: "查看全部" link → /notifications（用 next/link）
    - 关闭：点击外部（useRef + useEffect mousedown listener）、Escape 键
    - 空 dropdown：显示 EmptyState 简化版
    - 过渡动画: scale + fade in, origin top-right

    **(H) src/components/notification/notification-bell.tsx（新建文件）**
    Bell icon + badge 组件：
    - 使用 useUnreadCount hook 获取未读计数
    - Bell default: on-surface-variant, has unread (> 0): primary color
    - Badge: 仅当 count > 0 时显示, primary bg pill, 16px height
      - 1-9: 显示实际数字
      - 10+: 显示 "9+"
    - Tooltip: "通知"
    - onClick: toggle dropdown（控制 notification-dropdown 的 isOpen）
    - 渲染 NotificationDropdown 作为子组件

    **(I) src/app/notifications/page.tsx（新建文件）**
    /notifications 页面：
    - RSC 页面（Server Component）
    - 使用 NotificationList 客户端组件
    - Layout: max-w-[680px] mx-auto px-xl py-lg
    - Header: "通知中心" heading (text-xl font-semibold font-lexend) + "全部标为已读" button（secondary variant, CheckCheck icon）
      - "全部标为已读" 点击 → POST /api/notification/mark-read { markAll: true } → mutate 列表
    - metadata title: "通知中心"

    **(J) 替换 teacher layout 中的 Bell icon**
    在 src/app/(teacher)/teacher/layout.tsx 中，将 line 63 的 `<Bell className="size-5" />` 替换为 `<NotificationBell />`。
    需要 import NotificationBell from components。不需要 student layout 修改（CONTEXT.md 未提及；若 student layout 无 Bell icon，则仅在 teacher layout 替换）。

    所有组件使用 Lexend 字体（DESIGN.md 要求）、cn() 合并 class、使用表面分层而非 1px border。
    遵循 81-UI-SPEC.md 中的 color tokens 和 spacing 值。
    不要使用 fenced code blocks 在 action 中。
  </action>
  <verify>
    <automated>
ls src/components/notification/notification-bell.tsx src/components/notification/notification-dropdown.tsx src/components/notification/notification-item.tsx src/components/notification/notification-list.tsx src/components/notification/empty-state.tsx src/app/notifications/page.tsx src/hooks/use-unread-count.ts src/hooks/use-notifications.ts src/lib/relative-time.ts &&
grep -c "NotificationBell" src/app/\(teacher\)/teacher/layout.tsx | grep -q "^[1-9]" &&
grep -c "通知中心" src/app/notifications/page.tsx | grep -q "^[1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - 所有 6 个 UI 组件文件存在（bell, dropdown, item, list, empty-state, notifications/page）
    - 2 个 hooks 文件存在（use-unread-count, use-notifications）
    - relative-time.ts 工具函数存在并正确处理所有时间范围
    - teacher layout 中 Bell icon 已被替换为 NotificationBell 组件
    - useUnreadCount 使用 SWR + 10s refreshInterval + Page Visibility API 暂停
    - useNotifications 支持 cursor-based loadMore
    - Bell icon 正确显示未读计数 badge（0 隐藏，1-9 显示数字，10+ 显示 "9+"）
    - Dropdown 显示最近 5 条通知 + "查看全部" link
    - /notifications 页面 header 有 "通知中心" + "全部标为已读" button
    - 所有 copywriting 与 81-UI-SPEC.md 一致
    - `pnpm typecheck` 通过
    - `pnpm build` 通过（Next.js 编译成功）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: BullMQ 通知清理 worker</name>
  <files>
src/server/workers/notification-cleanup.ts,
src/features/async-tasks/worker/bootstrap.ts
  </files>
  <read_first>
src/features/async-tasks/worker/bootstrap.ts — AsyncTaskWorkerRuntime 类，start() 方法（lines 146-187），startDueDispatchSweepLoop 模式（lines 115-127）
src/features/async-tasks/infra/connection.ts — getBullmqProducerConnection, getBullmqEnvironmentCapability
src/features/async-tasks/infra/bullmq.ts — createAsyncTaskWorker, getAsyncTaskQueueNames, Queue/Worker 创建模式
src/db/schema.ts — pluginNotifications 表定义（81-01 创建）
node_modules/bullmq/dist/esm/classes/queue.d.ts line 193 — upsertJobScheduler API 类型签名
  </read_first>
  <action>
    **(A) src/server/workers/notification-cleanup.ts（新建文件）**
    1. 导入 Queue from "bullmq"
    2. 导出 async function registerNotificationCleanupScheduler()：
       - 检查 capability.asyncTasksEnabled，未启用 → console.warn + return
       - 创建 Queue("notification-cleanup", { connection: await getBullmqProducerConnection(), prefix: capability.prefix })
       - 调用 queue.upsertJobScheduler("cleanup-notifications", { pattern: "0 0 3 * * *" }, { name: "notification.cleanup", data: {}, opts: { removeOnComplete: true, removeOnFail: 10 } })
       - 使用固定的 jobSchedulerId "cleanup-notifications"（per D-02, per RESEARCH.md）
       - 不要 import-time 创建 Queue（per Pitfall 4）

    3. 导出 async function processNotificationCleanup(job: Job)：
       - 计算 cutoff 时间：Date.now() - 90 * 24 * 60 * 60 * 1000
       - DELETE FROM pluginNotifications WHERE readAt IS NOT NULL AND createdAt < new Date(cutoff)
       - 使用 db.delete(pluginNotifications).where(and(isNotNull(pluginNotifications.readAt), lt(pluginNotifications.createdAt, new Date(cutoff)))).returning({ deletedId: pluginNotifications.id })
       - 返回 { deletedCount: result.length }
       - console.log(`[notification-cleanup] cleaned ${result.length} read notifications older than 90 days`)

    **(B) src/features/async-tasks/worker/bootstrap.ts**
    1. 在文件顶部新增 import：
       ```
       import { registerNotificationCleanupScheduler } from "@/server/workers/notification-cleanup";
       ```
       （注意：bootstrap.ts 在 features/ 目录下，cleanup 在 server/workers/ 下）

    2. 在 AsyncTaskWorkerRuntime.start() 方法中，startDueDispatchSweepLoop() 调用之后（约 line 184），
       新增调用：
       ```
       registerNotificationCleanupScheduler().catch((error) => {
         console.error("[async-task-worker] notification cleanup scheduler failed to register", error);
       });
       ```

    3. Per RESEARCH.md Pitfall 4，不创建独立 Queue for processNotificationCleanup。BullMQ upsertJobScheduler 注册的 jobScheduler 自动在对应的 Queue 中调度 job。

    如果需要 worker processor 来处理 notification.cleanup job，不在 bootstrap.ts 中显式注册（by design：upsertJobScheduler 的 job 会入队到 "notification-cleanup" queue）。若项目 BullMQ 架构要求显式创建 worker processor，则在 bootstrap.ts 中创建：
    ```
    const cleanupWorker = new Worker("notification-cleanup", processNotificationCleanup, { connection, prefix });
    ```
    并添加到 this.workers Map 中进行生命周期管理。
  </action>
  <verify>
    <automated>
grep -c "registerNotificationCleanupScheduler" src/features/async-tasks/worker/bootstrap.ts | grep -q "^[1-9]" &&
grep -c "upsertJobScheduler" src/server/workers/notification-cleanup.ts | grep -q "^[1-9]" &&
grep -c "cleanup-notifications" src/server/workers/notification-cleanup.ts | grep -q "^[1-9]" &&
grep -c "DELETE\|delete.*pluginNotifications" src/server/workers/notification-cleanup.ts | grep -q "^[1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - notification-cleanup.ts 包含 registerNotificationCleanupScheduler 和 processNotificationCleanup 两个导出函数
    - upsertJobScheduler 使用 cron pattern "0 0 3 * * *"（每天凌晨 3:00）
    - jobSchedulerId 固定为 "cleanup-notifications"
    - processNotificationCleanup 的 DELETE 语句只删除 readAt IS NOT NULL AND createdAt < 90 days 的通知
    - bootstrap.ts 已在 start() 方法中调用 registerNotificationCleanupScheduler
    - 未读通知（readAt IS NULL）不受影响（保留 90 天以上的未读通知）
    - `pnpm typecheck` 通过
  </acceptance_criteria>
</task>

<task type="auto">
  <name>[BLOCKING] Task 3: 数据库 Schema Push</name>
  <files>src/db/schema.ts</files>
  <read_first>
src/db/schema.ts — pluginNotifications 表定义（81-01 创建，确认表结构 + 索引 + FK 引用）
  </read_first>
  <action>
    执行 Drizzle schema push 命令将 pluginNotifications 表同步到 SQLite 数据库：

    ```bash
    npx drizzle-kit push
    ```

    这会在本地 SQLite 数据库中创建 pluginNotifications 表及其索引。

    验证命令（push 完成后执行）：
    ```bash
    sqlite3 local.db ".schema pluginNotification" 2>/dev/null || sqlite3 data/local.db ".schema pluginNotification" 2>/dev/null || echo "Manually verify table exists in SQLite"
    ```

    此 task 是 BLOCKING 级别 — 所有 schema 修改完成后必须执行，否则后续验证会失败（类型检查通过但数据库表不存在）。
  </action>
  <verify>
    <automated>npx drizzle-kit push 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx drizzle-kit push` 执行成功（无错误退出）
    - pluginNotifications 表在 SQLite 数据库中创建
    - 三个索引已创建（可通过 sqlite3 .schema 命令验证）
    - `pnpm typecheck` 通过（不变）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| client→API | 浏览器经 Auth.js session cookie 访问通知读 API |
| worker→DB | BullMQ worker 进程直连 Drizzle → SQLite 删除过期通知 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-12 | Tampering | notification-bell.tsx unread count badge | mitigate | count 值来自 Auth.js 认证的 API 响应，前端不做信任假设；SWR 数据为 number 类型验证 |
| T-81-13 | Spoofing | notifications/page.tsx mark-all-read | mitigate | "全部标为已读" button 发送 POST 经 Auth.js session 认证，DAL 校验 userId |
| T-81-14 | Information Disclosure | notification-dropdown.tsx 列表 | mitigate | dropdown 仅渲染 API 返回的数据（API 已按 session.user.id 过滤），无客户端数据泄漏通道 |
| T-81-15 | Denial of Service | notification-cleanup.ts cron job | mitigate | DELETE 语句有条件限制（readAt IS NOT NULL AND createdAt < 90 days）；cron 每天仅一次；不扫描全表（有 pluginNotif_cleanup_idx 索引支持） |
| T-81-16 | Elevation of Privilege | notification-cleanup.ts DB access | mitigate | worker processor 仅执行 DELETE 操作，不通过用户输入参数化；cutoff 时间由服务端计算 |
| T-81-17 | Tampering | relative-time.ts 时间格式化 | accept | 纯展示层工具函数，不参与任何安全决策。XSS 风险受控（React JSX 自动转义文本输出） |
| T-81-SC | Tampering | npm 依赖 | mitigate | 本 plan 零新依赖；所有包已在 RESEARCH.md 验证为 [OK] |
</threat_model>

<verification>
1. `ls -la src/components/notification/*.tsx src/app/notifications/page.tsx src/hooks/use-*.ts src/lib/relative-time.ts` 确认所有 UI 文件存在
2. `pnpm build` 编译通过（Next.js 完整构建）
3. `npx drizzle-kit push` 输出确认数据库表已创建
4. `grep "registerNotificationCleanupScheduler" src/features/async-tasks/worker/bootstrap.ts` 确认 worker 注册
5. `grep "NotificationBell" src/app/\(teacher\)/teacher/layout.tsx` 确认 Bell icon 已替换
</verification>

<success_criteria>
- Teacher layout 顶部 navigator 中 Bell icon 显示实时未读计数 badge
- 点击 Bell icon 展开 360px dropdown 显示最近 5 条通知 + "查看全部" link
- /notifications 页面正确显示通知列表 + 分页加载 + 已读管理
- 未读计数每 10s 自动轮询更新
- 相对时间显示正确（刚刚 / N分钟前 / N小时前 / N天前 / 日期）
- BullMQ notification-cleanup job scheduler 已注册（cron: 每天 3:00 AM）
- cleanup processor 仅删除 90 天以上已读通知，保留未读通知
- `pnpm build` 通过
- `npx drizzle-kit push` 成功执行
</success_criteria>

<output>
Create .planning/phases/81-system-notification/81-03-SUMMARY.md when done
</output>
