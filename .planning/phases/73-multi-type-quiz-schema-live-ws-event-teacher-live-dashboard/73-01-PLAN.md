---
phase: 73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/quiz-sample/data-model.ts
  - src/db/schema/generated/plugin-owned/quiz.ts
  - src/db/schema/generated/plugin-owned/data-access-allowlist.ts
  - drizzle/0021_phase73_question_type.sql
  - drizzle/meta/_journal.json
  - drizzle/meta/0021_snapshot.json
  - src/lib/dto/plugin-data-model.ts
  - src/lib/dto/plugin-data-access-allowlist.ts
  - src/lib/dal/classroom.ts
  - src/lib/dal/classroom.test.ts
  - src/lib/dto/classroom.ts
  - src/actions/classroom-actions.ts
  - src/actions/classroom-actions.test.ts
  - src/lib/cache-policy.ts
  - src/components/learning/quiz-sample-step-card.tsx
autonomous: true
requirements: [QUIZ-EXT-01-A, QUIZ-EXT-01-B, QUIZ-EXT-01-C, QUIZ-EXT-01-D, QUIZ-EXT-01-E]
must_haves:
  truths:
    - "QUIZ-EXT-01-A: `plugin_owned_quiz_questions.questionType TEXT NOT NULL DEFAULT 'single_choice'` 列已落库；5 枚举 (`single_choice` | `multi_choice` | `true_false` | `fill_blank` | `ordering`) 完整；旧行默认 `single_choice`（D-01 forward + D-67 兼容）"
    - "QUIZ-EXT-01-B: `plugins/quiz-sample/data-model.ts` 是 schema 单一真相源；`plugin:compile` 重新生成 `src/db/schema/generated/plugin-owned/quiz.ts` + `data-access-allowlist.ts` 零漂移；DTO 类型化 payload union (`questionType` 派生 5 种结构)"
    - "QUIZ-EXT-01-C: `dispatchPluginDataAccess` 5 动词 × 5 题型 = 25 用例 (insert / getByIndex / count / aggregate) 走 allowlist 路径，跨 `questionType` 全部通过"
    - "QUIZ-EXT-01-D: `submitQuizSampleAnswerAction` 接受 5 题型 payload，append-only/`isLatest` 翻转跨 5 题型全成立；`updateTag(cacheTags.quizStats(sessionId))` 写入成功后失效"
    - "QUIZ-EXT-01-E: `buildQuizSampleRecapStats` 扩展 5 题型聚合 (`countByOption` 单选 / `countByOptionSet` 多选 / `countByBool` 判断 / `topAnswers` 填空 / `topOrderings` 排序)；`ClassroomSessionRecapSurface` 按 `questionType` 分组渲染"
  artifacts:
    - path: "plugins/quiz-sample/data-model.ts"
      provides: "扩展后的 quiz sample 多题型 data-model 单一真相源"
      contains: "questionType"
    - path: "drizzle/0021_phase73_question_type.sql"
      provides: "questionType 列加列迁移（additive, 老行 default single_choice）"
      contains: "ALTER TABLE plugin_owned_quiz_questions ADD COLUMN questionType"
    - path: "src/lib/dto/plugin-data-model.ts"
      provides: "questionType 字段 + 5 题型类型化 payload union"
      contains: "QuestionTypeSchema"
    - path: "src/lib/dto/plugin-data-access-allowlist.ts"
      provides: "allowlist quiz.questions / quiz.responses getByIndex 接受 questionType 索引"
      contains: "questionType"
    - path: "src/lib/dal/classroom.ts"
      provides: "5 题型 buildQuizSampleRecapStats 聚合 helper"
      contains: "countByOptionSet"
    - path: "src/lib/dto/classroom.ts"
      provides: "ClassroomSessionRecapDTOSchema 5 题型 quizSampleStats 段"
      contains: "questionType"
  key_links:
    - from: "plugins/quiz-sample/data-model.ts"
      to: "src/db/schema/generated/plugin-owned/quiz.ts"
      via: "pnpm plugin:compile codegen"
      pattern: "questionType"
    - from: "src/db/schema/generated/plugin-owned/data-access-allowlist.ts"
      to: "src/lib/dto/plugin-data-access-allowlist.ts"
      via: "编译期派生 → 受治理 allowlist 单一真相源"
      pattern: "quiz.questions"
    - from: "src/actions/classroom-actions.ts submitQuizSampleAnswerAction"
      to: "src/lib/cache-policy.ts cacheTags.quizStats(sessionId)"
      via: "updateTag 在 5 题型写入成功后失效（read-your-writes）"
      pattern: "updateTag.*quizStats"
    - from: "src/lib/dal/classroom.ts buildQuizSampleRecapStats"
      to: "src/components/classroom/classroom-session-recap-surface.tsx"
      via: "DTO schema → UI 渲染按 questionType 分组"
      pattern: "questionType"
---

<objective>
把 v4.0 `quiz` 样板的题型从「单选」扩到 5 种（`single_choice` / `multi_choice` / `true_false` / `fill_blank` / `ordering`）：data-model 单一真相源 + Drizzle 迁移 + DTO 联合 + 5 题型 append-only 写入 + 5 题型课后统计聚合 + recap surface 按 questionType 分组渲染。Plan 73-02 后续接入 `quiz.answer.received` WS 事件需要 73-01 提供的「写入成功后 dispatch hook 点」，因此本 plan 必须在 `submitQuizSampleAnswerAction` 成功路径暴露该 hook。

Purpose: 先把 data-model + DAL + write hook 定住,Plan 73-02 的 WS 事件 + dashboard tab 才能落地。
Output: 5 题型数据模型、迁移、DTO、append-only 写入、cap 5 聚合、recap surface 渲染、写入 hook 点就位。
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-CONTEXT.md
@.planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-RESEARCH.md
@.planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-UI-SPEC.md
@.planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto" gate="blocking" tdd="true">
  <name>Task 1: 扩展 quiz sample data-model 增加 questionType 列并生成 Drizzle 迁移</name>
  <read_first>
    - 修改文件：plugins/quiz-sample/data-model.ts
    - 生成产物：src/db/schema/generated/plugin-owned/quiz.ts、src/db/schema/generated/plugin-owned/data-access-allowlist.ts
    - 既有 DTO 元 schema：src/lib/dto/plugin-data-model.ts（ColumnSpecSchema only accepts text/integer/boolean/timestamp/enum）
    - codegen 脚本：scripts/compile-plugin-data-model.ts
    - 上一条 quiz sample 迁移：drizzle/00XX_phase69_quiz_sample_*.sql（作为基线）
    - 既有问题行：plugin_owned_quiz_questions 的所有现存行必须默认 single_choice
  </read_first>
  <files>plugins/quiz-sample/data-model.ts, src/db/schema/generated/plugin-owned/quiz.ts, src/db/schema/generated/plugin-owned/data-access-allowlist.ts, drizzle/0021_phase73_question_type.sql, drizzle/meta/_journal.json, drizzle/meta/0021_snapshot.json</files>
  <behavior>
    - Test: `plugin_owned_quiz_questions.questionType` 列已落库，type = text，NOT NULL，default "single_choice"
    - Test: 5 枚举 (`single_choice` | `multi_choice` | `true_false` | `fill_blank` | `ordering`) 在 data-model.ts 的 enumValues 中齐全
    - Test: 旧 question 行的 questionType 通过迁移默认 `single_choice`，forward + backward 都兼容
    - Test: `pnpm plugin:compile` 后 `src/db/schema/generated/plugin-owned/quiz.ts` 与 `data-access-allowlist.ts` 重新生成且零意外 diff
    - Test: `pnpm db:migrate` exit 0；Drizzle journal 连续
  </behavior>
  <action>
    在 `plugins/quiz-sample/data-model.ts` 的 `plugin_owned_quiz_questions` 表上追加一列：
    ```
    { name: "questionType", type: "enum", notNull: true, default: "single_choice", enumValues: ["single_choice", "multi_choice", "true_false", "fill_blank", "ordering"] }
    ```
    注意 ColumnSpecSchema 的 `default: z.union([z.string(), z.number(), z.boolean()]).optional()` 已支持字符串默认值，无需改 DTO。
    随后 `pnpm plugin:compile` 重新生成 `src/db/schema/generated/plugin-owned/quiz.ts` + `data-access-allowlist.ts`。
    手动生成 Drizzle 迁移：`pnpm exec drizzle-kit generate --name=phase73_question_type`（或手写 `drizzle/0021_phase73_question_type.sql`，包含 `ALTER TABLE plugin_owned_quiz_questions ADD COLUMN questionType TEXT NOT NULL DEFAULT 'single_choice'`，确保 SQLite 老行正确填默认值）。
    跑 `pnpm db:migrate` 让本地 dev DB 物理落地新 schema。
    不要 stage `local.db` / `local.db-shm` / `local.db-wal` / `.local/pilot-host/**`。
  </action>
  <verify>
    <automated>pnpm plugin:compile && git diff --exit-code src/db/schema/generated/plugin-owned/quiz.ts src/db/schema/generated/plugin-owned/data-access-allowlist.ts && pnpm exec drizzle-kit generate --name=phase73_question_type && pnpm db:migrate && pnpm vitest run src/db/schema/ src/lib/dto/plugin-data-model.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "questionType" plugins/quiz-sample/data-model.ts` 命中，新增列在 `plugin_owned_quiz_questions` 表定义内
    - 5 枚举值完整：`single_choice` / `multi_choice` / `true_false` / `fill_blank` / `ordering`
    - `drizzle/0021_phase73_question_type.sql` 存在并含 `ALTER TABLE plugin_owned_quiz_questions ADD COLUMN questionType`
    - 迁移 SQL 中 `DEFAULT 'single_choice'` 保证旧行不破
    - `pnpm db:migrate` exit 0
    - `src/db/schema/generated/plugin-owned/quiz.ts` 包含 `questionType` 列
    - `src/db/schema/generated/plugin-owned/data-access-allowlist.ts` 包含 `questionType` 索引入口
    - `pnpm plugin:compile` 重跑零新增 diff
    - `git status --short local.db local.db-shm local.db-wal` 不会被作为本任务产物提交
  </acceptance_criteria>
  <done>questionType 列已落库、5 枚举完整、迁移安全可前滚/后滚、codegen 零漂移。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DTO 类型化 payload union + allowlist 接受 questionType 索引</name>
  <read_first>
    - DTO 元 schema：src/lib/dto/plugin-data-model.ts、src/lib/dto/plugin-data-access-allowlist.ts
    - 现有 quiz DTO 段：ClassroomSessionRecapDTOSchema（src/lib/dto/classroom.ts）
    - 既有 quiz allowlist：`quiz.questions` / `quiz.responses` 的 getByIndex / count / aggregate 索引列
    - v4.0 64-01/69-01 的 DTO strict pattern：`.strictObject` + superRefine
  </read_first>
  <files>src/lib/dto/plugin-data-model.ts, src/lib/dto/plugin-data-access-allowlist.ts, src/lib/dto/classroom.ts</files>
  <behavior>
    - Test: `QuestionTypeSchema` 接受 5 枚举值，拒绝其他字符串
    - Test: 类型化 `QuizQuestionDTOSchema` 含 `questionType` 字段 + 5 派生子结构（`payloadSingle` / `payloadMulti` / `payloadTrueFalse` / `payloadFillBlank` / `payloadOrdering`）
    - Test: `QuizResponseDTOSchema.selectedOption` 从 enum A-D 改为 `text`，按 `questionType` 派生 5 种 decoded payload
    - Test: `plugin-data-access-allowlist.ts` 编译期派生 `quiz.questions.getByIndex` 接受 `questionType` 索引列
    - Test: `ClassroomSessionRecapDTOSchema.quizSampleStats.questions[*]` 包含 `questionType` 字段
  </behavior>
  <action>
    在 `src/lib/dto/plugin-data-model.ts` 新增 `QuestionTypeSchema = z.enum(["single_choice", "multi_choice", "true_false", "fill_blank", "ordering"])`。
    新增 `QuizQuestionDTOSchema`（基于 `plugin_owned_quiz_questions` 行 + 5 派生子 payload schema,使用 `z.discriminatedUnion("questionType", [...])`）。
    把 `plugin_owned_quiz_responses` 的 `selectedOption` 从 enum 改 `text`,新增 `QuizResponseDTOSchema` 通过 `z.discriminatedUnion("questionType", [...])` 解码 payload。
    在 `src/lib/dto/plugin-data-access-allowlist.ts` 添加 `questionType` 索引列入口,确保 `quiz.questions` / `quiz.responses` 的 `getByIndex` 可按 `questionType` 过滤(不修改只读路径,只接受索引列)。
    扩展 `ClassroomSessionRecapDTOSchema.quizSampleStats.questions[*]` 加入 `questionType` 字段和 5 题型 stats 段。
    复刻 v4.0 64-01 / 69-01 的 strict pattern: `.strictObject` 拒额外字段,enum 不变量 superRefine 校验。
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/dto/plugin-data-model.test.ts src/lib/dto/plugin-data-access-allowlist.test.ts src/lib/dto/classroom.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/dto/plugin-data-model.ts` 含 `QuestionTypeSchema` 和 5 派生 payload union
    - `src/lib/dto/plugin-data-access-allowlist.ts` 编译期派生 `quiz.questions.getByIndex` 接受 `questionType` 索引列(不修改 verbs)
    - `src/lib/dto/classroom.ts` `ClassroomSessionRecapDTOSchema.quizSampleStats.questions[*]` 包含 `questionType` 字段
    - `QuizResponseDTOSchema.selectedOption` 不再是 enum(从 `text("selectedOption", { enum: ["A", "B", "C", "D"] })` 改为 `text("selectedOption")`)
    - 所有新增 schema 用 `.strict()` / `.strictObject()` 拒额外字段
    - 既有 5 题型 DTO 测试 + recap DTO 测试全绿
  </acceptance_criteria>
  <done>DTO 类型化层完整,5 题型 payload union 通过 strict 校验,allowlist 接受 questionType 索引。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: dispatchPluginDataAccess 5 动词 × 5 题型 = 25 DAL 用例</name>
  <read_first>
    - 现有 facade 实现：src/features/runtime-platform/ 中的 dispatchPluginDataAccess
    - 既有用例：src/lib/dal/plugins.test.ts (5 动词 × 通用场景)
    - 现有 governance 路径：pluginDataAccessAllowlist 编译期派生
    - 既有 quiz 测试：src/lib/dal/classroom.test.ts
  </read_first>
  <files>src/lib/dal/classroom.test.ts, src/lib/dal/plugins.test.ts</files>
  <behavior>
    - Test: 5 动词 (insert / upsert / getByIndex / count / aggregate) × 5 题型 (single_choice / multi_choice / true_false / fill_blank / ordering) = 25 用例全过
    - Test: insert 5 题型 payload → 落库 5 行,各 `questionType` 正确
    - Test: getByIndex 按 `questionType` 过滤返回该课该题型的所有题
    - Test: count 按 `questionType` 聚合
    - Test: aggregate 按 `questionType` × payload 维度聚合(5 题型各自不同维度)
    - Test: 写动词走 Command Bus producer,读动词走 governed DAL,不直连 DB
  </behavior>
  <action>
    在 `src/lib/dal/classroom.test.ts` 增 25 用例矩阵 (5 动词 × 5 题型),每题型 payload 严格匹配 D-03 联合(`single_choice = "A"|"B"|"C"|"D"`, `multi_choice = string[]`, `true_false = boolean`, `fill_blank = string`, `ordering = string[]`)。
    复刻 v4.0 69 阶段的 pluginKey = "quiz" 走 `dispatchPluginDataAccess` facade 路径,**绝不**走 built-in registration id。
    写动词断言走 Command Bus producer;读动词断言走 governed DAL + session-derived schoolId scope。
    aggregate 投影 `{key, count}` 二元组,符合 D-05 投影规则。
    测试使用真实 libSQL + seeded 临时数据库(沿用 68/69 阶段 Option A stub auth + real DB 模式)。
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/dal/classroom.test.ts -t "multi-type quiz dal" && pnpm vitest run src/lib/dal/plugins.test.ts -t "quiz questionType"</automated>
  </verify>
  <acceptance_criteria>
    - 25 用例 0 failure 0 skip
    - 每题型的 payload 都被 `dispatchPluginDataAccess` 接受并落库
    - 写路径断言 Command Bus producer 被调用,不直连 `db`
    - 读路径断言 schoolId scope 由 session 派生
    - aggregate 投影结果仅为 `{key, count}` 二元组
    - 既有 5 动词 × 通用场景测试不破
  </acceptance_criteria>
  <done>5 题型 DAL 行为 25 用例全绿,append-only 写入路径与 v4.0 单选样板等价。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: submitQuizSampleAnswerAction 接受 5 题型 payload + append-only/isLatest 翻转 + 暴露 quiz.answer.received hook 点</name>
  <read_first>
    - 现有 action：src/actions/classroom-actions.ts submitQuizSampleAnswerAction (line 318 附近)
    - 现有 cache tag：src/lib/cache-policy.ts cacheTags.quizStats(sessionId)
    - 既有 v4.0 路径：D-72.1-04 append-only/isLatest 事务 (UPDATE isLatest = false → INSERT isLatest = true)
    - 既有 DTO：QuizResponseDTOSchema
    - Plan 73-02 依赖：写入成功后 `dispatchPlatformCommand({ type: "quiz.answer.received", payload })`
  </read_first>
  <files>src/actions/classroom-actions.ts, src/actions/classroom-actions.test.ts, src/lib/cache-policy.ts, src/components/learning/quiz-sample-step-card.tsx</files>
  <behavior>
    - Test: 5 题型 payload 各自 accept + 落库 1 行
    - Test: 同一 student + question 反复提交 → 旧 `isLatest` 翻转为 `false`,新行 `isLatest = true`(5 题型全成立)
    - Test: 写入成功后 `updateTag(cacheTags.quizStats(sessionId))` 被调用
    - Test: 写入成功后暴露 hook:返回 payload `{ type: "quiz.answer.received", payload: { questionId, studentId, responseType, payload, receivedAt, classroomSessionId } }`(由 Plan 73-02 订阅,本 plan 只暴露入口)
    - Test: 教师配置入口 `saveQuizSampleLessonStepAction` 接受 5 题型 payload
  </behavior>
  <action>
    改写 `submitQuizSampleAnswerAction`:入参 schema 扩展为 `QuizResponseDTOSchema`(支持 5 题型 discriminated union)。
    写入路径保持 D-72.1-04 事务:`UPDATE plugin_owned_quiz_responses SET isLatest = false WHERE classroomSession = ? AND student = ? AND question = ?` → `INSERT INTO plugin_owned_quiz_responses ... isLatest = true`。
    写入成功后:
    1. `updateTag(cacheTags.quizStats(parsed.data.classroomSession))` 触发 read-your-writes。
    2. 调用 `dispatchPlatformCommand({ type: "quiz.answer.received", dedupeKey: \`quiz-answer-\${sessionId}-\${studentId}-\${questionId}-\${receivedAt}\`, payload: { questionId, studentId, responseType, payload, receivedAt, classroomSessionId } })` —— **这是 Plan 73-02 订阅的 hook 点**;本 plan 不实现订阅者,只保证 hook 被调用。
    扩展 `saveQuizSampleLessonStepAction` 接受 5 题型 payload(`questionType` 字段 + 对应 payload 子集)。
    扩展 `src/components/learning/quiz-sample-step-card.tsx` 渲染 5 题型 UI(单选 radio / 多选 checkbox / 判断 true-false toggle / 填空 input / 排序 drag),调用对应 payload 形态的 `submitQuizSampleAnswerAction`。
  </action>
  <verify>
    <automated>pnpm vitest run src/actions/classroom-actions.test.ts -t "multi-type quiz submit" && pnpm vitest run src/lib/cache-policy.test.ts -t "quizStats invalidation"</automated>
  </verify>
  <acceptance_criteria>
    - 5 题型 payload 全部接受并落库,旧 `isLatest` 翻转跨 5 题型全成立
    - `updateTag(cacheTags.quizStats(sessionId))` 在写入成功后被调用(vitest 断言)
    - `dispatchPlatformCommand({ type: "quiz.answer.received", ... })` 在写入成功后被调用(Plan 73-02 依赖的 hook 入口存在)
    - `saveQuizSampleLessonStepAction` 接受 5 题型 payload
    - `quiz-sample-step-card.tsx` 渲染 5 题型 UI 并提交正确 payload 形态
    - 既有单选测试仍绿(向后兼容)
  </acceptance_criteria>
  <done>学生端 5 题型提交路径 + 缓存失效 + quiz.answer.received hook 点就位;Plan 73-02 可订阅该 hook。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: buildQuizSampleRecapStats 扩展 5 题型聚合 + ClassroomSessionRecapSurface 按 questionType 分组渲染</name>
  <read_first>
    - 现有 helper：src/lib/dal/classroom.ts buildQuizSampleRecapStats (line 890 附近)
    - 现有 surface：src/components/classroom/classroom-session-recap-surface.tsx
    - 现有 DTO：ClassroomSessionRecapQuizStatsSectionDTOSchema
    - 既有用例：src/lib/dal/classroom.test.ts -t "quiz sample stats"
  </read_first>
  <files>src/lib/dal/classroom.ts, src/lib/dal/classroom.test.ts, src/lib/dto/classroom.ts, src/components/classroom/classroom-session-recap-surface.tsx</files>
  <behavior>
    - Test: 5 题型 stats 聚合全过:`countByOption` (单选) / `countByOptionSet` (多选) / `countByBool` (判断) / `topAnswers` (填空) / `topOrderings` (排序)
    - Test: 聚合走 latest-only (`isLatest = true`),不回写核心 analytics 表
    - Test: 单 SQL `GROUP BY` 聚合源(沿用 Phase 70 决策),不逐题 fetch
    - Test: `ClassroomSessionRecapSurface` 按 `questionType` 分组渲染 5 题型 stats(vitest 表格断言 + RSC snapshot)
    - Test: `cacheTags.quizStats(sessionId)` 仍能 invalidate 新段
  </behavior>
  <action>
    改写 `buildQuizSampleRecapStats`:按 `questionType` 分支渲染 5 题型 stats:
    - `single_choice`:复用 v4.0 `countByOption` / `correctRate` / `denominatorLabel`
    - `multi_choice`:新增 `countByOptionSet`(统计各选项集合的作答数)+ `topCombinations` (top 3 集合)
    - `true_false`:新增 `countByBool`(统计 true/false 各自作答数)+ `correctRate`
    - `fill_blank`:新增 `topAnswers`(统计文本出现频次,top 10)+ `uniquenessRatio`
    - `ordering`:新增 `topOrderings`(统计选项 ID 序列出现频次,top 5)+ `correctSequenceRate`
    聚合 SQL:`GROUP BY (questionType, payload-derived-key)`,沿用 single SQL 聚合源(Phase 70 决策)。
    `ClassroomSessionRecapSurface` 按 `questionType` 分组渲染,每题型用独立的子组件 (`QuizSingleChoiceRecap` / `QuizMultiChoiceRecap` / `QuizTrueFalseRecap` / `QuizFillBlankRecap` / `QuizOrderingRecap`)。
    保持 `cacheTags.quizStats(sessionId)` 仍能通过 `updateTag` invalidate 新段。
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/dal/classroom.test.ts -t "quiz sample recap stats multi-type" && pnpm vitest run src/components/classroom/classroom-session-recap-surface.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 5 题型 stats 聚合 helper 通过测试,每个聚合都是 latest-only
    - `ClassroomSessionRecapSurface` 按 questionType 分组渲染(vitest snapshot + 表格断言)
    - 既有 v4.0 单选 `countByOption` / `correctRate` 测试不破
    - 聚合走 single SQL `GROUP BY`,不在 JS 端 N+1
    - `cacheTags.quizStats(sessionId)` 通过 `updateTag` 仍能 invalidate 新段
    - 不写任何 summary / core analytics 表
  </acceptance_criteria>
  <done>5 题型课后统计聚合与渲染全部就位,recap surface 按 questionType 分组呈现,真实源仍为 SQLite + DAL。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data-model declaration -> generated schema -> SQLite | 编译期单一真相源链 data-model.ts → codegen → migration,所有 schema 变更必须经这条链 |
| plugin-owned quiz DAL 写路径 -> 核心 SQLite | 写路径经 Command Bus producer,append-only/`isLatest` 事务保 5 题型 |
| student submit 5 题型 payload -> server action -> DB | 5 题型 discriminated union 在 DTO 边界被校验,任何非法 payload 在 Zod parse 阶段被拒 |
| recap stats 聚合 -> recap surface 渲染 | 单一 SQL `GROUP BY` 聚合,latest-only,不写新表;UI 只读 quizSampleStats DTO 段 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Tampering | 5 题型 append-only | mitigate | D-72.1-04 事务跨 5 题型;Task 4 显式 vitest 断言 5 题型 `isLatest` 翻转 |
| T-73-02 | Information Disclosure | 5 题型 payload 在事件中的暴露 | mitigate | Plan 73-02 的 `quiz.answer.received` 事件 payload 是 summary-only(D-07),不暴露 student PII;本 plan 不写事件订阅,只暴露 hook |
| T-73-03 | Elevation | 5 题型 allowlist | mitigate | DTO 边界用 `.strict()` / `.discriminatedUnion` 拒额外字段,allowlist 编译期派生 0 硬编码 |
| T-73-04 | Repudiation | 写操作审计 | mitigate | 5 题型写入走 Command Bus producer,审计通过既有 `governanceAudits` 表(D-04) |
| T-73-05 | Tampering | data-model 漂移 | mitigate | Task 1 `pnpm plugin:compile` + `git diff --exit-code` 守卫,零意外 diff |
</threat_model>

<verification>
- `pnpm plugin:compile` 零漂移
- `pnpm exec drizzle-kit generate` + `pnpm db:migrate` exit 0
- `pnpm vitest run src/db/schema/ src/lib/dto/plugin-data-model.test.ts src/lib/dto/plugin-data-access-allowlist.test.ts src/lib/dto/classroom.test.ts src/lib/dal/classroom.test.ts src/lib/dal/plugins.test.ts src/actions/classroom-actions.test.ts src/lib/cache-policy.test.ts src/components/classroom/classroom-session-recap-surface.test.tsx` 全绿
- `pnpm typecheck` exit 0
- 5 题型 DAL 25 用例全过
- 5 题型 append-only/`isLatest` 翻转全过
- 5 题型课后 stats 聚合全过
- 既有 v4.0 单选测试不破
</verification>

<success_criteria>
QUIZ-EXT-01-A 落库 / QUIZ-EXT-01-B DTO 联合 / QUIZ-EXT-01-C 25 DAL 用例 / QUIZ-EXT-01-D 5 题型 append-only + hook / QUIZ-EXT-01-E 5 题型 recap stats 全部就位。Plan 73-02 可订阅 `quiz.answer.received` hook 接入 WS 通路。
</success_criteria>

<output>
完成后创建 `.planning/phases/73-multi-type-quiz-schema-live-ws-event-teacher-live-dashboard/73-01-SUMMARY.md`
</output>
