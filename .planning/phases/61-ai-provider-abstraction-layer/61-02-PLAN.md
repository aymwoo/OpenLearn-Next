---
phase: 61-ai-provider-abstraction-layer
plan: 02
type: execute
wave: 2
depends_on: [00, 01]
files_modified:
  - src/server/ai/providers/redis-client.ts
  - src/server/ai/providers/rate-limit.ts
  - src/server/ai/providers/rate-limit.test.ts
autonomous: true
requirements: [PROV-03]

must_haves:
  truths:
    - "教师维度与全局维度双层固定窗口限流，任一超限即抛 ProviderRateLimitError"
    - "ProviderRateLimitError 带 retryAfter（= key 剩余 TTL）+ 中文消息"
    - "Redis 不可达时 fail-closed：拒绝调用并抛可读错误，绝不放行"
    - "限额/窗口走 env 可调；INCR+EXPIRE 经 Lua 原子（无永久无 TTL key）"
  artifacts:
    - path: "src/server/ai/providers/redis-client.ts"
      provides: "限流专用 ioredis lazyConnect 单例（失败时清空 promise 以便重连）"
      exports: ["getAiRedis"]
    - path: "src/server/ai/providers/rate-limit.ts"
      provides: "teacher+global 双层固定窗口计数 + fail-closed"
      exports: ["enforceRateLimit"]
  key_links:
    - from: "src/server/ai/providers/rate-limit.ts"
      to: "src/server/ai/providers/redis-client.ts"
      via: "getAiRedis().eval(LUA, ...)"
      pattern: "getAiRedis"
    - from: "src/server/ai/providers/rate-limit.ts"
      to: "src/server/ai/providers/errors.ts"
      via: "超限/不可达抛 ProviderRateLimitError"
      pattern: "ProviderRateLimitError"
---

<objective>
PROV-03：教师 AI 调用受限流保护，超限返回明确可读错误；后端用现有 ioredis 跨进程共享计数（server.ts 主进程 + BullMQ worker 共享同一额度）。

Purpose: 防单教师刷爆 + 全局成本上限双保险；Redis 不可达时 fail-closed（拒绝而非放行），堵住 DoS / 成本耗尽。
Output: `redis-client.ts`（限流专用单例）、`rate-limit.ts`（双层固定窗口 + fail-closed）及测试，全绿。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-ai-provider-abstraction-layer/61-RESEARCH.md
@.planning/phases/61-ai-provider-abstraction-layer/61-PATTERNS.md

# lazyConnect 单例 + 失败 delete 重置 + UPPER_SNAKE env 抛错（行 111-189）
@src/features/async-tasks/infra/connection.ts
# vi.mock("ioredis") class-mock 先例（行 8）
@src/features/async-tasks/infra/connection.test.ts

<interfaces>
<!-- 来自 Plan 01（已实现）: -->
import { ProviderRateLimitError } from "./errors";   // ctor(msg: string, retryAfter: number)

<!-- Wave 0 夹具: makeMockRedis({ counts?, ttl?, failConnect? }) → 含 eval(lua,n,key,win)=>[count,ttl], connect(), quit() -->

<!-- 原子 Lua（RESEARCH lines 352-357）: INCR → 若 c==1 设 EXPIRE → 返回 {INCR结果, TTL} -->

<!-- 限流 env（默认值）: AI_RL_TEACHER_WINDOW_SEC=60 / AI_RL_TEACHER_MAX=20 / AI_RL_GLOBAL_WINDOW_SEC=60 / AI_RL_GLOBAL_MAX=200 -->
<!-- key 命名空间: openlearn:ai:rl:teacher:<id>:<bucket> 与 openlearn:ai:rl:global:<bucket>（与 openlearn:async-tasks 隔离）-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: redis-client.ts — 限流专用 ioredis lazyConnect 单例</name>
  <files>src/server/ai/providers/redis-client.ts, src/server/ai/providers/rate-limit.test.ts</files>
  <behavior>
    - Test 1: getAiRedis() 复用同一连接 promise（连续两次返回同一实例）。
    - Test 2: 未配置任何 Redis URL（AI_REDIS_URL/BULLMQ_REDIS_URL/REDIS_URL 均缺）→ 抛 Error message "AI_REDIS_URL_NOT_CONFIGURED"。
    - Test 3: connect() 失败时单例 promise 被重置（下次调用重新尝试连接，不卡在坏 promise）。
  </behavior>
  <action>
    创建 `redis-client.ts`，首行 `import "server-only";`，`import Redis from "ioredis";`。仿 connection.ts:153-166 的 **Record memoized promise + 失败 delete 重置** 纪律（PATTERNS 修正：用 delete-on-failure，不用裸 `let|null`）：
    ```
    let connectionPromise: Promise<Redis> | undefined;
    export async function getAiRedis(): Promise<Redis> {
      if (!connectionPromise) {
        const url = process.env.AI_REDIS_URL?.trim() || process.env.BULLMQ_REDIS_URL?.trim() || process.env.REDIS_URL?.trim();
        if (!url) throw new Error("AI_REDIS_URL_NOT_CONFIGURED");
        const r = new Redis(url, { lazyConnect: true, maxRetriesPerRequest: 1, connectionName: "openlearn-ai-ratelimit" });
        connectionPromise = r.connect().then(() => r).catch((e) => { connectionPromise = undefined; throw e; });
      }
      return connectionPromise;
    }
    ```
    `maxRetriesPerRequest: 1` 是 fail-fast（支撑 Task 2 的 fail-closed）。本任务把 redis-client 的 3 条 behavior 写进 `rate-limit.test.ts` 顶部 describe 块（与限流测试同文件，复用 `vi.mock("ioredis")`）。`vi.mock("ioredis", ...)` 用 connection.test.ts:8 的 class-mock；用 `vi.resetModules()` 重置单例。
  </action>
  <verify>
    <automated>pnpm vitest run src/server/ai/providers/rate-limit.test.ts -t "redis-client"</automated>
  </verify>
  <done>redis-client 单例复用、URL 缺失抛 AI_REDIS_URL_NOT_CONFIGURED、connect 失败后可重连；对应 describe 全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rate-limit.ts — 双层固定窗口 + fail-closed（PROV-03 / T-61-dos）</name>
  <files>src/server/ai/providers/rate-limit.ts, src/server/ai/providers/rate-limit.test.ts</files>
  <behavior>
    - Test 1: teacher 维度，前 MAX 次放行；第 MAX+1 次抛 ProviderRateLimitError，retryAfter === 返回的 TTL（>0）。
    - Test 2: teacher 未超但 global 超限 → 抛 ProviderRateLimitError（证明双层各自独立触发）。
    - Test 3: 两层都未超 → resolve（不抛）。
    - Test 4（fail-closed / T-61-dos）：getAiRedis() reject（Redis 不可达，用 makeMockRedis({failConnect:true}) 或 mock eval throw）→ enforceRateLimit 抛错且**不放行**（断言抛出，绝不静默 return）。
    - Test 5: 限额走 env —— 设 AI_RL_TEACHER_MAX=1 后第 2 次即超限（证明 env 可调）。
  </behavior>
  <action>
    创建 `rate-limit.ts`，首行 `import "server-only";`，import `getAiRedis` from "./redis-client"、`ProviderRateLimitError` from "./errors"。常量 `LUA`（RESEARCH lines 352-357，INCR + 首次 EXPIRE + 返回 [count, ttl]，原子）。内部 `hit(redis, key, limit, windowSec)`：`const [count, ttl] = await redis.eval(LUA, 1, key, String(windowSec))`；`if (count > limit) throw new ProviderRateLimitError("AI 请求过于频繁，请稍后再试。", ttl > 0 ? ttl : windowSec)`。
    导出 `enforceRateLimit(teacherId: string)`：读 env（`AI_RL_TEACHER_WINDOW_SEC` 默认 60 / `AI_RL_TEACHER_MAX` 默认 20 / `AI_RL_GLOBAL_*` 默认 60/200，Number 解析）；bucket = `Math.floor(Date.now()/1000/window)`；**先 teacher 后 global**，key `openlearn:ai:rl:teacher:${teacherId}:${tBucket}` 与 `openlearn:ai:rl:global:${gBucket}`。
    **fail-closed（D-05 / Missing-deps / T-61-dos）**：整个 enforceRateLimit 包 try/catch —— 若是 `ProviderRateLimitError` 直接 rethrow（限额命中）；若是 Redis 连接/eval 异常（不可达），rethrow 一个 ProviderRateLimitError（或 server-side 记 warn 后 throw），**绝不 fall-through 放行**。在 SUMMARY 注明 fail-closed 语义。
    `rate-limit.test.ts`：沿用 `vi.mock("server-only")` + `vi.mock("ioredis")`；用 Wave 0 `makeMockRedis` 控制 count/ttl/failConnect；`vi.resetModules()` + `await import("./rate-limit")`。
  </action>
  <verify>
    <automated>pnpm vitest run src/server/ai/providers/rate-limit.test.ts</automated>
  </verify>
  <done>rate-limit.test.ts 全绿：teacher+global 双层各自触发、retryAfter>0、env 可调、Redis 不可达 fail-closed（抛错不放行）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 教师调用 → provider 资源（上游 LLM 成本） | enforceRateLimit 是滥用/成本耗尽的闸门；teacherId 由上游（Phase 62 已鉴权）传入，仅作限流维度 |
| provider 层 → Redis | 计数后端跨进程共享；Redis 失联是可用性边界，本层选择 fail-closed 而非 fail-open |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-dos | Denial of Service | rate-limit.ts | mitigate | 教师层（默认 20/60s）防单人刷爆 + 全局层（默认 200/60s）护 provider 成本；任一超限抛 ProviderRateLimitError(retryAfter)（Test 1/2/5） |
| T-61-dos | Denial of Service | redis-client.ts / enforceRateLimit | mitigate | Redis 不可达 **fail-closed**：拒绝放行（Test 4），杜绝「计数失效=无限放行」的成本失控；`maxRetriesPerRequest:1` fail-fast 缩短失联探测 |
| T-61-key-leak | Information Disclosure | rate-limit.ts | accept | 本层不读 apiKey/baseURL，仅操作 Redis 计数 key；server-only 即足够，无新增 key 暴露面 |
</threat_model>

<verification>
- `pnpm vitest run src/server/ai/providers/rate-limit.test.ts` 全绿（含 redis-client describe）。
- Lua 脚本对每个新 key 首次设 EXPIRE，断言无遗留无 TTL key（mock 计数 + ttl 元组覆盖）。
</verification>

<success_criteria>
- 教师 + 全局双层固定窗口生效，超限抛 ProviderRateLimitError 带 retryAfter（=TTL）+ 中文消息。
- 限额/窗口经 env 可调。
- Redis 不可达 fail-closed：抛可读错误，绝不放行。
</success_criteria>

<output>
完成后创建 `.planning/phases/61-ai-provider-abstraction-layer/61-02-SUMMARY.md`，记录：enforceRateLimit 签名、key 命名约定、env 名与默认值、fail-closed 语义，供 Plan 04 facade 调用。
</output>
