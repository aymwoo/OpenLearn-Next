---
phase: 61-ai-provider-abstraction-layer
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - .env.example
  - src/server/ai/providers/__fixtures__/mock-model.ts
  - src/server/ai/providers/__fixtures__/mock-redis.ts
  - src/server/ai/providers/__fixtures__/fixtures.test.ts
autonomous: true
requirements: [PROV-01, PROV-02, PROV-03, PROV-04]
user_setup:
  - service: openai-compatible-llm-endpoint
    why: "v1 首接国内 OpenAI 兼容端点（DeepSeek/通义/智谱）做真实生成；CI/单测用 mock，不需要真实 key"
    env_vars:
      - name: OPENAI_COMPAT_BASE_URL
        source: "供应商控制台 → API base URL（如 https://api.deepseek.com/v1）"
      - name: OPENAI_COMPAT_API_KEY
        source: "供应商控制台 → API Keys（仅 .env.local，禁 NEXT_PUBLIC_）"
      - name: OPENAI_COMPAT_MODEL
        source: "供应商可用 model id（如 deepseek-chat）"

must_haves:
  truths:
    - "项目可 import `ai` 与 `@ai-sdk/openai-compatible`，类型解析通过"
    - "package.json 钉死 ai 与 @ai-sdk/openai-compatible 的 minor 版本"
    - ".env.example 含 AI provider + 限流 env 段（key 留空、有 server-only 注释）"
    - "共享测试夹具可注入 MockLanguageModelV3 与 mock ioredis，零真实网络"
  artifacts:
    - path: "package.json"
      provides: "ai + @ai-sdk/openai-compatible 依赖与 pin"
      contains: "@ai-sdk/openai-compatible"
    - path: ".env.example"
      provides: "AI provider env 文档化"
      contains: "OPENAI_COMPAT_BASE_URL"
    - path: "src/server/ai/providers/__fixtures__/mock-model.ts"
      provides: "MockLanguageModelV3 工厂（ok-text / ok-object / throw-APICallError）"
      exports: ["makeOkTextModel", "makeOkObjectModel", "makeThrowingModel"]
    - path: "src/server/ai/providers/__fixtures__/mock-redis.ts"
      provides: "ioredis eval mock helper（可控 count/ttl，可模拟连接失败）"
      exports: ["makeMockRedis"]
  key_links:
    - from: "src/server/ai/providers/__fixtures__/mock-model.ts"
      to: "ai/test"
      via: "import { MockLanguageModelV3 } from 'ai/test'"
      pattern: "MockLanguageModelV3"
---

<objective>
Wave 0 地基：安装并钉死 LLM SDK 依赖、登记 env、建立零真实网络的共享测试夹具。后续所有实现 wave（config/errors/registry/rate-limit/facade）都依赖此 wave 的依赖与 fixtures。

Purpose: 把「装包 / 配 env / 准备 mock」一次性收口，使 Wave 1+ 的每个实现任务可立即 RED→GREEN，不再各自处理 SDK 安装与 mock 样板。
Output: 已 pin 的依赖、`.env.example` AI 段、`__fixtures__/` 下的 MockLanguageModelV3 工厂与 ioredis mock helper（+ 自检测试）。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-ai-provider-abstraction-layer/61-RESEARCH.md
@.planning/phases/61-ai-provider-abstraction-layer/61-PATTERNS.md
@.planning/phases/61-ai-provider-abstraction-layer/61-VALIDATION.md

# 现有 env 分段与 ioredis 复用先例
@.env.example
@src/features/async-tasks/infra/connection.ts

<interfaces>
<!-- 安装当日须复跑核对版本（RESEARCH valid-until 2026-06-14）：
     npm view ai version   → 预期 6.0.x（研究锁定 6.0.193）
     npm view @ai-sdk/openai-compatible version → 预期 2.0.x（研究锁定 2.0.48）
     传递依赖 @ai-sdk/provider 预期 3.0.x（错误类来源）
     三者 zod peer dep ^3.25.76 || ^4.1.8，与项目 zod@4.4.3 兼容 -->

MockLanguageModelV3（来自 `ai/test`，ai@6.x）—— doGenerate 返回形如：
  {
    finishReason: "stop",
    usage: { inputTokens: 1, outputTokens: 1, totalTokens: 2 },
    content: [{ type: "text", text: "<string>" }],
    warnings: [],
  }
错误路径：让 doGenerate `throw` 一个 APICallError（statusCode 429 / 5xx，isRetryable）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装并钉死 LLM SDK 依赖 + 登记 .env.example AI 段</name>
  <files>package.json, .env.example, pnpm-lock.yaml</files>
  <action>
    1. **安装当日先复核版本**（RESEARCH valid-until 2026-06-14，AI SDK 高频发版）：
       运行 `npm view ai version` 与 `npm view @ai-sdk/openai-compatible version`。
       - 若 latest 仍为 `6.0.x` / `2.0.x` 同 minor → 安装研究锁定值。
       - 若 minor 已跳变 → 仍优先安装研究锁定的 `ai@6.0.193` + `@ai-sdk/openai-compatible@2.0.48`（已对 d.ts 验证 MockLanguageModelV3 / createOpenAICompatible 导出），并在 SUMMARY 记录漂移，不擅自升 minor。
    2. 执行 `pnpm add ai@6.0.193 @ai-sdk/openai-compatible@2.0.48`。
    3. 在 package.json 把两者改成锁 minor 的 `~` 范围：`"ai": "~6.0.193"`、`"@ai-sdk/openai-compatible": "~2.0.48"`（避免 AI SDK patch/minor 漂移引入 breaking；升级走独立 PR）。
    4. 在 `.env.example` 仿现有 BullMQ 注释段（第 13-20 行风格）追加一段（key 留空，明确 server-only / 禁 NEXT_PUBLIC_）：
       ```
       # AI provider — OpenAI-compatible endpoint (server-only, never NEXT_PUBLIC_)
       OPENAI_COMPAT_BASE_URL=
       OPENAI_COMPAT_API_KEY=
       OPENAI_COMPAT_MODEL=
       OPENAI_COMPAT_NAME=openai-compatible
       # AI rate limit (optional overrides; fixed-window seconds + max count)
       AI_RL_TEACHER_WINDOW_SEC=60
       AI_RL_TEACHER_MAX=20
       AI_RL_GLOBAL_WINDOW_SEC=60
       AI_RL_GLOBAL_MAX=200
       # AI rate-limit Redis (defaults to BULLMQ_REDIS_URL / REDIS_URL when unset)
       AI_REDIS_URL=
       ```
    5. 不创建任何 `src/server/ai/providers/*.ts` 源码（留给后续 wave）。注意（PATTERNS 修正）：仓库根**没有**空 `server/` 残留 —— 不做任何「清理根 server/」动作。
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const a=(p.dependencies||{}).ai||'';const o=(p.dependencies||{})['@ai-sdk/openai-compatible']||'';if(!/^~?6\.0\./.test(a))throw new Error('ai not pinned: '+a);if(!/^~?2\.0\./.test(o))throw new Error('openai-compatible not pinned: '+o);require('node:fs').readFileSync('.env.example','utf8').includes('OPENAI_COMPAT_BASE_URL')||(()=>{throw new Error('.env.example missing AI block')})();console.log('deps+env OK')"</automated>
  </verify>
  <done>package.json 含 `~6.0.x` ai 与 `~2.0.x` openai-compatible；`.env.example` 含 AI provider + 限流 env 段；lockfile 更新。</done>
</task>

<task type="auto">
  <name>Task 2: 共享测试夹具 — MockLanguageModelV3 工厂 + ioredis mock helper</name>
  <files>src/server/ai/providers/__fixtures__/mock-model.ts, src/server/ai/providers/__fixtures__/mock-redis.ts, src/server/ai/providers/__fixtures__/fixtures.test.ts</files>
  <action>
    建立后续所有测试复用的零网络夹具（VALIDATION §"共享夹具"）。

    **mock-model.ts**（import `{ MockLanguageModelV3 }` from `"ai/test"`；不加 `import "server-only"` —— 这是测试夹具）：
    - `makeOkTextModel(text = "hello"): MockLanguageModelV3` —— doGenerate 返回 `content:[{type:"text",text}]`、`finishReason:"stop"`、`usage`、`warnings:[]`。
    - `makeOkObjectModel(obj: unknown): MockLanguageModelV3` —— doGenerate 返回 `content:[{type:"text",text:JSON.stringify(obj)}]`（供 generateObject 解析）。
    - `makeThrowingModel(err: unknown): MockLanguageModelV3` —— doGenerate `async () => { throw err; }`，用于错误映射/重试路径。
    - 导出一个 `makeApiCallError(opts:{status:number;retryAfter?:string;retryable?:boolean})` 便捷工厂：`new APICallError({...})`（从 `"ai"` 导入 APICallError），设置 `statusCode`、`responseHeaders`（含 `retry-after`）、`isRetryable`，供 error-mapping / facade 测试复用。

    **mock-redis.ts**：
    - `makeMockRedis(opts?:{ counts?: Record<string,number>; ttl?: number; failConnect?: boolean })` —— 返回一个最小对象，实现 `eval(lua, numKeys, key, windowSec)`：对传入 key 自增内存计数并返回 `[count, ttl]` 元组（RESEARCH LUA 契约）；`connect()` 在 `failConnect` 时 reject（模拟 Redis 不可达 → 供 fail-closed 测试）；`quit()` resolve。形状对齐 `connection.test.ts:8` 的 class-mock 风格。

    **fixtures.test.ts**（自检，含 `vi.mock("server-only", () => ({}))` 以防未来夹具引入 server-only 传递依赖）：
    - 断言 `makeOkTextModel().doGenerate` 可调用并返回含 text 的 content。
    - 断言 `makeMockRedis({ttl:42}).eval(...)` 返回 `[1, 42]`，再次 eval 同 key 返回 `[2, 42]`。
    - 断言 `makeApiCallError({status:429}).statusCode === 429`。
  </action>
  <verify>
    <automated>pnpm vitest run src/server/ai/providers/__fixtures__/fixtures.test.ts</automated>
  </verify>
  <done>三个夹具文件存在；fixtures.test.ts 全绿；MockLanguageModelV3 与 mock ioredis 可被后续测试 import 注入，零真实网络。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 运维 `.env.local` → Node 进程 | provider key/baseURL 仅经服务端 env 进入；本 wave 只登记 `.env.example` 模板（值留空），真实 key 不入 git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-key-leak | Information Disclosure | `.env.example` AI 段 | mitigate | 模板 key 一律留空 + 注释「server-only, never NEXT_PUBLIC_」；不写真实值；不引入 `NEXT_PUBLIC_` 变量 |
| T-61-dos | Denial of Service | 限流 env 默认值 | mitigate | `.env.example` 提供有界默认（teacher 20/60s、global 200/60s），保证未显式配置时仍有限流保护 |
</threat_model>

<verification>
- `pnpm typecheck` 能解析 `import { generateText } from "ai"` 与 `import { createOpenAICompatible } from "@ai-sdk/openai-compatible"`（无需写源码，安装后类型即可解析）。
- `pnpm vitest run src/server/ai/providers/__fixtures__/fixtures.test.ts` 全绿。
</verification>

<success_criteria>
- ai 与 @ai-sdk/openai-compatible 已安装并在 package.json 钉死 minor。
- `.env.example` 含 AI provider + 限流 env 段，key 留空且标注 server-only。
- 共享夹具（mock-model / mock-redis）可注入，自检测试全绿，零真实网络。
</success_criteria>

<output>
完成后创建 `.planning/phases/61-ai-provider-abstraction-layer/61-00-SUMMARY.md`，记录：实际安装版本（含当日 `npm view` 复核结果与任何版本漂移）、新增 env 清单、夹具导出 API。
</output>
