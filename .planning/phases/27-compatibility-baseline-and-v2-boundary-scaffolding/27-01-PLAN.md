---
phase: 27-compatibility-baseline-and-v2-boundary-scaffolding
plan: 01
type: execute
wave: 3
depends_on: [27-02, 27-03, 27-04]
files_modified:
  - scripts/verify-phase27-runtime-platform.ts
  - package.json
  - src/app/(teacher)/teacher/editor/page.test.tsx
  - src/app/(teacher)/teacher/launch/page.test.tsx
  - src/app/(classroom)/classroom/page.test.tsx
  - src/app/(student)/student/player/page.test.tsx
autonomous: true
requirements: [SAFE-01]
must_haves:
  truths:
    - "D-02 / D-03：仓库存在单一 `verify:phase27` 命令，并采用组合旧 verifier + 少量新 guard 的 compatibility gate。"
    - "D-01 / D-04：teacher authoring、launch、classroom、student player 的关键 guardrail 与 legacy 入口会在兼容回归中 fail loudly。"
    - "D-01：兼容基线同时覆盖 editor `courseId + lessonId`、launch published snapshot、classroom conflict refresh、player `locked/unlocked + resume`。"
  artifacts:
    - path: "scripts/verify-phase27-runtime-platform.ts"
      provides: "canonical phase 27 compatibility verifier"
      contains: "verify:phase3"
    - path: "package.json"
      provides: "verify:phase27 script entry"
      contains: "verify:phase27"
    - path: "src/app/(classroom)/classroom/page.test.tsx"
      provides: "classroom compatibility regression coverage"
      contains: "sessionId"
  key_links:
    - from: "package.json"
      to: "scripts/verify-phase27-runtime-platform.ts"
      via: "npm script"
      pattern: "verify:phase27"
    - from: "scripts/verify-phase27-runtime-platform.ts"
      to: "verify:phase3 / verify:phase4 / verify:phase5"
      via: "composed verifier commands"
      pattern: "execFileSync(\"pnpm\""
---

<objective>
冻结 compatibility baseline，并把旧 verifier、新 static guards 与 focused route regressions 收束成单一 `verify:phase27` 安全门。

Purpose: 让 Phase 27 之后的 V2 改动有一个可复制执行、会 loud fail 的兼容门，而不是继续依赖分散脚本与人工回忆。
Output: canonical verifier script、package.json 入口、四条主链 route focused regressions。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-compatibility-baseline-and-v2-boundary-scaffolding/27-CONTEXT.md
@.planning/phases/27-compatibility-baseline-and-v2-boundary-scaffolding/27-RESEARCH.md
@scripts/verify-phase3-authoring.ts
@scripts/verify-phase4-learning.ts
@scripts/verify-phase5-classroom.ts
@scripts/verify-phase18-schedule.ts
@scripts/verify-phase26-trends-productization.ts
@package.json
@src/app/(teacher)/teacher/editor/page.tsx
@src/app/(teacher)/teacher/launch/page.tsx
@src/app/(classroom)/classroom/page.tsx
@src/app/(student)/student/player/page.tsx

<interfaces>
Verifier composition pattern to reuse:

```ts
function runPnpm(args: readonly string[], label: string) {
  execFileSync("pnpm", [...args], { stdio: "inherit" });
}
```

Current compatibility-critical route contracts:

```ts
// editor
courseId?: string; lessonId?: string;

// classroom
sessionId?: string; studentId?: string; stepId?: string;

// player
lessonId?: string; stepId?: string;
```

Current guardrails to preserve from context:

```text
editor = courseId + lessonId
launch = published snapshot only
classroom = conflict refresh + same-route recap
player = locked/unlocked + resume
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为四条课堂主链补 focused compatibility regressions</name>
  <read_first>
    - src/app/(teacher)/teacher/editor/page.tsx
    - src/app/(teacher)/teacher/launch/page.tsx
    - src/app/(classroom)/classroom/page.tsx
    - src/app/(student)/student/player/page.tsx
    - .planning/phases/27-compatibility-baseline-and-v2-boundary-scaffolding/27-CONTEXT.md
  </read_first>
  <files>src/app/(teacher)/teacher/editor/page.test.tsx, src/app/(teacher)/teacher/launch/page.test.tsx, src/app/(classroom)/classroom/page.test.tsx, src/app/(student)/student/player/page.test.tsx</files>
  <action>按 D-01、D-04 为四个 route page 新增 focused regressions：`/teacher/editor` 必须在缺失 `courseId` 或 `lessonId` 时保留当前 guidance/guardrail；`/teacher/launch` 必须继续只消费 published snapshot console DTO，不引入 runtime host 新入口；`/classroom` 必须继续以 `sessionId` 驱动 live snapshot / ended recap / student detail；`/student/player` 必须继续保留 locked/unlocked + resume shell/personal split。测试要用结构化断言或 mock loader 行为，不使用 comment counting、snapshot-only gating 或易漂移的 prose checklist。</action>
  <acceptance_criteria>
    - 四个测试文件都包含对应路由的关键 guardrail 断言，而不是只做 render smoke。
    - editor 测试覆盖 `courseId + lessonId` 双重入口约束。
    - classroom 测试覆盖 `sessionId` live/ended 分支与 same-route 行为。
    - player 测试覆盖 shell/personal split 或 locked/unlocked/resume 语义。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/app/(teacher)/teacher/editor/page.test.tsx src/app/(teacher)/teacher/launch/page.test.tsx src/app/(classroom)/classroom/page.test.tsx src/app/(student)/student/player/page.test.tsx</automated>
  </verify>
  <done>四条主链 route 的兼容语义被 focused tests 锁住，后续 boundary refactor 漂移会立即失败。</done>
</task>

<task type="auto">
  <name>Task 2: 组装 canonical `verify:phase27` compatibility gate</name>
  <read_first>
    - scripts/verify-phase3-authoring.ts
    - scripts/verify-phase4-learning.ts
    - scripts/verify-phase5-classroom.ts
    - scripts/verify-phase18-schedule.ts
    - scripts/verify-phase26-trends-productization.ts
    - package.json
    - src/features/runtime-platform/shared/boundary-map.ts
    - src/features/runtime-platform/contracts/index.ts
    - src/features/runtime-platform/seams/index.ts
  </read_first>
  <files>scripts/verify-phase27-runtime-platform.ts, package.json</files>
  <action>按 D-02、D-03 创建单一 `scripts/verify-phase27-runtime-platform.ts` 并在 `package.json` 注册 `verify:phase27`。该 verifier 必须先做 Phase 27 静态 guards（route consumers 已导入 `@/features/runtime-platform/*`、contracts root 纯边界、seams centralized 且 default-only、host actions 通过 guard wrapper），再组合运行 `verify:phase3`、`verify:phase4`、`verify:phase5`，并追加本计划的 focused route tests；如适合，可借用 `verify:phase18` / `verify:phase26` 的 static-guard style，但不要递归地重写它们的全部断言。失败输出必须精确指出哪一类 compatibility gate 失守。</action>
  <acceptance_criteria>
    - `package.json` 出现 `verify:phase27`，指向新 verifier 脚本。
    - verifier 脚本同时包含 Phase 27 静态 guards 与对旧 `verify:phase3/4/5` 的组合执行。
    - verifier 脚本包含 contracts/seams/route-import 三类新静态检查，不只转发旧 verifier。
    - verifier 输出中存在 phase-27 specific failure labels，能区分 baseline failure 与 boundary failure。
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase27</automated>
  </verify>
  <done>Phase 27 拥有单一 canonical compatibility gate，后续 V2 phases 可直接复用这一入口守住现有课堂产品。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compatibility gate → repository state | 若 gate 不完整，破坏主链的 refactor 会被误放行 |
| new boundary/contracts/seams → old classroom product | V2 scaffolding 直接影响现有教学产品主链，回归漏检会造成生产行为漂移 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | T | verify:phase27 completeness | mitigate | `verify:phase27` 同时组合旧 verifier、新静态 guards 与 focused route tests，避免单点漏检 |
| T-27-01-02 | R | compatibility regression reporting | mitigate | verifier 输出分层 failure label，明确是 route guardrail、contracts purity、seam posture 或 legacy flow 失守 |
| T-27-01-03 | D | broken refactor shipping unnoticed | mitigate | `package.json` 暴露一键命令并要求 CI/本地都可运行，失败即中断 Phase 27 之后的改造提交 |
</threat_model>

<verification>
- `pnpm test --run src/app/(teacher)/teacher/editor/page.test.tsx src/app/(teacher)/teacher/launch/page.test.tsx src/app/(classroom)/classroom/page.test.tsx src/app/(student)/student/player/page.test.tsx`
- `pnpm verify:phase27`
- 读取 verifier 脚本，确认组合了旧 verifier 与 Phase 27 新 static guards
</verification>

<success_criteria>
- 仓库存在单一 `verify:phase27` compatibility gate。
- 主链 editor / launch / classroom / player 的关键 guardrail 在自动化测试中固定下来。
- 新 boundary / contracts / seams 漂移会在同一个 phase gate 中 loud fail。
</success_criteria>

<output>
After completion, create `.planning/phases/27-compatibility-baseline-and-v2-boundary-scaffolding/27-01-SUMMARY.md`
</output>
