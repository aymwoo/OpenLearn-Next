---
phase: 75-second-external-plugin-marketplace-generalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugins/homework/data-model.ts
  - src/db/schema/generated/plugin-owned/homework.ts
  - src/db/schema/generated/plugin-owned/data-access-allowlist.ts
  - drizzle/0022_phase75_homework_tables.sql
  - drizzle/meta/_journal.json
  - drizzle/meta/0022_snapshot.json
  - src/lib/plugins/external-catalog.ts
  - src/features/platform-core/plugin-data-access/allowlist.ts
  - src/lib/dto/plugin-data-model.ts
  - pnpm-lock.yaml
autonomous: true
requirements: [MKT-EXT-03]
must_haves:
  truths:
    - "D-01: homework pluginKey='homework'，走与 quiz 完全相同的受治理路径（dataModel 声明 → Drizzle 编译 → dispatchPluginDataAccess facade）"
    - "D-02: 行为模型 — 布置 + 提交 + 批改（教师创建作业 → 学生提交 → 教师打分+评语）"
    - "D-03: 三表结构 — plugin_owned_homework_assignments/submissions/grades，submissions 和 grades 走 append-only/isLatest 写入路径"
    - "homework data-model 三表通过 compile-plugin-data-model.ts 编译生成 Drizzle schema + allowlist，与 quiz 走相同编译链路"
    - "external-catalog.ts 新增 homework 条目（manifest + dataModel），pluginKey='homework'"
    - "allowlist alias 映射支持 homework pluginId 前缀（若不同于 quiz）"
    - "跨插件回归：quiz 既有测试全部保持绿色，homework dataModel 通过 meta-schema 校验"
  artifacts:
    - path: "src/plugins/homework/data-model.ts"
      provides: "homework 三表声明的单一真相源"
      contains: "plugin_owned_homework_assignments"
    - path: "src/db/schema/generated/plugin-owned/homework.ts"
      provides: "编译生成的 Drizzle schema"
      contains: "pluginOwnedHomeworkAssignments"
    - path: "drizzle/0022_phase75_homework_tables.sql"
      provides: "三表 Drizzle 迁移"
      contains: "CREATE TABLE plugin_owned_homework_assignments"
    - path: "src/lib/plugins/external-catalog.ts"
      provides: "homework 插件 market 注册"
      contains: "homework"
  key_links:
    - from: "src/plugins/homework/data-model.ts"
      to: "src/db/schema/generated/plugin-owned/homework.ts"
      via: "pnpm plugin:compile"
      pattern: "homework"
    - from: "src/db/schema/generated/plugin-owned/data-access-allowlist.ts"
      to: "src/features/platform-core/plugin-data-access/allowlist.ts"
      via: "编译期派生 → 受治理 allowlist"
      pattern: "homework"
---

<objective>
创建 homework 插件 data-model 声明（三表：assignments / submissions / grades），接入与 quiz 相同的编译链路（compile-plugin-data-model.ts → Drizzle schema + allowlist），在 external-catalog 注册 homework 条目。这是 homework 插件的最基础层——后续 authoring / runtime / lifecycle 全部依赖此 data-model 落库。

Purpose: 先把 data-model + 编译链 + catalog 定住，Plan 02 的 UI + Server Actions 才能落地。
Output: homework 三表落库、编译生成零漂移、catalog 注册完成、quiz 回归全绿。
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/75-second-external-plugin-marketplace-generalization/75-CONTEXT.md
@.planning/phases/75-second-external-plugin-marketplace-generalization/75-RESEARCH.md
@.planning/phases/75-second-external-plugin-marketplace-generalization/75-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto" gate="blocking">
  <name>Task 1: 创建 homework data-model.ts 三表声明并接入编译链</name>
  <read_first>
    - 新建文件：src/plugins/homework/data-model.ts
    - 参考样板：src/plugins/quiz-sample/data-model.ts（双表 → 三表类比）
    - 编译脚本：scripts/compile-plugin-data-model.ts（确认 barrel export 逻辑）
    - DTO meta-schema：src/lib/dto/plugin-data-model.ts（ColumnSpecSchema 只接受 text/integer/boolean/timestamp/enum）
    - 生成产物：src/db/schema/generated/plugin-owned/homework.ts、src/db/schema/generated/plugin-owned/data-access-allowlist.ts
    - 既有 quiz 生成产物：src/db/schema/generated/plugin-owned/quiz.ts（作为对照）
  </read_first>
  <files>src/plugins/homework/data-model.ts, src/db/schema/generated/plugin-owned/homework.ts, src/db/schema/generated/plugin-owned/data-access-allowlist.ts</files>
  <behavior>
    - Test: homework data-model 声明包含三表：plugin_owned_homework_assignments、plugin_owned_homework_submissions、plugin_owned_homework_grades
    - Test: data-model 通过 Zod meta-schema（PluginDataModelSchema）校验
    - Test: pnpm plugin:compile 后 src/db/schema/generated/plugin-owned/homework.ts 生成，三表 Drizzle schema 完整
    - Test: data-access-allowlist.ts 自动追加 homework 条目（三表 + 5 动词）
    - Test: 既有 quiz 的 data-access-allowlist.ts 条目不受影响
    - Test: 表名前缀 "plugin_owned_" 是通用约定，不硬编码 quiz
  </behavior>
  <action>
    在 src/plugins/homework/ 目录下创建 data-model.ts，声明三表：

    plugin_owned_homework_assignments 列：classroomSession(text,notNull)、title(text,notNull)、description(text)、attachmentUrl(text)；索引：classroomSession
    plugin_owned_homework_submissions 列：classroomSession(text,notNull)、student(text,notNull)、assignment(text,notNull)、content(text,notNull)、attachmentUrl(text)；uniques：(classroomSession,student,assignment)；索引：(classroomSession,assignment)
    plugin_owned_homework_grades 列：classroomSession(text,notNull)、student(text,notNull)、submission(text,notNull)、score(integer)、comment(text)；uniques：(classroomSession,student,submission)；索引：(classroomSession,submission)

    不声明 id/pluginId/schoolId/createdAt/updatedAt/attemptNo/isLatest（编译器固定注入）。
    schoolId 不声明（编译器强制注入）。
    submission 和 grade 表的 uniques 将触发编译器的 append-only 注入（attemptNo + isLatest）。

    参考 quiz-sample/data-model.ts 的 export 结构（export const quizDataModel = { pluginKey, tables }），homework 对应 export const homeworkDataModel = { pluginKey: "homework", tables: [...] }。

    确认 scripts/compile-plugin-data-model.ts 的 barrel export 逻辑正确追加 homework 表到 generated/index.ts。
  </action>
  <verify>
    <automated>pnpm plugin:compile && git diff --exit-code src/db/schema/generated/plugin-owned/homework.ts src/db/schema/generated/plugin-owned/data-access-allowlist.ts && pnpm vitest run src/plugins/quiz-sample/</automated>
  </verify>
  <acceptance_criteria>
    - src/plugins/homework/data-model.ts 存在，含 pluginKey="homework" + 三表完整列定义
    - pnpm plugin:compile exit 0，生成 src/db/schema/generated/plugin-owned/homework.ts
    - 生成的 homework.ts 含三表 Drizzle schema（pluginOwnedHomeworkAssignments/Submissions/Grades）
    - 三表均含固定注入列（id/pluginId/schoolId/createdAt/updatedAt）+ homework 特有列
    - data-access-allowlist.ts 含 homework.assignments/submissions/grades 的 5 动词入口
    - quiz 既有测试 pnpm vitest run src/plugins/quiz-sample/ 全绿
    - pnpm typecheck exit 0
  </acceptance_criteria>
  <done>homework data-model 三表声明完成，编译链产出零漂移。</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: 生成 Drizzle 迁移 + 落库 + external-catalog 注册</name>
  <read_first>
    - 编译产物（Task 1 产出）：src/db/schema/generated/plugin-owned/homework.ts
    - 既有迁移：drizzle/ 目录下最新迁移（0021_phase73_question_type.sql 之后）
    - catalog 文件：src/lib/plugins/external-catalog.ts（当前只有 quiz 条目）
    - allowlist 别名：src/features/platform-core/plugin-data-access/allowlist.ts（PLUGIN_DATA_ACCESS_ALIASES）
    - 既有 quiz catalog 条目结构作为模板
  </read_first>
  <files>drizzle/0022_phase75_homework_tables.sql, drizzle/meta/_journal.json, drizzle/meta/0022_snapshot.json, src/lib/plugins/external-catalog.ts, src/features/platform-core/plugin-data-access/allowlist.ts</files>
  <behavior>
    - Test: drizzle/0022_phase75_homework_tables.sql 含三张表的 CREATE TABLE 语句
    - Test: pnpm db:migrate exit 0，三表在本地 SQLite 中落库
    - Test: external-catalog.ts 含 homework 条目（pluginKey="homework"、manifest 含 name/version/description、dataModel 引用 homeworkDataModel）
    - Test: allowlist alias 映射（若 homework 使用 "builtin-teaching-step-homework" 前缀，在 PLUGIN_DATA_ACCESS_ALIASES 添加映射到 "homework"）
    - Test: quiz install 流程不受影响（pnpm vitest run src/plugins/quiz-sample/ 全绿）
  </behavior>
  <action>
    运行 pnpm exec drizzle-kit generate --name=phase75_homework_tables 生成迁移文件（或手写 drizzle/0022_phase75_homework_tables.sql，包含三张 plugin_owned_homework_* 表的 CREATE TABLE 语句，每表含固定列 id/pluginId/schoolId/createdAt/updatedAt + homework 特有列 + 索引/唯一约束）。

    跑 pnpm db:migrate 让本地 dev DB 物理落地三表。

    在 src/lib/plugins/external-catalog.ts 的 EXTERNAL_MARKETPLACE_CATALOG 数组中追加 homework 条目：
    pluginKey="homework"、manifest={ name: "作业", version: "1.0.0", description: "课堂作业插件——布置+提交+批改" }、dataModel=homeworkDataModel。

    在 src/features/platform-core/plugin-data-access/allowlist.ts 检查 PLUGIN_DATA_ACCESS_ALIASES，若 homework 使用不同于 quiz 的 pluginId 前缀则添加映射。

    不要 stage local.db / local.db-shm / local.db-wal。
  </action>
  <verify>
    <automated>pnpm db:migrate && pnpm vitest run src/plugins/quiz-sample/ && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - drizzle/0022_phase75_homework_tables.sql 存在，含 plugin_owned_homework_assignments/submissions/grades 三表 CREATE TABLE
    - pnpm db:migrate exit 0，Drizzle journal 连续（_journal.json 含 0022 条目）
    - src/lib/plugins/external-catalog.ts 含 homework 条目（pluginKey="homework"）
    - pnpm vitest run src/plugins/quiz-sample/ 全绿（quiz 不受影响）
    - pnpm typecheck exit 0
    - git status --short local.db local.db-shm local.db-wal 不会被作为产物提交
  </acceptance_criteria>
  <done>homework 三表已落库，catalog 注册完成，quiz 全绿。</done>
</task>

<task type="auto">
  <name>Task 3: homework DTO + allowlist DTO 层定义</name>
  <read_first>
    - DTO 元 schema：src/lib/dto/plugin-data-model.ts
    - allowlist DTO：src/lib/dto/plugin-data-access-allowlist.ts
    - 既有 quiz DTO：src/lib/dto/plugin-data-model.ts 中的 QuizDTOSchema 模式
    - v4.0 DTO strict pattern：.strictObject + superRefine
  </read_first>
  <files>src/lib/dto/plugin-data-model.ts, src/lib/dto/plugin-data-access-allowlist.ts</files>
  <behavior>
    - Test: HomeworkAssignmentDTOSchema 校验 title/description/attachmentUrl/classroomSession 字段
    - Test: HomeworkSubmissionDTOSchema 校验 content/attachmentUrl/student/assignment/classroomSession 字段
    - Test: HomeworkGradeDTOSchema 校验 score/comment/student/submission/classroomSession 字段
    - Test: 所有 schema 用 .strictObject() 拒额外字段
  </behavior>
  <action>
    在 src/lib/dto/plugin-data-model.ts 新增 homework 三表的 DTO schema：
    HomeworkAssignmentDTOSchema、HomeworkSubmissionDTOSchema、HomeworkGradeDTOSchema。
    每 schema 使用 z.strictObject({...}) 声明字段 + 类型约束（score 为 z.number().int().min(0).max(100).optional()）。
    复刻 v4.0 64-01/69-01 的 strict pattern。

    在 src/lib/dto/plugin-data-access-allowlist.ts 确认 homework 的三表 + 5 动词入口与编译期派生的 allowlist 一致。
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/dto/plugin-data-model.test.ts src/lib/dto/plugin-data-access-allowlist.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/dto/plugin-data-model.ts 含 HomeworkAssignmentDTOSchema / HomeworkSubmissionDTOSchema / HomeworkGradeDTOSchema
    - 三 schema 均用 .strictObject() 拒额外字段
    - score 字段为 z.number().int().min(0).max(100).optional()
    - pnpm vitest run src/lib/dto/ 全绿
    - pnpm typecheck exit 0
  </acceptance_criteria>
  <done>homework DTO 层完整，strict 校验通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data-model declaration → generated schema → SQLite | 编译期单一真相源链 data-model.ts → codegen → migration，所有 schema 变更必须经这条链 |
| homework DAL → SQLite | 写路径经 dispatchPluginDataAccess facade + Command Bus，append-only/isLatest 事务跨三表 |
| external-catalog → install preflight | catalog 条目是 install 前置条件，假条目会导致 install 失败 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01 | Tampering | data-model 声明与编译产物漂移 | mitigate | Task 1 pnpm plugin:compile + git diff --exit-code 守卫 |
| T-75-02 | Elevation | allowlist 注入 | mitigate | allowlist 编译期派生，0 硬编码；Task 2 别名映射仅限 pluginId 前缀 |
| T-75-03 | Information Disclosure | homework 三表数据 | mitigate | 所有 DAL 操作经 dispatchPluginDataAccess facade + governance audit（复用 quiz 路径） |
| T-75-04 | Denial of Service | 迁移冲突 | mitigate | migration-first 流程，drizzle-kit generate 自动检测冲突 |
</threat_model>

<verification>
- pnpm plugin:compile 零漂移（homework.ts + allowlist.ts 与 data-model.ts 一致）
- pnpm db:migrate exit 0
- pnpm vitest run src/plugins/quiz-sample/ 全绿（quiz 不受影响）
- pnpm vitest run src/lib/dto/ 全绿
- pnpm typecheck exit 0
- homework 三表在 SQLite 中物理存在
- external-catalog 含 homework 条目
</verification>
