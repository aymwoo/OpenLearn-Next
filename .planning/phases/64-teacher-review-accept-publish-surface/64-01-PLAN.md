---
phase: 64-teacher-review-accept-publish-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - drizzle/0015_phase64_draft_review_lifecycle.sql
  - drizzle/meta/_journal.json
  - drizzle/meta/0015_snapshot.json
  - drizzle/meta/_snapshot.json
  - src/db/schema.draft-lesson-versions.test.ts
  - src/lib/dto/lesson-authoring.ts
  - src/lib/dto/lesson-authoring.test.ts
autonomous: true
requirements: [REVIEW-01, REVIEW-02, REVIEW-03]
must_haves:
  truths:
    - "D-01/D-02: review DTOs can represent single-column index-aligned step diff rows with new/modified/deleted/unchanged states"
    - "D-05/D-07/D-08: draftLessonVersions has lifecycle fields status and archivedAt; lessons can record aiDraftAppliedAt and latestDraftVersionId backlink"
    - "D-13: review edit payload schemas expose title, description, and content only, not step type or plugin/voting configuration"
    - "Schema migration is generated and applied before downstream DAL/actions rely on new columns"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Phase 64 draft lifecycle columns and lesson AI draft backlink columns"
      contains: "aiDraftAppliedAt"
    - path: "src/lib/dto/lesson-authoring.ts"
      provides: "draft review DTOs, editable step schema, and pure diff classification helper"
      contains: "LessonDraftReviewDTOSchema"
    - path: "drizzle/0015_phase64_draft_review_lifecycle.sql"
      provides: "migration for Phase 64 schema changes"
      contains: "ALTER TABLE"
  key_links:
    - from: "draftLessonVersions.status"
      to: "apply/discard DAL in Plan 02"
      via: "status values pending/applied/discarded"
      pattern: "status"
    - from: "LessonDraftReviewDTOSchema"
      to: "review UI in Plan 03"
      via: "sanitized DTO passed from Server Component to Client Component"
      pattern: "diffRows"
---

<objective>
Prepare Phase 64 data contracts before behavior work: add draft lifecycle/backlink schema fields, generate and apply the required Drizzle migration, and create validated DTO/diff helpers that all later DAL, Server Action, and UI work consume. This plan covers REVIEW-01's diff data model, REVIEW-02's limited editable fields, and REVIEW-03's applied/discarded lifecycle states without changing live lesson behavior yet.
</objective>

<context>
@.planning/phases/64-teacher-review-accept-publish-surface/64-CONTEXT.md
@.planning/phases/64-teacher-review-accept-publish-surface/64-UI-SPEC.md
@.planning/phases/64-teacher-review-accept-publish-surface/64-RESEARCH.md
@.planning/phases/64-teacher-review-accept-publish-surface/64-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add schema fields and source assertions for draft review lifecycle</name>
  <read_first>
    - src/db/schema.ts
    - src/db/schema.draft-lesson-versions.test.ts
    - drizzle/0014_phase63_draft_lesson_versions.sql
    - .planning/phases/64-teacher-review-accept-publish-surface/64-CONTEXT.md
  </read_first>
  <files>src/db/schema.ts, src/db/schema.draft-lesson-versions.test.ts</files>
  <behavior>
    - Schema test asserts draftLessonVersions includes status enum values pending/applied/discarded with default pending.
    - Schema test asserts draftLessonVersions includes archivedAt timestamp for discarded drafts.
    - Schema test asserts lessons includes aiDraftAppliedAt and latestDraftVersionId fields.
    - Schema source assertion documents the backlink decision and prevents adding source/provenance columns to lessonSteps.
  </behavior>
  <action>
Add lifecycle fields to `draftLessonVersions`: `status` as text enum `["pending", "applied", "discarded"]` with default `"pending"`, and nullable `archivedAt` timestamp_ms. Add lesson-level backlinks to `lessons`: nullable `aiDraftAppliedAt` timestamp_ms and nullable `latestDraftVersionId` text. Use plain text for `latestDraftVersionId` unless execution confirms a cascade-compatible FK that cannot delete a lesson when a draft row is removed; this follows D-05 while avoiding an unsafe reverse-cascade relationship. Update `src/db/schema.draft-lesson-versions.test.ts` to assert those exact field names and to assert the source text does not add `source` or `sourceCommandId` columns to `lessonSteps`.
  </action>
  <verify>
    <automated>pnpm test -- src/db/schema.draft-lesson-versions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/schema.ts` contains `status: text("status", { enum: ["pending", "applied", "discarded"] })`.
    - `src/db/schema.ts` contains `archivedAt: integer("archivedAt", { mode: "timestamp_ms" })` inside draftLessonVersions.
    - `src/db/schema.ts` contains `aiDraftAppliedAt` and `latestDraftVersionId` inside lessons.
    - `src/db/schema.draft-lesson-versions.test.ts` fails if `lessonSteps` gains `sourceCommandId`.
  </acceptance_criteria>
  <done>Schema source and schema assertions encode the Phase 64 lifecycle/backlink contract.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: Generate and apply the Drizzle migration for Phase 64 schema fields</name>
  <read_first>
    - drizzle.config.ts
    - package.json
    - drizzle/meta/_journal.json
    - drizzle/0014_phase63_draft_lesson_versions.sql
  </read_first>
  <files>drizzle/0015_phase64_draft_review_lifecycle.sql, drizzle/meta/_journal.json, drizzle/meta/0015_snapshot.json, drizzle/meta/_snapshot.json</files>
  <action>
Run `pnpm exec drizzle-kit generate` after Task 1, then keep the generated migration as the next migration after `0014_phase63_draft_lesson_versions.sql`. If the generated filename differs, rename it to `0015_phase64_draft_review_lifecycle.sql` and keep the Drizzle meta journal consistent. The SQL must add `status` and `archivedAt` to `draftLessonVersion`, and `aiDraftAppliedAt` plus `latestDraftVersionId` to `lesson`. Apply the migration with `pnpm db:migrate`. Do not use `drizzle-kit push` as the source of truth. Do not stage `local.db`, `local.db-shm`, `local.db-wal`, or `.local/pilot-host/**`.
  </action>
  <verify>
    <automated>pnpm exec drizzle-kit generate && pnpm db:migrate</automated>
  </verify>
  <acceptance_criteria>
    - `drizzle/0015_phase64_draft_review_lifecycle.sql` exists and contains `ALTER TABLE`.
    - The migration includes columns `status`, `archivedAt`, `aiDraftAppliedAt`, and `latestDraftVersionId`.
    - `pnpm db:migrate` exits 0.
    - `git status --short local.db local.db-shm local.db-wal .local/pilot-host/shared/data/local.db-shm .local/pilot-host/shared/data/local.db-wal` is not used as staged output for the task.
  </acceptance_criteria>
  <done>Blocking schema migration is generated and applied before downstream plans run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add review DTOs and pure diff classification helper</name>
  <read_first>
    - src/lib/dto/lesson-authoring.ts
    - src/lib/dto/lesson-authoring.test.ts
    - src/features/platform-core/commands/handlers/lesson-draft.ts
    - .planning/phases/64-teacher-review-accept-publish-surface/64-UI-SPEC.md
  </read_first>
  <files>src/lib/dto/lesson-authoring.ts, src/lib/dto/lesson-authoring.test.ts</files>
  <behavior>
    - New helper classifies index-aligned live/draft arrays as `new`, `modified`, `deleted`, or `unchanged`.
    - Review DTO parses live steps, draft steps, draft metadata, diff rows, and `hasPendingDraft`.
    - Editable draft step schema allows title, description, and content only, rejecting `type`, `pluginConfig`, `executableConfig`, and `builtInSource`.
    - DTO tests cover content/task/quiz description extraction without inventing a second payload schema.
  </behavior>
  <action>
Extend `src/lib/dto/lesson-authoring.ts` with `LessonDraftStatusSchema`, `LessonDraftDiffStateSchema`, `EditableDraftStepSchema`, `LessonDraftDiffRowSchema`, `LessonDraftReviewDTOSchema`, and inferred exported types. Add a pure exported helper named `buildLessonDraftDiffRows` that accepts live `LessonStepDTO[]` and draft step DTOs or normalized draft step records, compares by index per D-02, and returns rows with stable `index`, `state`, `liveStep`, and `draftStep`. Treat missing live plus present draft as `new`, present live plus missing draft as `deleted`, different title/payload-derived content as `modified`, and equal normalized title/content/type as `unchanged`. Add tests in `src/lib/dto/lesson-authoring.test.ts`; create the test file if absent.
  </action>
  <verify>
    <automated>pnpm test -- src/lib/dto/lesson-authoring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `buildLessonDraftDiffRows` returns all four states in tests.
    - `EditableDraftStepSchema.safeParse({ type: "quiz" })` fails.
    - `EditableDraftStepSchema.safeParse({ pluginConfig: {} })` fails.
    - `LessonDraftReviewDTOSchema` parses a DTO containing `diffRows`.
  </acceptance_criteria>
  <done>DTO and diff contracts are ready for DAL and UI without raw DB rows or duplicate step schemas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database schema to DAL | New lifecycle/backlink fields become authoritative inputs for apply/discard behavior. |
| Draft snapshot to UI DTO | Raw snapshotJson must be validated and normalized before any client component receives it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| S64-01 | Spoofing | review DTO source | mitigate | DTOs are produced by DAL only; no UI DB import and no raw row pass-through. |
| S64-02 | Tampering | schema migration | mitigate | Blocking migration generation and `pnpm db:migrate` before plan completion. |
| S64-05 | Information Disclosure | draft snapshot | mitigate | DTO parse exposes only normalized review fields; event payloads remain out of scope and summary-only if added later. |
</threat_model>

<verification>
- `pnpm test -- src/db/schema.draft-lesson-versions.test.ts`
- `pnpm test -- src/lib/dto/lesson-authoring.test.ts`
- `pnpm typecheck`
</verification>

<success_criteria>
- REVIEW-01 has a tested diff DTO foundation.
- REVIEW-02 has a tested limited edit payload contract.
- REVIEW-03 has schema fields for pending/applied/discarded lifecycle and lesson backlinks.
- Blocking migration has been generated and applied.
</success_criteria>

<output>
完成后创建 `.planning/phases/64-teacher-review-accept-publish-surface/64-01-SUMMARY.md`
</output>

