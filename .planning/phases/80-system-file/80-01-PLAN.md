---
phase: 80-system-file
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/lib/dto/resource-ai.ts
  - src/features/platform-core/commands/contracts.ts
  - src/features/runtime-platform/contracts/permissions.ts
autonomous: true
requirements: [FILE-01, FILE-02, FILE-03, FILE-04, FILE-05, FILE-06, FILE-07, FILE-09]

must_haves:
  truths:
    - "pluginFiles 表存在于 Drizzle schema 中，带有 schoolId/pluginId/sha256/operation/isLatest 字段"
    - "SystemCommandFileSchema 导出于 resource-ai.ts，包含 allowedPaths + allowedOperations 字段"
    - "SystemCommandDiscriminatedSchema 包含 system.file variant"
    - "SystemCommandTypes 包含 system.file.upload 和 system.file.delete"
    - "GovernanceDeniedReasonValues 包含 path_not_allowed / operation_not_allowed / quota_exceeded"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "pluginFiles 表定义"
      contains: "pluginFiles"
    - path: "src/lib/dto/resource-ai.ts"
      provides: "SystemCommandFileSchema + SYSTEM_COMMAND_REASONS 扩展"
      contains: "SystemCommandFileSchema"
    - path: "src/features/platform-core/commands/contracts.ts"
      provides: "SystemCommandTypes 扩展 + 文件 payload schemas"
      contains: "system.file.upload"
    - path: "src/features/runtime-platform/contracts/permissions.ts"
      provides: "GovernanceDeniedReasonValues 扩展"
      contains: "path_not_allowed"
  key_links:
    - from: "src/db/schema.ts pluginFiles"
      to: "src/lib/dal/files.ts (Plan 02)"
      via: "DAL query"
      pattern: "import.*pluginFiles"
    - from: "src/lib/dto/resource-ai.ts SystemCommandFileSchema"
      to: "src/features/system-commands/handler.ts (Plan 03)"
      via: "handler authorize manifest re-parse"
      pattern: "PluginManifestSchema.parse"
    - from: "src/features/platform-core/commands/contracts.ts"
      to: "src/features/system-commands/facade.ts (Plan 03)"
      via: "PlatformCommandType union"
      pattern: "system.file.upload|system.file.delete"
---

<objective>
建立 Phase 80 的类型/契约/数据模型基础层。创建 pluginFiles 表、扩展 manifest DTO schema（SystemCommandFileSchema）、扩展 Command Bus 类型系统（PlatformCommandType + payload schemas）、新增文件相关治理拒因码。

Purpose: 为后续 Plan 的 DAL、Handler、API Route 提供类型安全的契约基础。
Output: pluginFiles 表已通过迁移文件应用到数据库，所有类型/契约文件通过 tsc 检查。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/80-system-file/80-RESEARCH.md

<interfaces>
<!-- 本 plan 扩展的现有接口，执行器直接使用，无需代码库探索 -->

从 src/db/schema.ts（现有 Drizzle 模式）:
```typescript
// 表创建模式：sqliteTable("name", { columns }, (table) => [indexes])
// isLatest 模式参考（来自 taskSubmissions）：
//   isLatest: integer("isLatest", { mode: "boolean" }).notNull().default(true)
//   createdAt: integer("createdAt", { mode: "timestamp_ms" }).$defaultFn(() => new Date())
// $defaultFn(() => crypto.randomUUID()) 用于 id
// schoolId / pluginId 均带 .references().onDelete("cascade")
```

从 src/lib/dto/resource-ai.ts 第 760-876 行:
```typescript
export const SYSTEM_COMMAND_REASONS = [
  "SYSTEM_COMMAND_DOMAIN_INVALID",
  "SYSTEM_COMMAND_METHOD_INVALID",
  "SYSTEM_COMMAND_KEY_INVALID",
] as const;

export const SystemCommandConfigSchema = z.strictObject({ /* ... */ });
export const SystemCommandDiscriminatedSchema = z.discriminatedUnion("command", [/* ... */]);
// PluginManifestSchema 中: systemCommands: z.array(SystemCommandDiscriminatedSchema).optional()
```

从 src/features/platform-core/commands/contracts.ts 第 39 行:
```typescript
export const SystemCommandTypes = ["system.http.request", "system.config.set"] as const;
// system.config.get 被故意排除——纯 DAL 读，不是 PlatformCommandType
```

从 src/features/runtime-platform/contracts/permissions.ts 第 32-44 行:
```typescript
export const GovernanceDeniedReasonValues = [
  "not_allowlisted", "capability_missing", "permission_denied",
  "lifecycle_blocked", "school_mismatch", "kill_switch",
  "unsupported_action", "domain_not_allowed", "method_not_allowed",
  "private_ip_blocked", "config_key_denied",
] as const;
```

从 RESEARCH.md 第 536-565 行（pluginFiles 表 shape）和 PATTERNS.md 第 68-98 行（schema 代码）:
```typescript
export const pluginFiles = sqliteTable(
  "pluginFile",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    schoolId: text("schoolId").notNull().references(() => schools.id, { onDelete: "cascade" }),
    pluginId: text("pluginId").notNull().references(() => pluginRegistrations.id, { onDelete: "cascade" }),
    operation: text("operation", { enum: ["upload", "delete"] }).notNull(),
    sha256: text("sha256"),           // null for delete operations
    fileName: text("fileName").notNull(),
    mimeType: text("mimeType"),
    diskPath: text("diskPath"),       // relative path from FILE_STORAGE_ROOT
    sizeBytes: integer("sizeBytes"),
    isLatest: integer("isLatest", { mode: "boolean" }).notNull().default(true),
    previousRowId: text("previousRowId"),  // append-only chain
    createdAt: integer("createdAt", { mode: "timestamp_ms" })
      .$defaultFn(() => new Date()),
  },
  (table) => [
    index("pluginFiles_school_plugin_latest_idx")
      .on(table.schoolId, table.pluginId, table.isLatest),
    index("pluginFiles_sha256_idx").on(table.sha256),
    uniqueIndex("pluginFiles_school_plugin_sha256_upload_unique")
      .on(table.schoolId, table.pluginId, table.sha256),
  ],
);
```

从 RESEARCH.md 第 488-502 行（SystemCommandFileSchema shape）:
```typescript
const PATH_PATTERN = /^[a-zA-Z0-9_\-./]+$/;  // 安全路径字符，禁止 %00 和 ..

export const SystemCommandFileSchema = z.strictObject({
  allowedPaths: z.array(
    z.string().min(1).regex(PATH_PATTERN, { message: "SYSTEM_COMMAND_PATH_INVALID" }),
  ).min(1, { message: "SYSTEM_COMMAND_PATH_INVALID" }),
  allowedOperations: z.array(
    z.enum(["upload", "download", "delete", "list", "metadata"]),
  ).min(1),
  maxSingleFileSize: z.number().int().positive().optional(),
  maxTotalStorage: z.number().int().positive().optional(),
});
```

从 RESEARCH.md 第 197-198 行（payload schema for delete）:
```typescript
const SystemFileDeletePayloadSchema = z.strictObject({
  fileId: z.string().min(1),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 pluginFiles 表 (schema.ts)</name>
  <files>src/db/schema.ts</files>
  <read_first>
    - src/db/schema.ts — 现有完整 schema，查找 schools 和 pluginRegistrations 表定义用于参考 references 模式，查找 taskSubmissions 表用于 isLatest 模式参考
    - .planning/phases/80-system-file/80-RESEARCH.md 第 536-565 行 — 完整的 pluginFiles 表 shape（所有列、索引、unique constraint）
    - .planning/phases/80-system-file/80-PATTERNS.md 第 35-98 行 — schema.ts 的 imports 模式和 append-only isLatest 模式代码
  </read_first>
  <behavior>
    - 表名: "pluginFile"
    - 列: id (text PK, crypto.randomUUID()), schoolId (text, FK→schools, cascade), pluginId (text, FK→pluginRegistrations, cascade), operation (text enum: "upload"|"delete"), sha256 (text, nullable), fileName (text, notNull), mimeType (text, nullable), diskPath (text, nullable), sizeBytes (integer, nullable), isLatest (integer boolean, default true), previousRowId (text, nullable), createdAt (integer timestamp_ms, $defaultFn→new Date())
    - 索引: pluginFiles_school_plugin_latest_idx (schoolId, pluginId, isLatest), pluginFiles_sha256_idx (sha256), pluginFiles_school_plugin_sha256_upload_unique (unique, schoolId+pluginId+sha256)
  </behavior>
  <action>
    在 src/db/schema.ts 末尾（所有现有表定义之后）添加 pluginFiles 表定义。使用 sqliteTable、text、integer、index、uniqueIndex 从 drizzle-orm/sqlite-core 导入。完整复制 RESEARCH.md 第 536-565 行的表 shape：id 使用 $defaultFn(() => crypto.randomUUID())、schoolId/pluginId 带 references 和 onDelete: "cascade"、operation 使用 enum: ["upload", "delete"]、isLatest 使用 { mode: "boolean" }、createdAt 使用 { mode: "timestamp_ms" }。三个索引/约束：pluginFiles_school_plugin_latest_idx（schoolId+pluginId+isLatest）、pluginFiles_sha256_idx（sha256）、pluginFiles_school_plugin_sha256_upload_unique（uniqueIndex on schoolId+pluginId+sha256，用于幂等上传约束）。
  </action>
  <verify>
    <automated>grep -c "pluginFile" src/db/schema.ts | grep -v '^#' | head -1</automated>
  </verify>
  <done>pluginFiles 表存在于 src/db/schema.ts 中，类型检查通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 DTO schemas (resource-ai.ts)</name>
  <files>src/lib/dto/resource-ai.ts</files>
  <read_first>
    - src/lib/dto/resource-ai.ts 第 760-876 行 — SYSTEM_COMMAND_REASONS、SystemCommandConfigSchema、SystemCommandDiscriminatedSchema、PluginManifestSchema
    - .planning/phases/80-system-file/80-RESEARCH.md 第 488-502 行 — SystemCommandFileSchema 完整 shape（PATH_PATTERN + allowedPaths + allowedOperations + optional quotas）
    - .planning/phases/80-system-file/80-PATTERNS.md 第 103-156 行 — 完整插入代码模式（REASONS 扩展、PATH_PATTERN、SystemCommandFileSchema、discriminated union 扩展）
  </read_first>
  <behavior>
    - Test 1: SystemCommandFileSchema.parse({ allowedPaths: ["uploads/"], allowedOperations: ["upload", "download"] }) 通过
    - Test 2: SystemCommandFileSchema.parse({ allowedPaths: [], ... }) 被 Zod 拒绝（min(1)）
    - Test 3: SystemCommandFileSchema.parse({ allowedPaths: ["../etc"], ... }) 被 PATH_PATTERN 正则拒绝（禁止 ..）
    - Test 4: SystemCommandDiscriminatedSchema.parse({ command: "system.file", allowedPaths: ["docs/"], allowedOperations: ["list"] }) 通过
  </behavior>
  <action>
    在 src/lib/dto/resource-ai.ts 中做三处修改，与 PATTERNS.md 第 103-156 行完全对齐：
    
    1. 在 SYSTEM_COMMAND_REASONS 数组中添加两个新字面量: "SYSTEM_COMMAND_PATH_INVALID"（路径模式验证失败）和 "SYSTEM_COMMAND_OPERATION_INVALID"（操作不在枚举中）。
    
    2. 在 SystemCommandConfigSchema 之后、SystemCommandDiscriminatedSchema 之前添加 PATH_PATTERN 常量和 SystemCommandFileSchema。PATH_PATTERN 使用值 /^[a-zA-Z0-9_\-./]+$/（允许字母数字、下划线、连字符、点、斜杠；禁止 %00 和 ..）。SystemCommandFileSchema 使用 z.strictObject，包含：allowedPaths（z.array(z.string().min(1).regex(PATH_PATTERN, { message: "SYSTEM_COMMAND_PATH_INVALID" })).min(1)）、allowedOperations（z.array(z.enum(["upload", "download", "delete", "list", "metadata"])).min(1)）、maxSingleFileSize（optional, z.number().int().positive()）、maxTotalStorage（optional, z.number().int().positive()）。allowedOperations 缺少自定义 error message 时，使用 error: () => ({ message: "SYSTEM_COMMAND_OPERATION_INVALID" })。
    
    3. 在 SystemCommandDiscriminatedSchema 的 discriminatedUnion 数组中添加新 variant: z.strictObject({ command: z.literal("system.file") }).merge(SystemCommandFileSchema)，放在 system.config variant 之后。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "resource-ai" || echo "NO_ERRORS"</automated>
  </verify>
  <done>SystemCommandFileSchema 导出、SYSTEM_COMMAND_REASONS 包含 PATH_INVALID 和 OPERATION_INVALID、discriminated union 包含 system.file variant、tsc --noEmit 通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 扩展 Command Bus 类型系统和拒因码 (contracts.ts + permissions.ts)</name>
  <files>src/features/platform-core/commands/contracts.ts, src/features/runtime-platform/contracts/permissions.ts</files>
  <read_first>
    - src/features/platform-core/commands/contracts.ts 第 30-48 行 — SystemCommandTypes 和 PlatformCommandTypeSchema
    - src/features/platform-core/commands/contracts.ts 第 282-391 行 — PlatformCommandPayloadSchemas 和 PlatformCommandSchema（discriminatedUnion 变体）
    - src/features/runtime-platform/contracts/permissions.ts 第 32-44 行 — GovernanceDeniedReasonValues 现有拒因码
    - .planning/phases/80-system-file/80-PATTERNS.md 第 187-220 行 — contracts.ts 和 permissions.ts 的扩展模式
    - .planning/phases/80-system-file/80-RESEARCH.md 第 197-198 行 — SystemFileDeletePayloadSchema
  </read_first>
  <behavior>
    - Test 1: PlatformCommandTypeSchema.safeParse("system.file.upload") 返回 success
    - Test 2: PlatformCommandTypeSchema.safeParse("system.file.delete") 返回 success
    - Test 3: PlatformCommandTypeSchema.safeParse("system.file.download") 返回 failure（纯 DAL 读，不注册）
    - Test 4: GovernanceDeniedReasonValues 包含 "path_not_allowed", "operation_not_allowed", "quota_exceeded"
  </behavior>
  <action>
    两文件修改，每个与 PATTERNS.md 第 187-220 行完全对齐：
    
    **contracts.ts:**
    1. 在 SystemCommandTypes 数组中追加两个新字面量: "system.file.upload" 和 "system.file.delete"（不添加 system.file.download/list/metadata——它们与 system.config.get 一样是纯 DAL 读，不走 Command Bus，遵循第 40 行注释的范式）。
    
    2. 在 SystemConfigSetPayloadSchema 之后添加 SystemFileUploadPayloadSchema（metadata-only payload，包含 fileId、sha256、fileName、mimeType、sizeBytes、diskPath）和 SystemFileDeletePayloadSchema（包含 fileId）。
    
    3. 在 PlatformCommandPayloadSchemas 中添加两个条目: "system.file.upload": SystemFileUploadPayloadSchema 和 "system.file.delete": SystemFileDeletePayloadSchema。
    
    4. 在 PlatformCommandSchema（discriminatedUnion）中添加两个新变体，遵循现有模式：PlatformCommandEnvelopeSchema.extend({ type: z.literal("system.file.upload"), payload: SystemFileUploadPayloadSchema }) 和 对应的 delete 变体。放在 system.config.set 变体之后。
    
    **permissions.ts:**
    1. 在 GovernanceDeniedReasonValues 数组中追加三个新字面量: "path_not_allowed"（manifest allowedPaths 不匹配）、"operation_not_allowed"（manifest allowedOperations 不包含请求的操作）、"quota_exceeded"（文件大小配额超限）。添加顺序：在 "config_key_denied" 之后。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "contracts\|permissions" || echo "NO_ERRORS"</automated>
  </verify>
  <done>SystemCommandTypes 包含 system.file.upload + system.file.delete，PlatformCommandPayloadSchemas 包含对应的 payload schemas，PlatformCommandSchema discriminatedUnion 包含两个新变体，GovernanceDeniedReasonValues 包含 path_not_allowed / operation_not_allowed / quota_exceeded，tsc --noEmit 通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema.ts → SQLite | pluginFiles 表定义，数据存储边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Tampering | pluginFiles.sha256 uniqueIndex | mitigate | uniqueIndex on (schoolId, pluginId, sha256) 确保相同内容同插件同校不会产生重复记录；operation="upload" 的行 sha256 NOT NULL，operation="delete" 的行 sha256=NULL |
| T-80-02 | Info Disclosure | pluginFiles.diskPath | mitigate | diskPath 仅存储 FILE_STORAGE_ROOT 下的相对路径（如 school-a/plugin-1/a1b2.pdf），不暴露绝对路径；跨插件/跨校物理隔离由 schoolId/pluginId 双重 FK 施加，在 DAL 查询时（Plan 02）强制双重 filter |
| T-80-03 | Elevation | SystemCommandFileSchema.allowedPaths PATH_PATTERN | mitigate | PATH_PATTERN /^[a-zA-Z0-9_\-./]+$/ 拒绝对 manifest 声明的 %00 和 .. 注入——仅允许安全字符；路径穿越的完整防护在 file-path-guard.ts（Plan 02）中实现 |
| T-80-04 | Elevation | SystemCommandDiscriminatedSchema | mitigate | Zod discriminatedUnion 的 "command" 字段仅接受已知字面量；未知 command 被 Zod 自动拒绝，无法通过 manifest 解析 |
| T-80-SC | Tampering | npm/pip/cargo installs | mitigate | 无新包安装——drizzle-orm 和 zod 已是项目依赖 [VERIFIED: package.json] |
</threat_model>

<verification>
**schema migration:** Task 1 完成后，遵循 CLAUDE.md 的迁移优先策略——禁止使用 `drizzle-kit push`。取而代之：先运行 `npx drizzle-kit generate` 生成迁移文件，然后运行 `npx drizzle-kit migrate` 将 pluginFiles 表应用到 SQLite 数据库（或运行 `pnpm db:migrate` 一次性完成生成+迁移）。类型检查: `npx tsc --noEmit` 针对 src/ 目录。
</verification>

<success_criteria>
- [ ] pluginFiles 表存在于 src/db/schema.ts 中，匹配 RESEARCH.md 第 536-565 行的完整 shape
- [ ] `npx drizzle-kit generate` 成功生成迁移文件，`npx drizzle-kit migrate`（或 `pnpm db:migrate`）成功应用迁移，pluginFile 表在 dev 数据库中可用
- [ ] SystemCommandFileSchema 导出，通过 PATH_PATTERN 验证
- [ ] SYSTEM_COMMAND_REASONS 包含 "SYSTEM_COMMAND_PATH_INVALID" 和 "SYSTEM_COMMAND_OPERATION_INVALID"
- [ ] SystemCommandDiscriminatedSchema 包含 system.file variant
- [ ] SystemCommandTypes 包含 "system.file.upload" 和 "system.file.delete"
- [ ] PlatformCommandPayloadSchemas 包含 system.file.upload 和 system.file.delete
- [ ] PlatformCommandSchema discriminatedUnion 包含 system.file.upload 和 system.file.delete 变体
- [ ] GovernanceDeniedReasonValues 包含 "path_not_allowed"、"operation_not_allowed"、"quota_exceeded"
- [ ] `npx tsc --noEmit` 零错误通过
</success_criteria>

<output>
Create `.planning/phases/80-system-file/80-01-SUMMARY.md` when done
</output>
