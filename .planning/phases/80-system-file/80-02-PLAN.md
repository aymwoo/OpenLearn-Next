---
phase: 80-system-file
plan: 02
type: execute
wave: 2
depends_on: ["80-01"]
files_modified:
  - src/lib/dal/files.ts
  - src/features/system-commands/file-path-guard.ts
  - src/lib/file-storage/storage-path.ts
  - src/lib/file-storage/quota-check.ts
  - src/lib/file-storage/mime-fallback.ts
autonomous: true
requirements: [FILE-01, FILE-02, FILE-04, FILE-05, FILE-06, FILE-08, FILE-09]

must_haves:
  truths:
    - "DAL 层提供 pluginFiles 的 CRUD 操作（幂等查询、插入、单文件查询、分页列表、元数据查询、软删除）"
    - "file-path-guard 实现多层路径穿越防护（双重 URL 解码 → null byte 拒绝 → .. 拒绝 → path.resolve 前缀验证）"
    - "storage-path 构建 {FILE_STORAGE_ROOT}/{schoolId}/{pluginKey}/{sha256}.{ext} 绝对路径"
    - "QuotaTransform 在流中累计字节数并在超限时中断流"
    - "mime-fallback 从文件扩展名回退 MIME 类型"
  artifacts:
    - path: "src/lib/dal/files.ts"
      provides: "pluginFiles 表的所有 DAL 操作"
      exports: ["getFileBySha256", "insertFileRecord", "getFileRecord", "listFiles", "getFileMetadata", "softDeleteFile"]
    - path: "src/features/system-commands/file-path-guard.ts"
      provides: "sanitizeFilePath 多层消毒函数"
      exports: ["sanitizeFilePath"]
    - path: "src/lib/file-storage/storage-path.ts"
      provides: "buildStoragePath + resolveStoragePath"
      exports: ["buildStoragePath", "resolveStoragePath"]
    - path: "src/lib/file-storage/quota-check.ts"
      provides: "QuotaTransform (Transform stream)"
      exports: ["QuotaTransform"]
    - path: "src/lib/file-storage/mime-fallback.ts"
      provides: "getMimeType MIME 回退逻辑"
      exports: ["getMimeType"]
  key_links:
    - from: "src/lib/dal/files.ts"
      to: "src/db/schema.ts pluginFiles"
      via: "Drizzle ORM query"
      pattern: "import.*pluginFiles.*from.*schema"
    - from: "src/features/system-commands/file-path-guard.ts"
      to: "src/app/api/system/file/upload/route.ts (Plan 04)"
      via: "sanitizeFilePath 导入"
      pattern: "import.*sanitizeFilePath"
    - from: "src/lib/file-storage/quota-check.ts"
      to: "src/app/api/system/file/upload/route.ts (Plan 04)"
      via: "stream pipeline"
      pattern: "import.*QuotaTransform"
---

<objective>
创建文件存储的 DAL 层和工具库。DAL 层提供 pluginFiles 表的完整 CRUD 操作（全部强制 schoolId+pluginId 双重隔离）。工具库提供路径穿越防护、存储路径构建、流式配额检查和 MIME 类型回退。

Purpose: 为 API Routes（Plan 04）和 Command Bus Handler（Plan 03）提供可复用的数据访问层和安全工具。
Output: 5 个新文件，全部模块通过 tsc 检查。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-system-file/80-RESEARCH.md
@.planning/phases/80-system-file/80-PATTERNS.md

<interfaces>
从 src/db/schema.ts（Plan 01 创建）:
```typescript
export const pluginFiles = sqliteTable("pluginFile", { /* ... */ });
// 列: id, schoolId, pluginId, operation, sha256, fileName, mimeType, diskPath, sizeBytes, isLatest, previousRowId, createdAt
// 索引: pluginFiles_school_plugin_latest_idx, pluginFiles_sha256_idx
// 唯一约束: pluginFiles_school_plugin_sha256_upload_unique(schoolId, pluginId, sha256)
```

从 src/lib/dal/plugin-data.ts（DAL 模式参考）:
```typescript
import "server-only";
import { and, eq } from "drizzle-orm";
import { db } from "@/db";
// 查询模式: db.query.pluginFiles.findFirst({ where: and(eq(...), eq(...)) })
// 列表模式: db.query.pluginFiles.findMany({ where: ..., orderBy: ..., limit: ... })
```

从 RESEARCH.md 第 406-445 行（upload pipeline with SHA-256 + quota）:
```typescript
import { createHash } from "node:crypto";
import { createWriteStream } from "node:fs";
import { pipeline } from "node:stream/promises";
import { Transform } from "node:stream";

class QuotaTransform extends Transform {
  private bytesWritten = 0;
  constructor(private maxBytes: number) { super(); }
  _transform(chunk, _encoding, cb) { /* 累计字节，超限则 error */ }
  getTotalBytes(): number { return this.bytesWritten; }
}
```

从 RESEARCH.md 第 448-477 行（path traversal guard multi-layer）:
```typescript
import { resolve } from "node:path";

function sanitizeFilePath(rawPath: string): string | null {
  // Layer 1: 双重 URI 解码
  // Layer 2: Null byte 拒绝
  // Layer 3: Parent reference 拒绝
  // Layer 4: Absolute path resolve + prefix verification
}
```

从 PATTERNS.md 第 625-695 行（file-path-guard.ts、storage-path.ts、quota-check.ts、mime-fallback.ts 完整代码）

从 PATTERNS.md 第 625-675 行（DAL files.ts 完整代码模式——imports、getBySha256、listFiles cursor-based、softDelete 事务模式）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建文件 DAL 层 (src/lib/dal/files.ts)</name>
  <files>src/lib/dal/files.ts</files>
  <read_first>
    - src/db/schema.ts — pluginFiles 表的最新定义（Plan 01 创建）
    - src/lib/dal/plugin-data.ts 第 1-10 行 — DAL 文件结构模式（imports、server-only）
    - src/lib/dal/plugin-data.ts 第 471-512 行 — listPluginStepExtensions（cursor-based 分页模式参考）
    - .planning/phases/80-system-file/80-PATTERNS.md 第 625-675 行 — DAL files.ts 的完整代码模式
    - .planning/phases/80-system-file/80-RESEARCH.md 第 369-377 行 — 幂等检测竞态条件（wx flag 策略）
  </read_first>
  <behavior>
    - Test 1: getFileBySha256(validSha) 对已存在文件返回文件记录
    - Test 2: insertFileRecord({...}) 写入新行，返回带 id 的记录
    - Test 3: listFiles({ schoolId, pluginId, prefix: "documents/" }) 返回过滤后的列表
    - Test 4: softDeleteFile(fileId) 将 isLatest 更新为 false 并插入新的 delete 行
    - Test 5: 所有 DAL 函数同时施加 schoolId + pluginId 过滤
  </behavior>
  <action>
    创建 src/lib/dal/files.ts，采用 server-only 模式。与 PATTERNS.md 第 625-675 行完全对齐。实现六个导出函数：

    1. **getFileBySha256(schoolId, pluginId, sha256)** — 查询 pluginFiles WHERE schoolId+pluginId+sha256+isLatest=true+operation="upload"。用于上传幂等性检查（相同 SHA-256 不重复写磁盘，D-04）。返回 row | null。

    2. **insertFileRecord(params)** — 在 pluginFiles 中插入一条新行，operation="upload"，使用 crypto.randomUUID() 作为 id，包含所有必填字段（schoolId、pluginId、sha256、fileName、mimeType、diskPath、sizeBytes）。isLatest 默认为 true。返回插入的行。

    3. **getFileRecord(schoolId, pluginId, fileId)** — 查询 pluginFiles WHERE schoolId+pluginId+id+isLatest=true。用于 download/metadata API Route 查找文件记录。返回 row | null。

    4. **listFiles({ schoolId, pluginId, prefix, cursor, limit })** — cursor-based 分页列表。WHERE schoolId+pluginId+isLatest=true+operation="upload"。如果提供了 prefix，则执行 fileName LIKE 'prefix%' 过滤。按 createdAt desc 排序，limit+1 查询以检测 hasMore。返回 { files: FileRecord[], nextCursor: string | null }。

    5. **getFileMetadata(schoolId, pluginId, fileId)** — 与 getFileRecord 相同但返回所有元数据（sizeBytes、mimeType、sha256、createdAt）。用于 system.file.metadata API Route。

    6. **softDeleteFile(schoolId, pluginId, fileId)** — 在事务中：(a) UPDATE pluginFiles SET isLatest=false WHERE id=fileId AND isLatest=true AND schoolId+pluginId，(b) INSERT 新行 operation="delete"、sha256=null、diskPath=null、sizeBytes=null、mimeType=null、isLatest=true、previousRowId=fileId、复制原始 fileName。返回新插入的 delete 行。

    所有函数在与 eq 条件中强制添加 schoolId + pluginId 过滤器。遵循现有 Drizzle 模式——db.query.table.findFirst/findMany 和 db.insert/update。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "files.ts" || echo "NO_ERRORS"</automated>
  </verify>
  <done>src/lib/dal/files.ts 存在，导出所有六个函数，所有查询强制执行 schoolId+pluginId 双重过滤器，tsc --noEmit 通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建路径穿越防护 + 存储路径工具 (file-path-guard.ts + storage-path.ts)</name>
  <files>src/features/system-commands/file-path-guard.ts, src/lib/file-storage/storage-path.ts</files>
  <read_first>
    - .planning/phases/80-system-file/80-RESEARCH.md 第 448-477 行 — sanitizeFilePath 多层防护的完整实现和注释
    - .planning/phases/80-system-file/80-RESEARCH.md 第 361-366 行 — 双重 URL 解码路径穿越的验证结果（Pitfall 2）
    - .planning/phases/80-system-file/80-PATTERNS.md 第 587-695 行 — file-path-guard.ts 和 storage-path.ts 的完整代码模式
    - src/features/system-commands/ssrf-guard.ts 第 1-10 行 — 多层验证 guard 的 imports 模式参考
  </read_first>
  <behavior>
    - Test 1: sanitizeFilePath("documents/report.pdf") 返回有效的绝对路径
    - Test 2: sanitizeFilePath("..%2f..%2fetc%2fpasswd") 返回 null（URL 编码路径穿越）
    - Test 3: sanitizeFilePath("%252e%252e%252fetc%252fpasswd") 返回 null（双重编码）
    - Test 4: sanitizeFilePath("foo\x00bar") 返回 null（null byte）
    - Test 5: buildStoragePath("school-a", "plugin-1", "a1b2...", "pdf") 返回 "data/files/school-a/plugin-1/a1b2.....pdf"
  </behavior>
  <action>
    创建两个文件，与 PATTERNS.md 第 587-695 行和 RESEARCH.md 第 448-477 行完全对齐：

    **src/features/system-commands/file-path-guard.ts:**
    实现 sanitizeFilePath 函数——四层防护管线：
    - Layer 1: 双重 decodeURIComponent（catch URIError → return null）
    - Layer 2: 检查 decoded.includes("\x00") → return null
    - Layer 3: 检查 decoded.includes("..") → return null
    - Layer 4: const resolved = resolve(ABSOLUTE_STORAGE_ROOT, decoded); if (!resolved.startsWith(ABSOLUTE_STORAGE_ROOT + "/") && resolved !== ABSOLUTE_STORAGE_ROOT) return null
    从 process.env.FILE_STORAGE_ROOT（默认 "data/files"）读取 STORAGE_ROOT。使用 node:path 的 resolve。导出 sanitizeFilePath 和 ABSOLUTE_STORAGE_ROOT 常量。

    **src/lib/file-storage/storage-path.ts:**
    实现两个函数：
    - buildStoragePath(schoolId, pluginKey, sha256, extension) — 构建 "data/files/{schoolId}/{pluginKey}/{sha256}.{ext}" 并调用 resolve() 返回绝对路径
    - resolveStoragePath(relativePath) — 将相对于 FILE_STORAGE_ROOT 的路径解析为绝对路径（用于 download Route 将 DB 存储的 diskPath 转换为文件系统路径）

    两个文件均从 process.env.FILE_STORAGE_ROOT（默认 "data/files"）读取根目录。使用 node:path 的 resolve。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "file-path-guard\|storage-path" || echo "NO_ERRORS"</automated>
  </verify>
  <done>file-path-guard.ts 导出 sanitizeFilePath，storage-path.ts 导出 buildStoragePath 和 resolveStoragePath，tsc --noEmit 通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 创建配额检查 + MIME 回退工具 (quota-check.ts + mime-fallback.ts)</name>
  <files>src/lib/file-storage/quota-check.ts, src/lib/file-storage/mime-fallback.ts</files>
  <read_first>
    - .planning/phases/80-system-file/80-RESEARCH.md 第 412-425 行 — QuotaTransform 的完整实现
    - .planning/phases/80-system-file/80-RESEARCH.md 第 390-396 行 — 流式管道异常时文件清理策略（Pitfall 5）
    - .planning/phases/80-system-file/80-PATTERNS.md 第 699-758 行 — quota-check.ts 和 mime-fallback.ts 的完整代码模式
  </read_first>
  <behavior>
    - Test 1: QuotaTransform 在 bytesWritten <= maxBytes 时正常传递 chunk
    - Test 2: QuotaTransform 在 bytesWritten > maxBytes 时回调 error("QUOTA_EXCEEDED")
    - Test 3: getMimeType("photo.jpg", null) 返回 "image/jpeg"
    - Test 4: getMimeType("photo.jpg", "image/png") 返回 "image/png"（DB MIME 优先）
    - Test 5: getMimeType("unknown.xyz", null) 返回 "application/octet-stream"
  </behavior>
  <action>
    创建两个文件，与 PATTERNS.md 第 699-758 行和 RESEARCH.md 第 412-425 行完全对齐：

    **src/lib/file-storage/quota-check.ts:**
    实现 QuotaTransform 类（extends Transform from node:stream）：
    - 构造函数接收 maxBytes: number 和可选的 label: string（默认 "file_upload"）
    - _transform(chunk, _encoding, callback): bytesWritten += chunk.length; 如果 bytesWritten > maxBytes → callback(new Error(`QUOTA_EXCEEDED:${label}`)); 否则 push(chunk) 并 callback()
    - getTotalBytes(): number — 返回累计字节数
    - 类型标注 Buffer、BufferEncoding、callback 签名与 Node.js Transform 兼容

    **src/lib/file-storage/mime-fallback.ts:**
    实现 getMimeType(fileName, dbMimeType?) 函数：
    - 如果 dbMimeType 存在且非空 → 返回 dbMimeType（DB 记录的 MIME 优先）
    - 从 fileName 提取扩展名（lastIndexOf(".") + toLowerCase）
    - 使用扩展名到 MIME 的映射表（包含 .jpg/.jpeg → image/jpeg, .png → image/png, .gif → image/gif, .pdf → application/pdf, .json → application/json, .txt → text/plain, .html → text/html, .css → text/css, .js → application/javascript, .mp4 → video/mp4, .mp3 → audio/mpeg 等）
    - 如果扩展名不匹配任何已知类型 → 返回 "application/octet-stream"
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "quota-check\|mime-fallback" || echo "NO_ERRORS"</automated>
  </verify>
  <done>quota-check.ts 导出 QuotaTransform，mime-fallback.ts 导出 getMimeType，tsc --noEmit 通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DAL → SQLite | 数据访问层是 DB 的唯一入口；所有查询强制 schoolId+pluginId |
| API Route → 文件系统 | sanitizeFilePath 消毒所有路径后文件系统写入 |
| 上传流 → 磁盘 | QuotaTransform 在 stream pipeline 中拦截超限流量 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-05 | Tampering | sanitizeFilePath | mitigate | 四层防护：双重 decodeURIComponent（捕获 %2e%2e%2f 和 %252f 变体）→ null byte 拒绝 → .. 拒绝 → path.resolve 前缀验证（确保解析路径在 ABSOLUTE_STORAGE_ROOT 内） |
| T-80-06 | Elevation | DAL 双重隔离 | mitigate | 所有 DAL 查询的 where 子句强制包含 and(eq(schoolId), eq(pluginId))；绝不信任调用者传入的 schoolId/pluginId 而不加过滤 |
| T-80-07 | Denial of Service | QuotaTransform | mitigate | stream.Transform 在 _transform 回调中立即检测 bytesWritten > maxBytes 并 error；pipeline 的 catch 处理清理部分写入文件 |
| T-80-08 | Info Disclosure | diskPath | mitigate | diskPath 仅存储相对路径；resolveStoragePath 在 FILE_STORAGE_ROOT 下解析，不暴露系统绝对路径给调用者 |
| T-80-09 | Tampering | softDeleteFile 事务 | mitigate | UPDATE isLatest=false + INSERT new row 在同一 Drizzle 事务中执行；操作失败时整个事务回滚，不会出现不一致状态 |
| T-80-SC | Tampering | npm/pip/cargo installs | mitigate | 无新包安装——仅使用 Node.js 内置模块（crypto、fs、stream、path） |
</threat_model>

<verification>
类型检查: `npx tsc --noEmit` 通过所有 5 个新文件。DAL 函数通过 vitest 单元测试——测试 schoolId+pluginId 双重隔离、cursor-based 分页、soft delete 事务原子性。路径穿越防护通过 fuzz 测试——验证 %2e%2e%2f、%252e%252e%252f、\x00、..、../ 变体全部返回 null。
</verification>

<success_criteria>
- [ ] src/lib/dal/files.ts 存在，导出 6 个函数，全部强制执行 schoolId+pluginId 双重过滤
- [ ] src/features/system-commands/file-path-guard.ts 存在，sanitizeFilePath 实现四层防护
- [ ] src/lib/file-storage/storage-path.ts 存在，buildStoragePath 和 resolveStoragePath 正确解析
- [ ] src/lib/file-storage/quota-check.ts 存在，QuotaTransform 正确累计字节并在超限时中断
- [ ] src/lib/file-storage/mime-fallback.ts 存在，getMimeType 正确处理 DB MIME 优先和扩展名回退
- [ ] `npx tsc --noEmit` 零错误通过
</success_criteria>

<output>
Create `.planning/phases/80-system-file/80-02-SUMMARY.md` when done
</output>
