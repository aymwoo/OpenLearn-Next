---
phase: 80-system-file
plan: 03
type: execute
wave: 3
depends_on: ["80-01", "80-02"]
files_modified:
  - src/features/system-commands/handler.ts
  - src/features/platform-core/commands/registry.ts
  - src/features/system-commands/facade.ts
  - src/features/system-commands/audit.ts
autonomous: true
requirements: [FILE-01, FILE-03, FILE-07]

must_haves:
  truths:
    - "systemFileHandler 实现 upload 和 delete 的 authorize + execute，authorize 中 re-parse manifest 并 first-match-wins 匹配 allowedPaths + allowedOperations"
    - "platformCommandRegistry 注册 system.file.upload 和 system.file.delete 两个 commandType"
    - "dispatchSystemCommand facade 新增 system.file.upload（仅元数据）和 system.file.delete 分支"
    - "writeSystemCommandAudit 的 commandType union 扩展为包含 system.file.upload 和 system.file.delete"
    - "所有拒绝路径在 throw 前先写 audit（audit-before-throw 模式）"
  artifacts:
    - path: "src/features/system-commands/handler.ts"
      provides: "systemFileHandler（authorize + execute for upload/delete）"
      exports: ["systemFileHandler", "systemFileGetAuthorize", "systemFileGetExecute", "systemFileListExecute", "systemFileMetadataExecute"]
    - path: "src/features/platform-core/commands/registry.ts"
      provides: "platformCommandRegistry 新注册项"
      contains: "system.file.upload"
    - path: "src/features/system-commands/facade.ts"
      provides: "dispatchSystemCommand 新分支"
      contains: "system.file.upload"
    - path: "src/features/system-commands/audit.ts"
      provides: "commandType union 扩展"
      contains: "system.file.upload"
  key_links:
    - from: "src/features/system-commands/handler.ts systemFileHandler"
      to: "src/lib/dto/resource-ai.ts PluginManifestSchema"
      via: "manifest re-parse"
      pattern: "PluginManifestSchema.parse"
    - from: "src/features/system-commands/facade.ts dispatchSystemCommand"
      to: "src/features/platform-core/commands/bus.ts dispatchPlatformCommand"
      via: "Command Bus dispatch"
      pattern: "dispatchPlatformCommand"
    - from: "src/features/system-commands/handler.ts systemFileAuthorize"
      to: "src/features/system-commands/audit.ts writeSystemCommandAudit"
      via: "denySystemFile helper"
      pattern: "writeSystemCommandAudit"
    - from: "src/features/system-commands/handler.ts systemFileUploadExecute"
      to: "src/lib/dal/files.ts insertFileRecord"
      via: "DAL import"
      pattern: "import.*insertFileRecord"
---

<objective>
将 system.file 命令接入 v4.3 Command Bus 三段式链路。在 handler.ts 中实现 systemFileHandler（authorize/execute for upload + delete），在 registry.ts 中注册新 commandType，在 facade.ts 中新增 dispatchSystemCommand 分支（upload 仅元数据经 Bus，delete 完整经 Bus），在 audit.ts 中扩展 commandType union。

Purpose: 所有 system.file 写操作经治理门 → manifest 白名单校验 → Command Bus execute → audit 的完整链路。
Output: 4 个修改文件使 system.file.upload 和 system.file.delete 可通过 Command Bus 派发。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-system-file/80-RESEARCH.md
@.planning/phases/80-system-file/80-PATTERNS.md

<interfaces>
从 src/features/system-commands/handler.ts（现有代码——本 plan 扩展此文件）:
```typescript
// 现有 imports: server-only, z, db, pluginRegistrations, PluginManifestSchema, eq, and
// 现有 helpers: successResult(summary) 返回 PlatformCommandExecutionResult
// 现有模式: denySystemConfig (audit-before-throw)
// 现有模式: systemConfigSetAuthorize (re-parse manifest + first-match-wins)
// 现有模式: systemConfigSetExecute (写 DB + write allowed audit)
// 现有 exports: systemConfigHandler, systemConfigGetAuthorize, systemConfigGetExecute
```

从 src/features/system-commands/handler.ts 第 800-1191 行（system.config 完整实现——authorize+execute+export 模式）:
```typescript
// manifest 解析模式（resolveSystemConfigManifestEntry）：
//   db.query.pluginRegistrations.findFirst({ where: and(eq(id, pluginId), eq(schoolId, schoolId)) })
//   PluginManifestSchema.parse(row.manifestJson)
//   manifest.systemCommands?.filter(entry => entry.command === "system.config")

// authorize 模式（systemConfigSetAuthorize）：
//   遍历 configEntries → 对每个 entry 的 allowedKeys 调用 matchConfigKey → first-match-wins
//   若无匹配 → denySystemConfig(audit → throw)

// execute 模式（systemConfigSetExecute）：
//   写 DB → writeSystemCommandAudit(decision: "allowed") → successResult({...})

// export 模式:
//   export const systemConfigHandler = {
//     "system.config.set": { authorize: ..., execute: ... },
//     "system.config.get": { authorize: ..., execute: ... },
//   };
```

从 src/features/system-commands/facade.ts 第 173-341 行（dispatchSystemCommand 现有结构）:
```typescript
export async function dispatchSystemCommand(input: {
  commandType: string;
  pluginKey: string;
  actorId: string;
  configKey?: string;
  configValue?: unknown;
}) {
  // ① assertActionExecutable (governance gate)
  // ② 判别派发: system.config.set → Command Bus / system.config.get → DAL
  // ③ unknown commandType → audit + throw
}
```

从 src/features/platform-core/commands/registry.ts 第 153-171 行（system.http.request + system.config.set 注册模式）:
```typescript
"system.http.request": createPlatformCommandDefinition({
  commandType: "system.http.request",
  payloadSchema: PlatformCommandPayloadSchemas["system.http.request"],
  dedupe: "required",
  authorize: async (input) => { await systemHttpRequestHandler["system.http.request"].authorize(input); },
  execute: systemHttpRequestHandler["system.http.request"].execute,
}),
```

从 src/features/system-commands/audit.ts 第 18 行（commandType union）:
```typescript
commandType: "system.http.request" | "system.config.set" | "system.config.get";
```

从 PATTERNS.md 第 271-400 行（handler.ts 扩展、registry.ts 扩展、facade.ts 扩展、audit.ts 扩展的完整模式）

从 src/lib/dal/files.ts（Plan 02 创建）:
```typescript
export { insertFileRecord, softDeleteFile, getFileBySha256, getFileRecord, listFiles, getFileMetadata };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 systemFileHandler — authorize + execute (handler.ts)</name>
  <files>src/features/system-commands/handler.ts</files>
  <read_first>
    - src/features/system-commands/handler.ts — 完整现有文件（检查现有 imports、successResult、denySystemConfig 模式）
    - src/features/system-commands/handler.ts 第 800-1191 行 — system.config handler 完整实现（authorize/execute/export 模式，是直接的复制模板）
    - src/features/system-commands/audit.ts — writeSystemCommandAudit 函数签名（denySystemFile helper 调用的目标）
    - src/features/platform-core/commands/contracts.ts — PlatformCommandExecutionError 构造函数签名
    - src/lib/dto/resource-ai.ts — PluginManifestSchema 的 systemCommands 字段（SystemCommandDiscriminatedSchema 中已含 system.file variant）
    - .planning/phases/80-system-file/80-PATTERNS.md 第 271-366 行 — handler.ts 扩展的完整代码模式（authorize pattern + matchFilePath + execute pattern + export pattern）
    - .planning/phases/80-system-file/80-RESEARCH.md 第 292-301 行 — manifest 白名单 first-match-wins 匹配规则
  </read_first>
  <behavior>
    - Test 1: authorize 对 manifest 声明 allowedPaths=["uploads/"] + allowedOperations=["upload"] 且请求路径 "uploads/photo.jpg" + operation "upload" 返回 void（授权通过）
    - Test 2: authorize 对 manifest 声明的 allowedPaths=["docs/"] 但请求路径 "secret/data.txt" → denySystemFile → audit-before-throw（path_not_allowed）
    - Test 3: authorize 对 manifest 声明的 allowedOperations=["upload"] 但请求 operation "delete" → denySystemFile → audit-before-throw（operation_not_allowed）
    - Test 4: execute for upload 插入 pluginFiles 行并返回 { fileId }
    - Test 5: execute for delete 在事务中设置 isLatest=false + 插入新 delete 行
  </behavior>
  <action>
    在 src/features/system-commands/handler.ts 末尾（systemConfigHandler export 之后）添加 system.file handler 实现。与 PATTERNS.md 第 271-366 行和 RESEARCH.md 第 292-301 行完全对齐：

    **准备工作：**
    - 在文件顶部添加导入: import { insertFileRecord, softDeleteFile } from "@/lib/dal/files";
    - 添加类型别名: type SystemFileUploadCommand = Extract<PlatformCommand, { type: "system.file.upload" }>; 和 type SystemFileDeleteCommand = Extract<PlatformCommand, { type: "system.file.delete" }>;

    **路径匹配函数（matchFilePath）:**
    - 实现 matchFilePath(pattern: string, filePath: string): boolean
    - 规则来自 RESEARCH.md 第 296-300 行: 如果 pattern 以 "/" 结尾 → filePath.startsWith(pattern)（前缀匹配）；否则 filePath === pattern（精确匹配）

    **Manifest 解析辅助（resolveSystemFileManifestEntry）:**
    - 镜像 resolveSystemConfigManifestEntry（handler.ts 第 836-865 行）
    - 查询 pluginRegistrations WHERE id=pluginId AND schoolId=schoolId
    - PluginManifestSchema.parse(row.manifestJson)
    - 过滤 systemCommands 中 command === "system.file" 的条目
    - 返回 { row, manifest, fileEntries }

    **denySystemFile helper:**
    - 镜像 denySystemConfig（handler.ts 第 868-924 行）
    - 参数包含: pluginId, schoolId, commandId, actorId, actorScope, lifecycleState, correlationId, reasonCode, filePath, commandType ("system.file.upload" | "system.file.delete"), recommendedRecoveryAction
    - 在 throw 前调用 writeSystemCommandAudit（decision: "denied", reasonCode）
    - reasonCode 使用: "path_not_allowed"（allowedPaths 不匹配）、"operation_not_allowed"（allowedOperations 不包含）等

    **systemFileUploadAuthorize:**
    - 镜像 systemConfigSetAuthorize（第 937-1003 行）
    - 从 command.payload 提取 filePath（插件声明的逻辑路径，如 "uploads/photo.jpg"）
    - 调用 resolveSystemFileManifestEntry → 遍历 fileEntries → 对每个 entry 的 allowedPaths 和 allowedOperations 执行 first-match-wins
    - authorized 条件: entry.allowedOperations 包含 "upload" AND 至少一个 entry.allowedPaths 匹配（matchFilePath）filePath
    - 若无匹配 → denySystemFile({ reasonCode: "path_not_allowed" | "operation_not_allowed" })

    **systemFileDeleteAuthorize:**
    - 与 upload authorize 结构相同但检查 allowedOperations 包含 "delete"
    - filePath 取自被删除文件的 diskPath（需要先从 DAL 查询 fileId 获取 fileName/diskPath）

    **systemFileUploadExecute:**
    - 镜像 systemConfigSetExecute（第 1014-1067 行）
    - 从 command.payload 提取: fileId, sha256, fileName, mimeType, sizeBytes, diskPath
    - 调用 insertFileRecord({ schoolId, pluginId, ... }) 写入 pluginFiles 表
    - 调用 writeSystemCommandAudit（decision: "allowed", commandType: "system.file.upload"）
    - 返回 successResult({ fileId, sha256, fileName, sizeBytes })

    **systemFileDeleteExecute:**
    - 从 command.payload 提取 fileId
    - 调用 softDeleteFile(schoolId, pluginId, fileId) 执行事务性软删除
    - 调用 writeSystemCommandAudit（decision: "allowed", commandType: "system.file.delete"）
    - 返回 successResult({ fileId, deleted: true })

    **Export:**
    - export const systemFileHandler = {
        "system.file.upload": { authorize: systemFileUploadAuthorize, execute: systemFileUploadExecute },
        "system.file.delete": { authorize: systemFileDeleteAuthorize, execute: systemFileDeleteExecute },
      };
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "handler.ts" || echo "NO_ERRORS"</automated>
  </verify>
  <done>systemFileHandler 导出，包含 upload 和 delete 的 authorize+execute，所有 deny 路径先写 audit 再 throw，manifest re-parse + first-match-wins 正确实现，tsc --noEmit 通过</done>
</task>

<task type="auto">
  <name>Task 2: 注册 system.file 命令 + 扩展 audit commandType (registry.ts + audit.ts)</name>
  <files>src/features/platform-core/commands/registry.ts, src/features/system-commands/audit.ts</files>
  <read_first>
    - src/features/platform-core/commands/registry.ts — 完整现有文件（第 153-171 行 system.http.request 注册模式）
    - src/features/system-commands/audit.ts — 完整现有文件（第 18 行 commandType union）
    - .planning/phases/80-system-file/80-PATTERNS.md 第 223-268 行 — registry.ts 和 audit.ts 的扩展模式
  </read_first>
  <behavior>
    - Registry 断言: platformCommandRegistry["system.file.upload"] 存在且 commandType 为 "system.file.upload"
    - Registry 断言: platformCommandRegistry["system.file.delete"] 存在且 commandType 为 "system.file.delete"
    - Registry 断言: platformCommandRegistry 不包含 system.file.download/list/metadata（纯 DAL 读）
    - Audit 断言: writeSystemCommandAudit 接受 commandType: "system.file.upload" | "system.file.delete"
  </behavior>
  <action>
    两个文件修改，与 PATTERNS.md 第 223-268 行完全对齐：

    **registry.ts:**
    1. 在文件顶部已有 imports 中添加: import { systemFileHandler } from "@/features/system-commands/handler";
    2. 在 platformCommandRegistry 对象中（system.config.set 条目之后、satisfies 子句之前）添加两个新条目:
       - "system.file.upload": createPlatformCommandDefinition({ commandType: "system.file.upload", payloadSchema: PlatformCommandPayloadSchemas["system.file.upload"], dedupe: "required", authorize: async (input) => { await systemFileHandler["system.file.upload"].authorize(input); }, execute: systemFileHandler["system.file.upload"].execute })
       - "system.file.delete": createPlatformCommandDefinition({ commandType: "system.file.delete", payloadSchema: PlatformCommandPayloadSchemas["system.file.delete"], dedupe: "required", authorize: async (input) => { await systemFileHandler["system.file.delete"].authorize(input); }, execute: systemFileHandler["system.file.delete"].execute })
    3. 确保 satisfies Record<PlatformCommandType, PlatformCommandDefinition> 类型检查通过。

    **audit.ts:**
    1. 在 writeSystemCommandAudit 函数的 commandType 参数 union 中追加: | "system.file.upload" | "system.file.delete"
    2. 最终类型: commandType: "system.http.request" | "system.config.set" | "system.config.get" | "system.file.upload" | "system.file.delete"
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "registry\|audit" || echo "NO_ERRORS"</automated>
  </verify>
  <done>platformCommandRegistry 包含 system.file.upload 和 system.file.delete 条目，satisfies 类型约束通过，writeSystemCommandAudit 的 commandType union 包含 new types，tsc --noEmit 通过</done>
</task>

<task type="auto">
  <name>Task 3: 扩展 dispatchSystemCommand facade (facade.ts)</name>
  <files>src/features/system-commands/facade.ts</files>
  <read_first>
    - src/features/system-commands/facade.ts — 完整现有文件（第 173-341 行 dispatchSystemCommand 现有分支）
    - .planning/phases/80-system-file/80-PATTERNS.md 第 373-400 行 — facade.ts 扩展模式
    - .planning/phases/80-system-file/80-RESEARCH.md 第 80-83 行 — Binary Bypass 不变式：upload 仅元数据入 Bus，download 走独立 API Route
  </read_first>
  <behavior>
    - facade dispatch system.file.upload 调用 assertActionExecutable → dispatchPlatformCommand(metadata envelope) → 返回 { success, data, commandId }
    - facade dispatch system.file.delete 调用 assertActionExecutable → dispatchPlatformCommand(delete envelope) → 返回 { success, data, commandId }
    - facade 输入类型接受 fileId 和 fileMeta（upload 的元数据）
  </behavior>
  <action>
    在 src/features/system-commands/facade.ts 中扩展 dispatchSystemCommand function，与 PATTERNS.md 第 373-400 行完全对齐：

    **输入类型扩展:**
    在 dispatchSystemCommand 的参数类型中添加可选字段:
    - fileId?: string — 文件 ID（delete 操作使用）
    - fileMeta?: { sha256: string; fileName: string; mimeType: string; sizeBytes: number; diskPath: string; fileId: string } — 上传元数据（upload 操作使用）

    **system.file.upload 分支（插入在 system.config.get 分支之后）:**
    镜像 system.config.set 分支（第 219-282 行）的模式，关键差异：
    - commandType === "system.file.upload" 时的处理
    - 使用 fileMeta.fileId 作为 idempotency key component
    - 构建 envelope: type: "system.file.upload", payload: { fileId, sha256, fileName, mimeType, sizeBytes, diskPath }（仅元数据——二进制不进 Bus，遵循 Binary Bypass 不变式）
    - 使用 buildSystemCommandDedupeKey key 组件: input.fileMeta.fileId 或 input.fileMeta.sha256
    - 调用 dispatchPlatformCommand(envelope, { ... }) 并返回 { success, data, commandId, attemptNumber }

    **system.file.delete 分支:**
    镜像 system.config.set 分支的模式：
    - commandType === "system.file.delete" 时的处理
    - 构建 envelope: type: "system.file.delete", payload: { fileId: input.fileId }
    - 使用 buildSystemCommandDedupeKey key 组件: input.fileId
    - 调用 dispatchPlatformCommand(envelope, { ... }) 并返回 { success, data, commandId, attemptNumber }

    **Helper 调整:**
    - buildSystemCommandDedupeKey 需要支持动态 key（当前硬编码 configKey）——将其泛化接受可选的 key 参数
    - 或新建 buildFileCommandDedupeKey helper 处理 fileId/sha256 作为去重组件

    **未知 commandType 处理:**
    原有的最后分支（第 320-341 行）已处理 unknown commandType 的 audit+throw，无需修改。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "facade.ts" || echo "NO_ERRORS"</automated>
  </verify>
  <done>dispatchSystemCommand 接受 fileId/fileMeta 可选参数，system.file.upload 和 system.file.delete 分支正确派发，Binary Bypass 不变式保持（仅元数据入 Bus），tsc --noEmit 通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| facade → governance gate | assertActionExecutable 从认证 session 派生 schoolId（T-79-04） |
| facade → Command Bus | enveloped 命令只有元数据；二进制从不进入 Bus |
| handler authorize → manifest | PluginManifestSchema.parse 每次请求重新解析 manifest |
| handler → DAL | execute 通过 DAL 访问 pluginFiles，支持 schoolId+pluginId 隔离 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-10 | Elevation | systemFileAuthorize first-match-wins | mitigate | 每次请求重新解析 PluginManifestSchema.parse(row.manifestJson)；遍历 fileEntries 并在第一条同时匹配 allowedPaths 前缀和 allowedOperations 的条目处授权；无匹配时 audit-before-throw (reasonCode: "path_not_allowed" | "operation_not_allowed") |
| T-80-11 | Elevation | facade schoolId injection | mitigate | schoolId 由 assertActionExecutable 从认证 session 注入（T-79-04 不变式）；facade 不接受 schoolId 作为输入参数；DAL 查询和文件路径构造使用治理门派生的 schoolId |
| T-80-12 | Repudiation | writeSystemCommandAudit | mitigate | 所有 deny（在 handler authorize 中）和所有 allow（在 handler execute 中）在执行结果操作之前写入审计记录；关联到 commandId 和 correlationId 以支持追溯 |
| T-80-13 | Information Disclosure | upload metadata payload | mitigate | Binary Bypass 不变式：Command Bus 的 payload 仅包含元数据（fileId、sha256、fileName、mimeType、sizeBytes、diskPath）——原始二进制数据绝不进入 Bus payload |
| T-80-14 | Denial of Service | dedupeKey | mitigate | 所有 system.file.* 命令使用 dedupe: "required"（registry.ts 声明），使用相同的 dedupeKey 防止重放攻击；bus 层对同 dedupeKey 的相同命令复用已有记录 |
| T-80-SC | Tampering | npm/pip/cargo installs | mitigate | 无新包安装——仅使用 Node.js 内置模块和已有的 drizzle-orm + zod 项目依赖 |
</threat_model>

<verification>
类型检查: `npx tsc --noEmit` 通过所有 4 个修改文件。验证 registry satisfies 约束——所有 PlatformCommandType 字面量在 registry 对象中有对应的键。
</verification>

<success_criteria>
- [ ] handler.ts 中 systemFileHandler 导出，包含 "system.file.upload" 和 "system.file.delete" 的 authorize+execute
- [ ] authorize 正确执行 manifest re-parse + first-match-wins allowedPaths/allowedOperations
- [ ] 所有 deny 路径在 throw PlatformCommandExecutionError 前调用 writeSystemCommandAudit
- [ ] platformCommandRegistry 包含 system.file.upload 和 system.file.delete 条目
- [ ] audit.ts 的 commandType union 包含 "system.file.upload" 和 "system.file.delete"
- [ ] dispatchSystemCommand facade 正确处理 system.file.upload 和 system.file.delete
- [ ] Binary Bypass 不变式保持：upload 仅传元数据给 Command Bus
- [ ] `npx tsc --noEmit` 零错误通过，registry satisfies 约束通过
</success_criteria>

<output>
Create `.planning/phases/80-system-file/80-03-SUMMARY.md` when done
</output>
