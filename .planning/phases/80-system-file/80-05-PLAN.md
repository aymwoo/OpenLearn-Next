---
phase: 80-system-file
plan: 05
type: execute
wave: 4
depends_on: ["80-01", "80-02"]
files_modified:
  - scripts/gc-files.ts
autonomous: true
requirements: [FILE-03]

must_haves:
  truths:
    - "GC 脚本扫描 pluginFiles WHERE isLatest=false 的行，检查对应物理文件是否存在"
    - "物理文件存在时，unlink 并输出统计信息（删除文件数、释放字节数）"
    - "GC 脚本不写 governanceAudit（运维操作，非插件操作）"
  artifacts:
    - path: "scripts/gc-files.ts"
      provides: "垃圾回收脚本"
      exports: []
  key_links:
    - from: "scripts/gc-files.ts"
      to: "src/db/schema.ts pluginFiles"
      via: "Drizzle query"
      pattern: "import.*pluginFiles"
    - from: "scripts/gc-files.ts"
      to: "src/lib/file-storage/storage-path.ts"
      via: "resolveStoragePath"
      pattern: "import.*resolveStoragePath"
---

<objective>
创建垃圾回收（GC）脚本——扫描所有已软删除（isLatest=false）的文件行，检查物理文件是否存在，若存在则删除并输出统计信息。不写 governanceAudit（D-10 运维操作非插件操作）。

Purpose: 提供手动触发的文件清理机制，释放已删除文件占用的磁盘空间。
Output: 可执行的 scripts/gc-files.ts Node.js 脚本。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-system-file/80-RESEARCH.md
@.planning/phases/80-system-file/80-PATTERNS.md
@.planning/phases/80-system-file/80-CONTEXT.md

<interfaces>
从 src/db/schema.ts pluginFiles（Plan 01 创建）:
```typescript
// 列: id, schoolId, pluginId, operation, sha256, fileName, mimeType, diskPath, sizeBytes, isLatest, previousRowId, createdAt
// isLatest: integer boolean, default true
// diskPath: text, nullable（delete 操作行 diskPath=null）
```

从 src/lib/file-storage/storage-path.ts（Plan 02 创建）:
```typescript
export function resolveStoragePath(relativePath: string): string;
```

从 CONTEXT.md 的锁定决策:
- D-08: GC 由手动脚本触发（scripts/gc-files.ts），不使用 BullMQ 定时任务
- D-09: 软删除——system.file.delete 标记 isLatest=false，单文件删除；物理清除由 GC 脚本执行
- D-10: GC 脚本输出统计信息（删除数量、释放空间），不写 governanceAudit（运维操作非插件操作）
- D-11: 暂不提供文件恢复 API；GC 执行前可由运维手动恢复（操作 DB）

从 PATTERNS.md 第 763-780 行 — gc-files.ts 脚本的完整代码模式
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 GC 垃圾回收脚本 (scripts/gc-files.ts)</name>
  <files>scripts/gc-files.ts</files>
  <read_first>
    - src/db/schema.ts — pluginFiles 列（特别是 isLatest、diskPath、operation）
    - src/lib/dal/files.ts — 现有 DAL 查询模式，理解如何连接 db 和查询 pluginFiles
    - .planning/phases/80-system-file/80-PATTERNS.md 第 763-780 行 — gc-files.ts 的完整代码模式
    - .planning/phases/80-system-file/80-RESEARCH.md 第 44-46 行 — D-08/D-09/D-10 锁定决策（手动脚本、软删除、统计输出、不写 audit）
    - scripts/ 目录中的现有脚本（如 verify-phase32-end-to-end.ts）——standalone Node.js CLI 脚本模式参考
  </read_first>
  <behavior>
    - GC 脚本运行: npx tsx scripts/gc-files.ts → 输出 "Scanned: 120, Deleted: 15, Freed: 45.2 MB"
    - 仅删除 operation="upload" 且 isLatest=false 且物理文件存在的行对应的磁盘文件
    - 不写入 governanceAudit 表
    - 支持 --dry-run 标志——打印将要删除的内容但不实际删除
  </behavior>
  <action>
    创建 scripts/gc-files.ts，与 PATTERNS.md 第 763-780 行和 CONTEXT.md D-08/D-09/D-10 锁定决策完全对齐。

    **Imports:** 使用 node:fs（existsSync、statSync、unlinkSync）、node:path（resolve）、db（从 src/db）、pluginFiles（从 src/db/schema）、eq（从 drizzle-orm）。

    **Script 逻辑:**
    1. **连接数据库** — 导入项目的 db 实例和 Drizzle schema。
    2. **查询待回收文件** — 查询 pluginFiles WHERE operation = "upload" AND isLatest = false。这些是被软删除的上传行——delete 操作仅标记 isLatest=false（D-09），原始文件仍在磁盘上。
    3. **遍历检查** — 对每一行：检查 diskPath 是否存在且非空。使用 existsSync 检查物理文件。若存在，使用 statSync 记录文件大小。
    4. **物理删除** — 使用 unlinkSync 删除物理文件。除非传递了 --dry-run 标志。
    5. **统计输出（D-10）** — 打印统计信息：scanned（总行数）、deleted（已删除文件数）、freed（释放的总字节数，人类可读格式如 MB/KB）。无任何 governanceAudit 写入。
    6. **可选 --dry-run 标志** — 当 process.argv.includes("--dry-run") 时，仅打印将要删除的文件路径和大小，不实际 unlink。

    脚本应可通过 `npx tsx scripts/gc-files.ts` 直接运行，独立于 Next.js 服务器。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "gc-files" || echo "NO_ERRORS"</automated>
  </verify>
  <done>scripts/gc-files.ts 存在，扫描 isLatest=false 的上传行，unlink 物理文件（若存在），打印统计信息，不写 governanceAudit，支持 --dry-run，tsc --noEmit 通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → 文件系统 | 脚本手动运行，操作员直接访问文件系统 |
| CLI → SQLite | 脚本连接本地 SQLite 数据库 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-22 | Tampering | gc-files.ts diskPath injection | mitigate | diskPath 使用 resolveStoragePath 在 FILE_STORAGE_ROOT 下解析；仅删除已知的 pluginFiles diskPath 引用的文件——不进行任意文件系统扫描 |
| T-80-23 | Denial of Service | gc-files.ts 批量删除 | mitigate | 仅删除 isLatest=false 的行——活跃文件（isLatest=true）不受影响；--dry-run 允许在删除前预览 |
| T-80-24 | Repudiation | gc-files.ts 审计 | accept | D-10 明确声明不写 governanceAudit——GC 是运维操作，非插件操作；运维日志应通过操作系统日志捕获 |
| T-80-SC | Tampering | npm/pip/cargo installs | mitigate | 无新包安装——仅使用 Node.js 内置 fs/path 模块和已有 drizzle-orm 项目依赖 |
</threat_model>

<verification>
类型检查: `npx tsc --noEmit` 通过。手动运行 `npx tsx scripts/gc-files.ts --dry-run` 验证正确识别可回收文件。
</verification>

<success_criteria>
- [ ] scripts/gc-files.ts 存在，可通过 npx tsx 运行
- [ ] 查询 pluginFiles WHERE operation="upload" AND isLatest=false
- [ ] 使用 fs.existsSync + fs.statSync + fs.unlinkSync 检查和删除物理文件
- [ ] 输出统计信息：scanned、deleted count、freed bytes（人类可读格式）
- [ ] 不写入 governanceAudit 表
- [ ] --dry-run 标志仅打印而不删除
- [ ] `npx tsc --noEmit` 零错误通过
</success_criteria>

<output>
Create `.planning/phases/80-system-file/80-05-SUMMARY.md` when done
</output>
