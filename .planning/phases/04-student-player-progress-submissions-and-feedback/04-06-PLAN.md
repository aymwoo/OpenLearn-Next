---
phase: 04-student-player-progress-submissions-and-feedback
plan: 06
type: execute
wave: 5
depends_on:
  - 04-04
  - 04-05
files_modified:
  - scripts/verify-phase4-learning.ts
  - package.json
autonomous: true
requirements:
  - LEARN-01
  - LEARN-02
  - LEARN-03
  - LEARN-04
  - LEARN-05
  - LEARN-06
  - LEARN-07
  - LEARN-08
  - LEARN-09
must_haves:
  truths:
    - "All Phase 04 source requirements and D-01 through D-32 decisions are protected by an automated verification script."
    - "Drizzle schema changes are pushed before final verification, preventing false-positive TypeScript-only success."
    - "UI/DAL/action boundaries are checked for no direct DB imports and no deferred feature leakage."
  artifacts:
    - path: scripts/verify-phase4-learning.ts
      provides: "closed Phase 04 verification gate"
      contains: "Phase 4 learning verification passed"
    - path: package.json
      provides: "verify:phase4 command"
      contains: "verify:phase4"
  key_links:
    - from: scripts/verify-phase4-learning.ts
      to: src/components/learning/teacher-review-surface.tsx
      via: "source invariant checks"
      pattern: "TeacherReviewSurface|待反馈"
    - from: scripts/verify-phase4-learning.ts
      to: src/actions/learning-actions.ts
      via: "cache update checks"
      pattern: "cacheTags\.teacherReview|cacheTags\.submission"
---

<objective>
Close Phase 04 by pushing the Drizzle schema and tightening/running the automated
verification gate across schema, DTO, DAL, actions, student UI, teacher review,
and deferred-scope exclusions.

Purpose: Prove the student learning loop is executable and protected against
the most likely regressions before the phase is considered ready.
Output: Passing schema push, typecheck, and `pnpm verify:phase4`.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-student-player-progress-submissions-and-feedback/04-CONTEXT.md
@.planning/phases/04-student-player-progress-submissions-and-feedback/04-UI-SPEC.md
@scripts/verify-phase4-learning.ts
@package.json
@drizzle.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize Phase 04 verification invariants</name>
  <files>scripts/verify-phase4-learning.ts, package.json</files>
  <read_first>scripts/verify-phase4-learning.ts, package.json, src/db/schema.ts, src/lib/dto/learning.ts, src/lib/dal/learning.ts, src/actions/learning-actions.ts, src/components/surfaces/student-dashboard-surface.tsx, src/components/surfaces/player-surface.tsx, src/components/learning/task-step-card.tsx, src/components/learning/quiz-step-card.tsx, src/components/learning/teacher-review-surface.tsx, src/components/learning/feedback-composer.tsx</read_first>
  <action>Update `scripts/verify-phase4-learning.ts` so every check now targets actual files created by Plans 01-05 and all must pass. Include exact checks for: schema table exports and latest/history indexes; DTO schemas and fields `canRetryTask`, `canRetryQuiz`, `showCorrectAnswer`, `body: z.string().min(1).max(200)`; DAL server-only, `publishedLessonVersions`, `snapshotJson`, `db.transaction`, `isLatest: 0`, `isLatest: 1`, `getTeacherLessonReviewDTO`, `saveAttemptFeedback`; Server Actions `"use server"`, Zod safeParse calls, `updateTag(cacheTags.progress`, `updateTag(cacheTags.submission`, `updateTag(cacheTags.teacherReview`; UI no DB imports; required copy `继续学习`, `还没有可学习的课时`, `老师指定`, `提交任务`, `提交答案`, `给学生的简短反馈`, `最多 200 字`; and absence of non-comment out-of-scope tokens `gradebook`, `rubric`, `bulk`, `EventSource`, `notification center`. Keep `package.json` `verify:phase4` script unchanged unless missing.</action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-phase4-learning.ts` contains `taskSubmissions_latest_idx`, `quizAttempts_latest_idx`, and `attemptFeedback_target_idx`.
    - `scripts/verify-phase4-learning.ts` contains `updateTag(cacheTags.teacherReview`.
    - `scripts/verify-phase4-learning.ts` contains `给学生的简短反馈` and `最多 200 字`.
    - `scripts/verify-phase4-learning.ts` contains `withoutLineComments` or equivalent non-comment filtering before absence checks.
  </acceptance_criteria>
  <done>Verification script checks all delivered Phase 04 artifacts and deferred-scope exclusions deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Push Drizzle schema and run final verification</name>
  <files>src/db/schema.ts, scripts/verify-phase4-learning.ts, package.json</files>
  <read_first>drizzle.config.ts, src/db/schema.ts, scripts/verify-phase4-learning.ts, package.json</read_first>
  <action>[BLOCKING] Run `pnpm exec drizzle-kit push` after all schema file modifications are complete. Then run `pnpm verify:phase4` and `pnpm exec tsc --noEmit`. If `pnpm exec drizzle-kit push` requires an interactive prompt that cannot be suppressed, stop and report the exact prompt as a checkpoint; otherwise complete autonomously. Record the successful commands in the plan summary. This is mandatory because Phase 04 modifies Drizzle schema and TypeScript-only checks do not prove the live SQLite schema was updated.</action>
  <verify>
    <automated>pnpm exec drizzle-kit push && pnpm verify:phase4 && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Terminal output includes successful `pnpm exec drizzle-kit push` exit 0.
    - Terminal output includes `Phase 4 learning verification passed`.
    - Terminal output includes successful `pnpm exec tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>SQLite schema is pushed and all Phase 04 automated gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification script → source tree | Automated checks validate security and scope invariants before completion. |
| Drizzle schema → SQLite database | Schema definitions are applied to local database state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | verification gate | mitigate | Source checks require append-only/latest marker code and block mutable overwrite regressions. |
| T-04-06-02 | Information Disclosure | UI source checks | mitigate | Verification fails on direct DB imports in student/teacher UI. |
| T-04-06-03 | Denial of Service | schema drift | mitigate | Blocking `pnpm exec drizzle-kit push` applies schema changes before final verification. |
</threat_model>

<verification>
`pnpm exec drizzle-kit push && pnpm verify:phase4 && pnpm exec tsc --noEmit`
</verification>

<success_criteria>
Phase 04 has a passing verification gate covering all LEARN requirements, user decisions, security boundaries, design/copy requirements, and explicit out-of-scope exclusions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-student-player-progress-submissions-and-feedback/04-06-SUMMARY.md`.
</output>
