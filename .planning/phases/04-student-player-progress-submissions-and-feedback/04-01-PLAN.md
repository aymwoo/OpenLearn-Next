---
phase: 04-student-player-progress-submissions-and-feedback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/lib/dto/learning.ts
  - package.json
  - scripts/verify-phase4-learning.ts
autonomous: true
requirements:
  - LEARN-01
  - LEARN-02
  - LEARN-03
  - LEARN-04
  - LEARN-05
  - LEARN-06
  - LEARN-07
  - LEARN-08
  - LEARN-09
must_haves:
  truths:
    - "D-01/D-02: Student resume target can represent first incomplete step and a future teacher-forced active step placeholder."
    - "D-05/D-06/D-07/D-27: Task and quiz attempts are append-only, expose latest defaults, and preserve history DTOs."
    - "D-09/D-10/D-12/D-31: Feedback targets task/quiz attempts only, is one updatable short record, and exposes latest content/time."
  artifacts:
    - path: src/db/schema.ts
      provides: "lessonStepProgress, taskSubmissions, quizAttempts, attemptFeedback tables with cascade relations and indexes"
      contains: "export const lessonStepProgress"
    - path: src/lib/dto/learning.ts
      provides: "student dashboard, player, attempt, quiz outcome, teacher review, and feedback DTO schemas"
      exports:
        - StudentDashboardDTOSchema
        - StudentPlayerDTOSchema
        - TeacherLessonReviewDTOSchema
    - path: scripts/verify-phase4-learning.ts
      provides: "Phase 04 invariant gate"
      contains: "Phase 4 learning verification passed"
  key_links:
    - from: src/db/schema.ts
      to: src/lib/dto/learning.ts
      via: "DTO field names match persisted progress/attempt/feedback states"
      pattern: "lessonStepProgress|taskSubmissions|quizAttempts|attemptFeedback"
    - from: scripts/verify-phase4-learning.ts
      to: package.json
      via: "pnpm verify:phase4 script"
      pattern: "verify:phase4"
---

<objective>
Create the Phase 04 persistence, DTO, and verification contracts that all later
student/player/review work builds against.

Purpose: Prevent executor ambiguity by defining append-only attempts, latest
read markers, feedback constraints, resume DTOs, and a deterministic verification
gate before DAL and UI implementation.
Output: Drizzle schema additions, learning DTO module, and `pnpm verify:phase4`.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-student-player-progress-submissions-and-feedback/04-CONTEXT.md
@.planning/phases/04-student-player-progress-submissions-and-feedback/04-UI-SPEC.md
@.planning/phases/04-student-player-progress-submissions-and-feedback/04-RESEARCH.md
@.planning/phases/04-student-player-progress-submissions-and-feedback/04-PATTERNS.md
@.planning/phases/03-courses-lessons-steps-and-teacher-authoring/03-01-SUMMARY.md
@src/db/schema.ts
@src/lib/dto/lesson-authoring.ts
@scripts/verify-phase3-authoring.ts
@package.json

<interfaces>
Existing Phase 03 contracts to preserve:

```ts
export const publishedLessonVersions = sqliteTable("publishedLessonVersion", {
  id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
  lessonId: text("lessonId").notNull().references(() => lessons.id, { onDelete: "cascade" }),
  version: integer("version").notNull(),
  snapshotJson: text("snapshotJson", { mode: "json" }).notNull(),
  publishedById: text("publishedById").notNull().references(() => users.id, { onDelete: "cascade" }),
  publishedAt: integer("publishedAt", { mode: "timestamp_ms" }).$defaultFn(() => new Date()),
});

export const lessonStepPayloadSchema = z.discriminatedUnion("type", [
  contentStepPayloadSchema,
  taskStepPayloadSchema,
  quizStepPayloadSchema,
]);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend schema for progress, append-only attempts, and feedback</name>
  <files>src/db/schema.ts</files>
  <read_first>src/db/schema.ts, .planning/phases/04-student-player-progress-submissions-and-feedback/04-CONTEXT.md, .planning/phases/04-student-player-progress-submissions-and-feedback/04-RESEARCH.md</read_first>
  <behavior>
    - Schema exports `lessonStepProgress`, `taskSubmissions`, `quizAttempts`, and `attemptFeedback`.
    - Every new foreign key to `users`, `lessons`, `lessonSteps`, or `publishedLessonVersions` uses `{ onDelete: "cascade" }`.
    - Task/quiz attempts include `attemptNo`, JSON payload/outcome fields, `isLatest`, and `createdAt`.
    - Feedback records target only `task_submission` or `quiz_attempt` via `targetType` + `targetId` and store one short body.
  </behavior>
  <action>Add four SQLite tables to `src/db/schema.ts`: `lessonStepProgress`, `taskSubmissions`, `quizAttempts`, and `attemptFeedback`. Use text primary IDs with `crypto.randomUUID()`, integer timestamp columns with `mode: "timestamp_ms"`, and cascade references. Include states `not_started`, `in_progress`, `completed`, and `skipped` as text values for progress DTO compatibility. Add indexes named `lessonStepProgress_version_student_idx`, `lessonStepProgress_lesson_student_idx`, `taskSubmissions_latest_idx`, `taskSubmissions_history_idx`, `quizAttempts_latest_idx`, `quizAttempts_history_idx`, `attemptFeedback_target_idx`, and `attemptFeedback_student_idx`. This implements D-05, D-09, D-10, D-27, and D-31. Do not add gradebook, rubric, score percentage, notification, or SSE tables.</action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/schema.ts` contains `export const lessonStepProgress = sqliteTable`.
    - `src/db/schema.ts` contains `export const taskSubmissions = sqliteTable`.
    - `src/db/schema.ts` contains `export const quizAttempts = sqliteTable`.
    - `src/db/schema.ts` contains `export const attemptFeedback = sqliteTable`.
    - `src/db/schema.ts` contains `taskSubmissions_latest_idx` and `quizAttempts_latest_idx`.
    - `src/db/schema.ts` contains at least four occurrences of `onDelete: "cascade"` inside the newly added table definitions.
  </acceptance_criteria>
  <done>New learning tables exist with cascade ownership, latest/history indexes, append-only fields, and no out-of-scope gradebook/SSE schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define learning DTO and validation contracts</name>
  <files>src/lib/dto/learning.ts</files>
  <read_first>src/lib/dto/lesson-authoring.ts, .planning/phases/04-student-player-progress-submissions-and-feedback/04-CONTEXT.md, .planning/phases/04-student-player-progress-submissions-and-feedback/04-UI-SPEC.md</read_first>
  <behavior>
    - DTO schemas expose student dashboard lesson ordering fields and resume reason copy.
    - Player DTO separates shell, progress, runtime placeholder, latest submissions, and history summaries.
    - Task retry, quiz retry, and correct-answer reveal are server-controlled booleans.
    - Feedback input validates maximum 200 Chinese characters.
  </behavior>
  <action>Create `src/lib/dto/learning.ts`. Import `z` and reuse `lessonStepPayloadSchema` where a published step payload is represented. Export schemas and inferred types for: `ProgressStateSchema`, `LearningStepDTOSchema`, `StudentLessonCardDTOSchema`, `StudentDashboardDTOSchema`, `RuntimeStepStateDTOSchema`, `TaskAttemptDTOSchema`, `QuizAttemptDTOSchema`, `AttemptFeedbackDTOSchema`, `StudentPlayerDTOSchema`, `SubmitTaskInputSchema`, `SubmitQuizInputSchema`, `MarkProgressInputSchema`, `TeacherReviewFilterSchema`, `TeacherLessonReviewDTOSchema`, `TeacherStudentReviewDTOSchema`, `FeedbackInputSchema`, and `MutationResultDTOSchema`. Include exact DTO booleans `canRetryTask`, `canRetryQuiz`, and `showCorrectAnswer`. Include Chinese copy-compatible fields `resumeLabel`, `inaccessibleMessage`, `successMessage`, and `feedbackEmptyLabel`. This implements D-01 through D-32 at contract level without adding deferred Phase 05 SSE behavior.</action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/dto/learning.ts` contains `export const StudentDashboardDTOSchema`.
    - `src/lib/dto/learning.ts` contains `export const StudentPlayerDTOSchema`.
    - `src/lib/dto/learning.ts` contains `export const TeacherLessonReviewDTOSchema`.
    - `src/lib/dto/learning.ts` contains `body: z.string().min(1).max(200)`.
    - `src/lib/dto/learning.ts` contains `canRetryTask`, `canRetryQuiz`, and `showCorrectAnswer`.
  </acceptance_criteria>
  <done>Learning DTO schemas and types fully describe dashboard, player, attempts, feedback, and teacher review contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 04 verification gate and package script</name>
  <files>scripts/verify-phase4-learning.ts, package.json</files>
  <read_first>scripts/verify-phase3-authoring.ts, package.json, src/db/schema.ts, src/lib/dto/learning.ts</read_first>
  <action>Create `scripts/verify-phase4-learning.ts` using the same source-invariant style as `scripts/verify-phase3-authoring.ts`. The script must read `src/db/schema.ts`, `src/lib/dto/learning.ts`, future paths `src/lib/dal/learning.ts`, `src/actions/learning-actions.ts`, `src/components/surfaces/student-dashboard-surface.tsx`, `src/components/surfaces/player-surface.tsx`, `src/components/learning/task-step-card.tsx`, `src/components/learning/quiz-step-card.tsx`, `src/components/learning/teacher-review-surface.tsx`, and route files if they exist; missing future files should be reported as failed checks once the script is run after this plan. Add checks for required schema exports/indexes, DTO exports/booleans/copy, server-only DAL, append-only insert semantics, Server Action `updateTag`, no UI DB imports, required Chinese copy, and absence of `gradebook`, `rubric`, `bulk`, `EventSource`, and `notification center`. Add `"verify:phase4": "tsx scripts/verify-phase4-learning.ts"` to `package.json`. This script is allowed to fail until later Phase 04 plans create the referenced files, but `pnpm exec tsc --noEmit` must pass.</action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"verify:phase4": "tsx scripts/verify-phase4-learning.ts"`.
    - `scripts/verify-phase4-learning.ts` contains `Phase 4 learning verification passed`.
    - `scripts/verify-phase4-learning.ts` contains `taskSubmissions_latest_idx`.
    - `scripts/verify-phase4-learning.ts` contains `StudentPlayerDTOSchema`.
    - `scripts/verify-phase4-learning.ts` contains `EventSource` in an absence/out-of-scope check.
  </acceptance_criteria>
  <done>Phase 04 verification command is registered and ready for later plans to close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DTO schema | Untrusted persisted JSON and future form inputs are normalized before UI use. |
| Schema relations | User-owned progress, attempts, and feedback cross from app logic into SQLite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | `taskSubmissions` / `quizAttempts` | mitigate | Store append-only attempts with `attemptNo`, `isLatest`, and history indexes; later DAL must clear latest marker before insert. |
| T-04-01-02 | Information Disclosure | `StudentPlayerDTOSchema` | mitigate | DTO contract exposes published snapshots and sanitized fields only; no raw rows or draft payloads. |
| T-04-01-03 | Denial of Service | `attemptFeedback.body` | mitigate | `FeedbackInputSchema` caps feedback at 200 characters per D-12. |
</threat_model>

<verification>
Run `pnpm exec tsc --noEmit`. After later plans complete, run `pnpm verify:phase4` and expect it to pass.
</verification>

<success_criteria>
Schema, DTO, and verification contracts exist; all LEARN requirement IDs are represented in concrete artifacts; no deferred Phase 05/v2 features are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/04-student-player-progress-submissions-and-feedback/04-01-SUMMARY.md`.
</output>
