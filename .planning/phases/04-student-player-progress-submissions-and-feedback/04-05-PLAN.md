---
phase: 04-student-player-progress-submissions-and-feedback
plan: 05
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - src/app/(teacher)/teacher/review/page.tsx
  - src/components/learning/teacher-review-surface.tsx
  - src/components/learning/feedback-composer.tsx
autonomous: true
requirements:
  - LEARN-08
  - LEARN-09
must_haves:
  truths:
    - "D-13/D-15/D-20: Teacher review opens at lesson overview with basic status filters and waiting-condition empty states."
    - "D-14/D-16: Student detail prioritizes progress, latest submissions, attempt history, outcomes, feedback status, and attached feedback composer."
    - "D-28: Teacher review shows quiz status plus latest outcome and avoids full answer-detail tables except inside selected student detail."
    - "D-29/D-30/D-31/D-32: Feedback send/update preserves failed input and students see latest feedback only after Server Action success/cache update."
  artifacts:
    - path: src/app/(teacher)/teacher/review/page.tsx
      provides: "teacher review route loading lesson review DTO"
      contains: "getTeacherLessonReviewDTO"
    - path: src/components/learning/teacher-review-surface.tsx
      provides: "lightweight teacher review cockpit"
      contains: "needs_feedback"
    - path: src/components/learning/feedback-composer.tsx
      provides: "200-character feedback composer"
      contains: "最多 200 字"
  key_links:
    - from: src/app/(teacher)/teacher/review/page.tsx
      to: src/lib/dal/learning.ts
      via: "route loads teacher review DTO"
      pattern: "getTeacherLessonReviewDTO"
    - from: src/components/learning/feedback-composer.tsx
      to: src/actions/learning-actions.ts
      via: "send feedback Server Action"
      pattern: "sendAttemptFeedbackAction"
---

<objective>
Add the teacher-facing review cockpit and feedback composer for published lesson
progress, submissions, quiz outcomes, attempt history, and short feedback.

Purpose: Give teachers the Phase 04 learning-evidence loop without building a
full gradebook, rubric system, bulk grading, or notification center.
Output: Teacher review route, review surface, and feedback composer.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@DESIGN.md
@.planning/phases/04-student-player-progress-submissions-and-feedback/04-CONTEXT.md
@.planning/phases/04-student-player-progress-submissions-and-feedback/04-UI-SPEC.md
@src/app/(teacher)/teacher/editor/page.tsx
@src/components/authoring/authoring-status-panel.tsx
@src/lib/dto/learning.ts
@src/lib/dal/learning.ts
@src/actions/learning-actions.ts

<interfaces>
Use prior-plan contracts:

```ts
getTeacherLessonReviewDTO(input: { lessonId?: string; filter?: TeacherReviewFilter }): Promise<TeacherLessonReviewDTO>
getTeacherStudentReviewDTO(input: { lessonId: string; studentId: string }): Promise<TeacherStudentReviewDTO>
sendAttemptFeedbackAction(input: FormData | Record<string, unknown>)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create teacher review route and DTO loading</name>
  <files>src/app/(teacher)/teacher/review/page.tsx</files>
  <read_first>src/app/(teacher)/teacher/editor/page.tsx, src/lib/dal/learning.ts, src/lib/dto/learning.ts</read_first>
  <action>Create `src/app/(teacher)/teacher/review/page.tsx` as an async Server Component. Accept optional `searchParams` for `lessonId`, `studentId`, and `filter`; call `getTeacherLessonReviewDTO({ lessonId, filter })`; pass the DTO plus selected student id to `TeacherReviewSurface`. Keep all DB access inside DAL. If no published lesson data exists, still render a safe empty DTO from DAL with copy `暂无学生数据` or `还没有提交学习证据`, not class-management or gradebook actions.</action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(teacher)/teacher/review/page.tsx` contains `getTeacherLessonReviewDTO`.
    - `src/app/(teacher)/teacher/review/page.tsx` contains `TeacherReviewSurface`.
    - `src/app/(teacher)/teacher/review/page.tsx` does not contain `@/db` or `@/db/schema`.
  </acceptance_criteria>
  <done>Teacher review route loads scoped DTOs through DAL and renders a safe review surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build lightweight teacher review cockpit</name>
  <files>src/components/learning/teacher-review-surface.tsx</files>
  <read_first>src/lib/dto/learning.ts, .planning/phases/04-student-player-progress-submissions-and-feedback/04-UI-SPEC.md, DESIGN.md</read_first>
  <behavior>
    - Overview shows counts for `未开始`, `进行中`, `已完成`, latest submissions, quiz outcomes, and feedback status.
    - Filters are exactly all, not_started, in_progress, completed, and needs_feedback.
    - Student detail prioritizes progress before latest task/quiz submissions, attempt history, outcome, and feedback status.
    - Empty states use `还没有提交学习证据` and `暂无学生数据` only, without gradebook actions.
  </behavior>
  <action>Create `src/components/learning/teacher-review-surface.tsx`. Render a tonal review cockpit with summary cards, filter pills labelled `全部`, `未开始`, `进行中`, `已完成`, `待反馈`, a student list/detail area, latest task and quiz attempt cards, historical attempts labelled `第 1 次尝试` etc., quiz outcome text without percentage/gradebook language, feedback status, and embedded `FeedbackComposer` for the selected task/quiz latest attempt. Use no 1px divider lines; separate regions with surface colors and spacing. Do not add full answer-detail tables except for selected student detail, and do not add gradebook-style sort/filter complexity.</action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/learning/teacher-review-surface.tsx` contains `全部`, `未开始`, `进行中`, `已完成`, and `待反馈`.
    - `src/components/learning/teacher-review-surface.tsx` contains `还没有提交学习证据` and `暂无学生数据`.
    - `src/components/learning/teacher-review-surface.tsx` contains `FeedbackComposer`.
    - `src/components/learning/teacher-review-surface.tsx` does not contain `gradebook`, `rubric`, or `bulk`.
  </acceptance_criteria>
  <done>Teacher review cockpit exposes required overview/detail/filter states without gradebook scope creep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add feedback composer with retry-preserving failure</name>
  <files>src/components/learning/feedback-composer.tsx</files>
  <read_first>src/actions/learning-actions.ts, src/lib/dto/learning.ts, .planning/phases/04-student-player-progress-submissions-and-feedback/04-CONTEXT.md, .planning/phases/04-student-player-progress-submissions-and-feedback/04-UI-SPEC.md</read_first>
  <behavior>
    - Composer label is `给学生的简短反馈` and helper is `最多 200 字，聚焦下一步改进`.
    - CTA is `发送反馈` and success message is `反馈已发送给学生`.
    - Failed sends preserve typed content and show retry path per D-32.
    - Updated feedback displays latest content and updated time only, no edit history per D-31.
  </behavior>
  <action>Create client component `src/components/learning/feedback-composer.tsx` with `"use client";`. Use local state for the textarea, enforce `maxLength={200}`, show a live `当前字数/200` counter, call `sendAttemptFeedbackAction`, clear the input only on `ok: true`, preserve typed content on `ok: false`, and render failure copy `反馈暂时没有发送成功，请保留内容后重试。`. Render latest feedback content and updated time when provided, with no feedback edit history. Use tonal input styling from `DESIGN.md`: `surface-container-low`, white focus, 2px primary ghost outline, no section divider borders.</action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/learning/feedback-composer.tsx` starts with `"use client";`.
    - `src/components/learning/feedback-composer.tsx` contains `maxLength={200}`.
    - `src/components/learning/feedback-composer.tsx` contains `给学生的简短反馈`, `最多 200 字，聚焦下一步改进`, `发送反馈`, and `反馈已发送给学生`.
    - `src/components/learning/feedback-composer.tsx` contains `sendAttemptFeedbackAction`.
    - `src/components/learning/feedback-composer.tsx` does not contain `history` or `edit history`.
  </acceptance_criteria>
  <done>Teachers can send/update one short feedback record per latest task/quiz attempt with retry-preserving failure behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Teacher route → DAL | Teacher query params select lesson/filter/student and must be scoped server-side. |
| Teacher browser → feedback action | Feedback text crosses from browser to Server Action and DAL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Elevation of Privilege | `teacher/review/page.tsx` | mitigate | Route calls DAL only; DAL verifies active teacher and lesson scope. |
| T-04-05-02 | Information Disclosure | `TeacherReviewSurface` | mitigate | Render sanitized DTOs only; no DB/schema imports or raw student records. |
| T-04-05-03 | Tampering | `FeedbackComposer` | mitigate | Submit via `sendAttemptFeedbackAction` with `FeedbackInputSchema` validation and cache updates. |
| T-04-05-04 | Denial of Service | feedback text | mitigate | Client `maxLength={200}` plus server-side 200-character validation. |
</threat_model>

<verification>
Run `pnpm exec tsc --noEmit`; after Plan 06, run `pnpm verify:phase4`.
</verification>

<success_criteria>
Teachers can review progress/evidence/outcomes/feedback status for a published lesson and send short feedback without gradebook features.
</success_criteria>

<output>
After completion, create `.planning/phases/04-student-player-progress-submissions-and-feedback/04-05-SUMMARY.md`.
</output>
