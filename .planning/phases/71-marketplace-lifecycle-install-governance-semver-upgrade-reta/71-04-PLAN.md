---
phase: 71-marketplace-lifecycle-install-governance-semver-upgrade-reta
plan: "04"
type: execute
wave: 4
depends_on: [02, 03]
files_modified:
  - src/features/platform-core/plugins/governance-projection.ts
  - src/features/platform-core/actions/registry.ts
  - src/actions/plugin-actions.ts
  - src/actions/plugin-actions.test.ts
  - src/components/surfaces/plugin-marketplace-surface.tsx
  - src/components/surfaces/plugin-marketplace-detail-panel.tsx
  - src/components/surfaces/plugin-lifecycle-operator-surface.tsx
  - src/components/surfaces/plugin-marketplace-surface.test.tsx
autonomous: true
requirements:
  - MKT-01
  - MKT-02
  - MKT-03
  - MKT-04
  - MKT-05
must_haves:
  truths:
    - "D-71-01 D-71-02 D-71-03 D-71-04: `/settings/plugins` stays the single marketplace entry, shows built-in and external sections on the same page, puts governance summary before install CTA, renders inline reject reasons, and keeps an app-store-like but governance-first tone."
    - "D-71-05 D-71-06 D-71-07 D-71-08: Upgrade cards open preflight-first details, prioritize data impact/blockers over changelog, show explicit backfill -> verify -> cutover progress, and display verify-failure honesty without pretending the new version is active."
    - "D-71-09 D-71-10 D-71-11 D-71-12 D-71-13 D-71-14 D-71-15 D-71-16: Retained plugins remain visible as recoverable cards, cleanup shows real blast radius + token with retain as default, recovered data is called out honestly, and active blockers list affected classrooms/sessions first with only governed follow-up actions."
  artifacts:
    - path: src/features/platform-core/actions/registry.ts
      provides: external lifecycle rows projected into a single SSR read model
    - path: src/components/surfaces/plugin-marketplace-surface.tsx
      provides: single-page dual-section marketplace lifecycle UI
    - path: src/components/surfaces/plugin-marketplace-detail-panel.tsx
      provides: client-side detail panel and dialog state for preflight / cleanup / blocker interactions
    - path: src/components/surfaces/plugin-marketplace-surface.test.tsx
      provides: SSR/client surface coverage for external lifecycle states and inline reasons
  key_links:
    - from: src/features/platform-core/actions/registry.ts
      to: src/components/surfaces/plugin-marketplace-surface.tsx
      via: built-in + external dual-section read model
      pattern: external
    - from: src/components/surfaces/plugin-marketplace-surface.tsx
      to: src/actions/plugin-actions.ts
      via: marketplace install / recover / upgrade / uninstall actions
      pattern: preflightMarketplace
    - from: src/components/surfaces/plugin-marketplace-surface.tsx
      to: src/components/surfaces/plugin-marketplace-detail-panel.tsx
      via: SSR data passed into client interaction layer
      pattern: PluginMarketplaceDetailPanel
---

<objective>
把 `/settings/plugins` 扩成同页 built-in + external 双分区的真实 lifecycle UI，忠实表达 install / upgrade / uninstall / recover / active-session blocking 的治理状态与风险信息。

Purpose: 落地 D-71-01..D-71-16 的产品表现，避免 backend 能力藏在 action 层无人可用。
Output: external SSR read-model、dual-section marketplace surface、inline reject reasons、upgrade preflight/detail panel、recoverable retained cards、active blocker explanation UI。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-CONTEXT.md
@.planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-UI-SPEC.md
@.planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-PATTERNS.md
@src/components/surfaces/plugin-marketplace-surface.tsx
@src/components/surfaces/plugin-lifecycle-operator-surface.tsx
@src/actions/plugin-actions.ts

<interfaces>
From src/components/surfaces/plugin-marketplace-surface.tsx:
```tsx
export async function PluginMarketplaceSurface()
```

From 71-UI-SPEC.md:
```md
Card states: 未安装 | 已安装可用 | 可升级 | 已卸载但可恢复 | 被 active classroom 阻断
Primary labels: 安装插件 / 查看升级预检 / 开始升级 / 卸载并保留数据 / 确认 cleanup / 重新安装并恢复 / 查看受影响课堂
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 接好 external SSR read-model 与同页 dual-section marketplace</name>
  <files>
    src/features/platform-core/plugins/governance-projection.ts
    src/features/platform-core/actions/registry.ts
    src/actions/plugin-actions.ts
    src/actions/plugin-actions.test.ts
    src/components/surfaces/plugin-marketplace-surface.tsx
    src/components/surfaces/plugin-marketplace-detail-panel.tsx
    src/components/surfaces/plugin-marketplace-surface.test.tsx
  </files>
  <read_first>
    .planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-CONTEXT.md
    .planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-UI-SPEC.md
    .planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-PATTERNS.md
    src/features/platform-core/plugins/governance-projection.ts
    src/features/platform-core/actions/registry.ts
    src/actions/plugin-actions.ts
    src/actions/plugin-actions.test.ts
    src/components/surfaces/plugin-marketplace-surface.tsx
    src/components/surfaces/plugin-marketplace-detail-panel.tsx
    src/components/surfaces/plugin-marketplace-surface.test.tsx
    .claude/skills/gitnexus/gitnexus-impact-analysis/SKILL.md
  </read_first>
  <action>先把 external lifecycle 的 SSR read-model 接好，再重写 `PluginMarketplaceSurface` 的数据装配与 IA。修改 `governance-projection.ts`、`registry.ts`、`plugin-actions.ts`、现有 surface 组件前，先运行 GitNexus upstream impact analysis，完成后执行 GitNexus detect_changes。read-model 层必须基于 `71-PATTERNS.md` 中 projection-first analog，新增 external marketplace rows，覆盖 `not-installed`、`installed-usable`、`upgrade-available`、`retained-recoverable`、`active-blocked` 五类 posture，并带治理摘要字段 `currentVersion`、`availableVersion`、`requestedPermissions`、`declaredDataTables`、`dbNamespace`、`sourceType`。然后保留 built-in section，新增 external section 并与 built-in 同页共存（per D-71-01）。同时新增 `src/components/surfaces/plugin-marketplace-detail-panel.tsx` 作为 client 交互承载文件，供 surface 把 SSR 读模型 DTO 传入 detail/panel 交互层。external 卡片顺序固定为：插件名+状态 badge -> 版本信息 -> 治理摘要 badges（Permissions / Data Model / Namespace / Source）-> 说明文案 -> 内联成功/失败/阻断区 -> 主操作区；禁止把安装按钮放在最顶端（per D-71-02）。安装失败要在卡片正文内联显示具名 reason token 对应的中文文案，而不是 toast-only 或跳转结果页（per D-71-03）。整体语言可更接近应用商店，但不得出现评分评论/付费等运营层元素（per D-71-04）。</action>
  <acceptance_criteria>
    - 页面源码中可 grep 到 built-in 与 external 两个 section heading，且仍只使用 `/settings/plugins` 单页入口。
    - `src/components/surfaces/plugin-marketplace-detail-panel.tsx` 存在，并作为 client interaction layer 被 `PluginMarketplaceSurface` 引用。
    - `src/features/platform-core/actions/registry.ts` / `src/features/platform-core/plugins/governance-projection.ts` 的测试或 action+surface 断言能证明 external posture rows 已产出。
    - external 卡片源码中治理摘要字段出现在 `安装插件` CTA 之前。
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/actions/plugin-actions.test.ts -t "marketplace catalog|marketplace posture|recover takeover"</automated>
    <automated>pnpm vitest run src/components/surfaces/plugin-marketplace-surface.test.tsx -t "external section|governance summary|inline reject reason|retained"</automated>
    <automated>gitnexus_detect_changes()</automated>
  </verify>
  <done>external plugin 已在 `/settings/plugins` 以治理优先的双分区方式呈现，未安装与 retained recoverable 卡片都能被 operator 看见和理解。</done>
</task>

<task type="auto">
  <name>Task 2: 把升级预检/执行、retain-cleanup、active blocker 细节层嵌回卡片与 detail panel</name>
  <files>
    src/components/surfaces/plugin-marketplace-surface.tsx
    src/components/surfaces/plugin-marketplace-detail-panel.tsx
    src/components/surfaces/plugin-lifecycle-operator-surface.tsx
    src/components/surfaces/plugin-marketplace-surface.test.tsx
  </files>
  <read_first>
    .planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-CONTEXT.md
    .planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-UI-SPEC.md
    .planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-PATTERNS.md
    src/components/surfaces/plugin-lifecycle-operator-surface.tsx
    src/components/surfaces/plugin-marketplace-surface.tsx
    src/components/surfaces/plugin-marketplace-detail-panel.tsx
    src/components/surfaces/plugin-marketplace-surface.test.tsx
    .claude/skills/gitnexus/gitnexus-impact-analysis/SKILL.md
  </read_first>
  <action>在 `src/components/surfaces/plugin-marketplace-detail-panel.tsx` 中实现同页 detail panel / dialog 的 client-tier 状态层，并复用 `71-PATTERNS.md` 中 `plugin-lifecycle-operator-surface.tsx` 的 `useTransition + inlineError + dialog` 交互 analog，而不是把这些交互硬塞回 async Server Component。修改现有 detail/status 相关符号前先运行 GitNexus upstream impact analysis，完成后执行 GitNexus detect_changes。`PluginMarketplaceSurface` 只负责 SSR 取数并把 selected external row / preflight DTO / action endpoints 传给 detail panel。升级入口默认是 `查看升级预检`，detail 第一屏按 D-71-06 顺序展示：当前版本 -> 目标版本、真实 owned-data、`backfill -> verify -> cutover` 三阶段、blocker 列表、影响说明、最后才是 changelog；执行中必须逐行显示三阶段状态而不是单一进度条（D-71-05/06/07）。verify 失败时固定展示“升级未完成，系统已保持旧版本继续可用”，卡片仍保留旧版本态并叠加 `升级失败` badge（D-71-08）。卸载默认按钮是 `卸载并保留数据`，cleanup 只在 detail panel 第二层选择，并先展示真实影响面计数 + confirmation token（D-71-09/10）。retained 卡片必须可见并显示 `已卸载但可恢复` + `已接管保留数据` 文案链路（D-71-11/12）。active blocker 区第一优先展示受影响 classroom/session 名单，只给 `查看受影响课堂` 与 `稍后重试`，不出现 queue / override（D-71-13/14/15/16）。</action>
  <acceptance_criteria>
    - UI 测试可断言 `查看升级预检` 为升级默认入口，而非 `开始升级`。
    - `src/components/surfaces/plugin-marketplace-detail-panel.tsx` 存在，且测试/源码可证明确由 `PluginMarketplaceSurface` 的 SSR DTO 驱动。
    - 升级 detail panel 中同时出现 `Backfill`、`Verify`、`Cutover` 三段标签和 verify-failure honesty copy。
    - cleanup detail 中同时出现真实 impact count 与 confirmation token；active blocker 细节先显示 classroom/session 名单，再显示 `查看受影响课堂` CTA。
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/components/surfaces/plugin-marketplace-surface.test.tsx -t "upgrade preflight|Backfill|Verify|Cutover|cleanup|confirmation token|active classroom"</automated>
    <automated>gitnexus_detect_changes()</automated>
  </verify>
  <done>Phase 71 所有 operator-facing lifecycle 交互都已在 `/settings/plugins` 单页内完成闭环，且 UI 诚实映射后端治理状态。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| marketplace SSR surface -> server actions | UI 只能消费后端治理 DTO，不能自行推断 destructive-op legality |
| inline reason rendering -> operator decisions | 错误/阻断 copy 必须准确映射具名 reason code，避免误导 operator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-04-1 | I | external card summary | mitigate | 治理摘要字段固定为 version/permissions/data model/namespace/source，先展示风险信息再展示 CTA，防止盲点安装 |
| T-71-04-2 | T | upgrade/uninstall UI controls | mitigate | 所有 destructive CTA 都先走 preflight/detail panel；verify failure、cleanup token、active blocker 都以内联明示，不允许“直接升级/直接 cleanup” |
| T-71-04-3 | R | active blocker explanation | mitigate | blocker 区先显示 affected classrooms/sessions 和后续动作，copy 直接来自后端 DTO，避免 UI 自造 override 语义 |
</threat_model>

<verification>
完成后，`/settings/plugins` 会成为唯一 external lifecycle 入口；SSR surface tests 可以证明 dual-section、preflight-first、retain-cleanup、recovery、active blocker 五类关键状态。</verification>

<success_criteria>
- `/settings/plugins` 达到 UI-SPEC 要求的同页双分区和治理优先排序。
- operator 在不离开本页的前提下即可完成 install / recover / upgrade preflight / uninstall retain-cleanup 决策。
- active classroom 下 destructive-op 只呈现“查看受影响课堂 + 稍后重试”的统一硬阻断策略。</success_criteria>

<output>
After completion, create `.planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-04-SUMMARY.md`
</output>
