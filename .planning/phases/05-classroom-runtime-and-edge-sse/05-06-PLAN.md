---
phase: 05-classroom-runtime-and-edge-sse
plan: 06
type: execute
wave: 5
depends_on:
  - 05-03
  - 05-04
  - 05-05
files_modified:
  - scripts/verify-phase5-classroom.ts
  - package.json
autonomous: true
requirements:
  - CLASS-01
  - CLASS-02
  - CLASS-03
  - CLASS-04
  - CLASS-05
  - CLASS-06
  - CLASS-07
must_haves:
  truths:
    - "One automated gate verifies schema, DAL, actions, Edge SSE, teacher UI, student UI, cache boundaries, and D-01 through D-16 decision coverage."
    - "Phase verification runs only after the blocking Drizzle schema push from Plan 01 has completed."
    - "Deferred ideas from 05-CONTEXT.md are absent from implementation source."
  artifacts:
    - path: scripts/verify-phase5-classroom.ts
      provides: "source-level classroom runtime regression gate"
      contains: "Phase 5 classroom verification passed"
    - path: package.json
      provides: "pnpm verify:phase5 script"
      contains: "verify:phase5"
  key_links:
    - from: package.json
      to: scripts/verify-phase5-classroom.ts
      via: "script command"
      pattern: "tsx scripts/verify-phase5-classroom.ts"
---

<objective>
Add the final automated verification gate for Phase 05 classroom runtime.

Purpose: Classroom runtime has multiple boundaries that can appear type-correct while violating security, cache, SSE, or decision semantics; a focused source gate prevents false-positive completion.
Output: `pnpm verify:phase5` command and verification script.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-classroom-runtime-and-edge-sse/05-CONTEXT.md
@.planning/phases/05-classroom-runtime-and-edge-sse/05-RESEARCH.md
@.planning/phases/05-classroom-runtime-and-edge-sse/05-PATTERNS.md
@scripts/verify-phase4-learning.ts
@package.json

<interfaces>
Follow `scripts/verify-phase4-learning.ts` style: read files with `existsSync`/`readFileSync`, strip line comments before deferred-scope and import checks, report failed labels, exit 1 on failure, and print `Phase 5 classroom verification passed` on success.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create classroom verification script</name>
  <read_first>
    - .planning/phases/05-classroom-runtime-and-edge-sse/05-CONTEXT.md
    - .planning/phases/05-classroom-runtime-and-edge-sse/05-PATTERNS.md
    - scripts/verify-phase4-learning.ts
    - src/db/schema.ts
    - src/lib/dto/classroom.ts
    - src/lib/dal/classroom.ts
    - src/actions/classroom-actions.ts
    - src/app/api/classroom/[sessionId]/events/route.ts
    - src/components/surfaces/player-surface.tsx
    - src/components/surfaces/classroom-console-surface.tsx
  </read_first>
  <files>scripts/verify-phase5-classroom.ts</files>
  <action>Create `scripts/verify-phase5-classroom.ts`. Checks must cover: schema exports classroom tables, cascade deletes, session version, locked, activeStepId, participant and event indexes; DTO exports classroom schemas and D-01 through D-16 required fields/copy; DAL starts with server-only, uses `assertActiveTeacher`, participant auth, `db.transaction`, `expectedVersion`, `VERSION_CONFLICT`, and sanitized DTO parsing; Server Actions start with `"use server"`, use `safeParse`, and call `updateTag(cacheTags.classroom`; snapshot route uses `getClassroomSnapshotDTO`, `no-store`, and no `updateTag`; Edge SSE route exports `runtime = "edge"`, has `text/event-stream`, `event: snapshot`, keepalive, `ClassroomSnapshotDTOSchema`, and no DB/DAL/Auth imports; teacher UI has launch, roster, conflict, refresh, and required Chinese copy; student UI has locked disabled copy, unlocked recommendation copy, EventSource cleanup, snapshot-before-jump fetch, and `aria-live`; cached shell function does not reference classroom runtime; deferred tokens `multi-class merged`, `WebSocket`, `gradebook`, `AI classroom control`, `notification center`, and `ad hoc student` are absent outside comments. Use comment-stripped checks where absence matters so grep gates are not self-invalidating.</action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Script contains `Phase 5 classroom verification passed`.
    - Script contains checks for all exact decision IDs or decision behaviors D-01 through D-16 through copy/field/action assertions.
    - Script checks Edge SSE route has no imports matching `@/db`, `@/db/schema`, `drizzle-orm`, `@/lib/dal`, or `@/auth` outside comments.
    - Script checks cached shell function avoids classroom runtime reads.
    - Script checks no deferred-scope tokens outside comments.
  </acceptance_criteria>
  <done>Phase 05 has a deterministic verification script that fails on missing classroom runtime, security, caching, SSE, UI, or deferred-scope invariants.</done>
</task>

<task type="auto">
  <name>Task 2: Wire and run final Phase 05 verification</name>
  <read_first>
    - package.json
    - scripts/verify-phase5-classroom.ts
    - .planning/phases/05-classroom-runtime-and-edge-sse/05-01-SUMMARY.md
    - .planning/phases/05-classroom-runtime-and-edge-sse/05-02-SUMMARY.md
    - .planning/phases/05-classroom-runtime-and-edge-sse/05-03-SUMMARY.md
    - .planning/phases/05-classroom-runtime-and-edge-sse/05-04-SUMMARY.md
    - .planning/phases/05-classroom-runtime-and-edge-sse/05-05-SUMMARY.md
  </read_first>
  <files>package.json, scripts/verify-phase5-classroom.ts</files>
  <action>Add package script `"verify:phase5": "tsx scripts/verify-phase5-classroom.ts"`. Run verification in this order: confirm Plan 01 summary reports successful `npx drizzle-kit push`, run `pnpm typecheck`, then run `pnpm verify:phase5`. If Plan 01 schema push did not complete, stop and run `npx drizzle-kit push` before typecheck and `pnpm verify:phase5`; do not accept a typecheck-only pass as phase verification.</action>
  <verify>
    <automated>pnpm typecheck && pnpm verify:phase5</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains exact script key `verify:phase5` with value `tsx scripts/verify-phase5-classroom.ts`.
    - `pnpm typecheck` passes.
    - `pnpm verify:phase5` prints `Phase 5 classroom verification passed`.
    - Verification is run after the blocking schema push task from `05-01-PLAN.md` has passed.
  </acceptance_criteria>
  <done>Phase 05 exposes and passes one command that validates classroom runtime across schema, DAL, Server Actions, SSE, teacher UI, student UI, security, and decision coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification script -> source tree | Verification must detect security and behavior regressions without executing unsafe flows. |
| Package script -> developer workflow | A missing script can let phase completion skip classroom-specific checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | `scripts/verify-phase5-classroom.ts` | mitigate | Source-scan critical files for exact schema, authz, version, SSE, and UI invariants. |
| T-05-06-02 | Information Disclosure | Verification gates | mitigate | Check Edge route and UI files for forbidden DB/DAL/Auth imports. |
| T-05-06-03 | Denial of Service | SSE verification | mitigate | Assert bounded named polling interval and no in-memory authoritative session store. |
| T-05-06-04 | Repudiation | Teacher conflict recovery | mitigate | Assert conflict copy, preserved attempted action, and no automatic replay path. |
| T-05-06-05 | Tampering | Draft/submission preservation | mitigate | Assert SSE client does not own task/quiz Server Actions or draft payload state. |
</threat_model>

<verification>
Run `pnpm typecheck && pnpm verify:phase5` after `npx drizzle-kit push` has completed.
</verification>

<success_criteria>
`pnpm verify:phase5` passes and confirms CLASS-01 through CLASS-07, D-01 through D-16, security boundaries, schema push dependency, SSE constraints, stale conflict recovery, reconnect fallback, and deferred-scope exclusions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-classroom-runtime-and-edge-sse/05-06-SUMMARY.md`.
</output>
