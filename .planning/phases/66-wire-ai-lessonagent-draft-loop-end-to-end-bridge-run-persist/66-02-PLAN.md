---
phase: 66-wire-ai-lessonagent-draft-loop-end-to-end-bridge-run-persist
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/ai/agents/lesson-agent.ts
  - src/server/ai/agents/lesson-agent.test.ts
  - src/features/platform-core/commands/handlers/lesson-draft.persist.test.ts
autonomous: true
requirements: [DRAFT-01, DRAFT-02]
must_haves:
  truths:
    - "A successful lesson.draft.run is followed by a lesson.draft.persist dispatch carrying the produced step"
    - "draftLessonStep returns the persisted draft identity (draftVersionId, version) to callers"
    - "Persist reuses the run correlationId so run→persist is one correlated unit"
    - "A duplicate persist dispatch with the SAME sourceCommandId is idempotent (no double-write, no duplicate draft row / event)"
  artifacts:
    - path: "src/server/ai/agents/lesson-agent.ts"
      provides: "run→persist orchestration bridge"
      contains: "lesson.draft.persist"
  key_links:
    - from: "lesson.draft.run resultSummary.step"
      to: "lesson.draft.persist payload.steps"
      via: "wrap single step as [step]"
      pattern: "steps: \\[step\\]"
---

<objective>
Bridge the LessonAgent orchestration entry so a successful `lesson.draft.run` is followed by a
`lesson.draft.persist` dispatch — closing the gap where the agent produces a step but never writes
a draft version. Per locked decision D-01: sequential dispatch at the orchestration entry (NOT nested
dispatch inside the run handler), sharing one correlationId. The run resultSummary returns a single
`step`; the persist payload schema requires `steps: []` (min 1), so wrap as `[step]`.

Purpose: DRAFT-01 (agent draft persists to a draft lesson version via Command Bus) and DRAFT-02
(idempotent / replay-safe persist) require the run output to actually reach the persist command path.
Output: draftLessonStep dispatches run then persist, returns the persisted draft identity.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/66-wire-ai-lessonagent-draft-loop-end-to-end-bridge-run-persist/66-CONTEXT.md
@.planning/phases/66-wire-ai-lessonagent-draft-loop-end-to-end-bridge-run-persist/66-PATTERNS.md

<interfaces>
src/server/ai/agents/lesson-agent.ts (existing):
```typescript
const LESSON_AGENT_PLUGIN_ID = "core.lesson-agent";
const LESSON_AGENT_SYSTEM_ACTOR = { actorId: LESSON_AGENT_PLUGIN_ID, actorScope: "system" };
// draftLessonStep builds a lesson.draft.run envelope, dispatches via dispatchPlatformCommand,
// then: const step = (result.resultSummary?.step as LessonStepPayload|undefined) ?? null;
// returns { status, commandId, step };
// deps?: Pick<PlatformCommandBusDependencies,"store"|"persistPlatformEvents"|"publicationPort">
```

contracts.ts — persist payload schema (strict):
```typescript
const LessonDraftPersistPayloadSchema = z.object({
  lessonId: z.string().min(1),
  steps: z.array(lessonStepPayloadSchema).min(1),
}).strict();
// teacherId/source NEVER in payload — persist handler injects via assertActiveTeacher closure.
```

persist handler (handlers/lesson-draft.ts): returns resultSummary `{ draftVersionId, version, stepCount }`
and emits `lesson.draft.persisted` with `source:"ai"`, dedupe via sourceCommandId.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing bridge test (RED)</name>
  <files>src/server/ai/agents/lesson-agent.test.ts</files>
  <behavior>
    - When run succeeds with a non-null step, draftLessonStep dispatches a second command of type lesson.draft.persist
    - persist payload.steps === [step] (the single run step wrapped in an array)
    - persist envelope reuses the run correlationId (one correlated unit)
    - When run yields null step (failure), NO persist dispatch occurs
    - draftLessonStep return includes draftVersionId + version from the persist result
  </behavior>
  <action>Using the existing in-memory deps injection point (store + captured persistPlatformEvents),
  extend lesson-agent.test.ts to capture dispatched commands. Assert the two-command run→persist sequence,
  the `[step]` wrap, shared correlationId, the no-step short-circuit, and the returned draft identity. Run RED.</action>
  <verify>
    <automated>npx vitest run src/server/ai/agents/lesson-agent.test.ts</automated>
  </verify>
  <done>New assertions fail (no persist dispatch exists yet).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add run→persist bridge dispatch (GREEN)</name>
  <files>src/server/ai/agents/lesson-agent.ts</files>
  <behavior>
    - After run dispatch resolves with step !== null, build a lesson.draft.persist envelope
    - payload: { lessonId, steps: [step] } (.strict-compatible — no teacherId/source)
    - reuse the run correlationId; persist command id derived from same correlationId
    - dispatch via the same dispatchPlatformCommand + deps; return { status, commandId, step, draftVersionId, version }
    - if step === null, skip persist and return as today (failure passthrough, D-53-08)
  </behavior>
  <action>In draftLessonStep, after obtaining `step`, if `step !== null` construct the persist envelope
  (actor = same system actor, scope = { schoolId, pluginId }, correlation reusing the run correlationId as
  causation/correlation per producer convention) and dispatch it through the same `dispatchPlatformCommand`
  with the same `deps`. Wrap the single step as `steps: [step]`. Extract `draftVersionId`/`version` from the
  persist `result.resultSummary` and include in the return. Keep the run handler PURE — do NOT add nested
  dispatch inside the handler. teacherId/source NEVER enter the payload (handler closure injects).</action>
  <verify>
    <automated>npx vitest run src/server/ai/agents/lesson-agent.test.ts</automated>
  </verify>
  <done>Two-command run→persist sequence dispatched with [step] wrap + shared correlationId; null-step short-circuits; draft identity returned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Assert persist idempotency on same sourceCommandId (DRAFT-02)</name>
  <files>src/features/platform-core/commands/handlers/lesson-draft.persist.test.ts</files>
  <read_first>
    - src/features/platform-core/commands/handlers/lesson-draft.persist.test.ts — already has
      describe("幂等双层：dedupe 短路 + pending 重放兜底") with 用例 A (same dedupeKey short-circuit) and
      用例 B (same command.id → DAL unique-constraint (lessonId, sourceCommandId)). Extend, do not duplicate.
    - src/features/platform-core/commands/handlers/lesson-draft.ts — persist handler: sourceCommandId =
      command.id; emits lesson.draft.persisted.
  </read_first>
  <behavior>
    - A second persist dispatch carrying the SAME sourceCommandId does NOT cause a second
      persistDraftLessonVersion write (DAL invoked exactly once for the duplicate path) — no duplicate draft row
    - The duplicate dispatch emits NO second lesson.draft.persisted event (no duplicate event in the ledger)
    - The replay-safe path resolves to the SAME draftVersionId as the first dispatch
  </behavior>
  <action>Extend the existing idempotency describe block in lesson-draft.persist.test.ts. The dedupe
  short-circuit (用例 A) and unique-constraint replay (用例 B) already exist — add an explicit assertion
  that for a same-sourceCommandId duplicate dispatch: (1) persistDraftLessonVersion is NOT called a second
  time on the deduped path (or, on the pending-replay path, no new draftLessonVersions row is created), AND
  (2) no duplicate lesson.draft.persisted event is emitted, AND (3) the returned draftVersionId matches the
  first dispatch. Reuse the existing in-memory command store + emittedEvents capture in the file. Do NOT
  weaken or remove 用例 A / 用例 B. This task does not touch lesson-agent.ts and is independent of Tasks 1–2.</action>
  <verify>
    <automated>npx vitest run src/features/platform-core/commands/handlers/lesson-draft.persist.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/features/platform-core/commands/handlers/lesson-draft.persist.test.ts` exits 0
    - `grep -c "sourceCommandId" src/features/platform-core/commands/handlers/lesson-draft.persist.test.ts` >= 1
    - spec asserts the duplicate path emits no second lesson.draft.persisted event and creates no second draft row
  </acceptance_criteria>
  <done>Idempotent re-dispatch on same sourceCommandId asserted: no double-write, no duplicate draft row, no duplicate event; same draftVersionId returned; existing 用例 A/B preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool output → persist command | agent-produced step crosses into a write-type command |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-04 | Tampering | persist payload (LLM step) | mitigate | persist payload validated by `LessonDraftPersistPayloadSchema.strict()` (lessonStepPayloadSchema) at dispatch entry; malformed LLM step rejected before write |
| T-66-05 | Elevation of privilege | teacherId injection | mitigate | teacherId/source NEVER in payload; persist handler injects via assertActiveTeacher closure — bridge cannot forge author |
| T-66-06 | Denial of service (replay) | duplicate persist | mitigate | persist dedupes on sourceCommandId (D-02 replay-safe); shared correlationId keeps retries idempotent |
</threat_model>

<verification>
- `npx vitest run src/server/ai/agents/lesson-agent.test.ts` green
- `npx vitest run src/features/platform-core/commands/handlers/lesson-draft.persist.test.ts` green (incl. same-sourceCommandId idempotency)
- `grep -n "lesson.draft.persist" src/server/ai/agents/lesson-agent.ts` shows the new dispatch
- run handler unchanged (no nested dispatch): `grep -c "dispatch" handlers/lesson-draft.ts` unchanged from baseline
</verification>

<success_criteria>
draftLessonStep produces a correlated run→persist sequence with `[step]` wrapping; failure (null step)
short-circuits without persist; persisted draft identity returned; run handler stays pure; a duplicate
persist with the same sourceCommandId is idempotent (no double-write, no duplicate draft row / event).
</success_criteria>

<output>
After completion, create `.planning/phases/66-wire-ai-lessonagent-draft-loop-end-to-end-bridge-run-persist/66-02-SUMMARY.md`
</output>
