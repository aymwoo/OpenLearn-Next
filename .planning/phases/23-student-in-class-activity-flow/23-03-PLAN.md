---
phase: 23-student-in-class-activity-flow
plan: 03
type: execute
wave: 3
depends_on:
  - 23-01
  - 23-02
files_modified:
  - src/components/learning/student-step-cards.test.ts
  - src/components/surfaces/student-player-surfaces.test.ts
  - src/lib/dal/learning.test.ts
  - src/actions/classroom-actions.test.ts
  - scripts/verify-phase23-student-activity.ts
  - package.json
autonomous: true
requirements:
  - ACT-01
  - ACT-02
must_haves:
  truths:
    - Phase 23 有专属 verifier，能同时守住 player cache boundary、统一活动壳和 quick-response durable 写链路。
    - Focused regression 需要覆盖 runtime client、player surface、DAL split read 和 classroom action wiring，而不是只靠人工检查。
    - 后续 Phase 24-26 即使继续改 classroom runtime，也不能回退学生端的 append-only quick-response 语义或 Suspense player boundary。
  artifacts:
    - path: scripts/verify-phase23-student-activity.ts
      provides: dedicated phase verifier for student activity flow and quick-response durability
      contains: verify:phase23
    - path: package.json
      provides: package command for phase verification
      contains: verify:phase23
    - path: src/components/learning/student-step-cards.test.ts
      provides: focused player runtime and step-card regression coverage
      contains: 提交课堂回应
  key_links:
    - from: scripts/verify-phase23-student-activity.ts
      to: src/app/(student)/student/player/page.tsx
      via: verifier locks the shell plus Suspense plus personal-state split
      pattern: Suspense|getStudentPlayerShellDTO|getStudentPlayerPersonalDTO
    - from: scripts/verify-phase23-student-activity.ts
      to: src/actions/classroom-actions.ts
      via: verifier locks quick-response writes to the classroom evidence action path
      pattern: recordClassroomEvidence|student-quick-response
---

<objective>
为 Phase 23 增加 focused regression 和 `verify:phase23` 守卫，确保学生活动流、课堂回应持久化、
以及 `/student/player` 的缓存与运行时边界在后续阶段中不会悄悄回退。

Purpose: 让 Phase 23 的结果具备可重复验证的安全网，而不是依赖上下文记忆。
Output: updated tests, `verify:phase23` script, package command, static/runtime guards.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-student-in-class-activity-flow/23-CONTEXT.md
@.planning/phases/23-student-in-class-activity-flow/23-RESEARCH.md
@src/app/(student)/student/player/page.tsx
@src/components/learning/student-step-cards.test.ts
@src/components/surfaces/student-player-surfaces.test.ts
@src/lib/dal/learning.test.ts
@src/actions/classroom-actions.test.ts
@scripts/verify-phase21-contracts.ts
@scripts/verify-phase22-orchestration.ts
@package.json

<interfaces>
Phase 23 invariants to lock:
```ts
Suspense
getStudentPlayerShellDTO
getStudentPlayerPersonalDTO
activityGuidance
expectedOutput
提交课堂回应
student-quick-response
recordClassroomEvidence
```

Verifier pattern already used in the repo:
```ts
type StaticCheck = { label: string; passed: boolean }
runPnpm(["test", "--run", ...], "phase 23 focused regression suite")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand focused regression coverage for player guidance, quick response, and DAL boundaries</name>
  <files>src/components/learning/student-step-cards.test.ts, src/components/surfaces/student-player-surfaces.test.ts, src/lib/dal/learning.test.ts, src/actions/classroom-actions.test.ts</files>
  <read_first>src/components/learning/student-step-cards.test.ts, src/components/surfaces/student-player-surfaces.test.ts, src/lib/dal/learning.test.ts, src/actions/classroom-actions.test.ts, src/components/learning/classroom-runtime-client.tsx, src/components/learning/quick-response-step-card.tsx, src/actions/classroom-actions.ts, src/lib/dal/learning.ts</read_first>
  <behavior>
    - Test 1: student step-card/runtime coverage asserts unified activity guidance and quick-response wiring instead of only generic task/quiz presence.
    - Test 2: player surface coverage still proves the Suspense shell/personal split survives Phase 23.
    - Test 3: DAL and action tests assert quick-response reads and writes remain separate from task/quiz attempts and continue to use classroom evidence.
  </behavior>
  <action>Update the focused test files to cover the new Phase 23 contracts. In `src/components/learning/student-step-cards.test.ts`, assert the runtime now includes unified activity guidance fields and the quick-response card with the exact primary CTA `提交课堂回应`. In `src/components/surfaces/student-player-surfaces.test.ts`, keep the shell/personal split assertions and add assertions for the activity-oriented player language. In `src/lib/dal/learning.test.ts`, add coverage for the new activity guidance fields and quick-response latest/history read model while preserving existing cache-boundary assertions. In `src/actions/classroom-actions.test.ts`, add assertions that quick-response submission still routes through classroom evidence rather than task or quiz submission helpers. Keep these tests focused and grep-verifiable; do not replace them with broad opaque snapshots.</action>
  <acceptance_criteria>
    - `src/components/learning/student-step-cards.test.ts` contains assertions for `提交课堂回应` and unified activity guidance wording.
    - `src/components/surfaces/student-player-surfaces.test.ts` still contains `Suspense`, `getStudentPlayerShellDTO`, and `getStudentPlayerPersonalDTO` assertions and adds Phase 23 activity wording coverage.
    - `src/lib/dal/learning.test.ts` contains assertions for the new activity guidance or quick-response read model.
    - `src/actions/classroom-actions.test.ts` contains assertions that quick-response wiring uses classroom evidence rather than task/quiz submission actions.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/learning/student-step-cards.test.ts src/components/surfaces/student-player-surfaces.test.ts src/lib/dal/learning.test.ts src/actions/classroom-actions.test.ts</automated>
  </verify>
  <done>Phase 23 behavior is protected by focused tests across player runtime, DTO boundaries, and classroom action wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `verify:phase23` to guard student activity flow and quick-response durability</name>
  <files>scripts/verify-phase23-student-activity.ts, package.json</files>
  <read_first>scripts/verify-phase21-contracts.ts, scripts/verify-phase22-orchestration.ts, src/app/(student)/student/player/page.tsx, src/components/learning/classroom-runtime-client.tsx, src/components/learning/quick-response-step-card.tsx, src/actions/classroom-actions.ts, package.json</read_first>
  <behavior>
    - Test 1: verifier fails if `/student/player` loses the split shell plus Suspense personal-state model.
    - Test 2: verifier fails if quick-response UI or action wiring stops using the classroom evidence path.
    - Test 3: verifier fails if the focused Phase 23 regression suite fails.
  </behavior>
  <action>Create `scripts/verify-phase23-student-activity.ts` and register `verify:phase23` in `package.json`, following the repo's existing phase verifier pattern. The static checks must cover at least these items: (1) `src/app/(student)/student/player/page.tsx` still contains `Suspense`, `getStudentPlayerShellDTO`, `getStudentPlayerPersonalDTO`, and `PlayerPersonalRegion`; (2) `src/components/learning/classroom-runtime-client.tsx` still contains `TaskStepCard`, `QuizStepCard`, and quick-response rendering with classroom activity guidance; (3) `src/components/learning/quick-response-step-card.tsx` contains `提交课堂回应` and append-only history wording; (4) `src/actions/classroom-actions.ts` contains the quick-response action and `recordClassroomEvidence`; (5) quick-response wiring contains `student-quick-response`; and (6) package scripts expose `verify:phase23`. Then run the focused Phase 23 test files from the verifier. Keep the verifier concrete and source-accurate; do not degrade it into a prose checklist.</action>
  <acceptance_criteria>
    - `scripts/verify-phase23-student-activity.ts` exists and contains checks for `Suspense`, `getStudentPlayerShellDTO`, `getStudentPlayerPersonalDTO`, `提交课堂回应`, and `student-quick-response`.
    - `scripts/verify-phase23-student-activity.ts` runs `pnpm test --run` against the focused Phase 23 regression files.
    - `package.json` contains `verify:phase23` pointing to `tsx scripts/verify-phase23-student-activity.ts`.
    - the verifier contains at least one guard proving quick-response writes still depend on `recordClassroomEvidence`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase23</automated>
  </verify>
  <done>Phase 23 has a dedicated verification entry point that will catch regressions in player activity UX, cache boundaries, and quick-response durability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository source -> phase verifier | verifier decides whether Phase 23 activity and durability invariants still hold |
| player runtime and action layer -> focused tests | regressions must be caught by code-level checks rather than manual visual inspection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-09 | T | `scripts/verify-phase23-student-activity.ts` | mitigate | Statically check cache boundary, quick-response UI strings, and classroom evidence wiring so contract drift fails fast. |
| T-23-10 | D | `src/app/(student)/student/player/page.tsx` | mitigate | Fail verification if the player stops using Suspense or merges shell/personal reads. |
| T-23-11 | E | `src/actions/classroom-actions.ts` | mitigate | Guard the quick-response path so it continues to depend on `recordClassroomEvidence` and student-scoped classroom auth. |
| T-23-12 | R | focused regressions | accept | Test suites read source and component behavior only and do not expose sensitive classroom data. |
</threat_model>

<verification>
- `pnpm test --run src/components/learning/student-step-cards.test.ts src/components/surfaces/student-player-surfaces.test.ts src/lib/dal/learning.test.ts src/actions/classroom-actions.test.ts`
- `pnpm verify:phase23`
</verification>

<success_criteria>
- Phase 23 has focused regression coverage for student activity flow and quick-response durability.
- Phase 23 has a dedicated verification command.
- future classroom/runtime phases cannot silently break the player cache boundary or append-only quick-response path.
</success_criteria>

<output>
After completion, create `.planning/phases/23-student-in-class-activity-flow/23-03-SUMMARY.md`
</output>
