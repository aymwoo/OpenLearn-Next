---
phase: 67-declarative-plugin-owned-data-model-migration-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/dto/plugin-data-model.ts
  - src/lib/dto/plugin-data-model.test.ts
  - plugins/quiz-sample/data-model.ts
requirements: [DATA-01]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "A pure, non-server-only Zod meta-schema validates a declarative dataModel (tables/columns/types/constraints) and is runnable directly under vitest with no DB and no server-only import."
    - "A legal quiz dataModel declaration (question + response tables) parses successfully and becomes the single source of truth consumed by both the happy-path test and the Phase 2 compiler."
    - "Five illegal declarations are rejected AT THE BOUNDARY, each asserting a SPECIFIC reject reason: raw SQL/DDL string, missing plugin_owned_ prefix, FK to a core table, missing schoolId scope, json/blob column type."
    - "Column types are restricted to exactly text/integer/boolean/timestamp/enum; json and blob are unrepresentable; every column carries type+notNull, enum carries a named values array."
    - "package.json exposes db:generate (drizzle-kit generate) and plugin:compile script entries; semver + drizzle-zod + @types/semver are installed."
  artifacts:
    - path: "src/lib/dto/plugin-data-model.ts"
      provides: "PluginDataModelSchema meta-schema + UPPER_SNAKE reject-reason constants + inferred TS types"
      contains: "PluginDataModelSchema"
      min_lines: 60
    - path: "src/lib/dto/plugin-data-model.test.ts"
      provides: "1 legal + 5 illegal negative-sample suite, each asserting a specific reject reason"
      contains: "RAW_SQL_FORBIDDEN"
    - path: "plugins/quiz-sample/data-model.ts"
      provides: "Single legal quiz dataModel declaration (question + response tables) — compiler input source of truth"
      contains: "plugin_owned_quiz_responses"
  key_links:
    - from: "src/lib/dto/plugin-data-model.test.ts"
      to: "plugins/quiz-sample/data-model.ts"
      via: "import the legal sample for the happy-path parse assertion"
      pattern: "quiz-sample/data-model"
    - from: "src/lib/dto/plugin-data-model.ts"
      to: "column type whitelist"
      via: "z.enum of exactly 5 scalar types rejects json/blob at boundary"
      pattern: "text.*integer.*boolean.*timestamp.*enum"
---

<objective>
建立声明式 `dataModel` 的契约与校验地基（DATA-01）：一个纯 DTO 层 Zod meta-schema（无 `server-only`、可 vitest 直跑）、其 1 合法 + 5 非法负样本测试集、以及一份**合法 quiz 样板声明**作为后续编译器与测试共用的单一真相源。同时把本 phase 的依赖与脚本入口（`db:generate` / `plugin:compile`）就位（VALIDATION.md Wave 0 基建）。

Purpose: meta-schema 是整条「compile, don't execute」流水线的**安全边界**——它在声明进入编译器之前就拒绝裸 SQL/DDL、缺前缀、向 core 加 FK、缺 schoolId、json/blob 列。把它做成纯函数 DTO（复用 `resource-ai.ts` 的 `superRefine` 范式）使负样本可秒级单测，是 DATA-01 的验收抓手。
Output: `src/lib/dto/plugin-data-model.ts` + 其测试 + `plugins/quiz-sample/data-model.ts` + package.json 脚本/依赖就位。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/67-declarative-plugin-owned-data-model-migration-proof/67-CONTEXT.md
@.planning/phases/67-declarative-plugin-owned-data-model-migration-proof/67-RESEARCH.md
@.planning/phases/67-declarative-plugin-owned-data-model-migration-proof/67-PATTERNS.md
@.planning/phases/67-declarative-plugin-owned-data-model-migration-proof/67-VALIDATION.md

<interfaces>
<!-- Analog Zod superRefine 范式 (src/lib/dto/resource-ai.ts:671-691) — 抄文件头/superRefine/export type 形态。 -->
import { z } from "zod";
export const PluginManifestSchema = z.object({ ... }).superRefine((m, ctx) => {
  if (cond) ctx.addIssue({ code: z.ZodIssueCode.custom, message: "REASON_CONST", path: ["x"] });
});
export type PluginManifest = z.infer<typeof PluginManifestSchema>;

<!-- 拒因常量风格 analog (src/lib/dto/draft-guardrails.ts) — UPPER_SNAKE message 常量 + z.enum.options 顺序可断言。 -->

<!-- 锁定约束 (CONTEXT D-01..D-12)：
  COLUMN_TYPES = ["text","integer","boolean","timestamp","enum"] as const   // D-01 白名单; z.enum 边界拒 json/blob
  OWNED_TABLE_PREFIX = "plugin_owned_"                                       // D-10
  每层 object .strict()  -> 拒未知键 (含夹带的 sql/ddl/foreignKeys 键 = 负样本#1/#3)
  每列强制 { type, notNull }，可选标量 default；type==="enum" 时 enumValues 必填   // D-03
  table superRefine 强制 columns 含 schoolId 且 notNull                      // D-11 = 负样本#4
  扫所有字符串值命中 /\b(CREATE|ALTER|DROP)\b/i -> RAW_SQL_FORBIDDEN          // 负样本#1
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 67-01-01: 安装依赖 + package.json 脚本入口</name>
  <files>package.json</files>
  <read_first>
    - package.json (lines 44-73) — `verify:phaseNN` / `verify:phase` 别名 / `db:migrate` 现状；新脚本须对齐风格。
  </read_first>
  <action>
    1. 安装研究核验过的两个新库 + 类型（RESEARCH §Standard Stack，版本已 npm registry 核验）：
       `pnpm add semver@^7.8.1 drizzle-zod@^0.8.3` 然后 `pnpm add -D @types/semver`。
       说明：本 phase 仅写 `dataVersion=1` 基线（semver 比较逻辑在 Phase 71 消费）、drizzle-zod 供 Phase 68 表↔Zod 同源；本 phase 装入即满足「依赖就位」，最小使用。**禁止** ajv / knex / umzug / 任何运行时 schema 引擎（红线）。
    2. 在 package.json scripts 块新增（值用真实命令，目标脚本将在 67-02 创建——本 plan 不调用它们）：
       - `"db:generate": "drizzle-kit generate"`  (drizzle-kit 自读 drizzle.config.ts；RESEARCH Open Q#3)
       - `"plugin:compile": "tsx scripts/compile-plugin-data-model.ts"`  (编译器只依赖纯 DTO，无 server-only，裸 tsx)
    3. **不要**在本 plan 改 `verify:phase` 别名（仍指 `verify:phase65`）——重指 `verify:phase67` 留待 67-03（verify 脚本存在后），避免中途 `pnpm verify:phase` 断裂。
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');if(!p.scripts['db:generate']||!p.scripts['plugin:compile'])process.exit(1);if(!p.dependencies.semver||!p.dependencies['drizzle-zod']||!p.devDependencies['@types/semver'])process.exit(1);console.log('ok')"</automated>
  </verify>
  <done>semver/drizzle-zod/@types/semver 已装；package.json 含 `db:generate` 与 `plugin:compile`；`verify:phase` 暂未改（仍 verify:phase65）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 67-01-02: dataModel Zod meta-schema + 5 类拒因 + 合法 quiz 样板声明</name>
  <files>src/lib/dto/plugin-data-model.ts, src/lib/dto/plugin-data-model.test.ts, plugins/quiz-sample/data-model.ts</files>
  <read_first>
    - src/lib/dto/resource-ai.ts (lines 671-710) — `PluginManifestSchema` superRefine + `export type = z.infer` 范式（逐字抄文件头/结构）。
    - src/lib/dto/draft-guardrails.ts + draft-guardrails.test.ts (lines 1-43) — UPPER_SNAKE 拒因常量 + `z.enum(...).options` 顺序断言 + 中文 describe/it + 1 happy + N negative 风格。
    - src/db/schema.ts (lines 1883-1903 pluginOwnedBusinessData; 709-736 quizAttempts) — 样板声明须映射到的目标物理表形态（列/scope/索引语义）。
  </read_first>
  <behavior>
    - 合法 quiz 样板声明（question + response 两表）→ `PluginDataModelSchema.parse` 成功。
    - 非法#1 夹带 raw SQL/DDL（任一字符串值含 CREATE/ALTER/DROP，或额外 `sql`/`ddl` 键）→ 拒，reason `RAW_SQL_FORBIDDEN`（或 `.strict()` unknown-key，二选一明确断言）。
    - 非法#2 表名缺 `plugin_owned_` 前缀 → 拒，reason `MISSING_OWNED_PREFIX`。
    - 非法#3 声明对 core 表的 FK（出现 `foreignKeys`/`references` 键，schoolId→schools 之外）→ 拒，`.strict()` unknown-key（断言 issue.code === "unrecognized_keys"）。
    - 非法#4 缺 schoolId scope 列 → 拒，reason `MISSING_SCHOOL_SCOPE`。
    - 非法#5 出现 `json`/`blob` 列类型 → 拒，reason `INVALID_COLUMN_TYPE`（z.enum 白名单边界拒；断言 issue path 指向 type）。
  </behavior>
  <action>
    创建 `src/lib/dto/plugin-data-model.ts`（纯 DTO，**无 `import "server-only"`**，仅 `import { z } from "zod"`）：
    - `export const COLUMN_TYPES = ["text","integer","boolean","timestamp","enum"] as const;`（D-01 白名单——`json`/`blob` 不在内，z.enum 边界即拒 = 负样本#5 `INVALID_COLUMN_TYPE`）。
    - `export const OWNED_TABLE_PREFIX = "plugin_owned_";`（D-10）。
    - `export const PLUGIN_DATA_MODEL_REASONS = ["INVALID_COLUMN_TYPE","MISSING_OWNED_PREFIX","RAW_SQL_FORBIDDEN","MISSING_SCHOOL_SCOPE","ENUM_REQUIRES_VALUES"] as const;`（拒因常量，供测试逐一断言；顺序固定）。
    - `ColumnSpecSchema = z.object({ name: z.string().regex(/^[a-z][a-z0-9_]*$/), type: z.enum(COLUMN_TYPES), notNull: z.boolean(), default: z.union([z.string(),z.number(),z.boolean()]).optional(), enumValues: z.array(z.string()).min(1).optional() }).strict().superRefine(...)` — enum 缺 values → `ENUM_REQUIRES_VALUES`。`.strict()` 拒未知键（含夹带的 `sql`/`json`/`mode` 键 → 负样本#1/#3/#5 加固）。
    - `IndexSpecSchema` / `UniqueSpecSchema`（声明式约束，DATA-01「约束」）：`z.object({ columns: z.array(z.string()).min(1) }).strict()`，table superRefine 校验所引用列均在 columns 中（防悬空列引用）。
    - `TableSpecSchema = z.object({ name: z.string().startsWith(OWNED_TABLE_PREFIX, "MISSING_OWNED_PREFIX"), columns: z.array(ColumnSpecSchema).min(1), indexes: z.array(IndexSpecSchema).optional(), uniques: z.array(UniqueSpecSchema).optional() }).strict().superRefine((t,ctx)=>{ ... })`：
        · columns 必含 `schoolId` 且 `notNull` → 否则 `MISSING_SCHOOL_SCOPE`（负样本#4，path:["columns"]）。
        · 扫该表所有字符串值（列名/默认值/enum 值/索引列名/表名）命中 `/\b(CREATE|ALTER|DROP)\b/i` → `RAW_SQL_FORBIDDEN`（负样本#1，path:["name"] 或对应 path）。
        · index/unique 引用的列必须存在于 columns。
    - `export const PluginDataModelSchema = z.object({ pluginKey: z.string().regex(/^[a-z][a-z0-9_]*$/), tables: z.array(TableSpecSchema).min(1) }).strict();`
    - 末尾 `export type PluginDataModel = z.infer<typeof PluginDataModelSchema>;` 及 `TableSpec`/`ColumnSpec` 类型。
    注意：**声明里不允许任何 `foreignKeys`/`references` 字段**——`.strict()` 自动拒（负样本#3）；schoolId→schools 与 pluginId→pluginRegistrations 的 FK 由编译器固定输出（67-02），不在声明面表达（D-11）。

    创建 `plugins/quiz-sample/data-model.ts`（合法样板，编译器与测试共用单一真相源；纯对象常量，无 DDL 字符串）：
    - `export const quizDataModel = { pluginKey: "quiz", tables: [ ... ] } satisfies ... }`（用 `satisfies PluginDataModel` 或留 `as const` 后由编译器/测试 parse 校验）。
    - 至少两表（满足 ROADMAP SC1「question/response 表」）：
        · `plugin_owned_quiz_questions`：columns `schoolId(text,notNull)`、`classroomSession(text,notNull)`、`question(text,notNull)`、`prompt(text,notNull)`、`correctOption(enum,notNull,enumValues:["A","B","C","D"])`；index columns `["schoolId","classroomSession","question"]`。
        · `plugin_owned_quiz_responses`：columns `schoolId(text,notNull)`、`classroomSession(text,notNull)`、`student(text,notNull)`、`question(text,notNull)`、`selectedOption(enum,notNull,enumValues:["A","B","C","D"])`；**index columns `["schoolId","classroomSession","student","question"]`（D-12 复合索引列序）**；**uniques columns `["classroomSession","student","question"]`（D-12 去重唯一约束）**。
      （`id`/`pluginId`/`isLatest`/`createdAt`/`updatedAt` 不在声明面——由编译器固定注入，见 67-02。）

    创建 `src/lib/dto/plugin-data-model.test.ts`（`import { describe, expect, it } from "vitest"`，中文文案）：
    - happy-path：`import { quizDataModel } from "../../../plugins/quiz-sample/data-model"`（或经 path alias），`expect(()=>PluginDataModelSchema.parse(quizDataModel)).not.toThrow()`。
    - `expect([...PLUGIN_DATA_MODEL_REASONS]).toEqual([...])`（拒因常量顺序固定）。
    - 5 个 negative-sample，每个用 `safeParse` 并断言**特定**拒因：从 quizDataModel 深拷贝后各注入一处非法（夹带 `body:"DROP TABLE x"` 值 / 改表名去前缀 / 加 `foreignKeys:[...]` 键 / 删 schoolId 列 / 把某列 `type:"json"`），断言对应 issue.message 或 issue.code。
  </action>
  <verify>
    <automated>pnpm exec vitest --run src/lib/dto/plugin-data-model.test.ts</automated>
  </verify>
  <done>meta-schema 文件存在且无 server-only；quiz 合法样板 parse 通过；5 类非法各断言特定拒因；vitest 全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| plugin source `dataModel` 声明 → 编译器/DB schema | 不可信的插件声明在此跨界进入受治理建表链路；meta-schema 是把守该边界的唯一验证点，阻止裸 DDL/SQL 偷渡、跨租户逃逸、向 core 表越界耦合。 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Tampering / EoP | `dataModel` 夹带 `CREATE/ALTER/DROP` 字符串 | mitigate | meta-schema superRefine 扫所有字符串值命中 DDL 关键字 → `RAW_SQL_FORBIDDEN`；负样本#1 常驻断言（运行时 ripgrep 闸门在 67-03 二次兜底）。 |
| T-67-02 | Tampering | 表名缺命名空间前缀 → 跨插件表名撞库 | mitigate | `name.startsWith("plugin_owned_","MISSING_OWNED_PREFIX")`；负样本#2。 |
| T-67-03 | EoP | 向 core 表加出向 FK → 越界耦合/隔离破坏 | mitigate | 每层 `.strict()`，声明面无 `foreignKeys`/`references` 字段；FK 由编译器固定仅输出 schoolId→schools；负样本#3 断言 unknown-key 拒。 |
| T-67-04 | Information Disclosure | 缺 schoolId scope → 跨租户读写泄漏 | mitigate | table superRefine 强制 schoolId notNull → `MISSING_SCHOOL_SCOPE`；负样本#4（物理 cascade 断言在 67-03）。 |
| T-67-05 | Tampering | json/blob 袋子绕过结构化契约 | mitigate | `COLUMN_TYPES` 白名单 z.enum 边界拒 → `INVALID_COLUMN_TYPE`；负样本#5（D-02）。 |
</threat_model>

<verification>
- `pnpm exec vitest --run src/lib/dto/plugin-data-model.test.ts` 全绿（1 合法 + 5 非法各特定拒因）。
- `grep -L 'server-only' src/lib/dto/plugin-data-model.ts` 命中（确认无 server-only）。
- package.json 含 `db:generate` / `plugin:compile`；semver/drizzle-zod/@types/semver 已装。
</verification>

<success_criteria>
DATA-01 校验地基就位：声明式 dataModel meta-schema 在边界拒 5 类非法声明（各给明确拒因），合法 quiz 样板通过并成为编译器输入真相源；新依赖与 `db:generate`/`plugin:compile` 脚本入口就绪，供 67-02 编译链消费。
</success_criteria>

<output>
After completion, create `.planning/phases/67-declarative-plugin-owned-data-model-migration-proof/67-01-SUMMARY.md`.
</output>
