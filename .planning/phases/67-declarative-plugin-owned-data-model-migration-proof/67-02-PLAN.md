---
phase: 67-declarative-plugin-owned-data-model-migration-proof
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scripts/compile-plugin-data-model.ts
  - src/db/schema/generated/plugin-owned/quiz.ts
  - src/db/schema/generated/index.ts
  - src/db/schema.ts
  - drizzle/
requirements: [DATA-02, DATA-03]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "`pnpm plugin:compile` reads the legal quiz declaration (plugins/quiz-sample/data-model.ts), re-validates it through PluginDataModelSchema, and emits deterministic Drizzle table fragments under src/db/schema/generated/ — never executing any DDL itself."
    - "The compiler fixed-injects id (PK), pluginId (FK→pluginRegistrations cascade) and schoolId (FK→schools cascade) plus createdAt/updatedAt; the plugin declaration cannot express these FKs (compile, don't trust)."
    - "Generated tables carry the D-12 physical invariants: composite index (schoolId,classroomSession,student,question) and unique (classroomSession,student,question) on the responses table."
    - "schema.ts re-exports the generated barrel so drizzle-kit sees the new tables, and `pnpm db:generate` produces a checked-in SQL migration under drizzle/ that `pnpm db:migrate` applies cleanly."
    - "pluginRegistrations gains a dataVersion integer column, notNull, default 1, materialized by the checked-in migration — not by runtime DDL."
    - "Recompiling is byte-stable: `pnpm plugin:compile && git diff --exit-code src/db/schema/generated` is clean (declaration↔generated drift guard)."
  artifacts:
    - path: "scripts/compile-plugin-data-model.ts"
      provides: "Declaration→Drizzle codegen (compile, don't execute); validates then writes deterministic fragments"
      contains: "PluginDataModelSchema"
      min_lines: 80
    - path: "src/db/schema/generated/plugin-owned/quiz.ts"
      provides: "Generated sqliteTable defs for plugin_owned_quiz_questions + plugin_owned_quiz_responses"
      contains: "plugin_owned_quiz_responses"
    - path: "src/db/schema/generated/index.ts"
      provides: "Barrel re-exporting every generated plugin-owned fragment"
      contains: "export"
    - path: "src/db/schema.ts"
      provides: "pluginRegistrations.dataVersion column + generated-barrel re-export"
      contains: "dataVersion"
  key_links:
    - from: "scripts/compile-plugin-data-model.ts"
      to: "plugins/quiz-sample/data-model.ts"
      via: "import quizDataModel + PluginDataModelSchema.parse before emit"
      pattern: "quiz-sample/data-model"
    - from: "src/db/schema.ts"
      to: "src/db/schema/generated"
      via: "export * barrel so drizzle-kit generate sees generated tables"
      pattern: "schema/generated"
    - from: "drizzle/"
      to: "src/db/schema/generated/plugin-owned/quiz.ts"
      via: "drizzle-kit generate diffs generated tables into a checked-in migration"
      pattern: "plugin_owned_quiz"
---

<objective>
把已校验的声明式 `dataModel` 编译成**生成式 Drizzle 表片段**并落地为 checked-in 迁移（DATA-02 / DATA-03 物理面）。编译器 `scripts/compile-plugin-data-model.ts` 读 `plugins/quiz-sample/data-model.ts`、用 67-01 的 `PluginDataModelSchema` 二次校验、再确定性输出 `src/db/schema/generated/plugin-owned/quiz.ts` 与 barrel；schema.ts 仅手工加一行 barrel re-export + `pluginRegistrations.dataVersion`，随后 `pnpm db:generate` 产出 checked-in 迁移、`pnpm db:migrate` 实体化。

Purpose: 这是「compile, don't execute」的核心落点——声明永不直接建表，编译器把声明翻译成受治理的 Drizzle DSL，物理 DDL 只由 drizzle-kit 写进 `drizzle/`（运行时零 DDL，闸门在 67-03 兜底）。固定注入 id/pluginId/schoolId FK 与 D-12 复合索引/唯一约束，确保插件无法表达跨界 FK 或绕过租户隔离。
Output: 编译器 + 生成片段 + barrel + schema.ts（dataVersion + re-export）+ checked-in 迁移。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/67-declarative-plugin-owned-data-model-migration-proof/67-CONTEXT.md
@.planning/phases/67-declarative-plugin-owned-data-model-migration-proof/67-RESEARCH.md
@.planning/phases/67-declarative-plugin-owned-data-model-migration-proof/67-PATTERNS.md
@.planning/phases/67-declarative-plugin-owned-data-model-migration-proof/67-VALIDATION.md
@plugins/quiz-sample/data-model.ts
@src/lib/dto/plugin-data-model.ts

<interfaces>
<!-- 67-01 产出的契约（编译器消费）：
  import { PluginDataModelSchema, type PluginDataModel, type TableSpec, type ColumnSpec,
           COLUMN_TYPES, OWNED_TABLE_PREFIX } from "src/lib/dto/plugin-data-model";
  import { quizDataModel } from "plugins/quiz-sample/data-model";
-->

<!-- 生成片段须对齐的物理范式 (src/db/schema.ts:1883-1903 pluginOwnedBusinessData) —— 逐字抄列/FK/index 形态：
  import { sqliteTable, text, integer, index, uniqueIndex } from "drizzle-orm/sqlite-core";
  import { schools, pluginRegistrations } from "../../schema";   // 生成文件相对 src/db/schema/generated/plugin-owned/
  id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
  schoolId: text("schoolId").notNull().references(() => schools.id, { onDelete: "cascade" }),
  pluginId: text("pluginId").notNull().references(() => pluginRegistrations.id, { onDelete: "cascade" }),
  createdAt: integer("createdAt", { mode: "timestamp_ms" }).$defaultFn(() => new Date()),
  updatedAt: integer("updatedAt", { mode: "timestamp_ms" }).$defaultFn(() => new Date()),
  (table) => [ uniqueIndex("...").on(...), index("...").on(...) ]
-->

<!-- pluginRegistrations 现状 (src/db/schema.ts:1241-1264) —— dataVersion 须并入对象、不破坏既有两 uniqueIndex 尾闭合。 -->

<!-- 列类型映射 (D-01 白名单 → Drizzle DSL)：
  text       -> text("name")
  integer    -> integer("name")
  boolean    -> integer("name", { mode: "boolean" })
  timestamp  -> integer("name", { mode: "timestamp_ms" })
  enum       -> text("name", { enum: [...enumValues] })
  notNull -> .notNull(); default -> .default(v)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 67-02-01: 编译器 — 声明→确定性 Drizzle 片段 + barrel（compile, don't execute）</name>
  <files>scripts/compile-plugin-data-model.ts, src/db/schema/generated/plugin-owned/quiz.ts, src/db/schema/generated/index.ts</files>
  <read_first>
    - src/lib/dto/plugin-data-model.ts (67-01 产出) — `PluginDataModelSchema` / 导出类型 / `COLUMN_TYPES` / `OWNED_TABLE_PREFIX`（编译器逐字消费这些导出）。
    - plugins/quiz-sample/data-model.ts (67-01 产出) — 编译器输入真相源；列/索引/唯一约束形态。
    - src/db/schema.ts (lines 1883-1903) — 生成片段须 1:1 对齐的物理范式（id PK、schoolId/pluginId cascade FK、`uniqueIndex(...).on(...)`）。
    - scripts/prepare-dev-db.ts — 仓库内 tsx 脚本 file-I/O / 路径 / 退出码风格 analog（裸 tsx，无 server-only）。
  </read_first>
  <action>
    创建 `scripts/compile-plugin-data-model.ts`（裸 tsx，**无 `import "server-only"`**；`pnpm plugin:compile` 的目标，入口已在 67-01 写入 package.json）：
    1. `import { PluginDataModelSchema, COLUMN_TYPES, OWNED_TABLE_PREFIX } from "../src/lib/dto/plugin-data-model";` 与 `import { quizDataModel } from "../plugins/quiz-sample/data-model";`。
    2. **二次校验**：`const model = PluginDataModelSchema.parse(quizDataModel);`（声明若非法，编译器在 emit 前即 `process.exit(1)`——编译器绝不信任声明，绝不执行任何 SQL/DDL）。
    3. 维护一个声明列表 `const MODELS = [quizDataModel] as const;`（本 phase 仅 quiz；结构上为 Phase 68+ 预留多插件，但不抽象过度）。
    4. **确定性代码生成**——对每个 model 生成一个 `sqliteTable` 定义字符串，规则固定（字节稳定，保证重编译 `git diff --exit-code` 干净）：
       - 文件头固定写 `// AUTO-GENERATED by scripts/compile-plugin-data-model.ts — DO NOT EDIT.` + `// Source: plugins/<key>/data-model.ts`。
       - import 行固定：`import { sqliteTable, text, integer, index, uniqueIndex } from "drizzle-orm/sqlite-core";` + `import { schools, pluginRegistrations } from "../../schema";`。
       - 每张表 = `export const <camelTableName> = sqliteTable("<table.name>", { ... }, (table) => [ ... ]);`。
       - **固定注入列（声明面不可表达，D-11/隔离）**，顺序固定：`id`(PK uuid) → `schoolId`(text notNull, `.references(() => schools.id,{onDelete:"cascade"})`) → `pluginId`(text notNull, `.references(() => pluginRegistrations.id,{onDelete:"cascade"})`) → **声明列**（按声明顺序，schoolId 已在固定注入中消费，编译器从声明列里跳过同名 schoolId 避免重复）→ `createdAt`/`updatedAt`(timestamp_ms `$defaultFn`)。
       - **声明列类型映射**用上面的 interfaces 映射表；enum 用 `text("name",{ enum: [...] })`。
       - **索引/唯一约束**：声明的 `indexes[]` → `index("<table>_<cols_join_>_idx").on(table.<c>...)`；`uniques[]` → `uniqueIndex("<table>_<cols_join_>_unique").on(table.<c>...)`。命名确定性（列名以 `_` 连接），保证 D-12 复合索引 `(schoolId,classroomSession,student,question)` 与唯一 `(classroomSession,student,question)` 落在 responses 表。
    5. 输出文件：`src/db/schema/generated/plugin-owned/<key>.ts`（每插件一文件，这里 `quiz.ts` 含两张表）；以及 barrel `src/db/schema/generated/index.ts`（`export * from "./plugin-owned/quiz";`，按文件名排序确定性写出）。用 `mkdirSync(..., {recursive:true})` + `writeFileSync`。
    6. 写出后用 prettier/默认格式保持稳定（若仓库有 prettier 则 `execFileSync` 格式化生成文件，否则手工模板已对齐缩进——关键是**幂等**）。
    运行 `pnpm plugin:compile` 一次，确认两文件生成且内容含两张 `plugin_owned_quiz_*` 表 + D-12 索引/唯一约束。
  </action>
  <verify>
    <automated>pnpm plugin:compile && node -e "const fs=require('fs');const q=fs.readFileSync('src/db/schema/generated/plugin-owned/quiz.ts','utf8');if(!/plugin_owned_quiz_questions/.test(q)||!/plugin_owned_quiz_responses/.test(q))process.exit(1);if(!/onDelete:\s*['\"]cascade['\"]/.test(q))process.exit(1);if(!/uniqueIndex/.test(q)||!/index\(/.test(q))process.exit(1);if(!fs.existsSync('src/db/schema/generated/index.ts'))process.exit(1);console.log('ok')" && pnpm plugin:compile && git diff --exit-code src/db/schema/generated</automated>
  </verify>
  <done>`pnpm plugin:compile` 生成 quiz.ts（两表 + cascade FK + D-12 复合索引/唯一约束）与 barrel；二次编译 `git diff --exit-code src/db/schema/generated` 干净（确定性/漂移守卫）；编译器不含任何 `client.execute`/裸 DDL。</done>
</task>

<task type="auto">
  <name>Task 67-02-02: schema.ts 接线（dataVersion + 生成 barrel）→ checked-in 迁移</name>
  <files>src/db/schema.ts, drizzle/</files>
  <read_first>
    - src/db/schema.ts (lines 1241-1264) — `pluginRegistrations` 对象边界与既有两 uniqueIndex 尾；dataVersion 须并入对象主体，不破坏 `(table) => [...]` 闭合。
    - src/db/schema.ts (line 1903 末尾) — 文件尾，barrel re-export 追加点。
    - drizzle.config.ts — `schema:'./src/db/schema.ts'`、`out:'./drizzle'`（drizzle-kit 经 schema.ts 看见 barrel 再导出的生成表；迁移落 drizzle/）。
    - scripts/prepare-dev-db.ts — `db:migrate` 目标，确认本地迁移落库路径。
  </read_first>
  <action>
    1. 在 `pluginRegistrations` 定义内（src/db/schema.ts:1241-1260 列块中，建议紧随 `killSwitchEnabled` 或 `lifecycleState` 之后、`createdAt` 之前）新增一列：
       `dataVersion: integer("dataVersion").notNull().default(1),`（D — 整数、notNull、默认 1；由 checked-in 迁移物理化，**非**运行时 DDL）。不改动既有列与两条 `uniqueIndex`。
    2. 在 src/db/schema.ts **文件最末尾**（1903 行之后，独立一行）追加：`export * from "./schema/generated";`（让 drizzle-kit 经 schema.ts 看见全部生成的 plugin-owned 表）。
       说明：生成表实体住在 `src/db/schema/generated/`，**手写 schema.ts 主体永不被 codegen 改写**——schema.ts 只手工拥有这一行 re-export + dataVersion 列；满足「hand-written region untouched by codegen」红线。
    3. 确认 67-01 已装 `db:generate` 脚本后，依次运行：
       `pnpm plugin:compile`（确保生成最新）→ `pnpm db:generate`（drizzle-kit generate；diff 出 plugin_owned_quiz_* 两表 + pluginRegistration.dataVersion 列，写入 `drizzle/<NNNN>_*.sql` + 更新 `drizzle/meta/_journal.json`，**这些文件须 git add 提交**）→ `pnpm db:migrate`（把新迁移实体化到本地 sqlite，确认无错应用）。
    4. 检查生成的迁移 SQL 含 `CREATE TABLE` `plugin_owned_quiz_questions`/`plugin_owned_quiz_responses` 与 `ALTER TABLE pluginRegistration ADD ... dataVersion`（或表重建迁移含该列）；确认这些 DDL **只**存在于 `drizzle/`（白名单），运行时代码零 DDL。
  </action>
  <verify>
    <automated>pnpm plugin:compile && pnpm db:generate && git diff --exit-code src/db/schema/generated && node -e "const fs=require('fs');const files=fs.readdirSync('drizzle').filter(f=>f.endsWith('.sql'));const all=files.map(f=>fs.readFileSync('drizzle/'+f,'utf8')).join('\n');if(!/plugin_owned_quiz_questions/.test(all)||!/plugin_owned_quiz_responses/.test(all))process.exit(1);if(!/dataVersion/.test(all))process.exit(1);console.log('migration ok')" && pnpm db:migrate</automated>
  </verify>
  <done>pluginRegistrations 含 `dataVersion` (integer/notNull/default 1)；schema.ts 末尾 re-export 生成 barrel；`pnpm db:generate` 产出 checked-in 迁移（含两 plugin_owned_quiz 表 + dataVersion），`pnpm db:migrate` 干净应用；DDL 仅落 `drizzle/`。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 已校验声明 → 编译器 → 生成 Drizzle 片段 → drizzle-kit 迁移 | 声明在此被翻译成物理建表 DSL。编译器是「翻译 + 固定注入」层，确保插件无法表达跨界 FK、无法注入裸 DDL、无法逃逸租户 scope；物理 DDL 只能由 drizzle-kit 写入白名单 `drizzle/`。 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-06 | Tampering | 编译器被诱导直接执行 DDL（绕过 drizzle-kit 迁移审计） | mitigate | 编译器只 `writeFileSync` TS 片段，绝无 `createClient`/`client.execute`/`.run(sql\`...\`)`；运行时零-DDL 闸门在 67-03 静态兜底。 |
| T-67-07 | EoP | 声明伪造 pluginId/schoolId FK 指向他校或核心表 | mitigate | id/schoolId/pluginId FK 由编译器**固定注入**（声明面 `.strict()` 已拒 foreignKeys 键，67-01）；schoolId→schools、pluginId→pluginRegistrations 均 cascade，租户隔离物理保证。 |
| T-67-08 | Tampering | 生成片段被手改后与声明漂移 | mitigate | `pnpm plugin:compile && git diff --exit-code src/db/schema/generated` 漂移守卫；生成文件头标注 DO NOT EDIT；闸门进 67-03 verify。 |
| T-67-09 | Repudiation | dataVersion 经运行时 DDL 偷加（无迁移审计痕迹） | mitigate | dataVersion 由 checked-in `drizzle/` 迁移物理化，journal 留痕；运行时零-DDL 闸门兜底。 |
</threat_model>

<verification>
- `pnpm plugin:compile` 生成 `src/db/schema/generated/plugin-owned/quiz.ts`（两表 + cascade FK + D-12 复合索引/唯一约束）与 `index.ts` barrel。
- 二次 `pnpm plugin:compile && git diff --exit-code src/db/schema/generated` 干净（确定性/漂移）。
- `pnpm db:generate` 产出 checked-in 迁移含两 plugin_owned_quiz 表 + `dataVersion`；`pnpm db:migrate` 干净应用。
- `grep -n 'client.execute\|\.run(sql' scripts/compile-plugin-data-model.ts` 无命中（编译器零 DDL 执行）。
</verification>

<success_criteria>
DATA-02/03 物理面就位：声明经编译器确定性翻译为受治理 Drizzle 片段、由 drizzle-kit 落地为 checked-in 迁移；plugin_owned_quiz 两表携 schoolId/pluginId cascade FK 与 D-12 复合索引/唯一约束；pluginRegistrations.dataVersion=1 经迁移物理化；重编译零漂移。物理 DDL 全部限于 `drizzle/`，供 67-03 零-DDL 闸门与物理断言收口。
</success_criteria>

<output>
After completion, create `.planning/phases/67-declarative-plugin-owned-data-model-migration-proof/67-02-SUMMARY.md`.
</output>
