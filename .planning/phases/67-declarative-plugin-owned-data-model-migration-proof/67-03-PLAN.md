---
phase: 67-declarative-plugin-owned-data-model-migration-proof
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - scripts/gate-no-runtime-ddl.ts
  - scripts/verify-phase67-plugin-owned-data.ts
  - package.json
requirements: [DATA-02, DATA-03, DATA-04]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "A static zero-DDL gate (scripts/gate-no-runtime-ddl.ts) scans the codebase for DDL execution (CREATE/ALTER/DROP TABLE, raw SQL .run/.execute) and FAILS if any appears outside the whitelist drizzle/** + src/db/schema/generated/**."
    - "An integration verifier (scripts/verify-phase67-plugin-owned-data.ts) materializes the checked-in migrations into a temp SQLite DB and asserts the D-12 physical invariants via PRAGMA: plugin_owned_quiz_* tables exist with schoolId, the composite scope index, and the dedupe unique constraint."
    - "The verifier proves declaration↔physical↔migration three-way alignment, deletes a school row and asserts plugin-owned rows cascade-delete, and asserts PRAGMA foreign_key_check returns zero violations (DATA-04)."
    - "The verifier asserts pluginRegistration.dataVersion physically exists with default 1, and re-runs the drift guard (plugin:compile + git diff --exit-code)."
    - "package.json exposes verify:phase67 and the verify:phase alias is repointed from verify:phase65 to verify:phase67; `pnpm verify:phase` runs green end-to-end."
  artifacts:
    - path: "scripts/gate-no-runtime-ddl.ts"
      provides: "Static zero-runtime-DDL gate with drizzle/** + generated/** whitelist"
      contains: "src/db/schema/generated"
      min_lines: 40
    - path: "scripts/verify-phase67-plugin-owned-data.ts"
      provides: "Physical PRAGMA assertions + cascade proof + foreign_key_check + drift + zero-DDL gate orchestration"
      contains: "foreign_key_check"
      min_lines: 80
    - path: "package.json"
      provides: "verify:phase67 script + verify:phase repointed to verify:phase67"
      contains: "verify:phase67"
  key_links:
    - from: "scripts/verify-phase67-plugin-owned-data.ts"
      to: "scripts/lib/sqlite-migration-proof.ts"
      via: "reuse materializeDrizzleMigrations + cleanupSqliteArtifacts (don't rebuild)"
      pattern: "materializeDrizzleMigrations"
    - from: "package.json verify:phase"
      to: "verify:phase67"
      via: "alias repoint closes the phase gate"
      pattern: "verify:phase67"
    - from: "scripts/gate-no-runtime-ddl.ts"
      to: "drizzle/ + src/db/schema/generated/"
      via: "whitelist excludes the only two legitimate DDL homes"
      pattern: "drizzle"
---

<objective>
封口本 phase 的 close gate（DATA-02 静态面 / DATA-03 / DATA-04）：`scripts/gate-no-runtime-ddl.ts` 静态证明运行时零 DDL（白名单仅 `drizzle/**` + `src/db/schema/generated/**`），`scripts/verify-phase67-plugin-owned-data.ts` 把 checked-in 迁移实体化进临时 SQLite、用 PRAGMA 断言 D-12 物理不变量 + 删 school 级联 + `foreign_key_check` 净 + dataVersion 默认 1 + 漂移守卫；最后新增 `verify:phase67` 并把 `verify:phase` 别名从 65 重指 67，`pnpm verify:phase` 全绿。

Purpose: 67-01 在边界拒非法声明、67-02 让物理 DDL 只生于 `drizzle/`；本 plan 用「静态零-DDL 闸门 + 行为优先物理证明」双保险收口——克隆 `verify-phase45-plugin-schema.ts` 的成熟范式（临时库 materialize → PRAGMA → delete 级联 → foreign_key_check），并把别名重指作为最后一步避免中途断裂 `pnpm verify:phase`。
Output: 零-DDL 闸门脚本 + phase67 物理验证脚本 + package.json（verify:phase67 + 别名重指）。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/67-declarative-plugin-owned-data-model-migration-proof/67-CONTEXT.md
@.planning/phases/67-declarative-plugin-owned-data-model-migration-proof/67-RESEARCH.md
@.planning/phases/67-declarative-plugin-owned-data-model-migration-proof/67-PATTERNS.md
@.planning/phases/67-declarative-plugin-owned-data-model-migration-proof/67-VALIDATION.md
@scripts/verify-phase45-plugin-schema.ts
@scripts/lib/sqlite-migration-proof.ts

<interfaces>
<!-- 复用（don't rebuild） scripts/lib/sqlite-migration-proof.ts：
  materializeDrizzleMigrations(databaseUrl, {rootDir?, throughTag?}) -> client   // 顺序跑 drizzle/ 全迁移，PRAGMA foreign_keys=ON
  cleanupSqliteArtifacts(databasePath)                                           // 删 .db/.db-shm/.db-wal
-->

<!-- 物理断言范式 (scripts/verify-phase45-plugin-schema.ts) —— 逐字抄这些 helper 形态：
  assertIndex(client, tableName, indexName, isUnique)  // PRAGMA index_list + unique 标志比对
  PRAGMA table_info(<table>) -> rows.map(r=>r.name)     // 列存在性
  PRAGMA foreign_key_check  -> rows.length === 0        // 净
  runVitest(paths,label) / read(path) / nonCommentIncludes(src,token) / withoutLineComments
  临时库路径: path.join("/tmp/opencode", `phase67-...-${randomUUID()}.db`)
-->

<!-- D-12 物理不变量（须 PRAGMA 断言）：
  表 plugin_owned_quiz_questions / plugin_owned_quiz_responses 存在
  列含 schoolId(notNull); responses 复合索引列序 (schoolId,classroomSession,student,question)
  responses 唯一约束 (classroomSession,student,question)
  pluginRegistration 含列 dataVersion，默认 1（PRAGMA table_info 的 dflt_value === 1）
-->

<!-- package.json 现状 (line 47/64/65)：
  "verify:phase45": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase45-plugin-schema.ts"
  "verify:phase65": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase65-eval-guardrails.ts"
  "verify:phase":   "pnpm verify:phase65"   <-- 本 plan 末步重指为 "pnpm verify:phase67"
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 67-03-01: 零-运行时-DDL 静态闸门（DATA-02 静态面）</name>
  <files>scripts/gate-no-runtime-ddl.ts</files>
  <read_first>
    - scripts/verify-phase45-plugin-schema.ts (lines 21-48) — `read` / `withoutLineComments` / `nonCommentIncludes` helper + 退出码风格（逐字复用，不重造）。
    - drizzle.config.ts — 确认 `out:'./drizzle'`（白名单第一项）。
    - VALIDATION.md (line 38) — 白名单定义 `drizzle/**` + `src/db/schema/generated/**`。
  </read_first>
  <action>
    创建 `scripts/gate-no-runtime-ddl.ts`（裸 tsx，无 server-only；`tsx scripts/gate-no-runtime-ddl.ts` 可直跑）：
    1. 递归遍历仓库源码（`src/**`、`scripts/**`、`plugins/**`；跳过 `node_modules`、`.next`、`dist`、`drizzle/**`、`src/db/schema/generated/**` 白名单两项，跳过本闸门自身与任何 `*.test.ts`/`*.md`）。用 `node:fs` `readdirSync(recursive)` 或手写递归；**不要**调用外部 ripgrep（保持零依赖、CI 稳定）。
    2. 对每个 `.ts`/`.tsx` 文件取内容、`withoutLineComments` 去行注释后，扫描 DDL 执行特征（命中即违规，记录 file:line）：
       - 物理 DDL 关键字嵌入可执行字符串：`/\b(CREATE|ALTER|DROP)\s+TABLE\b/i`、`/\bCREATE\s+(UNIQUE\s+)?INDEX\b/i`。
       - 运行时执行通道：`client.execute(` / `.run(sql\`` / `db.run(` 同行附近含上面 DDL 关键字（保守：先标记所有含 DDL 关键字的非白名单行，再人工/规则确认）。
       说明：声明里只有结构化对象、生成片段是 Drizzle DSL（非裸 DDL）、真正的 `CREATE TABLE` SQL 只应活在 `drizzle/*.sql`（已白名单）——故任何白名单外的 DDL 关键字嵌入 = 违规。
    3. 违规清单非空 → 打印每条 `file:line  <snippet>` 后 `process.exit(1)`；为空 → 打印 `zero-runtime-DDL gate: PASS (scanned N files, whitelist=drizzle/**, src/db/schema/generated/**)` 并 `exit(0)`。
    4. 自检：脚本自身/白名单匹配逻辑须真的排除 `drizzle/` 与 `src/db/schema/generated/`（用前缀判断，跨平台用 `path.posix` 归一化分隔符）。
    运行 `tsx scripts/gate-no-runtime-ddl.ts` 应 PASS（67-02 后运行时代码无 DDL）。
  </action>
  <verify>
    <automated>tsx scripts/gate-no-runtime-ddl.ts</automated>
  </verify>
  <done>闸门遍历 src/scripts/plugins、白名单 `drizzle/**`+`src/db/schema/generated/**`、命中白名单外 DDL 即 exit(1)；当前仓库运行 PASS exit(0)。</done>
</task>

<task type="auto">
  <name>Task 67-03-02: phase67 物理验证脚本 + verify:phase67 + 别名重指（DATA-03/04 close gate）</name>
  <files>scripts/verify-phase67-plugin-owned-data.ts, package.json</files>
  <read_first>
    - scripts/verify-phase45-plugin-schema.ts (整文件) — 克隆其骨架：`materializeDrizzleMigrations` 临时库 → `assertIndex` / `PRAGMA table_info` → seed → `DELETE` 级联 `assertRowCount` → `PRAGMA foreign_key_check`；末段 static `nonCommentIncludes` 检查 + `runVitest`。
    - scripts/lib/sqlite-migration-proof.ts — 复用 `materializeDrizzleMigrations`/`cleanupSqliteArtifacts`（don't rebuild）。
    - package.json (lines 47/64/65) — `verify:phaseNN` 命令格式（`node --require ./scripts/server-only-node-shim.cjs --import tsx ...`）+ `verify:phase` 别名行。
    - src/db/schema/generated/plugin-owned/quiz.ts (67-02 产出) — 实际表名/列名/索引名（断言须与生成名一致）。
  </read_first>
  <action>
    创建 `scripts/verify-phase67-plugin-owned-data.ts`（克隆 verify-phase45 结构，复用 sqlite-migration-proof 两函数）：
    1. **物理证明（materialize 临时库）**：`materializeDrizzleMigrations(\`file:/tmp/opencode/phase67-verify-${randomUUID()}.db\`)` 跑全 checked-in 迁移，断言：
       - `PRAGMA table_info(plugin_owned_quiz_questions)` 与 `..._responses` 非空，列含 `schoolId`/`pluginId`/`id`/声明列；schoolId notNull。
       - `assertIndex` 断言 responses 的**复合 scope 索引存在**（名取 67-02 生成名，列序 schoolId,classroomSession,student,question）与 **dedupe 唯一约束**（`(classroomSession,student,question)` unique=true）——用 `PRAGMA index_info(<index>)` 校验列序，不只校验存在。
       - `PRAGMA table_info(pluginRegistration)` 含 `dataVersion` 且 `dflt_value` 解析为 `1`（DATA-04 dataVersion 默认 1）。
    2. **级联 + foreign_key_check（DATA-04）**：seed 最小 fixtures（school + pluginRegistration + 各一行 plugin_owned_quiz_questions/responses，参照 verify-phase45 seed 风格），`DELETE FROM school WHERE id=...` 后 `assertRowCount(plugin_owned_quiz_responses,0)`/`..._questions,0)`（schoolId cascade 证明）；`PRAGMA foreign_key_check` `rows.length===0`。
    3. **三对齐 + 漂移（DATA-02/03）**：static 段用 `nonCommentIncludes(read("src/db/schema/generated/plugin-owned/quiz.ts"), "plugin_owned_quiz_responses")` 等断言声明↔生成对齐；并 `execFileSync("pnpm",["plugin:compile"])` 后 `execFileSync("git",["diff","--exit-code","src/db/schema/generated"])`（漂移守卫，非零退出即 fail）。
    4. **零-DDL 闸门编排**：`execFileSync("tsx",["scripts/gate-no-runtime-ddl.ts"],{stdio:"inherit"})`（把 67-03-01 闸门纳入 phase gate）。
    5. 每步 try/catch 打印 + `process.exit(1)`；全绿打印 `🎉 Phase 67 closeout PASSED`；`finally` `cleanupSqliteArtifacts`。
    6. **package.json**（两处编辑）：
       - 新增 `"verify:phase67": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase67-plugin-owned-data.ts"`（格式对齐 line 47/64）。
       - 把 `"verify:phase": "pnpm verify:phase65"` 改为 `"verify:phase": "pnpm verify:phase67"`（**最后一步**，前序脚本就位后才重指）。
    7. 运行 `pnpm verify:phase` 全绿（= verify:phase67）。
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');if(!p.scripts['verify:phase67'])process.exit(1);if(p.scripts['verify:phase']!=='pnpm verify:phase67')process.exit(1);console.log('scripts ok')" && pnpm verify:phase</automated>
  </verify>
  <done>verify 脚本 materialize 临时库断言 D-12 物理不变量 + dataVersion 默认 1 + 删 school 级联 + foreign_key_check 净 + 漂移守卫 + 零-DDL 闸门编排；package.json 含 verify:phase67 且 verify:phase 重指 67；`pnpm verify:phase` 全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 已生成迁移 + 运行时源码 → close gate（静态闸门 + 物理证明） | close gate 是发布前最后验证面：静态闸门证明运行时无任何 DDL 执行路径（白名单外）；物理证明把迁移实体化进真实 SQLite 引擎，用 PRAGMA + 级联 + foreign_key_check 证明租户隔离与去重不变量真实生效（非仅源码假设）。 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-10 | EoP | 某处代码运行时 `client.execute("CREATE TABLE ...")` 绕过迁移审计建表 | mitigate | `gate-no-runtime-ddl.ts` 全仓扫描，白名单仅 `drizzle/**`+`src/db/schema/generated/**`，命中即 exit(1)；纳入 `pnpm verify:phase` phase gate。 |
| T-67-11 | Information Disclosure | schoolId cascade 实际未生效 → 删校后跨租户残留 | mitigate | 物理证明 `DELETE FROM school` 后断言 plugin_owned_quiz_* 行数归零 + `PRAGMA foreign_key_check` 净（行为优先，非源码假设）。 |
| T-67-12 | Tampering | 去重唯一约束列序错误 → 重复作答静默写入 | mitigate | `PRAGMA index_info` 断言唯一约束列序精确为 `(classroomSession,student,question)`、复合索引为 `(schoolId,classroomSession,student,question)`（D-12）。 |
| T-67-13 | Tampering | 生成片段被手改、与声明/迁移漂移逃过验证 | mitigate | phase gate 内 `pnpm plugin:compile && git diff --exit-code src/db/schema/generated` 漂移守卫，非零即 fail。 |
</threat_model>

<verification>
- `tsx scripts/gate-no-runtime-ddl.ts` PASS（白名单外零 DDL）。
- `pnpm verify:phase`（= verify:phase67）全绿：临时库 materialize + D-12 PRAGMA 断言（表/列/复合索引列序/唯一约束）+ dataVersion 默认 1 + 删 school 级联归零 + `foreign_key_check` 净 + 漂移守卫 + 零-DDL 闸门编排。
- package.json：`verify:phase67` 存在且 `verify:phase` === `pnpm verify:phase67`。
</verification>

<success_criteria>
DATA-02/03/04 close gate 封口：静态零-DDL 闸门证明运行时无白名单外 DDL；物理验证脚本把 checked-in 迁移实体化进真实 SQLite，断言 plugin_owned_quiz 两表的 schoolId cascade、D-12 复合索引/唯一约束列序、dataVersion=1、删 school 级联归零、foreign_key_check 净、声明↔生成零漂移；`verify:phase` 重指 67 并全绿 → 进 `/gsd-verify-work`。
</success_criteria>

<output>
After completion, create `.planning/phases/67-declarative-plugin-owned-data-model-migration-proof/67-03-SUMMARY.md`.
</output>
