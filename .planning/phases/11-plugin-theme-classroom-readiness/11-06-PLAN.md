---
phase: 11-plugin-theme-classroom-readiness
plan: 06
type: execute
wave: 4
depends_on:
  - 11-01
  - 11-02
  - 11-03
  - 11-04
  - 11-05
files_modified:
  - scripts/verify-phase11-readiness.ts
  - scripts/bootstrap-dev-db.ts
  - docs/plugin-system-review.md
  - docs/plugin-theme-implementation-plan.md
  - docs/theme-system-design.md
  - docs/teacher-classroom-flow-review.md
  - .planning/phases/11-plugin-theme-classroom-readiness/11-VERIFICATION.md
autonomous: true
requirements:
  - PLUGIN-01
  - PLUGIN-02
  - PLUGIN-03
  - PLUGIN-04
  - PLUGIN-05
  - PLUGIN-06
  - PLUGIN-07
  - CLASS-01
  - CLASS-02
  - CLASS-03
  - CLASS-04
  - CLASS-05
  - CLASS-06
  - CLASS-07
  - LESSON-05
  - AUTH-05
  - DATA-04
requirements_addressed:
  - PLUGIN-01
  - PLUGIN-02
  - PLUGIN-03
  - PLUGIN-04
  - PLUGIN-05
  - PLUGIN-06
  - PLUGIN-07
  - CLASS-01
  - CLASS-02
  - CLASS-03
  - CLASS-04
  - CLASS-05
  - CLASS-06
  - CLASS-07
  - LESSON-05
  - AUTH-05
  - DATA-04
must_haves:
  truths:
    - "Automated checks prove plugin permission/school denial, theme compilation/application, autosave persistence, server lock enforcement, and classroom connection/snapshot behavior per D-19."
    - "The phase can pass `pnpm test`, `pnpm typecheck`, `pnpm lint`, and `pnpm build` after all prior plans."
    - "Docs accurately describe implemented plugin/theme/classroom readiness behavior and explicitly exclude marketplace/sandbox/gradebook/branching scope per D-06 and D-17."
  artifacts:
    - path: "scripts/verify-phase11-readiness.ts"
      provides: "Cross-flow static/runtime readiness smoke checks"
    - path: ".planning/phases/11-plugin-theme-classroom-readiness/11-VERIFICATION.md"
      provides: "Release verification evidence and source coverage audit"
    - path: "docs/plugin-theme-implementation-plan.md"
      provides: "Implemented state notes for plugin/theme runtime"
  key_links:
    - from: "scripts/verify-phase11-readiness.ts"
      to: "src/lib/dal/plugins.ts"
      via: "grep/static checks for school_mismatch and permission_denied"
      pattern: "school_mismatch|permission_denied"
    - from: "scripts/verify-phase11-readiness.ts"
      to: "src/components/theme/theme-injector.tsx"
      via: "theme injector and activeThemeId checks"
      pattern: "theme-injector|activeThemeId"
    - from: "scripts/verify-phase11-readiness.ts"
      to: "src/lib/dal/learning.ts"
      via: "locked active step enforcement checks"
      pattern: "forcedStepId"
---

<objective>
Create the cross-flow release verification layer for plugin, theme, lesson autosave, and classroom readiness.

Purpose: This proves D-19 with concrete tests/scripts and prevents Phase 11 from shipping as isolated code paths that do not work together.

Output: Phase 11 readiness verifier, seed support if needed, docs updates reflecting implemented behavior, and a verification evidence file.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-plugin-theme-classroom-readiness/11-CONTEXT.md
@.planning/phases/11-plugin-theme-classroom-readiness/11-RESEARCH.md
@docs/plugin-system-review.md
@docs/plugin-theme-implementation-plan.md
@docs/theme-system-design.md
@docs/teacher-classroom-flow-review.md
@AGENTS.md
@DESIGN.md
@package.json
@scripts/verify-phase6-foundations.ts
@scripts/verify-phase5-classroom.ts
@scripts/bootstrap-dev-db.ts

<interfaces>
Existing script conventions:

```json
{
  "scripts": {
    "verify:phase5": "tsx scripts/verify-phase5-classroom.ts",
    "verify:phase6": "tsx scripts/verify-phase6-foundations.ts",
    "db:bootstrap:dev": "drizzle-kit push && tsx scripts/bootstrap-dev-db.ts",
    "test": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "build": "next build --turbopack"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 11 readiness verifier and seed coverage</name>
  <read_first>
    - `scripts/verify-phase5-classroom.ts`
    - `scripts/verify-phase6-foundations.ts`
    - `scripts/bootstrap-dev-db.ts`
    - `package.json`
    - All `11-*-SUMMARY.md` files from prior plans if present
    - `11-CONTEXT.md` decisions D-01 through D-19
    - `11-RESEARCH.md` Phase Verification Matrix
  </read_first>
  <files>scripts/verify-phase11-readiness.ts, scripts/bootstrap-dev-db.ts, package.json</files>
  <action>
    Create `scripts/verify-phase11-readiness.ts` using existing verifier style. Include static checks that fail with clear messages if: `src/actions/plugin-actions.ts` still contains `manifestJson: z.any()`, `src/lib/dal/plugins.ts` lacks `setPluginEnabled`, `school_mismatch`, or `permission_denied`, `src/server/themes/tokens.ts` emits `--surface-`, `src/components/theme/theme-injector.tsx` lacks `theme-injector`, `src/app/layout.tsx` lacks `<ThemeInjector />`, `src/components/plugins/plugin-renderer.tsx` contains `eval(` or `dangerouslySetInnerHTML`, `src/components/authoring/lesson-step-editor.tsx` lacks `autosaveLessonStepAction` or `保存步骤`, `src/lib/dal/classroom.ts` lacks `ensureClassroomParticipant` or `updateClassroomParticipantConnection`, and `src/lib/dal/learning.ts` lacks locked runtime forced-step logic. Extend `scripts/bootstrap-dev-db.ts` only if existing seed data cannot exercise plugin/theme/classroom smoke checks; seed a safe declarative plugin manifest with `dashboard.widget`, `lesson.sidebar`, actions `addStepSuggestion`, `annotateLesson`, `createNotificationStub`, permissions matching `PLUGIN_ACTION_PERMISSION_REQUIREMENTS`, and optional valid Lexend/no-line theme tokens. Add `"verify:phase11": "tsx scripts/verify-phase11-readiness.ts"` to `package.json` only if project convention accepts phase verifier scripts.
  </action>
  <verify>
    <automated>pnpm exec tsx scripts/verify-phase11-readiness.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-phase11-readiness.ts` exists.
    - `scripts/verify-phase11-readiness.ts` contains checks for `manifestJson: z.any()`, `school_mismatch`, `permission_denied`, `--surface-`, `theme-injector`, `autosaveLessonStepAction`, `ensureClassroomParticipant`, and `forcedStepId`.
    - If `package.json` is modified, it contains `verify:phase11` with value `tsx scripts/verify-phase11-readiness.ts`.
    - `pnpm exec tsx scripts/verify-phase11-readiness.ts` exits 0 after prior plans are complete.
  </acceptance_criteria>
  <done>There is a single Phase 11 readiness command that catches missing cross-flow glue and unsafe regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Run release verification commands and write evidence file</name>
  <read_first>
    - `package.json`
    - `.planning/phases/11-plugin-theme-classroom-readiness/11-CONTEXT.md`
    - `.planning/phases/11-plugin-theme-classroom-readiness/11-RESEARCH.md`
    - Prior plan summaries in `.planning/phases/11-plugin-theme-classroom-readiness/` if present
  </read_first>
  <files>.planning/phases/11-plugin-theme-classroom-readiness/11-VERIFICATION.md</files>
  <action>
    Run the verification suite in this exact order and capture pass/fail status plus any remediation notes in `.planning/phases/11-plugin-theme-classroom-readiness/11-VERIFICATION.md`: `pnpm test`, `pnpm typecheck`, `pnpm lint`, `pnpm run verify:phase5`, `pnpm run verify:phase6`, `pnpm exec tsx scripts/verify-phase11-readiness.ts`, and `pnpm build`. The verification file must include a requirements coverage table listing every Phase 11 ID: PLUGIN-01 through PLUGIN-07, CLASS-01 through CLASS-07, LESSON-05, AUTH-05, DATA-04. It must include a decisions coverage table for D-01 through D-19, with file/function evidence for each. If a command fails, fix code/test issues within this plan scope and rerun the failed command plus any dependent checks; do not mark verification complete with failing commands.
  </action>
  <verify>
    <automated>pnpm test && pnpm typecheck && pnpm lint && pnpm run verify:phase5 && pnpm run verify:phase6 && pnpm exec tsx scripts/verify-phase11-readiness.ts && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/11-plugin-theme-classroom-readiness/11-VERIFICATION.md` exists.
    - `11-VERIFICATION.md` contains all requirement IDs `PLUGIN-01`, `PLUGIN-02`, `PLUGIN-03`, `PLUGIN-04`, `PLUGIN-05`, `PLUGIN-06`, `PLUGIN-07`, `CLASS-01`, `CLASS-02`, `CLASS-03`, `CLASS-04`, `CLASS-05`, `CLASS-06`, `CLASS-07`, `LESSON-05`, `AUTH-05`, and `DATA-04`.
    - `11-VERIFICATION.md` contains all decision IDs `D-01` through `D-19`.
    - The combined command in `<verify>` exits 0.
  </acceptance_criteria>
  <done>Phase 11 has recorded, command-backed evidence that plugin, theme, autosave, and classroom readiness flows pass release checks.</done>
</task>

<task type="auto">
  <name>Task 3: Update source docs to match implemented behavior and exclusions</name>
  <read_first>
    - `docs/plugin-system-review.md`
    - `docs/plugin-theme-implementation-plan.md`
    - `docs/theme-system-design.md`
    - `docs/teacher-classroom-flow-review.md`
    - `.planning/phases/11-plugin-theme-classroom-readiness/11-VERIFICATION.md`
    - `11-CONTEXT.md` deferred ideas and decisions D-06, D-17, D-18
  </read_first>
  <files>docs/plugin-system-review.md, docs/plugin-theme-implementation-plan.md, docs/theme-system-design.md, docs/teacher-classroom-flow-review.md</files>
  <action>
    Update the four source docs so they no longer describe implemented Phase 11 items as missing. Add concise `Phase 11 implemented state` sections listing exact files/functions: `setPluginEnabled`, `listPluginsForSchool`, `getEnabledPluginsForAnchor`, `PluginRenderer`, `ThemeInjector`, `activeThemeId`, `compileThemeTokensToCssVariables`, `autosaveLessonStepAction`, `ensureClassroomParticipant`, `updateClassroomParticipantConnection`, and server-side locked player enforcement. Preserve explicit exclusions from `11-CONTEXT.md`: external marketplace/registry, arbitrary plugin JS/WASM/remote import, plugin-to-plugin communication, shared theme marketplace, light/dark variant expansion, full gradebook, advanced branching classroom flows, production pub/sub/SIS/export. Do not add implementation tasks beyond those exclusions; document them only as not part of the Phase 11 shipped scope.
  </action>
  <verify>
    <automated>pnpm exec tsx scripts/verify-phase11-readiness.ts && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - Each of the four docs contains `Phase 11 implemented state`.
    - `docs/plugin-system-review.md` contains `setPluginEnabled` and `permission_denied`.
    - `docs/theme-system-design.md` contains `--color-surface-container-low` and does not present `--surface-*` as current behavior.
    - `docs/teacher-classroom-flow-review.md` contains `ensureClassroomParticipant` and `server-side locked` or `服务端强制`.
    - `pnpm exec tsx scripts/verify-phase11-readiness.ts && pnpm lint` exits 0.
  </acceptance_criteria>
  <done>Documentation accurately reflects the implemented plugin/theme/classroom readiness state and keeps excluded scope explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification scripts -> repository source | Static checks must not mask unsafe code paths with comments-only matches. |
| Seed data -> dev database | Test plugin/theme/classroom data enters local SQLite. |
| Docs -> executor/user expectations | Documentation can accidentally expand scope or describe unsafe behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | R | `scripts/verify-phase11-readiness.ts` | mitigate | Use source checks that ignore comments where counting matters; avoid self-invalidating grep gates by scanning meaningful source lines. |
| T-11-06-02 | T | Seed plugin manifest | mitigate | Seed only declarative allowlisted actions and valid theme tokens; no provider keys, remote code, eval, direct DB/API access. |
| T-11-06-03 | E | Verification gaps | mitigate | Run DAL/action/component/unit scripts plus typecheck/lint/build; coverage table maps every requirement and D-01..D-19. |
| T-11-06-04 | I | Docs | mitigate | Do not include secrets or raw DB rows in docs; document safe DTO/function behavior only. |
| T-11-06-05 | D | Build/release readiness | mitigate | `pnpm build` is required after tests/typecheck/lint/verifiers. |
</threat_model>

<verification>
Run in order:

1. `pnpm exec tsx scripts/verify-phase11-readiness.ts`
2. `pnpm test`
3. `pnpm typecheck`
4. `pnpm lint`
5. `pnpm run verify:phase5`
6. `pnpm run verify:phase6`
7. `pnpm build`

No Drizzle schema push/migration is expected unless Task 1 changes seed assumptions only; if a prior implementation introduced schema changes, run `pnpm db:bootstrap:dev` before the verification suite and record it in `11-VERIFICATION.md`.
</verification>

<success_criteria>
- D-19 is satisfied with automated checks for all required denial/theme/autosave/classroom behaviors.
- Every Phase 11 requirement ID appears in `11-VERIFICATION.md` with evidence.
- Documentation matches shipped behavior and does not imply unsafe plugin execution or marketplace scope.
</success_criteria>

<output>
After completion, create `.planning/phases/11-plugin-theme-classroom-readiness/11-06-SUMMARY.md`.
</output>
