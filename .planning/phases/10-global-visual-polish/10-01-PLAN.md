---
phase: 10-global-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
autonomous: true
requirements: [UI-04]
must_haves:
  truths:
    - "User sees one consistent primary gradient and focus treatment across shared UI primitives."
    - "User does not see border-based sectioning introduced by shared components."
    - "Semantic chips and CTA hierarchy stay visually distinct instead of page-local overrides drifting apart."
  artifacts:
    - path: "src/app/globals.css"
      provides: "Canonical Phase 10 visual tokens and ghost-focus rules"
    - path: "src/components/ui/button.tsx"
      provides: "Primary/secondary/tertiary action hierarchy"
    - path: "src/components/ui/card.tsx"
      provides: "Shared action-layer card treatment"
    - path: "src/components/ui/badge.tsx"
      provides: "Shared badge/chip semantics"
  key_links:
    - from: "src/components/ui/button.tsx"
      to: "src/app/globals.css"
      via: "primary and focus tokens"
      pattern: "from-primary to-primary-container"
    - from: "src/components/ui/card.tsx"
      to: "src/app/globals.css"
      via: "surface and shadow tokens"
      pattern: "bg-surface-container-lowest"
    - from: "src/components/ui/badge.tsx"
      to: "src/app/globals.css"
      via: "accent/success/default tone tokens"
      pattern: "bg-surface-container-low"
---

<objective>
Harden the shared visual system so Phase 10 page work inherits the same tokens,
CTA hierarchy, chip tones, and no-line focus behavior.

Purpose: Make global polish systemic first, per D-01 to D-05 and D-12 to D-17.
Output: Updated global tokens plus tightened shared Button/Card/Badge primitives.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-global-visual-polish/10-CONTEXT.md
@.planning/phases/10-global-visual-polish/10-UI-SPEC.md
@.planning/phases/10-global-visual-polish/10-PATTERNS.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize global tokens for Phase 10</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css
    - DESIGN.md
    - .planning/phases/10-global-visual-polish/10-CONTEXT.md
  </read_first>
  <action>Update `src/app/globals.css` so the existing utility classes already used across the app resolve to a single authoritative token set per D-01, D-03, D-04, D-12, D-14, D-15, and D-17. Set `--color-primary: #0050d4;` and `--color-primary-container: #7b9cff;` to match `DESIGN.md`. Add the missing surface and semantic tokens already referenced by current components: `--color-surface-container-high`, `--color-surface-container-highest`, `--color-error-container`, `--color-on-error-container`, and `--radius-card`. Keep ambient shadows tinted and soft; do not add border tokens for sectioning. Keep `:focus-visible` as a 2px ghost-outline treatment driven by tokenized colors rather than opaque ring-heavy emphasis.</action>
  <acceptance_criteria>
    - `src/app/globals.css` contains `--color-primary: #0050d4;`
    - `src/app/globals.css` contains `--color-primary-container: #7b9cff;`
    - `src/app/globals.css` contains `--color-surface-container-high:`
    - `src/app/globals.css` contains `--color-surface-container-highest:`
    - `src/app/globals.css` contains `--color-error-container:` and `--color-on-error-container:`
    - `src/app/globals.css` contains `--radius-card:`
    - `src/app/globals.css` still contains `outline: 2px solid`
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/components/ui/button.tsx src/components/ui/card.tsx src/components/ui/badge.tsx</automated>
  </verify>
  <done>Global tokens cover all currently referenced surface/error/focus variables without introducing border-based layout rules.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten shared CTA, card, and badge primitives</name>
  <files>src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/badge.tsx</files>
  <read_first>
    - src/components/ui/button.tsx
    - src/components/ui/card.tsx
    - src/components/ui/badge.tsx
    - src/app/globals.css
    - DESIGN.md
  </read_first>
  <action>Refine the existing shared primitives instead of introducing new component names, per D-02 and D-03. In `button.tsx`, keep `primary`, `secondary`, and `tertiary` only, but ensure primary remains the only clearly dominant gradient CTA, secondary stays tonal and behind primary, and tertiary stays transparent with low emphasis per D-14 and D-17. In `card.tsx`, keep the white action-layer surface with ambient shadow and token-driven radius, not route-local shadow recipes. In `badge.tsx`, preserve semantic-first tones so success, warning-like, and accent chips do not collapse into the brand-blue palette per D-15. Do not add borders or hard dividers as fallback styling.</action>
  <acceptance_criteria>
    - `src/components/ui/button.tsx` still exports only `primary`, `secondary`, and `tertiary`
    - `src/components/ui/button.tsx` contains `bg-linear-135 from-primary to-primary-container`
    - `src/components/ui/button.tsx` keeps `rounded-full`
    - `src/components/ui/card.tsx` contains `bg-surface-container-lowest`
    - `src/components/ui/card.tsx` contains `shadow-ambient`
    - `src/components/ui/badge.tsx` defines shared variant classes rather than route-local semantic colors
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/components/ui/button.tsx src/components/ui/card.tsx src/components/ui/badge.tsx</automated>
  </verify>
  <done>Primary, secondary, tertiary, card, and badge styles can be reused by later plans without local visual exceptions becoming the default again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared primitive -> route UI | visual rules flow from reused primitives into every user-facing page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | S | `button.tsx`, `badge.tsx` | mitigate | Keep active/primary states visually stronger than secondary states so action hierarchy cannot be visually spoofed by weaker variants. |
| T-10-01-02 | I | `badge.tsx`, `globals.css` | mitigate | Preserve semantic-first error/success tones so warning or destructive states do not get hidden behind brand-blue polish. |
| T-10-01-03 | D | `globals.css` | accept | This plan changes styling tokens only and does not introduce new request paths or user-controlled rendering loops. |
</threat_model>

<verification>
- `pnpm typecheck`
- `pnpm exec eslint src/components/ui/button.tsx src/components/ui/card.tsx src/components/ui/badge.tsx`
</verification>

<success_criteria>
- Shared tokens match `DESIGN.md` color and focus intent.
- Shared primitives encode no-line layering and CTA hierarchy without page-local border fallbacks.
- Downstream Phase 10 plans can consume primitives without redefining primary gradients or semantic chips.
</success_criteria>

<output>
After completion, create `.planning/phases/10-global-visual-polish/10-01-SUMMARY.md`
</output>
