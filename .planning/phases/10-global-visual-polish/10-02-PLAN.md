---
phase: 10-global-visual-polish
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/components/shell/glass-nav.tsx
  - src/components/shell/route-shell.tsx
  - src/components/shell/sidebar.tsx
  - src/app/(teacher)/teacher/layout.tsx
  - src/app/(auth)/login/LoginForm.tsx
  - src/components/home/home-login-card.tsx
autonomous: true
requirements: [UI-04]
must_haves:
  truths:
    - "User sees glass treatment mainly on navigation and floating shell layers, not arbitrary content sections."
    - "User sees teacher shell and login entry surfaces use the same tonal layering and ghost-focus rules as the rest of the app."
    - "User sees one obvious primary CTA on entry flows while secondary actions remain visually calmer."
  artifacts:
    - path: "src/components/shell/glass-nav.tsx"
      provides: "Floating glass navigation treatment"
    - path: "src/components/shell/sidebar.tsx"
      provides: "Teacher-side navigation density and active state hierarchy"
    - path: "src/app/(auth)/login/LoginForm.tsx"
      provides: "Token-aligned input and CTA treatment"
    - path: "src/components/home/home-login-card.tsx"
      provides: "Public login card aligned to no-line entry styling"
  key_links:
    - from: "src/components/shell/glass-nav.tsx"
      to: "src/app/globals.css"
      via: "glass surface and ambient shadow tokens"
      pattern: "bg-surface/80"
    - from: "src/app/(auth)/login/LoginForm.tsx"
      to: "src/components/ui/button.tsx"
      via: "shared primary CTA"
      pattern: "<Button"
    - from: "src/components/home/home-login-card.tsx"
      to: "src/app/globals.css"
      via: "ghost focus and tonal field colors"
      pattern: "bg-surface-container-low"
---

<objective>
Converge shell, navigation, and entry surfaces so floating glass, tonal shells,
and input emphasis follow one consistent Phase 10 rule set.

Purpose: Apply D-04, D-12 to D-14, D-17, D-19, and D-22 to the app frame and
login surfaces before page-level polish.
Output: Updated glass nav, route shell, sidebar, teacher layout, and login forms.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-global-visual-polish/10-CONTEXT.md
@.planning/phases/10-global-visual-polish/10-UI-SPEC.md
@.planning/phases/10-global-visual-polish/10-PATTERNS.md
@.planning/phases/09-core-page-alignment/09-01-PLAN-SUMMARY.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restrict glass styling to shell and navigation layers</name>
  <files>src/components/shell/glass-nav.tsx, src/components/shell/route-shell.tsx, src/components/shell/sidebar.tsx, src/app/(teacher)/teacher/layout.tsx</files>
  <read_first>
    - src/components/shell/glass-nav.tsx
    - src/components/shell/route-shell.tsx
    - src/components/shell/sidebar.tsx
    - src/app/(teacher)/teacher/layout.tsx
    - src/components/ui/button.tsx
    - src/app/globals.css
    - DESIGN.md
  </read_first>
  <action>Rework shell-level surfaces so glass appears only where it is semantically floating per D-12 and D-13: keep `glass-nav.tsx` as the main glass reference, keep `route-shell.tsx` and `teacher/layout.tsx` tonal rather than glass-heavy, and align `sidebar.tsx` active state, CTA, and navigation density to the same action hierarchy as `button.tsx`. Remove route-local shadow recipes where they duplicate `shadow-ambient`, keep the main teacher workspace denser without adding divider lines, and ensure the shell still highlights one clear primary action rather than multiple competing blue treatments per D-14 and D-17.</action>
  <acceptance_criteria>
    - `src/components/shell/glass-nav.tsx` remains the main file using `backdrop-blur`
    - `src/components/shell/route-shell.tsx` contains `bg-surface`
    - `src/components/shell/sidebar.tsx` keeps a single primary CTA and token-based active states
    - `src/app/(teacher)/teacher/layout.tsx` no longer relies on multiple custom `shadow-[` values for shell framing
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/components/shell/glass-nav.tsx src/components/shell/route-shell.tsx src/components/shell/sidebar.tsx "src/app/(teacher)/teacher/layout.tsx"</automated>
  </verify>
  <done>App framing uses glass only on intended floating layers, while teacher shell density and CTA hierarchy are token-driven and border-free.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize login cards and input focus states</name>
  <files>src/app/(auth)/login/LoginForm.tsx, src/components/home/home-login-card.tsx</files>
  <read_first>
    - src/app/(auth)/login/LoginForm.tsx
    - src/components/home/home-login-card.tsx
    - src/components/ui/button.tsx
    - src/app/globals.css
    - DESIGN.md
  </read_first>
  <action>Update both login surfaces so inputs and tabs stop using border-led emphasis and instead follow the shared tonal input rule from `DESIGN.md`, per D-04, D-14, D-19, and D-22. `LoginForm.tsx` should rely on tokenized ghost-outline focus, rounded-full fields, and the shared `Button` primary CTA rather than a local duplicate gradient class. `home-login-card.tsx` should remove the outer solid border, use tonal nesting for the card/tab switch, keep only one dominant submit CTA, and preserve semantic error treatment instead of blue-branding error states.</action>
  <acceptance_criteria>
    - `src/app/(auth)/login/LoginForm.tsx` uses the shared `Button` for submit
    - `src/app/(auth)/login/LoginForm.tsx` input classes do not contain `focus:ring-2`
    - `src/components/home/home-login-card.tsx` outer card class does not contain `border `
    - `src/components/home/home-login-card.tsx` keeps exactly one submit CTA for the selected role intent
    - both files keep `bg-surface-container-low` or `bg-surface-container-lowest` tonal fields
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint "src/app/(auth)/login/LoginForm.tsx" src/components/home/home-login-card.tsx</automated>
  </verify>
  <done>Login and entry flows visually match the shared shell system, with ghost-focus inputs and one clear primary CTA per state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigation shell -> user actions | active states and CTA emphasis guide where users click |
| auth entry form -> user credentials | visual changes must not obscure form errors or focus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | S | `sidebar.tsx`, `teacher/layout.tsx` | mitigate | Preserve strong active-state contrast and one dominant CTA so navigation destinations cannot be visually spoofed by equal-emphasis siblings. |
| T-10-02-02 | I | `LoginForm.tsx`, `home-login-card.tsx` | mitigate | Keep semantic error panels and clear focus outlines visible so auth failures are not hidden by visual polish. |
| T-10-02-03 | D | `glass-nav.tsx` | accept | Shell-only styling changes do not create new request amplification or runtime work beyond existing client rendering. |
</threat_model>

<verification>
- `pnpm typecheck`
- `pnpm exec eslint src/components/shell/glass-nav.tsx src/components/shell/route-shell.tsx src/components/shell/sidebar.tsx "src/app/(teacher)/teacher/layout.tsx" "src/app/(auth)/login/LoginForm.tsx" src/components/home/home-login-card.tsx`
</verification>

<success_criteria>
- Glass styling is constrained to shell and floating navigation contexts.
- Teacher shell and entry forms share the same tonal/focus language.
- No login or shell surface reintroduces solid divider borders as structure.
</success_criteria>

<output>
After completion, create `.planning/phases/10-global-visual-polish/10-02-SUMMARY.md`
</output>
