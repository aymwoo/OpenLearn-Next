---
phase: 10-global-visual-polish
plan: 05
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/components/surfaces/home-surface.tsx
  - src/components/surfaces/student-dashboard-surface.tsx
  - src/components/surfaces/player-surface.tsx
  - src/components/surfaces/library-surface.tsx
  - src/components/surfaces/settings-surface.tsx
autonomous: true
requirements: [UI-04]
must_haves:
  truths:
    - "Public and student pages keep polished hierarchy without forcing a hero onto every page type."
    - "Home and player keep one justified gradient stage, while calm settings/catalog pages stay tonal and restrained."
    - "Student and management pages remain stable on mobile, with primary actions and current context preserved first."
  artifacts:
    - path: "src/components/surfaces/home-surface.tsx"
      provides: "Public showcase surface with one justified hero"
    - path: "src/components/surfaces/student-dashboard-surface.tsx"
      provides: "Student progress-forward dashboard polish"
    - path: "src/components/surfaces/player-surface.tsx"
      provides: "Immersive student player stage"
    - path: "src/components/surfaces/library-surface.tsx"
      provides: "Catalog/list density cleanup"
    - path: "src/components/surfaces/settings-surface.tsx"
      provides: "Calm management shell and semantic labs actions"
  key_links:
    - from: "src/components/surfaces/home-surface.tsx"
      to: "src/components/ui/button.tsx"
      via: "public CTA hierarchy"
      pattern: "<Button"
    - from: "src/components/surfaces/student-dashboard-surface.tsx"
      to: "src/components/ui/card.tsx"
      via: "student progress cards"
      pattern: "<Card"
    - from: "src/components/surfaces/settings-surface.tsx"
      to: "src/app/globals.css"
      via: "tonal calm management tokens and semantic destructive colors"
      pattern: "bg-surface-container-low"
---

<objective>
Harmonize public, student, catalog, and settings surfaces so each page type
follows the same design language without being flattened into the same layout.

Purpose: Apply D-04, D-12 to D-18, and D-19 to D-22 across non-teacher-facing
or calmer management surfaces.
Output: Updated home, student dashboard, player, library, and settings surfaces.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-global-visual-polish/10-CONTEXT.md
@.planning/phases/10-global-visual-polish/10-UI-SPEC.md
@.planning/phases/10-global-visual-polish/10-PATTERNS.md
@.planning/phases/09-core-page-alignment/09-02-PLAN-SUMMARY.md
@.planning/phases/09-core-page-alignment/09-03-PLAN-SUMMARY.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve justified heroes on public and student learning pages</name>
  <files>src/components/surfaces/home-surface.tsx, src/components/surfaces/student-dashboard-surface.tsx, src/components/surfaces/player-surface.tsx</files>
  <read_first>
    - src/components/surfaces/home-surface.tsx
    - src/components/surfaces/student-dashboard-surface.tsx
    - src/components/surfaces/player-surface.tsx
    - src/components/ui/button.tsx
    - src/components/ui/card.tsx
    - DESIGN.md
  </read_first>
  <action>Refine the public and student learning surfaces so they keep only the justified hero/stage moments they already have, per D-12, D-14, and D-18. `home-surface.tsx` should keep one public-facing hero and avoid secondary gradient competition. `student-dashboard-surface.tsx` should keep progress and resume actions clearly ahead of supporting metrics. `player-surface.tsx` should remain immersive but keep supporting modules tonal and readable. Across all three, reduce route-local shadow drift, preserve rounded-full CTA hierarchy, and keep responsive behavior stability-first so mobile retains main CTA, current lesson context, and active state before secondary stats per D-19 to D-22.</action>
  <acceptance_criteria>
    - each of the three files keeps at most one main gradient hero/stage block
    - primary student/public CTAs use shared Button hierarchy instead of route-local duplicates
    - supporting metric cards remain tonal (`bg-surface-container-low` / `bg-surface-container-lowest`) rather than extra gradient blocks
    - no new divider borders are introduced for responsive fallback layouts
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/components/surfaces/home-surface.tsx src/components/surfaces/student-dashboard-surface.tsx src/components/surfaces/player-surface.tsx</automated>
  </verify>
  <done>Home and student pages feel polished and lively where appropriate, but stay disciplined about hero usage and mobile stability.</done>
</task>

<task type="auto">
  <name>Task 2: Keep library and settings surfaces calm, tonal, and semantically correct</name>
  <files>src/components/surfaces/library-surface.tsx, src/components/surfaces/settings-surface.tsx</files>
  <read_first>
    - src/components/surfaces/library-surface.tsx
    - src/components/surfaces/settings-surface.tsx
    - src/components/ui/button.tsx
    - src/components/ui/badge.tsx
    - DESIGN.md
  </read_first>
  <action>Polish `library-surface.tsx` and `settings-surface.tsx` with a restrained management-page treatment per D-16 and D-18: do not force extra hero blocks onto list-heavy or settings-heavy pages. Keep tonal shells, compact metadata cards, and one clear CTA where needed. In `settings-surface.tsx`, preserve semantic red/orange tones for destructive or fault-related lab actions per D-15 instead of merging them into the primary blue system. In `library-surface.tsx`, tighten card density and metadata rhythm without adding borders, table dividers, or extra shadows that compete with the main information hierarchy.</action>
  <acceptance_criteria>
    - `src/components/surfaces/library-surface.tsx` contains tonal cards and no new `border ` classes
    - `src/components/surfaces/settings-surface.tsx` keeps destructive/fault actions in semantic red/orange tones
    - neither file adds extra gradient hero sections beyond already justified CTA moments
    - both files keep `bg-surface-container-low` / `bg-surface-container-lowest` as the primary separation method
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/components/surfaces/library-surface.tsx src/components/surfaces/settings-surface.tsx</automated>
  </verify>
  <done>Catalog and settings pages look release-ready, calm, and consistent with the same no-line tonal system as brighter routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public/student management UI -> user decisions | visual emphasis guides navigation and action selection across calmer and brighter page types |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | S | `home-surface.tsx`, `student-dashboard-surface.tsx` | mitigate | Keep one dominant hero/CTA so public and student pages do not spoof priority with multiple competing bright stages. |
| T-10-05-02 | I | `settings-surface.tsx` | mitigate | Preserve semantic destructive/fault colors so dangerous actions are not obscured by premium blue styling. |
| T-10-05-03 | D | `library-surface.tsx`, `player-surface.tsx` | accept | This plan changes presentation only and does not introduce new data loading or interaction loops. |
</threat_model>

<verification>
- `pnpm typecheck`
- `pnpm exec eslint src/components/surfaces/home-surface.tsx src/components/surfaces/student-dashboard-surface.tsx src/components/surfaces/player-surface.tsx src/components/surfaces/library-surface.tsx src/components/surfaces/settings-surface.tsx`
</verification>

<success_criteria>
- Page types stay visually consistent without being forced into identical hero patterns.
- Library and settings remain tonal and calm; home and player remain intentionally brighter.
- Student/public/management routes stay stable and readable across responsive layouts.
</success_criteria>

<output>
After completion, create `.planning/phases/10-global-visual-polish/10-05-SUMMARY.md`
</output>
