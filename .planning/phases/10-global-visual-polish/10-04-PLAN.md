---
phase: 10-global-visual-polish
plan: 04
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/components/classroom/classroom-control-panel.tsx
  - src/components/classroom/classroom-launch-panel.tsx
  - src/components/classroom/classroom-roster-panel.tsx
  - src/components/learning/teacher-review-surface.tsx
autonomous: true
requirements: [UI-04]
must_haves:
  truths:
    - "Teacher live-operation pages keep one clear hero/stage and denser supporting modules underneath it."
    - "Classroom, review, and launch flows preserve semantic warnings/errors rather than washing them into brand blue."
    - "Mobile runtime views preserve current step, mode, attendance, and main controls before secondary details."
  artifacts:
    - path: "src/components/classroom/classroom-control-panel.tsx"
      provides: "Primary live classroom stage and supporting controls"
    - path: "src/components/classroom/classroom-launch-panel.tsx"
      provides: "Launch form aligned to shared inputs and CTA hierarchy"
    - path: "src/components/learning/teacher-review-surface.tsx"
      provides: "Teacher review shell with dense filters and detail panes"
  key_links:
    - from: "src/components/classroom/classroom-control-panel.tsx"
      to: "src/components/ui/button.tsx"
      via: "runtime CTA hierarchy"
      pattern: "<Button"
    - from: "src/components/classroom/classroom-launch-panel.tsx"
      to: "src/app/globals.css"
      via: "ghost focus input treatment"
      pattern: "bg-surface-container-low"
    - from: "src/components/learning/teacher-review-surface.tsx"
      to: "src/components/ui/card.tsx"
      via: "shared review metrics and detail shells"
      pattern: "<Card"
---

<objective>
Polish classroom runtime, launch, roster, and teacher review surfaces so live
operation pages stay dense, semantic, and release-ready.

Purpose: Apply D-09, D-12 to D-18, and D-19 to D-22 to teacher live-operation
and review flows.
Output: Updated classroom control/launch/roster panels plus teacher review surface.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-global-visual-polish/10-CONTEXT.md
@.planning/phases/10-global-visual-polish/10-UI-SPEC.md
@.planning/phases/10-global-visual-polish/10-PATTERNS.md
@.planning/phases/09-core-page-alignment/09-01-PLAN-SUMMARY.md
@.planning/phases/09-core-page-alignment/09-02-PLAN-SUMMARY.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten classroom runtime hierarchy without losing semantic states</name>
  <files>src/components/classroom/classroom-control-panel.tsx, src/components/classroom/classroom-roster-panel.tsx</files>
  <read_first>
    - src/components/classroom/classroom-control-panel.tsx
    - src/components/classroom/classroom-roster-panel.tsx
    - src/components/ui/button.tsx
    - src/components/ui/card.tsx
    - DESIGN.md
  </read_first>
  <action>Update `classroom-control-panel.tsx` and `classroom-roster-panel.tsx` so the classroom runtime keeps one true hero/stage per D-12 and D-18, with denser supporting modules beneath it. Replace repeated local shadow recipes with shared tonal layering wherever possible, keep semantic destructive actions like ending class clearly red per D-15, and ensure mobile layouts preserve current step, mode, attendance, and the main control actions before utility modules per D-20 to D-22. Do not introduce extra gradient sections beyond the existing main stage.</action>
  <acceptance_criteria>
    - `src/components/classroom/classroom-control-panel.tsx` contains only one gradient hero section for the runtime stage
    - semantic destructive classroom controls remain red-toned, not `bg-primary`
    - `src/components/classroom/classroom-roster-panel.tsx` uses tonal cards without new divider borders
    - the roster and control panels both rely on shared Button/Card patterns for primary modules
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/components/classroom/classroom-control-panel.tsx src/components/classroom/classroom-roster-panel.tsx</automated>
  </verify>
  <done>Runtime pages feel operationally dense and polished while keeping live-state and risk-state visibility intact.</done>
</task>

<task type="auto">
  <name>Task 2: Align classroom launch and teacher review flows to the same control language</name>
  <files>src/components/classroom/classroom-launch-panel.tsx, src/components/learning/teacher-review-surface.tsx</files>
  <read_first>
    - src/components/classroom/classroom-launch-panel.tsx
    - src/components/learning/teacher-review-surface.tsx
    - src/components/ui/button.tsx
    - src/components/ui/card.tsx
    - src/components/ui/badge.tsx
  </read_first>
  <action>Refresh `classroom-launch-panel.tsx` and `teacher-review-surface.tsx` so they share the same Phase 10 control language: tonal shells, one dominant CTA, compact dense filters, and semantic-first states per D-14, D-15, and D-17. Convert launch form fields away from ring-heavy input styling to the ghost-outline input treatment established in Plan 10-02. In the review surface, keep the hero justified because it frames a high-frequency teacher workflow, but reduce local shadow drift, keep filter pills clearly subordinate to the primary hero, and avoid adding extra glass or gradient sections outside the main stage.</action>
  <acceptance_criteria>
    - `src/components/classroom/classroom-launch-panel.tsx` input/select classes do not rely on `focus-visible:outline-primary/20` as the main emphasis rule
    - `src/components/classroom/classroom-launch-panel.tsx` keeps one dominant `开始课堂` CTA
    - `src/components/learning/teacher-review-surface.tsx` keeps a single hero section and tonal supporting panes
    - `src/components/learning/teacher-review-surface.tsx` preserves semantic feedback/status visibility with shared badges or tonal chips
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/components/classroom/classroom-launch-panel.tsx src/components/learning/teacher-review-surface.tsx</automated>
  </verify>
  <done>Launch and review flows visually match the live classroom control language without flattening important semantic states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teacher live-control UI -> classroom actions | visual hierarchy influences operational decisions in real time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | S | `classroom-control-panel.tsx` | mitigate | Keep current-step, lock mode, and primary controls clearly labeled and visually dominant so operators cannot mistake classroom state. |
| T-10-04-02 | I | `classroom-launch-panel.tsx`, `teacher-review-surface.tsx` | mitigate | Preserve error, warning, and feedback semantics with non-brand destructive tones and readable status chips. |
| T-10-04-03 | D | `classroom-roster-panel.tsx` | accept | UI-only density changes do not alter subscription, polling, or mutation frequency. |
</threat_model>

<verification>
- `pnpm typecheck`
- `pnpm exec eslint src/components/classroom/classroom-control-panel.tsx src/components/classroom/classroom-launch-panel.tsx src/components/classroom/classroom-roster-panel.tsx src/components/learning/teacher-review-surface.tsx`
</verification>

<success_criteria>
- Classroom runtime and review surfaces share one polished operational hierarchy.
- Semantic risk states remain visible after the visual cleanup.
- Mobile runtime views keep action/status/context stable without overflow or hierarchy collapse.
</success_criteria>

<output>
After completion, create `.planning/phases/10-global-visual-polish/10-04-SUMMARY.md`
</output>
