---
phase: 10-global-visual-polish
plan: 06
type: execute
wave: 3
depends_on: [10-02, 10-04]
files_modified:
  - src/components/ui/ghost-field.ts
  - src/app/(auth)/login/LoginForm.tsx
  - src/components/home/home-login-card.tsx
  - src/components/classroom/classroom-launch-panel.tsx
autonomous: true
gap_closure: true
requirements: [UI-04]
must_haves:
  truths:
    - "User navigates the application and sees no 1px divider lines, only surface tonal layering."
    - "Shell/login entry surfaces and classroom launch inputs use one shared DESIGN.md-compliant ghost-focus interaction language."
    - "Login and classroom launch controls keep one dominant primary CTA while semantic feedback stays semantic-color first."
  artifacts:
    - path: "src/components/ui/ghost-field.ts"
      provides: "Shared ghost-focus field and toggle classes for auth and launch controls"
    - path: "src/components/home/home-login-card.tsx"
      provides: "Remember-me control without border/ring-led emphasis"
    - path: "src/components/classroom/classroom-launch-panel.tsx"
      provides: "Launch selects using shared no-line ghost-focus treatment"
    - path: "src/app/(auth)/login/LoginForm.tsx"
      provides: "Canonical auth form consumer of the shared ghost-focus contract"
  key_links:
    - from: "src/components/ui/ghost-field.ts"
      to: "src/app/globals.css"
      via: "surface and focus tokens"
      pattern: "focus-visible:outline-primary/20"
    - from: "src/app/(auth)/login/LoginForm.tsx"
      to: "src/components/ui/ghost-field.ts"
      via: "shared auth input classes"
      pattern: "ghostTextFieldClassName"
    - from: "src/components/home/home-login-card.tsx"
      to: "src/components/ui/ghost-field.ts"
      via: "shared remember-me toggle classes"
      pattern: "ghostToggleClassName"
    - from: "src/components/classroom/classroom-launch-panel.tsx"
      to: "src/components/ui/ghost-field.ts"
      via: "shared launch select classes"
      pattern: "ghostSelectFieldClassName"
---

<objective>
Close the remaining Phase 10 verification blockers so auth entry controls and
classroom launch fields fully comply with the no-line, ghost-focus interaction
language.

Purpose: Finish D-03, D-04, D-14, D-15, D-19, and D-22 where Plans 10-02 and
10-04 left local border/ring exceptions.
Output: One shared ghost-focus control contract plus updated login and launch
controls that remove the remaining 1px outline patterns.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-global-visual-polish/10-CONTEXT.md
@.planning/phases/10-global-visual-polish/10-VERIFICATION.md
@.planning/phases/10-global-visual-polish/10-02-SUMMARY.md
@.planning/phases/10-global-visual-polish/10-04-SUMMARY.md
@DESIGN.md

<interfaces>
From src/app/(auth)/login/LoginForm.tsx:
```tsx
const inputClassName =
  "rounded-full bg-surface-container-low px-4 py-3 text-sm text-on-surface outline-none transition-colors placeholder:text-on-surface-variant/70 focus:bg-surface-container-lowest focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-primary/20";
```

From src/components/home/home-login-card.tsx:
```tsx
const inputClassName =
  "w-full rounded-full bg-surface-container-low px-4 py-3 text-on-surface outline-none transition-colors placeholder:text-on-surface-variant/70 focus:bg-surface-container-lowest focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-primary/20";

<input className="rounded border-outline-variant bg-surface-container-low text-primary focus:ring-primary" type="checkbox" />
```

From src/components/classroom/classroom-launch-panel.tsx:
```tsx
const fieldClassName = 'w-full min-h-[52px] rounded-[1.25rem] bg-surface-container-low px-4 text-on-surface outline-none shadow-[inset_0_0_0_1px_rgba(89,92,94,0.08)] transition-colors focus:bg-surface-container-lowest focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-[rgba(0,80,212,0.16)]'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the shared ghost-focus control contract and remove the login checkbox exception</name>
  <files>src/components/ui/ghost-field.ts, src/app/(auth)/login/LoginForm.tsx, src/components/home/home-login-card.tsx</files>
  <read_first>
    - src/components/ui/button.tsx
    - src/app/globals.css
    - src/app/(auth)/login/LoginForm.tsx
    - src/components/home/home-login-card.tsx
    - .planning/phases/10-global-visual-polish/10-VERIFICATION.md
    - DESIGN.md
  </read_first>
  <action>Create `src/components/ui/ghost-field.ts` as the single shared contract for Phase 10 control focus styling per D-03 and D-04. Export concrete class strings named `ghostTextFieldClassName`, `ghostSelectFieldClassName`, and `ghostToggleClassName`. `ghostTextFieldClassName` must keep the existing tonal auth field behavior (`bg-surface-container-low`, `focus:bg-surface-container-lowest`, `focus-visible:outline-2`, `focus-visible:outline-offset-4`, `focus-visible:outline-primary/20`) and stay borderless. `ghostToggleClassName` must support a visible checked state without using `border-outline-variant`, `focus:ring-*`, or user-agent checkbox chrome; implement the remember-me control in `home-login-card.tsx` as a controlled pressed toggle plus hidden form field so the visible control uses the shared ghost-focus treatment and the submitted value remains explicit. Update `LoginForm.tsx` to import and use `ghostTextFieldClassName` so auth entry becomes the canonical consumer of the shared contract. Keep the existing single primary submit CTA and preserve semantic error treatment per D-14 and D-15.</action>
  <acceptance_criteria>
    - `src/components/ui/ghost-field.ts` exports `ghostTextFieldClassName`
    - `src/components/ui/ghost-field.ts` exports `ghostSelectFieldClassName`
    - `src/components/ui/ghost-field.ts` exports `ghostToggleClassName`
    - `src/app/(auth)/login/LoginForm.tsx` imports from `@/components/ui/ghost-field`
    - `src/components/home/home-login-card.tsx` imports from `@/components/ui/ghost-field`
    - `src/components/home/home-login-card.tsx` contains `aria-pressed`
    - `src/components/home/home-login-card.tsx` contains `name="rememberMe"`
    - `src/components/home/home-login-card.tsx` does not contain `border-outline-variant`
    - `src/components/home/home-login-card.tsx` does not contain `focus:ring`
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/components/ui/ghost-field.ts "src/app/(auth)/login/LoginForm.tsx" src/components/home/home-login-card.tsx && ! rg -n "border-outline-variant|focus:ring" src/components/home/home-login-card.tsx</automated>
  </verify>
  <done>Login entry controls all consume the shared ghost-focus contract, and the remember-me control no longer reintroduces border-led emphasis.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire classroom launch fields to the shared no-line field treatment</name>
  <files>src/components/classroom/classroom-launch-panel.tsx, src/components/ui/ghost-field.ts</files>
  <read_first>
    - src/components/classroom/classroom-launch-panel.tsx
    - src/components/ui/ghost-field.ts
    - src/app/globals.css
    - .planning/phases/10-global-visual-polish/10-VERIFICATION.md
    - .planning/phases/10-global-visual-polish/10-04-SUMMARY.md
    - DESIGN.md
  </read_first>
  <action>Update `classroom-launch-panel.tsx` to import `ghostSelectFieldClassName` from the shared control contract and apply it to both lesson and class `<select>` controls, closing the remaining Plan 10-04 gap per D-04, D-19, and D-22. Remove the local `shadow-[inset_0_0_0_1px_rgba(89,92,94,0.08)]` and custom RGBA outline recipe entirely; the launch panel must rely on the same tokenized ghost-focus language as login surfaces, while keeping the existing `开始课堂` dominant CTA, the tonal summary panel, and semantic error banner colors. Do not add a second hero, new gradient section, or custom focus style in this file.</action>
  <acceptance_criteria>
    - `src/components/classroom/classroom-launch-panel.tsx` imports `ghostSelectFieldClassName`
    - both launch `<select>` elements use `className={ghostSelectFieldClassName}`
    - `src/components/classroom/classroom-launch-panel.tsx` does not contain `0_0_0_1px`
    - `src/components/classroom/classroom-launch-panel.tsx` does not contain `outline-[rgba(0,80,212,0.16)]`
    - `src/components/classroom/classroom-launch-panel.tsx` still contains `开始课堂`
    - `src/components/classroom/classroom-launch-panel.tsx` still contains `bg-[#fef2f2]`
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/components/ui/ghost-field.ts src/components/classroom/classroom-launch-panel.tsx && ! rg -n "0_0_0_1px|outline-\[rgba\(0,80,212,0\.16\)\]" src/components/classroom/classroom-launch-panel.tsx</automated>
  </verify>
  <done>Classroom launch selects share the same ghost-focus contract as auth entry fields, with no local 1px outline fallback left in the shipped UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auth entry controls -> sign-in intent | focus and toggle clarity affect whether users submit the intended login state |
| classroom launch controls -> live session creation | launch field emphasis affects which lesson/class a teacher starts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | S | `home-login-card.tsx` | mitigate | Keep the remember-me toggle visibly focused and pressed with the shared ghost-focus treatment so the control state cannot be mistaken for a passive label. |
| T-10-06-02 | T | `home-login-card.tsx` | mitigate | Submit the remember-me state through a hidden input bound to the visible toggle so visual restyling does not silently drop the form value. |
| T-10-06-03 | S | `classroom-launch-panel.tsx` | mitigate | Reuse the same tokenized focus treatment as auth entry fields so lesson/class selection remains clearly attributable and keyboard-visible. |
| T-10-06-04 | I | `home-login-card.tsx`, `classroom-launch-panel.tsx` | mitigate | Preserve semantic error colors and text while removing border/ring drift so polish does not hide auth or launch failures. |
</threat_model>

<verification>
- `pnpm typecheck`
- `pnpm exec eslint src/components/ui/ghost-field.ts "src/app/(auth)/login/LoginForm.tsx" src/components/home/home-login-card.tsx src/components/classroom/classroom-launch-panel.tsx`
- `! rg -n "border-outline-variant|focus:ring|0_0_0_1px|outline-\[rgba\(0,80,212,0\.16\)\]" src/components/home/home-login-card.tsx src/components/classroom/classroom-launch-panel.tsx`
</verification>

<success_criteria>
- The two blocker patterns called out in `10-VERIFICATION.md` are removed from shipped source.
- Auth entry and classroom launch controls share one tokenized ghost-focus contract instead of local border/ring exceptions.
- `UI-04` can be re-verified without remaining 1px-outline findings in the targeted controls.
</success_criteria>

<output>
After completion, create `.planning/phases/10-global-visual-polish/10-06-SUMMARY.md`
</output>
