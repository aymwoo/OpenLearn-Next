---
phase: 10-global-visual-polish
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/components/surfaces/teacher-dashboard-surface.tsx
  - src/components/surfaces/lesson-editor-surface.tsx
  - src/components/surfaces/students-management-surface.tsx
autonomous: true
requirements: [UI-04]
must_haves:
  truths:
    - "Teacher-facing core pages feel denser and more like a compact command center than airy marketing layouts."
    - "Teacher dashboard gives enough width and hierarchy to daily rhythm and live classroom state at the same time."
    - "Teacher pages reuse fewer card styles and keep high-value information visible first on desktop and coherent on mobile."
  artifacts:
    - path: "src/components/surfaces/teacher-dashboard-surface.tsx"
      provides: "Dual-focus teacher workspace hierarchy"
    - path: "src/components/surfaces/lesson-editor-surface.tsx"
      provides: "Dense multi-panel teacher authoring shell"
    - path: "src/components/surfaces/students-management-surface.tsx"
      provides: "High-density teacher management surface"
  key_links:
    - from: "src/components/surfaces/teacher-dashboard-surface.tsx"
      to: ".planning/debug/teacher-dashboard-layout.md"
      via: "wider primary information region"
      pattern: "xl:grid-cols"
    - from: "src/components/surfaces/lesson-editor-surface.tsx"
      to: "src/components/ui/badge.tsx"
      via: "shared label and status density"
      pattern: "<Badge"
    - from: "src/components/surfaces/students-management-surface.tsx"
      to: "src/components/ui/button.tsx"
      via: "management CTA hierarchy"
      pattern: "<Button"
---

<objective>
Polish the teacher command-center surfaces so dashboard, editor, and management
views converge on one dense operational hierarchy.

Purpose: Implement D-06 to D-11 plus D-19 to D-22 on the highest-frequency
teacher pages.
Output: Updated teacher dashboard, lesson editor, and students management surface.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-global-visual-polish/10-CONTEXT.md
@.planning/phases/10-global-visual-polish/10-UI-SPEC.md
@.planning/phases/10-global-visual-polish/10-PATTERNS.md
@.planning/debug/teacher-dashboard-layout.md
@.planning/phases/08-stitch-mcp-integration/08-03-PLAN-SUMMARY.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebalance teacher dashboard into a dual-focus workspace</name>
  <files>src/components/surfaces/teacher-dashboard-surface.tsx</files>
  <read_first>
    - src/components/surfaces/teacher-dashboard-surface.tsx
    - .planning/debug/teacher-dashboard-layout.md
    - src/components/ui/badge.tsx
    - src/components/ui/button.tsx
    - DESIGN.md
  </read_first>
  <action>Refactor `teacher-dashboard-surface.tsx` so the dashboard reads as a compact dual-focus workspace per D-06 to D-11 and the debug note. Expand the primary information region before shrinking content per D-07: the daily rhythm schedule and live classroom stage must both have room, with the main headline and key modules no longer squeezed by the current column allocation. Reduce the number of ad hoc card recipes, replace route-local shadow values with shared tonal layering where possible, keep exactly one energized main stage for the live classroom, and keep warning/destructive chips semantic instead of blue-branded per D-15 and D-16.</action>
  <acceptance_criteria>
    - `src/components/surfaces/teacher-dashboard-surface.tsx` contains an `xl:grid-cols` split wider than the previous left-heavy squeeze pattern
    - `src/components/surfaces/teacher-dashboard-surface.tsx` keeps only one gradient live-classroom stage
    - `src/components/surfaces/teacher-dashboard-surface.tsx` uses fewer route-local `shadow-[` values than before
    - warning and destructive blocks remain semantic rather than `bg-primary`
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/components/surfaces/teacher-dashboard-surface.tsx</automated>
  </verify>
  <done>Teacher dashboard headline width, daily rhythm, and live-state modules feel balanced, dense, and clearly prioritized without 1px dividers.</done>
</task>

<task type="auto">
  <name>Task 2: Propagate teacher-side density language to editor and students pages</name>
  <files>src/components/surfaces/lesson-editor-surface.tsx, src/components/surfaces/students-management-surface.tsx</files>
  <read_first>
    - src/components/surfaces/lesson-editor-surface.tsx
    - src/components/surfaces/students-management-surface.tsx
    - src/components/ui/button.tsx
    - src/components/ui/card.tsx
    - .planning/phases/10-global-visual-polish/10-CONTEXT.md
  </read_first>
  <action>Update `lesson-editor-surface.tsx` and `students-management-surface.tsx` to share the same teacher-side density language per D-09 and D-10: fewer visual card styles, shorter high-information copy, tighter panel spacing, and stronger prioritization of current context over decorative blocks. Keep desktop as the quality bar per D-19, but preserve mobile clarity per D-20 to D-22 by collapsing secondary detail before hiding primary actions or active context. Reuse the shared Button/Badge/Card hierarchy instead of adding new one-off visual variants.</action>
  <acceptance_criteria>
    - both files keep `bg-surface-container-low` / `bg-surface-container-lowest` tonal layering instead of border sectioning
    - both files reuse shared `Button` and/or `Badge` primitives for primary controls and labels
    - mobile-specific wrappers favor preserving primary actions and current context before secondary stats
    - neither file introduces new component-local color tokens or border-based separators
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/components/surfaces/lesson-editor-surface.tsx src/components/surfaces/students-management-surface.tsx</automated>
  </verify>
  <done>Teacher editor and students management pages visually align with the same compact command-center language as the dashboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teacher command UI -> teacher actions | dense dashboards must still preserve clear action/state hierarchy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | S | `teacher-dashboard-surface.tsx` | mitigate | Keep live-classroom state and daily schedule clearly labeled so denser layout does not spoof which module is active or primary. |
| T-10-03-02 | I | `teacher-dashboard-surface.tsx`, `students-management-surface.tsx` | mitigate | Preserve semantic warning/destructive colors and status badges so operational risk states stay visible after polish. |
| T-10-03-03 | D | `lesson-editor-surface.tsx` | accept | This plan changes UI hierarchy only and does not add new data-fetch, mutation, or background processing paths. |
</threat_model>

<verification>
- `pnpm typecheck`
- `pnpm exec eslint src/components/surfaces/teacher-dashboard-surface.tsx src/components/surfaces/lesson-editor-surface.tsx src/components/surfaces/students-management-surface.tsx`
</verification>

<success_criteria>
- Teacher dashboard no longer feels width-constrained or visually split into competing card styles.
- Editor and management pages share the same dense teacher-side hierarchy.
- Desktop polish increases while mobile remains overflow-free and action-first.
</success_criteria>

<output>
After completion, create `.planning/phases/10-global-visual-polish/10-03-SUMMARY.md`
</output>
