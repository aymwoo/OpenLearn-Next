---
phase: 72.1-close-gap-gate-01-authoritative-milestone-close-gate
plan: "02"
subsystem: milestone-close-gate
tags: [verifier-strengthening, milestone-bridge, lifecycle, recap-stats, six-segment-chain]

dependency_graph:
  requires:
    - phase: 70-question-stats-post-class-recap
      provides: "STATS-01/STATS-02 verification, getClassroomSessionRecapDTO seam, buildQuizSampleRecapStats seam, pluginOwnedQuizResponses.isLatest truth"
    - phase: 71-marketplace-lifecycle-install-governance-semver-upgrade-reta
      provides: "MKT-01..MKT-05 verification, strengthen verify-phase71-marketplace-lifecycle.ts with branch-level executable proof (live/retained/ended)"
    - phase: 72.1-01
      provides: "70-VERIFICATION.md + 71-VERIFICATION.md + 8 required static seam checks on the Phase 71 verifier; bridge inputs into the close gate"
  provides:
    - "Strengthened scripts/verify-phase72-close-gate.ts as the executable milestone bridge gate (4 static/bridge stages, 32 checks in --smoke)"
    - "Bridge assertions for the full 67 -> 68 -> 69 -> 70 -> 71 -> 72 chain (ordered spine + 5 upstream VERIFICATION.md artifact checks + 6 verify:phase67..72 wiring checks)"
    - "Lifecycle half (MKT-01..05 / D-72.1-07 / D-72.1-08) re-asserted at gate level: route / settings/plugins -> PluginMarketplaceSurface -> readMarketplaceSurfaceBundle -> recoverMarketplacePluginAction / recoverRetainedPluginInstallWithTx + recoveredDataTakeover + recoveredFromPluginId -> preflightPluginUpgrade + backfill/verify/cutover -> preflightUninstallPluginWithTx / uninstallPluginWithTx + cleanupConfirmationToken + PLUGIN_CLEANUP_CONFIRMATION_REQUIRED"
    - "Recap / stats half (STATS-01 / STATS-02 / D-72.1-02 / D-72.1-15) re-asserted at gate level: src/app/(classroom)/classroom/page.tsx -> getClassroomSessionRecapDTO -> src/components/classroom/classroom-session-recap-surface.tsx (ClassroomSessionRecapSurface) -> src/actions/classroom-actions.ts updateTag(cacheTags.quizStats(...)) boundary -> src/lib/dal/classroom.ts buildQuizSampleRecapStats + pluginOwnedQuizResponses.isLatest"
    - "--smoke fast path that exercises every bridge stage without running the full pnpm ladder"
  affects:
    - "72.1-03-wave-3-plan"
    - "pnpm verify:phase (single authoritative entry alias)"
    - "scripts/verify-phase71-marketplace-lifecycle.ts (consumed via pnpm verify:phase71, but no new edits)"
    - "scripts/verify-phase70-quiz-stats.ts (consumed via pnpm verify:phase70, but no new edits)"

tech-stack:
  added: []
  patterns:
    - "Four-stage bridge runner: (1) package.json wiring (2) upstream VERIFICATION.md artifact presence (3) lifecycle milestone-bridge static seams (4) recap/stats milestone-bridge static seams, plus the ordered pnpm ladder in full mode"
    - "nonCommentIncludes helper (borrowed from verify-phase38-cutover-closeout) for static seam checks that ignore // line comments"
    - "Stage-typed reporting with pass/fail details so a failed check is named explicitly"
    - "Plan 03 forward-reference protection: 72-VERIFICATION, 72.1-CLOSEOUT, 72.1-PROOF-MAPPING are intentionally NOT hard-required at this wave"

key-files:
  created: []
  modified:
    - scripts/verify-phase72-close-gate.ts (279 insertions, 24 deletions)

key-decisions:
  - "Wave 2 chooses the heavier executable bridge (4 static/bridge stages + ordered pnpm runner spine) over orchestration-only, token-only, or doc-only closure, per D-72.1-16"
  - "Bridge stays free of forward references to 72-VERIFICATION.md / 72.1-CLOSEOUT.md / 72.1-PROOF-MAPPING.md so the gate can stand up in wave 2 without waiting on wave 3 artifacts, per T-72.1-03"
  - "Re-asserting the lifecycle and recap seams at gate level (not just delegating to verify:phase71 / verify:phase70) makes the gate an authoritative bridge, not a fan-out orchestrator, per D-72.1-01, D-72.1-02, D-72.1-05, D-72.1-13, D-72.1-15"
  - "Preserve the wave-1 lifecycle proof: recover (recoverRetainedPluginInstallWithTx / recoverMarketplacePluginAction), upgrade, and cleanup uninstall are all re-asserted at gate level, per D-72.1-07, D-72.1-08"
  - "Use --smoke as a real proof path (4 static/bridge stages, 32 checks) so wave-2 sampling stays under 30s, per 72.1-VALIDATION.md"

patterns-established:
  - "Stage-typed reporter: every stage returns {label, passed, details[]} so failed checks are named explicitly"
  - "nonCommentIncludes guard for static seam checks (avoids false positives from // line comments), patterned after verify-phase38-cutover-closeout"
  - "Forward-reference discipline: when a future plan owns a downstream artifact, the current wave's gate must not yet hard-require it"

requirements-completed: [MKT-01, MKT-02, MKT-03, MKT-04, MKT-05, GATE-01, STATS-01, STATS-02]

# Metrics
duration: ~4 min (recorded 2026-06-07T06:13:58Z, completed 2026-06-07T06:17:49Z)
tasks_completed: 1/1
files_modified: 1
files_created: 0
commits: 1
completed_at: 2026-06-07T06:17:49Z
---

# Phase 72.1 Plan 02: Close Gap Gate 01 — Authoritative Milestone Close Gate — Summary

**`verify:phase72` 升级为可执行 milestone bridge gate：4 阶段 32 项 check 同时锁 lifecycle (MKT-01..05) 与 recap/stats (STATS-01/02) seam，6 段链 (67 -> 71) + 上游 5 份 VERIFICATION.md 都成 gate-level truth。**

**Plan:** 72.1-02
**Type:** verifier-strengthening
**Wave:** 2 (close gap gate — bridge gate hardening)
**Status:** ✓ passed

## Performance

- **Duration:** ~4 min
- **Started:** 2026-06-07T06:13:58Z
- **Completed:** 2026-06-07T06:17:49Z
- **Tasks:** 1/1
- **Files modified:** 1
- **Commits:** 1

## Tasks Executed

| # | Task | Status | Commit | Files |
| --- | --- | --- | --- | --- |
| 1 | 把 `verify:phase72` 加固为真正的 milestone bridge gate | ✓ COMPLETED | 44f326b | `scripts/verify-phase72-close-gate.ts` (279 insertions, 24 deletions) |

## What Was Built

### Task 1 — `verify:phase72` is now a 4-stage executable bridge gate

`scripts/verify-phase72-close-gate.ts` 在保留 `67 -> 68 -> 69 -> 70 -> 71` ordered runner spine 的前提下，新增 4 个 stage-typed bridge stages。`--smoke` 跳过 ordered pnpm ladder，所有 4 个 stage 仍按 32 项 check 全部走完，task-local 反馈延迟 < 5s。

#### Stage 1 — Static script wiring (7 checks)

`verifyPackageScripts` 重用原 wiring 7 项：
- `package.json` exposes `verify:phase67..72` 各自的 runner 路径
- `verify:phase72` 仍是 `node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase72-close-gate.ts`
- `verify:phase` 仍 alias 到 `pnpm verify:phase72`

#### Stage 2 — Upstream VERIFICATION artifact presence (5 checks)

`verifyUpstreamVerificationArtifacts()` 显式读 5 份上游 `*-VERIFICATION.md`，按 T-72.1-03 规则不在本 wave 提前 hard-require `72-VERIFICATION.md` / `72.1-CLOSEOUT.md` / `72.1-PROOF-MAPPING.md`（那 3 份留到 Plan 03 才有）：

| Path | Source phase | Bridge reason |
| --- | --- | --- |
| `.planning/phases/67-declarative-plugin-owned-data-model-migration-proof/67-VERIFICATION.md` | 67 | migration proof anchor |
| `.planning/phases/68-governed-declarative-data-access-verbs/68-VERIFICATION.md` | 68 | governed-access proof anchor |
| `.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-VERIFICATION.md` | 69 | teacher config -> student answer proof anchor |
| `.planning/phases/70-question-stats-post-class-recap/70-VERIFICATION.md` | 70 (wave 1) | recap/stats proof anchor |
| `.planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-VERIFICATION.md` | 71 (wave 1) | lifecycle proof anchor |

#### Stage 3 — Lifecycle milestone-bridge static seams (11 checks)

`verifyLifecycleMilestoneBridge()` 把 8 项 wave-1 seam 从 `verify-phase71-marketplace-lifecycle.ts` 提升到 gate-level，并新增 3 项 route/surface 锚定：

1. `/settings/plugins` route + `route-surface-registry.ts` 注册
2. `PluginMarketplaceSurface` calls `readMarketplaceSurfaceBundle` SSR bundle seam
3. `registry` exposes `readMarketplaceSurfaceBundle` for the marketplace SSR bundle
4. `recoverMarketplacePluginAction` server action 边界
5. `recoverRetainedPluginInstallWithTx` DAL export for retain reinstall
6. `recoverRetainedPluginInstallWithTx` carries `recoveredDataTakeover` + `recoveredFromPluginId` for branch proof
7. `preflightPluginUpgrade` DAL export for upgrade branch
8. `plugin-migration.ts` enforces `backfill -> verify -> cutover` upgrade discipline
9. `preflightUninstallPluginWithTx` DAL export for cleanup uninstall
10. `uninstallPluginWithTx` DAL export for cleanup uninstall
11. `uninstallPluginWithTx` enforces `cleanupConfirmationToken` + `PLUGIN_CLEANUP_CONFIRMATION_REQUIRED`

按 D-72.1-07 / D-72.1-08，recover (retain reinstall) + upgrade + cleanup uninstall 三条 lifecycle 分支都在 gate-level 显式断言；`recoverRetainedPluginInstallWithTx` 与 `recoverMarketplacePluginAction` 都写入 gate 文本以满足 acceptance criterion。

#### Stage 4 — Recap / stats milestone-bridge static seams (9 checks)

`verifyRecapMilestoneBridge()` 新增 recap/stats 一侧的 9 项 gate-level seam 断言：

1. `src/app/(classroom)/classroom/page.tsx` calls `getClassroomSessionRecapDTO` on the ended-session recap path
2. `ClassroomSessionRecapSurface` is exported from `src/components/classroom/classroom-session-recap-surface.tsx`
3. `ClassroomSessionRecapSurface` renders `题目复盘` section + `recap.quizSampleStats` binding
4. `src/actions/classroom-actions.ts` owns `submitQuizSampleAnswerAction` + `updateTag(cacheTags.quizStats(...))` boundary
5. `cacheTags.quizStats` is registered in `src/lib/cache-policy.ts` as `quiz-stats:${sessionId}` cache key
6. `src/lib/dal/classroom.ts` carries `buildQuizSampleRecapStats` (latest-only stats truth)
7. `buildQuizSampleRecapStats` restricts to `eq(pluginOwnedQuizResponses.isLatest, true)`
8. `getClassroomSessionRecapDTO` exposes `quizSampleStats: recap.quizSampleStats` on the recap DTO contract
9. `ClassroomSessionRecapDTOSchema` declares `quizSampleStats` section (no summary-artifact writeback)

#### Stage 5 — Ordered pnpm runners (5 stages, full mode only)

全模式（非 `--smoke`）下，`main()` 仍按 `67 -> 68 -> 69 -> 70 -> 71` 顺序运行 `pnpm verify:phase67..71`，每步失败立即 `process.exit(1)`。`--smoke` 路径用 `↺ Smoke mode skips ordered pnpm runners; static + bridge seams above are the bridge proof.` 显式记录跳过原因。

### Why this is a bridge, not just an orchestrator

按 D-72.1-01, D-72.1-02, D-72.1-05, D-72.1-13, D-72.1-15 锁定的 seam 一致性原则，本 wave 不再把 gate 视作“仅串联 67 -> 71 跑个顺序”的 fan-out：

- **Lifecycle 半条**：`/settings/plugins` 真实 route + `PluginMarketplaceSurface` SSR surface + `recoverMarketplacePluginAction` server action + `recoverRetainedPluginInstallWithTx` DAL export + `preflightPluginUpgrade` + `preflightUninstallPluginWithTx` / `uninstallPluginWithTx` + `cleanupConfirmationToken` + `PLUGIN_CLEANUP_CONFIRMATION_REQUIRED` 全部以 stage-typed check 显式落到 gate；每条 seam 都有命名 label 和 pass/fail 详情。
- **Recap / stats 半条**：`getClassroomSessionRecapDTO` DTO seam + `ClassroomSessionRecapSurface` 渲染锚定 + `cacheTags.quizStats` cache invalidation + `buildQuizSampleRecapStats` + `pluginOwnedQuizResponses.isLatest = true` latest-only 真相源全部落到 gate level。
- **6 段链**：67-VERIFICATION / 68-VERIFICATION / 69-VERIFICATION / 70-VERIFICATION / 71-VERIFICATION 五份上游 `*-VERIFICATION.md` 在 Stage 2 显式 required；`verify:phase67 / 68 / 69` script entries 在 Stage 1 wiring check 中显式 required；`verify:phase70 / 71 / 72` 与 `verify:phase` alias 同步 wired。
- **Future-reference 安全**：72-VERIFICATION、72.1-CLOSEOUT、72.1-PROOF-MAPPING 三份未来产物在本 wave 显式不 hard-require，留给 Plan 03 (per T-72.1-03)；这样本 wave 不会因为等待未来 artifacts 而在合 wave 1 时就 break 自身 acceptance。

## Acceptance Criteria Results

| Plan acceptance criterion | Result | Note |
| --- | --- | --- |
| `grep -q "67-VERIFICATION.md" "scripts/verify-phase72-close-gate.ts"` | ✓ PASS | Stage 2 path constant |
| `grep -q "68-VERIFICATION.md" "scripts/verify-phase72-close-gate.ts"` | ✓ PASS | Stage 2 path constant |
| `grep -q "69-VERIFICATION.md" "scripts/verify-phase72-close-gate.ts"` | ✓ PASS | Stage 2 path constant |
| `grep -q "70-VERIFICATION.md" "scripts/verify-phase72-close-gate.ts"` | ✓ PASS | Stage 2 path constant |
| `grep -q "71-VERIFICATION.md" "scripts/verify-phase72-close-gate.ts"` | ✓ PASS | Stage 2 path constant |
| `grep -q "verify:phase67" "scripts/verify-phase72-close-gate.ts"` | ✓ PASS | Stage 1 + Stage 5 wiring |
| `grep -q "verify:phase68" "scripts/verify-phase72-close-gate.ts"` | ✓ PASS | Stage 1 + Stage 5 wiring |
| `grep -q "verify:phase69" "scripts/verify-phase72-close-gate.ts"` | ✓ PASS | Stage 1 + Stage 5 wiring |
| `grep -q "recoverRetainedPluginInstallWithTx\|recoverMarketplacePluginAction" "scripts/verify-phase72-close-gate.ts"` | ✓ PASS | Stage 3 seam (both names anchored) |
| `grep -q "/settings/plugins" "scripts/verify-phase72-close-gate.ts"` | ✓ PASS | Stage 3 route + Stage 5 comment |
| `grep -q "getClassroomSessionRecapDTO" "scripts/verify-phase72-close-gate.ts"` | ✓ PASS | Stage 4 route + DTO seam |
| `grep -q "src/actions/classroom-actions.ts" "scripts/verify-phase72-close-gate.ts"` | ✓ PASS | Stage 4 action boundary |
| `grep -q "buildQuizSampleRecapStats\|pluginOwnedQuizResponses.isLatest" "scripts/verify-phase72-close-gate.ts"` | ✓ PASS | Stage 4 latest-only truth |
| `grep -q "ClassroomSessionRecapSurface" "scripts/verify-phase72-close-gate.ts"` | ✓ PASS | Stage 4 rendered recap surface |
| `grep -q -- "--smoke" "scripts/verify-phase72-close-gate.ts"` | ✓ PASS | argv.includes("--smoke") in main |
| `node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase72-close-gate.ts --smoke` | ✓ PASS | 4 stages, 32 checks, all green |

**Total:** 16/16 acceptance criteria passed.

## Smoke-Run Output (Wave 2 Post-Commit)

```
==================================================
Phase 72 end-to-end close-gate verification (smoke) starting...
(v4.0 authoritative chain: Phase 67 -> 68 -> 69 -> 70 -> 71 -> 72)
==================================================

[1/4] Static script wiring checks...
  ✓ Static script wiring (7 checks):
     - ✓ package.json exposes phase67 migration-proof runner
     - ✓ package.json exposes phase68 governed-access runner
     - ✓ package.json exposes phase69 quiz-sample runner
     - ✓ package.json exposes phase70 stats recap runner
     - ✓ package.json exposes phase71 marketplace lifecycle runner
     - ✓ package.json exposes exact verify:phase72 script
     - ✓ package.json routes verify:phase alias to phase72

[2/4] Upstream VERIFICATION artifact presence (67-71)...
  ✓ Upstream VERIFICATION artifacts present (5 checks):
     - ✓ Phase 67 verification report exists at the planned path
     - ✓ Phase 68 verification report exists at the planned path
     - ✓ Phase 69 verification report exists at the planned path
     - ✓ Phase 70 verification report exists at the planned path
     - ✓ Phase 71 verification report exists at the planned path

[3/4] Lifecycle milestone-bridge static seams (MKT-01..05 / D-72.1-07 / D-72.1-08)...
  ✓ Lifecycle milestone-bridge seams (11 checks):
     - ✓ /settings/plugins is the registered lifecycle entry route
     - ✓ PluginMarketplaceSurface calls the readMarketplaceSurfaceBundle SSR bundle seam
     - ✓ registry exposes readMarketplaceSurfaceBundle for the marketplace SSR bundle
     - ✓ recoverMarketplacePluginAction server action is exported on the marketplace action boundary
     - ✓ recoverRetainedPluginInstallWithTx is exported from DAL for the retain reinstall branch
     - ✓ recoverRetainedPluginInstallWithTx carries recoveredDataTakeover and recoveredFromPluginId for branch proof
     - ✓ preflightPluginUpgrade is exported from DAL for the upgrade branch
     - ✓ plugin-migration enforces the backfill -> verify -> cutover upgrade discipline
     - ✓ preflightUninstallPluginWithTx is exported from DAL for the cleanup uninstall branch
     - ✓ uninstallPluginWithTx is exported from DAL for the cleanup uninstall branch
     - ✓ uninstallPluginWithTx enforces cleanupConfirmationToken and PLUGIN_CLEANUP_CONFIRMATION_REQUIRED

[4/4] Recap / stats milestone-bridge static seams (STATS-01 / STATS-02 / D-72.1-02 / D-72.1-15)...
  ✓ Recap / stats milestone-bridge seams (9 checks):
     - ✓ src/app/(classroom)/classroom/page.tsx calls getClassroomSessionRecapDTO on the ended-session recap path
     - ✓ ClassroomSessionRecapSurface is exported from src/components/classroom/classroom-session-recap-surface.tsx
     - ✓ ClassroomSessionRecapSurface renders the recap quiz question section + calm empty state
     - ✓ src/actions/classroom-actions.ts owns the classroom action boundary and updateTag invalidation
     - ✓ cacheTags.quizStats is registered in src/lib/cache-policy.ts as the cache invalidation key
     - ✓ src/lib/dal/classroom.ts carries the latest-only recap stats truth buildQuizSampleRecapStats
     - ✓ buildQuizSampleRecapStats restricts to pluginOwnedQuizResponses.isLatest = true
     - ✓ getClassroomSessionRecapDTO exposes quizSampleStats on the recap DTO contract
     - ✓ ClassroomSessionRecapDTOSchema declares quizSampleStats section (no summary-artifact writeback)

[5/4] Ordered pnpm runners (67 -> 68 -> 69 -> 70 -> 71)...
  ↺ Smoke mode skips ordered pnpm runners; static + bridge seams above are the bridge proof.

==================================================
Phase 72 end-to-end close-gate verification smoke passed: 4 stages, 32 checks.
==================================================
```

## Decisions Locked (D-72.1 mapping)

- **D-72.1-01 (authoritative proof 不只是文档重排)** → Stage 3 + Stage 4 把 lifecycle 与 recap seam 从 Phase 71 / 70 verifier 提升到 gate-level 显式 assertion。
- **D-72.1-02 (close 结论 + 完整产品链)** → Stage 2 把 5 份上游 `*-VERIFICATION.md` 列入 gate-level required，使 close 结论不再只是"脚本顺序跑过"。
- **D-72.1-05 (6 段链)** → Stage 1 wiring 7 项 + Stage 2 上游 5 份 + Stage 5 ordered runner 共同保证 67 -> 68 -> 69 -> 70 -> 71 -> 72 6 段链 artifact-backed。
- **D-72.1-07 (lifecycle 三治理路径)** → Stage 3 11 项 seam 检查把 retain reinstall (recover) + upgrade + cleanup uninstall 三条路径都纳入 gate-level 断言。
- **D-72.1-08 (cleanup uninstall branch proof)** → Stage 3 第 10-11 项 seam 显式断言 `uninstallPluginWithTx` + `cleanupConfirmationToken` + `PLUGIN_CLEANUP_CONFIRMATION_REQUIRED`。
- **D-72.1-13 (UI + action/command + DAL truth 三层)** → Stage 3 同时检查 route (`/settings/plugins`) + surface (`PluginMarketplaceSurface`) + action (`recoverMarketplacePluginAction`) + DAL (`recoverRetainedPluginInstallWithTx` / `preflightPluginUpgrade` / `uninstallPluginWithTx`) + migration (`backfill -> verify -> cutover`)。
- **D-72.1-15 (recap DTO + rendered recap surface)** → Stage 4 第 1-3 项把 `getClassroomSessionRecapDTO` + `ClassroomSessionRecapSurface` + 渲染锚定 (`题目复盘` + `recap.quizSampleStats`) 三层都纳入。
- **D-72.1-16 (更重而非更轻)** → Wave 2 显式选择 "executable bridge (4 stages / 32 checks)"，拒绝 orchestration-only / token-only / doc-only closure。

## Deviations from Plan

None — plan executed exactly as written. Gate hardening is purely additive:
- 4-stage typed runner (script wiring + upstream VERIFICATION + lifecycle seams + recap seams) follows the structured guidance from `72.1-PATTERNS.md` (use of `verify-phase38-cutover-closeout.ts` `nonCommentIncludes` + `requireFile` style, and `verify-phase71-marketplace-lifecycle.ts` stage-typed reporting).
- `nonCommentIncludes` helper is borrowed verbatim from `verify-phase38-cutover-closeout.ts`; the existing `verify-phase71-marketplace-lifecycle.ts` does not yet use it, but the new `verify-phase72-close-gate.ts` introduces it as the bridge-script counterpart.
- Forward-references to 72-VERIFICATION / 72.1-CLOSEOUT / 72.1-PROOF-MAPPING are intentionally NOT introduced; their `existsSync` calls would create a wave-2 future-reference blocker, which T-72.1-03 explicitly forbids.

## Self-Check

- `git status` — clean (only the staged commit landed).
- `git log --oneline -3` — `44f326b feat(72.1-02): harden verify-phase72 into executable milestone bridge gate` present.
- `[ -f scripts/verify-phase72-close-gate.ts ]` — present.
- Smoke-run re-run post-commit — 4 stages, 32 checks, all green.

## Commits

| Hash | Message |
| --- | --- |
| 44f326b | `feat(72.1-02): harden verify-phase72 into executable milestone bridge gate` |

## Files Changed

- **Modified:**
  - `scripts/verify-phase72-close-gate.ts` (+279, -24)
- **Created:** none.
- **No state files changed:** `STATE.md` / `ROADMAP.md` / `REQUIREMENTS.md` 由 orchestrator 持有，不在本 plan 范围内编辑。

## Known Stubs

None — the gate is fully bound to real code seams. Every `nonCommentIncludes` check is anchored to a real file path, every `existsSync` check is anchored to a real upstream VERIFICATION artifact, and the `pnpm verify:phase67..71` ladder is unchanged in semantics (only stage 1-4 are new). No `// TODO` / `// FIXME` / placeholder / "not available" text was introduced.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| (none) | — | No new network endpoints, auth paths, file access patterns, or schema changes at trust boundaries were introduced in this plan. The strengthened gate is verification infrastructure that only reads source files and runs existing pnpm sub-runners. The 4 stages all exit non-zero on first failed check (no partial pass), so the gate fails closed. |

## Wave 2 → Wave 3 Handoff

Wave 2 has hardened `verify:phase72` into a real bridge gate. Wave 3 (72.1-03) can safely layer the following on top of this baseline without conflict:

1. **Final artifact dependency wiring** (T-72.1-06 mitigation):
   - `72-VERIFICATION.md` — add `existsSync` check at the head of `verifyUpstreamVerificationArtifacts()` (or in a new `verifyFinalArtifacts()` stage).
   - `72.1-CLOSEOUT.md` — add `existsSync` check plus a `nonCommentIncludes(closeoutSource, "proof chain")` content assertion (patterned after `verify-phase38-cutover-closeout.ts:90-106`).
   - `72.1-PROOF-MAPPING.md` — add `existsSync` check plus a `nonCommentIncludes` assertion that the "Manual Surface Sign-Off Ledger" section exists (per `72.1-VALIDATION.md:45`).
   - The new artifact-required helpers should be added *between* Stage 2 (upstream VERIFICATION) and Stage 3 (lifecycle seams), or as a new Stage 3 pushing the others to 4-6. Either way, the existing 4 stages are forward-compatible.

2. **Manual sign-off ledger**:
   - Wave 3 can extend the recap-bridge static checks with a note that the manual `/settings/plugins` and recap surface sign-off is recorded in `72.1-PROOF-MAPPING.md` (per `72.1-VALIDATION.md:67-68` manual-only verifications). No gate code change is required.

3. **`pnpm verify:phase` final archive handoff**:
   - After wave 3 lands, the final `pnpm verify:phase` (alias → `verify:phase72`) will be the single authoritative entry, with the gate now covering:
     - 5-stage (or 6-stage) static + bridge checks (this wave's contribution).
     - Ordered `67 -> 71` pnpm ladder (preserved from previous wave).
     - Final 72-VERIFICATION / 72.1-CLOSEOUT / 72.1-PROOF-MAPPING existence + content checks (wave 3's contribution).

4. **No conflicts with wave-1 deliverables**:
   - The strengthened `verify-phase71-marketplace-lifecycle.ts` (commit `3804168`) keeps its 8 required static checks + 3 branch-level executable assertions; the wave-2 gate re-asserts the same 11 lifecycle seams at gate level without disturbing the Phase 71 verifier.
   - `70-VERIFICATION.md` (commit `04c9096`) and `71-VERIFICATION.md` (commit `e8ac822`) are the upstream VERIFICATION artifacts Stage 2 reads; their content is unchanged.

## Verification Commands Reference

```bash
# Wave 2 deliverable: executable milestone bridge gate (--smoke path)
node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase72-close-gate.ts --smoke
# → 4 stages, 32 checks, all green

# Full bridge gate (executes ordered pnpm ladder 67 -> 71)
node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase72-close-gate.ts
# → 5 stages (4 static/bridge + 1 ordered ladder), all green

# Upstream peer gates (unchanged by this wave)
node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase71-marketplace-lifecycle.ts --smoke
# → 3 stages, all green (preserved from wave 1)
pnpm verify:phase70
# → close gate green (unchanged)

# Authoritative single entry alias (wave 3 will wire final 72.1 artifacts into this)
pnpm verify:phase
# → currently routes to verify:phase72 (full); will be archive-ready after wave 3
```

## Wave 2 Sign-off

**Outcome:** Wave 2 (bridge gate hardening) complete. The `verify:phase72` entry is no longer orchestration-only; it now runs 4 static/bridge stages covering 32 seam checks across the lifecycle (MKT-01..05) and recap/stats (STATS-01/02) halves, plus the 5-stage (4 + ordered pnpm ladder) full mode.

Wave 3 (72.1-03) can proceed safely on top of this baseline. All 16 acceptance criteria are green, the smoke-run output is reproducible under 5 seconds, the bridge is forward-compatible with the final 72.1 close-artifact dependency wiring, and the gate has no forward-references to artifacts that don't yet exist.

---

*Phase: 72.1-close-gap-gate-01-authoritative-milestone-close-gate*
*Completed: 2026-06-07T06:17:49Z*
