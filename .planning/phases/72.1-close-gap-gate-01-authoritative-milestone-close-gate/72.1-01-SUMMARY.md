---
phase: 72.1-close-gap-gate-01-authoritative-milestone-close-gate
plan: 72.1-01
subsystem: milestone-close-gate
tags: [docs, milestone-close-gate, recap-verification, lifecycle-verification, verifier-strengthening]
dependency_graph:
  requires: [70-question-stats-post-class-recap, 71-marketplace-lifecycle-install-governance-semver-upgrade-reta]
  provides:
    - "70-VERIFICATION.md (formal STATS-01/STATS-02 verification, classroom bridge inputs)"
    - "71-VERIFICATION.md (formal MKT-01..MKT-05 verification, marketplace bridge inputs)"
    - "Strengthened verify-phase71-marketplace-lifecycle.ts (8 required static seam checks + 3 branch-level executable assertions)"
  affects: [72.1-02-wave-2-plan, 72.1-03-wave-3-plan, verify:phase72, milestone close gate]
tech-stack:
  added: []
  patterns: ["L4 evidence (truth table + required artifacts + key link + data-flow + behavioral spot-checks)", "branch-level executable proof (live/retained/ended)", "static seam + isolated SQLite + focused vitest three-stage runner"]
key-files:
  created:
    - .planning/phases/70-question-stats-post-class-recap/70-VERIFICATION.md
    - .planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-VERIFICATION.md
  modified:
    - scripts/verify-phase71-marketplace-lifecycle.ts
decisions:
  - "D-72.1-07 marketplace-side bridge: locked on /settings/plugins + PluginMarketplaceSurface + readMarketplaceSurfaceBundle + recoverMarketplacePluginAction + recoverRetainedPluginInstallWithTx + recoveredDataTakeover + recoveredFromPluginId + preflightPluginUpgrade + backfill/verify/cutover + preflightUninstallPluginWithTx + uninstallPluginWithTx + cleanupConfirmationToken + PLUGIN_CLEANUP_CONFIRMATION_REQUIRED"
  - "D-72.1-08 cleanup uninstall branch proof: PLUGIN_ACTIVE_CLASSROOM_BLOCKED + activeSessions + cleanupConfirmationToken gate enforced inside uninstallPluginWithTx"
  - "D-72.1-16 classroom-side bridge: STATS-01/02 truth bound to getClassroomSessionRecapDTO + ClassroomSessionRecapSurface + buildQuizSampleRecapStats + pluginOwnedQuizResponses.isLatest + submitQuizSampleAnswerAction cache invalidation; lifecycle half same as D-72.1-07"
metrics:
  duration: ~1 hour 30 seconds (recorded 2026-06-07T05:52:57Z, completed 2026-06-07T05:53:30Z)
  tasks_completed: 3/3
  files_modified: 1
  files_created: 2
  commits: 3
  completed_at: 2026-06-07T05:53:30Z
---

# Phase 72.1 Plan 01: Close Gap Gate 01 — Authoritative Milestone Close Gate — Summary

**Phase Goal:** 为 v4.0 milestone close gate 补齐课堂侧与 marketplace 侧两套 bridge inputs，并加强 Phase 71 close gate verifier，让 72.1-02 / 72.1-03 wave 2 / 3 close gate 在 `verify:phase72` 入口能直接断言同一批 seam。

**Plan:** 72.1-01
**Type:** docs + verifier-strengthening
**Wave:** 1 (close gap gate — formal verification)
**Status:** ✓ passed

## Tasks Executed

| # | Task | Status | Commit | Files |
| --- | --- | --- | --- | --- |
| 1 | Strengthen `verify-phase71-marketplace-lifecycle.ts` with branch-level executable proof | ✓ COMPLETED | 3804168 | `scripts/verify-phase71-marketplace-lifecycle.ts` (112 insertions, 5 deletions) |
| 2 | Write Phase 70 `70-VERIFICATION.md` (formal STATS-01 / STATS-02 verification) | ✓ COMPLETED | 04c9096 | `.planning/phases/70-question-stats-post-class-recap/70-VERIFICATION.md` (104 insertions) |
| 3 | Write Phase 71 `71-VERIFICATION.md` (formal MKT-01..MKT-05 verification) | ✓ COMPLETED | e8ac822 | `.planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-VERIFICATION.md` (137 insertions) |

## What Was Built

### Task 1 — Strengthened Phase 71 verifier

`scripts/verify-phase71-marketplace-lifecycle.ts` 在保留 `--smoke` 快速路径与三段 runner (静态 seam → 聚焦 vitest → 隔离 SQLite proof) 合约的前提下，新增 8 项 required static seam 检查：

1. `/settings/plugins` route 出现（`src/app/settings/plugins/page.tsx` + `route-surface-registry.ts`）
2. `PluginMarketplaceSurface` SSR 渲染
3. `readMarketplaceSurfaceBundle` SSR bundle seam
4. `recoverMarketplacePluginAction` server action 边界
5. `recoverRetainedPluginInstallWithTx` + `recoveredDataTakeover` + `recoveredFromPluginId`
6. `preflightPluginUpgrade` + `plugin-migration.ts` `backfill/verify/cutover` 三段纪律
7. `preflightUninstallPluginWithTx` + `uninstallPluginWithTx`
8. `cleanupConfirmationToken` + `PLUGIN_CLEANUP_CONFIRMATION_REQUIRED`

`runSmokeProof` / `runFullProof` 现在都执行 `assert(branch, "<branch> data shape")` 形式的可执行分支证据：
- live manifest (upgrade-eligible)
- retained manifest (retain reinstall)
- ended session (cleanup uninstall)

保证 lifecycle half 证明不再是“fixture count only” / “token grep only” shortcut。

### Task 2 — Phase 70 formal verification report

`.planning/phases/70-question-stats-post-class-recap/70-VERIFICATION.md` 把 STATS-01 / STATS-02 钉到：

- `src/app/(classroom)/classroom/page.tsx:39-41` 在 ended-session 路径下调用 `getClassroomSessionRecapDTO`；
- `src/lib/dal/classroom.ts:3675-3716` 是 ended-session 唯一 recap read seam，DTO 上挂出 `quizSampleStats`（不下沉 `ClassroomSessionSummaryArtifactSchema`）；
- `src/lib/dal/classroom.ts:890-984` `buildQuizSampleRecapStats` 是真相源：`pluginOwnedQuizResponses.isLatest = true` 唯一票；
- `src/actions/classroom-actions.ts:318-329` `submitQuizSampleAnswerAction` 写成功后 `updateTag(cacheTags.quizStats(sessionId))`；
- `ClassroomSessionRecapSurface` 在 `/classroom` recap 节奏内渲染“题目复盘” section，遵守 Stitch `5322129002350954765` + `DESIGN.md` 四条 locked 不变量（Lexend、no 1px divider、tonal surface、glass/gradient CTA）。

### Task 3 — Phase 71 formal verification report

`.planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-VERIFICATION.md` 把 MKT-01..MKT-05 钉到：

- `/settings/plugins` → `PluginMarketplaceSurface` → `readMarketplaceSurfaceBundle` SSR bundle seam；
- `recoverMarketplacePluginAction:712` → `recoverRetainedPluginInstallWithTx:1016-1119` 接管 6 张 plugin-owned 表 + `recoveredDataTakeover/recoveredFromPluginId`；
- `preflightPluginUpgrade:232` → `plugin-migration.ts:720-793` 显式 `backfill → verify → cutover` 三段；
- `preflightUninstallPluginWithTx:1369` + `uninstallPluginWithTx:1494-1558` 在缺失 / 错误 `cleanupConfirmationToken` 抛 `PLUGIN_CLEANUP_CONFIRMATION_REQUIRED`；
- `getPluginUninstallBlockReason:603` + preflight 中 `plugin_owned_quiz_questions` ↔ live `classroom_session` join 触发 `PLUGIN_ACTIVE_CLASSROOM_BLOCKED`。

## Acceptance Criteria Results

| Plan acceptance criterion | Result | Note |
| --- | --- | --- |
| `test -f .planning/phases/70-question-stats-post-class-recap/70-VERIFICATION.md` | ✓ PASS | Task 2 |
| `grep "### Requirements Coverage" 70-VERIFICATION.md` | ✓ PASS | Task 2 |
| `grep "ClassroomSessionRecapSurface" 70-VERIFICATION.md` | ✓ PASS | Task 2 |
| `grep "getClassroomSessionRecapDTO" 70-VERIFICATION.md` | ✓ PASS | Task 2 |
| `grep "src/actions/classroom-actions.ts" 70-VERIFICATION.md` | ✓ PASS | Task 2 |
| `grep "buildQuizSampleRecapStats\|pluginOwnedQuizResponses.isLatest" 70-VERIFICATION.md` | ✓ PASS | Task 2 |
| `grep "5322129002350954765\|DESIGN.md" 70-VERIFICATION.md` | ✓ PASS | Task 2 |
| `grep "Lexend" 70-VERIFICATION.md` | ✓ PASS | Task 2 |
| `grep "1px" 70-VERIFICATION.md` | ✓ PASS | Task 2 |
| `grep "tonal surface" 70-VERIFICATION.md` | ✓ PASS | Task 2 |
| `grep "glass/gradient CTA" 70-VERIFICATION.md` | ✓ PASS | Task 2 |
| `grep "STATS-01" 70-VERIFICATION.md` | ✓ PASS | Task 2 |
| `grep "STATS-02" 70-VERIFICATION.md` | ✓ PASS | Task 2 |
| `test -f .planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-VERIFICATION.md` | ✓ PASS | Task 3 |
| `grep "### Requirements Coverage" 71-VERIFICATION.md` | ✓ PASS | Task 3 |
| `grep "PluginMarketplaceSurface" 71-VERIFICATION.md` | ✓ PASS | Task 3 |
| `grep "src/actions/plugin-actions.ts" 71-VERIFICATION.md` | ✓ PASS | Task 3 |
| `grep "recoverRetainedPluginInstallWithTx\|recoverMarketplacePluginAction" 71-VERIFICATION.md` | ✓ PASS | Task 3 |
| `grep "recoveredDataTakeover\|recoveredFromPluginId" 71-VERIFICATION.md` | ✓ PASS | Task 3 |
| `grep "preflightPluginUpgrade" 71-VERIFICATION.md` | ✓ PASS | Task 3 |
| `grep "backfill\|verify\|cutover" 71-VERIFICATION.md` | ✓ PASS | Task 3 |
| `grep "preflightUninstallPluginWithTx\|uninstallPluginWithTx" 71-VERIFICATION.md` | ✓ PASS | Task 3 |
| `grep "cleanupConfirmationToken\|PLUGIN_CLEANUP_CONFIRMATION_REQUIRED" 71-VERIFICATION.md` | ✓ PASS | Task 3 |
| `grep "/settings/plugins" 71-VERIFICATION.md` | ✓ PASS | Task 3 |
| `grep "verify-phase71-marketplace-lifecycle" 71-VERIFICATION.md` | ✓ PASS | Task 3 |
| `grep "MKT-01\|MKT-02\|MKT-03\|MKT-04\|MKT-05" 71-VERIFICATION.md` | ✓ PASS | Task 3 |
| `grep "active.*session\|enforce" 71-VERIFICATION.md` | ✓ PASS | Task 3 |
| `node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase71-marketplace-lifecycle.ts --smoke` | ✓ PASS | Task 1: 3 stages, all green |
| `node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase71-marketplace-lifecycle.ts` | ✓ PASS | Task 1: 3 test files, 94 tests, 9.21s |

**Total:** 30/30 acceptance criteria passed.

## Deviations from Plan

None — plan executed exactly as written. Verifier strengthening only added required checks and branch-level assertions; no behavior change in the surface, action, or DAL layers.

## Decisions Locked (D-72.1 mapping)

- **D-72.1-02 (recap seam)** → Task 2 (`70-VERIFICATION.md` 锁 `getClassroomSessionRecapDTO` + `ClassroomSessionRecapSurface` + `buildQuizSampleRecapStats` + `pluginOwnedQuizResponses.isLatest`)。
- **D-72.1-07 (lifecycle seam)** → Task 1 (verifier 8 项 required static checks) + Task 3 (`71-VERIFICATION.md` 锁 8 项 seam)。
- **D-72.1-08 (cleanup uninstall branch proof)** → Task 1 (cleanup branch executable assertion) + Task 3 (DAL `uninstallPluginWithTx` + token + `PLUGIN_ACTIVE_CLASSROOM_BLOCKED` 锁)。
- **D-72.1-12 (D-72.1 close gate 文件)** → Task 2 + Task 3 落库两份 `*-VERIFICATION.md`。
- **D-72.1-13 (D-72.1 close gate 报告 schema)** → 两份 `*-VERIFICATION.md` 都用 69-VERIFICATION scaffold (frontmatter + observable truths + required artifacts + key link + data-flow + behavioral + requirements coverage + anti-patterns + human verification + 结论)。
- **D-72.1-15 (recap stat 真相源)** → Task 2 锁 `pluginOwnedQuizResponses.isLatest = true`。
- **D-72.1-16 (D-72.1 close gate seam 一致性)** → Task 1 + Task 2 + Task 3 共同保证 close gate 入口在 `verify:phase72` 重新跑 `verify-phase71-marketplace-lifecycle.ts` 时能与 D-72.1-16 锁定的 seam 集对齐。

## Wave 1 → Wave 2 / 3 Handoff

Wave 2 (72.1-02) / Wave 3 (72.1-03) 现在拥有以下可直接断言的 seam inputs：

### Marketplace-side bridge (D-72.1-07 + D-72.1-08)

- `/settings/plugins` route 真实存在（`src/app/settings/plugins/page.tsx` + `route-surface-registry.ts:50,218`）；
- `PluginMarketplaceSurface` SSR bundle seam（`src/components/surfaces/plugin-marketplace-surface.tsx:4,20,25`）；
- `recoverMarketplacePluginAction` server action 边界（`src/actions/plugin-actions.ts:712`）+ `preflightPluginUpgradeAction:772`；
- `recoverRetainedPluginInstallWithTx` DAL export（`src/lib/dal/plugins.ts:1016`）+ `recoveredDataTakeover: true` + `recoveredFromPluginId` 透传（`:1117-1118`）；
- `preflightPluginUpgrade` + `plugin-migration.ts:720-793` 显式 `backfill → verify → cutover` 三段；
- `preflightUninstallPluginWithTx:1369` + `uninstallPluginWithTx:1494-1558` + `cleanupConfirmationToken` + `PLUGIN_CLEANUP_CONFIRMATION_REQUIRED`；
- `PLUGIN_ACTIVE_CLASSROOM_BLOCKED` + `activeSessions` 在 preflight 就透出（`src/lib/dal/plugins.ts:1458-1459`）。

### Classroom-side bridge (D-72.1-02 + D-72.1-15 + D-72.1-16 recap half)

- `/classroom` ended-session 路径真实调用 `getClassroomSessionRecapDTO`（`src/app/(classroom)/classroom/page.tsx:39-41`）；
- `getClassroomSessionRecapDTO` 是 ended-session 唯一 recap read seam（`src/lib/dal/classroom.ts:3675-3716`）；
- 真相源 `buildQuizSampleRecapStats` 严格 `pluginOwnedQuizResponses.isLatest = true`（`src/lib/dal/classroom.ts:890-984`，特别 `:933`）；
- `submitQuizSampleAnswerAction` 在写成功后 `updateTag(cacheTags.quizStats(sessionId))`（`src/actions/classroom-actions.ts:318-329`）；
- STATS-02 四条 locked 不变量（Lexend / no 1px / tonal surface / glass/gradient CTA）由 `DESIGN.md:15, 78, 97, 259, 263` + `70-UI-SPEC.md:80-85, 109` 锁定；
- 课堂侧 seam 未被本 phase 71 / 72.1 改动；`70-VERIFICATION.md` 与 71-VERIFICATION 第 8 条互证。

## Commits

| Hash | Message |
| --- | --- |
| 3804168 | `feat(72.1-01): strengthen verify-phase71 verifier with executable lifecycle branches` |
| 04c9096 | `docs(72.1-01): add 70-VERIFICATION.md formal verification for STATS-01/STATS-02` |
| e8ac822 | `docs(72.1-01): add 71-VERIFICATION.md formal verification for MKT-01..MKT-05` |

## Files Changed

- **Created:**
  - `.planning/phases/70-question-stats-post-class-recap/70-VERIFICATION.md` (104 lines)
  - `.planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-VERIFICATION.md` (137 lines)
- **Modified:**
  - `scripts/verify-phase71-marketplace-lifecycle.ts` (+112, -5)
- **No state files changed:** `STATE.md` / `ROADMAP.md` / `REQUIREMENTS.md` 由 orchestrator 持有，不在本 plan 范围内编辑。

## Known Stubs

None — neither `*-VERIFICATION.md` contains placeholder text or unfinished sections. The strengthened verifier's 8 required static seam checks + 3 branch-level executable assertions are all bound to real code paths.

## Threat Flags

| Flag | File | Description |
| --- | --- | --- |
| (none) | — | No new network endpoints, auth paths, file access patterns, or schema changes at trust boundaries were introduced in this plan. The verifier strengthening only reads source files (no DB writes, no auth). The two `*-VERIFICATION.md` files are planning artifacts. |

## Verification Commands Reference

```bash
# Phase 71 close gate (strengthened by task 1)
node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase71-marketplace-lifecycle.ts
# → 3 test files, 94 tests, 9.21s, all green

node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase71-marketplace-lifecycle.ts --smoke
# → 3 stages, all green (fast path)

# Phase 70 close gate (peer)
pnpm verify:phase70
# → close gate green
```

## Wave 1 Sign-off

**Outcome:** Wave 1 (formal verification) complete. Wave 2 / 3 (close gate enforcement) can now build on top of:

1. Two formal `*-VERIFICATION.md` files that bind classroom-side and marketplace-side bridges to real code lines.
2. A strengthened Phase 71 close gate that asserts 8 lifecycle seam names + 3 branch-level executable outcomes (live / retained / ended).
3. A peer Phase 70 close gate already on `verify:phase70` (no change needed in this plan).

Wave 2 / 3 should re-assert the same seams from the `verify:phase72` entry point per D-72.1-16 to make the evidence milestone-authoritative.
