---
phase: 72.1-close-gap-gate-01-authoritative-milestone-close-gate
plan: "02"
type: execute
wave: 2
depends_on:
  - 72.1-01
files_modified:
  - scripts/verify-phase72-close-gate.ts
autonomous: true
requirements:
  - MKT-01
  - MKT-02
  - MKT-03
  - MKT-04
  - MKT-05
  - GATE-01
  - STATS-01
  - STATS-02
must_haves:
  truths:
    - "`verify:phase72` is no longer orchestration-only; it enforces executable milestone bridge checks over both the lifecycle chain and the recap/stats chain (`getClassroomSessionRecapDTO` -> `src/actions/classroom-actions.ts` -> latest-only stats truth -> `ClassroomSessionRecapSurface`) plus the six-segment chain per D-72.1-01, D-72.1-02, D-72.1-05, D-72.1-13, and D-72.1-15."
    - "Retained reinstall is asserted at gate level, not merely described in docs, and recap/stats proof is also asserted at gate level instead of remaining document-only, satisfying D-72.1-07 and D-72.1-08."
    - "This wave explicitly chooses the heavier executable bridge path over orchestration-only, token-only, or doc-only closure for the gate seam per D-72.1-16."
    - "Final 72.1 close-artifact dependency wiring is intentionally deferred to Plan 03 so this wave has no future-reference dependency."
  artifacts:
    - path: "scripts/verify-phase72-close-gate.ts"
      provides: "single authoritative milestone gate with executable lifecycle + recap bridge assertions"
  key_links:
    - from: "scripts/verify-phase72-close-gate.ts"
      to: "package.json"
      via: "single authoritative `verify:phase` alias"
      pattern: "\"verify:phase\": \"pnpm verify:phase72\""
    - from: "scripts/verify-phase72-close-gate.ts"
      to: "scripts/verify-phase71-marketplace-lifecycle.ts"
      via: "gate-level retained reinstall / upgrade / cleanup branch assertions"
      pattern: "recoverRetainedPluginInstallWithTx|recoverMarketplacePluginAction|preflightPluginUpgrade|uninstallPluginWithTx"
    - from: "scripts/verify-phase72-close-gate.ts"
      to: "src/lib/dal/classroom.ts"
      via: "gate-level recap DTO + latest-only stats truth assertions"
      pattern: "getClassroomSessionRecapDTO|buildQuizSampleRecapStats|pluginOwnedQuizResponses\\.isLatest"
    - from: "scripts/verify-phase72-close-gate.ts"
      to: "src/actions/classroom-actions.ts and src/components/classroom/classroom-session-recap-surface.tsx"
      via: "gate-level classroom action boundary + rendered recap surface assertions"
      pattern: "src/actions/classroom-actions\\.ts|cacheTags\\.quizStats|ClassroomSessionRecapSurface"
---

<objective>
把 `verify:phase72` 补强成真正的 executable milestone bridge gate，为下一 wave 的 final artifact wiring 与 archive-ready closeout 提供稳定基线。

Purpose: 解决 orchestration-only / recap doc-only 的核心缺口，但不在本 wave 引入对未来 Plan 03 artifacts 的前向依赖。
Output: strengthened `scripts/verify-phase72-close-gate.ts`.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v4.0-MILESTONE-AUDIT.md
@.planning/phases/72.1-close-gap-gate-01-authoritative-milestone-close-gate/72.1-CONTEXT.md
@.planning/phases/72.1-close-gap-gate-01-authoritative-milestone-close-gate/72.1-RESEARCH.md
@.planning/phases/72.1-close-gap-gate-01-authoritative-milestone-close-gate/72.1-PATTERNS.md
@.planning/phases/72-end-to-end-verify-phase-close-gate/72-CONTEXT.md
@.planning/phases/72-end-to-end-verify-phase-close-gate/72-VALIDATION.md
@.planning/phases/72-end-to-end-verify-phase-close-gate/72-01-SUMMARY.md
@.planning/phases/70-question-stats-post-class-recap/70-VERIFICATION.md
@.planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-VERIFICATION.md

<interfaces>
From `scripts/verify-phase72-close-gate.ts`:
```ts
function verifyPackageScripts(packageSource: string): StaticCheck[]
function main()
```

Required final artifact dependency chain (completed in Plan 03 after artifacts exist):
```text
verify:phase67 -> verify:phase68 -> verify:phase69 -> verify:phase70 -> verify:phase71 -> verify:phase72
verify:phase72 must refuse to pass without:
70-VERIFICATION.md
71-VERIFICATION.md
72-VERIFICATION.md
72.1-CLOSEOUT.md
72.1-PROOF-MAPPING.md
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 把 `verify:phase72` 加固为真正的 milestone bridge gate</name>
  <read_first>
    - scripts/verify-phase72-close-gate.ts
    - scripts/verify-phase71-marketplace-lifecycle.ts
    - .planning/phases/72.1-close-gap-gate-01-authoritative-milestone-close-gate/72.1-PATTERNS.md
    - .planning/phases/70-question-stats-post-class-recap/70-VERIFICATION.md
    - .planning/phases/71-marketplace-lifecycle-install-governance-semver-upgrade-reta/71-VERIFICATION.md
    - src/app/settings/plugins/page.tsx
    - src/components/surfaces/plugin-marketplace-surface.tsx
    - src/features/platform-core/actions/registry.ts
    - src/app/(classroom)/classroom/page.tsx
    - src/components/classroom/classroom-session-recap-surface.tsx
    - src/actions/classroom-actions.ts
    - src/lib/dal/classroom.ts
  </read_first>
  <files>scripts/verify-phase72-close-gate.ts</files>
  <acceptance_criteria>
    - `grep -q "67-VERIFICATION.md" "scripts/verify-phase72-close-gate.ts"`
    - `grep -q "68-VERIFICATION.md" "scripts/verify-phase72-close-gate.ts"`
    - `grep -q "69-VERIFICATION.md" "scripts/verify-phase72-close-gate.ts"`
    - `grep -q "70-VERIFICATION.md" "scripts/verify-phase72-close-gate.ts"`
    - `grep -q "71-VERIFICATION.md" "scripts/verify-phase72-close-gate.ts"`
    - `grep -q "verify:phase67" "scripts/verify-phase72-close-gate.ts"`
    - `grep -q "verify:phase68" "scripts/verify-phase72-close-gate.ts"`
    - `grep -q "verify:phase69" "scripts/verify-phase72-close-gate.ts"`
    - `grep -q "recoverRetainedPluginInstallWithTx\|recoverMarketplacePluginAction" "scripts/verify-phase72-close-gate.ts"`
    - `grep -q "/settings/plugins" "scripts/verify-phase72-close-gate.ts"`
    - `grep -q "getClassroomSessionRecapDTO" "scripts/verify-phase72-close-gate.ts"`
    - `grep -q "src/actions/classroom-actions.ts" "scripts/verify-phase72-close-gate.ts"`
    - `grep -q "buildQuizSampleRecapStats\|pluginOwnedQuizResponses.isLatest" "scripts/verify-phase72-close-gate.ts"`
    - `grep -q "ClassroomSessionRecapSurface" "scripts/verify-phase72-close-gate.ts"`
    - `grep -q -- "--smoke" "scripts/verify-phase72-close-gate.ts"`
    - `node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase72-close-gate.ts --smoke`
  </acceptance_criteria>
  <action>Before editing, run GitNexus upstream impact analysis for the touched gate symbols in `scripts/verify-phase72-close-gate.ts` (`main`, `verifyPackageScripts`, and any new static-check helper) per AGENTS. Then use the current file plus the analog `scripts/verify-phase38-cutover-closeout.ts` named in `72.1-PATTERNS.md` to add milestone-bridge checks without forking a second runner. Keep the ordered `67 -> 68 -> 69 -> 70 -> 71 -> 72` execution spine intact and wire the gate so the full six-segment bridge is artifact-backed, not just 70/71. The bridge must reference `67-VERIFICATION.md`, `68-VERIFICATION.md`, `69-VERIFICATION.md`, `70-VERIFICATION.md`, `71-VERIFICATION.md` and the corresponding `verify:phase67` / `verify:phase68` / `verify:phase69` script entries alongside the lifecycle and recap bridge seams, so the gate cannot run while any upstream phase proof is missing its formal report. Do not hard-require `72-VERIFICATION.md`, `72.1-CLOSEOUT.md`, or `72.1-PROOF-MAPPING.md` yet; those final artifact dependencies belong to Plan 03 because those files do not exist until then. Preserve the lifecycle proof from 72.1-01 intact and keep gate-level retained reinstall coverage by asserting that the executable lifecycle proof lane still contains the recover branch (`recoverRetainedPluginInstallWithTx` or `recoverMarketplacePluginAction`) alongside `upgrade` and `cleanup uninstall`. In the same gate stage, add recap/stats milestone-bridge assertions that explicitly bind the real route/surface/action/data seams: `src/app/(classroom)/classroom/page.tsx` still calls `getClassroomSessionRecapDTO`, `src/components/classroom/classroom-session-recap-surface.tsx` still renders `ClassroomSessionRecapSurface`, `src/actions/classroom-actions.ts` still owns the classroom action/cache boundary (`cacheTags.quizStats` / `updateTag` path), and `src/lib/dal/classroom.ts` still carries latest-only stats truth (`buildQuizSampleRecapStats` / `pluginOwnedQuizResponses.isLatest`). This task must make recap proof a sibling gate-level truth to lifecycle proof, not a documentation reference. This task explicitly owns D-72.1-16 for the gate seam: lighter shortcuts such as orchestration-only success, token-only greps, or doc-only recap closure are forbidden when a heavier executable bridge proof is available. Add a `--smoke` fast path that exercises only the static/bridge stage for task-local verification; reserve the full alias/phase gate for later waves. Before committing this code-editing task, run `gitnexus_detect_changes()` and confirm the blast radius remains inside the intended Phase 72 close-gate script surface.</action>
  <verify>
    <automated>node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase72-close-gate.ts --smoke</automated>
  </verify>
  <done>`verify:phase72` now contains executable lifecycle + recap bridge behavior with a fast smoke path, while the final 72.1 close-artifact dependency wiring is intentionally deferred to Plan 03 to avoid future-reference breakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| milestone gate -> upstream phase proofs | 若 Phase 72 只顺序执行脚本而不验证 proof artifacts 和 branch coverage，authoritative close 仍是空壳 |
| executable bridge -> future archive artifacts | 若本 wave 提前依赖未来产物，会制造 future-reference blocker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72.1-03 | T | `scripts/verify-phase72-close-gate.ts` | mitigate | 在本 wave 只硬化 executable lifecycle/recap bridge，不提前依赖 72.1 close artifacts |
| T-72.1-04 | R | milestone bridge posture | mitigate | 保持 final artifact-dependent wiring 延后到 Plan 03，避免文档/脚本承诺超前 |
</threat_model>

<verification>
- `node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase72-close-gate.ts --smoke`
</verification>

<success_criteria>
- `verify:phase72` 已包含 lifecycle + recap 的 executable milestone bridge behavior。
- 该 bridge behavior 仍未提前引用 72.1 close artifacts，避免 future-reference blocker。
</success_criteria>

<output>
After completion, create `.planning/phases/72.1-close-gap-gate-01-authoritative-milestone-close-gate/72.1-02-SUMMARY.md`
</output>
