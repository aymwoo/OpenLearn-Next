---
phase: 76-v4-2-authoritative-close-gate
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scripts/verify-phase76-v42-close-gate.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Stage 1 (v4.0 gate regression) 和 Stage 2 (v4.1 quiz multi-type verification) 在 outer gate 中接线完成，能执行 pnpm verify:phase72 和 pnpm verify:phase73 && pnpm verify:phase73-v41-close-gate"
    - "Stage 1 和 Stage 2 的验证结果被 outer gate 正确消费：exit code 0 → passed，非 0 → failed 并阻止后续 stage"
    - "verify:phase 仍冻结在 pnpm verify:phase72 && pnpm verify:phase73-v41-close-gate（per D-13）"
  artifacts:
    - path: "scripts/verify-phase76-v42-close-gate.ts"
      provides: "Stage 1 + Stage 2 wired with real verification commands"
      contains: "pnpm verify:phase72"
  key_links:
    - from: "scripts/verify-phase76-v42-close-gate.ts Stage 1"
      to: "package.json#verify:phase72"
      via: "shell execution"
      pattern: "verify:phase72"
    - from: "scripts/verify-phase76-v42-close-gate.ts Stage 2"
      to: "package.json#verify:phase73 + verify:phase73-v41-close-gate"
      via: "shell execution (&& chain)"
      pattern: "verify:phase73"
---

<objective>
将 Phase 76 outer gate 的 Stage 1（v4.0 gate regression）和 Stage 2（v4.1 quiz multi-type verification）从占位 skeleton 升级为真实验证接线：Stage 1 执行 pnpm verify:phase72，Stage 2 执行 pnpm verify:phase73 && pnpm verify:phase73-v41-close-gate。

Purpose: 把 v4.0 和 v4.1 的既有验证链正确串联进 v4.2 gate 骨架，确保 gate 的基础阶段可执行。
Output: scripts/verify-phase76-v42-close-gate.ts 的 Stage 1 + Stage 2 已接线为真实 shell 执行。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-v4-2-authoritative-close-gate/76-CONTEXT.md
@.planning/phases/76-v4-2-authoritative-close-gate/76-RESEARCH.md
@.planning/phases/76-v4-2-authoritative-close-gate/76-01-PLAN.md
@scripts/verify-phase76-v42-close-gate.ts
@scripts/verify-phase72-close-gate.ts
@scripts/verify-phase73-v41-close-gate.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 接线 Stage 1 (v4.0 gate regression) + Stage 2 (v4.1 quiz multi-type verification)</name>
  <read_first>
    - scripts/verify-phase76-v42-close-gate.ts
    - scripts/verify-phase72-close-gate.ts
    - scripts/verify-phase73-v41-close-gate.ts
    - package.json
    - .planning/phases/76-v4-2-authoritative-close-gate/76-CONTEXT.md
    - .planning/phases/76-v4-2-authoritative-close-gate/76-RESEARCH.md
  </read_first>
  <files>scripts/verify-phase76-v42-close-gate.ts</files>
  <behavior>
    - Test 1: pnpm verify:phase76 --smoke exits 0, reports Stage 1+2 readiness (wired/not-executed), and all remaining stages as "pending"
    - Test 2: full mode Stage 1 execs pnpm verify:phase72 — tracks exit code, reports passed/failed
    - Test 3: full mode Stage 2 execs pnpm verify:phase73 && pnpm verify:phase73-v41-close-gate — tracks exit code, reports passed/failed
    - Test 4: Stage 1 failure (if verify:phase72 fails) stops execution before Stage 2 (per D-06 stage blocking strategy)
    - Test 5: Stage 2 failure stops execution before Stage 3
  </behavior>
  <action>
    修改 scripts/verify-phase76-v42-close-gate.ts，将 Stage 1 和 Stage 2 从占位 skeleton 替换为真实 shell 执行逻辑。

    对标 verify-phase72-close-gate.ts 的 run() / Stage / execCommand 模式：
    - 复用 execCommand() 或等价 shell 执行函数（child_process.execSync 或 execa），执行 pnpm script 命令并捕获 exit code + stdout/stderr
    - 每个 stage 的 run() 方法：执行对应 shell 命令 → exit 0 则标记 stage 为 passed → exit 非 0 则标记 stage 为 failed 并停止后续 stage 执行（per D-06）

    Stage 1 命令：pnpm verify:phase72
    Stage 2 命令：pnpm verify:phase73 && pnpm verify:phase73-v41-close-gate

    full 模式：执行真实的 pnpm 命令（通过 shell 调用），每个 stage 标注 "EXECUTING..." → "PASSED" / "FAILED"
    smoke 模式：不执行 shell 命令，只输出每个 stage 的 wiring status ("wired" 表示命令已接线但跳过执行) + readiness report

    需要在 Stage 类或 stage runner 中实现 D-06 的阻断策略：任一 stage 失败 → 记录失败 → 后续 stage 跳过并标记 "BLOCKED" → 最终 exit 1。
  </action>
  <verify>
    <automated>pnpm verify:phase76 --smoke</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-phase76-v42-close-gate.ts 的 Stage 1 run() 方法包含 pnpm verify:phase72 的真实命令调用（非注释占位）
    - scripts/verify-phase76-v42-close-gate.ts 的 Stage 2 run() 方法包含 pnpm verify:phase73 && pnpm verify:phase73-v41-close-gate 的真实命令调用（非注释占位）
    - pnpm verify:phase76 --smoke exit 0，报告 Stage 1+2 为 "wired"（接线就绪但跳过执行），Stage 3-6 为 "pending"
    - 脚本源码明确实现 D-06 阻断策略：任一 stage 失败即停止后续 stage 执行
    - 脚本源码不直接包含 quiz.answer.received, buildQuizSampleRecapStats, useLiveAnswerStore 等 inner product seam token（outer gate 不复制产品断言）
  </acceptance_criteria>
  <done>Stage 1 (v4.0 gate regression) 和 Stage 2 (v4.1 quiz verification) 已在 outer gate 中接线完成，smoke 模式可验证 wiring，full 模式可执行真实验证。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outer gate → pnpm verify:phase72 | Stage 1 trusts the existing v4.0 gate script for product truth and only consumes its exit code + output |
| outer gate → pnpm verify:phase73 → verify:phase73-v41-close-gate | Stage 2 trusts the existing v4.1 inner verifier + outer gate for product truth |
| outer gate → downstream stages | Stage 1 or 2 failure must block Stage 3-6 execution per D-06 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-02-01 | Tampering | Stage 1/2 shell execution | mitigate | Use exact pnpm script references; do not re-implement product assertions inside the outer gate |
| T-76-02-02 | Repudiation | Stage failure reporting | mitigate | Record exit code + stdout/stderr per stage; failure in Stage 1 or 2 must produce a clear failure report before stopping |
| T-76-02-03 | Denial of Service | Blocked downstream stages | accept | D-06 blocking is by design — if upstream stages fail, downstream stages must be blocked; this is the authoritative close gate's expected behavior |
</threat_model>

<verification>
- `pnpm verify:phase76 --smoke` exits 0
- `grep -q 'verify:phase72' scripts/verify-phase76-v42-close-gate.ts`
- `grep -q 'verify:phase73' scripts/verify-phase76-v42-close-gate.ts`
- `grep -q 'verify:phase73-v41-close-gate' scripts/verify-phase76-v42-close-gate.ts`
- `grep -q '"verify:phase": "pnpm verify:phase72 && pnpm verify:phase73-v41-close-gate"' package.json`
</verification>

<success_criteria>
- Stage 1 executes pnpm verify:phase72 in full mode
- Stage 2 executes pnpm verify:phase73 && pnpm verify:phase73-v41-close-gate in full mode
- Smoke mode confirms wiring without execution
- D-06 blocking strategy is implemented
- verify:phase alias remains frozen per D-13
</success_criteria>

<output>
After completion, create `.planning/phases/76-v4-2-authoritative-close-gate/76-02-SUMMARY.md`
</output>
