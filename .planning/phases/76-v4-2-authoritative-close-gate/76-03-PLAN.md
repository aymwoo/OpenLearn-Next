---
phase: 76-v4-2-authoritative-close-gate
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - scripts/verify-phase76-v42-close-gate.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Stage 3（Phase 75 homework 全链路验证）在 outer gate 中接线完成，能执行 pnpm verify:phase75"
    - "Stage 3 的验证结果被 outer gate 正确消费：exit code 0 → passed，非 0 → failed 并阻止 Stage 4+"
    - "outer gate 不复制 pnpm verify:phase75 内部的测试断言，只消费其 exit code + output"
  artifacts:
    - path: "scripts/verify-phase76-v42-close-gate.ts"
      provides: "Stage 3 wired with pnpm verify:phase75 real command"
      contains: "verify:phase75"
  key_links:
    - from: "scripts/verify-phase76-v42-close-gate.ts Stage 3"
      to: "package.json#verify:phase75"
      via: "shell execution"
      pattern: "verify:phase75"
---

<objective>
将 Phase 76 outer gate 的 Stage 3（Phase 75 homework 全链路验证）从占位 skeleton 升级为真实验证接线：执行 pnpm verify:phase75，消费其 exit code 判定 homework 全链路通过/失败。

Purpose: 把 Phase 75 已存在的 homework 全链路验证（21/21 测试，14/14 truths）正确串联进 v4.2 gate 骨架的 Stage 3。
Output: scripts/verify-phase76-v42-close-gate.ts 的 Stage 3 已接线为真实 shell 执行。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-v4-2-authoritative-close-gate/76-CONTEXT.md
@.planning/phases/76-v4-2-authoritative-close-gate/76-RESEARCH.md
@.planning/phases/76-v4-2-authoritative-close-gate/76-02-PLAN.md
@.planning/phases/75-second-external-plugin-marketplace-generalization/75-VERIFICATION.md
@scripts/verify-phase76-v42-close-gate.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 接线 Stage 3 — homework 全链路验证 (pnpm verify:phase75)</name>
  <read_first>
    - scripts/verify-phase76-v42-close-gate.ts
    - .planning/phases/76-v4-2-authoritative-close-gate/76-CONTEXT.md
    - .planning/phases/76-v4-2-authoritative-close-gate/76-RESEARCH.md
    - .planning/phases/75-second-external-plugin-marketplace-generalization/75-VERIFICATION.md
    - package.json
  </read_first>
  <files>scripts/verify-phase76-v42-close-gate.ts</files>
  <action>
    修改 scripts/verify-phase76-v42-close-gate.ts 的 Stage 3，从占位替换为真实 shell 执行。

    Stage 3 命令：pnpm verify:phase75

    复用 Stage 1+2 已有的 execCommand() 模式和 D-06 阻断逻辑（Stage 2 失败 → Stage 3 被跳过并标记 BLOCKED；Stage 3 自身失败 → Stage 4+ 被跳过）。

    full 模式：执行 pnpm verify:phase75，捕获 exit code + stdout/stderr
    - exit 0 → PASSED
    - exit 非 0 → FAILED，后续 stage 全部 BLOCKED

    smoke 模式：不执行命令，只报告 "wired"（命令已接线但跳过执行）

    outer gate 不读取 Phase 75 的测试文件内容，不做 grep 断言 homework 内部结构——只消费 verify:phase75 的 exit code。
  </action>
  <verify>
    <automated>pnpm verify:phase76 --smoke</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-phase76-v42-close-gate.ts 的 Stage 3 run() 方法包含 pnpm verify:phase75 的真实命令调用
    - pnpm verify:phase76 --smoke exit 0，报告 Stage 1+2+3 为 "wired"（接线就绪），Stage 4-6 为 "pending"
    - Stage 3 的命令引用与 package.json 中的 verify:phase75 entry 一致
    - 脚本源码不包含 homework 插件内部的结构断言（不 grep plugin_owned_homework_assignments 等表名）
  </acceptance_criteria>
  <done>Stage 3 (homework 全链路验证) 已在 outer gate 中接线完成，消费 pnpm verify:phase75 作为上游 proof lane。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outer gate Stage 3 → pnpm verify:phase75 | Stage 3 trusts the existing Phase 75 verification script for product truth, consuming only its exit code |
| Stage 2 → Stage 3 | Stage 3 must not execute if Stage 2 failed (D-06 blocking) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-03-01 | Tampering | Stage 3 shell execution | mitigate | Use exact pnpm verify:phase75 reference; do not re-implement homework assertions in the outer gate |
| T-76-03-02 | Repudiation | Stage 3 failure in isolated environment | accept | pnpm verify:phase75 may fail in isolated environments due to libsql DB path issues (v4.0 legacy); this is a known environment constraint, not a Phase 76 defect |
</threat_model>

<verification>
- `pnpm verify:phase76 --smoke` exits 0
- `grep -q 'verify:phase75' scripts/verify-phase76-v42-close-gate.ts`
</verification>

<success_criteria>
- Stage 3 executes pnpm verify:phase75 in full mode
- Smoke mode confirms wiring without execution
- Outer gate does not duplicate product assertions from Phase 75
</success_criteria>

<output>
After completion, create `.planning/phases/76-v4-2-authoritative-close-gate/76-03-SUMMARY.md`
</output>
