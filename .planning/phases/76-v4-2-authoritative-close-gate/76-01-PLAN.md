---
phase: 76-v4-2-authoritative-close-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/verify-phase76-v42-close-gate.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "package.json 包含精确的 verify:phase 新 alias 目标声明（作为注释或占位），但不修改当前的 verify:phase 值（per D-13 冻结约束）"
    - "outer gate skeleton 脚本 verify-phase76-v42-close-gate.ts 存在，定义了 6 个 STAGE_LABELS 对应 D-01 的 Stage 1-6，复用了 Phase 74 的 StaticCheck/Stage/run() 模式"
    - "Phase 76 采用 6-wave/6-plan 结构对标 Phase 74 的 5-wave（D-03：wave 1 gate 骨架→wave 2-4 验证阶段→wave 5 formal verification + proof mapping→wave 6 sign-off + audit）"
    - "verify:phase 当前值仍为 pnpm verify:phase72 && pnpm verify:phase73-v41-close-gate（per D-13，v4.1 alias 冻结）"
  artifacts:
    - path: "scripts/verify-phase76-v42-close-gate.ts"
      provides: "6-stage outer gate skeleton for v4.2 close gate"
      contains: "STAGE_LABELS"
    - path: "package.json"
      provides: "v4.2 target alias declaration while v4.1 alias remains frozen"
      contains: "verify:phase76"
  key_links:
    - from: "scripts/verify-phase76-v42-close-gate.ts"
      to: "scripts/verify-phase72-close-gate.ts"
      via: "skeleton reuse (StaticCheck / Stage / run() pattern)"
      pattern: "StaticCheck"
    - from: "scripts/verify-phase76-v42-close-gate.ts"
      to: "scripts/verify-phase73-v41-close-gate.ts"
      via: "skeleton reuse (STAGE_LABELS pattern / upstream proof lane pattern)"
      pattern: "STAGE_LABELS"
---

<objective>
建立 Phase 76 v4.2 authoritative close gate 的骨架：创建 6-stage outer gate 脚本 `verify-phase76-v42-close-gate.ts`，注册 package.json 脚本但不修改 `verify:phase` alias。对标 Phase 74 Plan 01（gate 骨架 + alias 建立）但适配 v4.2 的 6-stage 设计。

Purpose: 先建立 gate 骨架和脚本入口，确保后续 wave 的验证 stage 有确定的接线点。
Output: `scripts/verify-phase76-v42-close-gate.ts` + `package.json` 新增 script entries（不修改 verify:phase）。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-v4-2-authoritative-close-gate/76-CONTEXT.md
@.planning/phases/76-v4-2-authoritative-close-gate/76-RESEARCH.md
@.planning/phases/76-v4-2-authoritative-close-gate/76-VALIDATION.md

# Phase 74 close gate canonical patterns
@.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-01-PLAN.md
@.planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-02-PLAN.md
@scripts/verify-phase72-close-gate.ts
@scripts/verify-phase73-v41-close-gate.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 6-stage outer gate skeleton — verify-phase76-v42-close-gate.ts</name>
  <read_first>
    - scripts/verify-phase72-close-gate.ts
    - scripts/verify-phase73-v41-close-gate.ts
    - .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-01-PLAN.md
    - .planning/phases/74-v4-1-authoritative-close-gate-multi-type-live-dashboard/74-02-PLAN.md
    - .planning/phases/76-v4-2-authoritative-close-gate/76-CONTEXT.md
    - .planning/phases/76-v4-2-authoritative-close-gate/76-RESEARCH.md
    - scripts/verify-phase76-v42-close-gate.ts (create this file)
  </read_first>
  <files>scripts/verify-phase76-v42-close-gate.ts</files>
  <action>
    新建 scripts/verify-phase76-v42-close-gate.ts，直接复用 verify-phase72-close-gate.ts 的 StaticCheck / Stage / run() / countOccurrences() 结构骨架，并复刻 verify-phase73-v41-close-gate.ts 的 7 个 STAGE_LABELS 定义模式。

    定义 6 个 STAGE_LABELS 对应 D-01 的 6-stage 设计：
    1. "Stage 1: v4.0 gate regression (verify:phase72)"
    2. "Stage 2: v4.1 quiz multi-type verification (verify:phase73 + verify:phase73-v41-close-gate)"
    3. "Stage 3: Phase 75 homework full-chain verification (verify:phase75)"
    4. "Stage 4: Cross-plugin regression (verify:v42-cross-plugin)"
    5. "Stage 5: Formal verification + proof mapping"
    6. "Stage 6: Manual Surface Sign-Off + closeout artifacts + audit + alias cutover"

    本任务是 skeleton-only——每个 stage 的 run() 方法只写占位：输出 stage 名称 + "pending — implementation in wave N"，不执行实际的 shell/vitest 命令。Stage 1-3 的命令引用（verify:phase72, verify:phase73, verify:phase73-v41-close-gate, verify:phase75）作为注释标注在对应 stage 方法体内，但实际命令调用留到后续 plan 补全。

    Stage 5 和 Stage 6 的函数体内标注对应的 close-truth artifact 路径常量：
    - Stage 5: `.planning/phases/76-v4-2-authoritative-close-gate/76-VERIFICATION.md`, `.planning/milestones/v4.2-PROOF-MAP.md`
    - Stage 6: `.planning/phases/76-v4-2-authoritative-close-gate/76-MANUAL-SIGNOFF.md`, `.planning/milestones/v4.2-MILESTONE-AUDIT.md`, `.planning/milestones/v4.2-CLOSEOUT.md`

    脚本必须支持 --smoke 和 --full（默认）两种模式，smoke 模式只输出 stage labels + readiness reporting（所有 stage 报告 "pending"），full 模式留到后续 wave 逐步补全。

    导出 runPhase76V42CloseGate 函数，作为统一的 outer gate 入口。
  </action>
  <verify>
    <automated>node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase76-v42-close-gate.ts --smoke</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-phase76-v42-close-gate.ts 存在，并导出 runPhase76V42CloseGate 函数
    - 脚本源码包含 6 个精确的 STAGE_LABELS 字符串，对应 Stage 1 到 Stage 6
    - 脚本源码包含 verify:phase72, verify:phase73, verify:phase73-v41-close-gate, verify:phase75, verify:v42-cross-plugin 作为注释引用或 stage 描述
    - 脚本源码包含 artifact 路径常量：76-VERIFICATION.md, v4.2-PROOF-MAP.md, 76-MANUAL-SIGNOFF.md, v4.2-MILESTONE-AUDIT.md, v4.2-CLOSEOUT.md
    - node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase76-v42-close-gate.ts --smoke exit 0，输出 6 个 stage labels + readiness report（全部 "pending"）
    - 脚本源码不直接包含任何 pnpm vitest run 或 pnpm verify:phaseXX 的真实命令调用（占位 skeleton only）
    - smoke 模式不执行任何 shell/vitest 命令，不退 1
  </acceptance_criteria>
  <done>6-stage outer gate skeleton 就位，--smoke 可跑并输出 readiness report，所有 stage 为 pending 状态。</done>
</task>

<task type="auto">
  <name>Task 2: 注册 package.json scripts 并声明 v4.2 target alias（不修改 verify:phase）</name>
  <read_first>
    - package.json
    - scripts/verify-phase76-v42-close-gate.ts
    - scripts/verify-phase72-close-gate.ts
    - scripts/verify-phase73-v41-close-gate.ts
    - .planning/phases/76-v4-2-authoritative-close-gate/76-CONTEXT.md
  </read_first>
  <files>package.json</files>
  <action>
    修改 package.json 的 scripts 段：
    1. 新增精确 entry：
       "verify:phase76": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase76-v42-close-gate.ts"
    2. 新增占位 entry（Stage 4 的独立回归脚本，将在 Wave 4 创建对应 .ts 文件）：
       "verify:v42-cross-plugin": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-v42-cross-plugin.ts"
    3. 保留现有的 "verify:phase": "pnpm verify:phase72 && pnpm verify:phase73-v41-close-gate"（per D-13，冻结）
    4. 保留现有的 verify:phase72, verify:phase73, verify:phase73-v41-close-gate, verify:phase75

    在 verify:phase 所在行的上方新增一行注释，声明 v4.2 target alias：
    // v4.2 target (post-cutover): "verify:phase": "pnpm verify:phase72 && pnpm verify:phase73-v41-close-gate && pnpm verify:phase75 && pnpm verify:v42-cross-plugin"
    注释形态参照 package.json 中已有注释的模式，确认为 JSON 合法（// 注释在 package.json 中可通过 node 解析）。
  </action>
  <verify>
    <automated>grep -q '"verify:phase76"' /home/wuxf/Develop/OpenLearn-Next/package.json && grep -q '"verify:v42-cross-plugin"' /home/wuxf/Develop/OpenLearn-Next/package.json && grep -q '"verify:phase": "pnpm verify:phase72 && pnpm verify:phase73-v41-close-gate"' /home/wuxf/Develop/OpenLearn-Next/package.json && grep -q 'verify:phase75 && pnpm verify:v42-cross-plugin' /home/wuxf/Develop/OpenLearn-Next/package.json</automated>
  </verify>
  <acceptance_criteria>
    - package.json 出现精确字符串 "verify:phase76": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase76-v42-close-gate.ts"
    - package.json 出现精确字符串 "verify:v42-cross-plugin": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-v42-cross-plugin.ts"
    - package.json 仍保留精确字符串 "verify:phase": "pnpm verify:phase72 && pnpm verify:phase73-v41-close-gate"（per D-13 冻结）
    - package.json 仍保留 verify:phase72, verify:phase73, verify:phase73-v41-close-gate, verify:phase75 全部四个已有 entry
    - package.json 在 verify:phase 所在行附近出现 v4.2 target alias 声明注释，包含 verify:phase75 && pnpm verify:v42-cross-plugin
    - pnpm verify:phase76 --smoke exit 0（验证 skeleton 可执行）
  </acceptance_criteria>
  <done>package.json 已注册 verify:phase76 和 verify:v42-cross-plugin 两个新 entry，v4.2 target alias 已声明为注释，verify:phase 仍冻结在 v4.1 alias。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate skeleton → package.json | package.json script wiring controls the single milestone entry point, must not prematurely change verify:phase |
| Phase 74 gate pattern → Phase 76 skeleton | Phase 76 skeleton must faithfully replicate the StaticCheck/Stage/run() pattern from Phase 74 without adding new grammar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01-01 | Tampering | package.json #verify:phase | mitigate | Keep verify:phase exactly "pnpm verify:phase72 && pnpm verify:phase73-v41-close-gate" per D-13; only add new scripts and a comment-declared target alias |
| T-76-01-02 | Tampering | scripts/verify-phase76-v42-close-gate.ts | mitigate | Skeleton-only — no real shell/vitest execution in Wave 1; smoke mode exits 0 with pending readiness report |
| T-76-01-03 | Repudiation | v4.2 target alias declaration | mitigate | v4.2 target alias is declared as a comment only, not an active entry point; cutover occurs only in Wave 6 after all stages pass |
</threat_model>

<verification>
- `node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase76-v42-close-gate.ts --smoke` exits 0
- `grep -q '"verify:phase": "pnpm verify:phase72 && pnpm verify:phase73-v41-close-gate"' package.json`
- `grep -q '"verify:phase76"' package.json`
- `grep -q '"verify:v42-cross-plugin"' package.json`
</verification>

<success_criteria>
- 6-stage outer gate skeleton exists and can run in smoke mode
- package.json has new script entries without modifying verify:phase
- v4.2 target alias is declared as a comment for future reference
</success_criteria>

<output>
After completion, create `.planning/phases/76-v4-2-authoritative-close-gate/76-01-SUMMARY.md`
</output>
