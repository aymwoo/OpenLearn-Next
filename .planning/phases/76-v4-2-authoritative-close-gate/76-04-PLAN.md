---
phase: 76-v4-2-authoritative-close-gate
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - scripts/verify-v42-cross-plugin.ts
  - scripts/verify-phase76-v42-close-gate.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Stage 4 跨插件回归脚本 verify:v42-cross-plugin 存在且能独立运行，编排 quiz 全量 + homework 全量 + cross-plugin dedicated suite"
    - "quiz 全量 + homework 全量 + dedicated suite 三者独立跑、独立报告，任一失败即 exit 1（per D-04/D-05/D-06）"
    - "Stage 4 在 outer gate 中接线完成，能执行 pnpm verify:v42-cross-plugin"
  artifacts:
    - path: "scripts/verify-v42-cross-plugin.ts"
      provides: "v4.2 独立跨插件回归脚本入口"
      contains: "verify:v42-cross-plugin"
    - path: "scripts/verify-phase76-v42-close-gate.ts"
      provides: "Stage 4 wired with pnpm verify:v42-cross-plugin"
      contains: "verify:v42-cross-plugin"
  key_links:
    - from: "scripts/verify-phase76-v42-close-gate.ts Stage 4"
      to: "package.json#verify:v42-cross-plugin"
      via: "shell execution"
      pattern: "verify:v42-cross-plugin"
    - from: "scripts/verify-v42-cross-plugin.ts"
      to: "vitest run src/plugins/homework/__tests__/cross-plugin-regression.test.ts"
      via: "dedicated cross-plugin suite"
      pattern: "cross-plugin-regression"
    - from: "scripts/verify-v42-cross-plugin.ts"
      to: "vitest run src/components/learning/quiz-sample-step-card.test.tsx"
      via: "quiz full suite"
      pattern: "quiz-sample-step-card"
---

<objective>
创建独立的 Stage 4 跨插件回归脚本 verify:v42-cross-plugin，编排 quiz 全量测试 + homework 全量测试 + cross-plugin dedicated suite（基于 Phase 75 的 6-case cross-plugin-regression.test.ts），并在 outer gate 的 Stage 4 中接线。

Purpose: 实现 D-04/D-05 要求的独立跨插件回归 gate stage——quiz 不破 + homework 全绿 + dedicated cross-plugin suite 全部通过，任一失败即阻断 gate。
Output: scripts/verify-v42-cross-plugin.ts + scripts/verify-phase76-v42-close-gate.ts Stage 4 已接线。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-v4-2-authoritative-close-gate/76-CONTEXT.md
@.planning/phases/76-v4-2-authoritative-close-gate/76-RESEARCH.md
@.planning/phases/75-second-external-plugin-marketplace-generalization/75-VERIFICATION.md
@src/plugins/homework/__tests__/cross-plugin-regression.test.ts
@src/plugins/homework/__tests__/lifecycle.test.ts
@src/components/learning/quiz-sample-step-card.test.tsx
@scripts/verify-phase76-v42-close-gate.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 verify:v42-cross-plugin.ts — Stage 4 独立跨插件回归脚本</name>
  <read_first>
    - src/plugins/homework/__tests__/cross-plugin-regression.test.ts
    - src/plugins/homework/__tests__/lifecycle.test.ts
    - src/components/learning/quiz-sample-step-card.test.tsx
    - package.json
    - .planning/phases/76-v4-2-authoritative-close-gate/76-CONTEXT.md
    - .planning/phases/76-v4-2-authoritative-close-gate/76-RESEARCH.md
    - .planning/phases/75-second-external-plugin-marketplace-generalization/75-VERIFICATION.md
    - scripts/verify-v42-cross-plugin.ts (create this file)
  </read_first>
  <files>scripts/verify-v42-cross-plugin.ts</files>
  <behavior>
    - Test 1: node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-v42-cross-plugin.ts exits 0 when all three sub-suites pass
    - Test 2: quiz full suite runs pnpm vitest run src/components/learning/quiz-sample-step-card.test.tsx — exits non-0 on failure, stops before homework suite
    - Test 3: homework full suite runs pnpm vitest run src/plugins/homework/ — exits non-0 on failure
    - Test 4: cross-plugin dedicated suite runs pnpm vitest run src/plugins/homework/__tests__/cross-plugin-regression.test.ts — exits non-0 on failure
    - Test 5: each sub-suite produces identifiable output (section header + pass/fail) before the next sub-suite starts
  </behavior>
  <action>
    新建 scripts/verify-v42-cross-plugin.ts，实现 D-04/D-05 要求的独立 Stage 4 跨插件回归脚本。

    脚本内部编排三条 shell 命令，顺序执行（&& 链），任一失败 exit 1：

    1. Quiz 全量测试（Section header: "=== QUIZ FULL SUITE ==="）：
       命令：pnpm vitest run src/components/learning/quiz-sample-step-card.test.tsx
       - quiz-sample-step-card.test.tsx 涵盖 quiz 5 题型 + stats + lifecycle
       - 失败 → 输出 "QUIZ FULL SUITE: FAILED" → exit 1

    2. Homework 全量测试（Section header: "=== HOMEWORK FULL SUITE ==="）：
       命令：pnpm vitest run src/plugins/homework/
       - 覆盖 lifecycle.test.ts (12 tests) + cross-plugin-regression.test.ts (6 cases) = 18 tests
       - 失败 → 输出 "HOMEWORK FULL SUITE: FAILED" → exit 1

    3. Cross-Plugin Dedicated Suite（Section header: "=== CROSS-PLUGIN DEDICATED SUITE ==="）：
       命令：pnpm vitest run src/plugins/homework/__tests__/cross-plugin-regression.test.ts
       - 6 个检查点 A-F：quiz install 不被破坏、homework+quiz 编译共存、双 insert 可并行、双提交双绿、homework upgrade 后 quiz 数据完整、homework uninstall 后 quiz 功能正常
       - 失败 → 输出 "CROSS-PLUGIN DEDICATED SUITE: FAILED" → exit 1

    复用 child_process.execSync 执行 pnpm 命令，捕获 exit code + stdout/stderr。每个 sub-suite 执行前打印 section header，执行后打印 PASSED/FAILED。

    三段全部通过 → 输出 "=== STAGE 4 CROSS-PLUGIN REGRESSION: ALL PASSED ===" → exit 0。

    脚本支持 --smoke 模式：不执行真实 shell 命令，只输出三段 sub-suite 的 wiring status 和 readiness report，exit 0。
  </action>
  <verify>
    <automated>node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-v42-cross-plugin.ts --smoke</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-v42-cross-plugin.ts 存在，并包含三个 sub-suite 的 section header 字符串（"QUIZ FULL SUITE", "HOMEWORK FULL SUITE", "CROSS-PLUGIN DEDICATED SUITE"）
    - 脚本源码包含 pnpm vitest run src/components/learning/quiz-sample-step-card.test.tsx 命令
    - 脚本源码包含 pnpm vitest run src/plugins/homework/ 命令
    - 脚本源码包含 pnpm vitest run src/plugins/homework/__tests__/cross-plugin-regression.test.ts 命令
    - node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-v42-cross-plugin.ts --smoke exit 0
    - --smoke 模式输出三段 sub-suite 的 wiring status，不执行真实 vitest 命令
    - 脚本实现 D-04/D-05/D-06 的阻断策略：任一 sub-suite 失败 → exit 1，不继续执行后续 sub-suite
    - package.json 中的 verify:v42-cross-plugin 已指向本脚本（Wave 1 Plan 01 已设置）
  </acceptance_criteria>
  <done>Stage 4 独立跨插件回归脚本已创建，编排 quiz 全量 + homework 全量 + dedicated cross-plugin suite，smoke 模式可验证 wiring。</done>
</task>

<task type="auto">
  <name>Task 2: 接线 Stage 4 到 outer gate</name>
  <read_first>
    - scripts/verify-phase76-v42-close-gate.ts
    - scripts/verify-v42-cross-plugin.ts
    - .planning/phases/76-v4-2-authoritative-close-gate/76-CONTEXT.md
    - package.json
  </read_first>
  <files>scripts/verify-phase76-v42-close-gate.ts</files>
  <action>
    修改 scripts/verify-phase76-v42-close-gate.ts 的 Stage 4，从占位替换为真实 shell 执行。

    Stage 4 命令：pnpm verify:v42-cross-plugin

    复用 Stage 1-3 已有的 execCommand() 模式和 D-06 阻断逻辑。

    full 模式：执行 pnpm verify:v42-cross-plugin，捕获 exit code
    - exit 0 → PASSED
    - exit 非 0 → FAILED，Stage 5-6 全部 BLOCKED

    smoke 模式：执行 pnpm verify:v42-cross-plugin --smoke，只验证 wiring 而不真正跑 vitest。

    outer gate 不读取 cross-plugin 测试文件内容，只消费 verify:v42-cross-plugin 的 exit code。
  </action>
  <verify>
    <automated>pnpm verify:phase76 --smoke</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-phase76-v42-close-gate.ts 的 Stage 4 run() 方法包含 pnpm verify:v42-cross-plugin 的真实命令调用
    - pnpm verify:phase76 --smoke exit 0，报告 Stage 1-4 为 "wired"（接线就绪），Stage 5-6 为 "pending"
    - smoke 模式下 Stage 4 传递 --smoke 给 verify:v42-cross-plugin
    - 脚本源码不包含 cross-plugin 测试文件的 grep 断言或内部结构检查
  </acceptance_criteria>
  <done>Stage 4 (跨插件回归) 已在 outer gate 中接线完成。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify:v42-cross-plugin → vitest run (quiz + homework + dedicated) | Each sub-suite is a separate shell execution; failure in one blocks subsequent ones |
| outer gate Stage 4 → verify:v42-cross-plugin | Outer gate trusts the cross-plugin script for truth and consumes only its exit code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-04-01 | Tampering | Cross-plugin script shell execution | mitigate | Use exact pnpm vitest run commands; do not inline test logic or bypass vitest |
| T-76-04-02 | Repudiation | Sub-suite failure reporting | mitigate | Each sub-suite prints identifiable section header + PASSED/FAILED before exit; Stage 4 outer gate records which sub-suite failed |
| T-76-04-03 | Tampering | Quiz+hw suite overlap with Stage 2/3 | accept | D-04 explicitly requires quiz full + hw full in Stage 4 in addition to Stage 2/3; this is intentional cross-validation to ensure dual-plugin coexistence doesn't break either plugin |
</threat_model>

<verification>
- `node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-v42-cross-plugin.ts --smoke` exits 0
- `pnpm verify:phase76 --smoke` exits 0
- `grep -q 'QUIZ FULL SUITE' scripts/verify-v42-cross-plugin.ts`
- `grep -q 'HOMEWORK FULL SUITE' scripts/verify-v42-cross-plugin.ts`
- `grep -q 'CROSS-PLUGIN DEDICATED SUITE' scripts/verify-v42-cross-plugin.ts`
- `grep -q 'verify:v42-cross-plugin' scripts/verify-phase76-v42-close-gate.ts`
</verification>

<success_criteria>
- Independent Stage 4 cross-plugin regression script exists with three sub-suites
- Stage 4 is wired in outer gate
- Smoke mode confirms wiring without execution
- D-04/D-05/D-06 blocking strategy is implemented
</success_criteria>

<output>
After completion, create `.planning/phases/76-v4-2-authoritative-close-gate/76-04-SUMMARY.md`
</output>
