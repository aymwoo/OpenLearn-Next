---
phase: 21-teaching-design-contracts-and-evidence-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 21-01
  - 21-02
files_modified:
  - src/lib/dal/lesson-authoring.test.ts
  - src/lib/dal/classroom.test.ts
  - scripts/verify-phase21-contracts.ts
  - package.json
autonomous: true
requirements:
  - ORCH-01
  - EVAL-03
must_haves:
  truths:
    - Phase 21 有可执行 `verify:phase21`，能检查 teaching-design fallback、classroom evidence wiring 与安全边界。
    - 回归测试证明旧 lesson snapshot 与新 classroom evidence contract 可以共存，不需要先做破坏性迁移。
    - phase verifier 会在 contract 漂移、unsafe shortcut 或关键验证脚本失效时直接失败。
  artifacts:
    - path: scripts/verify-phase21-contracts.ts
      provides: dedicated phase verification command
      contains: verify:phase21
    - path: package.json
      provides: executable verification entry
      contains: verify:phase21
    - path: src/lib/dal/classroom.test.ts
      provides: regression coverage for evidence/timeline persistence and fallback launch behavior
      contains: evidence
  key_links:
    - from: package.json
      to: scripts/verify-phase21-contracts.ts
      via: phase-specific verification command
      pattern: verify:phase21
    - from: scripts/verify-phase21-contracts.ts
      to: src/lib/dal/lesson-authoring.test.ts
      via: targeted regression suite for fallback and contract wiring
      pattern: pnpm test --run
---

<objective>
给 Phase 21 补齐固定验证护栏，确保 teaching-design contract、durable classroom
evidence 与缓存/鉴权边界在后续 phase 中不会悄悄回退。

Purpose: 把本阶段从“新增合同”升级为“新增合同 + 可复跑证明”，为 Phase 22-25
提供稳定基线。
Output: targeted regression coverage、`verify:phase21` 脚本、package command。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-teacher-flow-editor-enhancement/17-04-PLAN.md
@src/lib/dal/lesson-authoring.test.ts
@src/lib/dal/classroom.test.ts
@src/actions/classroom-actions.test.ts
@scripts/verify-phase17-editor.ts
@scripts/verify-phase18-schedule.ts
@package.json

<interfaces>
Verification command pattern already in repo:
```ts
runPnpm(["test", "--run", ...], "phase regression suite")
type StaticCheck = { label: string; passed: boolean }
```

Phase 21 invariants to protect:
```ts
lessonStepPayloadSchema
getTeacherLessonPreviewDTO()
recordClassroomEvidenceAction()
recordClassroomInterventionAction()
touchClassroomPresenceAction()
updateTag(cacheTags.classroom(sessionId))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 补齐 teaching-design fallback 与 classroom evidence 的定向回归测试</name>
  <files>src/lib/dal/lesson-authoring.test.ts, src/lib/dal/classroom.test.ts</files>
  <behavior>
    - Test 1: 历史 lesson payload/snapshot 缺少 `teachingDesign` 时，preview/launch 仍能返回默认字段。
    - Test 2: presence touch、evidence write、intervention write 都能命中 durable contract，而不是只改当前内存状态。
    - Test 3: published snapshot launch preview 与 classroom evidence persistence 不互相破坏既有 classroom flow。
  </behavior>
  <action>扩展 `src/lib/dal/lesson-authoring.test.ts` 与 `src/lib/dal/classroom.test.ts`，把 Phase 21 的关键兼容性和持久化边界固定下来：一组测试覆盖旧 payload/snapshot 的 teaching-design 默认化；另一组测试覆盖 presence timeline、evidence capture、intervention note 的 DAL 写路径和 session ownership。保持测试粒度 focused，不新增 readFile-only 假验证；至少一条测试必须真正断言 fallback 后的结构化字段值，至少一条测试必须断言 timeline/evidence 记录使用 session-scoped contract。</action>
  <verify>
    <automated>pnpm test --run src/lib/dal/lesson-authoring.test.ts src/lib/dal/classroom.test.ts</automated>
  </verify>
  <done>Phase 21 的核心合同已经被 focused regression suite 锁住：旧课时兼容、新证据链路可持久化。</done>
</task>

<task type="auto">
  <name>Task 2: 新增 `verify:phase21` 验证脚本守住合同与安全边界</name>
  <files>scripts/verify-phase21-contracts.ts, package.json</files>
  <action>新增 `scripts/verify-phase21-contracts.ts`，并在 `package.json` 注册 `verify:phase21`。复用 `verify:phase17` / `verify:phase18` 的 `StaticCheck + runPnpm()` 模式，静态检查至少覆盖 7 项：`lessonStepPayloadSchema` 中存在 `teachingDesign`；`getTeacherLessonPreviewDTO()` 或 launch preview 仍在服务端做默认回退；`src/db/schema.ts` 中存在 `classroomEvidence` 与 `classroomTimeline`；`src/actions/classroom-actions.ts` 中存在 `recordClassroomEvidenceAction`、`recordClassroomInterventionAction` 与 `updateTag(cacheTags.classroom(...))`；classroom 写路径不含 `eval(`、`dangerouslySetInnerHTML`、client-side DB import；并执行 Phase 21 的 targeted Vitest suites。不要把 verifier 实现成手工 checklist，也不要只检查注释文本。</action>
  <verify>
    <automated>pnpm verify:phase21</automated>
  </verify>
  <done>仓库拥有了 Phase 21 专属验证入口；后续 orchestration、runtime、evaluation 和 analytics 若破坏这些合同，会立刻在本地或 CI 暴露。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository source -> phase verifier | 静态检查与 targeted tests 决定合同是否仍然成立 |
| legacy lesson/classroom data -> regression tests | 老数据兼容性需要通过自动测试证明，而不是靠人工假设 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-09 | T | `scripts/verify-phase21-contracts.ts` | mitigate | 同时检查 teaching-design、evidence tables、server action wiring 和 unsafe pattern，避免只护住单一文件。 |
| T-21-10 | R | `src/lib/dal/lesson-authoring.test.ts` | mitigate | 用明确 fallback 断言证明历史 lesson 兼容，而不是仅凭实现猜测。 |
| T-21-11 | R | `src/lib/dal/classroom.test.ts` | mitigate | 用 session-owned evidence/timeline 断言证明课堂过程可审计。 |
| T-21-12 | I | `scripts/verify-phase21-contracts.ts` | accept | verifier 仅读取源码和测试输出，不引入新的运行时数据暴露。 |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/lesson-authoring.test.ts src/lib/dal/classroom.test.ts`
- `pnpm verify:phase21`
</verification>

<success_criteria>
- Phase 21 拥有专属 verify 命令。
- teaching-design fallback 与 classroom evidence persistence 具备 focused regression coverage。
- 后续 phase 一旦回退服务端边界、cache invalidation 或数据合同，验证会直接失败。
</success_criteria>

<output>
After completion, create `.planning/phases/21-teaching-design-contracts-and-evidence-foundation/21-03-SUMMARY.md`
</output>
