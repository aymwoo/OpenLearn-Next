---
phase: 21-teaching-design-contracts-and-evidence-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 21-01
  - 21-02
files_modified:
  - src/components/authoring/lesson-authoring-workspace.tsx
  - src/components/authoring/lesson-authoring-workspace.test.tsx
  - src/components/surfaces/teacher-lesson-preview-surface.tsx
  - src/components/classroom/classroom-launch-preview.tsx
  - src/components/classroom/classroom-launch-panel.test.tsx
  - scripts/verify-phase21-contracts.ts
  - package.json
autonomous: true
requirements:
  - ORCH-01
  - EVAL-03
must_haves:
  truths:
    - 教师会在 editor、教师预览和开课预览中看到“默认推断 / 待完善”的 teaching-design 提示，但不会被本期直接阻断。
    - Phase 21 有可执行 `verify:phase21`，能检查 teaching-design fallback、teacher-facing 提示、classroom evidence wiring 与安全边界。
    - phase verifier 会在 contract 漂移、unsafe shortcut 或关键验证脚本失效时直接失败。
  artifacts:
    - path: src/components/authoring/lesson-authoring-workspace.tsx
      provides: teacher-facing teaching-design summary and fallback notice in editor
      contains: 待完善
    - path: src/components/classroom/classroom-launch-preview.tsx
      provides: launch preview fallback indicator for inferred teaching design
      contains: 默认推断
    - path: scripts/verify-phase21-contracts.ts
      provides: dedicated phase verification command
      contains: verify:phase21
  key_links:
    - from: src/components/authoring/lesson-authoring-workspace.tsx
      to: src/components/surfaces/teacher-lesson-preview-surface.tsx
      via: teacher-facing surfaces share the same fallback wording and refinement cue
      pattern: 默认推断|待完善
    - from: package.json
      to: scripts/verify-phase21-contracts.ts
      via: phase-specific verification command
      pattern: verify:phase21
---

<objective>
把 Phase 21 的 teaching-design fallback 真正露出到教师工作流里：编辑器、教师预览、开课预览都要能诚实提示“默认推断 / 待完善”，并用 `verify:phase21` 把这些合同锁住。

Purpose: 落实 D-06 / D-19 / D-20，避免 Phase 21 只新增后台字段却没有任何教师侧可见反馈。
Output: teacher-facing fallback cues、surface regression coverage、`verify:phase21` 脚本、package command。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-teacher-flow-editor-enhancement/17-03-SUMMARY.md
@src/components/authoring/lesson-authoring-workspace.tsx
@src/components/surfaces/teacher-lesson-preview-surface.tsx
@src/components/classroom/classroom-launch-preview.tsx
@src/components/classroom/classroom-launch-panel.test.tsx
@scripts/verify-phase17-editor.ts
@scripts/verify-phase18-schedule.ts
@package.json

<interfaces>
Verification command pattern already in repo:
```ts
runPnpm(["test", "--run", ...], "phase regression suite")
type StaticCheck = { label: string; passed: boolean }
```

Phase 21 invariants to protect:
```ts
teachingDesignStatus
needsTeachingDesignRefinement
getTeacherLessonPreviewDTO()
buildLaunchPreview()
recordClassroomEvidenceAction()
recordClassroomInterventionAction()
updateTag(cacheTags.classroom(sessionId))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在教师 editor / preview / launch preview 中显式呈现 teaching-design fallback 提示</name>
  <files>src/components/authoring/lesson-authoring-workspace.tsx, src/components/authoring/lesson-authoring-workspace.test.tsx, src/components/surfaces/teacher-lesson-preview-surface.tsx, src/components/classroom/classroom-launch-preview.tsx, src/components/classroom/classroom-launch-panel.test.tsx</files>
  <behavior>
    - Test 1: editor 中历史步骤缺少 `teachingDesign` 时，会显示默认推断摘要与“待完善”提示，但不阻断继续编辑。
    - Test 2: teacher preview surface 会展示同一条 fallback 状态，而不是把默认值伪装成显式配置。
    - Test 3: `/teacher/launch` 预览会显示默认推断提示与 evidence expectation 摘要，且继续只消费 published snapshot。
  </behavior>
  <action>更新 `src/components/authoring/lesson-authoring-workspace.tsx`、`src/components/surfaces/teacher-lesson-preview-surface.tsx`、`src/components/classroom/classroom-launch-preview.tsx`，消费 21-01 暴露的 `teachingDesignStatus` / `needsTeachingDesignRefinement` / `teachingDesignFallbackReason` 字段，在教师侧用简体中文明确区分“显式配置”“默认推断”“待完善”。按 D-06 / D-19 / D-20，这些提示首发只进入教师 editor、教师预览和开课预览；不要改学生 runtime，也不要把它们升级成 launch blocking gate。同步扩展 `lesson-authoring-workspace.test.tsx` 与 `classroom-launch-panel.test.tsx`，至少验证一条 inferred badge / copy 和一条 published-snapshot-only 约束。</action>
  <verify>
    <automated>pnpm test --run src/components/authoring/lesson-authoring-workspace.test.tsx src/components/classroom/classroom-launch-panel.test.tsx</automated>
  </verify>
  <done>教师已经能在三个 planning surface 中看到 teaching-design 默认推断提示；旧 lesson/snapshot 继续可编辑、可预览、可开课，但不会再伪装成“全部已配置”。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新增 `verify:phase21` 验证脚本守住 fallback 提示、evidence wiring 与安全边界</name>
  <files>scripts/verify-phase21-contracts.ts, package.json</files>
  <behavior>
    - Test 1: `verify:phase21` 会检查 teacher-facing surface 中的 inferred/refinement cue，而不是只检查 DTO 名称存在。
    - Test 2: verifier 会继续要求 `classroomEvidence` / `classroomTimeline` 与 classroom actions wiring 存在。
    - Test 3: targeted suites 失败时，`verify:phase21` 直接失败，不给假阳性通过结果。
  </behavior>
  <action>新增 `scripts/verify-phase21-contracts.ts`，并在 `package.json` 注册 `verify:phase21`。复用 `verify:phase17` / `verify:phase18` 的 `StaticCheck + runPnpm()` 模式，静态检查至少覆盖 8 项：`lessonStepPayloadSchema` 中存在 `teachingDesign`；teacher preview 或 editor surface 消费 `teachingDesignStatus` / `needsTeachingDesignRefinement`；`classroom-launch-preview.tsx` 存在“默认推断”或等价非阻断提示；`src/db/schema.ts` 中存在 `classroomEvidence` 与 `classroomTimeline`；`src/actions/classroom-actions.ts` 中存在 `recordClassroomEvidenceAction`、`recordClassroomInterventionAction` 与 `updateTag(cacheTags.classroom(...))`；classroom 写路径不含 `eval(`、`dangerouslySetInnerHTML`、client-side DB import；并执行 Phase 21 的 targeted Vitest suites。不要把 verifier 实现成手工 checklist，也不要只检查注释文本。</action>
  <verify>
    <automated>pnpm verify:phase21</automated>
  </verify>
  <done>仓库拥有了 Phase 21 专属验证入口；后续若移除教师 fallback 提示、破坏 evidence 写链路或回退服务端边界，会立刻在本地或 CI 暴露。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository source -> phase verifier | 静态检查与 targeted tests 决定合同是否仍然成立 |
| legacy lesson/classroom data -> regression tests | 老数据兼容性需要通过自动测试证明，而不是靠人工假设 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-09 | T | `scripts/verify-phase21-contracts.ts` | mitigate | 同时检查 teaching-design、teacher-facing fallback cue、evidence tables、server action wiring 和 unsafe pattern，避免只护住单一文件。 |
| T-21-10 | I | `src/components/surfaces/teacher-lesson-preview-surface.tsx` | mitigate | inferred/default 提示只在教师侧 surface 暴露，不把 teacher-only design note 泄漏到学生 runtime。 |
| T-21-11 | R | `src/components/classroom/classroom-launch-preview.tsx` | mitigate | 用 published-snapshot-only 约束和 UI regression 断言，证明 launch preview 没有回流到 draft/client 推断。 |
| T-21-12 | I | `scripts/verify-phase21-contracts.ts` | accept | verifier 仅读取源码和测试输出，不引入新的运行时数据暴露。 |
</threat_model>

<verification>
- `pnpm test --run src/components/authoring/lesson-authoring-workspace.test.tsx src/components/classroom/classroom-launch-panel.test.tsx`
- `pnpm verify:phase21`
</verification>

<success_criteria>
- editor、teacher preview、launch preview 都明确呈现 teaching-design 默认推断与待完善状态。
- Phase 21 拥有专属 verify 命令。
- teaching-design fallback、teacher-facing 提示与 classroom evidence persistence 具备 focused regression coverage。
- 后续 phase 一旦回退服务端边界、cache invalidation 或数据合同，验证会直接失败。
</success_criteria>

<output>
After completion, create `.planning/phases/21-teaching-design-contracts-and-evidence-foundation/21-03-SUMMARY.md`
</output>
