---
phase: 62-lessonagent-typed-tool-layer
plan: 03
type: execute
wave: 2
depends_on: ["62-01", "62-02"]
files_modified:
  - src/features/platform-core/commands/contracts.ts
  - src/features/platform-core/commands/handlers/lesson-draft.ts
  - src/features/platform-core/commands/registry.ts
  - src/features/platform-core/commands/handlers/lesson-draft.events.test.ts
autonomous: true
requirements: [AGENT-03, AGENT-04]
must_haves:
  truths:
    - "新增 lesson.draft.run command 类型，scope 复用既有 {schoolId, pluginId} 并携带保留 sentinel pluginId=core.lesson-agent（零改 scope schema / bus 失败路径）"
    - "lesson.draft.run handler 经只读授权（assertActiveTeacher）后调用 createDraftLessonStepTool 生成步骤，成功时经 emittedEvents 落账三条 AI 域事件（requested/tool.invoked/produced）"
    - "生成失败时 handler 抛 PlatformCommandExecutionError → 由 bus 落账唯一 generic 失败事件（不发任何 domain 事件，符合 D-53-08 superRefine）"
    - "生成的步骤包经 resultSummary 回传调用方（command 记录是 D-01 允许的唯一持久副作用），lesson/draft version 不写（归 Phase 63）"
  artifacts:
    - path: "src/features/platform-core/commands/contracts.ts"
      provides: "LessonDraftCommandTypes + 并入 PlatformCommandTypeSchema enum + payload schema + 并入 PlatformCommandSchema 联合 + PlatformCommandPayloadSchemas 映射"
      contains: "lesson.draft.run"
    - path: "src/features/platform-core/commands/handlers/lesson-draft.ts"
      provides: "lessonDraftCommandHandlers（authorize+execute），execute 调 tool → emit 三事件 / 失败抛错"
      contains: "lessonDraftCommandHandlers"
    - path: "src/features/platform-core/commands/registry.ts"
      provides: "platformCommandRegistry 追加 lesson.draft.run 定义"
      contains: "lesson.draft.run"
    - path: "src/features/platform-core/commands/handlers/lesson-draft.events.test.ts"
      provides: "handler 事件单测（三事件 eventType + summary-only payload / 失败路径无 domain 事件）"
      contains: "lesson.draft.produced"
  key_links:
    - from: "src/features/platform-core/commands/handlers/lesson-draft.ts"
      to: "@/server/ai/tools (createDraftLessonStepTool)"
      via: "handler execute 内确定性调用工具生成步骤"
      pattern: "createDraftLessonStepTool"
    - from: "src/features/platform-core/commands/handlers/lesson-draft.ts"
      to: "PlatformDomainEventSchema (lesson.draft.*)"
      via: "successResult.emittedEvents 承载三 AI 域事件"
      pattern: "lesson.draft.produced"
    - from: "src/features/platform-core/commands/registry.ts"
      to: "lessonDraftCommandHandlers"
      via: "createPlatformCommandDefinition 注册"
      pattern: "lesson.draft.run"
---

<objective>
为 AGENT-04 落地 **command 子系统**：在 `commands/contracts.ts` 新增 `lesson.draft.run` command 类型（复用既有 `{schoolId, pluginId}` scope，pluginId 携带保留 sentinel `core.lesson-agent` —— **零改** scope schema 与 bus 失败路径），新建 `handlers/lesson-draft.ts`（execute 经只读授权后确定性调用 Plan 62-02 的 `createDraftLessonStepTool` 生成步骤，成功落账三条 AI 域事件、失败抛 `PlatformCommandExecutionError` 走 bus 唯一 generic 失败事件），并在 `registry.ts` 注册。

Purpose: 事件落账无法脱离父 command（`appendPlatformEvents` 必填 `commandId` FK notNull），故起草关键节点必须经 `dispatchPlatformCommand` 产生 `commandId` 才能进 event ledger。本 plan 把「工具生成」嵌入命令执行轨道，使三条成功事件经 handler 自有 `emittedEvents` 落账、失败事件复用既有 generic 形态 —— 满足 D-53-07/08 不变式。生成步骤经 `resultSummary` 回传（command 记录是 D-01 允许的持久副作用），本 phase 不写 lesson/draft version。
Output: 扩展的 command 契约 + 新 handler + 注册项 + handler 事件单测，证明三事件落账与失败语义。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-lessonagent-typed-tool-layer/62-CONTEXT.md
@.planning/phases/62-lessonagent-typed-tool-layer/62-PATTERNS.md

<interfaces>
<!-- 已实读真实签名，executor 直接照抄 -->

command 类型枚举扩展点（src/features/platform-core/commands/contracts.ts:13-26）:
```typescript
export const PlatformPluginGovernanceCommandTypes = ["plugin.install", /* ... */, "plugin.kill_switch.set"] as const;
// 当前：export const PlatformCommandTypeSchema = z.enum(PlatformPluginGovernanceCommandTypes);
// 改为并入新数组：
export const LessonDraftCommandTypes = ["lesson.draft.run"] as const;
export const PlatformCommandTypeSchema = z.enum([...PlatformPluginGovernanceCommandTypes, ...LessonDraftCommandTypes]);
```

scope（:31-34，零改，sentinel pluginId 复用之）:
```typescript
export const PlatformCommandScopeSchema = z.object({ schoolId: z.string().min(1), pluginId: z.string().min(1) });
// lesson.draft.run 的 scope = { schoolId, pluginId: "core.lesson-agent" }（built-in 系统 agent 身份）
```

envelope + discriminated union 成员（:44-54, :140-181，照抄 extend 形）:
```typescript
PlatformCommandEnvelopeSchema.extend({ type: z.literal("lesson.draft.run"), payload: LessonDraftRunPayloadSchema });
```
PlatformCommandPayloadSchemas 映射（:130-138）：追加 `"lesson.draft.run": LessonDraftRunPayloadSchema`。

execute 结果契约（:197-227，superRefine：failed 不可带 emittedEvents；failureEvent 需 failureAttribution）:
```typescript
export const PlatformCommandExecutionResultSchema = z.object({
  resultSummary: z.record(z.string(), z.unknown()).nullable(),
  invalidation: PlatformCommandInvalidationSchema.default({ tags: [] }),
  emittedEvents: z.array(PlatformSuccessOrDomainEventSchema).default([]),  // 三 AI 域事件经此承载
  failureEvent: PlatformFailureEventSchema.nullable().default(null),
  failureAttribution: PlatformFailureAttributionSchema.nullable().default(null),
});
```

handler 助手与返回形（src/features/platform-core/commands/handlers/plugins.ts:54-59,117-160,215-239，照抄）:
```typescript
type ExecutionInput<T> = { command: T; attemptNumber: number };
type ExecutionResult = Awaited<ReturnType<PlatformCommandDefinition["execute"]>>;
function withAudit(event, audit) { return { ...event, audit }; }       // :138
function successResult({ resultSummary, invalidation, emittedEvents }) // :145
// 失败：throw new PlatformCommandExecutionError({ message, failureAttribution, failureEvent })（contracts.ts:230）
// 授权：const scope = await assertActiveTeacher();  // plugins.ts:26,88 复用，抛 TEACHER_AUTH_REQUIRED
// 末行：} satisfies Record<PlatformCommandType, Pick<PlatformCommandDefinition, "authorize"|"execute">>;  // plugins.ts:960
```

registry 注册（src/features/platform-core/commands/registry.ts:14-21，照抄）:
```typescript
"lesson.draft.run": createPlatformCommandDefinition({
  commandType: "lesson.draft.run",
  payloadSchema: PlatformCommandPayloadSchemas["lesson.draft.run"],
  dedupe: "optional",
  authorize: lessonDraftCommandHandlers["lesson.draft.run"].authorize,
  execute: lessonDraftCommandHandlers["lesson.draft.run"].execute,
}),
```

tool（Plan 62-02 产出）:
```typescript
import { createDraftLessonStepTool } from "@/server/ai/tools";
const step = await createDraftLessonStepTool({ teacherId }).execute({ lessonId, stepType, intent }, {} as any);
```

handler 事件测试骨架（src/features/platform-core/commands/handlers/plugins.events.test.ts:1-58，照抄 vi.hoisted + vi.mock）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为 lesson.draft.run handler 写事件单测（RED）</name>
  <read_first>
    - src/features/platform-core/commands/handlers/plugins.events.test.ts:1-120（vi.hoisted + vi.mock DAL/db + 断言 emittedEvents eventType/payload 的骨架，全文照搬）
    - src/features/platform-core/events/contracts.ts:5-27（SummaryRecordSchema，断言 payload summary-only）
    - src/features/platform-core/commands/contracts.ts:197-227（执行结果 superRefine 约束）
  </read_first>
  <behavior>
    - Test 1（成功落账三事件 / AGENT-04）: mock `assertActiveTeacher` 返回 `{ userId:"t1", schoolIds:["s1"] }`，mock `@/server/ai/tools` 的 `createDraftLessonStepTool` 返回 `{ execute: async () => ({ type:"content", title:"导入", body:"...", teacherNotes:"...", materialRefs:[] }) }`。调用 `lessonDraftCommandHandlers["lesson.draft.run"].execute({ command: <合法 envelope，scope.pluginId="core.lesson-agent"，payload{lessonId,stepType:"content",intent}>, attemptNumber:1 })` → 返回 `emittedEvents` 长度==3，eventType 集合 == {lesson.draft.requested, lesson.tool.invoked, lesson.draft.produced}，`failureEvent==null`。
    - Test 2（payload summary-only）: 三事件每个 payload 经 `SummaryRecordSchema.safeParse` success（无 *Json 字段、无整包步骤快照）；断言 produced 事件 payload 不含 `body`/`materialRefs` 等整包字段，仅 `{ stepType, title, succeeded }` 类摘要。
    - Test 3（步骤经 resultSummary 回传 / SC3）: 返回 `resultSummary` 含可还原步骤包的字段或 step 引用（executor 定形，但须能让调用方拿到 content/task/quiz 步骤）。
    - Test 4（失败路径 / D-53-08）: mock tool.execute 抛错 → handler `execute` 抛 `PlatformCommandExecutionError`，其 `failureEvent` 非空、`failureAttribution` 非空，且**不**返回任何 domain 事件。
    - Test 5（correlation 串联 / SC4）: 三事件 aggregateId 同为 `command.payload.lessonId`，可经同一 commandId 追溯（断言 aggregateType=="lesson"）。
  </behavior>
  <action>
    新建 `src/features/platform-core/commands/handlers/lesson-draft.events.test.ts`：`vi.mock("server-only", () => ({}))`，`vi.hoisted` mock `@/lib/dal/lesson-authoring`（仅 `assertActiveTeacher`）与 `@/server/ai/tools`（`createDraftLessonStepTool`）。按 behavior 1-5 写断言（mock 风格照搬 plugins.events.test.ts:1-58）。导入 `lessonDraftCommandHandlers`（尚未实现 → RED）、`PlatformCommandExecutionError`、`SummaryRecordSchema`。构造合法 command envelope 时 `scope.pluginId="core.lesson-agent"`、`correlation` 三字段齐全。
  </action>
  <verify>
    <automated>pnpm vitest run src/features/platform-core/commands/handlers/lesson-draft.events.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lesson.draft.requested\|lesson.tool.invoked\|lesson.draft.produced" src/features/platform-core/commands/handlers/lesson-draft.events.test.ts` ≥ 3
    - `grep -c "PlatformCommandExecutionError" src/features/platform-core/commands/handlers/lesson-draft.events.test.ts` ≥ 1
    - 运行此刻 RED（handler 未实现）
  </acceptance_criteria>
  <done>事件单测存在，覆盖三事件落账 / summary-only / resultSummary 回传 / 失败语义 / correlation，运行 RED。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 command 类型 + handler + 注册（GREEN）</name>
  <read_first>
    - src/features/platform-core/commands/contracts.ts:13-26,44-54,130-181（枚举/envelope/payload 映射/union，全部扩展点）
    - src/features/platform-core/commands/handlers/plugins.ts:54-160,215-239,945-960（ExecutionInput/Result、helpers、execute 返回 emittedEvents、satisfies 收口）
    - src/features/platform-core/commands/registry.ts:14-21（createPlatformCommandDefinition 注册形）
    - src/features/platform-core/events/contracts.ts（Plan 62-01 新增的 Lesson* 事件 schema，作 emittedEvents 类型对齐）
  </read_first>
  <action>
    1. `commands/contracts.ts`：① 新增 `export const LessonDraftCommandTypes = ["lesson.draft.run"] as const;`，把 `PlatformCommandTypeSchema` 改为 `z.enum([...PlatformPluginGovernanceCommandTypes, ...LessonDraftCommandTypes])`；② 新增 `LessonDraftRunPayloadSchema = z.object({ lessonId: z.string().min(1), stepType: z.enum(["content","task","quiz"]), intent: z.string().min(1) }).strict()`；③ `PlatformCommandPayloadSchemas` 映射追加 `"lesson.draft.run": LessonDraftRunPayloadSchema`；④ `PlatformCommandSchema` 联合追加 `PlatformCommandEnvelopeSchema.extend({ type: z.literal("lesson.draft.run"), payload: LessonDraftRunPayloadSchema })`。**不改** scope schema（pluginId 容纳 sentinel）。
    2. 新建 `commands/handlers/lesson-draft.ts`：`import "server-only";`。`import { createDraftLessonStepTool } from "@/server/ai/tools"`、`import { assertActiveTeacher } from "@/lib/dal/lesson-authoring"`、`PlatformCommandExecutionError` 等。导出 `lessonDraftCommandHandlers` 含 `"lesson.draft.run": { authorize, execute }`，末尾 `satisfies Record<"lesson.draft.run", Pick<PlatformCommandDefinition, "authorize"|"execute">>`（或并入全量 Record，executor 按类型择形）。
       - `authorize`: `const scope = await assertActiveTeacher();`，校验 `command.scope.schoolId ∈ scope.schoolIds`，否则抛授权错误。
       - `execute({ command })`: ① `const { userId } = await assertActiveTeacher();`（teacherId 来源）；② try { `const step = await createDraftLessonStepTool({ teacherId: userId }).execute({ lessonId: command.payload.lessonId, stepType: command.payload.stepType, intent: command.payload.intent }, {} as any);` }；③ 成功 → `return successResult({ resultSummary: { stepType: step.type, title: step.title, succeeded: true, step }, invalidation: { tags: [] }, emittedEvents: [ withAudit({ eventType:"lesson.draft.requested", category:"domain", aggregateType:"lesson", aggregateId: command.payload.lessonId, payload:{ commandType:"lesson.draft.run", stepType: command.payload.stepType, intentSummary: command.payload.intent } }, command.audit), withAudit({ eventType:"lesson.tool.invoked", ..., payload:{ toolName:"draftLessonStep", stepType: command.payload.stepType, attempt:1 } }, command.audit), withAudit({ eventType:"lesson.draft.produced", ..., payload:{ stepType: step.type, title: step.title, succeeded: true } }, command.audit) ] });`（三事件 payload **仅摘要**，禁整包 step；step 仅入 resultSummary）；④ catch(err) → `throw new PlatformCommandExecutionError({ message, failureAttribution: { ...pluginId: "core.lesson-agent" 形 }, failureEvent: <generic platform.command.failed> })`，照抄 plugins handler 失败构造（不发 domain 事件）。
    3. `commands/registry.ts`：`import { lessonDraftCommandHandlers } from "./handlers/lesson-draft"`，在 `platformCommandRegistry` 追加 `"lesson.draft.run"` 项（dedupe 用 `"optional"`，N=1）。保持 `satisfies Record<PlatformCommandType, PlatformCommandDefinition>` 全覆盖（新增枚举值后此约束强制注册，编译即守卫）。
  </action>
  <verify>
    <automated>pnpm vitest run src/features/platform-core/commands/handlers/lesson-draft.events.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lesson.draft.run" src/features/platform-core/commands/contracts.ts` ≥ 3（枚举/payload 映射/union）
    - `grep -c "lesson.draft.run" src/features/platform-core/commands/registry.ts` ≥ 1
    - `grep -v '^\s*//' src/features/platform-core/commands/handlers/lesson-draft.ts | grep -c 'aggregateType: "lesson"\|aggregateType:"lesson"'` == 3
    - `grep -c "createDraftLessonStepTool\|assertActiveTeacher\|PlatformCommandExecutionError" src/features/platform-core/commands/handlers/lesson-draft.ts` ≥ 3
    - Task 1 五项断言 RED→GREEN
  </acceptance_criteria>
  <done>command 类型扩展、handler 实现（成功三事件 + 失败 generic）、registry 注册；事件单测全绿；既有 plugin command 注册/事件无回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调用方 → command payload | 不可信起草参数跨入命令轨道，payloadSchema 边界校验 |
| handler → event ledger | handler 产出事件跨入持久账本，须 summary-only 且失败仅一条 generic 事件 |
| sentinel pluginId | 系统 agent 身份复用 plugin scope 字段，须确保不被外部冒用为真实插件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-07 | Tampering | command payload | mitigate | LessonDraftRunPayloadSchema `.strict()` 边界校验，非法 lessonId/stepType 在 dispatch 入口被拒 |
| T-62-08 | Repudiation | 起草关键节点可追溯 | mitigate | 三 AI 域事件经同一 commandId/correlationId 落账，aggregateId=lessonId，operator 可追溯（Task 1 Test 5 / AGENT-04）|
| T-62-09 | Information Disclosure | 事件 payload | mitigate | 事件 payload summary-only（SummaryRecordSchema 守卫），整包 step 仅入 resultSummary 不入事件（Task 1 Test 2）|
| T-62-10 | Spoofing | sentinel pluginId=core.lesson-agent | accept | 保留命名空间 `core.*` 仅由 server-only handler 内部构造，外部 plugin 注册走独立校验路径；本 phase 不开放外部以该 id dispatch（低风险，N=1 内部链路）|
| T-62-11 | Elevation of Privilege | 越权起草他校课时 | mitigate | authorize 经 assertActiveTeacher 校验 schoolId ∈ scope.schoolIds + tool 内 getTeacherLessonPreviewDTO 二次授权域 |
</threat_model>

<verification>
- `pnpm vitest run src/features/platform-core/commands/handlers/lesson-draft.events.test.ts` 全绿
- `pnpm test` full suite 不回归（尤其 commands/bus.test.ts、handlers/plugins.events.test.ts、events/ledger.test.ts —— 枚举/注册表扩展不破既有 command）
</verification>

<success_criteria>
lesson.draft.run command 经 Command Bus 唯一合法路径，成功落账三条可追溯 AI 域事件（SC4）、失败发唯一 generic 失败事件（D-53-08），生成步骤经 resultSummary 回传（SC3）；零改 scope schema 与 bus 失败路径（Option B 架构张力解决）。
</success_criteria>

<output>
完成后创建 `.planning/phases/62-lessonagent-typed-tool-layer/62-03-SUMMARY.md`
</output>
