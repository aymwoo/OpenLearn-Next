---
phase: 65-eval-guardrails-verify-phase-close-gate
plan: 05
type: execute
wave: 3
depends_on: [65-01, 65-02, 65-03, 65-04]
files_modified:
  - scripts/verify-phase65-eval-guardrails.ts
  - package.json
autonomous: true
requirements: [EVAL-03]
must_haves:
  truths:
    - "A single command (verify:phase65) runs static boundary checks for Phase 65 plus the focused eval/guardrail/handler/contract test subset and exits non-zero on any failure."
    - "An authoritative verify:phase alias resolves to the Phase 65 close gate so the milestone has one canonical 'is this phase done' command."
    - "The close gate proves the EVAL-01 eval suite runs, the guardrail validator + tool wiring exist, the lesson.draft.rejected contract is registered summary-only, and the handler branch distinguishes rejection from failure — without requiring network or a provider key."
  artifacts:
    - path: "scripts/verify-phase65-eval-guardrails.ts"
      provides: "Phase 65 close-gate verifier (static checks + runVitest subset)"
      contains: "lesson.draft.rejected"
    - path: "package.json"
      provides: "verify:phase65 + verify:phase scripts"
      contains: "verify:phase65"
  key_links:
    - from: "package.json"
      to: "scripts/verify-phase65-eval-guardrails.ts"
      via: "verify:phase65 + verify:phase alias"
      pattern: "verify-phase65-eval-guardrails"
---

<objective>
Deliver the close gate (EVAL-03): a `verify:phase65` script that statically asserts the Phase 65 boundaries and runs the focused eval/guardrail/event test subset, plus an authoritative `verify:phase` alias.

Purpose: SC3 requires a single repeatable gate that fails loudly if eval/guardrail/rejection wiring regresses. Modeled on `scripts/verify-phase54-ai-contracts.ts` (same `read`/`listFiles`/`run`/`runVitest` helpers, `process.exit(1)` on static-check failure). The alias gives the milestone one canonical command.
Output: `scripts/verify-phase65-eval-guardrails.ts` and two `package.json` scripts.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-CONTEXT.md
@.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-PATTERNS.md
@.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-VALIDATION.md

<interfaces>
<!-- ANALOG to copy verbatim for harness plumbing: scripts/verify-phase54-ai-contracts.ts -->
<!--   helpers: read(filePath), listFiles(root), run(cmd,args,label), runVitest(paths,label) -->
<!--   pattern: build StaticCheck[] → if any fails, print gaps + process.exit(1) → else runVitest(subset) -->
<!--   runVitest resolves node_modules/vitest/vitest.mjs, falls back to .bin/vitest, then pnpm exec -->

<!-- Artifacts produced by 65-01..65-04 that this gate inspects: -->
src/lib/dto/draft-guardrails.ts                                   // enum + DraftGuardrailRejection (non-server-only)
src/server/ai/tools/guardrails.ts                                 // server-only assertStepWithinGuardrails
src/server/ai/tools/lesson-draft.ts                               // calls assertStepWithinGuardrails(step)
src/server/ai/tools/lesson-draft.eval.test.ts                     // EVAL-01 suite
src/features/platform-core/events/contracts.ts                    // lesson.draft.rejected registered
src/features/platform-core/commands/handlers/lesson-draft.ts      // instanceof DraftGuardrailRejection branch

<!-- package.json scripts block (lines 5-73). No existing verify:phase alias; "test":"vitest" at line 72. -->
<!-- Phase 65 invocation style matches verify:phase54: node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/<file>.ts -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 65-05-01: Author verify-phase65 close-gate script</name>
  <read_first>
    - scripts/verify-phase54-ai-contracts.ts (full) — ANALOG to copy: `read`, `listFiles`, `run`, `runVitest`, `verifyPackageScript`, `main()` static-check-then-test structure, `process.exit(1)` on gap.
    - src/server/ai/tools/lesson-draft.ts (post-65-02) — confirm the `assertStepWithinGuardrails` call string to grep.
    - src/features/platform-core/commands/handlers/lesson-draft.ts (post-65-04) — confirm the `instanceof DraftGuardrailRejection` string to grep.
  </read_first>
  <action>
    Create `scripts/verify-phase65-eval-guardrails.ts`. Copy the `read`, `listFiles`, `run`, `runVitest` helpers from verify-phase54 verbatim (keep `runVitest` resolution order). Rename log/error strings to "Phase 65". Replace `verifyPackageScript` to assert `pkg.scripts["verify:phase65"] === "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase65-eval-guardrails.ts"` AND `pkg.scripts["verify:phase"] === "pnpm verify:phase65"`.
    Build `staticChecks: StaticCheck[]` reading the relevant sources via `read(...)`:
    1. "package.json exposes exact verify:phase65 + verify:phase alias" → `verifyPackageScript(packageSource)`.
    2. "guardrail reason-code contract is non-server-only and exports DraftGuardrailRejection" → guardrailsDtoSource includes `class DraftGuardrailRejection` AND NOT `"server-only"` (reason-code enum module must not be server-only).
    3. "guardrail validator is server-only and pure" → `read("src/server/ai/tools/guardrails.ts")` includes `"server-only"` AND `assertStepWithinGuardrails` AND `FORBIDDEN_MARKERS`.
    4. "draft tool invokes the guardrail before returning the step" → `read("src/server/ai/tools/lesson-draft.ts")` includes `assertStepWithinGuardrails(`.
    5. "lesson.draft.rejected is registered summary-only" → contractsSource includes `lesson.draft.rejected` AND `LessonDraftRejectedEventSchema` AND `summaryOnlyStrictPayload`.
    6. "handler distinguishes guardrail rejection from real failure" → `read(".../handlers/lesson-draft.ts")` includes `instanceof DraftGuardrailRejection`.
    7. "phase 65 does not introduce a guardrail persistence table" (scope guard, NOT-doing list) → assert NO new `sqliteTable(` with a guardrail/rejection name across `listFiles("src/server/db")` combined source (e.g. `!/guardrailRejection|draftRejection/i.test(dbSource)`).
    On any failed check: print the gaps and `process.exit(1)`.
    Then `runVitest([...], "Phase 65 eval + guardrail close-gate suite")` over:
      - src/lib/dto/draft-guardrails.test.ts
      - src/server/ai/tools/guardrails.test.ts
      - src/server/ai/tools/lesson-draft.test.ts
      - src/server/ai/tools/lesson-draft.eval.test.ts
      - src/features/platform-core/events/contracts.test.ts
      - src/features/platform-core/commands/handlers/lesson-draft.events.test.ts
    End with the success banner. Call `main()` at the bottom.
  </action>
  <acceptance_criteria>
    - `grep -n 'process.exit(1)' scripts/verify-phase65-eval-guardrails.ts` returns a match.
    - `grep -cE 'lesson.draft.rejected|assertStepWithinGuardrails|instanceof DraftGuardrailRejection' scripts/verify-phase65-eval-guardrails.ts` returns >= 3.
    - `grep -n 'lesson-draft.eval.test.ts' scripts/verify-phase65-eval-guardrails.ts` returns a match (EVAL-01 suite included in the gate).
  </acceptance_criteria>
  <done>Close-gate script exists with Phase 65 static checks + focused test subset and non-zero exit on failure.</done>
</task>

<task type="auto">
  <name>Task 65-05-02: Wire verify:phase65 + authoritative verify:phase, run the gate</name>
  <read_first>
    - package.json (lines 5-73) — the scripts block; insert after `verify:phase60:local` (line 63), keep `"test":"vitest"` last. Match the verify:phase54 invocation exactly (line 56).
  </read_first>
  <action>
    In `package.json` scripts, add:
    - `"verify:phase65": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase65-eval-guardrails.ts"`
    - `"verify:phase": "pnpm verify:phase65"` (authoritative alias — the canonical close-gate command for this milestone; must resolve to the latest phase gate).
    Keep JSON valid (commas, ordering). Do not alter unrelated scripts.
  </action>
  <acceptance_criteria>
    - `node -e "const s=require('./package.json').scripts; process.exit(s['verify:phase65'] && s['verify:phase']==='pnpm verify:phase65' ? 0 : 1)"` exits 0.
    - `pnpm verify:phase65` runs the gate to completion and exits 0 (all static checks pass + subset green). Equivalently `npm run verify:phase65`.
    - `pnpm verify:phase` exits 0 (alias resolves to the Phase 65 gate).
  </acceptance_criteria>
  <done>verify:phase65 and verify:phase both registered; running the gate passes end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CI gate → source tree | The verifier only reads files + runs the local vitest subset; it executes no network calls and no provider model. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-GATE | Tampering / Elevation | scope-guard static check | mitigate | Static check 7 fails the gate if a guardrail/rejection persistence table is introduced (enforces the NOT-doing boundary: no new guardrail persistence table this phase). |
| T-65-GATE-NET | Information Disclosure | verifier process | accept | Verifier is local-only (read + vitest subset). The eval/guardrail suites it runs are themselves mocked (no key/network), so the gate inherits offline determinism. |
</threat_model>

<verification>
- `pnpm verify:phase65` exits 0 with all 7 static checks passing and the 6-file subset green.
- `pnpm verify:phase` resolves to the Phase 65 gate and exits 0.
- Gate exits non-zero if any guardrail/eval/contract/handler wiring is removed (static checks) or any subset test fails.
</verification>

<success_criteria>
A single authoritative command (verify:phase / verify:phase65) repeatably proves eval + guardrail + rejection-recording wiring is intact and fails loudly on regression (SC3 / EVAL-03).
</success_criteria>

<output>
After completion, create `.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-05-SUMMARY.md`.
</output>
