---
phase: 65-eval-guardrails-verify-phase-close-gate
plan: 02
type: execute
wave: 2
depends_on: [65-01]
files_modified:
  - src/server/ai/tools/guardrails.ts
  - src/server/ai/tools/guardrails.test.ts
  - src/server/ai/tools/lesson-draft.ts
autonomous: true
requirements: [EVAL-02]
must_haves:
  truths:
    - "A server-only pure guardrail validator rejects every out-of-bounds draft step (illegal type, oversize field, invalid teaching structure, quiz index out of range, forbidden/injection content) by throwing the typed DraftGuardrailRejection."
    - "A valid content/task/quiz step passes the guardrail untouched."
    - "The draft tool's execute runs the guardrail AFTER aiGenerateObject and BEFORE returning the step, so no out-of-bounds step ever leaves the single authoritative generation channel."
  artifacts:
    - path: "src/server/ai/tools/guardrails.ts"
      provides: "assertStepWithinGuardrails(step) server-only validator"
      contains: "assertStepWithinGuardrails"
    - path: "src/server/ai/tools/guardrails.test.ts"
      provides: "One unit case per reason code + a valid-passes case"
  key_links:
    - from: "src/server/ai/tools/lesson-draft.ts"
      to: "assertStepWithinGuardrails"
      via: "call in execute after aiGenerateObject, before return"
      pattern: "assertStepWithinGuardrails\\("
---

<objective>
Implement the guardrail validator and wire it into the draft tool so out-of-bounds model output is rejected at the source (EVAL-02, locked decision D-04).

Purpose: SC2 requires guardrails to intercept illegal step types, oversize output, and injected forbidden content. The validator lives inside the single authoritative generation path (`createDraftLessonStepTool.execute`), after `aiGenerateObject` returns and before `return step`, preserving the "only one generation channel" invariant from Phase 62.
Output: `src/server/ai/tools/guardrails.ts` (+ test) and the one-line wiring in `lesson-draft.ts`.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-CONTEXT.md
@.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-PATTERNS.md
@.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-VALIDATION.md

<interfaces>
<!-- From 65-01 (src/lib/dto/draft-guardrails.ts). -->
GuardrailReasonCodeSchema = z.enum(["illegal_step_type","oversize_field","invalid_teaching_structure","quiz_correct_index_out_of_range","forbidden_content"])
GUARDRAIL_MAX_FIELD_LENGTH = 8000
class DraftGuardrailRejection extends Error { reasonCode; stepType }  // carries NO step snapshot

<!-- From 65-01 corpus (src/server/ai/tools/__fixtures__/draft-step-corpus.ts). -->
draftStepCorpus.valid = { content, task, quiz }
draftStepCorpus.counterExamples = Array<{ reasonCode, step }>  // one per reason code

<!-- Tool insertion point (src/server/ai/tools/lesson-draft.ts:56-63). -->
const step = await aiGenerateObject({ teacherId, prompt, schema: lessonStepPayloadSchema });
// ← guardrail call goes HERE
return step;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 65-02-01: assertStepWithinGuardrails server-only validator</name>
  <read_first>
    - src/server/ai/tools/lesson-draft.ts (full, 66 lines) — ANALOG for the server-only module header (`import "server-only"`) and how the step shape is consumed.
    - src/lib/dto/draft-guardrails.ts (from 65-01) — the enum, GUARDRAIL_MAX_FIELD_LENGTH, and DraftGuardrailRejection to throw.
    - src/lib/dto/lesson-authoring.ts (lines 114-160) — exact teaching-structure fields per step type (content title+body; task prompt+submissionType; quiz question+options+correctOptionIndex).
  </read_first>
  <action>
    Create `src/server/ai/tools/guardrails.ts` starting with `import "server-only";`.
    Export `export function assertStepWithinGuardrails(step: unknown): asserts step is LessonStepPayload` (import `LessonStepPayload` + `lessonStepPayloadSchema` from `@/lib/dto/lesson-authoring`; import `DraftGuardrailRejection`, `GuardrailReasonCodeSchema`, `GUARDRAIL_MAX_FIELD_LENGTH` from `@/lib/dto/draft-guardrails`).
    Validation order (throw `new DraftGuardrailRejection({ reasonCode, stepType })` on first failure; `stepType` = the step's `type` cast, or the literal value if illegal):
    1. `illegal_step_type` — if `step.type` ∉ {content,task,quiz}. (Use the raw value; for stepType field default to `"content"` only when unknown is impossible — prefer narrowing: if not one of the three, reasonCode illegal_step_type with stepType set to the closest known or `"content"` sentinel; document the choice.)
    2. `invalid_teaching_structure` — content: `title`/`body` missing or empty after `.trim()`; task: `prompt` empty OR `submissionType` ∉ {text,image,file,link}; quiz: `question` empty OR `options.length < 2`. (D-02 teaching invariants.)
    3. `quiz_correct_index_out_of_range` — quiz with `correctOptionIndex` defined AND (`< 0` OR `>= options.length`).
    4. `oversize_field` — any of content.body / task.prompt / quiz.question length > `GUARDRAIL_MAX_FIELD_LENGTH`.
    5. `forbidden_content` — concatenate all text fields (title, body, prompt, question, options[], explanation, teacherNotes) and test against a documented deny-list of injection/forbidden markers (case-insensitive): `ignore previous instructions`, `<script`, `eval(`, `DROP TABLE`, `system prompt`. Keep the deny-list as a named exported-or-local `const FORBIDDEN_MARKERS: RegExp[]` so 65-05's static check can grep it.
    Keep it a PURE function: no DAL, no DB, no env, no eval, no network. It validates and either returns (asserts) or throws.
  </action>
  <acceptance_criteria>
    - `grep -n 'import "server-only"' src/server/ai/tools/guardrails.ts` returns a match (server-only module).
    - `grep -nE 'eval\(|process\.env|prisma|drizzle|fetch\(' src/server/ai/tools/guardrails.ts | grep -v 'eval(' ` — no DB/env/network usage (the only `eval(` token is inside the FORBIDDEN_MARKERS deny-list string, not a call).
    - Compiles: covered by the test run below.
  </acceptance_criteria>
  <done>guardrails.ts exports assertStepWithinGuardrails covering all 5 reason codes; server-only; pure.</done>
</task>

<task type="auto">
  <name>Task 65-02-02: guardrails.test.ts — one case per reason code + valid passes</name>
  <read_first>
    - src/server/ai/tools/lesson-draft.test.ts (lines 1-34) — ANALOG for `vi.mock("server-only", () => ({}))` no-op pattern required to test a server-only module.
    - src/server/ai/tools/__fixtures__/draft-step-corpus.ts (from 65-01) — the corpus driving every assertion.
    - src/lib/dto/draft-guardrails.ts — DraftGuardrailRejection for `instanceof` assertions.
  </read_first>
  <action>
    Create `src/server/ai/tools/guardrails.test.ts`. Begin with `vi.mock("server-only", () => ({}))` (no-op, per analog). Then:
    - For EACH entry in `draftStepCorpus.counterExamples`, assert `expect(() => assertStepWithinGuardrails(entry.step)).toThrow(DraftGuardrailRejection)` AND that the thrown error's `reasonCode` equals `entry.reasonCode` (use a try/catch capturing the error). This proves all 5 reason codes are enforced (T-65-INJ included via `forbidden_content`).
    - For EACH of `draftStepCorpus.valid.content|task|quiz`, assert `expect(() => assertStepWithinGuardrails(validStep)).not.toThrow()`.
    - Assert the thrown rejection for `forbidden_content` carries NO step snapshot: `Object.keys(caught)` ⊆ {reasonCode, stepType} (reinforces T-65-PII at the throw site).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/server/ai/tools/guardrails.test.ts` passes.
    - Test iterates the corpus counterExamples (grep `counterExamples` in the test file returns a match) — no hand-duplicated cases drifting from the shared corpus.
  </acceptance_criteria>
  <done>guardrails.test.ts green; every counter-example rejected with the correct reasonCode; every valid step passes.</done>
</task>

<task type="auto">
  <name>Task 65-02-03: Wire guardrail into the draft tool execute</name>
  <read_first>
    - src/server/ai/tools/lesson-draft.ts (full) — the file being modified; insertion point is between line 60 (`});` of aiGenerateObject) and line 63 (`return step;`).
    - src/server/ai/tools/lesson-draft.test.ts — the existing tool unit test that must remain green after wiring.
  </read_first>
  <action>
    In `src/server/ai/tools/lesson-draft.ts`, add `import { assertStepWithinGuardrails } from "./guardrails";`. Inside `execute`, immediately after `const step = await aiGenerateObject({...});` and before `return step;`, insert:
    `// ② b. Guardrail：拒绝越界输出（EVAL-02 / D-04），抛 DraftGuardrailRejection 由 handler 落 rejected 事件。`
    `assertStepWithinGuardrails(step);`
    Do NOT catch the rejection here — it must propagate to the command handler (65-04) which distinguishes it from real generation failure. Keep the comment numbering consistent with the existing ①/②/③ scheme.
  </action>
  <acceptance_criteria>
    - `grep -n 'assertStepWithinGuardrails(step)' src/server/ai/tools/lesson-draft.ts` returns a match positioned before `return step;`.
    - `npx vitest run src/server/ai/tools/lesson-draft.test.ts src/server/ai/tools/guardrails.test.ts src/server/ai/tools/no-leak.test.ts` all pass.
  </acceptance_criteria>
  <done>Tool calls the guardrail after generation, before return; existing tool tests + no-leak still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| AI model output → returned step | `aiGenerateObject` output is untrusted; the guardrail is the chokepoint before the step leaves the tool. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-INJ | Tampering / Elevation | `assertStepWithinGuardrails` forbidden_content branch (guardrails.ts) | mitigate | Deny-list (`FORBIDDEN_MARKERS`) scans every text field of the generated step; any injection/forbidden marker → `DraftGuardrailRejection(forbidden_content)`. Asserted by guardrails.test.ts via the corpus `forbidden_content` counter-example. |
| T-65-PII | Information Disclosure | thrown rejection object | mitigate | Validator throws the 65-01 `DraftGuardrailRejection` which carries only reasonCode + stepType; test asserts no step snapshot on the caught error. |
</threat_model>

<verification>
- `npx vitest run src/server/ai/tools/guardrails.test.ts` green (all 5 reason codes + valid passes).
- Tool execute calls `assertStepWithinGuardrails(step)` before return; existing tool + no-leak tests green.
- guardrails.ts is server-only and pure (no DB/env/network/eval-call).
</verification>

<success_criteria>
Out-of-bounds draft output is structurally impossible to return from the tool; rejection propagates as a typed DraftGuardrailRejection for the handler to record (SC2 part 1).
</success_criteria>

<output>
After completion, create `.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-02-SUMMARY.md`.
</output>
