---
phase: 65-eval-guardrails-verify-phase-close-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dto/draft-guardrails.ts
  - src/lib/dto/draft-guardrails.test.ts
  - src/server/ai/tools/__fixtures__/draft-step-corpus.ts
autonomous: true
requirements: [EVAL-02]
must_haves:
  truths:
    - "A neutral, non-server-only contract module exposes the guardrail reason-code enum consumable by both the AI tool layer and the events/contracts layer without crossing a server-only boundary."
    - "A typed DraftGuardrailRejection error exists that carries ONLY a reason code + step type — never a step snapshot — so downstream emitters cannot leak draft content."
    - "A single shared fixture corpus provides valid content/task/quiz steps AND at least one out-of-bounds counter-example per reason code, reused by both the eval suite (EVAL-01) and the guardrail unit test (EVAL-02)."
  artifacts:
    - path: "src/lib/dto/draft-guardrails.ts"
      provides: "GuardrailReasonCodeSchema enum + GuardrailReasonCode type + DraftGuardrailRejection class"
      contains: "DraftGuardrailRejection"
    - path: "src/server/ai/tools/__fixtures__/draft-step-corpus.ts"
      provides: "Shared valid + counter-example corpus keyed by reason code"
      contains: "draftStepCorpus"
  key_links:
    - from: "src/lib/dto/draft-guardrails.ts"
      to: "DraftGuardrailRejection"
      via: "export class carrying { reasonCode, stepType } only"
      pattern: "class DraftGuardrailRejection"
---

<objective>
Lay the guardrail foundation: a neutral shared contract module (reason-code enum + typed rejection error) plus the single shared fixture corpus that every downstream Phase 65 plan consumes.

Purpose: EVAL-02 needs a reason-code vocabulary referenced in THREE layers — the guardrail validator (server-only `guardrails.ts`), the rejection event payload (`events/contracts.ts`), and the command handler. Putting the enum + error in the lowest layer (`src/lib/dto`) avoids any `events → server/ai/tools` import and any server-only leak. The shared corpus (locked decision D-03) is the one source of truth feeding both the EVAL-01 pass path and the EVAL-02 reject path.
Output: `src/lib/dto/draft-guardrails.ts`, its unit test, and `src/server/ai/tools/__fixtures__/draft-step-corpus.ts`.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-CONTEXT.md
@.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-PATTERNS.md
@.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-VALIDATION.md

<interfaces>
<!-- DTO step shapes the corpus must satisfy (src/lib/dto/lesson-authoring.ts:114-160). -->
content: { type:"content", title:string.min(1), body:string.min(1), teacherNotes?, materialRefs[] }
task:    { type:"task", prompt:string.min(1), submissionType:"text"|"image"|"file"|"link", materialRefs[] }
quiz:    { type:"quiz", question:string.min(1), options:string[].min(2), correctOptionIndex?:int>=0, materialRefs[] }
lessonStepPayloadSchema = z.discriminatedUnion("type",[content,task,quiz])

<!-- Analog enum to mirror (src/features/platform-core/events/contracts.ts:70). -->
const LessonStepTypeSchema = z.enum(["content","task","quiz"]);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 65-01-01: Reason-code enum + DraftGuardrailRejection contract module</name>
  <read_first>
    - src/features/platform-core/events/contracts.ts (lines 1-130) — ANALOG for `z.enum` declaration style (`LessonStepTypeSchema` at line 70) and the `summaryOnlyStrictPayload` rationale this enum will later feed.
    - src/lib/dto/lesson-authoring.ts (lines 114-160) — the step payload shapes; the rejection carries `stepType` from this enum domain, NOT the payload.
    - src/server/ai/tools/no-leak.test.ts — confirms which modules are server-only-bound; this NEW module MUST stay non-server-only (no `import "server-only"`).
  </read_first>
  <action>
    Create `src/lib/dto/draft-guardrails.ts` — a PURE, non-server-only contract module (NO `import "server-only"`).
    1. `export const GuardrailReasonCodeSchema = z.enum([
         "illegal_step_type",
         "oversize_field",
         "invalid_teaching_structure",
         "quiz_correct_index_out_of_range",
         "forbidden_content",
       ]);`
       and `export type GuardrailReasonCode = z.infer<typeof GuardrailReasonCodeSchema>;`
    2. `export const GUARDRAIL_MAX_FIELD_LENGTH = 8000;` (deterministic oversize threshold for body/prompt/question — referenced by guardrails.ts in 65-02).
    3. `export class DraftGuardrailRejection extends Error {` carrying ONLY `readonly reasonCode: GuardrailReasonCode` and `readonly stepType: "content" | "task" | "quiz"`. Constructor `({ reasonCode, stepType }: { reasonCode: GuardrailReasonCode; stepType: ... })`, set `this.name = "DraftGuardrailRejection"`, `this.message = \`DRAFT_GUARDRAIL_REJECTED:${reasonCode}\``. It MUST NOT accept or store any step payload, `*Json`, or free-form content field — this is the T-65-PII structural guarantee at the source (per D-07).
    Add a file-level doc comment stating: lives in `lib/dto` (lowest layer) so the server-only guardrail validator, the events/contracts payload schema, and the command handler can all import the reason-code vocabulary without an `events → server/ai/tools` import or a server-only leak.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/dto/draft-guardrails.test.ts` passes (test authored below).
    - `grep -n 'import "server-only"' src/lib/dto/draft-guardrails.ts` returns NO match.
    - `grep -nE 'step|payload|Json' src/lib/dto/draft-guardrails.ts | grep -v '^#' | grep -vi 'stepType\|GuardrailReason'` shows the DraftGuardrailRejection stores no step/payload/*Json field.
    - `npx vitest run src/server/ai/tools/no-leak.test.ts` still passes (new module did not introduce a boundary violation).
  </acceptance_criteria>
  <action_after>
    In the SAME task, create `src/lib/dto/draft-guardrails.test.ts`:
    - Asserts `GuardrailReasonCodeSchema.options` equals the exact 5-member array above.
    - Constructs `new DraftGuardrailRejection({ reasonCode: "forbidden_content", stepType: "quiz" })` and asserts: `instanceof Error` is true, `instanceof DraftGuardrailRejection` is true, `Object.keys(rejection)` contains only `reasonCode` + `stepType` (no `step`, no `payload`, no key ending in `Json`), `rejection.message` starts with `DRAFT_GUARDRAIL_REJECTED:`.
  </action_after>
  <done>draft-guardrails.ts + its test exist; enum has 5 members; DraftGuardrailRejection carries only reasonCode + stepType; no-leak.test.ts green.</done>
</task>

<task type="auto">
  <name>Task 65-01-02: Single shared fixture corpus (valid + counter-examples)</name>
  <read_first>
    - src/server/ai/tools/lesson-draft.test.ts (lines 36-60) — ANALOG for fixture step literals (`FAKE_CONTENT_STEP`) that satisfy `lessonStepPayloadSchema`.
    - src/lib/dto/lesson-authoring.ts (lines 114-160) — exact field requirements per step type.
    - src/lib/dto/draft-guardrails.ts — the reason-code enum each counter-example must map to (created in Task 01).
  </read_first>
  <action>
    Create `src/server/ai/tools/__fixtures__/draft-step-corpus.ts` (non-server-only fixture; follow mock-model.ts convention of no `server-only`).
    Export `draftStepCorpus` with two groups:
    1. `valid`: at least ONE fully-valid step per type — `content` (title + body non-empty), `task` (prompt non-empty + submissionType:"text"), `quiz` (question + options.length>=2 + correctOptionIndex in range). These feed EVAL-01 (must pass `lessonStepPayloadSchema` AND the teaching invariants D-02).
    2. `counterExamples`: an array of `{ reasonCode: GuardrailReasonCode; step: unknown }` covering EVERY reason code (D-03 requires ≥1 out-of-bounds counter-example):
       - `illegal_step_type`: `{ type: "lecture", ... }`
       - `oversize_field`: a valid content step whose `body` length > `GUARDRAIL_MAX_FIELD_LENGTH`
       - `invalid_teaching_structure`: a content step with empty `body` (or task with empty `prompt`, quiz with `options.length < 2`)
       - `quiz_correct_index_out_of_range`: quiz with `options.length === 2` and `correctOptionIndex === 5`
       - `forbidden_content`: a content step whose `body` contains a prompt-injection / forbidden marker (e.g. "ignore previous instructions" / `<script>` / `eval(`).
    Type counterExamples so `reasonCode` is `GuardrailReasonCode` (import from `@/lib/dto/draft-guardrails`). Keep `step` as `unknown`/`Record<string,unknown>` so out-of-bounds literals do not fight the discriminated union type.
  </action>
  <acceptance_criteria>
    - `grep -c 'reasonCode:' src/server/ai/tools/__fixtures__/draft-step-corpus.ts` returns >= 5 (one per reason code).
    - `npx vitest run src/lib/dto/draft-guardrails.test.ts` still passes (no regression from the import graph).
    - `node -e "require('node:fs').readFileSync('src/server/ai/tools/__fixtures__/draft-step-corpus.ts','utf8')"` — file readable (sanity); actual consumption verified by 65-02 / 65-03.
  </acceptance_criteria>
  <done>draft-step-corpus.ts exports `draftStepCorpus.valid` (3 types) and `draftStepCorpus.counterExamples` (all 5 reason codes).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| AI model output → tool/handler/event | Untrusted generated step content crosses here; the reason-code + rejection vocabulary defined here governs how rejections are represented downstream. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-PII | Information Disclosure | `DraftGuardrailRejection` (src/lib/dto/draft-guardrails.ts) | mitigate | Error class structurally carries ONLY `{ reasonCode, stepType }` — no step snapshot, no `*Json`, no free text. Asserted by draft-guardrails.test.ts so a later emitter cannot leak draft content via the rejection object. |
</threat_model>

<verification>
- `npx vitest run src/lib/dto/draft-guardrails.test.ts` green.
- `npx vitest run src/server/ai/tools/no-leak.test.ts` green (no boundary regression).
- Reason-code enum = exactly 5 members; corpus covers all 5 + 3 valid types.
</verification>

<success_criteria>
Foundation ready: enum + typed rejection (no content leak) + single shared corpus, all importable by 65-02/65-03/65-04 with zero server-only or cross-feature import violations.
</success_criteria>

<output>
After completion, create `.planning/phases/65-eval-guardrails-verify-phase-close-gate/65-01-SUMMARY.md`.
</output>
