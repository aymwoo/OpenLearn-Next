---
phase: 12-classroom-launch-and-built-in-teaching-steps
plan: 04
type: execute
wave: 2
depends_on:
  - 12-02
  - 12-03
files_modified:
  - src/server/plugins/registry.ts
  - src/lib/dto/resource-ai.ts
  - src/lib/dal/plugins.ts
  - src/components/authoring/lesson-authoring-workspace.tsx
  - src/components/plugins/plugin-renderer.tsx
  - src/components/plugins/widgets/index.tsx
  - scripts/verify-phase12-launch-and-builtins.ts
  - package.json
autonomous: true
requirements:
  - CLASS-02
  - CLASS-03
  - CLASS-04
  - LESSON-03
  - PLUGIN-04
  - PLUGIN-05
requirements_addressed:
  - CLASS-02
  - CLASS-03
  - CLASS-04
  - LESSON-03
  - PLUGIN-04
  - PLUGIN-05
must_haves:
  truths:
    - "D-07 / D-08 / D-09: Built-in teaching steps appear in the authoring `新增步骤` zone as a direct first-level `内置教学环节` group with the five locked names."
    - "D-14: Built-in plugin behavior stays inside explicit allowlisted actions and local renderers; no arbitrary script path is introduced."
    - "D-15: Phase 12 keeps the existing design/runtime contract while adding a dedicated verifier for launch routing, preview presence, seeded built-ins, and authoring exposure."
  artifacts:
    - path: "src/server/plugins/registry.ts"
      provides: "Explicit first-party safe action allowlist for built-in teaching steps"
    - path: "src/components/authoring/lesson-authoring-workspace.tsx"
      provides: "First-level built-in teaching-step quick-add group"
    - path: "scripts/verify-phase12-launch-and-builtins.ts"
      provides: "Phase 12 regression verifier"
  key_links:
    - from: "src/components/authoring/lesson-authoring-workspace.tsx"
      to: "src/server/plugins/registry.ts"
      via: "authoring UI aligns with seeded built-in registry/action vocabulary"
      pattern: "教师讲授|问卷调查|学生探究|课堂测验|评价"
    - from: "scripts/verify-phase12-launch-and-builtins.ts"
      to: "src/components/surfaces/classroom-launch-surface.tsx"
      via: "check dedicated launch surface and built-in integration markers"
      pattern: "开启新课堂|内置教学环节"
---

<objective>
Extend the safe first-party plugin contract so built-in teaching-step plugins can
surface in authoring and classroom-related UI without violating registry safety.

Purpose: Phase 12 is complete only when built-in plugins are not just seeded and
visible in management, but also usable in the teacher authoring flow.
Output: allowlisted first-party action support, built-in authoring quick-add UI,
and a dedicated Phase 12 verifier.
</objective>

<execution_context>
@/home/wuxf/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-CONTEXT.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-RESEARCH.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-UI-SPEC.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-PATTERNS.md
@src/server/plugins/registry.ts
@src/components/authoring/lesson-authoring-workspace.tsx
@docs/plugin-system-review.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the safe first-party plugin action contract for built-in teaching steps</name>
  <read_first>
    - `src/server/plugins/registry.ts`
    - `src/lib/dto/resource-ai.ts`
    - `src/lib/dal/plugins.ts`
    - `src/components/plugins/plugin-renderer.tsx`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-RESEARCH.md`
  </read_first>
  <files>src/server/plugins/registry.ts, src/lib/dto/resource-ai.ts, src/lib/dal/plugins.ts, src/components/plugins/plugin-renderer.tsx, src/components/plugins/widgets/index.tsx</files>
  <action>
    Extend the current plugin action vocabulary only as needed to support the
    five built-in teaching-step plugins. Add explicit allowlisted action names,
    permission mappings, and local proposal types for built-in first-party
    behaviors such as inserting a step template or exposing a built-in authoring
    suggestion. Keep all dispatch logic deterministic in `registry.ts`, update
    DTO enums/schemas to match exactly, and update `PluginRenderer` / widget
    dispatch only if the new proposal types need local rendering. Do not add
    remote code execution, `dangerouslySetInnerHTML`, eval, or untyped action
    strings.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/server/plugins/registry.ts src/lib/dto/resource-ai.ts src/lib/dal/plugins.ts src/components/plugins/plugin-renderer.tsx src/components/plugins/widgets/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/server/plugins/registry.ts` contains explicit built-in first-party action handling.
    - `src/lib/dto/resource-ai.ts` action enums exactly match the allowlisted action set.
    - `src/components/plugins/plugin-renderer.tsx` does not contain `eval(`.
    - `src/components/plugins/plugin-renderer.tsx` does not contain `dangerouslySetInnerHTML`.
    - Built-in proposal rendering stays local and typed.
  </acceptance_criteria>
  <done>Built-in plugin runtime behavior remains declarative, explicit, and safe while gaining the minimum new capability needed for first-party teaching steps.</done>
</task>

<task type="auto">
  <name>Task 2: Expose built-in teaching steps as direct first-level authoring actions</name>
  <read_first>
    - `src/components/authoring/lesson-authoring-workspace.tsx`
    - `src/actions/lesson-authoring-actions.ts`
    - `src/lib/dto/lesson-authoring.ts`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-UI-SPEC.md`
    - `DESIGN.md`
  </read_first>
  <files>src/components/authoring/lesson-authoring-workspace.tsx</files>
  <action>
    Update `LessonAuthoringWorkspace` so the existing `新增步骤` action area keeps
    the current base step buttons (`内容` / `任务` / `测验`) and adds a second
    direct group labeled `内置教学环节`. Render five direct actions named
    `教师讲授`、`问卷调查`、`学生探究`、`课堂测验`、`评价`. Each action must insert a
    valid initial payload using the existing lesson-step add path or the minimal
    new built-in-safe action contract from Task 1. Do not add a second chooser,
    modal, or extra configuration gate before insertion. Keep the group visually
    system-native through tonal grouping and labeling rather than a new visual
    language.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/components/authoring/lesson-authoring-workspace.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/authoring/lesson-authoring-workspace.tsx` contains `内置教学环节`.
    - `src/components/authoring/lesson-authoring-workspace.tsx` contains all five names `教师讲授`、`问卷调查`、`学生探究`、`课堂测验`、`评价`.
    - Built-in teaching-step actions are rendered in the same first-level action zone as base step buttons.
    - No modal-only or second-chooser-only insertion path is introduced for built-in teaching steps.
  </acceptance_criteria>
  <done>Teachers can add first-party teaching steps directly from the authoring surface with the same quick-add rhythm as base steps.</done>
</task>

<task type="auto">
  <name>Task 3: Add a dedicated Phase 12 verifier and wire it into project scripts</name>
  <read_first>
    - `package.json`
    - `scripts/verify-phase11-readiness.ts`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-RESEARCH.md`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-01-PLAN.md`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-02-PLAN.md`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-03-PLAN.md`
  </read_first>
  <files>scripts/verify-phase12-launch-and-builtins.ts, package.json</files>
  <action>
    Create `scripts/verify-phase12-launch-and-builtins.ts` following the existing
    verifier style. Add static checks that fail if the dedicated launch route is
    missing, if teacher CTAs still do not route to `/teacher/launch`, if launch
    preview files or markers are missing, if built-in plugin names are absent
    from seed/bootstrap or authoring source, if built-in labels `系统内置` and
    `默认开启` are absent from management UI, or if plugin runtime files contain
    unsafe patterns such as `eval(`. Add `verify:phase12` to `package.json` with
    value `tsx scripts/verify-phase12-launch-and-builtins.ts`.
  </action>
  <verify>
    <automated>pnpm exec tsx scripts/verify-phase12-launch-and-builtins.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-phase12-launch-and-builtins.ts` exists.
    - `scripts/verify-phase12-launch-and-builtins.ts` checks for `/teacher/launch`, `内置教学环节`, `系统内置`, `默认开启`, and the five built-in names.
    - `package.json` contains `verify:phase12` with value `tsx scripts/verify-phase12-launch-and-builtins.ts`.
    - `pnpm exec tsx scripts/verify-phase12-launch-and-builtins.ts` exits 0 after implementation.
  </acceptance_criteria>
  <done>Phase 12 has a repeatable regression gate for launch-flow and built-in teaching-step coverage.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| plugin registry allowlist -> runtime behavior | Any new first-party action must remain explicit and typed. |
| authoring UI -> lesson-step persistence | Quick-add built-in actions must still create valid payloads through server-backed paths. |
| verifier script -> release confidence | The script must catch missing launch/built-in integration without accepting unsafe shortcuts. |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | E | plugin action contract | mitigate | Keep built-in additions in explicit allowlists and permission maps only. |
| T-12-04-02 | T | authoring quick-add | mitigate | Use existing validated add-step paths or a minimal DTO-backed extension, not ad hoc client mutations. |
| T-12-04-03 | I | plugin runtime | mitigate | Forbid `eval`, remote imports, and raw HTML rendering in built-in runtime paths. |
| T-12-04-04 | R | verifier | mitigate | Check concrete route names, copy markers, built-in labels, and unsafe-code absence patterns. |
</threat_model>

<verification>
- `pnpm typecheck`
- `pnpm exec eslint src/server/plugins/registry.ts src/lib/dto/resource-ai.ts src/lib/dal/plugins.ts src/components/authoring/lesson-authoring-workspace.tsx src/components/plugins/plugin-renderer.tsx src/components/plugins/widgets/index.tsx`
- `pnpm exec tsx scripts/verify-phase12-launch-and-builtins.ts`
</verification>

<success_criteria>
- Built-in plugin behavior stays inside the safe registry and allowlisted action model.
- Authoring exposes a direct `内置教学环节` group with all five first-party step types.
- Phase 12 gains a dedicated verifier that protects launch-routing and built-in integration regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-04-SUMMARY.md`.
</output>
