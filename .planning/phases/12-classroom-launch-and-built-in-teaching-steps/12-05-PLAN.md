---
phase: 12-classroom-launch-and-built-in-teaching-steps
plan: 05
type: execute
wave: 2
depends_on:
  - 12-01
  - 12-02
files_modified:
  - src/lib/dal/classroom.ts
  - src/components/classroom/classroom-launch-panel.tsx
  - src/lib/dal/classroom.test.ts
  - src/components/classroom/classroom-launch-panel.test.tsx
autonomous: true
gap_closure: true
requirements:
  - CLASS-01
  - CLASS-07
requirements_addressed:
  - CLASS-01
  - CLASS-07
must_haves:
  truths:
    - "D-01 / D-03 / D-13: 教师在 `/teacher/launch` 只能看到自己 school scope 内可开课的已发布课时和班级，且不破坏 dedicated launch 主路径。"
    - "D-02 / D-13: 新建课堂成功后会进入精确的 `/classroom?sessionId=...` 运行台，而不是裸 `/classroom`。"
  artifacts:
    - path: "src/lib/dal/classroom.ts"
      provides: "teacher-scoped launch DTO 和精确 session handoff 数据"
    - path: "src/components/classroom/classroom-launch-panel.tsx"
      provides: "使用返回 sessionId 的 launch 成功跳转"
    - path: "src/components/classroom/classroom-launch-panel.test.tsx"
      provides: "launch redirect behavior regression test"
  key_links:
    - from: "src/lib/dal/classroom.ts"
      to: "src/app/(teacher)/teacher/launch/page.tsx"
      via: "getClassroomConsoleDTO() teacher-scoped published lessons"
      pattern: "scope\.schoolIds"
    - from: "src/components/classroom/classroom-launch-panel.tsx"
      to: "/classroom?sessionId="
      via: "launchClassroomSessionAction success result"
      pattern: "sessionId"
---

<objective>
Close the launch-flow blockers by filtering launchable content to the teacher's
school scope and routing successful launches to the exact live classroom
session.

Purpose: Phase 12 currently leaks cross-school launch options and can send a
teacher back to the wrong live classroom after creating a new session.
Output: scoped launch DTO data, exact session-aware redirect logic, and
behavior-level tests for both regressions.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-CONTEXT.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-RESEARCH.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-VERIFICATION.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-01-SUMMARY.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-02-SUMMARY.md

<interfaces>
From src/actions/classroom-actions.ts:
```ts
export async function launchClassroomSessionAction(
  input: FormData | Record<string, unknown>
): Promise<{ ok: true; data: { sessionId?: string } } | { ok: false; error: string; message: string }>;
```

From src/lib/dto/classroom.ts:
```ts
export const ClassroomConsoleDTOSchema = z.object({
  liveSessions: z.array(ClassroomLiveSessionSummaryDTOSchema),
  publishedLessons: z.array(ClassroomLaunchLessonOptionDTOSchema),
  emptyStateCopy: z.string(),
  launchPreviewEmptyState: ClassroomLaunchPreviewEmptyStateDTOSchema,
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scope launchable classroom data and use the returned session id for runtime handoff</name>
  <read_first>
    - `src/lib/dal/classroom.ts`
    - `src/lib/dto/classroom.ts`
    - `src/actions/classroom-actions.ts`
    - `src/components/classroom/classroom-launch-panel.tsx`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-CONTEXT.md`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-VERIFICATION.md`
  </read_first>
  <files>src/lib/dal/classroom.ts, src/components/classroom/classroom-launch-panel.tsx</files>
  <action>
    In `src/lib/dal/classroom.ts`, fix the launch DTO per D-01, D-02, D-03, and
    D-13 so `getClassroomConsoleDTO()` only returns published lessons, classes,
    and `courseClasses` reachable through `scope.schoolIds`. Import any missing
    schema tables needed to constrain lesson/course ownership, and ensure the
    final `publishedLessons` array excludes out-of-scope classes instead of
    filtering only at the UI layer. In
    `src/components/classroom/classroom-launch-panel.tsx`, read the successful
    `launchClassroomSessionAction()` result payload, extract `sessionId`, and
    route to `/classroom?sessionId=${sessionId}`. Keep `successHref` only as a
    fallback when it already carries an explicit session target or when no
    `sessionId` is returned; do not keep the current unconditional bare
    `/classroom` handoff.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/lib/dal/classroom.ts src/components/classroom/classroom-launch-panel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/dal/classroom.ts` filters launchable lessons/classes using `scope.schoolIds`.
    - Out-of-scope classes are not included in `publishedLessons[].classes`.
    - `src/components/classroom/classroom-launch-panel.tsx` reads a returned `sessionId` on success.
    - Successful launch navigation targets `/classroom?sessionId=` instead of a bare `/classroom` string.
  </acceptance_criteria>
  <done>Launch options are school-scoped and a newly created classroom always opens the exact live runtime session.</done>
</task>

<task type="auto">
  <name>Task 2: Replace string checks with behavior tests for launch scoping and redirect</name>
  <read_first>
    - `src/lib/dal/classroom.test.ts`
    - `src/components/classroom/classroom-launch-panel.tsx`
    - `src/actions/classroom-actions.ts`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-VERIFICATION.md`
  </read_first>
  <files>src/lib/dal/classroom.test.ts, src/components/classroom/classroom-launch-panel.test.tsx</files>
  <action>
    Update `src/lib/dal/classroom.test.ts` so it stops proving correctness via
    source-string matching and instead mocks teacher scope plus the Drizzle query
    layer, then asserts `getClassroomConsoleDTO()` only returns lesson/class data
    inside the active teacher's `schoolIds`. Add a new
    `src/components/classroom/classroom-launch-panel.test.tsx` React Testing
    Library spec that mocks `launchClassroomSessionAction()` and Next router,
    selects a lesson and class, clicks `开启新课堂`, and proves the component calls
    `router.push('/classroom?sessionId=<returned-id>')`. Keep these tests focused
    on the failed truths from VERIFICATION.md, not generic rendering snapshots.
  </action>
  <verify>
    <automated>pnpm test -- src/lib/dal/classroom.test.ts src/components/classroom/classroom-launch-panel.test.tsx && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/dal/classroom.test.ts` calls `getClassroomConsoleDTO()` and asserts out-of-scope data is absent.
    - `src/components/classroom/classroom-launch-panel.test.tsx` exists.
    - The new component test asserts a `/classroom?sessionId=` navigation target.
    - No new source-string-only assertion is used as the primary proof for these two regressions.
  </acceptance_criteria>
  <done>The two launch blockers have executable regression coverage that fails if scoping or redirect behavior regresses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teacher scope -> launch DTO | Untrusted or over-broad query results can expose out-of-scope lesson/class metadata. |
| launch action result -> runtime route | Wrong session handoff can strand the teacher in the wrong live classroom. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | I | `getClassroomConsoleDTO()` | mitigate | Filter launchable lesson/class data with `scope.schoolIds` before DTO shaping. |
| T-12-05-02 | T | `ClassroomLaunchPanel` redirect | mitigate | Use returned `sessionId` from `launchClassroomSessionAction()` and route to `/classroom?sessionId=...`. |
| T-12-05-03 | R | launch regression tests | mitigate | Add behavior tests that fail on cross-school data exposure and wrong runtime handoff. |
</threat_model>

<verification>
- `pnpm typecheck`
- `pnpm exec eslint src/lib/dal/classroom.ts src/components/classroom/classroom-launch-panel.tsx`
- `pnpm test -- src/lib/dal/classroom.test.ts src/components/classroom/classroom-launch-panel.test.tsx`
</verification>

<success_criteria>
- Teachers only see launchable lessons and classes within their school scope.
- New classroom creation returns teachers to the exact live session they just launched.
- Both regressions fail automatically if future changes remove scoping or session-aware routing.
</success_criteria>

<output>
After completion, create `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-05-SUMMARY.md`
</output>
