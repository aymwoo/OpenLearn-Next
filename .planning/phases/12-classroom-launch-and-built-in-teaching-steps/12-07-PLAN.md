---
phase: 12-classroom-launch-and-built-in-teaching-steps
plan: 07
type: execute
wave: 4
depends_on:
  - 12-04
  - 12-06
files_modified:
  - src/app/(teacher)/teacher/editor/page.tsx
  - src/components/surfaces/lesson-editor-surface.tsx
  - src/components/authoring/lesson-authoring-workspace.tsx
autonomous: true
gap_closure: true
requirements:
  - LESSON-03
  - PLUGIN-05
requirements_addressed:
  - LESSON-03
  - PLUGIN-05
must_haves:
  truths:
    - "D-07 / D-08 / D-09 / D-12: 作者页 `内置教学环节` 分组只展示当前学校已启用的 built-in plugins。"
    - "D-10 / D-12: 停用内置插件后，教师不能继续从 quick-add 插入对应 built-in step。"
  artifacts:
    - path: "src/app/(teacher)/teacher/editor/page.tsx"
      provides: "school-scoped built-in template data for the authoring route"
    - path: "src/components/authoring/lesson-authoring-workspace.tsx"
      provides: "registry-backed built-in quick-add UI"
  key_links:
    - from: "src/app/(teacher)/teacher/editor/page.tsx"
      to: "src/lib/dal/plugins.ts"
      via: "listBuiltInTeachingStepTemplates()"
      pattern: "listBuiltInTeachingStepTemplates"
    - from: "src/components/authoring/lesson-authoring-workspace.tsx"
      to: "addLessonStepAction"
      via: "built-in template initialPayload insertion"
      pattern: "initialPayload"
---

<objective>
Rewire the authoring quick-add surface so built-in teaching steps come from
enabled school-scoped plugin templates instead of hard-coded system constants.

Purpose: Phase 12 currently violates its own built-in plugin contract because a
disabled built-in can still be inserted from the authoring UI.
Output: teacher editor data plumbing for built-in templates and a registry-backed
`内置教学环节` quick-add group.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-CONTEXT.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-UI-SPEC.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-VERIFICATION.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-04-SUMMARY.md
@src/lib/dal/plugins.ts

<interfaces>
From src/lib/dal/plugins.ts:
```ts
export async function listBuiltInTeachingStepTemplates(input: { actorId: string; schoolId: string })
```

From src/components/authoring/lesson-authoring-workspace.tsx:
```ts
async function addBuiltInStep(definition: {
  stepType: "content" | "task" | "quiz";
  initialTitle: string;
  initialPayload: unknown;
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load enabled built-in templates at the teacher editor route boundary</name>
  <read_first>
    - `src/app/(teacher)/teacher/editor/page.tsx`
    - `src/components/surfaces/lesson-editor-surface.tsx`
    - `src/lib/dal/plugins.ts`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-VERIFICATION.md`
  </read_first>
  <files>src/app/(teacher)/teacher/editor/page.tsx, src/components/surfaces/lesson-editor-surface.tsx</files>
  <action>
    Update the teacher editor route per D-07, D-08, D-10, and D-12 so it loads
    built-in teaching-step templates with `listBuiltInTeachingStepTemplates({
    actorId: scope.userId, schoolId: lesson.course.schoolId })` when a lesson is
    active, then passes that array through `LessonEditorSurface` into
    `LessonAuthoringWorkspace`. Use an empty array when there is no active lesson.
    Keep this data flow server-side and school-scoped; do not fetch built-in
    templates from the client or reintroduce a hard-coded fallback in the route.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/app/(teacher)/teacher/editor/page.tsx src/components/surfaces/lesson-editor-surface.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(teacher)/teacher/editor/page.tsx` imports and calls `listBuiltInTeachingStepTemplates`.
    - `LessonEditorSurface` receives a `builtInTemplates` prop or equivalent typed data path.
    - The no-lesson code path passes an empty built-in template array.
  </acceptance_criteria>
  <done>The authoring page receives built-in teaching-step data from the school-scoped registry path instead of reconstructing it locally.</done>
</task>

<task type="auto">
  <name>Task 2: Render only enabled built-in teaching steps in the first-level quick-add group</name>
  <read_first>
    - `src/components/authoring/lesson-authoring-workspace.tsx`
    - `src/actions/lesson-authoring-actions.ts`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-UI-SPEC.md`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-VERIFICATION.md`
  </read_first>
  <files>src/components/authoring/lesson-authoring-workspace.tsx</files>
  <action>
    Refactor `LessonAuthoringWorkspace` per D-07, D-08, D-09, and D-12 so the
    `内置教学环节` group is rendered from the injected `builtInTemplates` prop
    rather than from `BUILT_IN_TEACHING_STEP_DEFINITIONS` and the fixed
    `builtInTeachingStepButtonOrder`. Preserve the first-level quick-add rhythm:
    base schema buttons (`新增内容` / `新增任务` / `新增测验`) stay untouched, and
    built-in buttons remain direct actions in the same action zone. Each
    built-in button must insert `template.stepType`, `template.initialTitle`, and
    `template.initialPayload` through `addLessonStepAction()`. If a built-in is
    disabled and therefore absent from `builtInTemplates`, do not render its
    button and do not allow insertion through a hidden fallback branch.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/components/authoring/lesson-authoring-workspace.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/authoring/lesson-authoring-workspace.tsx` no longer imports `BUILT_IN_TEACHING_STEP_DEFINITIONS` for the quick-add button list.
    - The `内置教学环节` group renders from a passed-in template array.
    - Built-in insertion still goes through `addLessonStepAction()` with `initialTitle` and `initialPayload`.
    - Missing templates do not render hidden or fallback built-in buttons.
  </acceptance_criteria>
  <done>Authoring quick-add stays fast, but it now respects plugin enable/disable state and registry truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enabled built-in plugin list -> authoring UI | Out-of-date UI constants can bypass admin toggles and expose disabled system steps. |
| authoring quick-add -> step persistence | Inserted built-in templates must still use the validated server-backed add-step path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07-01 | E | `LessonAuthoringWorkspace` | mitigate | Render built-in quick-add buttons only from `listBuiltInTeachingStepTemplates()` results. |
| T-12-07-02 | T | built-in insertion path | mitigate | Reuse `addLessonStepAction()` with typed template payloads; do not introduce client-only mutations. |
| T-12-07-03 | R | school-scoped template loading | mitigate | Load built-in templates on the authenticated teacher route boundary using `scope.userId` and `lesson.course.schoolId`. |
</threat_model>

<verification>
- `pnpm typecheck`
- `pnpm exec eslint src/app/(teacher)/teacher/editor/page.tsx src/components/surfaces/lesson-editor-surface.tsx src/components/authoring/lesson-authoring-workspace.tsx`
</verification>

<success_criteria>
- The built-in authoring group only shows enabled built-ins for the active school.
- Disabled built-ins disappear from quick-add without breaking base lesson-step creation.
- All built-in insertions still use the validated lesson step persistence path.
</success_criteria>

<output>
After completion, create `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-07-SUMMARY.md`
</output>
