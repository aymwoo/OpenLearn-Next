---
phase: 12-classroom-launch-and-built-in-teaching-steps
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - scripts/bootstrap-dev-db.ts
  - src/lib/dto/resource-ai.ts
  - src/lib/dal/plugins.ts
  - src/actions/plugin-actions.ts
  - src/components/surfaces/settings-surface.tsx
  - src/app/settings/labs/page.tsx
autonomous: true
requirements:
  - PLUGIN-04
  - PLUGIN-05
requirements_addressed:
  - PLUGIN-04
  - PLUGIN-05
must_haves:
  truths:
    - "D-10: 系统内置教学环节插件作为真实 registry 记录存在，默认启用、可停用、但不可删除。"
    - "D-11: Built-in plugin cards clearly expose `系统内置` and `默认开启` metadata in management UI."
    - "D-12: Built-in plugins still flow through the safe registry enable/disable chain instead of UI-only constants."
  artifacts:
    - path: "scripts/bootstrap-dev-db.ts"
      provides: "Built-in first-party plugin seed records"
    - path: "src/lib/dto/resource-ai.ts"
      provides: "Built-in plugin metadata contract"
    - path: "src/components/surfaces/settings-surface.tsx"
      provides: "Labs/plugin management built-in labels and non-delete framing"
  key_links:
    - from: "scripts/bootstrap-dev-db.ts"
      to: "src/lib/dto/resource-ai.ts"
      via: "seeded manifest shape and built-in metadata stay schema-valid"
      pattern: "PluginManifestSchema"
    - from: "src/lib/dal/plugins.ts"
      to: "src/components/surfaces/settings-surface.tsx"
      via: "built-in flags flow through list/get DTOs to labs UI"
      pattern: "builtIn"
---

<objective>
Seed built-in first-party teaching-step plugins, expose built-in metadata through
the safe registry, and label them clearly in plugin management surfaces.

Purpose: The built-in teaching steps must be real system-provided plugins, not
just UI shortcuts, so management surfaces and authoring stay in sync.
Output: seed data for five built-in plugins, DTO/DAL support for built-in
metadata, and labs/plugin-management UI that distinguishes built-ins cleanly.
</objective>

<execution_context>
@/home/wuxf/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-CONTEXT.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-RESEARCH.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-UI-SPEC.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-PATTERNS.md
@src/server/plugins/registry.ts
@docs/plugin-system-review.md
@scripts/bootstrap-dev-db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add schema and DAL support for built-in plugin metadata and delete guard</name>
  <read_first>
    - `src/lib/dto/resource-ai.ts`
    - `src/lib/dal/plugins.ts`
    - `src/actions/plugin-actions.ts`
    - `src/server/plugins/registry.ts`
    - `docs/plugin-system-review.md`
  </read_first>
  <files>src/lib/dto/resource-ai.ts, src/lib/dal/plugins.ts, src/actions/plugin-actions.ts</files>
  <action>
    Extend the plugin manifest or plugin registration DTOs with explicit built-in
    metadata required by Phase 12, such as `builtIn`, `defaultEnabled`, and
    `nonDeletable`, using the smallest schema extension that keeps current plugin
    contracts intact. Flow those fields through `registerPluginManifest`,
    `listPluginsForSchool`, and `getPluginForSchool`. Update the delete path so
    built-in plugins return a stable error like `PLUGIN_BUILT_IN_NOT_DELETABLE`
    instead of deleting the row. Preserve enable/disable toggles for built-ins.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/lib/dto/resource-ai.ts src/lib/dal/plugins.ts src/actions/plugin-actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/dto/resource-ai.ts` contains built-in plugin metadata fields.
    - `src/lib/dal/plugins.ts` contains a delete guard for built-in plugins.
    - `src/lib/dal/plugins.ts` still exposes enable/disable behavior for built-in plugins.
    - `src/actions/plugin-actions.ts` surfaces the delete-guard error without bypassing DAL logic.
  </acceptance_criteria>
  <done>Built-in plugin truth is registry-backed, typed, and protected from accidental deletion.</done>
</task>

<task type="auto">
  <name>Task 2: Seed the five built-in teaching-step plugins as default-enabled records</name>
  <read_first>
    - `scripts/bootstrap-dev-db.ts`
    - `src/lib/dto/resource-ai.ts`
    - `src/server/plugins/registry.ts`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-CONTEXT.md`
  </read_first>
  <files>scripts/bootstrap-dev-db.ts</files>
  <action>
    Extend `scripts/bootstrap-dev-db.ts` so dev bootstrap creates or upserts five
    built-in plugin registrations for `教师讲授`、`问卷调查`、`学生探究`、`课堂测验`、`评价`.
    Each seed record must use a valid declarative manifest, be marked built-in,
    start `enabled: true`, and carry explicit metadata for `defaultEnabled` and
    `nonDeletable`. Keep the manifest/actions within the existing safe plugin
    system or the explicitly added allowlisted first-party actions from Plan
    12-04; do not seed arbitrary remote code or unsupported anchors.
  </action>
  <verify>
    <automated>pnpm run db:bootstrap:dev</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/bootstrap-dev-db.ts` contains all five built-in names `教师讲授`、`问卷调查`、`学生探究`、`课堂测验`、`评价`.
    - Seeded built-in plugins are written as enabled records.
    - Seeded built-in plugins include built-in metadata and do not depend on arbitrary code execution.
    - `pnpm run db:bootstrap:dev` exits 0.
  </acceptance_criteria>
  <done>Dev bootstrap reliably creates the first-party built-in teaching-step plugin set in the registry.</done>
</task>

<task type="auto">
  <name>Task 3: Label built-in plugins clearly in labs or marketplace management surfaces</name>
  <read_first>
    - `src/components/surfaces/settings-surface.tsx`
    - `src/app/settings/labs/page.tsx`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-UI-SPEC.md`
    - `DESIGN.md`
  </read_first>
  <files>src/components/surfaces/settings-surface.tsx, src/app/settings/labs/page.tsx</files>
  <action>
    Update the plugin-management section in `SettingsSurface` so built-in plugin
    cards show `系统内置` and `默认开启` before secondary metadata. Frame the
    main control as a runtime toggle (`停用环节` / `重新启用`) instead of an
    ownership action. Do not render delete controls for built-in plugins. If the
    current route is labs-only and no separate marketplace route exists yet,
    use the existing labs/plugin-management surface as the Phase 12 management
    destination rather than creating a speculative marketplace feature.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/components/surfaces/settings-surface.tsx src/app/settings/labs/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/surfaces/settings-surface.tsx` contains `系统内置`.
    - `src/components/surfaces/settings-surface.tsx` contains `默认开启`.
    - Built-in plugin cards do not render a delete action.
    - Toggle copy is framed as enable/disable behavior, not ownership transfer or deletion.
  </acceptance_criteria>
  <done>Teachers and admins can immediately distinguish built-in plugins from ordinary removable extensions.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| seed script -> plugin registry | Incorrect seed data can create invalid or unsafe built-in plugin records. |
| DAL metadata -> management UI | UI must not guess built-in state; it must render typed registry metadata. |
| delete action -> built-in records | Deletion logic must not remove required system plugins. |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | T | seed data | mitigate | Seed only schema-valid declarative manifests and explicit built-in metadata. |
| T-12-03-02 | E | plugin deletion | mitigate | Guard built-in plugins in DAL and surface a stable non-deletable error. |
| T-12-03-03 | I | management UI | mitigate | Render built-in/default-enabled labels from DTO fields, not inferred naming conventions. |
| T-12-03-04 | D | plugin manager | mitigate | Reuse the existing labs/settings management surface instead of creating a speculative marketplace architecture. |
</threat_model>

<verification>
- `pnpm typecheck`
- `pnpm exec eslint src/lib/dto/resource-ai.ts src/lib/dal/plugins.ts src/actions/plugin-actions.ts src/components/surfaces/settings-surface.tsx src/app/settings/labs/page.tsx`
- `pnpm run db:bootstrap:dev`
</verification>

<success_criteria>
- The five built-in teaching-step plugins exist as real seeded registry records.
- Built-in metadata is typed, flows through DAL/Actions, and prevents deletion.
- Management UI clearly labels built-ins and default-enabled state.
</success_criteria>

<output>
After completion, create `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-03-SUMMARY.md`.
</output>
