---
phase: 12-classroom-launch-and-built-in-teaching-steps
plan: 08
type: execute
wave: 3
depends_on:
  - 12-03
files_modified:
  - src/app/settings/plugins/page.tsx
  - src/components/surfaces/plugin-marketplace-surface.tsx
  - src/components/surfaces/settings-surface.tsx
  - src/components/surfaces/settings-surface.test.tsx
autonomous: true
gap_closure: true
requirements:
  - PLUGIN-05
requirements_addressed:
  - PLUGIN-05
must_haves:
  truths:
    - "D-11: Built-in teaching-step plugins 在真实的 plugin marketplace surface 中可见。"
    - "D-10 / D-11 / D-15: Marketplace cards 明确显示 `系统内置`、`默认开启`，并保留 enable/disable 语义而不是删除语义。"
  artifacts:
    - path: "src/app/settings/plugins/page.tsx"
      provides: "plugin marketplace route"
    - path: "src/components/surfaces/plugin-marketplace-surface.tsx"
      provides: "built-in plugin marketplace surface"
  key_links:
    - from: "src/app/settings/plugins/page.tsx"
      to: "src/components/surfaces/plugin-marketplace-surface.tsx"
      via: "dedicated marketplace route assembly"
      pattern: "PluginMarketplaceSurface"
    - from: "src/components/surfaces/settings-surface.tsx"
      to: "/settings/plugins"
      via: "settings entry link"
      pattern: "/settings/plugins"
---

<objective>
Add the missing plugin marketplace surface so built-in teaching-step plugins are
visible in a dedicated discovery page instead of only inside labs management.

Purpose: The roadmap contract explicitly calls for plugin marketplace visibility,
and the current labs-only implementation is not an accepted equivalent.
Output: a dedicated plugin marketplace route and surface plus settings navigation
into it.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-CONTEXT.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-UI-SPEC.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-VERIFICATION.md
@src/components/surfaces/settings-surface.tsx
@docs/plugin-system-review.md

<interfaces>
From src/components/surfaces/settings-surface.tsx:
```ts
export async function SettingsSurface({ mode }: { mode: 'general' | 'labs' })
```

From src/actions/plugin-actions.ts:
```ts
export async function listPluginsAction(data: { schoolId: string })
export async function setPluginEnabledAction(data: { pluginId: string; schoolId: string; enabled: boolean })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a dedicated built-in plugin marketplace route and surface</name>
  <read_first>
    - `src/app/settings/labs/page.tsx`
    - `src/components/surfaces/settings-surface.tsx`
    - `src/actions/plugin-actions.ts`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-UI-SPEC.md`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-VERIFICATION.md`
  </read_first>
  <files>src/app/settings/plugins/page.tsx, src/components/surfaces/plugin-marketplace-surface.tsx</files>
  <action>
    Create `src/app/settings/plugins/page.tsx` as a dedicated settings route that
    renders a new `PluginMarketplaceSurface`. Create
    `src/components/surfaces/plugin-marketplace-surface.tsx` to present built-in
    teaching-step plugins as a marketplace-style catalog per D-11 and D-15: a
    calm tonal page with one main stage, built-in cards showing `系统内置` and
    `默认开启` before secondary metadata, and enable/disable controls framed as
    runtime toggles rather than ownership or deletion actions. Reuse the current
    safe registry data model and local actions only. Do not implement external
    marketplace sync, third-party discovery, or arbitrary plugin code loading.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/app/settings/plugins/page.tsx src/components/surfaces/plugin-marketplace-surface.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/settings/plugins/page.tsx` exists.
    - `src/app/settings/plugins/page.tsx` renders `PluginMarketplaceSurface`.
    - `src/components/surfaces/plugin-marketplace-surface.tsx` contains `系统内置` and `默认开启`.
    - The marketplace surface does not render delete actions for built-in plugins.
  </acceptance_criteria>
  <done>Phase 12 now has a real plugin marketplace surface where built-in teaching-step plugins are visible and clearly framed as system-provided.</done>
</task>

<task type="auto">
  <name>Task 2: Link settings into the new marketplace route and cover the new entry</name>
  <read_first>
    - `src/components/surfaces/settings-surface.tsx`
    - `src/components/surfaces/settings-surface.test.tsx`
    - `src/app/settings/plugins/page.tsx`
  </read_first>
  <files>src/components/surfaces/settings-surface.tsx, src/components/surfaces/settings-surface.test.tsx</files>
  <action>
    Add a clear settings entry to `/settings/plugins` from the existing settings
    shell so the marketplace is discoverable without going through labs. Update
    `src/components/surfaces/settings-surface.test.tsx` to assert the new
    marketplace route is linked from settings and that the settings surface still
    advertises plugin-management affordances. Keep labs as the operational
    management surface, but use the new marketplace route as the visibility
    surface required by the roadmap.
  </action>
  <verify>
    <automated>pnpm test -- src/components/surfaces/settings-surface.test.tsx && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/surfaces/settings-surface.tsx` contains a `/settings/plugins` link.
    - `src/components/surfaces/settings-surface.test.tsx` asserts the marketplace entry path.
    - Labs plugin management remains available after the new marketplace entry is added.
  </acceptance_criteria>
  <done>The plugin marketplace route is discoverable from settings and protected by an automated regression test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| marketplace route -> plugin registry data | The new visibility surface must not bypass the existing safe plugin data and toggle boundaries. |
| settings navigation -> marketplace route | A missing or misleading entry can leave the new marketplace unreachable even if the route exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08-01 | T | `PluginMarketplaceSurface` | mitigate | Reuse the current safe registry/listing and toggle actions only; no external marketplace integration. |
| T-12-08-02 | I | built-in marketplace cards | mitigate | Render `系统内置` and `默认开启` from typed plugin metadata, not guessed UI labels. |
| T-12-08-03 | R | settings navigation | mitigate | Add a test that proves the `/settings/plugins` entry exists and remains discoverable. |
</threat_model>

<verification>
- `pnpm typecheck`
- `pnpm exec eslint src/app/settings/plugins/page.tsx src/components/surfaces/plugin-marketplace-surface.tsx src/components/surfaces/settings-surface.tsx`
- `pnpm test -- src/components/surfaces/settings-surface.test.tsx`
</verification>

<success_criteria>
- Built-in teaching-step plugins are visible on a dedicated marketplace route.
- The marketplace route is reachable from settings without replacing labs management.
- Built-in marketplace cards keep system labels and non-delete semantics intact.
</success_criteria>

<output>
After completion, create `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-08-SUMMARY.md`
</output>
