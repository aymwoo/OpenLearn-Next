---
phase: 12-classroom-launch-and-built-in-teaching-steps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(teacher)/teacher/launch/page.tsx
  - src/components/surfaces/classroom-launch-surface.tsx
  - src/components/classroom/classroom-launch-panel.tsx
  - src/components/shell/sidebar.tsx
  - src/app/(teacher)/teacher/layout.tsx
  - src/app/(classroom)/classroom/page.tsx
  - src/lib/dal/classroom.ts
autonomous: true
requirements:
  - CLASS-01
  - CLASS-06
  - CLASS-07
requirements_addressed:
  - CLASS-01
  - CLASS-06
  - CLASS-07
must_haves:
  truths:
    - "D-01: 教师可以从专用的 `开启新课堂` 页面新建课堂，而不是只能在 `/classroom` 内嵌启动。"
    - "D-02 / D-03: 若存在 live classroom，页面会以次级恢复卡片提供继续上课入口，且现有入口统一收敛到 dedicated launch surface。"
    - "D-13: 原有运行台 `/classroom` 继续负责 active session 控制，不被新页面复制或替代。"
  artifacts:
    - path: "src/app/(teacher)/teacher/launch/page.tsx"
      provides: "教师专用课堂启动入口路由"
    - path: "src/components/surfaces/classroom-launch-surface.tsx"
      provides: "独立开课准备页与恢复区布局"
    - path: "src/components/shell/sidebar.tsx"
      provides: "侧边栏 `开启新课堂` 入口路由到新页面"
    - path: "src/app/(teacher)/teacher/layout.tsx"
      provides: "教师头部主 CTA 路由到新页面"
  key_links:
    - from: "src/app/(teacher)/teacher/launch/page.tsx"
      to: "src/lib/dal/classroom.ts"
      via: "服务器端读取开课准备 DTO 与 live session 恢复数据"
      pattern: "getClassroomConsoleDTO"
    - from: "src/components/surfaces/classroom-launch-surface.tsx"
      to: "src/components/classroom/classroom-launch-panel.tsx"
      via: "组合开课表单与恢复信息"
      pattern: "ClassroomLaunchPanel"
---

<objective>
Create the dedicated teacher classroom launch route and re-route existing
teacher launch CTAs through it without rewriting the active classroom runtime.

Purpose: Phase 12 needs a real preparation surface that privileges new launch
while still offering recovery for live sessions.
Output: a new teacher launch page, updated teacher CTA links, and a secondary
resume affordance that hands teachers back to `/classroom`.
</objective>

<execution_context>
@/home/wuxf/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-CONTEXT.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-RESEARCH.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-UI-SPEC.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-PATTERNS.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the dedicated teacher launch route and surface</name>
  <read_first>
    - `src/app/(classroom)/classroom/page.tsx`
    - `src/components/surfaces/classroom-console-surface.tsx`
    - `src/components/classroom/classroom-launch-panel.tsx`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-UI-SPEC.md`
    - `DESIGN.md`
  </read_first>
  <files>src/app/(teacher)/teacher/launch/page.tsx, src/components/surfaces/classroom-launch-surface.tsx, src/components/classroom/classroom-launch-panel.tsx, src/lib/dal/classroom.ts</files>
  <action>
    Create `src/app/(teacher)/teacher/launch/page.tsx` as an async teacher route
    that calls the existing teacher-scoped classroom DAL and renders a new
    `ClassroomLaunchSurface`. Create `src/components/surfaces/classroom-launch-surface.tsx`
    using the existing classroom hero language: one gradient top stage with page
    title, launch metrics, and a primary launch column plus a lower-emphasis
    resume or tips column. Extend the data returned from `getClassroomConsoleDTO()`
    only enough to support launch-page rendering of `liveSessions` resume data
    such as session id, lesson title, class name, updated time, lock state, and
    version. Keep `ClassroomLaunchPanel` as the launch form primitive, but make
    it reusable inside the new dedicated surface rather than the old combined
    console page.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/app/(teacher)/teacher/launch/page.tsx src/components/surfaces/classroom-launch-surface.tsx src/components/classroom/classroom-launch-panel.tsx src/lib/dal/classroom.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(teacher)/teacher/launch/page.tsx` exists.
    - `src/app/(teacher)/teacher/launch/page.tsx` contains `ClassroomLaunchSurface`.
    - `src/components/surfaces/classroom-launch-surface.tsx` exists.
    - `src/components/surfaces/classroom-launch-surface.tsx` contains `开启新课堂`.
    - `src/components/surfaces/classroom-launch-surface.tsx` contains resume/live-session copy that is visually secondary to the new-launch section.
    - `src/lib/dal/classroom.ts` returns enough live-session data for a resume card without duplicating snapshot control logic.
  </acceptance_criteria>
  <done>Teachers have a dedicated launch-preparation page that keeps new launch primary and live-session recovery secondary.</done>
</task>

<task type="auto">
  <name>Task 2: Re-route existing teacher launch CTAs to the new page while keeping `/classroom` as the active runtime</name>
  <read_first>
    - `src/components/shell/sidebar.tsx`
    - `src/app/(teacher)/teacher/layout.tsx`
    - `src/app/(classroom)/classroom/page.tsx`
    - `src/components/surfaces/classroom-console-surface.tsx`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-CONTEXT.md`
  </read_first>
  <files>src/components/shell/sidebar.tsx, src/app/(teacher)/teacher/layout.tsx, src/app/(classroom)/classroom/page.tsx, src/components/surfaces/classroom-console-surface.tsx</files>
  <action>
    Update the sidebar `开启新课堂` button and the teacher header CTA so both
    navigate to `/teacher/launch`. Preserve button styling and focus behavior.
    Keep `/classroom` focused on live runtime control: if there is an active
    session, continue to render `ClassroomControlPanel`; if there is no active
    session, downgrade the launch copy so `/classroom` reads as the runtime
    console entry, with a clear link back to the dedicated launch page instead of
    rendering launch prep as the page's primary experience.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm exec eslint src/components/shell/sidebar.tsx src/app/(teacher)/teacher/layout.tsx src/app/(classroom)/classroom/page.tsx src/components/surfaces/classroom-console-surface.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/shell/sidebar.tsx` contains `href="/teacher/launch"` or equivalent navigation to `/teacher/launch`.
    - `src/app/(teacher)/teacher/layout.tsx` contains navigation to `/teacher/launch`.
    - `/classroom` code path still renders `ClassroomControlPanel` for active sessions.
    - `src/components/surfaces/classroom-console-surface.tsx` no longer presents embedded launch prep as the page's main non-live state.
  </acceptance_criteria>
  <done>All teacher launch entry points converge on the dedicated launch route, while `/classroom` remains the active runtime console.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| teacher CTA -> launch route | Wrong routing can strand teachers on the runtime page without the intended preparation flow. |
| dedicated launch page -> classroom runtime | Resume/new-launch UI must not mutate live runtime state outside existing Server Actions. |
| launch surface -> live session data | Resume cards expose classroom metadata and must use teacher-scoped DAL only. |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | S | sidebar/header CTA routing | mitigate | Use fixed internal route targets (`/teacher/launch`) rather than ad hoc client handlers. |
| T-12-01-02 | T | launch surface | mitigate | Reuse existing `launchClassroomSessionAction()` and do not add client-only launch logic. |
| T-12-01-03 | I | resume card | mitigate | Load live session data only from teacher-scoped DAL; expose DTO-safe fields only. |
| T-12-01-04 | D | runtime page | mitigate | Keep `/classroom` focused on the active session console to avoid duplicated control surfaces and drift. |
</threat_model>

<verification>
- `pnpm typecheck`
- `pnpm exec eslint src/app/(teacher)/teacher/launch/page.tsx src/components/surfaces/classroom-launch-surface.tsx src/components/classroom/classroom-launch-panel.tsx src/components/shell/sidebar.tsx src/app/(teacher)/teacher/layout.tsx src/app/(classroom)/classroom/page.tsx src/lib/dal/classroom.ts`
</verification>

<success_criteria>
- Teachers can reach a dedicated launch page from both major `开启新课堂` entry points.
- Resume guidance exists for active classrooms but stays visually secondary.
- `/classroom` continues to own active classroom control and does not become a duplicated launch-prep page.
</success_criteria>

<output>
After completion, create `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-01-SUMMARY.md`.
</output>
