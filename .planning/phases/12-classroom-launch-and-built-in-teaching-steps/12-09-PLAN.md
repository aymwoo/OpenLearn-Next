---
phase: 12-classroom-launch-and-built-in-teaching-steps
plan: 09
type: execute
wave: 5
depends_on:
  - 12-05
  - 12-06
  - 12-07
  - 12-08
files_modified:
  - scripts/verify-phase12-launch-and-builtins.ts
  - src/lib/dal/classroom.test.ts
  - src/components/classroom/classroom-launch-panel.test.tsx
  - src/lib/dal/plugins.builtins.test.ts
  - src/components/authoring/lesson-authoring-workspace.test.tsx
autonomous: true
gap_closure: true
requirements:
  - CLASS-01
  - CLASS-07
  - PLUGIN-04
  - PLUGIN-05
requirements_addressed:
  - CLASS-01
  - CLASS-07
  - PLUGIN-04
  - PLUGIN-05
must_haves:
  truths:
    - "D-13 / D-14: Phase 12 verifier 会在 school scope、session redirect、disabled built-in hidden、seeded built-in hook 失效时自动失败。"
    - "D-15: Phase 12 回归不再主要依赖源码字符串匹配。"
  artifacts:
    - path: "scripts/verify-phase12-launch-and-builtins.ts"
      provides: "behavior-first phase verifier"
    - path: "src/lib/dal/plugins.builtins.test.ts"
      provides: "seeded built-in hook execution regression test"
    - path: "src/components/authoring/lesson-authoring-workspace.test.tsx"
      provides: "disabled built-in visibility regression test"
  key_links:
    - from: "scripts/verify-phase12-launch-and-builtins.ts"
      to: "pnpm test -- ..."
      via: "behavior-level phase regression suite"
      pattern: "execFileSync|spawnSync|test --"
    - from: "src/components/authoring/lesson-authoring-workspace.test.tsx"
      to: "builtInTemplates"
      via: "assert hidden disabled built-ins"
      pattern: "builtInTemplates"
---

<objective>
Replace the shallow Phase 12 verification guard with behavior-level regression
coverage for the exact blockers found in VERIFICATION.md.

Purpose: The current verifier can pass while the phase is broken, so execution
must end with tests and a verifier that actually prove the shipped behavior.
Output: targeted behavior tests plus a `verify:phase12` script that executes the
relevant regression suite and unsafe-pattern checks.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-VERIFICATION.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-05-PLAN.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-06-PLAN.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-07-PLAN.md
@.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-08-PLAN.md
@scripts/verify-phase11-readiness.ts

<interfaces>
Expected behavior specs to lock in:
```text
- school scope: out-of-scope launch options are absent
- launch redirect: successful create routes to /classroom?sessionId=<id>
- disabled built-ins: missing from quick-add when not enabled
- seeded built-in hook: enabled built-in with correct manifest actions returns a template proposal
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add behavior-level regression tests for every failed Phase 12 truth</name>
  <read_first>
    - `src/lib/dal/classroom.test.ts`
    - `src/components/classroom/classroom-launch-panel.test.tsx`
    - `src/components/authoring/lesson-authoring-workspace.tsx`
    - `src/lib/dal/plugins.ts`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-VERIFICATION.md`
  </read_first>
  <files>src/lib/dal/classroom.test.ts, src/components/classroom/classroom-launch-panel.test.tsx, src/lib/dal/plugins.builtins.test.ts, src/components/authoring/lesson-authoring-workspace.test.tsx</files>
  <action>
    Build one focused behavior spec for each failed blocker in VERIFICATION.md.
    Keep `src/lib/dal/classroom.test.ts` on school-scope behavior. Keep
    `src/components/classroom/classroom-launch-panel.test.tsx` on exact
    session-aware redirect behavior. Add `src/lib/dal/plugins.builtins.test.ts`
    that mocks built-in plugin rows and hook dispatch to prove an enabled seed
    with `insertBuiltInTeachingStepTemplate` returns a template proposal, while a
    disabled or action-mismatched built-in returns `null`. Add
    `src/components/authoring/lesson-authoring-workspace.test.tsx` that renders
    the workspace with a partial `builtInTemplates` array and proves only enabled
    built-ins are visible in the `内置教学环节` group. Do not add snapshot-only or
    source-string-only tests for these truths.
  </action>
  <verify>
    <automated>pnpm test -- src/lib/dal/classroom.test.ts src/components/classroom/classroom-launch-panel.test.tsx src/lib/dal/plugins.builtins.test.ts src/components/authoring/lesson-authoring-workspace.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/dal/plugins.builtins.test.ts` exists.
    - `src/components/authoring/lesson-authoring-workspace.test.tsx` exists.
    - The regression suite includes explicit assertions for school scope, session redirect, disabled built-in visibility, and seeded built-in hook execution.
    - None of the new tests use file-source substring matching as their primary proof.
  </acceptance_criteria>
  <done>The failed Phase 12 truths are all covered by executable behavior tests instead of inferred source markers.</done>
</task>

<task type="auto">
  <name>Task 2: Make `verify:phase12` execute the real regression suite and block shallow passes</name>
  <read_first>
    - `scripts/verify-phase12-launch-and-builtins.ts`
    - `package.json`
    - `scripts/verify-phase11-readiness.ts`
    - `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-VERIFICATION.md`
  </read_first>
  <files>scripts/verify-phase12-launch-and-builtins.ts</files>
  <action>
    Rewrite `scripts/verify-phase12-launch-and-builtins.ts` so it stops treating
    string presence as sufficient proof. Keep targeted static unsafe-pattern
    checks for `eval(`, `dangerouslySetInnerHTML`, and similar disallowed tokens,
    but make the script run the Phase 12 behavior suite from Task 1 plus the
    marketplace/settings regression coverage. The script must exit non-zero when
    any targeted Vitest command fails or when required routes/files are missing.
    Preserve the existing `verify:phase12` package script entry; do not rename
    the command.
  </action>
  <verify>
    <automated>pnpm exec tsx scripts/verify-phase12-launch-and-builtins.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-phase12-launch-and-builtins.ts` executes targeted tests instead of only checking source strings.
    - The script still fails on unsafe runtime patterns such as `eval(`.
    - `pnpm exec tsx scripts/verify-phase12-launch-and-builtins.ts` exits 0 only after the Phase 12 behavior suite passes.
  </acceptance_criteria>
  <done>`verify:phase12` becomes a trustworthy release gate for launch routing, built-in visibility, and safe first-party hook behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier script -> release confidence | A shallow verifier can falsely mark a broken phase as complete. |
| regression tests -> built-in/runtime behaviors | Missing behavior tests leave critical launch and plugin regressions undetected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09-01 | R | `verify:phase12` | mitigate | Execute targeted behavior tests for all failed truths before printing success. |
| T-12-09-02 | T | plugin/runtime safety checks | mitigate | Keep static unsafe-pattern checks for `eval(`, raw script injection, and `dangerouslySetInnerHTML`. |
| T-12-09-03 | I | regression test suite | mitigate | Cover disabled built-in hiding and school-scoped launch data so future regressions cannot pass silently. |
</threat_model>

<verification>
- `pnpm test -- src/lib/dal/classroom.test.ts src/components/classroom/classroom-launch-panel.test.tsx src/lib/dal/plugins.builtins.test.ts src/components/authoring/lesson-authoring-workspace.test.tsx`
- `pnpm exec tsx scripts/verify-phase12-launch-and-builtins.ts`
</verification>

<success_criteria>
- Phase 12 verification fails if any of the previously missed blockers reappear.
- `verify:phase12` uses behavior tests as its main proof surface.
- Unsafe plugin/runtime execution patterns remain blocked by the verifier.
</success_criteria>

<output>
After completion, create `.planning/phases/12-classroom-launch-and-built-in-teaching-steps/12-09-SUMMARY.md`
</output>
