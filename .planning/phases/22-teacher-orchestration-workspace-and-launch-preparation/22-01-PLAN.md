---
phase: 22-teacher-orchestration-workspace-and-launch-preparation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dto/lesson-authoring.ts
  - src/lib/dal/lesson-authoring.ts
  - src/lib/dal/lesson-authoring.test.ts
  - src/components/authoring/authoring-status-panel.tsx
  - src/components/authoring/authoring-status-panel.test.tsx
  - src/components/authoring/lesson-editor-header-actions.tsx
  - src/components/authoring/lesson-editor-header-actions.test.tsx
  - src/components/surfaces/lesson-editor-surface.tsx
autonomous: true
requirements:
  - ORCH-02
  - ORCH-03
must_haves:
  truths:
    - `/teacher/editor` 继续保持显式 `courseId + lessonId` 的 teacher-owned 入口，不新增独立 orchestration route。
    - editor 侧会暴露 launch-preparation summary，但不会直接创建 classroom session 或绕过 published snapshot launch path。
    - preparation summary 会把现有 publish readiness、teaching-design cue、materials/evidence summary 组织成教师可读的开课前摘要。
  artifacts:
    - path: src/lib/dto/lesson-authoring.ts
      provides: typed lesson-side launch preparation summary contract
      contains: preparationSummary
    - path: src/lib/dal/lesson-authoring.ts
      provides: teacher-owned orchestration/preparation aggregation for editor DTOs
      contains: getLessonEditorDTO
    - path: src/components/authoring/authoring-status-panel.tsx
      provides: editor-side preparation and readiness rendering
      contains: 阻断项
  key_links:
    - from: src/lib/dal/lesson-authoring.ts
      to: src/lib/dto/lesson-authoring.ts
      via: lesson editor DTO and readiness/preparation summary share the same typed contract
      pattern: LessonEditorDTOSchema|publishState|preparationSummary
    - from: src/components/surfaces/lesson-editor-surface.tsx
      to: src/components/authoring/authoring-status-panel.tsx
      via: existing editor shell continues to host readiness/preparation summary instead of creating a new workspace shell
      pattern: LessonEditorSurface|AuthoringStatusPanel
---

<objective>
在不破坏现有 `/teacher/editor` 入口纪律的前提下，补齐 editor 侧的开课前准备摘要，
让教师在当前 lesson shell 内就能看到 orchestration-quality cues、readiness 分层和
前往 launch workspace 的清晰上下文。

Purpose: 先把 Phase 22 的 lesson-side truth 建稳，避免 `/teacher/launch` 单独发明
另一套 readiness/preparation 语义。
Output: preparation summary DTO、lesson-authoring DAL 聚合、editor header/status surface
更新与回归测试。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-teacher-orchestration-workspace-and-launch-preparation/22-CONTEXT.md
@.planning/phases/22-teacher-orchestration-workspace-and-launch-preparation/22-RESEARCH.md
@.planning/phases/22-teacher-orchestration-workspace-and-launch-preparation/22-UI-SPEC.md
@src/app/(teacher)/teacher/editor/page.tsx
@src/lib/dto/lesson-authoring.ts
@src/lib/dal/lesson-authoring.ts
@src/components/surfaces/lesson-editor-surface.tsx
@src/components/authoring/authoring-status-panel.tsx
@src/components/authoring/lesson-editor-header-actions.tsx

<interfaces>
Current lesson-side readiness contract:
```ts
publishState: {
  canPublish: boolean
  blockingIssues: LessonPublishIssueDTO[]
  warnings: LessonPublishIssueDTO[]
}
```

Current step-side teaching-design cue inputs already available in editor DTO:
```ts
teachingDesignStatus
needsTeachingDesignRefinement
teachingDesignFallbackReason
payload.teachingDesign?.evidenceExpectation
payload.materialRefs
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define lesson-side launch preparation summary contract and DAL aggregation</name>
  <files>src/lib/dto/lesson-authoring.ts, src/lib/dal/lesson-authoring.ts, src/lib/dal/lesson-authoring.test.ts</files>
  <read_first>src/lib/dto/lesson-authoring.ts, src/lib/dal/lesson-authoring.ts, src/lib/dal/lesson-authoring.test.ts, src/lib/dto/classroom.ts, src/lib/dal/classroom.ts, .planning/phases/22-teacher-orchestration-workspace-and-launch-preparation/22-CONTEXT.md, .planning/phases/22-teacher-orchestration-workspace-and-launch-preparation/22-RESEARCH.md</read_first>
  <behavior>
    - Test 1: editor DTO now exposes a typed preparation summary derived from existing lesson facts instead of client heuristics.
    - Test 2: missing teaching-design richness or evidence/material coverage is graded into non-blocking buckets, not promoted to publish blockers.
    - Test 3: teacher-owned editor reads still require the current lesson/course scope and do not introduce session-specific writable state.
  </behavior>
  <action>Extend `src/lib/dto/lesson-authoring.ts` with a typed lesson-side preparation summary contract under the editor DTO. The contract must include exact fields for (1) a high-level summary of active step count, total estimated minutes, material cue count, and evidence-ready step count, (2) readiness buckets split into `blockingIssues`, `attentionIssues`, and `advisoryIssues`, and (3) an explicit launch handoff href or route hint that still points to `/teacher/launch` instead of creating a new route. In `src/lib/dal/lesson-authoring.ts`, compute this summary inside the existing teacher-owned lesson read path by reusing `getLessonPublishReadinessDTO()`, hydrated teaching-design markers, `payload.materialRefs`, and `payload.teachingDesign.evidenceExpectation`. Keep Phase 22 blockers narrowly scoped to lesson facts that truly make orchestration impossible from the editor side; inferred teaching design, weak materials, or incomplete evidence expectation must land in attention/advisory buckets. Do not add draft session notes, launch overrides, or any write path.</action>
  <acceptance_criteria>
    - `src/lib/dto/lesson-authoring.ts` contains a typed `preparationSummary` field on the lesson editor DTO.
    - `src/lib/dal/lesson-authoring.ts` contains logic that computes `attentionIssues` and `advisoryIssues` from teaching-design/material/evidence facts.
    - `src/lib/dal/lesson-authoring.ts` does not introduce any new launch mutation or session config write helper.
    - `src/lib/dal/lesson-authoring.test.ts` contains assertions for `blockingIssues`, `attentionIssues`, and `advisoryIssues` in the preparation summary.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/lesson-authoring.test.ts</automated>
  </verify>
  <done>lesson-side orchestration summary is now a server-owned, typed contract that future launch UI can consume directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface the preparation summary inside the existing editor shell</name>
  <files>src/components/authoring/authoring-status-panel.tsx, src/components/authoring/authoring-status-panel.test.tsx, src/components/authoring/lesson-editor-header-actions.tsx, src/components/authoring/lesson-editor-header-actions.test.tsx, src/components/surfaces/lesson-editor-surface.tsx</files>
  <read_first>src/components/authoring/authoring-status-panel.tsx, src/components/authoring/authoring-status-panel.test.tsx, src/components/authoring/lesson-editor-header-actions.tsx, src/components/authoring/lesson-editor-header-actions.test.tsx, src/components/surfaces/lesson-editor-surface.tsx, src/components/surfaces/teacher-lesson-preview-surface.tsx, src/app/(teacher)/teacher/editor/page.tsx, .planning/phases/22-teacher-orchestration-workspace-and-launch-preparation/22-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: editor status area shows the new preparation summary with `阻断项 / 需关注 / 建议完善` buckets.
    - Test 2: editor header/surface still keeps preview, save, publish, and route discipline intact.
    - Test 3: the new preparation cues point teachers toward launch preparation without turning the editor into a second launch page.
  </behavior>
  <action>Update `src/components/authoring/authoring-status-panel.tsx` to render the new preparation summary alongside the existing publish readiness contract. The panel must show three explicit group labels in Chinese: `阻断项`, `需关注`, and `建议完善`, and it must keep publish feedback separate from launch-preparation guidance. Then update `src/components/authoring/lesson-editor-header-actions.tsx` and `src/components/surfaces/lesson-editor-surface.tsx` so the current editor shell can surface a clear launch-preparation cue such as a `/teacher/launch` handoff or preparation status hint while preserving existing preview/save/publish actions and the explicit `courseId + lessonId` discipline. Do not add a new route, do not trigger classroom launch from the editor, and do not duplicate the full run-sheet UI here.</action>
  <acceptance_criteria>
    - `src/components/authoring/authoring-status-panel.tsx` contains the exact bucket labels `阻断项`, `需关注`, and `建议完善`.
    - `src/components/authoring/lesson-editor-header-actions.tsx` still contains `预览课堂` and `发布课时`, and also contains a launch-preparation cue or `/teacher/launch` handoff.
    - `src/components/surfaces/lesson-editor-surface.tsx` still renders `LessonAuthoringWorkspace` and does not add any classroom launch mutation.
    - `src/components/authoring/authoring-status-panel.test.tsx` and `src/components/authoring/lesson-editor-header-actions.test.tsx` contain assertions for the new preparation summary UI.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/authoring/authoring-status-panel.test.tsx src/components/authoring/lesson-editor-header-actions.test.tsx</automated>
  </verify>
  <done>teachers can now see launch-preparation quality cues inside the existing editor shell without losing current authoring boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lesson authoring facts -> editor preparation summary | launch-preparation cues must come from server-owned DTOs, not client inference |
| editor shell -> launch handoff | editor may guide the teacher toward launch preparation but must not create a parallel launch path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | T | `src/lib/dal/lesson-authoring.ts` | mitigate | Compute preparation summary from teacher-owned DAL facts only; do not let client-side heuristics define readiness truth. |
| T-22-02 | E | `src/components/authoring/lesson-editor-header-actions.tsx` | mitigate | Keep `/teacher/editor` as guidance-only for launch preparation and do not add direct classroom-launch writes. |
| T-22-03 | I | `src/components/authoring/authoring-status-panel.tsx` | mitigate | Separate publish readiness from launch-preparation advice so teachers are not misled about which issues actually block publish vs launch. |
| T-22-04 | R | `src/lib/dal/lesson-authoring.test.ts` | accept | Focused tests validate contract shape and scope without exposing sensitive data. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/lesson-authoring.test.ts`
- `pnpm test --run src/components/authoring/authoring-status-panel.test.tsx src/components/authoring/lesson-editor-header-actions.test.tsx`
</verification>

<success_criteria>
- editor DTO has a stable preparation summary contract.
- editor UI exposes launch-preparation quality cues without spawning a second launch system.
- publish readiness and launch-preparation guidance are clearly separated for teachers.
</success_criteria>

<output>
After completion, create `.planning/phases/22-teacher-orchestration-workspace-and-launch-preparation/22-01-SUMMARY.md`
</output>
