# Context: Phase 8 - Stitch MCP Integration

**Goal:** The system can fetch design tokens and page structures from the remote Stitch project via MCP.

*Note: The user explicitly clarified that "Stitch MCP Integration" means the **AI Agent** using the `stitch` tools (like `stitch_get_project`, `stitch_get_screen`) to read the remote project `5322129002350954765` and directly refactor the UI code in the repository. It DOES NOT mean building an MCP client into the OpenLearn-Next application itself.*

## Canonical Refs

*Mandatory docs/specs downstream agents must read before planning/execution.*

- `DESIGN.md` (Defines local visual constraints: Luminous Academy, Lexend font, tonal layering, no 1px lines)
- `.planning/PROJECT.md` (Context for Stitch project ID: `5322129002350954765`)

## Decisions Captured

### Execution Strategy

- **Agent Tool Usage:** The planner and executor agents must utilize their available Stitch tools (e.g., `stitch_get_project`, `stitch_get_screen`, `stitch_list_screens`) to inspect project ID `5322129002350954765`.
- **Target Pages:** Focus initially on the Home page (`/`) and Teacher Dashboard (`/teacher`).
- **Refactoring Approach:** The AI should parse the layout, density, typography, and spacing from the Stitch screens and immediately rewrite the corresponding local Next.js components to match them 1:1.

### Visual Alignment Goals

- **Layout Density:** Make the pages more compact and aesthetically pleasing, matching the tight layouts in Stitch.
- **Constraints Enforcement:** Eradicate any legacy 1px borders, incorrect surface shadows, or non-Primary Blue CTA gradients that violate `DESIGN.md` rules.

## Code Context

*Codebase assets and patterns relevant to this phase.*

- **UI Primitives:** Reusable components reside in `src/components/ui/` and `src/components/surfaces/`.
- **Design Tokens:** Tailwind v4 design tokens are configured in the `globals.css` and `tailwind.config` files.

---
*Context generated by gsd-discuss-phase on 2026-05-05*