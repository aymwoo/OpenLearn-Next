# Discussion Log: Phase 8 - Stitch MCP Integration

*Recorded: 2026-05-05*

This file captures the raw discussion that produced the decisions in `08-CONTEXT.md`. It is for human reference only.

## Q: MCP Credentials

- **Options presented:**
  - .env.local variables (Recommended: Easiest for dev-time alignment tools, keeps secrets out of repo)
  - Database table (Allows runtime/admin configuration but might be overkill)
  - Config file (A dedicated config file, e.g., stitch.config.ts)

- **Selected:** .env.local variables

## Q: Data Lifecycle

- **Options presented:**
  - Local source files (Recommended: Guarantees build reproducibility and easy diffing in PRs)
  - Database content (Good if designs change dynamically and apply to users instantly without deploying)
  - In-memory cache (Fetches on every start, no disk persistence)

- **Selected:** Local source files

## Q: Sync Trigger

- **Options presented:**
  - CLI script (Recommended: Developer-controlled, simple script like `pnpm sync:stitch`)
  - Admin UI button (Adds an admin interface button that writes to the filesystem)
  - On dev start (Automatically runs whenever `npm run dev` starts)

- **Selected:** CLI script

## Clarification

- **User override:** The user clarified that they do NOT want to build an MCP synchronization feature into the codebase. Instead, the user expects the AI Agent itself to use the Stitch tools available in its environment (e.g., `stitch_get_project`) to read the remote Stitch project ID `5322129002350954765` and directly refactor the UI components (`/`, `/teacher`) to be more compact, aesthetic, and aligned with the design 1:1.

## Deferred Ideas

- None.

---
*Log generated by gsd-discuss-phase*