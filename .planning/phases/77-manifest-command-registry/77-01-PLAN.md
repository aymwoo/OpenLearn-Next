---
phase: 77-manifest-command-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dto/resource-ai.ts
  - src/lib/dto/resource-ai.system-commands.test.ts
autonomous: true
requirements:
  - SYS-03
must_haves:
  truths:
    - "PluginManifestSchema accepts .optional() systemCommands array with discriminated union on command field"
    - "system.http.request manifest shape validates allowedDomains (string[] with domain regex) and allowedMethods (z.enum GET/POST/PUT/DELETE/PATCH)"
    - "system.config manifest shape validates allowedKeys (string[] with prefix wildcard regex, no bare : in key names)"
    - "parseManifestOrThrow rejects invalid systemCommands declarations with UPPER_SNAKE reason codes (SYSTEM_COMMAND_DOMAIN_INVALID, SYSTEM_COMMAND_METHOD_INVALID, SYSTEM_COMMAND_KEY_INVALID)"
    - "Existing quiz/homework manifests from external-catalog.ts parse without errors through the extended PluginManifestSchema"
    - "New manifest with valid systemCommands parses correctly with resolved command shapes"
  artifacts:
    - path: "src/lib/dto/resource-ai.ts"
      provides: "PluginManifestSchema with systemCommands field"
      contains: "systemCommands"
    - path: "src/lib/dto/resource-ai.system-commands.test.ts"
      provides: "Comprehensive vitest assertions for manifest schema extension"
      min_lines: 100
  key_links:
    - from: "src/lib/dal/plugins.ts parseManifestOrThrow"
      to: "src/lib/dto/resource-ai.ts PluginManifestSchema"
      via: "Zod .parse() call"
      pattern: "PluginManifestSchema\\.parse"
    - from: "src/lib/plugins/external-catalog.ts"
      to: "src/lib/dto/resource-ai.ts PluginManifestSchema"
      via: "compatibility scan vitest assertion"
      pattern: "PluginManifestSchema\\.parse"
    - from: "SystemCommandHttpRequestSchema"
      to: "src/lib/dto/resource-ai.ts exports"
      via: "export for Phase 78 reuse"
      pattern: "export const SystemCommandHttpRequestSchema"
    - from: "SystemCommandConfigSchema"
      to: "src/lib/dto/resource-ai.ts exports"
      via: "export for Phase 79 reuse"
      pattern: "export const SystemCommandConfigSchema"
---

<objective>
Extend PluginManifestSchema with an `.optional()` systemCommands discriminated union array, define complete Zod shapes for system.http.request and system.config manifest declarations with inline validation (regex + superRefine), export reusable schemas for Phase 78/79, and verify full backward compatibility with existing quiz/homework manifests via vitest.

Purpose: Enable plugins to declaratively request system commands in their manifest. The schema definitions created here serve as the source of truth that Phase 78 (HTTP proxy) and Phase 79 (KV config) will reuse for runtime manifest re-parsing.

Output: Extended PluginManifestSchema in resource-ai.ts, comprehensive vitest test file with positive/negative/compatibility assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/77-manifest-command-registry/77-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md

<interfaces>
From src/lib/dto/resource-ai.ts (lines 761-781) — current PluginManifestSchema:
- z.object({ id, version, manifestVersion, permissions, anchors, actions, builtIn, defaultEnabled, nonDeletable, theme, governance })
- .superRefine((manifest, ctx) => { ... }) — v2 requires governance
- governance field is .optional() — same pattern for systemCommands

From src/lib/dto/plugin-data-model.ts — UPPER_SNAKE reason code pattern:
- export const PLUGIN_DATA_MODEL_REASONS = ["INVALID_COLUMN_TYPE", "MISSING_OWNED_PREFIX", ...] as const
- z.enum(COLUMN_TYPES, { error: () => "INVALID_COLUMN_TYPE" }) — error message as reason code
- ctx.addIssue({ code: z.ZodIssueCode.custom, message: "MISSING_OWNED_PREFIX", path: [...] }) — superRefine pattern

From src/lib/plugins/external-catalog.ts — existing manifests for compat scan:
- buildExternalQuizManifest(version) — PluginManifest with governance (manifestVersion 2), no systemCommands
- buildExternalHomeworkManifest(version) — same shape
- EXTERNAL_MARKETPLACE_CATALOG — 3 entries (quiz 1.0.0, quiz 1.1.0, homework 1.0.0)

From src/lib/dal/plugins.ts — parseManifestOrThrow:
- function parseManifestOrThrow(manifest: PluginManifest) — calls PluginManifestSchema.parse(manifest)
- Used in install preflight (line 982) and recovery install (line 1023)
- Throws Error(PLUGIN_MANIFEST_INVALID) on parse failure
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define SystemCommandHttpRequestSchema and SystemCommandConfigSchema</name>
  <files>src/lib/dto/resource-ai.ts</files>
  <read_first>
    - src/lib/dto/resource-ai.ts (lines 761-781 for PluginManifestSchema structure, lines 1-15 for imports)
    - src/lib/dto/plugin-data-model.ts (for UPPER_SNAKE reason code pattern: PLUGIN_DATA_MODEL_REASONS array + superRefine custom message pattern)
    - .planning/phases/77-manifest-command-registry/77-CONTEXT.md (D-01 through D-06 for exact shape decisions)
  </read_first>
  <behavior>
    - Test: system.http.request manifest shape parses when allowedDomains=["api.example.com","*.github.com"] and allowedMethods=["GET","POST"]
    - Test: system.http.request manifest shape parses when allowedDomains contains single wildcard domain "*.my-service.io"
    - Test: system.http.request manifest shape rejects when allowedDomains is empty array — error message includes "SYSTEM_COMMAND_DOMAIN_INVALID"
    - Test: system.http.request manifest shape rejects when allowedDomains contains invalid domain like "not a domain!!!" — error message includes "SYSTEM_COMMAND_DOMAIN_INVALID"
    - Test: system.http.request manifest shape rejects when allowedMethods contains "INVALID_VERB" — error message includes "SYSTEM_COMMAND_METHOD_INVALID"
    - Test: system.http.request manifest shape rejects when allowedMethods is empty array — error message includes "SYSTEM_COMMAND_METHOD_INVALID"
    - Test: system.config manifest shape parses when allowedKeys=["homework:*","quiz:stats"]
    - Test: system.config manifest shape rejects when allowedKeys contains key with bare ":" at a position that violates the prefix-wildcard-only rule — error message includes "SYSTEM_COMMAND_KEY_INVALID"
    - Test: system.config manifest shape rejects when allowedKeys is empty array — error message includes "SYSTEM_COMMAND_KEY_INVALID"
    - Test: system.config manifest shape rejects when allowedKeys element contains bare ":" not in prefix position — error message includes "SYSTEM_COMMAND_KEY_INVALID"
  </behavior>
  <action>
    Define three named constant arrays and two Zod schemas in src/lib/dto/resource-ai.ts, inserted immediately BEFORE the PluginManifestSchema definition (around line 760).

    Reason-code constants (UPPER_SNAKE, per D-06):
    export const SYSTEM_COMMAND_REASONS = ["SYSTEM_COMMAND_DOMAIN_INVALID", "SYSTEM_COMMAND_METHOD_INVALID", "SYSTEM_COMMAND_KEY_INVALID"] as const;

    Allowed HTTP methods enum:
    export const SYSTEM_HTTP_METHODS = ["GET", "POST", "PUT", "DELETE", "PATCH"] as const;

    Allowed domain pattern (regex): must match either a plain domain like "api.example.com" or a wildcard domain like "*.example.com". The regex should accept only valid domain label characters (alphanumeric, hyphens, dots) with optional ".*" prefix.

    Allowed key pattern (regex, per D-02): must be either a plain key like "homework:deadline" OR a prefix wildcard like "homework:*". Key segments before ":" must be valid identifiers. The key itself cannot end with bare ":" unless followed by "*".

    SystemCommandHttpRequestSchema:
    - z.strictObject with allowedDomains: z.array(z.string().regex(DOMAIN_PATTERN, { message: "SYSTEM_COMMAND_DOMAIN_INVALID" })).min(1, { message: "SYSTEM_COMMAND_DOMAIN_INVALID" })
    - allowedMethods: z.array(z.enum(SYSTEM_HTTP_METHODS)).min(1, { message: "SYSTEM_COMMAND_METHOD_INVALID" })
    - maxResponseSize: z.number().int().positive().optional() (reserved for Phase 78)
    - defaultTimeout: z.number().int().positive().optional() (reserved for Phase 78)

    SystemCommandConfigSchema:
    - z.strictObject with allowedKeys: z.array(z.string().regex(KEY_PATTERN, { message: "SYSTEM_COMMAND_KEY_INVALID" })).min(1, { message: "SYSTEM_COMMAND_KEY_INVALID" })
    - maxValueSize: z.number().int().positive().optional() (reserved for Phase 79)

    Export both schemas as named exports — Phase 78 imports SystemCommandHttpRequestSchema, Phase 79 imports SystemCommandConfigSchema.

    Do NOT use index-based path references in superRefine. Use the D-05 pattern: inline .regex() with error messages in the array/string schemas, apply .min(1) on arrays with error messages. Invalid method values are caught by z.enum with a custom error map that returns "SYSTEM_COMMAND_METHOD_INVALID" for unrecognized values.
  </action>
  <verify>
    <automated>npx vitest run src/lib/dto/resource-ai.system-commands.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - systemCommands field is .optional() — existing manifests without it parse without errors
    - system.http.request accepted when allowedDomains=["api.example.com","*.github.com"] and allowedMethods=["GET","POST"]
    - system.http.request rejected with "SYSTEM_COMMAND_DOMAIN_INVALID" when allowedDomains contains "not a domain!!!"
    - system.http.request rejected with "SYSTEM_COMMAND_METHOD_INVALID" when allowedMethods contains "INVALID_VERB"
    - system.config accepted when allowedKeys=["homework:*","quiz:stats"]
    - system.config rejected with "SYSTEM_COMMAND_KEY_INVALID" when allowedKeys contains key with bare ":" in wrong position
    - Empty arrays trigger min(1) errors with correct reason code messages
    - Both schemas are exported for Phase 78/79 reuse
  </acceptance_criteria>
  <done>A valid systemCommands manifest parses through PluginManifestSchema; invalid ones produce named UPPER_SNAKE error codes matching SYSTEM_COMMAND_REASONS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add systemCommands to PluginManifestSchema and wire discriminated union</name>
  <files>src/lib/dto/resource-ai.ts</files>
  <read_first>
    - src/lib/dto/resource-ai.ts (lines 761-781 for existing PluginManifestSchema with governance .optional() pattern)
    - src/features/platform-core/commands/contracts.ts (lines 255-332 for PlatformCommandSchema discriminatedUnion pattern — reference for how z.discriminatedUnion syntax works)
    - .planning/phases/77-manifest-command-registry/77-CONTEXT.md (D-01: discriminator field = "command")
  </read_first>
  <behavior>
    - Test: PluginManifestSchema.parse(manifest with no systemCommands) succeeds — backward compat
    - Test: PluginManifestSchema.parse(manifest with systemCommands=[]) succeeds — empty array is valid
    - Test: PluginManifestSchema.parse(manifest with systemCommands=[{command:"system.http.request",allowedDomains:["api.example.com"],allowedMethods:["GET"]}]) succeeds and the parsed result has command="system.http.request" with correct shape
    - Test: PluginManifestSchema.parse(manifest with systemCommands=[{command:"system.config",allowedKeys:["homework:*"]}]) succeeds
    - Test: PluginManifestSchema.parse(manifest with systemCommands=[{command:"system.http.request",allowedDomains:["api.example.com"],allowedMethods:["GET"]},{command:"system.config",allowedKeys:["quiz:*"]}]) succeeds — multiple commands
    - Test: PluginManifestSchema.parse(manifest with systemCommands=[{command:"system.unknown"}]) fails — command name not in union
    - Test: PluginManifestSchema.parse rejects when systemCommands contains a non-object value — Zod type error
    - Test: PluginManifestSchema.parse(manifest with no systemCommands AND governance present) still enforces v2 governance requirement via superRefine
  </behavior>
  <action>
    In src/lib/dto/resource-ai.ts, after the SystemCommandHttpRequestSchema and SystemCommandConfigSchema definitions:

    1. Define the SystemCommandDiscriminatedSchema using z.discriminatedUnion("command", [...]):
       - First variant: z.strictObject({ command: z.literal("system.http.request") }).merge(SystemCommandHttpRequestSchema)
       - Second variant: z.strictObject({ command: z.literal("system.config") }).merge(SystemCommandConfigSchema)

    2. Add to PluginManifestSchema z.object({...}) — insert BEFORE the .superRefine() call:
       systemCommands: z.array(SystemCommandDiscriminatedSchema).optional()

    3. Add a superRefine check (inside the existing superRefine block) that:
       - When manifestVersion === 2 and systemCommands is present and non-empty, no additional requirement (governance is already checked)
       - The discriminatedUnion auto-rejects unknown command names
       - The strictObject on each variant rejects extra fields

    4. Export SystemCommandDiscriminatedSchema as a named export for Phase 78/79 runtime manifest re-parsing.

    Per D-08: do NOT add any registry-lookup validation in the schema. The discriminatedUnion's z.literal naturally constrains valid command names. Invalid command names (not in the union) are rejected by Zod's discriminated union mechanism with a standard Zod error.
  </action>
  <verify>
    <automated>npx vitest run src/lib/dto/resource-ai.system-commands.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - PluginManifestSchema accepts manifests without systemCommands (backward compatible)
    - PluginManifestSchema accepts manifests with systemCommands: [] (empty array)
    - PluginManifestSchema accepts manifests with valid system.http.request and system.config entries
    - PluginManifestSchema accepts manifests with multiple system command entries
    - PluginManifestSchema rejects manifests with unrecognized command names
    - PluginManifestSchema rejects manifests with invalid command shapes (domain/method/key validation)
    - governance v2 requirement still enforced when systemCommands is present
    - SystemCommandDiscriminatedSchema is exported
  </acceptance_criteria>
  <done>PluginManifestSchema accepts .optional() systemCommands with discriminated union validation; unrecognized commands and invalid shapes produce parse errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Full compatibility scan — existing quiz/homework manifests + positive/negative assertions</name>
  <files>src/lib/dto/resource-ai.system-commands.test.ts</files>
  <read_first>
    - src/lib/dto/resource-ai.ts (full PluginManifestSchema after Task 2, exported schemas)
    - src/lib/plugins/external-catalog.ts (buildExternalQuizManifest, buildExternalHomeworkManifest, EXTERNAL_MARKETPLACE_CATALOG)
    - src/lib/dal/plugins.ts (parseManifestOrThrow at line 282 — confirms manifests go through PluginManifestSchema.parse)
    - .planning/phases/77-manifest-command-registry/77-CONTEXT.md (D-09, D-10, D-11 for test coverage requirements)
  </read_first>
  <behavior>
    - Test: buildExternalQuizManifest("1.0.0") parsed by PluginManifestSchema — no throw, manifest.id matches
    - Test: buildExternalQuizManifest("1.1.0") parsed by PluginManifestSchema — no throw, manifest.id matches
    - Test: buildExternalHomeworkManifest("1.0.0") parsed by PluginManifestSchema — no throw, manifest.id matches
    - Test: EXTERNAL_MARKETPLACE_CATALOG every entry.manifest parsed by PluginManifestSchema — all pass without error
    - Test: Construct manifest with systemCommands=[{command:"system.http.request",allowedDomains:["api.example.com"],allowedMethods:["GET"]}] — parse succeeds, parsedResult.systemCommands[0].command === "system.http.request"
    - Test: Construct manifest with systemCommands containing both command types — parse succeeds, array length 2
    - Test: Manifest with systemCommands=[{command:"system.http.request",allowedDomains:[],allowedMethods:["GET"]}] — parse throws, error message or issue includes "SYSTEM_COMMAND_DOMAIN_INVALID"
    - Test: Manifest with systemCommands=[{command:"system.http.request",allowedDomains:["example.com"],allowedMethods:["UNKNOWN"]}] — parse throws, error includes "SYSTEM_COMMAND_METHOD_INVALID"
    - Test: Manifest with systemCommands=[{command:"system.config",allowedKeys:[]}] — parse throws, error includes "SYSTEM_COMMAND_KEY_INVALID"
    - Test: Manifest with systemCommands=[{command:"system.config",allowedKeys:["invalid:key:here"]}] — parse throws, error includes "SYSTEM_COMMAND_KEY_INVALID"
    - Test: Manifest with systemCommands=[{command:"system.unknown"}] — parse throws (discriminated union rejects unknown literal)
    - Test: Manifest with manifestVersion=2, governance present, systemCommands present — parse succeeds (both governance and systemCommands work together)
    - Test: Manifest with manifestVersion=2, NO governance, systemCommands present — parse throws (governance v2 requirement enforced)
    - Test: Manifest with manifestVersion=1, systemCommands present — parse succeeds (v1 has no governance requirement)
  </behavior>
  <action>
    Create src/lib/dto/resource-ai.system-commands.test.ts with vitest describe/it blocks.

    Import PluginManifestSchema, SystemCommandHttpRequestSchema, SystemCommandConfigSchema, SystemCommandDiscriminatedSchema from src/lib/dto/resource-ai.ts.
    Import buildExternalQuizManifest, buildExternalHomeworkManifest from src/lib/plugins/external-catalog.ts.

    Test structure:
    describe("PluginManifestSchema systemCommands") with nested describe blocks:

    1. describe("backward compatibility") — parse existing quiz/homework manifests without systemCommands, assert no throw, assert manifest.id and manifest.governance are present.

    2. describe("positive cases") — construct valid manifests with systemCommands, assert parse succeeds, assert resolved command shape fields are correct (allowedDomains, allowedMethods, allowedKeys).

    3. describe("negative cases") — construct invalid manifests and use expect(() => PluginManifestSchema.parse(...)).toThrow(). For ZodError, check that issue messages include the expected UPPER_SNAKE reason code. For empty arrays, check .min(1) error. For invalid methods, check enum error. For invalid domains, check regex error. For invalid keys, check regex error. For unknown commands, check that parse throws (discriminated union rejection).

    4. describe("governance interaction") — v2 with governance + systemCommands succeeds, v2 without governance fails, v1 with systemCommands succeeds.

    5. describe("individual schema exports") — verify SystemCommandHttpRequestSchema.parse and SystemCommandConfigSchema.parse work standalone.

    Use vitest's expect().toThrow() for negative cases. For ZodError inspection: catch the error, check error.issues array for message strings matching the expected reason code constants.
  </action>
  <verify>
    <automated>npx vitest run src/lib/dto/resource-ai.system-commands.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 3 EXTERNAL_MARKETPLACE_CATALOG entries parse without errors
    - buildExternalQuizManifest and buildExternalHomeworkManifest both parse successfully
    - Valid systemCommands manifests parse with correct resolved shapes
    - 6+ negative test cases all produce expected error codes
    - governance v2 interaction tests pass
    - Individual schema exports work standalone
    - Test file runs in under 5 seconds
  </acceptance_criteria>
  <done>Full compatibility verified: existing manifests parse, valid systemCommands parse, invalid declarations produce named reason codes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin manifest (untrusted JSON) -> PluginManifestSchema.parse() | Plugin authors control manifest content; Zod is the trust boundary gate |
| manifestJson column -> parseManifestOrThrow | Stored manifest JSON parsed at install/upgrade; Zod is the re-validation gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01 | Tampering | PluginManifestSchema systemCommands | mitigate | discriminatedUnion z.literal prevents command name injection; strictObject rejects extra fields; regex validates domain/key format |
| T-77-02 | Spoofing | systemCommands allowedDomains | mitigate | Domain regex prevents non-domain strings from passing validation; wildcard prefix constrained to `*.` |
| T-77-03 | Elevation | systemCommands allowedKeys with bare `:` | mitigate | Key regex rejects bare `:` in non-prefix positions; prefix wildcard must be `segment:*` |
| T-77-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |

Note: This phase adds no new packages. Only Zod (already in project) is used. T-77-SC included per protocol.
</threat_model>

<verification>
Run the full test suite for the new test file:
```bash
npx vitest run src/lib/dto/resource-ai.system-commands.test.ts
```

All tests must pass. Backward compatibility with existing manifests is verified by the test assertions on EXTERNAL_MARKETPLACE_CATALOG entries.
</verification>

<success_criteria>
- PluginManifestSchema accepts .optional() systemCommands with discriminated union
- system.http.request and system.config shapes validated with regex + enum + min(1) constraints
- Invalid declarations produce UPPER_SNAKE reason codes matching SYSTEM_COMMAND_REASONS
- All existing quiz/homework manifests parse without errors
- Valid systemCommands manifests parse with correct resolved shapes
- Individual command schemas exported for Phase 78/79 reuse
- 15+ vitest assertions pass
</success_criteria>

<output>
Create `.planning/phases/77-manifest-command-registry/77-01-SUMMARY.md` when done
</output>
