---
phase: 77-manifest-command-registry
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/platform-core/commands/contracts.ts
  - src/features/platform-core/commands/registry.ts
  - src/features/runtime-platform/contracts/permissions.ts
  - src/features/platform-core/commands/system-commands.test.ts
autonomous: true
requirements:
  - SYS-05
must_haves:
  truths:
    - "PlatformCommandTypeSchema includes system.http.request and system.config.set in its enum"
    - "PlatformCommandSchema discriminated union has variants for system.http.request and system.config.set"
    - "platformCommandRegistry has entries for system.http.request and system.config.set using createPlatformCommandDefinition factory"
    - "GovernanceDeniedReasonValues includes domain_not_allowed, method_not_allowed, private_ip_blocked, config_key_denied"
    - "Each new commandType has a payload schema registered in PlatformCommandPayloadSchemas"
  artifacts:
    - path: "src/features/platform-core/commands/contracts.ts"
      provides: "SystemCommandTypes array, new payload schemas, PlatformCommandTypeSchema and PlatformCommandSchema updated"
      contains: "SystemCommandTypes"
    - path: "src/features/platform-core/commands/registry.ts"
      provides: "platformCommandRegistry entries for system.http.request and system.config.set"
      contains: "system.http.request"
    - path: "src/features/runtime-platform/contracts/permissions.ts"
      provides: "GovernanceDeniedReasonValues with 4 new reason codes"
      contains: "domain_not_allowed"
    - path: "src/features/platform-core/commands/system-commands.test.ts"
      provides: "Vitest assertions for registry entries, type schema, and audit reason codes"
      min_lines: 80
  key_links:
    - from: "src/features/platform-core/commands/contracts.ts SystemCommandTypes"
      to: "src/features/platform-core/commands/contracts.ts PlatformCommandTypeSchema"
      via: "spread into z.enum array"
      pattern: "...SystemCommandTypes"
    - from: "src/features/platform-core/commands/registry.ts"
      to: "src/features/platform-core/commands/contracts.ts PlatformCommandPayloadSchemas"
      via: "createPlatformCommandDefinition factory"
      pattern: "PlatformCommandPayloadSchemas"
    - from: "src/features/runtime-platform/contracts/permissions.ts GovernanceDeniedReasonValues"
      to: "governanceAudit inserts"
      via: "reasonCode column (text, no migration needed per D-15)"
      pattern: "GovernanceDeniedReasonValues"
---

<objective>
Register 2 new system command types (system.http.request, system.config.set) in platformCommandRegistry, update PlatformCommandTypeSchema and PlatformCommandSchema discriminated union with corresponding variants, define payload schemas with placeholder authorize/execute handlers (authorize defers to Phase 78 Phase 79 implementations per scope boundary), and add 4 new governance audit reason codes to GovernanceDeniedReasonValues.

Purpose: The Command Bus must recognize system.* command types so that Phase 78 (HTTP proxy) and Phase 79 (KV config) can wire real handlers. The registry registration, command types, and payload schemas are the foundation that Phase 78/79 build on.

Output: Updated contracts.ts (types + payloads + discriminated union), registry.ts (2 new entries), permissions.ts (4 new reason codes), and comprehensive vitest test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/77-manifest-command-registry/77-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md

<interfaces>
From src/features/platform-core/commands/contracts.ts — full file read:
- PlatformPluginGovernanceCommandTypes: 12-element const array (lines 14-27)
- LessonDraftCommandTypes: 4-element const array (line 31)
- PluginDataCommandTypes: 2-element const array (line 35)
- QuizTransportCommandTypes: 1-element const array (line 37)
- PlatformCommandTypeSchema: z.enum([...all four arrays]) (lines 39-44)
- PlatformCommandPayloadSchemas: const object mapping type string to Zod schema (lines 233-253)
- PlatformCommandSchema: z.discriminatedUnion("type", [...]) with ~19 variants (lines 255-332)
- PlatformCommandEnvelopeSchema: z.object with id, actor, scope, correlation, audit, dedupeKey (lines 62-72)
- PlatformCommandDefinition<TType>: type with commandType, payloadSchema, dedupe, authorize, execute (lines 404-410)

From src/features/platform-core/commands/registry.ts — full file read:
- createPlatformCommandDefinition<TType>(input): identity factory (lines 11-15)
- platformCommandRegistry: Record<PlatformCommandType, PlatformCommandDefinition> with 19 entries (lines 17-151)
- Each entry: commandType (literal), payloadSchema (from PlatformCommandPayloadSchemas), dedupe ("required"/"optional"), authorize (handler function), execute (handler function)

From src/features/runtime-platform/contracts/permissions.ts — full file read:
- GovernanceDeniedReasonValues: 7-element const array (lines 32-40)
- GovernanceDeniedReasonSchema: z.enum(GovernanceDeniedReasonValues) (line 61)
- All other exports: RuntimeCapabilityValues, HostActionPermissionValues, RuntimeActorScopeValues, PluginPermissionValues, PluginLifecycleStateValues

From .planning/phases/77-manifest-command-registry/77-CONTEXT.md:
- D-12: 4 new reason codes: domain_not_allowed, method_not_allowed, private_ip_blocked, config_key_denied
- D-13: audit granularity — at least 1 record per call (deny + allow)
- D-14: action field = commandType (system.http.request, system.config.set, system.config.get)
- D-15: no schema migration — reasonCode is text column

Scope boundary: system.config.set goes through Command Bus; system.config.get is pure DAL read (not a PlatformCommandType per milestone decision). The registry only gets system.http.request and system.config.set entries. system.config.set payload schema follows the pattern: schoolId injected by session (not in payload), pluginId injected by scope, configKey + configValue in payload.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SystemCommandTypes array + payload schemas + update discriminated union</name>
  <files>src/features/platform-core/commands/contracts.ts</files>
  <read_first>
    - src/features/platform-core/commands/contracts.ts (full file — lines 1-410, for exact pattern of existing command types, payload schemas, discriminated union variants)
    - src/features/runtime-platform/contracts/permissions.ts (lines 32-40 for GovernanceDeniedReasonValues — reference for const array pattern)
    - .planning/phases/77-manifest-command-registry/77-CONTEXT.md (D-12 through D-15)
  </read_first>
  <behavior>
    - Test: PlatformCommandTypeSchema.parse("system.http.request") succeeds
    - Test: PlatformCommandTypeSchema.parse("system.config.set") succeeds
    - Test: PlatformCommandTypeSchema.parse("system.config.get") fails — not a PlatformCommandType (pure DAL read)
    - Test: PlatformCommandTypeSchema still accepts all 19 existing command types
    - Test: PlatformCommandPayloadSchemas["system.http.request"] exists and is a Zod schema
    - Test: PlatformCommandPayloadSchemas["system.config.set"] exists and is a Zod schema
    - Test: PlatformCommandSchema.parse with type="system.http.request" and valid payload succeeds
    - Test: PlatformCommandSchema.parse with type="system.config.set" and valid payload succeeds
  </behavior>
  <action>
    In src/features/platform-core/commands/contracts.ts:

    1. Add SystemCommandTypes const array (after QuizTransportCommandTypes, around line 38):
       export const SystemCommandTypes = ["system.http.request", "system.config.set"] as const;

       Note: system.config.get is deliberately excluded — it is a pure DAL read, not a PlatformCommandType (per milestone decision).

    2. Add ...SystemCommandTypes to PlatformCommandTypeSchema:
       export const PlatformCommandTypeSchema = z.enum([
         ...PlatformPluginGovernanceCommandTypes,
         ...LessonDraftCommandTypes,
         ...PluginDataCommandTypes,
         ...QuizTransportCommandTypes,
         ...SystemCommandTypes,
       ]);

    3. Define SystemHttpRequestPayloadSchema (after QuizAnswerReceivedPayloadSchema, before PlatformCommandPayloadSchemas):
       - z.strictObject with:
         - url: z.string().url() (the target URL to proxy — Phase 78 runtime validates against manifest allowedDomains)
         - method: z.enum(["GET", "POST", "PUT", "DELETE", "PATCH"])
         - headers: z.record(z.string(), z.string()).optional()
         - body: z.string().optional()
         - maxResponseSize: z.number().int().positive().optional()
         - timeout: z.number().int().positive().optional()
       Note: schoolId and pluginId are NOT in this payload — they are injected by the envelope scope (per milestone decision: schoolId from session, pluginId from scope).

    4. Define SystemConfigSetPayloadSchema:
       - z.strictObject with:
         - configKey: z.string().min(1) (Phase 79 validates against manifest allowedKeys)
         - configValue: z.unknown() (Phase 79 validates JSON serializability and 64KB limit)
       Note: schoolId and pluginId are NOT in this payload — injected by envelope scope.

    5. Add to PlatformCommandPayloadSchemas:
       "system.http.request": SystemHttpRequestPayloadSchema,
       "system.config.set": SystemConfigSetPayloadSchema,

    6. Add 2 new discriminated union variants to PlatformCommandSchema (after the quiz.answer.received variant):
       - PlatformCommandEnvelopeSchema.extend({ type: z.literal("system.http.request"), payload: SystemHttpRequestPayloadSchema })
       - PlatformCommandEnvelopeSchema.extend({ type: z.literal("system.config.set"), payload: SystemConfigSetPayloadSchema })

    Per D-14: the action field in governanceAudits will use the commandType string ("system.http.request", "system.config.set") — this is handled by the existing audit writer pattern that reads command.type.
  </action>
  <verify>
    <automated>npx vitest run src/features/platform-core/commands/system-commands.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - SystemCommandTypes const array with ["system.http.request", "system.config.set"] exists
    - SystemCommandTypes spread into PlatformCommandTypeSchema z.enum
    - PlatformCommandTypeSchema.parse("system.http.request") succeeds
    - PlatformCommandTypeSchema.parse("system.config.set") succeeds
    - PlatformCommandTypeSchema.parse("system.config.get") fails (not a PlatformCommandType)
    - SystemHttpRequestPayloadSchema defines url, method, headers, body, maxResponseSize, timeout with .strict()
    - SystemConfigSetPayloadSchema defines configKey, configValue with .strict()
    - PlatformCommandPayloadSchemas has both new entries
    - PlatformCommandSchema has both new discriminated union variants
    - All 19 existing command types still parse correctly
  </acceptance_criteria>
  <done>PlatformCommandTypeSchema and PlatformCommandSchema accept system.http.request and system.config.set with payload schemas; system.config.get is excluded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register system commands in platformCommandRegistry</name>
  <files>src/features/platform-core/commands/registry.ts</files>
  <read_first>
    - src/features/platform-core/commands/registry.ts (full file — lines 1-151, for exact createPlatformCommandDefinition pattern and existing registry entries)
    - src/features/platform-core/commands/contracts.ts (after Task 1 — PlatformCommandPayloadSchemas, PlatformCommandDefinition type)
    - .planning/phases/77-manifest-command-registry/77-CONTEXT.md (scope boundary: placeholder authorize/execute handlers)
  </read_first>
  <behavior>
    - Test: platformCommandRegistry["system.http.request"] exists with commandType "system.http.request"
    - Test: platformCommandRegistry["system.http.request"].payloadSchema matches PlatformCommandPayloadSchemas["system.http.request"]
    - Test: platformCommandRegistry["system.http.request"].dedupe is "required"
    - Test: platformCommandRegistry["system.config.set"] exists with commandType "system.config.set"
    - Test: platformCommandRegistry["system.config.set"].payloadSchema matches PlatformCommandPayloadSchemas["system.config.set"]
    - Test: platformCommandRegistry["system.config.set"].dedupe is "required"
    - Test: platformCommandRegistry satisfies Record<PlatformCommandType, PlatformCommandDefinition> — TypeScript compile check
    - Test: Existing registry entries still work (spot-check: "plugin.install", "quiz.answer.received")
  </behavior>
  <action>
    In src/features/platform-core/commands/registry.ts:

    Add 2 new registry entries to platformCommandRegistry object, following the exact pattern of existing entries:

    1. "system.http.request" entry:
       - commandType: "system.http.request"
       - payloadSchema: PlatformCommandPayloadSchemas["system.http.request"]
       - dedupe: "required"
       - authorize: a placeholder async function that resolves immediately (returns Promise.resolve()). Add a comment: "// TODO Phase 78: validate manifest allowedDomains + allowedMethods against request payload"
       - execute: a placeholder async function that throws an Error("system.http.request handler not implemented — Phase 78"). Add a comment: "// TODO Phase 78: HTTP proxy implementation with SSRF protection"

    2. "system.config.set" entry:
       - commandType: "system.config.set"
       - payloadSchema: PlatformCommandPayloadSchemas["system.config.set"]
       - dedupe: "required"
       - authorize: a placeholder async function that resolves immediately (returns Promise.resolve()). Add a comment: "// TODO Phase 79: validate manifest allowedKeys against configKey, schoolId injection"
       - execute: a placeholder async function that throws an Error("system.config.set handler not implemented — Phase 79"). Add a comment: "// TODO Phase 79: KV config write via Command Bus producer"

    Per scope boundary: the registry entries exist for type registration purposes. The authorize function defers to Phase 78 (HTTP proxy) for domain/method whitelist validation and SSRF guard. The execute function defers to Phase 78/79 for actual implementation.

    The satisfies Record<PlatformCommandType, PlatformCommandDefinition> constraint ensures TypeScript validates that all command types in the union have registry entries.
  </action>
  <verify>
    <automated>npx vitest run src/features/platform-core/commands/system-commands.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - platformCommandRegistry["system.http.request"] registered with correct commandType and payloadSchema
    - platformCommandRegistry["system.config.set"] registered with correct commandType and payloadSchema
    - Both entries have dedupe: "required"
    - Both entries have placeholder authorize (resolves) and execute (throws with clear message)
    - TypeScript compile: registry satisfies Record<PlatformCommandType, PlatformCommandDefinition>
    - All existing 19 registry entries still present and functional
  </acceptance_criteria>
  <done>platformCommandRegistry has system.http.request and system.config.set entries with placeholder handlers for Phase 78/79.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add governance audit reason codes + comprehensive vitest assertions</name>
  <files>
    src/features/runtime-platform/contracts/permissions.ts,
    src/features/platform-core/commands/system-commands.test.ts
  </files>
  <read_first>
    - src/features/runtime-platform/contracts/permissions.ts (full file — lines 1-120, for GovernanceDeniedReasonValues array and associated exports)
    - .planning/phases/77-manifest-command-registry/77-CONTEXT.md (D-12, D-13, D-14, D-15 for exact reason code values)
    - src/features/platform-core/commands/contracts.ts (after Task 1 — SystemCommandTypes, PlatformCommandTypeSchema, PlatformCommandSchema, payload schemas)
    - src/features/platform-core/commands/registry.ts (after Task 2 — platformCommandRegistry entries)
  </read_first>
  <behavior>
    - Test: GovernanceDeniedReasonValues includes "domain_not_allowed"
    - Test: GovernanceDeniedReasonValues includes "method_not_allowed"
    - Test: GovernanceDeniedReasonValues includes "private_ip_blocked"
    - Test: GovernanceDeniedReasonValues includes "config_key_denied"
    - Test: GovernanceDeniedReasonSchema.parse("domain_not_allowed") succeeds
    - Test: GovernanceDeniedReasonSchema.parse("method_not_allowed") succeeds
    - Test: GovernanceDeniedReasonSchema.parse("private_ip_blocked") succeeds
    - Test: GovernanceDeniedReasonSchema.parse("config_key_denied") succeeds
    - Test: GovernanceDeniedReasonValues still contains all 7 existing reason codes
    - Test: PlatformCommandTypeSchema.parse validates all 21 command types (19 existing + 2 new)
    - Test: platformCommandRegistry has 21 entries total
    - Test: SystemCommandTypes === ["system.http.request", "system.config.set"]
    - Test: system.config.get is NOT in SystemCommandTypes
    - Test: PlatformCommandSchema.parse with system.http.request payload validates correctly
    - Test: PlatformCommandSchema.parse with system.config.set payload validates correctly
  </behavior>
  <action>
    In src/features/runtime-platform/contracts/permissions.ts:

    1. Append 4 new entries to GovernanceDeniedReasonValues (after "unsupported_action", around line 39):
       "domain_not_allowed",
       "method_not_allowed",
       "private_ip_blocked",
       "config_key_denied",

    Per D-15: No schema migration needed. governanceAudits.reasonCode is a text column (src/db/schema.ts:1336), so these new string values can be written directly without any DB migration.

    Per D-12: These 4 codes cover all system command denial scenarios:
    - domain_not_allowed: target domain not in manifest allowedDomains whitelist (Phase 78)
    - method_not_allowed: HTTP method not in manifest allowedMethods whitelist (Phase 78)
    - private_ip_blocked: SSRF guard detected private/loopback/link-local IP (Phase 78)
    - config_key_denied: configKey not in manifest allowedKeys whitelist (Phase 79)

    No other changes needed in permissions.ts — GovernanceDeniedReasonSchema (z.enum) automatically includes the new values since it references GovernanceDeniedReasonValues.

    ───

    Create src/features/platform-core/commands/system-commands.test.ts:

    Import PlatformCommandTypeSchema, PlatformCommandSchema, PlatformCommandPayloadSchemas, SystemCommandTypes, SystemHttpRequestPayloadSchema, SystemConfigSetPayloadSchema from contracts.ts.
    Import platformCommandRegistry from registry.ts.
    Import GovernanceDeniedReasonValues, GovernanceDeniedReasonSchema from permissions.ts.

    Test structure with describe blocks:

    1. describe("SystemCommandTypes") — assert array equals ["system.http.request", "system.config.set"], assert "system.config.get" is not included.

    2. describe("PlatformCommandTypeSchema") — parse all 21 types (iterate existing 19 + 2 new), assert no throw. Assert system.config.get fails.

    3. describe("PlatformCommandPayloadSchemas") — assert keys for both new types exist. Assert SystemHttpRequestPayloadSchema validates with url + method. Assert SystemConfigSetPayloadSchema validates with configKey + configValue. Assert strict mode rejects extra keys.

    4. describe("PlatformCommandSchema discriminated union") — construct valid system.http.request command envelope, assert parse succeeds and type is "system.http.request". Same for system.config.set. Assert invalid type is rejected.

    5. describe("platformCommandRegistry") — assert 21 entries. Assert each entry has commandType, payloadSchema, dedupe, authorize, execute.

    6. describe("GovernanceDeniedReasonValues") — assert includes all 11 values (7 original + 4 new). Assert GovernanceDeniedReasonSchema.parse accepts each.
  </action>
  <verify>
    <automated>npx vitest run src/features/platform-core/commands/system-commands.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - GovernanceDeniedReasonValues has 11 entries (7 original + 4 new)
    - 4 new reason codes are: domain_not_allowed, method_not_allowed, private_ip_blocked, config_key_denied
    - GovernanceDeniedReasonSchema accepts all new values
    - All 7 original reason codes still present
    - SystemCommandTypes equals ["system.http.request", "system.config.set"]
    - PlatformCommandTypeSchema parses all 21 command types
    - PlatformCommandSchema parses both new command variants
    - platformCommandRegistry has 21 entries
    - Test file runs in under 3 seconds
  </acceptance_criteria>
  <done>4 new governance audit reason codes added; comprehensive vitest assertions cover all contract, registry, and permissions changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Command payload -> Zod schema parse | system command payloads are validated at the contract boundary; schoolId/pluginId never in payload |
| Registry authorize -> Phase 78/79 handlers | Placeholder authorize accepts all (no security decisions made yet); real authorization deferred to downstream phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-04 | Tampering | SystemHttpRequestPayloadSchema | mitigate | .strict() rejects extra keys; url uses z.string().url(); method uses z.enum; schoolId not in payload |
| T-77-05 | Information Disclosure | SystemConfigSetPayloadSchema | mitigate | .strict() rejects extra keys; schoolId and pluginId injected by envelope, not from payload |
| T-77-06 | Elevation | platformCommandRegistry placeholder authorize | accept | Placeholder authorize accepts all — no authorization decisions made at this phase; real gates added in Phase 78 (HTTP domain/method whitelist) and Phase 79 (config key whitelist) |
| T-77-07 | Repudiation | GovernanceDeniedReasonValues expansion | mitigate | New reason codes added to typed enum; audit records will use these codes when Phase 78/79 write denials |
| T-77-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |

Note: This phase adds no new packages. T-77-SC included per protocol.
</threat_model>

<verification>
Run the full test suite for the new test file:
```bash
npx vitest run src/features/platform-core/commands/system-commands.test.ts
```

All tests must pass. Also run the existing command tests to ensure no regression:
```bash
npx vitest run src/features/platform-core/commands/
```
</verification>

<success_criteria>
- PlatformCommandTypeSchema includes system.http.request and system.config.set
- PlatformCommandSchema has 2 new discriminated union variants with payload schemas
- platformCommandRegistry has 2 new entries with createPlatformCommandDefinition
- GovernanceDeniedReasonValues has 4 new reason codes (11 total)
- system.config.get is NOT added as a PlatformCommandType (pure DAL read)
- All existing 19 command types still parse and register correctly
- Comprehensive vitest assertions pass
</success_criteria>

<output>
Create `.planning/phases/77-manifest-command-registry/77-02-SUMMARY.md` when done
</output>
