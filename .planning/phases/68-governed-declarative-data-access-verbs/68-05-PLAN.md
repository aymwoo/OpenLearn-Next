---
phase: 68-governed-declarative-data-access-verbs
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - scripts/verify-phase68-data-access-verbs.ts
  - package.json
autonomous: true
requirements: [ACCESS-01, ACCESS-02, ACCESS-03]
must_haves:
  truths:
    - "存在端到端 close gate：所有合法五动词通过、所有非法样本被拒并审计（SC1/SC2/SC3）"
    - "负样本集断言『具体拒因码 + 一条 audit 写入』，覆盖 D-08 全部 10 类拒绝场景"
    - "zero-drift 门：plugin:compile 后 allowlist 生成文件 git diff 为空（D-06 单一真相源）"
    - "pnpm verify:phase68 串联 编译/漂移/单测/负样本，可一键复跑"
  artifacts:
    - path: "scripts/verify-phase68-data-access-verbs.ts"
      provides: "负样本 + 合法样本端到端断言 runner（10 类拒因 + audit）"
      contains: "raw_sql_rejected"
    - path: "package.json"
      provides: "verify:phase68 脚本 + verify:phase 别名指向 phase68"
      contains: "verify:phase68"
  key_links:
    - from: "scripts/verify-phase68-data-access-verbs.ts"
      to: "dispatchPluginDataAccess"
      via: "对每个负样本断言 throws(reasonCode) + audit 行存在"
      pattern: "dispatchPluginDataAccess"
    - from: "package.json"
      to: "scripts/verify-phase68-data-access-verbs.ts"
      via: "verify:phase68 = plugin:compile && git diff --exit-code && tsx runner"
      pattern: "verify:phase68"
---

<objective>
建立 Phase 68 的**端到端 close gate**：脚本 `verify-phase68-data-access-verbs.ts` 对统一入口 `dispatchPluginDataAccess` 跑两组样本——(a) 全部合法五动词成功；(b) **负样本集**对 D-08 的 10 类拒绝场景逐一断言"具体拒因码 + 恰好一条对应 audit 写入"。再用 `package.json` 把 `plugin:compile`（重生成 allowlist）、`git diff --exit-code`（zero-drift，D-06）、Vitest、负样本 runner 串成 `verify:phase68`，并把 `verify:phase` 别名从 phase67 切到 phase68。

D-08 的 10 类：`raw_sql_rejected` / `free_where_rejected` / `unknown_column_rejected` / `unknown_table_rejected` / `cross_school_rejected` / `invalid_payload_rejected` / `unindexed_column_rejected` + `lifecycle_not_executable` / `kill_switch_rejected` / `non_school_actor_rejected`（第 10 类经治理动作注册门）。

Purpose: 把"拒注入面 + 越权必审计 + 单一真相源零漂移"固化成可一键复跑的验收闸。
Output: 负样本 runner、verify:phase68 脚本、verify:phase 别名更新。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-governed-declarative-data-access-verbs/68-PATTERNS.md
@.planning/phases/68-governed-declarative-data-access-verbs/68-VALIDATION.md
@.planning/phases/68-governed-declarative-data-access-verbs/68-01-SUMMARY.md
@.planning/phases/68-governed-declarative-data-access-verbs/68-02-SUMMARY.md
@.planning/phases/68-governed-declarative-data-access-verbs/68-03-SUMMARY.md
@.planning/phases/68-governed-declarative-data-access-verbs/68-04-SUMMARY.md

<interfaces>
<!-- 被测入口 —— Plan 04 -->
import { dispatchPluginDataAccess } from "../src/features/platform-core/plugin-data-access/facade";
import { PLUGIN_DATA_ACCESS_REASONS } from "../src/features/platform-core/plugin-data-access/allowlist";  // 10 拒因 tuple（Plan 01）

<!-- phase67 runner 形态（照搬调用约定）—— package.json 现有脚本 -->
// "verify:phase67": "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase67-*.ts"
// "verify:phase": "pnpm verify:phase67"   <-- 本计划改指 phase68
// "plugin:compile": "tsx scripts/compile-plugin-data-model.ts"（Plan 01 扩展）

<!-- audit 查询 —— governanceAudits 表（plugins.ts L273 写入；此处读断言） -->
// 每个负样本后：select count from governanceAudits where decision='denied' and reasonCode=<期望> => 恰好 +1
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: verify-phase68-data-access-verbs.ts —— 合法 + 负样本端到端断言</name>
  <read_first>
    - 新建文件：scripts/verify-phase68-data-access-verbs.ts
    - PATTERNS 分析源：scripts/verify-phase67-*.ts（shim+tsx runner、setup/teardown、断言失败 process.exit(1)）
    - 单一真相源：Plan 04 dispatchPluginDataAccess（唯一入口）；Plan 01 PLUGIN_DATA_ACCESS_REASONS（10 拒因）；governanceAudits 读断言
    - 负样本契约：68-VALIDATION.md「negative-sample contract」表（每行=拒因码 + 触发输入 + 期望 audit）
  </read_first>
  <action>
    创建 runner（shim + tsx，照搬 phase67 调用约定）。两组：
    - **合法组**：对 insert/upsert/getByIndex/count/aggregate 各跑一次合法调用，断言成功（写动词落自有表、读动词返回受治理结果）。
    - **负样本组**：对 D-08 的 10 类各构造一个触发输入，断言 `dispatchPluginDataAccess` 抛出**具体拒因码**（来自 `PLUGIN_DATA_ACCESS_REASONS`，逐一 assert）**且** governanceAudits 恰好新增一条 `decision='denied'` 且 `reasonCode` 匹配的记录：
      raw_sql（payload 夹 SQL 串）/ free_where（自由 where）/ unknown_column / unknown_table / cross_school（payload 带 schoolId）/ invalid_payload（类型错）/ unindexed_column（getByIndex 非索引列）/ lifecycle_not_executable（停用插件）/ kill_switch_rejected（kill-switch 开）/ non_school_actor（非本校 actor）。
    每个断言失败打印拒因 + 实际值并 `process.exit(1)`；全过 `exit(0)`。**断言遍历 `PLUGIN_DATA_ACCESS_REASONS` 全员**——新增拒因若无样本则 runner 报"未覆盖拒因"失败（防 D-08 漂移）。
  </action>
  <verify>
    <automated>node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase68-data-access-verbs.ts</automated>
  </verify>
  <acceptance_criteria>
    - runner 覆盖全部 10 拒因：`grep -oE "[a-z_]+_rejected|lifecycle_not_executable|kill_switch_rejected" scripts/verify-phase68-data-access-verbs.ts | sort -u | wc -l` ≥ 10
    - 每负样本断言 audit +1：`grep -c "governanceAudits" scripts/verify-phase68-data-access-verbs.ts` ≥ 1 且测试逻辑断言 denied 计数增量
    - 覆盖完备性守卫存在：runner 遍历 `PLUGIN_DATA_ACCESS_REASONS`，未覆盖即失败（`grep -c "PLUGIN_DATA_ACCESS_REASONS" ...` ≥ 1）
    - 合法五动词各一成功断言
    - runner 退出码：全过 0、任一败 1（CI 可判定）
  </acceptance_criteria>
  <done>合法五动词通过、10 类负样本各被具体拒因拒绝并审计 +1、拒因覆盖完备守卫到位。</done>
</task>

<task type="auto">
  <name>Task 2: package.json —— verify:phase68 串联 + verify:phase 别名切换</name>
  <read_first>
    - 修改文件：package.json（现有 verify:phase67 / verify:phase / plugin:compile 脚本）
    - 单一真相源：Plan 01 plugin:compile 生成 data-access-allowlist.ts（zero-drift 目标）
    - PATTERNS：phase67 verify:phase 别名模式
  </read_first>
  <action>
    在 package.json scripts：
    - 新增 `"verify:phase68": "pnpm plugin:compile && git diff --exit-code src/db/schema/generated/plugin-owned/data-access-allowlist.ts && pnpm vitest run src/features/platform-core/plugin-data-access src/features/platform-core/commands/handlers/plugin-data.test.ts && node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase68-data-access-verbs.ts"`
    - 更新 `"verify:phase": "pnpm verify:phase68"`（从 phase67 切换；保留 verify:phase67 脚本本身不删）
    zero-drift（D-06）：compile 后 git diff 非空即非零退出，闸失败。
  </action>
  <verify>
    <automated>pnpm verify:phase68</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "verify:phase68" package.json` ≥ 1
    - `verify:phase` 指向 phase68：`grep -A0 '"verify:phase"' package.json` 含 `verify:phase68`
    - 脚本含 zero-drift 门：`grep -c "git diff --exit-code" package.json` ≥ 1 且目标为 data-access-allowlist.ts
    - `pnpm verify:phase68` 端到端绿（编译/漂移/单测/负样本全过，~60s）
  </acceptance_criteria>
  <done>verify:phase68 一键串联编译/零漂移/单测/负样本；verify:phase 别名已切 phase68。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/开发者 → verify:phase68 | 验收闸；拒注入面 + 越权审计 + 单一真相源在合并前被强制验证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-18 | Tampering(Audit gap) | 负样本 runner | mitigate | 每拒绝断言"恰好一条 denied audit"，审计缺失即闸失败（D-04/D-08）— ASVS L1 V7 |
| T-68-19 | Information Disclosure(Injection 回归) | 拒因覆盖守卫 | mitigate | 遍历 PLUGIN_DATA_ACCESS_REASONS，新增拒因无样本则失败，防注入面回归（SC1）|
| T-68-20 | Tampering(Drift) | zero-drift 门 | mitigate | compile 后 git diff --exit-code，allowlist 与声明源漂移即非零退出（D-06 单一真相源）|
</threat_model>

<verification>
- `pnpm verify:phase68` 退出码 0
- 负样本 runner 单独跑：10 拒因全覆盖、每条 audit +1
- 故意改坏一个白名单列 → runner 失败（闸有效性自检，可在 SUMMARY 记录手测）
</verification>

<success_criteria>
Phase 68 三条 SC 全部由一键闸验证：SC1（具名动词、服务端常量、注入面被负样本闸守）、SC2（schoolId session 注入、越权/非法 payload 被拒并审计）、SC3（写路径单一权威 replay-safe、allowlist 零漂移）。ACCESS-01/02/03 端到端可验收。
</success_criteria>

<output>
完成后创建 `.planning/phases/68-governed-declarative-data-access-verbs/68-05-SUMMARY.md`
</output>
