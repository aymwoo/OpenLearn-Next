---
phase: 68-governed-declarative-data-access-verbs
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - src/features/platform-core/plugin-data-access/facade.ts
  - src/features/platform-core/plugin-data-access/read-verbs.ts
  - src/features/platform-core/plugin-data-access/facade.test.ts
  - src/features/platform-core/plugin-data-access/read-verbs.test.ts
autonomous: true
requirements: [ACCESS-01, ACCESS-03]
must_haves:
  truths:
    - "dispatchPluginDataAccess 是五动词的单一治理入口；按 verb 判别派发（D-01）"
    - "读动词 getByIndex/count/aggregate 直连受治理 DAL，绝不经 platformCommands（D-03）"
    - "getByIndex/groupBy 只命中声明索引列，否则拒（D-07）；aggregate 仅 count + 白名单列 groupBy → {key,count}（D-05）"
    - "读动词成功不写审计，仅 reject/越权写（D-04）；schoolId 由 session 注入并强制过滤每个查询"
  artifacts:
    - path: "src/features/platform-core/plugin-data-access/facade.ts"
      provides: "dispatchPluginDataAccess —— 单入口，写→producer，读→read-verbs"
      exports: ["dispatchPluginDataAccess"]
    - path: "src/features/platform-core/plugin-data-access/read-verbs.ts"
      provides: "getByIndex / count / aggregate 受治理只读查询"
      exports: ["getByIndex", "count", "aggregate"]
  key_links:
    - from: "src/features/platform-core/plugin-data-access/facade.ts"
      to: "producePluginDataInsert / read-verbs"
      via: "switch(verb): insert/upsert→producer, getByIndex/count/aggregate→read"
      pattern: "switch"
    - from: "src/features/platform-core/plugin-data-access/read-verbs.ts"
      to: "plugin_owned_quiz_* (SQLite)"
      via: "db.select().where(eq(schoolId) AND 白名单索引列)；无 platformCommands"
      pattern: "where"
---

<objective>
落地**统一治理入口** `dispatchPluginDataAccess`（D-01）与三个**读动词**。facade 先过 `assertActionExecutable`（治理门 + schoolId session 注入），再按 `verb` 判别派发：写动词（insert/upsert）转 Plan 03 的 producer 走 Command Bus；读动词（getByIndex/count/aggregate）走 `read-verbs.ts` 的受治理直连 DAL，**绝不**经 platformCommands（D-03）。

读动词全部对照 Plan 01 白名单：`getByIndex` 仅命中声明索引列（否则 `unindexed_column_rejected`/`unknown_column_rejected`）；`aggregate` 受限为"命名聚合"——只做 `count` + 白名单列 `groupBy`，投影 `{key,count}`，不暴露任意聚合/选项分布（D-05，正确性/分布留 Phase 70）。每个查询强制 `eq(schoolId, 派生值)`；schoolId 永不接受入参（SC2）。读成功不审计，reject/越权才审计（D-04）。

Purpose: 五动词收敛到单一治理入口；读半边无注入面、无第二命令真相源。
Output: facade、read-verbs、两组单元测试。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-governed-declarative-data-access-verbs/68-PATTERNS.md
@.planning/phases/68-governed-declarative-data-access-verbs/68-VALIDATION.md
@.planning/phases/68-governed-declarative-data-access-verbs/68-01-SUMMARY.md
@.planning/phases/68-governed-declarative-data-access-verbs/68-02-SUMMARY.md
@.planning/phases/68-governed-declarative-data-access-verbs/68-03-SUMMARY.md

<interfaces>
<!-- Plan 01 复用 —— src/features/platform-core/plugin-data-access/allowlist.ts -->
import { resolvePluginTable, assertIndexAllowed, assertGroupByAllowed, PLUGIN_DATA_ACCESS_REASONS } from "./allowlist";
<!-- Plan 02 复用 -->
import { assertActionExecutable } from "./governance-gate";   // -> { schoolId, scope, projectionRow }
import { writePluginDataAccessAudit } from "./audit";
import type { PluginDataAccessInput } from "./contracts";     // verb 判别联合
<!-- Plan 03 复用 -->
import { producePluginDataInsert, producePluginDataUpsert } from "../commands/producers/plugin-data";

<!-- 只读查询模式 —— src/lib/dal/learning.ts 索引 select -->
const rows = await db.select().from(table).where(and(eq(table.schoolId, schoolId), eq(table.<indexCol>, value)));
// count: db.select({ count: count() }).from(table).where(...)
// aggregate(D-05): db.select({ key: table[col], count: count() }).from(table).where(eq(schoolId,...)).groupBy(table[col])  -> {key,count}[]

<!-- schoolId 永不入参；由 assertActionExecutable 派生 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: read-verbs.ts —— getByIndex / count / aggregate（受治理只读）</name>
  <read_first>
    - 新建文件：src/features/platform-core/plugin-data-access/read-verbs.ts
    - 测试同建：src/features/platform-core/plugin-data-access/read-verbs.test.ts
    - PATTERNS 分析源：src/lib/dal/learning.ts 索引 select / count / groupBy
    - 单一真相源：Plan 01 allowlist（assertIndexAllowed/assertGroupByAllowed/resolvePluginTable）；Plan 02 audit（仅 reject 写）
  </read_first>
  <behavior>
    - Test: getByIndex 命中声明索引列 → 返回本校行；非索引列 → unindexed_column_rejected + denied audit
    - Test: getByIndex 含未知列 → unknown_column_rejected + denied audit；含 schoolId 入参字段 → cross_school_rejected
    - Test: count 受 eq(schoolId) 约束，仅计本校
    - Test: aggregate count+groupBy(白名单列) → `[{key,count}]`；groupBy 非白名单/reserved 列 → unknown_column_rejected
    - Test: aggregate 不接受任意聚合函数/额外列（仅 count、仅 {key,count}）
    - Edge: 所有查询强制 `eq(table.schoolId, 派生 schoolId)`；跨校数据不可见
  </behavior>
  <action>
    创建 read-verbs.ts，`import "server-only"`。导出 `getByIndex/count/aggregate(schoolId, pluginKey, table, ...)`（schoolId 为派生值，由 facade 注入，非外部入参）：
    - `getByIndex`：`assertIndexAllowed(pluginKey,table,index)` 后 `db.select().from(t).where(and(eq(t.schoolId,schoolId), ...白名单 eq 列))`。
    - `count`：`db.select({count:count()}).from(t).where(and(eq(t.schoolId,schoolId), ...可选白名单 eq))`。
    - `aggregate`（D-05）：`assertGroupByAllowed(pluginKey,table,groupBy)` 后 `db.select({key:t[groupBy], count:count()}).from(t).where(eq(t.schoolId,schoolId)).groupBy(t[groupBy])`，**只**投影 `{key,count}`。
    任一白名单失败：`writePluginDataAccessAudit(denied, reasonCode)` 后抛。读成功**不**写审计。**绝不**调 producer/Command Bus（D-03）。
  </action>
  <verify>
    <automated>pnpm vitest run src/features/platform-core/plugin-data-access/read-verbs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - read-verbs.ts 不出现 `dispatchPlatformCommand`/`produce`：`grep -nE "dispatchPlatformCommand|producePluginData" src/features/platform-core/plugin-data-access/read-verbs.ts` 无命中（D-03）
    - 每个查询含 schoolId 过滤：`grep -c "schoolId" src/features/platform-core/plugin-data-access/read-verbs.ts` ≥ 3
    - aggregate 仅 `{key,count}`：测试断言返回对象键集合恰为 `["key","count"]`
    - 测试覆盖 unindexed_column_rejected / unknown_column_rejected / cross_school_rejected 各一条 + denied audit 断言
    - `import "server-only"` 在首行
  </acceptance_criteria>
  <done>三读动词受白名单 + schoolId 约束、直连 DAL、reject 才审计；aggregate 限定命名聚合。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: facade.ts —— dispatchPluginDataAccess 单一治理入口</name>
  <read_first>
    - 新建文件：src/features/platform-core/plugin-data-access/facade.ts
    - 测试同建：src/features/platform-core/plugin-data-access/facade.test.ts
    - PATTERNS 分析源：68-PATTERNS.md「dispatchPluginDataAccess facade」判别派发伪结构
    - 单一真相源：Plan 02 contracts `PluginDataAccessInput`；gate；Plan 03 producers；Task 1 read-verbs
  </read_first>
  <behavior>
    - Test: verb="insert"/"upsert" → 调 producePluginData*（经 Command Bus），不直连 DAL 写
    - Test: verb="getByIndex"/"count"/"aggregate" → 调 read-verbs，不经 producer
    - Test: 任何 verb 先过 assertActionExecutable；lifecycle/kill-switch/越校在派发前被拒
    - Test: payload 携带 schoolId → cross_school_rejected（schoolId 仅 gate 派生注入）
    - Test: 未知 verb → 类型层不可达；运行时兜底抛 invalid_payload_rejected
  </behavior>
  <action>
    创建 facade.ts，`import "server-only"`。实现 `dispatchPluginDataAccess(input: PluginDataAccessInput)`：
    1) `const { schoolId, scope, projectionRow } = await assertActionExecutable({...})`（治理门 + schoolId 派生注入，绝不取 input.schoolId）。
    2) `switch(input.verb)`：
       - `"insert"` → `producePluginDataInsert({scope:{schoolId,pluginId}, payload, ...})`
       - `"upsert"` → `producePluginDataUpsert(...)`
       - `"getByIndex"|"count"|"aggregate"` → 对应 read-verbs 函数（传派生 schoolId）。
    写分支结果来自 Command Bus；读分支直连。facade 是五动词唯一公开入口（D-01）。
  </action>
  <verify>
    <automated>pnpm vitest run src/features/platform-core/plugin-data-access/facade.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "switch" src/features/platform-core/plugin-data-access/facade.ts` ≥ 1 且五动词分支齐全
    - facade 先治理后派发：测试断言 mock gate 抛错时 producer/read 均未被调用
    - schoolId 仅派生：`grep -nE "input\.schoolId" src/features/platform-core/plugin-data-access/facade.ts` 无命中（除拒绝判定）
    - 写分支走 producer、读分支不走 producer（测试以 spy 断言）
    - `import "server-only"` 在首行
  </acceptance_criteria>
  <done>五动词收敛到单一治理入口 dispatchPluginDataAccess；写经 Command Bus、读直连，治理前置。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 插件调用 → dispatchPluginDataAccess | 唯一公开入口；治理门 + schoolId 注入前置于任何数据触达 |
| read-verbs → SQLite | 只读边界；每查询强制本校 eq(schoolId) + 白名单列 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-14 | Information Disclosure | getByIndex/aggregate | mitigate | 仅命中声明索引/白名单 groupBy 列，越界 → unindexed/unknown_column_rejected；aggregate 仅 {key,count}（D-05/D-07）— ASVS L1 V4 |
| T-68-15 | Elevation(Cross-tenant) | 读查询 schoolId | mitigate | 每查询强制 eq(schoolId, session 派生)；payload schoolId → cross_school_rejected（SC2）|
| T-68-16 | Tampering(2nd truth) | 读路径 | mitigate | 读动词不经 platformCommands/producer，无第二命令真相源（D-03）|
| T-68-17 | Repudiation | 读审计 | mitigate | reject/越权写 denied audit；读成功不写（D-04，避免审计噪音同时保留越权留痕）|
</threat_model>

<verification>
- `pnpm vitest run src/features/platform-core/plugin-data-access` 全绿
- read-verbs 无 producer/command 依赖（grep 断言 D-03）
- aggregate 仅命名聚合（{key,count}）断言通过
</verification>

<success_criteria>
五动词收敛到单一治理入口；读动词受白名单 + 本校约束直连 DAL、无注入面、无第二真相源；aggregate 限定命名聚合。对应 SC1（具名动词、服务端常量、拒注入面）与 SC2（schoolId session 注入、越权审计）。
</success_criteria>

<output>
完成后创建 `.planning/phases/68-governed-declarative-data-access-verbs/68-04-SUMMARY.md`
</output>
