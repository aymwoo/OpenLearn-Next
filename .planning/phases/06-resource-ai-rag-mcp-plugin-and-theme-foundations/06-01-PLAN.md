---
phase: 06-resource-ai-rag-mcp-plugin-and-theme-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/lib/dto/resource-ai.ts
  - src/lib/cache-policy.ts
autonomous: true
requirements: [AI-01, AI-02, AI-03, AI-04, AI-05, AI-06, AI-07, PLUGIN-01, PLUGIN-02, PLUGIN-03, PLUGIN-04, PLUGIN-05, PLUGIN-06, PLUGIN-07]
must_haves:
  truths:
    - "D-01/D-02/D-03/D-08: teacher resources are link-and-metadata records scoped by owner, school, optional course, visibility, textbook classification, and explicit RAG eligibility"
    - "D-05/D-06/D-07: RAG has Qdrant-ready knowledge source, chunk metadata, and safe retrieval filter contracts without live embedding/search execution"
    - "D-09/D-10/D-11/D-12: AI agents share a registry, capability manifest, proposal, approval, and audit contract"
    - "D-13/D-14/D-15/D-16: MCP metadata stores server, credential reference, disabled capability, and non-sensitive audit rows"
    - "D-17/D-18/D-19/D-20/D-21: plugin and theme tables store declarative manifests, allowlisted action intent, hook anchors, kill-switch state, token registry, and audit records"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Phase 06 persistent schema tables with cascade FKs and indexes"
      contains: "export const resources = sqliteTable"
    - path: "src/lib/dto/resource-ai.ts"
      provides: "Zod DTO contracts for resources, RAG, AI, MCP, plugin, and theme"
      exports: ["ResourceCardDTOSchema", "RetrievalFilterDTOSchema", "PluginManifestSchema", "ThemeTokenRegistrySchema"]
    - path: "src/lib/cache-policy.ts"
      provides: "Phase 06 cache tag vocabulary"
      contains: "resources: (schoolId: string)"
  key_links:
    - from: "src/lib/dto/resource-ai.ts"
      to: "src/db/schema.ts"
      via: "DTO enums and field names match table contracts"
      pattern: "ResourceVisibilitySchema"
    - from: "src/lib/cache-policy.ts"
      to: "future Phase 06 Server Actions"
      via: "resource/plugin/theme cache tag helpers"
      pattern: "resource:\${id}"
---

<objective>
Create the durable contract layer for Phase 06 before any DAL, Server Action, or UI consumes it.

Purpose: The phase goal requires secure resource, AI/RAG/MCP, plugin, and theme foundations. This plan defines the shared schema, DTO, and cache vocabulary so later plans implement against exact contracts rather than inventing incompatible shapes.

Output: Drizzle tables, Zod DTOs, cache tags, local schema push.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-resource-ai-rag-mcp-plugin-and-theme-foundations/06-CONTEXT.md
@.planning/phases/06-resource-ai-rag-mcp-plugin-and-theme-foundations/06-RESEARCH.md
@src/db/schema.ts
@src/lib/dto/lesson-authoring.ts
@src/lib/dto/classroom.ts
@src/lib/cache-policy.ts

<interfaces>
Existing schema pattern:
```ts
export const classroomSessions = sqliteTable("classroomSession", { ... }, (table) => [index("classroomSessions_version_idx").on(table.version)]);
```

Existing DTO pattern:
```ts
export const ClassroomSnapshotDTOSchema = z.object({ sessionId: z.string(), version: z.number().int() });
export type ClassroomSnapshotDTO = z.infer<typeof ClassroomSnapshotDTOSchema>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 06 Drizzle schema tables</name>
  <files>src/db/schema.ts</files>
  <read_first>src/db/schema.ts, .planning/phases/06-resource-ai-rag-mcp-plugin-and-theme-foundations/06-CONTEXT.md</read_first>
  <behavior>
    - Schema exports `resources`, `knowledgeSources`, `knowledgeChunks`, `agentRegistry`, `agentProposals`, `agentAuditLogs`, `mcpServers`, `mcpCredentialRefs`, `mcpCapabilities`, `mcpAuditLogs`, `pluginRegistrations`, `pluginHookRuns`, `pluginActionAudits`, `themeTokenRegistries`, and `themeAuditLogs`.
    - Parent-owned foreign keys use `{ onDelete: "cascade" }`.
    - MCP credentials contain `credentialRef`, `provider`, `status`, and `scopesJson`, not secret material fields.
  </behavior>
  <action>Extend `src/db/schema.ts` with Phase 06 tables per D-01 through D-21. Use SQLite text JSON columns for manifests/tokens/metadata. Resource visibility enum values must be `private`, `course`, `school`; `ragEligible` defaults false. RAG chunk rows must include `sourceId`, `chunkIndex`, `textHash`, `tokenEstimate`, `payloadJson`, `metadataJson`, and `indexingStatus`. AI registry must represent `LessonAgent`, `HomeworkAgent`, `DataAgent`, `TutorAgent`, and `ParentAgent` through a shared registry table with `agentKey`, `displayName`, `capabilityManifestJson`, `featureFlag`, and `enabled`. Proposal rows must store `targetType`, `targetId`, `structuredOutputJson`, `status`, `approvalState`, `requestedById`, optional `approvedById`, and timestamps. MCP capabilities must default disabled and include allowed roles plus school/course scoping. Plugin registrations must include manifest JSON, enabled, and kill-switch state. Theme registries must store token JSON and validation status. Avoid `eval`, provider execution fields, secret value fields, and direct plugin handler paths.</action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/schema.ts` contains `export const resources = sqliteTable`.
    - `src/db/schema.ts` contains `export const knowledgeSources = sqliteTable` and `export const knowledgeChunks = sqliteTable`.
    - `src/db/schema.ts` contains `export const agentRegistry = sqliteTable` and `export const agentProposals = sqliteTable`.
    - `src/db/schema.ts` contains `export const mcpCredentialRefs = sqliteTable` and the exact token `credentialRef`.
    - `src/db/schema.ts` contains `export const pluginRegistrations = sqliteTable` and `killSwitchEnabled`.
    - `src/db/schema.ts` contains `export const themeTokenRegistries = sqliteTable`.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>All Phase 06 persistence contracts exist with cascade ownership, scoped indexes, safe defaults, and no secret/code execution fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Phase 06 DTO and validation contracts</name>
  <files>src/lib/dto/resource-ai.ts</files>
  <read_first>src/lib/dto/lesson-authoring.ts, src/lib/dto/classroom.ts, .planning/phases/06-resource-ai-rag-mcp-plugin-and-theme-foundations/06-CONTEXT.md</read_first>
  <behavior>
    - Resource DTOs expose sanitized card and form shapes for the teacher resource center.
    - Retrieval filter DTO requires safe default filters: `schoolId`, `courseId`, `visibility`, `resourceId`, `grade`, and `subject`.
    - Plugin manifest DTO rejects executable fields and only allows anchors `dashboard.widget` and `lesson.sidebar` plus actions `addStepSuggestion`, `annotateLesson`, and `createNotificationStub`.
    - Theme token DTO contains color, surface, radius, and typography mappings without arbitrary CSS/script fields.
  </behavior>
  <action>Create `src/lib/dto/resource-ai.ts`. Export Zod schemas and inferred types for `ResourceVisibilitySchema`, `ResourceClassificationSchema`, `ResourceCardDTOSchema`, `CreateResourceInputSchema`, `UpdateResourceInputSchema`, `KnowledgeSourceDTOSchema`, `KnowledgeChunkDTOSchema`, `RetrievalFilterDTOSchema`, `AgentRegistryDTOSchema`, `AgentProposalDTOSchema`, `McpServerDTOSchema`, `McpCapabilityDTOSchema`, `McpAuditDTOSchema`, `PluginManifestSchema`, `PluginRegistrationDTOSchema`, `PluginActionInputSchema`, `PluginAuditDTOSchema`, `ThemeTokenRegistrySchema`, and `ThemeRegistryDTOSchema`. Encode D-01 through D-21 directly in schema constraints: link metadata only, `ragEligible` explicit boolean default false, credential references only, disabled MCP capability default, plugin anchors/actions allowlist, and theme token registry without arbitrary CSS or scripts.</action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/dto/resource-ai.ts` contains `export const ResourceCardDTOSchema`.
    - `src/lib/dto/resource-ai.ts` contains `schoolId: z.string()` and `courseId: z.string().nullable()` in retrieval/resource-related schemas.
    - `src/lib/dto/resource-ai.ts` contains `z.enum(["dashboard.widget", "lesson.sidebar"])`.
    - `src/lib/dto/resource-ai.ts` contains `z.enum(["addStepSuggestion", "annotateLesson", "createNotificationStub"])`.
    - `src/lib/dto/resource-ai.ts` does not contain `eval`, `Function`, `script`, or `cssText` outside comments.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>Executors in later plans can import stable DTO contracts for every Phase 06 domain without reading schema internals.</done>
</task>

<task type="auto">
  <name>Task 3: Add cache tags and push Drizzle schema</name>
  <files>src/lib/cache-policy.ts, src/db/schema.ts</files>
  <read_first>src/lib/cache-policy.ts, src/db/schema.ts, package.json</read_first>
  <action>Add cache tag helpers to `cacheTags`: `resource(id) => resource:${id}`, `resources(schoolId) => resources:${schoolId}`, `agentRegistry => ai:agent-registry`, `agentProposal(id) => ai:proposal:${id}`, `mcpServer(id) => mcp:server:${id}`, `pluginRegistry => plugin:registry`, `plugin(id) => plugin:${id}`, `themeRegistry => theme:registry`, and `theme(id) => theme:${id}`. Then run `npx drizzle-kit push` after schema modifications. This is the blocking schema push required before any verification can trust live SQLite structure.</action>
  <verify>
    <automated>npx drizzle-kit push && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/cache-policy.ts` contains `resource: (id: string) => \`resource:${id}\``.
    - `src/lib/cache-policy.ts` contains `resources: (schoolId: string) => \`resources:${schoolId}\``.
    - `src/lib/cache-policy.ts` contains `pluginRegistry: 'plugin:registry'`.
    - `npx drizzle-kit push` exits 0.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>Phase 06 tables are pushed locally and cache vocabulary is ready for Server Actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DTO -> DB schema | Validated contracts become persisted resource, AI, MCP, plugin, and theme rows. |
| Plugin manifest -> registry | Untrusted declarative plugin JSON crosses into stored platform configuration. |
| MCP credential reference -> adapter | Credential references identify external secrets without exposing values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | T | `src/db/schema.ts` plugin/theme JSON columns | mitigate | Store manifests/tokens as data only; no executable handler path, arbitrary CSS, or script fields. |
| T-06-01-02 | I | `mcpCredentialRefs` | mitigate | Persist `credentialRef` only and verify schema lacks `secret`, `tokenValue`, `apiKey`, or `password` fields. |
| T-06-01-03 | E | `PluginManifestSchema` | mitigate | DTO allowlists only `dashboard.widget`, `lesson.sidebar`, `addStepSuggestion`, `annotateLesson`, and `createNotificationStub`. |
| T-06-01-04 | D | JSON metadata columns | mitigate | Zod schemas cap strings/arrays where practical and verifier checks DTO presence before implementation. |
</threat_model>

<verification>
Run `npx drizzle-kit push` and `pnpm typecheck`. Later `pnpm verify:phase6` must inspect these schema/DTO/cache contracts.
</verification>

<success_criteria>
All Phase 06 downstream plans can import stable schema exports, Zod schemas, TypeScript types, and cache tags. The local SQLite schema has been pushed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-resource-ai-rag-mcp-plugin-and-theme-foundations/06-01-SUMMARY.md`.
</output>
