---
phase: 06-resource-ai-rag-mcp-plugin-and-theme-foundations
plan: 06
type: execute
wave: 3
depends_on: [06-02, 06-03, 06-04, 06-05]
files_modified:
  - scripts/verify-phase6-foundations.ts
  - package.json
autonomous: true
requirements: [AI-01, AI-02, AI-03, AI-04, AI-05, AI-06, AI-07, PLUGIN-01, PLUGIN-02, PLUGIN-03, PLUGIN-04, PLUGIN-05, PLUGIN-06, PLUGIN-07]
must_haves:
  truths:
    - "All Phase 06 requirements and decisions D-01 through D-21 are guarded by automated source verification"
    - "Verifier proves deferred-scope items are absent outside comments"
    - "Verifier proves UI components do not import DB/DAL directly and avoid divider-heavy styling"
    - "Package exposes `pnpm verify:phase6`"
  artifacts:
    - path: "scripts/verify-phase6-foundations.ts"
      provides: "automated Phase 06 verification gate"
      contains: "Phase 6 foundations verification passed"
    - path: "package.json"
      provides: "phase verification script"
      contains: "verify:phase6"
  key_links:
    - from: "package.json"
      to: "scripts/verify-phase6-foundations.ts"
      via: "npm script"
      pattern: "tsx scripts/verify-phase6-foundations.ts"
---

<objective>
Add the final automated verification gate for Phase 06 foundations.

Purpose: This phase creates safety boundaries more than finished end-user workflows. Automated source invariants are required to prove the schema, DTO, DAL, Server Actions, UI, plugin security, MCP credential safety, RAG filters, and deferred-scope exclusions remain intact.

Output: `pnpm verify:phase6` script and passing verification.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-resource-ai-rag-mcp-plugin-and-theme-foundations/06-CONTEXT.md
@.planning/phases/06-resource-ai-rag-mcp-plugin-and-theme-foundations/06-RESEARCH.md
@scripts/verify-phase5-classroom.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Phase 06 source invariant verifier</name>
  <files>scripts/verify-phase6-foundations.ts</files>
  <read_first>scripts/verify-phase5-classroom.ts, src/db/schema.ts, src/lib/dto/resource-ai.ts, src/lib/dal/resources.ts, src/lib/dal/ai-rag.ts, src/lib/dal/mcp.ts, src/lib/dal/plugins.ts, src/lib/dal/themes.ts, src/actions/resource-actions.ts, src/actions/ai-rag-actions.ts, src/actions/mcp-actions.ts, src/actions/plugin-actions.ts, src/components/surfaces/library-surface.tsx, src/components/surfaces/admin-surface.tsx</read_first>
  <behavior>
    - Verifier checks D-01 through D-21 through concrete source markers.
    - Verifier ignores line comments when checking deferred-scope and forbidden tokens.
    - Verifier fails if UI imports DB/DAL/schema or uses divider-heavy classes.
  </behavior>
  <action>Create `scripts/verify-phase6-foundations.ts` using the style of `scripts/verify-phase5-classroom.ts`. Implement helpers `read`, `withoutLineComments`, `nonCommentIncludes`, `noForbiddenUiImports`, `noForbiddenPluginExecution`, `noSecretPersistence`, and `noDeferredScopeTokens`. Read all Phase 06 implementation files. Add checks for: schema table exports; DTO schema exports; resource DAL server-only and scoped; resource UI no `resourceCards`; AI registry five agents and approval requirement; retrieval helper includes required filters; AI DAL/action provider-free; MCP registry/DAL/action credentialRef-only and no external calls; plugin allowlist/anchors/kill switch/audit and no `eval`/dynamic import; theme compiler Lexend/no-line/CSS-variable only; admin UI safety copy; package script wiring. Deferred tokens to reject outside comments: `PDF parsing`, `embedding generation`, `real Qdrant`, `TutorAgent chat`, `ParentAgent summaries`, `autonomous AI classroom control`, `plugin marketplace`, `arbitrary CSS`, `eval(`, `new Function`.</action>
  <verify>
    <automated>pnpm exec tsx scripts/verify-phase6-foundations.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-phase6-foundations.ts` contains `withoutLineComments`.
    - `scripts/verify-phase6-foundations.ts` contains `noForbiddenUiImports`.
    - `scripts/verify-phase6-foundations.ts` contains `noForbiddenPluginExecution`.
    - `scripts/verify-phase6-foundations.ts` contains `Phase 6 foundations verification passed`.
    - `pnpm exec tsx scripts/verify-phase6-foundations.ts` exits 0 after implementation files exist.
  </acceptance_criteria>
  <done>Phase 06 has a deterministic verifier that catches scope, security, and wiring regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire package script and run final gates</name>
  <files>package.json, scripts/verify-phase6-foundations.ts</files>
  <read_first>package.json, scripts/verify-phase6-foundations.ts</read_first>
  <action>Add `"verify:phase6": "tsx scripts/verify-phase6-foundations.ts"` to `package.json` scripts without removing existing scripts. Run `pnpm typecheck` and `pnpm verify:phase6`. If verifier fails because a previous Phase 06 plan missed an invariant, fix the implementation file that violates the invariant rather than weakening the check.</action>
  <verify>
    <automated>pnpm typecheck && pnpm verify:phase6</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"verify:phase6": "tsx scripts/verify-phase6-foundations.ts"`.
    - `pnpm typecheck` exits 0.
    - `pnpm verify:phase6` exits 0 and prints `Phase 6 foundations verification passed`.
  </acceptance_criteria>
  <done>Phase 06 final automated verification is wired and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source verifier -> phase completion | Automated checks decide whether safety contracts are complete. |
| Package script -> developer workflow | `pnpm verify:phase6` becomes the regression gate for execution and future changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | T | `scripts/verify-phase6-foundations.ts` | mitigate | Use non-comment source filtering for forbidden-token checks so comments do not self-trigger false failures. |
| T-06-06-02 | R | Phase completion evidence | mitigate | Verifier prints a deterministic pass/fail message and lists failed checks. |
| T-06-06-03 | I | UI imports | mitigate | Verifier rejects DB/schema/DAL imports in UI components. |
| T-06-06-04 | E | Plugin execution regression | mitigate | Verifier rejects `eval`, `new Function`, and dynamic import in plugin registry/DAL/action files. |
</threat_model>

<verification>
Run `pnpm typecheck && pnpm verify:phase6`.
</verification>

<success_criteria>
All AI-01 through AI-07 and PLUGIN-01 through PLUGIN-07 Phase 06 implementation contracts are covered by one final automated verification gate.
</success_criteria>

<output>
After completion, create `.planning/phases/06-resource-ai-rag-mcp-plugin-and-theme-foundations/06-06-SUMMARY.md`.
</output>
