---
phase: 06-resource-ai-rag-mcp-plugin-and-theme-foundations
plan: 04
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/server/mcp/registry.ts
  - src/lib/dal/mcp.ts
  - src/actions/mcp-actions.ts
autonomous: true
requirements: [AI-06]
must_haves:
  truths:
    - "D-13: MCP foundations are metadata plus server-side adapter boundary only, with no live external connector calls"
    - "D-14: MCP credential records store references only, not secret values"
    - "D-15: MCP capabilities are disabled by default and require explicit authorization with role and school/course scope"
    - "D-16: MCP audit logs are request-level, non-sensitive summaries"
  artifacts:
    - path: "src/server/mcp/registry.ts"
      provides: "server-side MCP adapter boundary and supported capability registry"
      contains: "credentialRef"
    - path: "src/lib/dal/mcp.ts"
      provides: "server-only MCP metadata and audit DAL"
      contains: "import \"server-only\";"
    - path: "src/actions/mcp-actions.ts"
      provides: "validated MCP registration and capability actions"
      contains: "registerMcpServerAction"
  key_links:
    - from: "src/actions/mcp-actions.ts"
      to: "src/lib/dal/mcp.ts"
      via: "Server Actions call DAL registration/capability functions"
      pattern: "registerMcpServer"
    - from: "src/lib/dal/mcp.ts"
      to: "src/server/mcp/registry.ts"
      via: "DAL validates server/capability metadata against supported registry"
      pattern: "MCP_SUPPORTED_CAPABILITIES"
---

<objective>
Implement the MCP metadata, credential reference, capability authorization, and audit boundary without real connector execution.

Purpose: AI-06 requires MCP server, credential, capability, and audit-log tables behind a server-side adapter boundary. The boundary must be safe for future Moodle/GitHub/Notion/WeCom/DingTalk connectors without storing secrets or making external calls in Phase 06.

Output: MCP server registry, server-only DAL, and validated Server Actions.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-resource-ai-rag-mcp-plugin-and-theme-foundations/06-CONTEXT.md
@.planning/phases/06-resource-ai-rag-mcp-plugin-and-theme-foundations/06-RESEARCH.md
@src/lib/dto/resource-ai.ts
@src/db/schema.ts
@src/lib/dal/lesson-authoring.ts
@src/actions/classroom-actions.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MCP adapter registry boundary</name>
  <files>src/server/mcp/registry.ts</files>
  <read_first>src/lib/dto/resource-ai.ts, .planning/phases/06-resource-ai-rag-mcp-plugin-and-theme-foundations/06-CONTEXT.md</read_first>
  <behavior>
    - Registry describes supported server providers and capabilities as metadata only.
    - Registry uses `credentialRef` and never asks for `secret`, `tokenValue`, `apiKey`, or `password`.
    - Registry exports a disabled-by-default capability seed.
  </behavior>
  <action>Create `src/server/mcp/registry.ts`. Export `MCP_SUPPORTED_PROVIDERS`, `MCP_SUPPORTED_CAPABILITIES`, `createMcpCapabilitySeed(serverId, schoolId, courseId?)`, and `assertNoSecretMaterial(input)`. Include providers `moodle`, `github`, `notion`, `wecom`, and `dingtalk` as metadata labels only. Capability seed entries must include `enabled: false`, `allowedRoles: ["teacher", "admin", "developer"]`, `schoolId`, optional `courseId`, and a non-sensitive summary. Do not import SDKs, call `fetch`, or reference OAuth flows.</action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/server/mcp/registry.ts` contains `MCP_SUPPORTED_PROVIDERS`.
    - `src/server/mcp/registry.ts` contains `credentialRef`.
    - `src/server/mcp/registry.ts` contains `enabled: false`.
    - `src/server/mcp/registry.ts` does not contain `fetch(`, `apiKey`, `password`, `tokenValue`, or `secret` outside comments except inside `assertNoSecretMaterial` forbidden-key checks.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>MCP adapter boundary is represented as metadata-only TypeScript registry with no external calls or secret persistence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement server-only MCP DAL and audits</name>
  <files>src/lib/dal/mcp.ts</files>
  <read_first>src/lib/dal/lesson-authoring.ts, src/lib/dto/resource-ai.ts, src/server/mcp/registry.ts, src/db/schema.ts</read_first>
  <behavior>
    - DAL registers MCP servers and credential references after teacher/developer scope validation.
    - DAL creates disabled capabilities by default.
    - DAL records request-level audit logs with denied reason and non-sensitive summary.
  </behavior>
  <action>Create `src/lib/dal/mcp.ts` with `import "server-only";`. Export `registerMcpServer(input)`, `registerMcpCredentialRef(input)`, `setMcpCapabilityEnabled(input)`, `getMcpRegistryDTO()`, and `recordMcpAudit(input)`. Use `assertActiveTeacher()` for Phase 06 local authorization unless an existing admin/developer scope helper exists; keep allowed roles in capability rows for future expansion. Call `assertNoSecretMaterial()` before storing credential metadata. Default new capabilities to disabled. Audit logs must include `actorId`, `serverId`, `capabilityId`, `targetType`, `targetId`, `status`, `deniedReason`, and `summary`, but not full sensitive payloads.</action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/dal/mcp.ts` starts with `import "server-only";`.
    - `src/lib/dal/mcp.ts` contains `assertNoSecretMaterial`.
    - `src/lib/dal/mcp.ts` contains `enabled: false`.
    - `src/lib/dal/mcp.ts` contains `deniedReason` and `summary`.
    - `src/lib/dal/mcp.ts` does not contain `fetch(`, `apiKey`, `password`, `tokenValue`, or `secret` outside comments except forbidden-key checks.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>MCP metadata and audits are persisted through a server-only boundary with credential references only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add MCP Server Actions</name>
  <files>src/actions/mcp-actions.ts</files>
  <read_first>src/actions/classroom-actions.ts, src/lib/dal/mcp.ts, src/lib/dto/resource-ai.ts, src/lib/cache-policy.ts</read_first>
  <behavior>
    - Actions validate MCP registration, credential reference, and capability enablement input.
    - Successful changes call `updateTag(cacheTags.mcpServer(serverId))` when possible.
    - Actions never accept or persist raw secret values.
  </behavior>
  <action>Create `src/actions/mcp-actions.ts` with `"use server";`. Export `registerMcpServerAction`, `registerMcpCredentialRefAction`, `setMcpCapabilityEnabledAction`, and `recordMcpAuditAction`. Use Zod schemas from `resource-ai.ts` or focused local schemas with exact fields. Reject input containing keys `secret`, `tokenValue`, `apiKey`, `password`, or `clientSecret`. Return messages `MCP 配置信息不完整，请检查后再保存。` and `MCP 元数据操作失败，请重试。`. On success call `updateTag(cacheTags.mcpServer(serverId))` and return sanitized DTO data.</action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/actions/mcp-actions.ts` starts with `"use server";`.
    - `src/actions/mcp-actions.ts` contains `registerMcpServerAction` and `setMcpCapabilityEnabledAction`.
    - `src/actions/mcp-actions.ts` contains `safeParse`.
    - `src/actions/mcp-actions.ts` contains `updateTag(cacheTags.mcpServer`.
    - `src/actions/mcp-actions.ts` contains `credentialRef`.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>MCP setup changes are validated, cached, and secret-safe through Server Actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP registration input -> DB | Untrusted connector metadata enters persistent config. |
| Credential reference -> future vault/env | Stored references point to secrets not stored in SQLite. |
| Capability enablement -> future adapter | Disabled-by-default capability flags control future external tool access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | I | `mcpCredentialRefs` | mitigate | Reject secret-like keys and store `credentialRef` only. |
| T-06-04-02 | E | `setMcpCapabilityEnabled` | mitigate | Capabilities default disabled and carry allowed roles plus school/course scope. |
| T-06-04-03 | R | `mcpAuditLogs` | mitigate | Record actor/server/capability/target/status/denied reason/non-sensitive summary. |
| T-06-04-04 | T | Provider registry | mitigate | Static provider registry contains metadata only and no executable connector calls. |
</threat_model>

<verification>
Run `pnpm typecheck`. Plan 06-06 verifies absence of real external calls and secret fields.
</verification>

<success_criteria>
AI-06 is covered with MCP server metadata, credential references, disabled capability records, audit logs, and a server-side adapter boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/06-resource-ai-rag-mcp-plugin-and-theme-foundations/06-04-SUMMARY.md`.
</output>
