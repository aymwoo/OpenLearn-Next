---
phase: 70-question-stats-post-class-recap
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/lib/dal/classroom.ts
  - src/lib/dto/classroom.ts
  - src/actions/classroom-actions.ts
  - src/actions/classroom-actions.test.ts
  - src/lib/dal/classroom.test.ts
autonomous: true
requirements: [STATS-01]
must_haves:
  truths:
    - "getClassroomSessionRecapDTO 能按需返回 quiz sample stats section"
    - "submitQuizSampleAnswerAction 刷新 quizStats(sessionId)"
    - "classroomSessionSummary artifact 不承载 quiz stats durable payload"
---

<objective>
把 quiz sample 统计正式接进 ended-session recap 主链，但保持它是 live read model，而不是新的 durable analytics 表写入。
</objective>

<context>
@.planning/phases/70-question-stats-post-class-recap/70-CONTEXT.md
@.planning/phases/70-question-stats-post-class-recap/70-RESEARCH.md
@.planning/phases/70-question-stats-post-class-recap/70-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 把 quiz sample stats 接入 getClassroomSessionRecapDTO</name>
  <files>src/lib/dal/classroom.ts, src/lib/dal/classroom.test.ts, src/lib/dto/classroom.ts</files>
  <action>
    在 `getClassroomSessionRecapDTO` 的 ended-session path 中接入 Phase 70-01 的 quiz sample aggregate helper，输出新的 recap DTO section。保持 `executeClassroomSessionSummaryTask` / `ClassroomSessionSummaryArtifactSchema` 不扩写 quiz stats 字段。
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/dal/classroom.test.ts -t "quiz sample recap dto"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 提交答案后刷新 quiz stats cache</name>
  <files>src/actions/classroom-actions.ts, src/actions/classroom-actions.test.ts, src/lib/cache-policy.ts</files>
  <action>
    在 `submitQuizSampleAnswerAction` 成功路径中新增 `updateTag(cacheTags.quizStats(parsed.data.sessionId))`。不要影响现有 `classroom / progress / submission / teacherReview` invalidation。
  </action>
  <verify>
    <automated>pnpm vitest run src/actions/classroom-actions.test.ts src/lib/cache-policy.test.ts</automated>
  </verify>
</task>

</tasks>

<success_criteria>
Quiz sample stats 已经进入现有 recap DTO seam，并在学生作答后具备明确的新鲜度刷新路径。
</success_criteria>

<output>
完成后创建 `.planning/phases/70-question-stats-post-class-recap/70-02-SUMMARY.md`
</output>
