---
phase: 69-interactive-single-choice-quiz-sample-plugin
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - src/components/learning/classroom-runtime-client.tsx
  - src/components/learning/quiz-sample-step-card.tsx
  - src/actions/classroom-actions.ts
  - src/lib/dal/classroom.ts
  - src/features/runtime-platform/classroom/runtime-session.ts
  - src/components/learning/classroom-runtime-client.test.tsx
  - src/components/learning/quiz-sample-step-card.test.tsx
  - src/lib/dal/classroom.test.ts
  - src/features/runtime-platform/classroom/runtime-session.test.ts
autonomous: true
requirements: [QUIZ-02, QUIZ-03]
must_haves:
  truths:
    - "学生作答写入 plugin_owned_quiz_responses，经受治理动词 append-only/isLatest（QUIZ-02）"
    - "quiz sample path 不写 core quizAttempts，不 import UI->DB 直连（QUIZ-03）"
    - "开放时可改答，关闭/切走后只读，latest 一票语义在 UI 与持久层一致（D-08/D-09/D-10）"
  artifacts:
    - path: "src/components/learning/classroom-runtime-client.tsx"
      provides: "runtime container routing for quiz sample step"
      contains: "quiz sample"
    - path: "src/components/learning/quiz-sample-step-card.tsx"
      provides: "student quiz sample answer card"
      contains: "提交答案"
    - path: "src/actions/classroom-actions.ts"
      provides: "submitQuizSampleAnswerAction server boundary"
      contains: "submitQuizSampleAnswerAction"
    - path: "src/features/runtime-platform/classroom/runtime-session.ts"
      provides: "governed runtime submit integration"
      contains: "dispatchPluginDataAccess"
  key_links:
    - from: "src/components/learning/classroom-runtime-client.tsx"
      to: "src/components/learning/quiz-sample-step-card.tsx"
      via: "quiz sample step routing"
      pattern: "formal answer card"
    - from: "src/components/learning/quiz-sample-step-card.tsx"
      to: "src/actions/classroom-actions.ts"
      via: "submit/update answer CTA -> submitQuizSampleAnswerAction"
      pattern: "explicit governed submit wiring"
    - from: "src/actions/classroom-actions.ts"
      to: "src/lib/dal/classroom.ts"
      via: "submitQuizSampleAnswerAction -> submitQuizSampleAnswer"
      pattern: "open-window validation"
    - from: "src/lib/dal/classroom.ts"
      to: "src/features/platform-core/plugin-data-access/facade.ts"
      via: "submitQuizSampleAnswer -> dispatchPluginDataAccess"
      pattern: "plugin_owned_quiz_responses"
    - from: "src/lib/dal/classroom.ts"
      to: "src/lib/cache-policy.ts"
      via: "cacheTags.classroom/progress/submission/teacherReview"
      pattern: "readonly gating + latest answer refresh"
---

<objective>
交付学生侧闭环：在现有 classroom/player 容器内渲染 quiz sample 的独立答题卡，并把提交改成治理可见的 plugin-owned answer persistence。该 plan 是 QUIZ-02 的核心实现，必须彻底切断 quiz sample 对 core `quizAttempts` 的依赖。

Purpose: 打通学生看到正式答题卡 -> 选择答案 -> 提交/更新答案 -> plugin-owned append-only latest persistence 的整条链。
Output: student answer card、submit boundary、runtime integration/no-core-backdoor behavior。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-CONTEXT.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-UI-SPEC.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-RESEARCH.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-PATTERNS.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-VALIDATION.md

<interfaces>
dispatchPluginDataAccess
classroom-runtime-client.tsx
quiz-step-card.tsx (current core analog)
submitRuntimeState / classroom submit actions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 quiz sample 学生答题卡并接入 runtime 容器</name>
  <read_first>
    - 修改文件：src/components/learning/classroom-runtime-client.tsx
    - 新建或替换：src/components/learning/quiz-sample-step-card.tsx
    - UI 来源：69-UI-SPEC.md Student UI Contract
    - 参考：quiz-step-card.tsx 的交互骨架
  </read_first>
  <files>src/components/learning/classroom-runtime-client.tsx, src/components/learning/quiz-sample-step-card.tsx, src/components/learning/classroom-runtime-client.test.tsx, src/components/learning/quiz-sample-step-card.test.tsx</files>
  <behavior>
    - Test: quiz sample step 在 classroom/player 容器中显示正式答题卡，而不是复用旧 quiz card 逻辑
    - Test: 2-4 个选项整卡可点，点击不自动提交，必须显式点击“提交答案/更新答案”
    - Test: 开放中/已关闭/已作答三种状态文案与按钮行为正确
  </behavior>
  <action>
    新建 quiz sample 答题卡组件（或等价拆分），在 `classroom-runtime-client.tsx` 中识别 quiz sample step 并路由到该组件。视觉/文案按 `69-UI-SPEC.md` 实现，保留现有 classroom/player 容器，不重写整页。避免继续直接复用 `QuizStepCard` 的 core quiz feedback/history 语义。
  </action>
  <verify>
    <automated>pnpm vitest run src/components/learning/classroom-runtime-client.test.tsx src/components/learning/quiz-sample-step-card.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - quiz sample step 使用独立答题卡组件
    - 点击选项不会自动提交
    - 关闭后卡片立即只读，按钮禁用，状态文案切换到冻结态
  </acceptance_criteria>
  <done>学生端有独立、符合 UI-SPEC 的 quiz sample 答题卡。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 提交答案走 governed plugin-owned writes，彻底切断 core quiz backdoor</name>
  <read_first>
    - 修改文件：src/actions/classroom-actions.ts、src/lib/dal/classroom.ts、必要时 `src/features/runtime-platform/classroom/runtime-session.ts`
    - 参考：Phase 68 `dispatchPluginDataAccess`、append-only/isLatest handler
    - 现状对照：`QuizStepCard` / `submitRuntimeState` 仍会写 core `quizAttempts`
  </read_first>
  <files>src/components/learning/quiz-sample-step-card.tsx, src/actions/classroom-actions.ts, src/lib/dal/classroom.ts, src/features/runtime-platform/classroom/runtime-session.ts, src/lib/dal/classroom.test.ts, src/features/runtime-platform/classroom/runtime-session.test.ts, src/components/learning/quiz-sample-step-card.test.tsx</files>
  <behavior>
    - Test: 学生提交答案后 `plugin_owned_quiz_responses` 增加新行，`attemptNo` 递增，最新答案 `isLatest=true`
    - Test: 改答时旧 latest 被撤销，新 latest 生效
    - Test: quiz sample 路径不会创建 core `quizAttempts` 行
    - Test: 关闭或切走后再提交被拒；开放时允许改答
    - Test: governance audit 可看到相应 plugin-owned write path
  </behavior>
  <action>
    锁定唯一学生提交路线，不保留备选实现：`quiz-sample-step-card.tsx` 的“提交答案/更新答案”按钮直接调用 `submitQuizSampleAnswerAction`，后者再进入 `submitQuizSampleAnswer` -> `dispatchPluginDataAccess`。其中 Server Action 只做 actor/session 校验，DAL helper 只做课堂开放窗口与 latest 语义校验，最终写入 `plugin_owned_quiz_responses`。读模型/UI 基于 latest answer 回显提交状态，并在成功提交后显式刷新 `cacheTags.classroom(sessionId)`、`cacheTags.progress(lessonId, userId)`、`cacheTags.submission(lessonId, userId)`、`cacheTags.teacherReview(lessonId)`。禁止继续走 core quiz runtime submit bridge，也禁止写 `quizAttempts`。
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/dal/classroom.test.ts src/features/runtime-platform/classroom/runtime-session.test.ts src/components/learning/classroom-runtime-client.test.tsx src/components/learning/quiz-sample-step-card.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 提交链最终落到 `plugin_owned_quiz_responses`
    - `quiz-sample-step-card.tsx` 的提交/更新按钮直接接到 `submitQuizSampleAnswerAction`
    - quiz sample 提交不会新增 core `quizAttempts`
    - append-only / `isLatest` / latest-one-vote semantics 在测试中成立
    - 提交成功后课堂、个人进度、个人提交、teacher review cache tags 被显式刷新
    - governance-visible write path 可被 close gate 后续断言
  </acceptance_criteria>
  <done>学生作答已通过受治理 plugin-owned 写路径落库，且 latest 语义与 UI 完全对齐。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| student UI -> submit boundary | explicit submit only; no implicit auto-persist |
| submit boundary -> storage | must go through governed plugin-owned facade, never core quiz tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-10 | Tampering | core quiz backdoor | mitigate | assert quiz sample submit path never writes `quizAttempts` |
| T-69-11 | Repudiation | answer history | mitigate | append-only rows + `isLatest` semantics via Phase 68 handler |
| T-69-12 | Elevation | closed-round resubmission | mitigate | enforce classroom open/closed state in submit boundary and UI |
</threat_model>

<verification>
- learning UI tests 通过
- classroom/runtime integration tests 通过
- plugin-owned response rows vs core quiz rows assertions 通过
</verification>

<success_criteria>
学生端形成完整 quiz sample 作答闭环：看题、选项、提交、改答、关闭冻结都围绕 plugin-owned latest truth 运转，且整个路径无 built-in 后门。
</success_criteria>

<output>
完成后创建 `.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-04-SUMMARY.md`
</output>
