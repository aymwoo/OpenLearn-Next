---
phase: 69-interactive-single-choice-quiz-sample-plugin
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/lib/dal/classroom.ts
  - src/actions/classroom-actions.ts
  - src/components/classroom/classroom-control-panel.tsx
  - src/lib/dal/classroom.test.ts
  - src/components/classroom/classroom-control-panel.test.tsx
autonomous: true
requirements: [QUIZ-02, QUIZ-03]
must_haves:
  truths:
    - "开课时按 classroomSession 冻结完整题面到 plugin_owned question 表（D-02/D-03）"
    - "课堂开放/关闭控制能驱动 quiz sample 的可作答状态，且切走/关闭后不再允许改答（D-09）"
    - "launch / control 仍经现有 teacher auth + classroom shell，不造第二套课堂系统"
  artifacts:
    - path: "src/lib/dal/classroom.ts"
      provides: "launch freeze helper + classroom state assembly"
      contains: "freezeQuizSampleQuestionsForSession"
    - path: "src/actions/classroom-actions.ts"
      provides: "teacher control fallback action boundary"
      contains: "quiz sample"
    - path: "src/components/classroom/classroom-control-panel.tsx"
      provides: "quiz sample open/close controls in existing classroom shell"
      contains: "开放作答"
  key_links:
    - from: "src/lib/dal/classroom.ts"
      to: "src/features/platform-core/plugin-data-access/facade.ts"
      via: "launchClassroomSession -> freezeQuizSampleQuestionsForSession -> dispatchPluginDataAccess"
      pattern: "plugin_owned_quiz_questions"
    - from: "src/components/classroom/classroom-control-panel.tsx"
      to: "src/actions/classroom-actions.ts"
      via: "teacher control CTA"
      pattern: "quiz sample open/close"
    - from: "src/actions/classroom-actions.ts"
      to: "src/lib/dal/classroom.ts"
      via: "teacher control fallback -> classroom state DTO"
      pattern: "cacheTags.classroom(sessionId)"
---

<objective>
把 quiz sample 接入课堂权威链路：开课时冻结 session-scoped question snapshot；教师端继续用现有 classroom control transport/fallback 模式控制 quiz sample 题目的开放/关闭状态。这个 plan 不负责最终学生答题卡渲染，但要把 launch truth 和 control truth 建好。

Purpose: 让课堂一开始就拥有独立于备课配置的 plugin-owned question snapshot，并让老师控课状态成为学生能否改答的单一判定来源。
Output: launch freeze helper + classroom DTO / control integration。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-CONTEXT.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-UI-SPEC.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-RESEARCH.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-PATTERNS.md
@.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-VALIDATION.md

<interfaces>
launchClassroomSession
recordClassroomVotingRoundControl / recordRuntimeTeacherControlAction
dispatchPluginDataAccess
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 launchClassroomSession 冻结 quiz sample question snapshot</name>
  <read_first>
    - 修改文件：src/lib/dal/classroom.ts
    - 参考：现有 launch flow、Phase 68 facade、question schema 扩展后的列
  </read_first>
  <files>src/lib/dal/classroom.ts, src/lib/dal/classroom.test.ts</files>
  <behavior>
    - Test: launch 时每个 quiz sample step 都写入一条 `plugin_owned_quiz_questions` session-scoped row
    - Test: question row 包含 prompt + 启用的 option A-D 文本 + correctOption
    - Test: 已开 session 后再改备课配置，不影响既有 question snapshot
    - Test: plugin readiness 不通过则 launch 失败，不产生半完成 snapshot
  </behavior>
  <action>
    在 `launchClassroomSession` 中新增明确 helper `freezeQuizSampleQuestionsForSession`：从 lesson step shell + plugin step extension 取 authoring truth，构造完整题面快照，并通过 Phase 68 受治理写路径落入 `plugin_owned_quiz_questions`。让 question freeze 与 session 创建处于同一事务/同一 launch authority boundary，避免 session 已建但 question snapshot 缺失；launch 成功后只允许课堂读模型依赖冻结后的 question rows，而不是回读 authoring truth。
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/dal/classroom.test.ts -t "launch quiz sample freeze"</automated>
  </verify>
  <acceptance_criteria>
    - launch 成功后 question snapshot rows 可按 `(classroomSession, question)` 查询到
    - row 内容不依赖后续回读 plugin step extension 才能解释题面
    - launch 失败时不会残留半截 snapshot 数据
    - launch 完成后课堂读取面只消费 session snapshot，不重新拼 authoring 配置
  </acceptance_criteria>
  <done>classroom session 与 quiz sample question snapshot 已在开课权威边界冻结。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把 quiz sample open/close 接入现有课堂控制语义</name>
  <read_first>
    - 修改文件：src/lib/dal/classroom.ts、src/actions/classroom-actions.ts、src/components/classroom/classroom-control-panel.tsx
    - 参考：voting round opened/closed artifact 与 teacher control fallback
    - UI 来源：69-UI-SPEC.md Classroom State Contract
  </read_first>
  <files>src/lib/dal/classroom.ts, src/actions/classroom-actions.ts, src/components/classroom/classroom-control-panel.tsx, src/components/classroom/classroom-control-panel.test.tsx, src/lib/dal/classroom.test.ts</files>
  <behavior>
    - Test: 老师端能对 quiz sample 当前题触发“开放作答 / 已关闭”控制
    - Test: 关闭后学生侧所需 DTO 变只读；切走 active step 也不再允许改答
    - Test: 仍复用当前课堂 socket / fallback server action，不引入第二套 control endpoint
  </behavior>
  <action>
    让 classroom snapshot / control panel 对 quiz sample 复用现有 round artifact 模式，并暴露 quiz sample 所需最小状态（开放中、已关闭、已作答口径依赖的状态字段）。保留现有 socket teacher control 与 fallback action；只在判定和展示层补 quiz sample 的 DTO / 文案 / CTA。open/close 相关 mutation 必须显式刷新 `cacheTags.classroom(sessionId)`，确保教师控台与学生课堂态看到同一 authoritative state。
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/dal/classroom.test.ts src/components/classroom/classroom-control-panel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - teacher control 仍走既有 transport/fallback path
    - quiz sample 状态可明确区分开放中 / 已关闭
    - 当前 active step 切走后 quiz sample 的可改答窗口结束
    - open/close 后课堂态 cache 通过 `cacheTags.classroom(sessionId)` 刷新，不依赖本地 UI 猜测
  </acceptance_criteria>
  <done>quiz sample classroom control truth 已对齐现有课堂控制系统。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| launch authority -> plugin-owned question rows | question snapshot must be created atomically with session truth |
| teacher control -> student editability | editability only derives from classroom state, not local UI assumptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-07 | Tampering | launch freeze | mitigate | use launch-time authority boundary; avoid later lazy snapshot generation |
| T-69-08 | Repudiation | control semantics | mitigate | reuse existing round artifacts and classroom events rather than hidden local flags |
| T-69-09 | Elevation | disabled plugin launch | mitigate | fail launch on plugin readiness / lifecycle incompatibility before session is usable |
</threat_model>

<verification>
- classroom launch/control focused tests 通过
- launch freeze behavior can be asserted from plugin-owned question rows
</verification>

<success_criteria>
开课即建立 authoritative question snapshot，老师控课能明确控制 quiz sample 的作答开放窗口，为后续学生提交提供单一状态真相。
</success_criteria>

<output>
完成后创建 `.planning/phases/69-interactive-single-choice-quiz-sample-plugin/69-03-SUMMARY.md`
</output>
