// AUTO-GENERATED by scripts/compile-plugin-data-model.ts — DO NOT EDIT.
// Source: plugins/quiz-sample/data-model.ts

import { sqliteTable, text, integer, index, uniqueIndex } from "drizzle-orm/sqlite-core";
import { schools, pluginRegistrations } from "../../../schema";

export const pluginOwnedQuizQuestions = sqliteTable(
  "plugin_owned_quiz_questions",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    schoolId: text("schoolId").notNull().references(() => schools.id, { onDelete: "cascade" }),
    pluginId: text("pluginId").notNull().references(() => pluginRegistrations.id, { onDelete: "cascade" }),
    classroomSession: text("classroomSession").notNull(),
    question: text("question").notNull(),
    prompt: text("prompt").notNull(),
    optionAText: text("optionAText").notNull(),
    optionBText: text("optionBText").notNull(),
    optionCText: text("optionCText"),
    optionDText: text("optionDText"),
    questionType: text("questionType", { enum: ["single_choice", "multi_choice", "true_false", "fill_blank", "ordering"] }).notNull().default("single_choice"),
    correctOption: text("correctOption", { enum: ["A", "B", "C", "D"] }).notNull(),
    createdAt: integer("createdAt", { mode: "timestamp_ms" }).$defaultFn(() => new Date()),
    updatedAt: integer("updatedAt", { mode: "timestamp_ms" }).$defaultFn(() => new Date()),
  },
  (table) => [
    index("plugin_owned_quiz_questions_schoolId_classroomSession_question_idx").on(table.schoolId, table.classroomSession, table.question),
  ],
);

export const pluginOwnedQuizResponses = sqliteTable(
  "plugin_owned_quiz_responses",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    schoolId: text("schoolId").notNull().references(() => schools.id, { onDelete: "cascade" }),
    pluginId: text("pluginId").notNull().references(() => pluginRegistrations.id, { onDelete: "cascade" }),
    classroomSession: text("classroomSession").notNull(),
    student: text("student").notNull(),
    question: text("question").notNull(),
    selectedOption: text("selectedOption", { enum: ["A", "B", "C", "D"] }).notNull(),
    attemptNo: integer("attemptNo").notNull(),
    isLatest: integer("isLatest", { mode: "boolean" }).notNull().default(true),
    createdAt: integer("createdAt", { mode: "timestamp_ms" }).$defaultFn(() => new Date()),
    updatedAt: integer("updatedAt", { mode: "timestamp_ms" }).$defaultFn(() => new Date()),
  },
  (table) => [
    index("plugin_owned_quiz_responses_schoolId_classroomSession_student_question_idx").on(table.schoolId, table.classroomSession, table.student, table.question),
    uniqueIndex("plugin_owned_quiz_responses_classroomSession_student_question_attemptNo_unique").on(table.classroomSession, table.student, table.question, table.attemptNo),
    index("plugin_owned_quiz_responses_classroomSession_student_question_isLatest_idx").on(table.classroomSession, table.student, table.question, table.isLatest),
  ],
);
