---
phase: 32-end-to-end-hardening-and-milestone-proof
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(classroom)/classroom/page.tsx
  - src/components/classroom/classroom-live-snapshot-refresh.tsx
  - src/components/classroom/classroom-live-snapshot-refresh.test.tsx
  - src/components/classroom/classroom-control-panel.tsx
  - src/components/surfaces/classroom-console-surface.test.tsx
  - scripts/verify-phase32-end-to-end.ts
  - .planning/phases/32-end-to-end-hardening-and-milestone-proof/32-VALIDATION.md
autonomous: true
requirements: [RHOST-04]
gap_closure: true
must_haves:
  truths:
    - "per D-07、D-11、D-12，教师在 `/classroom` 就能先看到 proof 成功/异常反馈，随后再通过 `runtimeSessionId` deep link 进入 inspector。"
    - "教师 `/classroom` 在 live session 中会随 durable snapshot 变化自动刷新，因此在线人数与 runtime proof 不再停留在首屏旧值。"
    - "Phase 32 close gate 会显式把 teacher live refresh proof 纳入 `verify:phase32` 与 phase validation artifact，而不是只停留在 task-local regression。"
  artifacts:
    - path: "src/components/classroom/classroom-live-snapshot-refresh.tsx"
      provides: "teacher-side live snapshot refresh bridge that only triggers router.refresh from SSE snapshot events"
    - path: "src/app/(classroom)/classroom/page.tsx"
      provides: "live classroom page wiring for teacher-side refresh without duplicating snapshot truth"
    - path: "src/components/classroom/classroom-control-panel.tsx"
      provides: "proof first-feedback card rendered from runtimeProof truth even when current step is no longer a runtime step"
    - path: "scripts/verify-phase32-end-to-end.ts"
      provides: "canonical close gate coverage for teacher live refresh proof"
    - path: ".planning/phases/32-end-to-end-hardening-and-milestone-proof/32-VALIDATION.md"
      provides: "phase-close validation map that records classroom live refresh as required automated evidence"
  key_links:
    - from: "src/components/classroom/classroom-live-snapshot-refresh.tsx"
      to: "/api/classroom/[sessionId]/events"
      via: "EventSource snapshot subscription triggering router.refresh on version change"
      pattern: "EventSource"
    - from: "src/components/classroom/classroom-control-panel.tsx"
      to: "/settings/labs/runtime-inspector?runtimeSessionId=..."
      via: "runtimeProof.inspectorHref or runtimeSessionId fallback"
      pattern: "runtimeSessionId="
    - from: "scripts/verify-phase32-end-to-end.ts"
      to: "src/components/classroom/classroom-live-snapshot-refresh.test.tsx"
      via: "focused suite execution inside the canonical Phase 32 close gate"
      pattern: "classroom-live-snapshot-refresh"
---

<objective>
关闭 UAT Test 2：让教师 `/classroom` 在 live session 中自动消费最新 durable
snapshot，并把 proof first-feedback 与 inspector deep-link 从
`currentRuntimeDescriptor` gating 中解耦。

Purpose: 没有 teacher-side live refresh，presence 与 runtime proof 虽然已落 truth，
但教师 UI 仍停留在首屏旧值；没有 ungated proof card，教师就无法按 D-07/D-12
先在课堂确认再 drill-down；若新回归不进入 `verify:phase32`，phase close 仍会漏证。
Output: teacher live refresh bridge, `/classroom` wiring, proof feedback ungating, canonical gate coverage update.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-HUMAN-UAT.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-CONTEXT.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-RESEARCH.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-01-SUMMARY.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-04-SUMMARY.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-VALIDATION.md
@.planning/debug/classroom-feedback-link-pres.md
@scripts/verify-phase32-end-to-end.ts
@src/app/(classroom)/classroom/page.tsx
@src/components/classroom/classroom-control-panel.tsx
@src/components/learning/classroom-runtime-client.tsx
@src/app/api/classroom/[sessionId]/events/route.ts
@src/app/api/classroom/[sessionId]/snapshot/route.ts

<interfaces>
From `src/app/(classroom)/classroom/page.tsx`:
```ts
const snapshot = activeSession?.status === 'live'
  ? await getClassroomSnapshotDTO({ sessionId: activeSession.id })
  : null
```

From `src/components/classroom/classroom-control-panel.tsx`:
```ts
const currentSnapshot = conflict?.latest || initialSnapshot
const runtimeInspectorHref = primaryRuntimeProof?.inspectorHref
  ?? (primaryRuntimeProof?.runtimeSessionId
    ? `/settings/labs/runtime-inspector?runtimeSessionId=${primaryRuntimeProof.runtimeSessionId}`
    : null)
```

From `src/components/learning/classroom-runtime-client.tsx`:
```ts
source = new EventSource(`/api/classroom/${sessionId}/events`)
source.addEventListener('snapshot', async (e) => {
  // durable snapshot -> apply / refresh
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 给教师 `/classroom` 接入 live snapshot refresh，但继续只信任 server-owned DTO</name>
  <files>src/app/(classroom)/classroom/page.tsx, src/components/classroom/classroom-live-snapshot-refresh.tsx, src/components/classroom/classroom-live-snapshot-refresh.test.tsx</files>
  <behavior>
    - Test 1: live classroom 打开后，教师端会订阅 `/api/classroom/${sessionId}/events`，在收到更高版本 snapshot 事件时触发 `router.refresh()`。
    - Test 2: refresh bridge 不在客户端重建 `ClassroomSnapshotDTO` 或 presence 聚合，只负责刷新现有 server component 读模型。
    - Test 3: session ended、abort 或无 live session 时订阅会正确关闭，不残留事件流。
  </behavior>
  <action>新增一个 client-only `classroom-live-snapshot-refresh.tsx`，按 D-07、D-11、D-12 复用现有学生端 SSE 路线：监听 `/api/classroom/[sessionId]/events` 的 `snapshot` 事件，只在版本前进时触发 `router.refresh()`，不要在客户端维护第二份 snapshot state，也不要改成 polling-only。然后在 `src/app/(classroom)/classroom/page.tsx` 中仅对 `activeSession.status === 'live'` 挂载该组件，让 page 继续通过 `getClassroomSnapshotDTO()` 读取权威 DTO。不要在 refresh bridge 中查询 inspector DAL、不要复制学生端完整 applySnapshot 逻辑、不要把 page 改成全 client route。</action>
  <verify>
    <automated>pnpm test --run src/components/classroom/classroom-live-snapshot-refresh.test.tsx</automated>
  </verify>
  <done>教师 `/classroom` 会随着 live snapshot 真值自动刷新，在线人数和 proof 状态不再卡在首屏旧值。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 将 proof first-feedback 从 runtime-step gating 解耦，只要有 runtimeProof 就展示 drill-down 入口</name>
  <files>src/components/classroom/classroom-control-panel.tsx, src/components/surfaces/classroom-console-surface.test.tsx, scripts/verify-phase32-end-to-end.ts, .planning/phases/32-end-to-end-hardening-and-milestone-proof/32-VALIDATION.md</files>
  <behavior>
    - Test 1: 只要 snapshot 中任一 participant 含 `runtimeProof`，教师端就能看到 proof success/failure 文案与 `查看运行轨迹` 链接。
    - Test 2: runtime host 主舞台仍只在 `currentRuntimeDescriptor` 存在时渲染，不因 proof card 解耦而扩展 runtime stage 作用域。
    - Test 3: inspector 链接继续优先使用 `runtimeProof.inspectorHref`，否则回退到 `runtimeSessionId` deep link。
    - Test 4: `verify:phase32` 会把 live snapshot refresh regression 与 classroom-first feedback regression 一起纳入 canonical close gate，并在 `32-VALIDATION.md` 中记录该覆盖路径。
  </behavior>
  <action>修改 `classroom-control-panel.tsx`：将 proof first-feedback 区块从 `currentRuntimeDescriptor ? ... : null` 解耦，改为基于 `primaryRuntimeProof` 或 `runtimeAttentionParticipants.length > 0` 渲染，这样当前步骤已切离 runtime 时，教师仍能按 D-04、D-07、D-11、D-12 看到 first-feedback 与 inspector 入口。保留下方 `RuntimeHostClient` 主舞台对 `currentRuntimeDescriptor` 的 gating，不要把旧 proof 数据误渲染成当前 live runtime stage。同步更新 `classroom-console-surface.test.tsx`，锁住“classroom-first feedback + runtimeSessionId deep-link + 不引入 proof dashboard”的语义。随后补改 `scripts/verify-phase32-end-to-end.ts`：把 `src/components/classroom/classroom-live-snapshot-refresh.test.tsx` 与 `src/components/surfaces/classroom-console-surface.test.tsx` 纳入 Phase 32 focused suites，并新增明确的 `classroom live refresh drift` 或等价 guard，证明 teacher-side durable snapshot refresh 与 first-feedback/inspector deep-link 都属于 canonical close gate，而不是仅靠 task-local test。最后同步更新 `.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-VALIDATION.md`，把 32-06 的新 automated evidence、close-stage sampling、exit criteria 写入 validation map。不要新建第二个 verify 命令，不要把 live refresh proof 记成手工-only 检查。</action>
  <acceptance_criteria>
    - `classroom-control-panel.tsx` 中存在可 grep 的 ungated proof feedback 渲染与 `runtimeSessionId` deep-link
    - `scripts/verify-phase32-end-to-end.ts` 显式执行 `src/components/classroom/classroom-live-snapshot-refresh.test.tsx`，并含 `classroom live refresh drift` 或等价 Phase 32 guard
    - `32-VALIDATION.md` 新增 32-06 automated evidence，且 phase-close 采样/exit criteria 明确要求该 proof 由 `pnpm verify:phase32` 覆盖
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/classroom/classroom-live-snapshot-refresh.test.tsx src/components/surfaces/classroom-console-surface.test.tsx && pnpm verify:phase32</automated>
  </verify>
  <done>教师端先在 `/classroom` 获得 proof 反馈，再按同一 `runtimeSessionId` 进入 inspector；proof 入口不再因为当前步骤不是 runtime step 而消失。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| classroom SSE -> teacher client refresh bridge | 未持久化的 transport 事件只能触发刷新，不能变成新的 UI truth source |
| classroom snapshot DTO -> proof feedback card | 教师端显示 proof 成功/异常与 inspector 入口，不能泄漏额外 runtime state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-06-01 | T | teacher live refresh bridge | mitigate | client bridge 只做 `router.refresh()`，所有数据继续来自 `getClassroomSnapshotDTO()` |
| T-32-06-02 | I | classroom proof card | mitigate | 只消费 `runtimeProof` / `runtimeSessionId` / `inspectorHref`，不展示 raw runtime state JSON |
| T-32-06-03 | D | classroom live session refresh | mitigate | 仅在 live session 挂载 EventSource，并在 abort/ended 时关闭，避免无界重连或 stale subscription |
</threat_model>

<verification>
- `pnpm test --run src/components/classroom/classroom-live-snapshot-refresh.test.tsx src/components/surfaces/classroom-console-surface.test.tsx`
- `pnpm verify:phase32`
</verification>

<success_criteria>
- 教师 `/classroom` 在 live session 中会自动反映 presence 与 runtime proof 最新状态。
- proof first-feedback 与 inspector deep-link 不再受 `currentRuntimeDescriptor` gating 影响。
- `/classroom` 继续保持 server-owned snapshot truth，不引入第二真相源。
- `verify:phase32` 与 `32-VALIDATION.md` 都明确把 teacher live refresh proof 视为 phase-close 必过项。
</success_criteria>

<output>
After completion, create `.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-06-SUMMARY.md`
</output>
