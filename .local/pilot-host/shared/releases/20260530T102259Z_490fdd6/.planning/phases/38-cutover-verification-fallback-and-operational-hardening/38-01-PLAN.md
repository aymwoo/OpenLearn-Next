---
phase: 38-cutover-verification-fallback-and-operational-hardening
plan: 01
type: execute
wave: 1
depends_on: ["37-VERIFICATION"]
files_modified:
  - scripts/verify-phase38-cutover-closeout.ts
  - package.json
  - .planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-VERIFICATION.md
autonomous: true
requirements: [RTPX-02, RTPX-03]
must_haves:
  truths:
    - "Phase 38 必须发布 milestone-level 单一 canonical gate，但它只能作为 Phase 36 与 Phase 37 proof 的上层 close gate，不能与 `verify:phase36` / `verify:phase37` 竞争或替代它们。"
    - "route-by-route parity proof 必须把 classroom、player、runtime 三条主链与 operator transport surface 收口为一个 cutover story，而不是继续分散在两个 phase verifier 的人工解释里。"
    - "milestone-level verifier 必须继续诚实保留 Phase 36 的 SSE rollback posture 和 Phase 37 的 optional Redis posture，不得把 Redis fanout 改写成默认基线，也不得把 rollback 假装成完全消失。"
    - "Phase 38 的 verifier 以组合既有 proof 为主，只补 closeout-specific checks；默认不重写 transport seam 本体。"
  artifacts:
    - path: "scripts/verify-phase38-cutover-closeout.ts"
      provides: "milestone-level canonical close gate for ws + ioredis cutover"
    - path: "package.json"
      provides: "verify:phase38 command registration"
    - path: ".planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-VERIFICATION.md"
      provides: "unified parity proof and milestone verification report"
  key_links:
    - from: "scripts/verify-phase38-cutover-closeout.ts"
      to: "scripts/verify-phase36-websocket-cutover.ts"
      via: "Phase 36 remains prerequisite proof for websocket cutover correctness"
      pattern: "verify:phase36"
    - from: "scripts/verify-phase38-cutover-closeout.ts"
      to: "scripts/verify-phase37-redis-fanout.ts"
      via: "Phase 37 remains prerequisite proof for optional Redis fanout and degraded honesty"
      pattern: "verify:phase37"
    - from: "scripts/verify-phase38-cutover-closeout.ts"
      to: ".planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-VERIFICATION.md"
      via: "executable gate and written proof must agree on milestone scope"
      pattern: "ws + ioredis"
---

<objective>
发布 Phase 38 的 canonical close gate，把已在 Phase 36 / 37 分别成立的 websocket
cutover proof 与 optional Redis fanout proof，收口成 milestone 级单一可执行证明与
统一 parity story。

Purpose: 如果 milestone 仍然需要人工拼接 `verify:phase36` + `verify:phase37` 的含义，
那它就还没有真正具备“可验证 closeout”的形态。
Output: `verify:phase38`、统一 route-by-route parity proof、milestone-level verification report。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-CONTEXT.md
@.planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-RESEARCH.md
@.planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-PATTERNS.md
@.planning/phases/36-websocket-classroom-transport-cutover/36-VERIFICATION.md
@.planning/phases/37-redis-fanout-and-multi-instance-delivery-convergence/37-VERIFICATION.md
@scripts/verify-phase36-websocket-cutover.ts
@scripts/verify-phase37-redis-fanout.ts
@package.json
@src/app/api/ws/classroom/[sessionId]/route.ts
@src/features/runtime-platform/seams/transport/gateway.ts
@src/components/classroom/classroom-control-panel.tsx
@src/components/classroom/classroom-live-snapshot-refresh.tsx
@src/components/learning/classroom-runtime-client.tsx
@src/components/surfaces/settings-surface.tsx
@src/components/surfaces/runtime-inspector-surface.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 `verify:phase38` milestone close gate，并把 Phase 36/37 proof 组合为单一外部命令</name>
  <read_first>
    - .planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-RESEARCH.md
    - scripts/verify-phase36-websocket-cutover.ts
    - scripts/verify-phase37-redis-fanout.ts
    - package.json
    - .planning/phases/32-end-to-end-hardening-and-milestone-proof/32-03-PLAN.md
  </read_first>
  <files>scripts/verify-phase38-cutover-closeout.ts, package.json</files>
  <behavior>
    - Test 1: `verify:phase38` 先执行 `verify:phase36` 与 `verify:phase37`，把它们作为里层 prerequisite，而不是复制两边已有 focused suites。
    - Test 2: `verify:phase38` 额外执行 closeout-specific static guards，确认 milestone close docs、fallback posture docs、local demo or runbook docs 存在且 scope 没有漂移。
    - Test 3: verifier 输出会明确区分 websocket baseline 已验证、Redis optional capability 已验证、以及 Phase 38 自己新增的 closeout posture；不能输出成一个模糊“全部都好了”的大而化之结论。
  </behavior>
  <action>新增 `scripts/verify-phase38-cutover-closeout.ts`，风格复用 `verify:phase36` / `verify:phase37`，但职责固定为 milestone-level close gate。脚本必须先调用 `pnpm verify:phase36` 和 `pnpm verify:phase37`，把它们作为 prerequisite proof；然后再做 Phase 38 自己的 closeout checks。至少要有以下 guard：1) milestone close docs 存在；2) fallback / rollback matrix 或等价 artifact 存在；3) local demo/bootstrap runbook 或等价 artifact 存在；4) milestone docs 明确只覆盖 `ws + ioredis` classroom transport，而不是 PostgreSQL、BullMQ、Redis Streams、第二 runtime；5) repo 中没有把 Redis 改写成默认 transport baseline 的 closeout 口径。不要把 `verify:phase38` 写成新的 transport business verifier；它是上层 close gate，不是第三个 phase-implementation verifier。同步在 `package.json` 注册 `verify:phase38`。</action>
  <acceptance_criteria>
    - `package.json` 出现精确字符串 `"verify:phase38"`。
    - `scripts/verify-phase38-cutover-closeout.ts` 内显式调用 `verify:phase36` 与 `verify:phase37`。
    - verifier 内存在 closeout-specific guards，而不是只简单顺序跑两个旧命令。
    - verifier 输出明确保留 `SSE rollback surface` 与 `Redis optional capability` 的 honest wording，不把两者写成消失或默认开启。
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase38</automated>
  </verify>
  <done>仓库存在 milestone-level 单一 close gate，外部 close 命令不再依赖人工记忆 Phase 36/37 的组合方式。</done>
</task>

<task type="auto">
  <name>Task 2: 发布统一 route-by-route parity proof 与 Phase 38 verification report</name>
  <read_first>
    - .planning/phases/36-websocket-classroom-transport-cutover/36-VERIFICATION.md
    - .planning/phases/37-redis-fanout-and-multi-instance-delivery-convergence/37-VERIFICATION.md
    - .planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-CONTEXT.md
    - src/app/api/ws/classroom/[sessionId]/route.ts
    - src/components/classroom/classroom-live-snapshot-refresh.tsx
    - src/components/learning/classroom-runtime-client.tsx
    - src/components/surfaces/settings-surface.tsx
    - src/components/surfaces/runtime-inspector-surface.tsx
  </read_first>
  <files>.planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-VERIFICATION.md</files>
  <behavior>
    - Test 1: verification report 会把 teacher producer、runtime producer、classroom consumer、player consumer、operator observability 统一映射成一个 parity matrix。
    - Test 2: report 会明确说明哪些 proof 来自 `verify:phase36`，哪些来自 `verify:phase37`，哪些是 Phase 38 新增的 closeout checks。
    - Test 3: report 会明确列出 milestone boundary：已交付只到 `ws + ioredis` classroom transport；deferred 继续是 PostgreSQL、BullMQ、Redis Streams、第二 runtime、第三方 runtime/package、AI runtime expansion。
  </behavior>
  <action>新增 `38-VERIFICATION.md`，不要复写前两阶段的实现细节，而是做 milestone-level synthesis。文档至少包含：requirements coverage、truth table、artifact inventory、route-by-route parity matrix、close gate structure、fallback/rollback relationship、operator visibility summary、explicit exclusions。parity proof 至少覆盖：`teacher.control` producer、`runtime.command` producer、`classroom.snapshot` / `runtime.event` consumer、player reconnect + snapshot fallback、settings / runtime inspector / teacher classroom degraded visibility。说明这些路径如何由 36/37 的 focused suites 和 38 的 close gate 共同证明，而不是只列文件名。不要把这个 report 写成另一个 plan；它是 closeout evidence artifact。</action>
  <acceptance_criteria>
    - `38-VERIFICATION.md` 明确包含 route-by-route parity proof，而不是只有 summary prose。
    - 文档显式区分 prerequisite proofs 与 Phase 38 closeout proof。
    - 文档显式写出本 milestone 的 delivered scope 与 deferred scope。
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase38</automated>
  </verify>
  <done>Phase 38 的 parity proof 已从分散结论收口为单一 milestone verification artifact。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| milestone close gate → phase verifiers | 如果 Phase 38 不清楚自己只是上层 gate，就会和 Phase 36/37 verifier 竞争或重复 |
| closeout docs → executable truth | 如果 closeout artifact 没有被 verifier 守住，文档可能漂移成虚假完成声明 |
| parity proof → milestone claim | 如果 parity proof 只是 summary prose，reviewer 仍无法判断 cutover 是否真的闭环 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01-01 | T | `verify-phase38-cutover-closeout.ts` | mitigate | 明确把 `verify:phase36` 与 `verify:phase37` 作为 prerequisite，并增加 Phase 38 自己的 closeout guards，避免成为空壳 wrapper |
| T-38-01-02 | R | milestone close report | mitigate | `38-VERIFICATION.md` 必须带 parity matrix、requirements coverage 与 explicit exclusions，不接受 prose-only close claim |
| T-38-01-03 | I | closeout wording | mitigate | verifier 与 report 都显式保留 `SSE rollback surface`、`Redis optional capability` 与 deferred scope，避免误导性完成口径 |
</threat_model>

<verification>
- `pnpm verify:phase38`
</verification>

<success_criteria>
- 仓库存在 `verify:phase38`，并成为本 milestone 唯一外部 close gate。
- classroom / player / runtime / operator surfaces 的 parity proof 已被统一写入 `38-VERIFICATION.md`。
- Phase 38 没有重写 Phase 36/37 的 transport seam 实现，而是把既有 proof 收口为 milestone close posture。
</success_criteria>

<output>
After completion, create `.planning/phases/38-cutover-verification-fallback-and-operational-hardening/38-01-SUMMARY.md`
</output>
