---
phase: 33-project-level-auth-data-and-classroom-durability-closure
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - src/lib/dal/classroom.ts
  - src/lib/dal/course-authoring.ts
  - src/lib/dal/learning.ts
  - src/lib/dal/auth.ts
  - src/actions/classroom-actions.ts
  - src/actions/course-authoring-actions.ts
  - src/actions/learning-actions.ts
  - src/lib/dto/classroom.ts
  - src/lib/dto/course-authoring.ts
  - src/lib/dto/learning.ts
  - src/lib/dal/classroom.test.ts
  - src/lib/dal/course-authoring.test.ts
  - src/actions/classroom-actions.test.ts
  - src/actions/learning-actions.test.ts
autonomous: true
requirements: [AUTH-05, AUTH-06, DATA-03, DATA-04]
must_haves:
  truths:
    - "高风险 classroom、course-authoring、learning 读写继续只走 DAL + Server Actions only。"
    - "所有 touched actions 都以 Zod parse 作为持久化前输入边界，不信任 client 或 runtime payload。"
    - "UI 只接收 sanitized DTO，不接收 raw rows、password、provider token、internal prompt、private plugin data。"
  artifacts:
    - path: "src/lib/dal/classroom.ts"
      provides: "classroom actor/scope enforcement and DTO-only read model"
    - path: "src/lib/dal/course-authoring.ts"
      provides: "owner/school/enrollment scope-safe course reads and writes"
    - path: "src/lib/dto/classroom.ts"
      provides: "sanitized classroom DTO boundary"
    - path: "src/actions/classroom-actions.ts"
      provides: "server-side parsed classroom mutations only"
  key_links:
    - from: "src/actions/classroom-actions.ts"
      to: "src/lib/dal/classroom.ts"
      via: "parsed input to DAL-only write path"
      pattern: "Schema"
    - from: "src/lib/dal/course-authoring.ts"
      to: "src/lib/dto/course-authoring.ts"
      via: "teacher-scoped DTO shaping"
      pattern: "DTO"
---

<objective>
收紧当前 ship-critical DAL 与 Server Action 主链路，让 classroom、course-authoring、learning
 的高风险读写在 actor、role、school、ownership、enrollment 与 DTO sanitation 上达到 requirement truth。

Purpose: `AUTH-05` / `AUTH-06` 是本 milestone 的核心安全缺口；如果这些主链路仍靠客户端约定、原始行对象
或 ad hoc parse 撑着，Phase 34 的成员管理会直接叠在松散授权上。
Output: tightened DAL guards, DTO-only read models, Zod-first mutation boundaries, focused regressions.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/33-project-level-auth-data-and-classroom-durability-closure/33-CONTEXT.md
@src/lib/dal/classroom.ts
@src/lib/dal/course-authoring.ts
@src/lib/dal/learning.ts
@src/actions/classroom-actions.ts
@src/actions/course-authoring-actions.ts
@src/actions/learning-actions.ts
@src/lib/dto/classroom.ts
@src/lib/dto/course-authoring.ts
@src/lib/dto/learning.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 审计并收紧 classroom 与 learning 主链路的 actor or scope enforcement</name>
  <files>src/lib/dal/classroom.ts, src/lib/dal/learning.ts, src/actions/classroom-actions.ts, src/actions/learning-actions.ts, src/lib/dto/classroom.ts, src/lib/dto/learning.ts, src/lib/dal/classroom.test.ts, src/actions/classroom-actions.test.ts, src/actions/learning-actions.test.ts</files>
  <behavior>
    - Test 1: teacher 只能读写自己有权访问的 classroom session、学生只能读写自己的 learning or runtime state。
    - Test 2: classroom or learning DTO 不泄漏 raw participant rows、credentials、internal audit payload 或 private runtime/plugin data。
  </behavior>
  <action>从现有 `classroom.ts`、`learning.ts` 和对应 actions 入手，补齐 actor identity、teacher ownership、student self-scope、school membership、participant membership 与 runtime or classroom resource scope 检查。检查所有 touched mutation 是否先经过 Zod parse 再进入 DAL。对返回给 UI 的 DTO 做诚实 sanitation，避免把 raw row 或内嵌敏感 JSON 直接透传到 surface。</action>
  <verify>
    <automated>pnpm test --run src/lib/dal/classroom.test.ts src/actions/classroom-actions.test.ts src/actions/learning-actions.test.ts</automated>
  </verify>
  <done>classroom 与 learning 主链路满足 `AUTH-05`、`AUTH-06`、`DATA-03`、`DATA-04` 的高风险部分。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 收紧 `course-authoring` 的 owner or school or enrollment 边界，为 Phase 34 铺底</name>
  <files>src/lib/dal/course-authoring.ts, src/actions/course-authoring-actions.ts, src/lib/dto/course-authoring.ts, src/lib/dal/course-authoring.test.ts</files>
  <behavior>
    - Test 1: teacher 只能操作自己 owner 且 school-scoped 的课程，same-school foreign course 继续按 not found or unauthorized 处理。
    - Test 2: enrollment-related read models 只暴露 DTO-safe count or summary，不为 Phase 34 提前泄漏 roster 明细。
  </behavior>
  <action>复用 `course-authoring` 当前 owner + school scope posture，把课程详情、关联计数、成员相关摘要与写入动作统一收紧到 shared auth contract 上。Phase 33 不实现完整 COURSE-07，但必须先把 course enrollment 相关 read or write 边界做诚实封口，确保 Phase 34 不会借旧路径绕过新的 auth/data contract。</action>
  <verify>
    <automated>pnpm test --run src/lib/dal/course-authoring.test.ts</automated>
  </verify>
  <done>`course-authoring` 已成为 Phase 34 可复用的安全底座，而不是历史松散入口。</done>
</task>

</tasks>

<verification>
- `pnpm test --run src/lib/dal/classroom.test.ts src/actions/classroom-actions.test.ts src/actions/learning-actions.test.ts src/lib/dal/course-authoring.test.ts`
</verification>

<risks>
- `classroom.ts` 体量较大，过度重构会放大回归面；优先做最小边界收紧，而不是拆新模块。
- 课程成员相关 contract 如果收得过宽，会提前侵入 Phase 34；如果收得过窄，又会保留旧 leak。
</risks>

<dependencies>
- 依赖 33-01 提供统一 actor truth 与 role vocabulary。
- 为 33-03 的 schema or durability 审计提供“哪些读写是 ship-critical”的优先级清单。
</dependencies>

<output>
After completion, create `.planning/phases/33-project-level-auth-data-and-classroom-durability-closure/33-02-SUMMARY.md`
</output>
