---
phase: 57-classroom-runtime-sample-chain
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dto/classroom.ts
  - src/lib/dto/learning.ts
  - src/lib/dal/classroom.ts
  - src/components/classroom/classroom-launch-panel.tsx
  - src/actions/classroom-actions.test.ts
  - src/components/classroom/classroom-launch-panel.test.tsx
autonomous: true
requirements:
  - CHAIN-03
  - SAFE-01
must_haves:
  truths:
    - "教师发起开课前只能被真正 blocker 阻断，attention/advisory 只提示不拦截。"
    - "launch surface 的 readiness 问题能定位到具体 step 与投票插件上下文。"
    - "后续 runtime plan 可以直接消费统一的 voting round / student runtime DTO 合同。"
  artifacts:
    - path: src/lib/dto/classroom.ts
      provides: "Phase 57 的 launch issue 上下文与 voting round DTO 合同"
    - path: src/lib/dto/learning.ts
      provides: "student voting waiting/reconnect runtime 字段合同"
    - path: src/lib/dal/classroom.ts
      provides: "step-scoped launch readiness 读模型"
    - path: src/components/classroom/classroom-launch-panel.tsx
      provides: "只拦 blocker 的 launch UI"
  key_links:
    - from: src/lib/dal/classroom.ts
      to: src/components/classroom/classroom-launch-panel.tsx
      via: ClassroomLaunchReadinessDTOSchema
      pattern: "launchReadiness"
    - from: src/lib/dto/learning.ts
      to: src/components/learning/classroom-runtime-client.tsx
      via: RuntimeStepStateDTOSchema
      pattern: "RuntimeStepStateDTOSchema"
---

<objective>
锁定 Phase 57 的 runtime launch/readiness 与投票轮次 DTO 合同，让后续 teacher control、student submit、teacher aggregation 都建立在同一份 authoritative contract 上。

Purpose: 先把 D-57-01 / D-57-02 / D-57-03 的 launch truth 和后续要消费的 voting runtime contract 固化，避免后续计划各自发明字段。
Output: 带 step/plugin 上下文的 readiness DTO、student runtime 新状态字段、launch panel 非阻断 warning 展示与对应回归测试。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/57-classroom-runtime-sample-chain/57-CONTEXT.md
@.planning/phases/57-classroom-runtime-sample-chain/57-RESEARCH.md
@.planning/phases/57-classroom-runtime-sample-chain/57-UI-SPEC.md
@.planning/phases/57-classroom-runtime-sample-chain/57-VALIDATION.md
@.planning/phases/56-voting-plugin-contract-and-authoring-integration/56-VERIFICATION.md

<interfaces>
From src/lib/dto/classroom.ts:
```ts
export const ClassroomLaunchReadinessIssueDTOSchema = z.object({
  code: ClassroomLaunchReadinessIssueCodeSchema,
  message: z.string(),
  stepId: z.string().nullable().optional(),
});

export const ClassroomSnapshotDTOSchema = z.object({
  sessionId: z.string(),
  activeStepId: z.string(),
  locked: z.boolean(),
  status: z.enum(["live", "ended"]),
  version: z.number().int(),
});
```

From src/lib/dto/learning.ts:
```ts
export const RuntimeStepStateDTOSchema = z.object({
  forcedStepId: z.string().nullable(),
  classroomSessionId: z.string().nullable().default(null),
  classroomVersion: z.number().int().nullable().default(null),
  connectionState: z.enum(["connected", "reconnecting", "offline", "snapshot_fallback"]).default("offline"),
  latestRuntimeStateSummary: z.record(z.string(), z.unknown()).default({}),
  runtimeRecoveryStatus: z.enum(["unavailable", "available", "restored"]).default("unavailable"),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 补 launch readiness 与 DTO contract 回归测试</name>
  <read_first>
    - src/actions/classroom-actions.test.ts
    - src/components/classroom/classroom-launch-panel.test.tsx
    - src/lib/dto/classroom.ts
    - src/lib/dto/learning.ts
    - .planning/phases/57-classroom-runtime-sample-chain/57-CONTEXT.md
    - .planning/phases/57-classroom-runtime-sample-chain/57-UI-SPEC.md
  </read_first>
  <files>src/actions/classroom-actions.test.ts, src/components/classroom/classroom-launch-panel.test.tsx</files>
  <action>先新增失败用例，锁定 D-57-01 / D-57-02 / D-57-03：1) launchClassroomSessionAction 只因为 blockingIssues 禁用 launch，attention/advisory-only lesson 仍可启动；2) launch panel 的 issue row 必须渲染 step title 或 `step:{id}` 与 plugin/voting context；3) launch panel 必须显示 `课堂仍会按已发布快照启动，本项不会阻断开课。` 文案且不能出现二次确认 modal 相关断言。测试直接锚定 `ClassroomLaunchLessonOptionDTO.launchReadiness` 与组件可见文本，不写模糊快照。</action>
  <acceptance_criteria>
    - `src/components/classroom/classroom-launch-panel.test.tsx` 断言 blocker-only disabled、attention/advisory-enabled、step/plugin context copy 三种场景。
    - `src/actions/classroom-actions.test.ts` 继续保留 published snapshot launch action 的成功路径断言，不删除现有 `cacheTags.classroom` 断言。
    - 新测试在实现前失败，且失败原因直接对应 Phase 57 copy/contract 缺口。
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/actions/classroom-actions.test.ts src/components/classroom/classroom-launch-panel.test.tsx</automated>
  </verify>
  <done>launch gating 与 step-scoped context 的测试先红灯，后续实现有明确 target。</done>
</task>

<task type="auto">
  <name>Task 2: 固化 launch issue 上下文与 voting runtime DTO 合同</name>
  <read_first>
    - src/lib/dto/classroom.ts
    - src/lib/dto/learning.ts
    - src/lib/dal/classroom.ts
    - src/components/classroom/classroom-launch-panel.tsx
    - .planning/phases/57-classroom-runtime-sample-chain/57-CONTEXT.md
    - .planning/phases/57-classroom-runtime-sample-chain/57-RESEARCH.md
  </read_first>
  <files>src/lib/dto/classroom.ts, src/lib/dto/learning.ts, src/lib/dal/classroom.ts, src/components/classroom/classroom-launch-panel.tsx</files>
  <action>扩展 `ClassroomLaunchReadinessIssueDTOSchema` 与 `buildLaunchReadiness()`，让每条 issue 除 `message` / `stepId` 外还带 `stepTitle`、`pluginKey`、`pluginLabel`、`severityCopy` 等可渲染上下文，并继续只把真正 blocker 放进 `blockingIssues`（per D-57-01）。同时在 `src/lib/dto/classroom.ts` 增加 Phase 57 后续会复用的 `ClassroomVotingRound*` DTO（至少包含 `status`, `stepId`, `stepTitle`, `startedAt`, `endedAt`, `submittedCount`, `remainingCount`, `isFrozen`，默认可为 `null`/空数组），在 `src/lib/dto/learning.ts` 增加 student runtime 的 `waitingForTeacher`, `roundEnded`, `roundStatusCopy`, `latestVotingSubmission` 等字段合同。`classroom-launch-panel.tsx` 需要按 D-57-02 / D-57-03 渲染 step/plugin context 和 attention helper copy，但保持现有单按钮 launch 流程，不新增确认弹层。</action>
  <acceptance_criteria>
    - `ClassroomLaunchReadinessIssueDTOSchema` 与 `RuntimeStepStateDTOSchema` 含有后续 voting round 所需字段，且默认值不会破坏现有 parse。
    - `buildLaunchReadiness()` 仍只在无可开课班级等 blocker 场景写入 `blockingIssues`，attention/advisory 仅写入对应数组。
    - `classroom-launch-panel.tsx` 的 readiness list 可见 `stepTitle`/`step:{id}` 和 plugin label，不出现新的 modal/confirm 组件调用。
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/actions/classroom-actions.test.ts src/components/classroom/classroom-launch-panel.test.tsx</automated>
  </verify>
  <done>Phase 57 后续 plans 可直接依赖统一 DTO contract，launch panel 已符合 blocker/attention 决策。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teacher launch surface → DAL readiness | untrusted UI 只能展示服务器给出的 blocker/attention/advisory truth |
| published snapshot → launch panel | readiness 只能来自 published snapshot，不允许 draft lesson 混入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | T | `launchClassroomSession` / readiness DTO | mitigate | 继续在 `src/lib/dal/classroom.ts` 用 `publishedVersionId + snapshotJson` 构造 launch readiness，UI 只消费 DTO，不自行推断。 |
| T-57-02 | I | `classroom-launch-panel.tsx` | mitigate | 输出 step/plugin 上下文而不是泛化摘要，避免 teacher 在错误课时上课前无法识别具体 blocker。 |
|
</threat_model>

<verification>
- 运行 `pnpm vitest run src/actions/classroom-actions.test.ts src/components/classroom/classroom-launch-panel.test.tsx`
- 手动抽查 `ClassroomLaunchReadinessDTOSchema` 新字段仍能被现有 console DTO parse 通过
</verification>

<success_criteria>
- 开课按钮只因 blocker 被禁用，attention/advisory 保持可开课。
- readiness 行项带 step/plugin 上下文，满足 D-57-02。
- 后续 voting round / student waiting 状态已有统一 DTO contract，无需后续 plans 再发明字段。
</success_criteria>

<output>
After completion, create `.planning/phases/57-classroom-runtime-sample-chain/57-01-SUMMARY.md`
</output>
