---
phase: 30-capability-enforcement-and-plugin-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/runtime-platform/contracts/descriptors.ts
  - src/lib/dto/resource-ai.ts
  - src/lib/dal/plugins.ts
  - src/server/plugins/registry.ts
  - src/lib/dal/plugins.builtins.test.ts
  - src/lib/dal/plugins.test.ts
autonomous: true
requirements: [GOVR-02]
must_haves:
  truths:
    - "D-06 / D-07：plugin manifest v2 继续沿用 school-scoped `manifestJson` 路径，显式声明 runtime、requested capabilities、permission contract 与 lifecycle metadata。"
    - "D-08：manifest v2 首发只允许本地 built-in 或受控本地资源入口，不接受 remote URL、动态脚本或任意下载执行。"
    - "D-09 / D-10：built-in HTML courseware 必须能迁移到 manifest v2，且缺失 v2 字段时默认更保守，不能隐式放宽权限。"
  artifacts:
    - path: "src/features/runtime-platform/contracts/descriptors.ts"
      provides: "canonical runtime manifest v2 and lifecycle metadata contracts"
      contains: "manifestVersion"
    - path: "src/lib/dto/resource-ai.ts"
      provides: "plugin manifest v2 DTO contract on the existing school-scoped manifest path"
      contains: "PluginManifestSchema"
    - path: "src/lib/dal/plugins.ts"
      provides: "manifest v1/v2 parse and registration migration posture"
      contains: "manifestJson"
  key_links:
    - from: "src/lib/dto/resource-ai.ts"
      to: "src/features/runtime-platform/contracts/descriptors.ts"
      via: "runtime declaration reuse"
      pattern: "RuntimeDescriptorSchema"
    - from: "src/lib/dal/plugins.ts"
      to: "src/server/plugins/registry.ts"
      via: "allowlist and permission contract consumption"
      pattern: "permissions"
---

<objective>
把当前 plugin manifest 升级到 v2，使 runtime declaration、requested
capabilities、permissions 与 lifecycle metadata 成为显式治理输入，同时保持
school-scoped truth 与无 remote execution posture。

Purpose: 让 runtime/plugin governance 依赖结构化 manifest truth，而不是继续靠
零散字段、默认值和隐式约定拼装。
Output: canonical manifest v2 contract, migration-safe parsing, built-in runtime
compatibility coverage.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-capability-enforcement-and-plugin-lifecycle/30-CONTEXT.md
@.planning/phases/30-capability-enforcement-and-plugin-lifecycle/30-RESEARCH.md
@.planning/phases/30-capability-enforcement-and-plugin-lifecycle/30-PATTERNS.md
@src/features/runtime-platform/contracts/descriptors.ts
@src/lib/dto/resource-ai.ts
@src/lib/dal/plugins.ts
@src/server/plugins/registry.ts
@src/lib/dal/plugins.builtins.test.ts

<interfaces>
Current runtime manifest placeholder:

```ts
export const RuntimeManifestV2Schema = z.object({
  manifestVersion: z.literal(2),
  contractVersion: RuntimeContractVersionSchema.default(...),
  pluginId: z.string().min(1),
  runtime: RuntimeDescriptorSchema,
});
```

Current plugin manifest truth:

```ts
export const PluginManifestSchema = z.object({
  id: z.string(),
  version: z.string(),
  permissions: z.array(z.string()).default([]),
  anchors: z.array(PluginHookAnchorSchema),
  actions: z.array(PluginActionSchema),
  ...
});
```

Phase 30 should evolve these names rather than replace them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义 manifest v2 的 canonical governance contract</name>
  <read_first>
    - src/features/runtime-platform/contracts/descriptors.ts
    - src/lib/dto/resource-ai.ts
    - .planning/phases/30-capability-enforcement-and-plugin-lifecycle/30-CONTEXT.md
  </read_first>
  <files>src/features/runtime-platform/contracts/descriptors.ts, src/lib/dto/resource-ai.ts</files>
  <action>在现有 contract root 上把 `RuntimeManifestV2Schema` placeholder 升级为实际治理 contract，并把 `PluginManifestSchema` 演进成可承载 v2 的 canonical manifest schema。v2 至少要显式表达：runtime declaration、requested capabilities、permission contract、lifecycle metadata、local-only entry constraints。可以保留 v1/v2 union 或 migration helper，但任何旧 manifest 缺失新字段时都必须默认更保守。禁止在 v2 中接受 remote bootstrap URL、第三方 iframe source、动态 script 或任意下载执行入口。</action>
  <acceptance_criteria>
    - `descriptors.ts` 中的 manifest v2 不再只是 placeholder，而是包含 runtime 与 lifecycle metadata 的真实 contract。
    - `resource-ai.ts` 中的 plugin manifest contract 明确支持 v2，而不是把治理字段散落在外部约定里。
    - contract 中对 entry/bootstrap 的限制能够表达 local-only posture。
    - 缺失 v2 字段的 legacy parse 不会隐式授予额外 capability 或 permission。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/plugins.builtins.test.ts src/features/runtime-platform/contracts/contracts.test.ts</automated>
  </verify>
  <done>manifest v2 的治理输入已经有唯一 contract truth，可被 registration、guard 和 audit 一致消费。</done>
</task>

<task type="auto">
  <name>Task 2: 让 school-scoped registration 与 built-in runtime 消费 manifest v2</name>
  <read_first>
    - src/lib/dal/plugins.ts
    - src/server/plugins/registry.ts
    - src/lib/dal/plugins.builtins.test.ts
    - src/lib/dal/plugins.test.ts
  </read_first>
  <files>src/lib/dal/plugins.ts, src/server/plugins/registry.ts, src/lib/dal/plugins.builtins.test.ts, src/lib/dal/plugins.test.ts</files>
  <action>让 plugin registration、enable chain 与 built-in runtime template resolution 使用新的 manifest v2 truth。`plugins.ts` 需要支持 v1/v2 parse 与 migration-safe registration，`registry.ts` 需要显式消费 permission contract 与 allowlist metadata，而不是继续假定所有 manifest 都只有旧字段。`plugins.builtins.test.ts` 与 `plugins.test.ts` 要覆盖：built-in HTML runtime 能以 v2 manifest 正常被解析；legacy manifest 缺失 v2 字段时不会获得更宽权限；remote entry 或不受控 bootstrap 在 schema 或 registration 层被拒绝。</action>
  <acceptance_criteria>
    - school-scoped registration 路径可读写 manifest v2，不新增第二套 registry 表或 truth。
    - built-in HTML courseware 的 local runtime descriptor 仍能通过 v2 contract 被解析。
    - remote/dynamic entry 在 parse 或 registration 层被拒绝。
    - focused tests 覆盖 built-in v2、legacy fallback、unsafe entry reject 三类情形。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/plugins.builtins.test.ts src/lib/dal/plugins.test.ts</automated>
  </verify>
  <done>manifest v2 已接入现有 school-scoped registration 与 built-in runtime 链路，`GOVR-02` 的执行基础成立。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| plugin manifest -> runtime/plugin execution | manifest 是声明，不是自由执行入口，必须在 schema 层限制本地受控 entry |
| legacy manifest -> v2 migration | 迁移若默认放宽字段，会直接扩大宿主权限面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-30-02-01 | E | manifest entry | mitigate | v2 contract 只允许本地 route / asset，不接受 remote URL 或动态脚本 |
| T-30-02-02 | T | legacy migration | mitigate | v1/v2 parse 缺失治理字段时默认更保守，不做 permissive fallback |
| T-30-02-03 | S | manifest truth ownership | mitigate | 继续沿用 school-scoped `manifestJson`，不引入平行 registry |

</threat_model>

<verification>
- `pnpm test --run src/lib/dal/plugins.builtins.test.ts src/lib/dal/plugins.test.ts`
- `pnpm test --run src/features/runtime-platform/contracts/contracts.test.ts`
- 读取 `src/lib/dto/resource-ai.ts`，确认 manifest v2 仍沿用现有 `PluginManifestSchema` 路径
</verification>

<success_criteria>
- plugin manifest v2 成为 runtime/plugin governance 的显式 contract truth。
- built-in HTML runtime 可迁移到 v2 而不破坏当前 `payload.runtime` freeze posture。
- manifest v2 不会引入 remote execution 或隐式放宽权限。
</success_criteria>

<output>
After completion, create `.planning/phases/30-capability-enforcement-and-plugin-lifecycle/30-02-SUMMARY.md`
</output>
