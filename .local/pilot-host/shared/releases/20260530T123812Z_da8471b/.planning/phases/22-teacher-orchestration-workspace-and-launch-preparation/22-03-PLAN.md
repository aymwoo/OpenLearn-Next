---
phase: 22-teacher-orchestration-workspace-and-launch-preparation
plan: 03
type: execute
wave: 3
depends_on:
  - 22-01
  - 22-02
files_modified:
  - src/components/authoring/authoring-status-panel.test.tsx
  - src/components/authoring/lesson-editor-header-actions.test.tsx
  - src/components/classroom/classroom-launch-panel.test.tsx
  - src/app/(teacher)/teacher/editor/page.tsx
  - scripts/verify-phase22-orchestration.ts
  - package.json
autonomous: true
requirements:
  - ORCH-02
  - ORCH-03
must_haves:
  truths:
    - Phase 22 has a dedicated verification command that fails when route discipline, launch safety boundaries, or readiness grading drift.
    - editor still requires explicit `courseId + lessonId`, and launch still relies on published versions rather than draft lesson state.
    - focused regression coverage exists for both editor-side preparation summary and launch-side orchestration behavior.
  artifacts:
    - path: scripts/verify-phase22-orchestration.ts
      provides: dedicated phase verifier for orchestration workspace and launch safety
      contains: verify:phase22
    - path: package.json
      provides: package command for phase verification
      contains: verify:phase22
    - path: src/components/classroom/classroom-launch-panel.test.tsx
      provides: launch workspace regression coverage
      contains: publishedVersionId
  key_links:
    - from: scripts/verify-phase22-orchestration.ts
      to: src/app/(teacher)/teacher/editor/page.tsx
      via: verifier locks explicit editor route scoping and prevents global fallback drift
      pattern: courseId|lessonId|CourseAwareEditorGuidance
    - from: scripts/verify-phase22-orchestration.ts
      to: src/components/classroom/classroom-launch-panel.tsx
      via: verifier locks launch readiness buckets and publishedVersion launch contract
      pattern: publishedVersionId|阻断项|需关注|建议完善
---

<objective>
为 Phase 22 补齐 focused regression 和 `verify:phase22` 守卫，确保 orchestration workspace、
teacher-owned editor entry、published-snapshot launch path 和 readiness grading 在后续阶段中
不会悄悄回退。

Purpose: 让 Phase 22 的结果可验证、可守护，而不是只靠人工记忆既定边界。
Output: updated tests, `verify:phase22` script, package command, route-boundary guard.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-teacher-orchestration-workspace-and-launch-preparation/22-CONTEXT.md
@.planning/phases/22-teacher-orchestration-workspace-and-launch-preparation/22-RESEARCH.md
@.planning/phases/22-teacher-orchestration-workspace-and-launch-preparation/22-UI-SPEC.md
@src/app/(teacher)/teacher/editor/page.tsx
@src/components/authoring/authoring-status-panel.test.tsx
@src/components/authoring/lesson-editor-header-actions.test.tsx
@src/components/classroom/classroom-launch-panel.test.tsx
@scripts/verify-phase17-editor.ts
@scripts/verify-phase19-shell-route-metadata.ts
@scripts/verify-phase21-contracts.ts
@package.json

<interfaces>
Phase 22 invariants to lock:
```ts
courseId
lessonId
publishedVersionId
blockingIssues
attentionIssues
advisoryIssues
```

Verification pattern to reuse:
```ts
type StaticCheck = { label: string; passed: boolean }
runPnpm(["test", "--run", ...], "phase 22 focused regression suite")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand focused regression coverage for editor preparation summary and launch orchestration</name>
  <files>src/components/authoring/authoring-status-panel.test.tsx, src/components/authoring/lesson-editor-header-actions.test.tsx, src/components/classroom/classroom-launch-panel.test.tsx</files>
  <read_first>src/components/authoring/authoring-status-panel.test.tsx, src/components/authoring/lesson-editor-header-actions.test.tsx, src/components/classroom/classroom-launch-panel.test.tsx, src/components/authoring/authoring-status-panel.tsx, src/components/authoring/lesson-editor-header-actions.tsx, src/components/classroom/classroom-launch-panel.tsx, src/components/classroom/classroom-launch-preview.tsx</read_first>
  <behavior>
    - Test 1: editor-side preparation summary renders `阻断项 / 需关注 / 建议完善` with the expected bucket semantics.
    - Test 2: editor header actions preserve preview/publish behavior while exposing launch-preparation guidance.
    - Test 3: launch panel regression asserts that default inference remains non-blocking and `publishedVersionId` is still passed to the action.
  </behavior>
  <action>Update the focused Vitest suites so Phase 22 behavior is asserted semantically rather than only through source-string reads. In `src/components/authoring/authoring-status-panel.test.tsx`, add coverage for the three readiness buckets and their Chinese labels. In `src/components/authoring/lesson-editor-header-actions.test.tsx`, add assertions that the header still includes preview/publish affordances and now exposes launch-preparation guidance without triggering a launch mutation. In `src/components/classroom/classroom-launch-panel.test.tsx`, extend the existing launch regression to assert the new readiness buckets, whole-class summary-only posture, and continued `publishedVersionId` form submission. Do not replace these with broad snapshot tests or source-file-only string tests.</action>
  <acceptance_criteria>
    - `src/components/authoring/authoring-status-panel.test.tsx` contains assertions for `阻断项`, `需关注`, and `建议完善`.
    - `src/components/authoring/lesson-editor-header-actions.test.tsx` contains assertions for both existing preview/publish actions and a launch-preparation cue.
    - `src/components/classroom/classroom-launch-panel.test.tsx` contains an assertion that `publishedVersionId` is present in submitted `FormData`.
    - none of the updated tests rely solely on `readFileSync(...).toContain(...)` for the new Phase 22 behavior.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/authoring/authoring-status-panel.test.tsx src/components/authoring/lesson-editor-header-actions.test.tsx src/components/classroom/classroom-launch-panel.test.tsx</automated>
  </verify>
  <done>Phase 22 UI behavior is protected by targeted semantic regressions across both editor and launch surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `verify:phase22` to guard route discipline, published-snapshot launch safety, and readiness grading</name>
  <files>src/app/(teacher)/teacher/editor/page.tsx, scripts/verify-phase22-orchestration.ts, package.json</files>
  <read_first>src/app/(teacher)/teacher/editor/page.tsx, src/components/classroom/classroom-launch-panel.tsx, src/components/classroom/classroom-launch-preview.tsx, src/components/authoring/authoring-status-panel.tsx, scripts/verify-phase17-editor.ts, scripts/verify-phase19-shell-route-metadata.ts, scripts/verify-phase21-contracts.ts, package.json</read_first>
  <behavior>
    - Test 1: verifier fails if `/teacher/editor` loses explicit `courseId + lessonId` gating.
    - Test 2: verifier fails if `/teacher/launch` loses published-snapshot wording or the `publishedVersionId` launch contract.
    - Test 3: verifier fails if readiness grading collapses back into a single blocking list or if the focused regression suite fails.
  </behavior>
  <action>Create `scripts/verify-phase22-orchestration.ts` and register `verify:phase22` in `package.json`, following the repo's existing `StaticCheck + runPnpm()` verifier pattern. The static checks must cover at least these items: (1) `src/app/(teacher)/teacher/editor/page.tsx` still references `courseId`, `lessonId`, and `CourseAwareEditorGuidance`; (2) `src/components/classroom/classroom-launch-panel.tsx` still appends `publishedVersionId` to the launch form data; (3) launch/editor surfaces contain the exact readiness bucket labels `阻断项`, `需关注`, and `建议完善`; (4) launch preview still contains the published-snapshot wording rather than draft-launch semantics; (5) launch UI does not introduce subgroup/exclusion controls; and (6) package scripts expose `verify:phase22`. Then run the focused Phase 22 test files from this verifier. Keep `src/app/(teacher)/teacher/editor/page.tsx` aligned with the existing explicit route discipline if minor string or guidance adjustments are needed for the verifier, but do not loosen that boundary.</action>
  <acceptance_criteria>
    - `scripts/verify-phase22-orchestration.ts` exists and contains checks for `courseId`, `lessonId`, `publishedVersionId`, and the three readiness bucket labels.
    - `scripts/verify-phase22-orchestration.ts` contains a guard against subgroup/exclusion UI strings.
    - `package.json` contains `verify:phase22` pointing to `tsx scripts/verify-phase22-orchestration.ts`.
    - `scripts/verify-phase22-orchestration.ts` runs `pnpm test --run` against the focused Phase 22 regression files.
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase22</automated>
  </verify>
  <done>Phase 22 gains an explicit verifier that will fail fast if orchestration route discipline or launch safety regresses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository source -> phase verifier | verifier decides whether orchestration and launch safety invariants still hold |
| editor/launch UI -> regression suites | semantic UI tests must prove behavior rather than rely on memory or manual review |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-09 | T | `scripts/verify-phase22-orchestration.ts` | mitigate | Statically check route discipline, readiness labels, published-snapshot wording, and subgroup-control absence so drift fails fast. |
| T-22-10 | E | `src/app/(teacher)/teacher/editor/page.tsx` | mitigate | Keep explicit `courseId + lessonId` guidance and fail verification if the editor falls back to a global route again. |
| T-22-11 | I | `src/components/classroom/classroom-launch-panel.test.tsx` | mitigate | Prove `publishedVersionId` remains part of the launch contract so the workspace cannot quietly start from draft-only state. |
| T-22-12 | R | focused UI tests | accept | Regression suites validate UI behavior locally and do not expose sensitive data. |
</threat_model>

<verification>
- `pnpm test --run src/components/authoring/authoring-status-panel.test.tsx src/components/authoring/lesson-editor-header-actions.test.tsx src/components/classroom/classroom-launch-panel.test.tsx`
- `pnpm verify:phase22`
</verification>

<success_criteria>
- Phase 22 has focused regression tests across editor and launch.
- Phase 22 has a dedicated verification command.
- editor route discipline and published-snapshot launch safety are explicitly guarded against regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/22-teacher-orchestration-workspace-and-launch-preparation/22-03-SUMMARY.md`
</output>
