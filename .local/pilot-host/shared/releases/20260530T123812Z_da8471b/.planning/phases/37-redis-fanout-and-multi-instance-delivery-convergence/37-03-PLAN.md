---
phase: 37-redis-fanout-and-multi-instance-delivery-convergence
plan: 03
type: execute
wave: 3
depends_on: ["37-01", "37-02"]
files_modified:
  - src/features/runtime-platform/seams/transport/redis-fanout-recovery.test.ts
  - scripts/verify-phase37-redis-fanout.ts
  - scripts/bootstrap-dev-db.ts
  - package.json
autonomous: true
requirements: [RTPX-02]
must_haves:
  truths:
    - "D-19/D-20/D-21: 仓库必须同时支持默认无 Redis 的 local-only 开发姿态，以及显式提供 Redis 时的专项 verify/smoke 证明；close posture 不能把 optional capability 写成默认已完成。"
    - "D-15/D-16/D-18: focused tests 与 verifier 必须证明 redis_fanout 的 degraded fallback 会被真实记录和呈现，而不是被静默吞掉。"
    - "D-01/D-04/D-05/D-08/D-09: verifier 必须守住 deploy authority、global toggle、new-session snapshot 与 no-hot-switch posture。"
    - "ROADMAP SC-2 + research: `verify:phase37` 与 Redis 专项 smoke 必须显式证明 reconnect 后 resubscribe 恢复成立，而不是只验证首次 fanout。"
  artifacts:
    - path: "src/features/runtime-platform/seams/transport/redis-fanout-recovery.test.ts"
      provides: "dedicated reconnect/resubscribe and degraded fallback proof"
    - path: "scripts/verify-phase37-redis-fanout.ts"
      provides: "canonical phase gate with static guards, focused suites, typecheck, and optional Redis smoke"
    - path: "package.json"
      provides: "verify:phase37 and Redis-specific entrypoints"
    - path: "scripts/bootstrap-dev-db.ts"
      provides: "honest local bootstrap note that defaults to local_only"
  key_links:
    - from: "scripts/verify-phase37-redis-fanout.ts"
      to: "package.json"
      via: "canonical verification command registration"
      pattern: "verify:phase37"
    - from: "scripts/verify-phase37-redis-fanout.ts"
      to: "src/features/runtime-platform/seams/transport/redis-fanout-recovery.test.ts"
      via: "reconnect/resubscribe and degraded fallback proof"
      pattern: "resubscribe"
    - from: "scripts/bootstrap-dev-db.ts"
      to: "src/lib/dal/system-transport-settings.ts"
      via: "dev bootstrap keeps default product posture honest"
      pattern: "local_only"
---

<objective>
收口 Phase 37 的证明与本地开发姿态：focused tests、`verify:phase37`、Redis 专项 smoke，以及“默认无 Redis 也能开发，但显式启用时必须证明 fanout 成立”的诚实 close posture。

Purpose: 没有统一 verifier 和 honest dev posture，Phase 37 很容易被误说成“默认多实例已完成”。
Output: focused test coverage, canonical phase verifier, optional Redis smoke path, honest bootstrap and close notes.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-redis-fanout-and-multi-instance-delivery-convergence/37-CONTEXT.md
@.planning/phases/37-redis-fanout-and-multi-instance-delivery-convergence/37-RESEARCH.md
@.planning/phases/37-redis-fanout-and-multi-instance-delivery-convergence/37-PATTERNS.md
@.planning/phases/36-websocket-classroom-transport-cutover/36-VERIFICATION.md
@.planning/phases/36-websocket-classroom-transport-cutover/36-04-PLAN.md
@scripts/verify-phase36-websocket-cutover.ts
@scripts/bootstrap-dev-db.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 dedicated Redis recovery / degraded smoke suite，锁住 reconnect、resubscribe 与 local-only fallback</name>
  <read_first>
    - .planning/phases/37-redis-fanout-and-multi-instance-delivery-convergence/37-CONTEXT.md
    - src/features/runtime-platform/seams/transport/redis-fanout-manager.ts
    - src/features/runtime-platform/seams/transport/gateway.ts
    - src/features/runtime-platform/seams/transport/ws-server.ts
    - .planning/phases/37-redis-fanout-and-multi-instance-delivery-convergence/37-01-PLAN.md
    - .planning/phases/37-redis-fanout-and-multi-instance-delivery-convergence/37-02-PLAN.md
  </read_first>
  <files>src/features/runtime-platform/seams/transport/redis-fanout-recovery.test.ts</files>
  <behavior>
    - Test 1: Redis subscriber reconnect 后，manager 会按 desired topic/ref-count 恢复订阅，新的 classroom/runtime fanout 能重新跨实例送达（ROADMAP SC-2）。
    - Test 2: subscriber 断开或 publish 失败时，publisher 实例允许 local-only degraded fallback，但 outer attempt truth 仍保持失败，不伪装成 delivered（D-15/D-16/D-17）。
    - Test 3: `local_only` session 不启动 Redis recovery path，避免把 optional capability 误写成默认基线（D-01/D-21）。
  </behavior>
  <action>新增一个 dedicated recovery/failure suite，用最小 blast radius 把 roadmap 要求的“连接异常恢复语义”固定下来，而不是继续把零散断言摊进所有前序 focused tests。该 suite 只聚焦 reconnect、resubscribe、cross-instance delivery 恢复与 degraded local fallback，并直接复用 37-01/37-02 已建立的 fanout manager、gateway、ws lifecycle seam。保持 Phase 36 的测试风格：语义断言优先于 implementation-detail snapshot；不要写“包含 redis 字样就算通过”的浅层 guard。</action>
  <acceptance_criteria>
    - 存在 `src/features/runtime-platform/seams/transport/redis-fanout-recovery.test.ts`，并显式覆盖 reconnect / resubscribe / degraded local fallback。
    - suite 明确断言恢复后的 cross-instance delivery 会重新成立，而不是只验证首次订阅成功。
    - suite 明确断言 `local_only` session 不触发 Redis recovery path。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/features/runtime-platform/seams/transport/redis-fanout-recovery.test.ts</automated>
  </verify>
  <done>Phase 37 的“连接异常恢复语义”已有独立、可聚焦的 proof，不再依赖泛化 action 描述或隐式默认行为。</done>
</task>

<task type="auto">
  <name>Task 2: 发布 `verify:phase37`、Redis 专项 smoke 与本地开发诚实姿态</name>
  <read_first>
    - scripts/verify-phase36-websocket-cutover.ts
    - scripts/bootstrap-dev-db.ts
    - package.json
    - .planning/phases/37-redis-fanout-and-multi-instance-delivery-convergence/37-RESEARCH.md
    - .planning/phases/37-redis-fanout-and-multi-instance-delivery-convergence/37-CONTEXT.md
  </read_first>
  <files>scripts/verify-phase37-redis-fanout.ts, scripts/bootstrap-dev-db.ts, package.json</files>
  <action>沿用 `verify:phase36` 风格新增 `scripts/verify-phase37-redis-fanout.ts` 与 `package.json` 脚本注册。`verify:phase37` 必须执行：1) non-comment static guards，防止绕过 `publishTransportEvent()`、把 Redis 写成 outer transport mode、把 student-facing surface 引入 Redis 文案、或在 source 中出现热切换 existing session 的实现；2) 汇总运行 37-01 与 37-02 已建立的 focused suites，再加上 Task 1 的 dedicated recovery suite；3) `pnpm typecheck`；4) 如果存在 `REDIS_URL` 或等价显式环境，则运行 Redis 专项 smoke，验证双实例/双 consumer fanout、reconnect 后 resubscribe 恢复，以及 degraded fallback，否则诚实输出“Redis smoke skipped because deploy capability not provided”，符合 D-19/D-20/D-21。同步更新 `scripts/bootstrap-dev-db.ts`，让 dev bootstrap 明确 seed 为 local_only 或 product-disabled，并输出“未提供 Redis 时默认走单实例 local fanout”的说明；不要在 bootstrap 里偷偷开启 Redis 模式。</action>
  <acceptance_criteria>
    - `package.json` 至少注册 `verify:phase37`，并可选注册 `verify:phase37:redis`。
    - verifier 明确检查“不得绕过 `publishTransportEvent()`”、“不得把 Redis 写成默认 transport baseline”、“不得热切换 existing session”。
    - verifier 明确把 reconnect / resubscribe recovery proof 列为 Redis 专项验证的一部分，而不是只验证首次订阅成功。
    - verifier 输出中明确写出：Redis fanout 只在显式启用且部署允许时成立；无 Redis 的默认开发路径仍合法（D-19/D-21）。
    - `scripts/bootstrap-dev-db.ts` 的开发说明与默认 seed posture 不会把 Redis 伪装成默认开启。
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase37</automated>
  </verify>
  <done>Phase 37 有单一可信 gate 和诚实的本地姿态：默认 local-only 合法，显式 Redis 模式必须被专项证明。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier static guard → source files | 不能让注释命中或宽松 token 误判 Phase 37 已完成 |
| local dev bootstrap → product posture | bootstrap 不能偷偷把 optional Redis 能力伪装成默认基线 |
| Redis smoke gate → close claim | 没有 Redis service 时必须诚实 skip，而不是假装多实例已验证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03-01 | T | `verify-phase37-redis-fanout.ts` | mitigate | 使用 non-comment static guards，并绑定 focused suites 与 typecheck，落实 D-20 |
| T-37-03-02 | R | close posture | mitigate | verifier 输出明确声明 optional capability 与 skip 原因，落实 D-19/D-21 |
| T-37-03-03 | I | bootstrap/dev messaging | mitigate | `bootstrap-dev-db.ts` 只 seed local_only / product-disabled，不泄漏“默认多实例已完成”的错误口径 |
</threat_model>

<verification>
 - `pnpm test --run src/lib/dal/system-transport-settings.test.ts src/lib/dal/classroom.test.ts src/features/runtime-platform/seams/transport/ws-adapter.test.ts src/features/runtime-platform/seams/transport/gateway.test.ts src/features/runtime-platform/seams/transport/ws-server.test.ts src/features/runtime-platform/seams/transport/redis-fanout-manager.test.ts src/lib/dal/runtime-inspector.test.ts src/components/surfaces/settings-surface.test.tsx src/components/surfaces/runtime-inspector-surface.test.tsx src/components/classroom/classroom-control-panel.test.tsx`
 - `pnpm verify:phase37`
</verification>

<success_criteria>
 - 仓库存在单一 canonical `verify:phase37` gate，覆盖 authority、snapshot、fanout、degraded、surface 与 typecheck。
 - Redis smoke 只有在显式提供 Redis capability 时才执行；缺失时诚实 skip，不影响默认 local-only 开发路径。
 - Phase 37 的 close posture 明确写出“显式启用且部署允许时成立”，不把 optional capability 误写成默认完成态。
</success_criteria>

<output>
After completion, create `.planning/phases/37-redis-fanout-and-multi-instance-delivery-convergence/37-03-SUMMARY.md`
</output>
