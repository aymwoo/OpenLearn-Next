---
phase: quick
plan: 260510-07a
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dal/themes.ts
  - src/lib/dal/themes.test.ts
  - src/components/surfaces/settings-surface.tsx
  - src/components/surfaces/settings-surface.test.tsx
autonomous: true
requirements:
  - QUICK-settings-manifest-theme
must_haves:
  truths:
    - "设置页始终展示默认主题入口。"
    - "当当前学校没有已启用且带 `manifest.theme` 的插件主题时，设置页明确提示暂无可用主题。"
    - "启用带 `manifest.theme` 的插件后，设置页会出现对应的学校范围主题选项。"
  artifacts:
    - path: "src/lib/dal/themes.ts"
      provides: "按学校返回仅可用的插件主题选项"
    - path: "src/components/surfaces/settings-surface.tsx"
      provides: "默认主题卡片、空态提示与可选主题卡片"
    - path: "src/components/surfaces/settings-surface.test.tsx"
      provides: "设置页主题空态与选项回归验证"
  key_links:
    - from: "src/lib/dal/themes.ts"
      to: "src/lib/dal/plugins.ts"
      via: "已启用插件的 manifest.theme 与 `${plugin.name} theme` 现有命名约定"
      pattern: "manifest.theme"
    - from: "src/components/surfaces/settings-surface.tsx"
      to: "src/actions/theme-actions.ts"
      via: "form action={setActiveThemeAction}"
      pattern: "setActiveThemeAction"
---

<objective>
把设置页的主题可选项收敛为“默认主题 + 当前学校已启用的 `manifest.theme` 插件主题”，并在没有可用插件主题时稳定展示中文空态提示。

Purpose: 让设置页文案与真实可选主题来源一致，避免插件已停用或学校无主题时仍出现误导性的额外主题选项。
Output: 一个按插件启用状态过滤的主题读取路径，以及一个能稳定显示默认主题/空态/可选项的设置页回归集。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@src/lib/dal/themes.ts
@src/lib/dal/plugins.ts
@src/components/surfaces/settings-surface.tsx

<interfaces>
From src/lib/dal/themes.ts:
```typescript
export async function getValidThemesForSchool(schoolId: string): Promise<ThemeRegistryDTO[]>;
export async function getActiveThemeForCurrentActor(themeId: string): Promise<ThemeRegistryDTO | null>;
```

From src/lib/dal/plugins.ts:
```typescript
export async function setPluginEnabled(input: {
  actorId: string;
  schoolId: string;
  pluginId: string;
  enabled: boolean;
});
// enabling theme plugins currently registers `${plugin.name} theme`
```

From src/actions/theme-actions.ts:
```typescript
export async function setActiveThemeAction(input: FormData | Record<string, unknown>);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 收紧学校主题列表到已启用的 manifest.theme 插件</name>
  <files>src/lib/dal/themes.ts, src/lib/dal/themes.test.ts</files>
  <behavior>
    - Test 1: `getValidThemesForSchool(schoolId)` 继续要求 school scope + `validationStatus === "valid"`，且只返回能匹配到当前学校已启用 `manifest.theme` 插件的主题记录。
    - Test 2: 已停用插件对应的主题不会继续出现在设置页候选列表里；默认主题不依赖数据库记录，仍由 UI 单独提供。
  </behavior>
  <action>在 `src/lib/dal/themes.ts` 中为 `getValidThemesForSchool` 增加插件启用态过滤：读取当前学校 `pluginRegistrations`，解析带 `manifest.theme` 且 `enabled === true` 的插件，并复用现有 `${plugin.name} theme` 命名约定筛出可展示主题；不要新增 schema/migration，也不要把 UI 默认主题写入数据库。同步更新 `src/lib/dal/themes.test.ts`，把断言提升为“school + valid + enabled theme-plugin contract”三件事都被覆盖，避免只验证原有 school/valid 条件。</action>
  <verify>
    <automated>pnpm vitest run src/lib/dal/themes.test.ts src/lib/dal/plugins.test.ts</automated>
  </verify>
  <done>学校主题读取结果只包含当前学校已启用且声明了 `manifest.theme` 的插件主题；停用插件后，对应主题不会再被设置页列为可选项。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 固化设置页默认主题与“暂无可用主题”空态</name>
  <files>src/components/surfaces/settings-surface.tsx, src/components/surfaces/settings-surface.test.tsx</files>
  <behavior>
    - Test 1: 当 `getValidThemesForSchool` 返回空数组时，设置页仍显示“默认主题”卡片，并出现“当前学校还没有可用主题。启用带有 `manifest.theme` 的插件后，这里会显示可选项。”提示。
    - Test 2: 当存在可用主题时，设置页显示默认主题 + 插件主题卡片，且空态提示消失；主题切换仍走 `setActiveThemeAction` 表单提交。
  </behavior>
  <action>在 `src/components/surfaces/settings-surface.tsx` 中保持默认主题卡片始终可见，并把附加主题区明确绑定到 Task 1 的过滤结果；无结果时展示现有中文空态提示，有结果时仅渲染真实可用的主题卡片。不要引入客户端状态、localStorage、额外 fetch，继续使用服务端组件 + `setActiveThemeAction`。同步更新 `src/components/surfaces/settings-surface.test.tsx`：优先用渲染级回归覆盖默认主题常驻、空态显示/消失、以及 action 绑定仍在，而不是只做源码字符串命中。</action>
  <verify>
    <automated>pnpm vitest run src/components/surfaces/settings-surface.test.tsx src/actions/theme-actions.test.ts</automated>
  </verify>
  <done>设置页在任何学校范围下都先展示默认主题；只有启用中的 `manifest.theme` 插件会带来额外可选项；无可选项时中文空态提示稳定出现。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin registry → theme list | 非受信插件 manifest 只能通过已存在的 schema 校验与启用状态进入主题候选集合 |
| settings form → theme action | 用户提交的 `themeId` 仍只能通过现有 Server Action 与 school scope 校验生效 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | I | `getValidThemesForSchool` | mitigate | 只暴露当前学校、`validationStatus=valid`、且匹配启用中 `manifest.theme` 插件的主题，避免停用插件主题继续泄露到设置页 |
| T-quick-02 | E | `settings-surface` + `setActiveThemeAction` | mitigate | 继续沿用服务端表单与既有 theme action，不新增客户端直写 cookie 或绕过 school scope 的入口 |
</threat_model>

<verification>
- `pnpm vitest run src/lib/dal/themes.test.ts src/lib/dal/plugins.test.ts`
- `pnpm vitest run src/components/surfaces/settings-surface.test.tsx src/actions/theme-actions.test.ts`
- 手工检查设置页逻辑时，应满足“默认主题始终可见；无插件主题时出现空态；启用带 `manifest.theme` 的插件后出现额外选项”。
</verification>

<success_criteria>
- [ ] 设置页不再把停用插件留下的主题记录误当作可选项展示
- [ ] 默认主题始终可用，不依赖任何主题 registry 数据
- [ ] 当前学校无可用插件主题时，会显示指定中文提示
- [ ] 启用带 `manifest.theme` 的插件后，设置页出现对应可选主题卡片
</success_criteria>

<output>
After completion, create `.planning/quick/260510-07a-settings-manifest-theme/260510-07a-SUMMARY.md`
</output>
