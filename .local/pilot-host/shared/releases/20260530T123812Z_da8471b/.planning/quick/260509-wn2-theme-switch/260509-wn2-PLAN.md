---
phase: quick
plan: 260509-wn2
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/bootstrap-dev-db.ts
  - scripts/bootstrap-dev-db.test.ts
  - src/components/surfaces/settings-surface.tsx
  - src/components/surfaces/settings-surface.test.tsx
autonomous: true
requirements:
  - QUICK-theme-switch
must_haves:
  truths:
    - "设置页主题区至少提供一个基于主题插件设计的非默认主题。"
    - "用户可以在默认主题与新增主题之间切换，并继续走 activeThemeId + ThemeInjector 现有链路。"
    - "新增主题继续受学校范围和主题令牌校验约束，不引入任意脚本或绕过 DAL/Server Action。"
  artifacts:
    - path: "scripts/bootstrap-dev-db.ts"
      provides: "开发环境可用的替代主题插件/主题种子"
    - path: "src/components/surfaces/settings-surface.tsx"
      provides: "可见的主题切换卡片与当前选中态"
  key_links:
    - from: "scripts/bootstrap-dev-db.ts"
      to: "src/lib/dal/themes.ts"
      via: "manifest.theme -> valid theme registry"
      pattern: "theme"
    - from: "src/components/surfaces/settings-surface.tsx"
      to: "src/actions/theme-actions.ts"
      via: "form action={setActiveThemeAction}"
      pattern: "setActiveThemeAction"
---

<objective>
基于现有主题插件设计，为开发环境补上一套可切换的第二主题，并让设置页明确展示当前主题与可切换选项。

Purpose: 让主题系统从“有切换管线”变成“有真实第二样式可切换”，便于后续继续扩展更多主题插件。
Output: 一个可重复 bootstrap 的替代主题来源，以及一个能清楚显示默认/当前/替代主题的设置页切换区。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@docs/theme-system-design.md
@docs/plugin-theme-implementation-plan.md
@scripts/bootstrap-dev-db.ts
@src/components/surfaces/settings-surface.tsx

<interfaces>
From src/lib/dto/resource-ai.ts:
```typescript
export const PluginManifestSchema = z.object({
  id: z.string(),
  version: z.string(),
  permissions: z.array(z.string()).default([]),
  anchors: z.array(PluginHookAnchorSchema),
  actions: z.array(PluginActionSchema),
  builtIn: z.boolean().default(false),
  defaultEnabled: z.boolean().default(false),
  nonDeletable: z.boolean().default(false),
  theme: ThemeTokenRegistrySchema.optional(),
});
```

From src/lib/dal/themes.ts:
```typescript
export async function getValidThemesForSchool(schoolId: string): Promise<ThemeRegistryDTO[]>;
export async function getActiveThemeForCurrentActor(themeId: string): Promise<ThemeRegistryDTO | null>;
```

From src/actions/theme-actions.ts:
```typescript
export async function setActiveThemeAction(input: FormData | Record<string, unknown>);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为开发环境补齐基于插件设计的第二主题来源</name>
  <files>scripts/bootstrap-dev-db.ts, scripts/bootstrap-dev-db.test.ts</files>
  <behavior>
    - Test 1: bootstrap 定义中存在一个带 `manifest.theme` 的替代主题插件，令牌继续满足 Lexend 与允许的 surface role 约束。
    - Test 2: 重复执行 bootstrap 时，同学校同主题名称不会无限新增重复主题注册记录。
  </behavior>
  <action>在 `scripts/bootstrap-dev-db.ts` 中新增一个面向开发环境的替代主题定义，主题令牌必须继续使用纯 JSON `manifest.theme` 描述，不新增任意 JS 执行入口；主题视觉需与默认主题明显区分，但仍遵守简体中文、Lexend、tonal surface 和 `--color-surface-*` 现有主题约定。为 bootstrap 增加与该主题插件同源的主题注册/upsert 逻辑，确保同一学校重复 bootstrap 时不会不断堆积同名主题记录。新增 `scripts/bootstrap-dev-db.test.ts`，用聚焦测试锁定“主题定义存在、来源单一、bootstrap 幂等”三件事，不把验证退化成只搜字符串。</action>
  <verify>
    <automated>pnpm vitest run scripts/bootstrap-dev-db.test.ts</automated>
  </verify>
  <done>开发环境完成 bootstrap 后，学校范围内至少存在默认主题之外的一套有效主题，且该主题来源仍然符合主题插件设计。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 让设置页清楚呈现当前主题并可切换到新增样式</name>
  <files>src/components/surfaces/settings-surface.tsx, src/components/surfaces/settings-surface.test.tsx</files>
  <behavior>
    - Test 1: 设置页读取当前 `activeThemeId` 后，默认主题与替代主题会显示明确的当前选中态。
    - Test 2: 主题卡片继续通过 `setActiveThemeAction` 提交，默认主题仍可清除 cookie 恢复系统外观。
  </behavior>
  <action>在 `src/components/surfaces/settings-surface.tsx` 中接入 `getActiveThemeId()`，让默认主题卡和每个有效主题卡都能显示“当前使用中”状态、选中 ring 或等价 tonal 强调，并补上能区分新增主题风格的简短说明文案。继续使用现有服务端表单 + `setActiveThemeAction`，不要引入客户端状态、localStorage 或绕过 cookie 的快捷路径。同步更新 `src/components/surfaces/settings-surface.test.tsx`，覆盖替代主题出现、当前选中态和切换表单仍绑定到现有 action 的回归。</action>
  <verify>
    <automated>pnpm vitest run src/components/surfaces/settings-surface.test.tsx src/actions/theme-actions.test.ts</automated>
  </verify>
  <done>设置页可见默认主题与新增主题，且当前主题状态一眼可见；用户继续通过现有服务端动作完成切换。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin manifest → theme registry | 非受信主题令牌进入系统前必须经过 schema 与 design guard 校验 |
| settings form → theme action | 用户提交的 themeId 只能通过现有 Server Action + school scope 生效 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | T | `scripts/bootstrap-dev-db.ts` theme seed | mitigate | 只写入 JSON `manifest.theme`，保持 Lexend/surface role 合法值，不新增任意代码执行能力 |
| T-quick-02 | E | `setActiveThemeAction` + `ThemeInjector` | mitigate | 继续复用 `getActiveThemeForCurrentActor()` 的 school scope 校验，设置页仅提交合法 themeId |
|
</threat_model>

<verification>
- `pnpm vitest run scripts/bootstrap-dev-db.test.ts`
- `pnpm vitest run src/components/surfaces/settings-surface.test.tsx src/actions/theme-actions.test.ts`
- 开发 bootstrap 后，设置页主题区能看到“默认主题 + 新增主题”至少两项可选卡片。
</verification>

<success_criteria>
- [ ] 开发环境存在一套新的、有效的、基于主题插件设计的替代主题
- [ ] 设置页能明确展示当前主题，并保留恢复默认主题入口
- [ ] 主题切换仍走现有 cookie + ThemeInjector + school-scope 校验链路
- [ ] 新增验证覆盖 bootstrap 幂等与设置页切换回归
</success_criteria>

<output>
After completion, create `.planning/quick/260509-wn2-theme-switch/260509-wn2-SUMMARY.md`
</output>
