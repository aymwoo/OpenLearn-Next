---
phase: quick
plan: 260510-v1f
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/authoring/lesson-authoring-workspace.tsx
  - src/components/authoring/lesson-authoring-workspace.test.tsx
autonomous: true
requirements:
  - QUICK-260510-V1F
---

<objective>
继续修改 /teacher/editor 页面，移除 `lesson-flow-composer` 外层 section 壳样式，只保留其内部资源库内容结构。

Purpose: 让左侧资源库不再带有 `Card` 生成的 section/card shell，只保留内部 rounded rail 内容层。

Output: 精简后的左侧资源库外层结构，以及对应回归测试。
</objective>

<context>
@.planning/STATE.md
@AGENTS.md
@src/components/authoring/lesson-authoring-workspace.tsx
@src/components/authoring/lesson-authoring-workspace.test.tsx
@src/components/ui/card.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 移除 lesson-flow-composer 外层 Card 壳并锁定回归</name>
  <files>src/components/authoring/lesson-authoring-workspace.tsx, src/components/authoring/lesson-authoring-workspace.test.tsx</files>
  <behavior>
    - 左侧 `lesson-flow-composer` 不再由带 `shadow-ambient` 的 `Card/section` 包裹。
    - 内部 rounded rail、搜索、筛选、资源项与流程主线行为保持不变。
  </behavior>
  <action>用普通容器替换左侧资源库 `Card` 外壳，仅保留 `lesson-flow-composer` 内部资源 rail 结构，并在测试中补充源码级回归断言，防止再次引入左侧 `Card` 壳。</action>
  <verify>
    <automated>pnpm test --run "src/components/authoring/lesson-authoring-workspace.test.tsx" "src/components/authoring/lesson-step-editor.test.tsx"</automated>
  </verify>
  <done>左侧资源库只剩内部结构，不再渲染为带 card shell 的 section，相关测试通过。</done>
</task>

</tasks>
