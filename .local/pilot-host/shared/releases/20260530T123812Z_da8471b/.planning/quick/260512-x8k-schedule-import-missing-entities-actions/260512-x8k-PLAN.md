---
+type: quick
+slug: 260512-x8k-schedule-import-missing-entities-actions
+scope: /teacher/schedule
+autonomous: true
+files_modified:
+  - src/components/surfaces/teacher-schedule-surface.tsx
+  - src/components/surfaces/teacher-schedule-surface.test.tsx
+must_haves:
+  truths:
+    - 课表导入遇到班级、教师、课程不存在时，仍可进入 display-only 主课表预览。
+    - 预览态只做展示和动作引导，不放宽 approve/import runtime 写入边界。
+    - 动作入口优先复用现有页面与路由，不伪造不存在的管理页面。
+  artifacts:
+    - path: src/components/surfaces/teacher-schedule-surface.tsx
+      provides: 主课表中缺失实体的标记与动作入口
+    - path: src/components/surfaces/teacher-schedule-surface.test.tsx
+      provides: display-only 预览与动作链接的最小回归覆盖
+---

<objective>
修改课表导入行为：即使班级、教师、课程不存在，也允许导入并在课表呈现时标记出来，同时给出跳转到现有管理入口或新建入口的动作。

Purpose: 基于现有 schedule import display-only 预览链路做最小正确实现，不新增新的导入写库路径。
Output: 主课表 display-only 预览可承载 class/teacher/course 缺失标记，并在单元格内展示现有动作链接。
</objective>

<verification>
1. `pnpm vitest run src/components/surfaces/teacher-schedule-surface.test.tsx src/components/surfaces/schedule-import-modal.test.tsx`
</verification>
