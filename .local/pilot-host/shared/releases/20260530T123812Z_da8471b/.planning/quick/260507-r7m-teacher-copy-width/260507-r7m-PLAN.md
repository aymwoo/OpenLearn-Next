---
phase: quick
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/surfaces/teacher-dashboard-surface.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "教师首页的'今日课表与运行节奏'说明文案不再被额外的窄宽度上限压缩。"
    - "保持上午直播、下午英语课和批改闭环的说明继续与主模块处于同一视野。"
  artifacts:
    - path: "src/components/surfaces/teacher-dashboard-surface.tsx"
      provides: "去除教师首页说明文案的无关宽度限制"
  key_links: []
---

<objective>
修复教师首页“今日课表与运行节奏”说明文案宽度异常的问题，移除不需要的 `max-w-2xl` 约束，让文案按当前主模块宽度自然展开。

Purpose: 用最小改动消除错误的文本宽度限制，不改变教师首页既有布局、信息层级与模块关系。
Output: 更新教师首页对应说明段落的 className，并完成最小校验。
</objective>

<context>
@.planning/STATE.md
@AGENTS.md
@src/components/surfaces/teacher-dashboard-surface.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 移除教师首页说明文案的额外宽度上限</name>
  <files>src/components/surfaces/teacher-dashboard-surface.tsx</files>
  <action>定位“今日课表与运行节奏”模块中的说明段落，只移除导致文案变窄的 `max-w-2xl` class，其余排版、语义和布局保持不变，不引入新的抽象或样式补丁。</action>
  <verify>
    <automated>npx eslint src/components/surfaces/teacher-dashboard-surface.tsx</automated>
  </verify>
  <done>教师首页该段说明文案不再被限制在窄列宽中，并保持现有页面结构不变。</done>
</task>

</tasks>

<verification>
- “保持上午直播、下午英语课和批改闭环在同一视野里” 这段文案不再带 `max-w-2xl`。
- 教师首页文件通过 scoped eslint。
</verification>

<success_criteria>
教师首页主信息说明宽度恢复为与当前模块一致的自然宽度，没有新增无关样式规则或扩大改动范围。</success_criteria>
