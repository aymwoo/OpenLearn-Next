---
phase: 37-redis-fanout-and-multi-instance-delivery-convergence
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - src/features/runtime-platform/seams/transport/gateway.ts
  - src/features/runtime-platform/seams/transport/ws-server.ts
  - src/features/runtime-platform/seams/transport/ws-connection-registry.ts
  - src/features/runtime-platform/seams/transport/redis-fanout-manager.ts
  - src/lib/dto/runtime-inspector.ts
  - src/lib/dal/runtime-inspector.ts
  - src/components/surfaces/runtime-inspector-surface.tsx
  - src/components/surfaces/settings-surface.tsx
  - src/components/classroom/classroom-control-panel.tsx
autonomous: true
requirements: [RTPX-02]
must_haves:
  truths:
    - "D-15/D-16/D-17: 当 redis_fanout 会话的 publish 或 subscribe 失效时，系统必须把跨实例 fanout 失败记成 degraded failure，同时允许 publisher 实例 local-only fallback，不伪装成 delivered。"
    - "D-18: Redis degrade 事实必须同时被 settings/runtime inspector 与 `/classroom` teacher operator 看见；学生面继续只保留 reconnect/snapshot fallback 语义。"
    - "D-03/D-08/D-09: gateway、ws-server、registry 都以 session snapshot 为准收口 fanout 行为，不新增绕过 `publishTransportEvent()` 的控制路径。"
    - "ROADMAP SC-2 + research: 连接异常恢复必须是显式计划内容；Redis reconnect 后要按 desired topics 恢复订阅，并重新建立跨实例 fanout 证明。"
  artifacts:
    - path: "src/features/runtime-platform/seams/transport/gateway.ts"
      provides: "attempt and consumer-trace truth for redis delivered vs degraded fallback"
    - path: "src/features/runtime-platform/seams/transport/ws-server.ts"
      provides: "subscription lifecycle tied to local socket ownership"
    - path: "src/lib/dal/runtime-inspector.ts"
      provides: "operator read model for transport topology and degraded reason"
    - path: "src/components/surfaces/settings-surface.tsx"
      provides: "server-first product surface for deploy authority, toggle state, and health"
    - path: "src/components/classroom/classroom-control-panel.tsx"
      provides: "teacher-only degraded banner with drill-down"
  key_links:
    - from: "src/features/runtime-platform/seams/transport/gateway.ts"
      to: "src/features/runtime-platform/seams/transport/redis-fanout-manager.ts"
      via: "attempt truth maps redis publish result and degraded fallback"
      pattern: "attemptStatus"
    - from: "src/features/runtime-platform/seams/transport/ws-server.ts"
      to: "src/features/runtime-platform/seams/transport/ws-connection-registry.ts"
      via: "register/unregister drives redis topic subscription lifecycle"
      pattern: "ensureSubscribed|releaseSubscription"
    - from: "src/lib/dal/runtime-inspector.ts"
      to: "src/components/surfaces/runtime-inspector-surface.tsx"
      via: "single timeline exposes transport topology and degraded reason"
      pattern: "degraded"
---

<objective>
把 fanout 真正收口到运行中的 websocket delivery：gateway 记录真实 degraded semantics，ws-server/registry 管理订阅生命周期，operator surface 诚实展示“仅本实例继续同步”与“跨实例 fanout 已失效”。

Purpose: 基础设施接好但没有诚实的运行时收口，会让 classroom/operator 误判 Redis 多实例已经可靠工作。
Output: Redis-aware gateway semantics, ws subscription lifecycle, runtime inspector and settings truth, teacher-only degraded affordance.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-redis-fanout-and-multi-instance-delivery-convergence/37-CONTEXT.md
@.planning/phases/37-redis-fanout-and-multi-instance-delivery-convergence/37-RESEARCH.md
@.planning/phases/37-redis-fanout-and-multi-instance-delivery-convergence/37-PATTERNS.md
@.planning/phases/36-websocket-classroom-transport-cutover/36-VERIFICATION.md
@.planning/phases/36-websocket-classroom-transport-cutover/36-04-PLAN.md
@src/features/runtime-platform/seams/transport/gateway.ts
@src/features/runtime-platform/seams/transport/ws-server.ts
@src/features/runtime-platform/seams/transport/ws-connection-registry.ts
@src/lib/dal/runtime-inspector.ts
@src/components/surfaces/runtime-inspector-surface.tsx
@src/components/surfaces/settings-surface.tsx
@src/components/classroom/classroom-control-panel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 让 gateway、ws-server、registry 按 session snapshot 执行 fanout，并把 degraded fallback 记成真失败</name>
  <read_first>
    - .planning/phases/37-redis-fanout-and-multi-instance-delivery-convergence/37-CONTEXT.md
    - src/features/runtime-platform/seams/transport/gateway.ts
    - src/features/runtime-platform/seams/transport/ws-server.ts
    - src/features/runtime-platform/seams/transport/ws-connection-registry.ts
    - src/features/runtime-platform/seams/transport/redis-fanout-manager.ts
    - src/lib/dal/classroom.ts
    - .planning/phases/36-websocket-classroom-transport-cutover/36-VERIFICATION.md
  </read_first>
  <files>src/features/runtime-platform/seams/transport/gateway.ts, src/features/runtime-platform/seams/transport/ws-server.ts, src/features/runtime-platform/seams/transport/ws-connection-registry.ts, src/features/runtime-platform/seams/transport/redis-fanout-manager.ts</files>
  <behavior>
    - Test 1: local_only session 仍走本实例 registry broadcast，attempt=delivered。
    - Test 2: redis_fanout session 正常 publish+subscriber 路径下，attempt=delivered，consumer trace 能区分 `receivedVia=redis_subscriber`。
    - Test 3: redis_fanout session 在 publish/subscriber 不健康时，gateway 把 attempt 标成 failed 并写 degraded reason，但 publisher 实例仍允许 local-only fallback（D-15/D-16/D-17）。
    - Test 4: Redis reconnect 后，fanout manager 会按 desired topic/ref-count 自动 resubscribe；恢复完成后新的 teacher control / runtime event 能重新跨实例送达（ROADMAP SC-2）。
    - Test 5: ws-server 在首个本地连接注册时订阅 topic，在最后一个连接离开时释放 topic；不把 Redis publish 搬进 inbound teacher/runtime handler（D-03）。
  </behavior>
  <action>按 D-03、D-15、D-16、D-17 收口 websocket runtime 行为：`gateway.ts` 继续作为唯一 attempt truth writer，对 redis_fanout 会话写出 `fanoutMode`、`redisTopic`、`degraded`、`degradedReason` 等 detail；发生 local fallback 时 attempt 仍记 failed，不能因为 publisher 实例还活着就伪装 delivered。`ws-server.ts`、`ws-connection-registry.ts` 与 `redis-fanout-manager.ts` 一起把本实例 socket ownership 映射到 topic subscribe ref-count，并显式维护 reconnect 后的 resubscribe 恢复语义：首个连接注册后 ensure subscribe，最后一个连接关闭后 release subscribe，Redis connection 重建后按 desired topics 自动恢复订阅。不要把 Redis publish、health probe 或任何业务 mutation 搬进 inbound `teacher.control` / `runtime.command` handler；这些路径继续只进 Phase 36 已锁定的 canonical DAL/runtime helper。</action>
  <acceptance_criteria>
    - `src/features/runtime-platform/seams/transport/gateway.ts` 对 redis degraded fallback 会写 `attemptStatus: "failed"` 或等价失败语义，并携带 degraded reason（D-15/D-17）。
    - `src/features/runtime-platform/seams/transport/ws-server.ts` 的 register/unregister 生命周期显式挂接 subscription manager，而不是在 message handler 内直接 publish Redis。
    - `src/features/runtime-platform/seams/transport/ws-connection-registry.ts` 继续只表达本实例连接所有权，不缓存 Redis truth。
    - `src/features/runtime-platform/seams/transport/redis-fanout-manager.ts` 明确存在 reconnect 后恢复 desired topics 的行为和 focused proof，不把“异常恢复语义”留给隐式默认值。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/features/runtime-platform/seams/transport/gateway.test.ts src/features/runtime-platform/seams/transport/ws-server.test.ts src/features/runtime-platform/seams/transport/redis-fanout-manager.test.ts</automated>
  </verify>
  <done>运行时 fanout 已按 session snapshot 工作，且 degraded 语义对 operator 诚实可解释。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把 deploy authority、health 与 degraded truth 暴露到 settings、runtime inspector 和 classroom operator surface</name>
  <read_first>
    - .planning/phases/37-redis-fanout-and-multi-instance-delivery-convergence/37-CONTEXT.md
    - src/lib/dto/runtime-inspector.ts
    - src/lib/dal/runtime-inspector.ts
    - src/components/surfaces/runtime-inspector-surface.tsx
    - src/components/surfaces/settings-surface.tsx
    - src/components/classroom/classroom-control-panel.tsx
  </read_first>
  <files>src/lib/dto/runtime-inspector.ts, src/lib/dal/runtime-inspector.ts, src/components/surfaces/runtime-inspector-surface.tsx, src/components/surfaces/settings-surface.tsx, src/components/classroom/classroom-control-panel.tsx</files>
  <behavior>
    - Test 1: settings surface 能区分 deploy disallowed、deploy allowed but product disabled、redis enabled、redis degraded 四种状态，并且非 developer/super_admin 是只读视图（D-04/D-05/D-07/D-18）。
    - Test 2: runtime inspector 的 summary + unified timeline 能解释 `local_only`、`redis_fanout`、`degraded local fallback`，不要求 operator 自己猜 transport 现状（D-18）。
    - Test 3: `/classroom` teacher operator 看到简洁但明确的 degraded banner，说明“当前仅保证本实例同步”；student/player 不新增 Redis 文案（D-18）。
  </behavior>
  <action>按 D-04、D-05、D-07、D-18 收口 operator surface。`settings-surface.tsx` 与 `/settings`/`/settings/labs` 继续走 server-first DTO，不新建 utilitarian admin console；在同一产品语言里同时展示 deploy authority、global toggle、effective mode、reachability 与最近 degraded reason。`runtime-inspector` 继续保持单时间线模型，只扩 transport topology、receivedVia、fanoutMode、degradedReason、lastHealthyAt 等字段。`classroom-control-panel.tsx` 仅在 teacher/operator 面显示紧凑提示卡，并提供去 settings 或 inspector 的 drill-down；不要把 Redis/multi-instance/degraded 文案泄漏到 student runtime client 或普通学习面。</action>
  <acceptance_criteria>
    - settings surface 明确表达“部署允许但产品关闭”和“部署不允许，因此无法启用”的差异（D-04/D-05）。
    - runtime inspector 继续是单一 timeline surface，不新增第二个 Redis operator page。
    - classroom degrade banner 只出现在 teacher/operator surface，不出现在 student-facing 文件中（D-18）。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/runtime-inspector.test.ts src/components/surfaces/runtime-inspector-surface.test.tsx src/components/surfaces/settings-surface.test.tsx src/components/classroom/classroom-control-panel.test.tsx</automated>
  </verify>
  <done>Redis mode 的真实行为已在 operator/product surface 中被诚实暴露，且不污染学生体验。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway attempt truth → operator UI | 如果 degraded 被误记为 delivered，operator 会错误判断跨实例健康 |
| ws register lifecycle → Redis subscribe lifecycle | 错误订阅释放会造成串流或重复消费 |
| teacher operator surface → student surface | 基础设施异常不应泄漏到 student-facing runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-02-01 | R | `gateway.ts` | mitigate | 对 redis_fanout degraded fallback 固定记录 failed + degraded reason，落实 D-15~D-17 |
| T-37-02-02 | D | `ws-server.ts` / registry | mitigate | 订阅按本实例连接 ref-count 管理，避免 subscribe-all 放大噪音或资源泄漏 |
| T-37-02-03 | I | classroom/player UI | mitigate | Redis degrade 仅在 settings/inspector/teacher operator 暴露，禁止 student-facing copy，落实 D-18 |
</threat_model>

<verification>
 - `pnpm test --run src/features/runtime-platform/seams/transport/gateway.test.ts src/features/runtime-platform/seams/transport/ws-server.test.ts`
 - `pnpm test --run src/lib/dal/runtime-inspector.test.ts src/components/surfaces/runtime-inspector-surface.test.tsx src/components/surfaces/settings-surface.test.tsx src/components/classroom/classroom-control-panel.test.tsx`
</verification>

<success_criteria>
 - redis_fanout 的正常与 degraded 语义都通过 gateway/trace 落为可回看的证据，不再只靠 console log。
 - settings、runtime inspector、classroom operator 三个 surface 都能解释 effective mode 与 degraded 原因。
 - 计划保持在 fanout/degrade/operator surfaces，不引入新的 durable truth、BullMQ、Redis Streams、Socket.IO 或 session 热切换。
</success_criteria>

<output>
After completion, create `.planning/phases/37-redis-fanout-and-multi-instance-delivery-convergence/37-02-SUMMARY.md`
</output>
