---
phase: 32-end-to-end-hardening-and-milestone-proof
plan: 03
type: execute
wave: 3
depends_on: [32-01, 32-02, 32-04]
files_modified:
  - scripts/verify-phase32-end-to-end.ts
  - package.json
  - src/components/classroom/classroom-launch-panel.test.tsx
  - src/lib/dal/lesson-authoring.test.ts
  - src/features/runtime-platform/host/runtime-host.test.tsx
  - src/components/surfaces/student-player-surfaces.test.ts
  - src/lib/dal/classroom.test.ts
  - src/lib/dal/runtime-inspector.test.ts
  - src/components/surfaces/runtime-inspector-surface.test.tsx
autonomous: true
requirements: [RHOST-04]
must_haves:
  truths:
    - "per D-13/D-15，仓库存在单一 `verify:phase32` gate，对外统一收口 compatibility baseline、cache freshness、session durability、capability denial、rollback-safe transport，以及 Phase 32 自己的 editor/publish continuity、canonical demo path、launch discoverability、terminal submit posture、failure recovery、classroom-first feedback 与 inspector review。"
    - "Phase 32 proof 不只是跑旧 verifier；它有自己针对 proof drift 的 focused assertions。"
  artifacts:
    - path: "scripts/verify-phase32-end-to-end.ts"
      provides: "canonical milestone proof verifier for phase 32"
    - path: "package.json"
      provides: "verify:phase32 script registration"
    - path: "src/components/classroom/classroom-launch-panel.test.tsx"
      provides: "regression coverage for launch-surface proof discoverability"
    - path: "src/lib/dal/lesson-authoring.test.ts"
      provides: "regression coverage for editor/publish continuity of the proof runtime step"
    - path: "src/lib/dal/runtime-inspector.test.ts"
      provides: "regression coverage for runtimeSessionId proof drill-down and reviewable event timeline posture"
  key_links:
    - from: "scripts/verify-phase32-end-to-end.ts"
      to: "package.json"
      via: "verify:phase32 command registration"
      pattern: "verify:phase32"
    - from: "scripts/verify-phase32-end-to-end.ts"
      to: "scripts/verify-phase27-runtime-platform.ts"
      via: "re-confirms milestone compatibility baseline inside one external gate"
      pattern: "verify:phase27"
    - from: "scripts/verify-phase32-end-to-end.ts"
      to: "src/lib/dal/lesson-authoring.test.ts"
      via: "editor/publish continuity of the proof runtime step"
      pattern: "published snapshot"
    - from: "scripts/verify-phase32-end-to-end.ts"
      to: "src/lib/dal/runtime-inspector.test.ts"
      via: "reviewable proof timeline and event log posture"
      pattern: "timeline"
    - from: "scripts/verify-phase32-end-to-end.ts"
      to: ".planning/phases/32-end-to-end-hardening-and-milestone-proof/32-DEMO-HANDOFF.md"
      via: "demo handoff drift guard for repeatable milestone proof"
      pattern: "teacher@example.com"
---

<objective>
建立 Phase 32 的 canonical proof gate：在依赖 Phase 27-31 已有 verifier 作为
milestone prerequisite 前置通过的同时，新增真正属于 Phase 32 milestone proof
的 drift checks 和 focused suites。

Purpose: 让 roadmap 要求的 compatibility baseline、freshness or durability、
capability denial、rollback-safe transport 与 Phase 32 自己负责的 editor or publish
continuity、proof path、inspector review 都能被单一外部 gate 自动证明，而不是
依赖一次性手工演示或多个分散命令。
Output: `verify:phase32` command, focused proof regression suites, static drift guards.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-CONTEXT.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-RESEARCH.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-PATTERNS.md
@scripts/verify-phase27-runtime-platform.ts
@scripts/verify-phase28-runtime-bridge.ts
@scripts/verify-phase29-runtime-host.ts
@scripts/verify-phase30-governance.ts
@scripts/verify-phase31-transport-inspector.ts
@package.json
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-DEMO-HANDOFF.md
@src/components/classroom/classroom-launch-panel.test.tsx
@src/lib/dal/lesson-authoring.test.ts
@src/lib/dal/runtime-inspector.test.ts
@src/features/runtime-platform/host/runtime-host.test.tsx
@src/components/surfaces/runtime-inspector-surface.test.tsx

<interfaces>
From `scripts/verify-phase29-runtime-host.ts`:
```ts
function run(args: readonly string[], label: string) { ... }
const checks = [ ... ];
```

From `scripts/verify-phase31-transport-inspector.ts`:
```ts
const checks = [
  { label: "transport drift" ... },
  { label: "scope drift" ... },
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 补齐 proof-focused regression suites</name>
  <read_first>src/lib/dal/lesson-authoring.test.ts, src/components/classroom/classroom-launch-panel.test.tsx, src/features/runtime-platform/host/runtime-host.test.tsx, src/components/surfaces/student-player-surfaces.test.ts, src/lib/dal/classroom.test.ts, src/lib/dal/runtime-inspector.test.ts, src/components/surfaces/runtime-inspector-surface.test.tsx</read_first>
  <files>src/lib/dal/lesson-authoring.test.ts, src/components/classroom/classroom-launch-panel.test.tsx, src/features/runtime-platform/host/runtime-host.test.tsx, src/components/surfaces/student-player-surfaces.test.ts, src/lib/dal/classroom.test.ts, src/lib/dal/runtime-inspector.test.ts, src/components/surfaces/runtime-inspector-surface.test.tsx</files>
  <behavior>
    - Test 1: editor/publish leg 持续把 canonical runtime proof step 冻结进 published snapshot。
    - Test 2: launch proof affordance 保持次级 discoverability，不取代现有新课堂主 CTA。
    - Test 3: terminal submit posture（success summary + no-save-after-submit + locked UI）被回归锁定。
    - Test 4: failure recovery（same-surface retry）被回归锁定。
    - Test 5: classroom first-feedback 与 inspector review posture 被回归锁定。
  </behavior>
  <action>围绕 32-01、32-02、32-04 补齐 focused suites，不新增宽泛 snapshot test。至少让现有 lesson authoring、launch、runtime host、student player、classroom DAL、inspector DAL/surface 测试分别断言：editor/publish 链路仍把 canonical runtime descriptor 冻结进 published snapshot、launch surface 仍保留 canonical proof affordance、submit success 后不再允许 save、save/submit failure 有 retry CTA、classroom 有 first-feedback 字段、inspector 按 `runtimeSessionId` 默认聚焦对应 proof session，并能审查 canonical proof 的 unified event timeline，而不是只打开空 inspector。避免用注释字符串计数代替行为断言。</action>
  <acceptance_criteria>
    - 七个 focused suites 至少各新增一条与 Phase 32 proof 相关的语义断言
    - 测试名称或断言文本可 grep 到 `editor/publish`、`runtimeSessionId`、`submit success`、`retry`、`proof`、`launch`、`timeline` 等 Phase 32 语义
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/lesson-authoring.test.ts src/components/classroom/classroom-launch-panel.test.tsx src/features/runtime-platform/host/runtime-host.test.tsx src/components/surfaces/student-player-surfaces.test.ts src/lib/dal/classroom.test.ts src/lib/dal/runtime-inspector.test.ts src/components/surfaces/runtime-inspector-surface.test.tsx</automated>
  </verify>
  <done>Phase 32 proof 的关键行为已由 focused regressions 锁住，而不是只靠人工点击演示。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立 `verify:phase32` 单一总闸门</name>
  <read_first>scripts/verify-phase29-runtime-host.ts, scripts/verify-phase31-transport-inspector.ts, package.json</read_first>
  <files>scripts/verify-phase32-end-to-end.ts, package.json</files>
  <behavior>
    - Test 1: `verify:phase32` 先执行上游 prerequisite verifiers，统一重确认 compatibility baseline、cache freshness、session durability、capability denial、rollback-safe transport，再执行 Phase 32 proof drift guards 与 focused suites。
    - Test 2: verifier 明确定义并检查 editor/publish continuity、canonical seed path、launch proof discoverability、terminal submit posture、failure recovery、classroom first-feedback、inspector review、demo handoff 这八类 Phase 32 contract，同时在输出中把它们和 milestone prerequisite baseline 区分开。
  </behavior>
  <action>复用 `32-PATTERNS.md` 的 Pattern 6（phase verifier structure），新增 `scripts/verify-phase32-end-to-end.ts`，复用 Phase 29/31 verifier 的结构，但按 D-13、D-15 新增自己的静态 guard 分类。脚本必须先调用 `pnpm verify:phase27`、`pnpm verify:phase28`、`pnpm verify:phase29`、`pnpm verify:phase30`、`pnpm verify:phase31` 作为 milestone prerequisite，用来确认 compatibility baseline、cache freshness、session durability、capability denial、rollback-safe transport 与 proof foundation 未失效；然后再执行 `verify:phase32` 自己的 Phase 32 drift checks 和 focused suites。guard 至少覆盖：`editor/publish drift`（canonical runtime proof step 仍沿现有 authoring-publish snapshot freeze 保留）、`proof seed drift`（bootstrap 里仍有 canonical html runtime proof step）、`launch affordance drift`（launch surface 仍保留 canonical proof discoverability）、`terminal posture drift`（host/runtime 页面存在 submit success locked state 与 no-save-after-submit）、`recovery drift`（存在 retry current action 和 same-surface failure handling）、`classroom proof drift`（classroom read model/teacher panel 仍保留 first-feedback fields）、`inspector review drift`（仍通过 `runtimeSessionId` 深链到 labs inspector，且 unified timeline 继续可审查 canonical proof 的关键事件与 transport trace）、`demo handoff drift`（`32-DEMO-HANDOFF.md` 仍包含 teacher or student 账号、bootstrap 命令、canonical proof chain、`runtimeSessionId` second-step drill-down 与 out-of-scope 说明）。然后在 `package.json` 注册 `verify:phase32`。旧 verifier 只能作为内部 prerequisite，不能替代本脚本自己的 Phase 32 coverage。</action>
  <acceptance_criteria>
    - `package.json` 包含 `"verify:phase32": "tsx scripts/verify-phase32-end-to-end.ts"`
    - `scripts/verify-phase32-end-to-end.ts` 内显式调用 `verify:phase27`、`verify:phase28`、`verify:phase29`、`verify:phase30`、`verify:phase31` 作为里层 prerequisite
    - `scripts/verify-phase32-end-to-end.ts` 内存在至少八类 Phase 32 静态 guard 标签，且包含 `editor/publish drift`、`inspector review drift` 与 `demo handoff drift`
    - 脚本末尾运行 focused suites，并输出清晰区分 milestone prerequisites 与 Phase 32 proof contract 的通过文案
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase32</automated>
  </verify>
  <done>仓库存在单一的 Phase 32 canonical verification gate，可统一证明 milestone proof 仍成立。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| verifier static guards -> repository proof claims | 如果 guard 只看旧脚本，Phase 32 会出现“假通过” |
| focused suites -> milestone close signal | 如果测试过宽或只做 snapshot，对真实 proof drift 没有约束力 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-03-01 | T | verify:phase32 guard coverage | mitigate | verifier 显式检查 editor/publish、seed、launch affordance、terminal posture、recovery、classroom proof、inspector review、demo handoff 八类 drift，并把 roadmap baseline prerequisite 与本 phase proof contract 分开输出 |
| T-32-03-02 | R | milestone proof regression suites | mitigate | focused suites 使用语义断言并覆盖关键 UI/DTO 合同，避免 only-string / only-snapshot false positive |
| T-32-03-03 | D | chained verifier posture | mitigate | 旧 verifier 只作为内部 prerequisite；Phase 32 脚本自己的 checks 必须继续存在，并且外部 close 只暴露 `verify:phase32` 一个命令 |

</threat_model>

<verification>
- `pnpm verify:phase32`
</verification>

<success_criteria>
- `verify:phase32` 已注册并可执行，是唯一外部 milestone-close gate。
- roadmap 要求的 compatibility baseline、cache freshness、session durability、capability denial、rollback-safe transport 由该 gate 在内部重确认。
- 关键 proof drift 均由脚本和 focused suites 守住，覆盖 editor/publish continuity、inspector reviewable event log 与 handoff contract，上游 baseline 以前置 verifier 形式复用但不会分裂成多个 close 命令。
- milestone close 不再依赖一次性手工演示。
</success_criteria>

<output>
After completion, create `.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-03-SUMMARY.md`
</output>
