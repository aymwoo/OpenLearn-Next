---
phase: 48-lifecycle-and-uninstall-semantics
plan: 04
type: execute
wave: 4
depends_on:
  - 48-03
files_modified:
  - src/lib/dal/plugins.ts
  - src/components/surfaces/plugin-lifecycle-operator-surface.tsx
  - scripts/verify-phase48-lifecycle-and-uninstall.ts
autonomous: true
gap_closure: true
requirements:
  - LIFE-01
  - LIFE-02
  - LIFE-03
  - LIFE-04
must_haves:
  truths:
    - "挂起态的“解除挂起”动作能根据历史 transitions 记录准确恢复挂起前的 runnable posture（如 ready、mounted、enabled 等），不再硬编码恢复为 enabled；同时正确更新 enabled 布尔值为 isRunnablePluginState(targetState)。"
    - "挂起态的“保持停用”动作能真正将插件状态 transition 到 disabled，而不是误触发重新启用；且在成功操作后，通过 router.refresh() 触发界面刷新以展示最新状态。"
    - "verify:phase48 对 package.json 的脚本格式无害变化保持强健，不再使用脆弱的文本精确匹配，而是采用解析 JSON 的方式。"
  artifacts:
    - path: src/lib/dal/plugins.ts
      provides: robust kill switch recovery and active runnable enabled field calculation
    - path: src/components/surfaces/plugin-lifecycle-operator-surface.tsx
      provides: separate suspended actions and router.refresh UI sync
    - path: scripts/verify-phase48-lifecycle-and-uninstall.ts
      provides: robust JSON-based package.json verification
  key_links:
    - from: src/components/surfaces/plugin-lifecycle-operator-surface.tsx
      to: src/actions/plugin-actions.ts
      via: setPluginKillSwitchAction and setPluginEnabledAction invocations with correct parameters
---

<objective>
彻底解决 Phase 48 验证报告中的遗留缺口：
1. 修复挂起态下的解除挂起动作，使其能够正确恢复到挂起前的具体活跃状态（ready/mounted/enabled），并用 isRunnablePluginState(targetState) 正确计算 enabled 字段；
2. 区分挂起卡片中的“解除挂起”（解除总开关）和“保持停用”（真正停用且解除总开关）动作；
3. 补齐成功变动后的 router.refresh()，保证 UI 的一致性更新；
4. 重构 verify:phase48 对 package.json 脚本校验的逻辑，使其能健壮地进行 JSON 语义解析校验，不再因空格或引号等格式微调而导致 brittle fail。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/48-lifecycle-and-uninstall-semantics/48-VERIFICATION.md
@src/lib/dal/plugins.ts
@src/components/surfaces/plugin-lifecycle-operator-surface.tsx
@scripts/verify-phase48-lifecycle-and-uninstall.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构 setPluginKillSwitch 恢复逻辑和 transition 矩阵</name>
  <files>src/lib/dal/plugins.ts</files>
  <read_first>src/lib/dal/plugins.ts</read_first>
  <action>
    1) 将 desc 从 drizzle-orm 引入，并在 assertPluginLifecycleTransition 允许 suspended 状态转换回 mounted 和 ready；
    2) 在 setPluginKillSwitch() 关闭 kill switch (killSwitchEnabled=false) 且当前在 suspended 状态时，查询最近一次从活跃状态 transition 到 suspended 的记录，恢复为该原始 runnableState。如果未找到，则退回 enabled 状态；
    3) 更新 db.update.set 时，enabled 字段应计算为 isRunnablePluginState(targetState), 而不再绑死在 targetState === 'enabled'；
    4) 在 transitionPluginLifecycle 接口中，把更新字段中的 killSwitchEnabled 属性设为 targetState === "suspended", 这样任何状态一旦离开 suspended 都会安全解除总开关状态。
  </action>
  <acceptance_criteria>
    - 从挂起（suspended）恢复一个原本是 mounted 或 ready 状态的插件时，生命周期状态能够准确还原，而不再全部强制重置为 enabled。
    - enabled 布尔字段在更新中均基于 isRunnablePluginState(targetState) 正确算出。
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/lib/dal/plugins.test.ts</automated>
  </verify>
  <done>DAL 层的挂起恢复和 runnable 计算完全就绪，满足 states 持续 active 真实语义。</done>
</task>

<task type="auto">
  <name>Task 2: 在 UI 区分恢复与停用，并增加 router.refresh() 界面同步</name>
  <files>src/components/surfaces/plugin-lifecycle-operator-surface.tsx</files>
  <read_first>src/components/surfaces/plugin-lifecycle-operator-surface.tsx</read_first>
  <action>
    1) 导入 useRouter 并初始化 router = useRouter()；
    2) 在 suspended 卡片中，将只提供单一按钮改为提供两个动作按钮：“解除挂起”（调用 submitKillSwitch(plugin, false)）和“保持停用”（调用 setPluginEnabledAction(..., enabled: false)）；
    3) 在 submitToggle、submitKillSwitch 和 confirmUninstall 异步操作成功后，调用 router.refresh()，保证 UI 的一致性更新。
  </action>
  <acceptance_criteria>
    - 挂起态卡片同时具备“解除挂起” and “保持停用”两个清晰的独立语义按钮。
    - 各类生命周期修改动作生效后，UI 会立即通过 router.refresh() 呈现最新的修改结果，保证 Operator 不会被陈旧的数据状态误导。
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx</automated>
  </verify>
  <done>UI 操作面上的生命周期按钮语义恢复安全正确，并且实现了界面操作变动的自动重渲染反馈。</done>
</task>

<task type="auto">
  <name>Task 3: 优化 verify:phase48 脚本以健壮地解析 package.json</name>
  <files>scripts/verify-phase48-lifecycle-and-uninstall.ts</files>
  <read_first>scripts/verify-phase48-lifecycle-and-uninstall.ts</read_first>
  <action>
    1) 将 verifyPackageScript 重写为：以 JSON.parse(packageSource) 方式解析，提取 scripts 对象中的 verify:phase${phase} 键值；
    2) 检查该脚本值是否存在且包含预期的脚本执行路径 scripts/verify-phase${phase}；
    3) 从而摆脱对极其脆弱的硬编码 package.json 全文字符串拼接比对。
  </action>
  <acceptance_criteria>
    - pnpm run verify:phase48 能够强壮地跑通，即便 package.json 中的 verify:phase 脚本有轻微的无害格式或 shim 命令微调，也不会触发 brittle hard fail。
  </acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase48</automated>
  </verify>
  <done>验证 close gate 真正健壮可信，行为优先且不易受外部微小无害改动误报中断。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI mutations → server database action authz | 仅允许具备管理权限的高级用户/教师操作员执行生命周期动作，所有 Server Actions 会通过 DAL 安全切面拦截和授权校验。 |
| database state recovery → lifecycle state transition safety | 处于 suspended 状态向外恢复的路径，需严格遵循已更新 of lifecycle transition matrix, 并在 query last state 时防范无数据时退化安全值。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-04-01 | T | `src/lib/dal/plugins.ts` | mitigate | 查询历史 lifecycleTransition 来恢复先前 runnable posture 时，若因手动数据操作无历史记录，安全兜底恢复为 enabled。 |
| T-48-04-02 | E | UI Cards | mitigate | 在 mutation 完成后主动触发 router.refresh() 确保页面数据的一致和 Operator 正确判断，防止由于信息不刷新引起的误操作或越权高风险动作。 |
</threat_model>

<verification>
- `pnpm exec vitest run src/lib/dal/plugins.test.ts src/actions/plugin-actions.test.ts src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx`
- `pnpm run verify:phase48`
</verification>

<success_criteria>
- 所有相关的生命周期、卸载 preflight 及 verify 测试文件都百分之百通过。
- 挂起状态的“解除挂起”能原样把挂载时的 mounted / ready 等活性形态安全还原出来。
- 挂起状态的“保持停用”真正安全过渡到已停用状态，并且 UI 在成功后能同步重刷响应。
- close gate 的 `verifyPackageScript` 检测通过解析 JSON 构建，无害的命令格式微调不会引起假失败。
</success_criteria>

<output>
工作完成后，更新 `.planning/phases/48-lifecycle-and-uninstall-semantics/48-04-SUMMARY.md`
</output>
