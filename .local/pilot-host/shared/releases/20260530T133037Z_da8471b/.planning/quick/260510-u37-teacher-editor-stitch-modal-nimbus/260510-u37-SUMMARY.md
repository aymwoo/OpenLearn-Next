---
phase: quick
plan: 260510-u37
status: complete
---

# Quick summary

已完成：将 `/teacher/editor` 的课堂流程组件区改为更接近 Stitch Nimbus 的资源库布局，并把步骤编辑从右侧抽屉升级为带实时预览的 modal。

- 在 `lesson-authoring-workspace.tsx` 中把左侧统一编排区重构为资源库式结构，加入搜索、分类筛选、资源行样式与资源库节奏。
- 将原先的右侧抽屉替换为中心大尺寸 modal，打开后使用 `编辑教学环节` 的 Nimbus 风格结构承载编辑器。
- 在 `lesson-step-editor.tsx` 中保留既有 autosave 与 schema 校验链路，同时新增右侧实时预览面板，实时同步标题、摘要、资料与 builtInSource 展示。
- 更新 `lesson-authoring-workspace.test.tsx` 与 `lesson-step-editor.test.tsx`，锁定 modal 打开/关闭、实时预览联动和保存链路回归。

验证：

- `pnpm test --run "src/components/authoring/lesson-authoring-workspace.test.tsx" "src/components/authoring/lesson-step-editor.test.tsx"`
