---
quick_id: 260510-9s2
title: 统一 settings、teacher-review、library surface 的二级卡片圆角语义
status: in_progress
created_at: 2026-05-10
---

# Quick Task 260510-9s2 Plan

## Goal

把 `settings-surface`、`teacher-review-surface`、`library-surface`
里仍然手写的二级卡片圆角收敛到共享语义，避免继续混用 `1.25rem`、`1.4rem`、`1.5rem`、`1.6rem` 和 `3xl`。

## Tasks

1. 检查这三个 surface 中二级卡片与 inset 卡片当前使用的圆角值。
2. 复用 `teacherSurfaceRhythm.card` 与 `teacherSurfaceRhythm.cardInset` 替换手写半径。
3. 运行定向格式化与测试，补充 quick summary 和 `.planning/STATE.md`。
