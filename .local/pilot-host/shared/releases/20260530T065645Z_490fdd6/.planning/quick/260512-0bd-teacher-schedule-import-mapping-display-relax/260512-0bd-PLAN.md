---
+type: quick
+slug: 260512-0bd-teacher-schedule-import-mapping-display-relax
+scope: /teacher/schedule
+autonomous: true
+files_modified:
+  - src/components/surfaces/schedule-import-modal.tsx
+  - src/components/surfaces/schedule-import-modal.test.tsx
+  - src/components/surfaces/teacher-schedule-surface.tsx
+  - src/components/surfaces/teacher-schedule-surface.test.tsx
+  - .planning/STATE.md
+must_haves:
+  truths:
+    - 当导入记录时间字段合法时，即使班级或教师未映射，也允许教师回到 `/teacher/schedule` 主课表页继续查看当前导入批次。
+    - 课程未映射、时间字段非法、真实节次冲突等阻断仍保持不变，不放宽到自动入库。
+    - normalized runtime / approve import 的边界不变，未映射班级或教师的记录不会被自动写入运行层。
+  artifacts:
+    - path: src/components/surfaces/schedule-import-modal.tsx
+      provides: 对 class/teacher 未映射的非入库型放宽，导入后可继续回主课表页
+    - path: src/components/surfaces/teacher-schedule-surface.tsx
+      provides: 将仅含 class/teacher 映射缺口的最新批次纳入主课表展示来源
+  key_links:
+    - from: src/components/surfaces/schedule-import-modal.tsx
+      to: /teacher/schedule
+      via: 导入成功或可展示批次时的 `router.push`
+    - from: src/components/surfaces/teacher-schedule-surface.tsx
+      to: src/features/schedule/import/server.ts
+      via: 读取 latest import batch 的 row status / validation issues 以决定主课表可见性
+---

<objective>
放宽教师课表导入映射阻断：当导入记录时间合法时，即使班级或教师未映射，也允许进入主课表展示。

Purpose: 保留导入审核与 runtime 边界，但不要因为 class/teacher 映射缺口把教师卡在导入弹窗错误态或让主课表页完全不认当前批次。
Output: 主课表可见性放宽、导入弹窗成功回流放宽、对应最小回归测试与 GSD 记录。
</objective>

<context>
@.planning/STATE.md
@src/components/surfaces/schedule-import-modal.tsx
@src/components/surfaces/schedule-import-modal.test.tsx
@src/components/surfaces/teacher-schedule-surface.tsx
@src/components/surfaces/teacher-schedule-surface.test.tsx
@src/features/schedule/import/server.ts

当前现象：
- `schedule-import-modal` 只要看到 `mapping_review` 就停在错误态，不回 `/teacher/schedule`
- `teacher-schedule-surface` 只把 `applied/partially_applied` 批次当作当前主课表来源

目标边界：
- 只放宽 `CLASS_NOT_FOUND` / `TEACHER_NOT_FOUND`
- 不放宽 `COURSE_NOT_FOUND`、`INVALID_WEEKDAY`、`conflict_review`
- 不改变 `approveScheduleImport()` 的 unresolved blocker 规则
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 放宽导入弹窗对 class/teacher 未映射行的返回主课表行为</name>
  <files>src/components/surfaces/schedule-import-modal.tsx, src/components/surfaces/schedule-import-modal.test.tsx</files>
  <behavior>
    - Test 1: 仅包含 `CLASS_NOT_FOUND` / `TEACHER_NOT_FOUND` 的 `mapping_review` 行时，导入流程不展示 blocking error，而是返回 `/teacher/schedule`
    - Test 2: 这类批次不会继续触发 `approveScheduleImportAction`
    - Test 3: `COURSE_NOT_FOUND`、`INVALID_WEEKDAY`、冲突类阻断仍维持现状
  </behavior>
  <action>在 modal 内区分“展示可继续主课表查看”的非自动入库批次与真正阻断批次；对前者直接进入 done 状态并回主课表，但跳过 approve action。</action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 放宽主课表页对最新导入批次的展示资格</name>
  <files>src/components/surfaces/teacher-schedule-surface.tsx, src/components/surfaces/teacher-schedule-surface.test.tsx</files>
  <behavior>
    - Test 1: 最新批次若仅因 class/teacher 未映射处于 `in_review`，仍作为当前学期主视图来源
    - Test 2: 最新批次若存在课程未映射或其他硬阻断，仍不作为当前主课表来源
  </behavior>
  <action>在 surface 内新增最小 helper，识别“仅含 class/teacher 映射缺口”的 displayable batch；保留 history 列表和已生效批次逻辑，不改 runtime DTO。</action>
</task>
</tasks>

<verification>
1. `pnpm vitest run src/components/surfaces/schedule-import-modal.test.tsx src/components/surfaces/teacher-schedule-surface.test.tsx`
</verification>

<success_criteria>
- [x] class/teacher 未映射不再阻止回到 `/teacher/schedule`
- [x] 主课表页可识别仅含 class/teacher 映射缺口的最新批次
- [x] 未放宽课程缺失、时间非法、冲突、审批入库等硬边界
</success_criteria>
