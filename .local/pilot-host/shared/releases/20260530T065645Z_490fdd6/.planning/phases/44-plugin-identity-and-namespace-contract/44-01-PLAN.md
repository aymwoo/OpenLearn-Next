---
phase: 44-plugin-identity-and-namespace-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/lib/dto/resource-ai.ts
  - scripts/prepare-dev-db.ts
  - drizzle/0011_phase44_plugin_identity_namespace.sql
  - drizzle/meta/0011_snapshot.json
  - drizzle/meta/_journal.json
autonomous: true
requirements:
  - PLUG-01
  - PLUG-02
  - PLUG-03
must_haves:
  truths:
    - "正式 SQL/DTO contract 已固定：插件安装记录与 registration DTO 都具备 `pluginKey`、`dbNamespace`、`sourceType`、`installSource` 四项正式字段。"
    - "同一学校内 pluginKey 与 dbNamespace 的唯一性由 SQL 真相层兜底。"
    - "历史插件安装记录经过一次性迁移/backfill 后，读取结果直接具备完整 identity/namespace contract，而不是依赖运行时补写。"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "pluginRegistrations 正式 identity / namespace / provenance 列与学校范围唯一约束"
    - path: "drizzle/0011_phase44_plugin_identity_namespace.sql"
      provides: "Phase 44 migration、旧记录 backfill、唯一索引"
    - path: "src/lib/dto/resource-ai.ts"
      provides: "PluginRegistrationDTO 暴露 pluginKey、dbNamespace、sourceType、installSource"
    - path: "scripts/prepare-dev-db.ts"
      provides: "Phase 44 migration bridge detection"
  key_links:
    - from: "src/db/schema.ts"
      to: "src/lib/dto/resource-ai.ts"
      via: "PluginRegistrationDTO fields mirror SQL truth columns"
      pattern: "pluginKey|dbNamespace|sourceType|installSource"
    - from: "drizzle/0011_phase44_plugin_identity_namespace.sql"
      to: "src/db/schema.ts"
      via: "migration backfills and indexes match declared contract"
      pattern: "pluginRegistration.*pluginKey|dbNamespace"
---

<objective>
把 Phase 44 的稳定身份与数据库命名空间 contract 先落到 SQL truth、DTO contract 与 migration 基线。

Purpose: 先把最不可逆的 schema contract 固定下来，避免后续 DAL、bootstrap、UI 继续围绕 `name` 或 `manifestJson.id` 分叉解释真相。
Output: 更新后的 `pluginRegistrations` schema、Phase 44 migration/meta、DTO 正式字段、dev DB bridge 检测。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-plugin-identity-and-namespace-contract/44-CONTEXT.md
@.planning/phases/44-plugin-identity-and-namespace-contract/44-RESEARCH.md
@.planning/phases/44-plugin-identity-and-namespace-contract/44-PATTERNS.md
@src/db/schema.ts
@src/lib/dto/resource-ai.ts
@scripts/prepare-dev-db.ts

<interfaces>
From `src/db/schema.ts`:
```ts
export const pluginRegistrations = sqliteTable("pluginRegistration", {
  id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
  schoolId: text("schoolId").notNull().references(() => schools.id, { onDelete: "cascade" }),
  name: text("name").notNull(),
  manifestJson: text("manifestJson", { mode: "json" }).notNull(),
  enabled: integer("enabled", { mode: "boolean" }).notNull().default(false),
  killSwitchEnabled: integer("killSwitchEnabled", { mode: "boolean" }).notNull().default(false),
  lifecycleState: text("lifecycleState", { enum: [...] }).notNull().default("installed"),
});
```

From `src/lib/dto/resource-ai.ts`:
```ts
export const PluginManifestSchema = z.object({
  id: z.string(),
  builtIn: z.boolean().default(false),
  defaultEnabled: z.boolean().default(false),
  nonDeletable: z.boolean().default(false),
});

export const PluginRegistrationDTOSchema = z.object({
  id: z.string(),
  schoolId: z.string(),
  name: z.string(),
  manifestJson: PluginManifestSchema,
  enabled: z.boolean(),
  killSwitchEnabled: z.boolean(),
  lifecycleState: PluginLifecycleStateSchema,
  builtIn: z.boolean(),
  defaultEnabled: z.boolean(),
  nonDeletable: z.boolean(),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Run GitNexus impact preflight for schema and DTO symbols</name>
  <files>src/db/schema.ts, src/lib/dto/resource-ai.ts, scripts/prepare-dev-db.ts</files>
  <read_first>
    @AGENTS.md
    @src/db/schema.ts
    @src/lib/dto/resource-ai.ts
    @scripts/prepare-dev-db.ts
  </read_first>
  <action>在编辑任何符号前，先按项目 GitNexus 约束对本计划会修改的主符号执行 impact analysis，并把 blast radius 记录到 summary：至少覆盖 `pluginRegistrations`、`PluginRegistrationDTOSchema` 与 `scripts/prepare-dev-db.ts` 中 migration bridge 主流程。如果任一结果为 HIGH 或 CRITICAL，执行器必须先停下并汇报，再决定是否继续。完成全部改动后，再运行 `gitnexus_detect_changes()`，确认影响范围仍限制在 schema / DTO / migration bridge 相关符号。</action>
  <acceptance_criteria>
    - 执行 summary 明确记录了 `pluginRegistrations` 与 `PluginRegistrationDTOSchema` 的 GitNexus impact 结果。
    - 若存在 HIGH 或 CRITICAL risk，summary 记录了暂停与升级处理，而不是直接继续改代码。
    - 执行 summary 记录了 `gitnexus_detect_changes()` 的结果。
  </acceptance_criteria>
  <verify>
    <manual>Read `.planning/phases/44-plugin-identity-and-namespace-contract/44-01-SUMMARY.md` and confirm it logs GitNexus impact analysis plus post-change detect_changes output before marking the plan complete.</manual>
  </verify>
  <done>执行器在改 schema / DTO 之前已经完成 blast-radius 预分析，且收尾时校验了实际影响范围。</done>
</task>

<task type="auto">
  <name>Task 1: Formalize SQL identity, namespace, and provenance columns</name>
  <files>src/db/schema.ts, src/lib/dto/resource-ai.ts, scripts/prepare-dev-db.ts, drizzle/0011_phase44_plugin_identity_namespace.sql, drizzle/meta/0011_snapshot.json, drizzle/meta/_journal.json</files>
  <read_first>
    @.planning/phases/44-plugin-identity-and-namespace-contract/44-CONTEXT.md
    @.planning/phases/44-plugin-identity-and-namespace-contract/44-RESEARCH.md
    @.planning/phases/44-plugin-identity-and-namespace-contract/44-PATTERNS.md
    @src/db/schema.ts
    @src/lib/dto/resource-ai.ts
    @scripts/prepare-dev-db.ts
  </read_first>
  <action>按 D-44-01、D-44-04、D-44-05、D-44-08、D-44-09、D-44-10、D-44-12，把 `pluginRegistrations` 扩展为正式 contract：新增非空 `pluginKey`（值锁定复制 `manifest.id`）、非空 `dbNamespace`（初次由集中规范化算法生成，但存为冻结列）、非空 `sourceType`（使用枚举 `default | external`，built-in/default 插件写 `default`，其他写 `external`）、非空 `installSource`（使用枚举 `manual | bootstrap | repair | seed`，旧 built-in backfill 为 `bootstrap`，旧非 built-in backfill 为 `manual`）。在 `sqliteTable(..., (table) => [...])` 中补 `uniqueIndex("pluginRegistration_school_pluginKey_unique").on(table.schoolId, table.pluginKey)` 与 `uniqueIndex("pluginRegistration_school_dbNamespace_unique").on(table.schoolId, table.dbNamespace)`，不要删除现有 `id` 或改动现有 lifecycle/audit 外键。生成 `drizzle/0011_phase44_plugin_identity_namespace.sql` 和 meta：migration 必须把旧行一次性 backfill 到完整 contract，不允许留下运行时首次访问再补写的半升级记录；SQLite rebuild 与唯一索引模式必须显式复用 `44-PATTERNS.md` 指定的 `drizzle/0008_phase43_validation_workloads.sql`（`__new_* -> INSERT SELECT -> DROP -> RENAME`）和 `drizzle/0009_phase43_knowledge_source_uniqueness.sql`（清洗后创建 unique index）这两个 analog。migration/backfill 自身必须内联并固定与 `44-RESEARCH.md` 已锁定的 namespace contract 一致的规则：至少覆盖供应商前缀、连字符、连续分隔符、首字符非字母、超长 key 这 5 类输入，避免历史记录生成另一套 namespace。同步更新 `PluginRegistrationDTOSchema` 暴露 `pluginKey`、`dbNamespace`、`sourceType`、`installSource`，且 `scripts/prepare-dev-db.ts` 在检测到这些列与唯一索引后返回 `0011_phase44_plugin_identity_namespace`。</action>
  <acceptance_criteria>
    - `src/db/schema.ts` 包含 `pluginKey: text("pluginKey").notNull()`、`dbNamespace: text("dbNamespace").notNull()`、`sourceType`、`installSource` 四个正式列。
    - `src/db/schema.ts` 包含 `pluginRegistration_school_pluginKey_unique` 与 `pluginRegistration_school_dbNamespace_unique` 两个学校范围唯一索引。
    - `src/lib/dto/resource-ai.ts` 的 `PluginRegistrationDTOSchema` 直接声明 `pluginKey`、`dbNamespace`、`sourceType`、`installSource`，且不要求 UI 再从 `manifestJson.id` 派生。
    - `drizzle/0011_phase44_plugin_identity_namespace.sql` 存在并包含 plugin registration backfill / unique index 语句。
    - `drizzle/0011_phase44_plugin_identity_namespace.sql` 或对应 verifier 明确覆盖供应商前缀、连字符、首字符非字母、超长 key 的 namespace parity 用例。
    - `scripts/prepare-dev-db.ts` 能检测 `0011_phase44_plugin_identity_namespace`。
  </acceptance_criteria>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
schema = Path('src/db/schema.ts').read_text()
dto = Path('src/lib/dto/resource-ai.ts').read_text()
prep = Path('scripts/prepare-dev-db.ts').read_text()
mig = Path('drizzle/0011_phase44_plugin_identity_namespace.sql').read_text()
checks = [
    'pluginKey: text("pluginKey").notNull()' in schema,
    'dbNamespace: text("dbNamespace").notNull()' in schema,
    'pluginRegistration_school_pluginKey_unique' in schema,
    'pluginRegistration_school_dbNamespace_unique' in schema,
    'pluginKey: z.string()' in dto,
    'dbNamespace: z.string()' in dto,
    'sourceType:' in dto,
    'installSource:' in dto,
    '0011_phase44_plugin_identity_namespace' in prep,
    'pluginKey' in mig and 'dbNamespace' in mig,
]
raise SystemExit(0 if all(checks) else 1)
PY</automated>
  </verify>
  <done>正式 SQL/DTO contract 已固定，migration 与 dev DB bridge 都能识别 Phase 44 identity/namespace truth。</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply the Phase 44 migration through the repo standard DB flow</name>
  <files>local.db</files>
  <read_first>
    @src/db/schema.ts
    @drizzle/0011_phase44_plugin_identity_namespace.sql
    @scripts/prepare-dev-db.ts
  </read_first>
  <action>这是 Phase 44 的强制顺序闸门。完成 Task 1 后，必须走仓库标准 migration-first 路径运行 `pnpm db:migrate`（即 `tsx scripts/prepare-dev-db.ts`），把 `pluginKey`、`dbNamespace`、`sourceType`、`installSource` 与学校范围唯一索引真实落到本地数据库，再继续任何验证。若 migration bridge 发现需要先应用 `drizzle/0011_phase44_plugin_identity_namespace.sql` 或补齐 journal/snapshot，执行器应按仓库既有 prepare-dev-db 流程完成，不得改用 `drizzle-kit push` 作为默认真相路径，也不能跳过此步骤直接依赖 TypeScript/测试假通过。</action>
  <acceptance_criteria>
    - `pnpm db:migrate` 已执行且退出码为 0。
    - 本地 `pluginRegistration` 表的 `PRAGMA table_info` 能看到 `pluginKey`、`dbNamespace`、`sourceType`、`installSource`。
    - `sqlite_master` 能查到 `pluginRegistration_school_pluginKey_unique` 与 `pluginRegistration_school_dbNamespace_unique`。
  </acceptance_criteria>
  <verify>
    <automated>pnpm db:migrate && node --input-type=module --import tsx -e "import { db } from './src/db'; import { sql } from 'drizzle-orm'; const cols = await db.values(sql.raw('PRAGMA table_info(\"pluginRegistration\")')); const idx = await db.values(sql.raw(\"select name from sqlite_master where type='index' and tbl_name='pluginRegistration'\")); const names = cols.map((row) => String(row[1] ?? '')); const indexes = idx.map((row) => String(row[0] ?? '')); const ok = ['pluginKey','dbNamespace','sourceType','installSource'].every((name) => names.includes(name)) && indexes.includes('pluginRegistration_school_pluginKey_unique') && indexes.includes('pluginRegistration_school_dbNamespace_unique'); process.exit(ok ? 0 : 1);"</automated>
  </verify>
  <done>本地数据库真相已通过仓库标准 migration/apply 流程与 Phase 44 schema contract 对齐，不存在“代码变了但库没迁移”的假阳性状态。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → SQLite truth | schema backfill 与唯一索引必须一次性把旧记录提升为正式 contract |
| SQL truth → DTO readers | 下游读取必须消费正式列，而不是继续回退到 JSON 解析 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | S | `pluginRegistrations.pluginKey` | mitigate | 将 `pluginKey` 固定写为 `manifest.id`，并做学校范围唯一索引，阻止展示名伪装成稳定身份 |
| T-44-02 | T | `pluginRegistrations.dbNamespace` | mitigate | 将 `dbNamespace` 落为冻结列并加学校范围唯一索引，阻止后续静默篡改或重名污染 |
| T-44-03 | R | migration/backfill | mitigate | `0011_phase44_plugin_identity_namespace.sql` 显式 backfill 与索引创建，避免运行时偷偷补写且无法审计 |
| T-44-04 | I | DTO exposure | accept | `pluginKey/dbNamespace/sourceType/installSource` 属于治理元数据而非敏感凭据，允许在受控 settings surface 暴露 |
</threat_model>

<verification>
- `python - <<'PY' ...` 静态检查 schema / dto / migration / prepare-dev-db token
- `pnpm db:migrate`
- `node --input-type=module --import tsx -e "...PRAGMA table_info..."`
</verification>

<success_criteria>
- `pluginRegistrations` 已具备正式 `pluginKey`、`dbNamespace`、`sourceType`、`installSource` 列。
- 学校范围 `pluginKey` 与 `dbNamespace` 唯一性已落到 SQL 约束。
- Phase 44 migration 与 dev DB bridge 可识别并应用该 contract。
</success_criteria>

<output>
After completion, create `.planning/phases/44-plugin-identity-and-namespace-contract/44-01-SUMMARY.md`
</output>
