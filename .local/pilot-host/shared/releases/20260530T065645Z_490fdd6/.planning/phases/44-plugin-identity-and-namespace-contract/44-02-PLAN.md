---
phase: 44-plugin-identity-and-namespace-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 44-01
files_modified:
  - src/lib/dal/plugins.ts
  - src/actions/plugin-actions.ts
  - src/lib/dal/plugins.test.ts
  - src/actions/plugin-actions.test.ts
autonomous: true
requirements:
  - PLUG-01
  - PLUG-02
  - PLUG-03
  - PLUG-04
must_haves:
  truths:
    - "学校安装流程会把正式 plugin identity / namespace 写入同一条安装真相记录，而不是让手动注册与 reconcile 走两套身份模型。"
    - "同校重复 `pluginKey` 或 `dbNamespace` 的安装会被明确拒绝，而不是静默覆盖或生成第二条等价记录。"
    - "默认启用策略只影响首次安装快照，后续 reconcile 不会偷偷改写学校当前的启停姿态。"
    - "历史记录与新安装记录对同一 pluginKey 会得到同一个 `dbNamespace`，不会因两套算法漂移而产生 namespace 分叉。"
  artifacts:
    - path: "src/lib/dal/plugins.ts"
      provides: "installOrReconcilePlugin truth seam、namespace normalizer、冲突错误"
    - path: "src/actions/plugin-actions.ts"
      provides: "Server Actions 继续经 DAL 调用并刷新 plugin cache tags"
    - path: "src/lib/dal/plugins.test.ts"
      provides: "identity/namespace/conflict regression assertions"
    - path: "src/actions/plugin-actions.test.ts"
      provides: "action-to-DAL contract and cache invalidation regression"
  key_links:
    - from: "src/actions/plugin-actions.ts"
      to: "src/lib/dal/plugins.ts"
      via: "registerPluginManifestAction delegates to install/reconcile seam"
      pattern: "registerPluginManifest\(|installOrReconcilePlugin"
    - from: "src/lib/dal/plugins.ts"
      to: "src/db/schema.ts"
      via: "pluginKey/dbNamespace/sourceType/installSource persistence + conflict lookup"
      pattern: "pluginKey|dbNamespace|sourceType|installSource"
---

<objective>
把 Phase 44 的 identity/namespace contract 收口到唯一的 DAL install/reconcile seam，并让 action 层继续只做认证、校验与 cache invalidation。

Purpose: 把最容易继续分叉的“谁负责写正式身份真相”问题先收口，避免 bootstrap、手动注册、修复流程各写各的 identity 规则。
Output: `installOrReconcilePlugin()`、集中 `deriveDbNamespace()`、显式冲突错误、action 回归测试。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-plugin-identity-and-namespace-contract/44-CONTEXT.md
@.planning/phases/44-plugin-identity-and-namespace-contract/44-RESEARCH.md
@.planning/phases/44-plugin-identity-and-namespace-contract/44-PATTERNS.md
@src/lib/dal/plugins.ts
@src/actions/plugin-actions.ts
@src/lib/dto/resource-ai.ts

<interfaces>
From `src/lib/dal/plugins.ts`:
```ts
type RegisterPluginManifestInput = PluginManagerScopeInput & {
  name: string;
  manifestJson: PluginManifest;
};

export async function registerPluginManifest(input: RegisterPluginManifestInput) {
  await assertTeacherManagerScope({ actorId: input.actorId, schoolId: input.schoolId });
  const parsedManifest = PluginManifestSchema.parse(input.manifestJson);
  // current code inserts directly without stable identity contract
}
```

From `src/actions/plugin-actions.ts`:
```ts
export async function registerPluginManifestAction(data: z.infer<typeof RegisterPluginSchema>) {
  const actorId = await requireCurrentActorId();
  const result = await registerPluginManifest({ ...parsed.data, actorId });
  updateTag(cacheTags.pluginRegistry);
  updateTag(cacheTags.plugin(result.id));
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Run GitNexus impact preflight for DAL and action identity seams</name>
  <files>src/lib/dal/plugins.ts, src/actions/plugin-actions.ts, src/lib/dal/plugins.test.ts, src/actions/plugin-actions.test.ts</files>
  <read_first>
    @AGENTS.md
    @src/lib/dal/plugins.ts
    @src/actions/plugin-actions.ts
  </read_first>
  <action>在编辑任何符号前，先按项目 GitNexus 约束对本计划会修改的主符号执行 impact analysis，并把 blast radius 记录到 summary：至少覆盖 `registerPluginManifest`、计划新增的 `installOrReconcilePlugin`、`registerPluginManifestAction`。如果任一结果为 HIGH 或 CRITICAL，执行器必须先停下并汇报风险。完成全部改动后，再运行 `gitnexus_detect_changes()`，确认受影响执行流仍聚焦插件注册 / reconcile / action 边界。</action>
  <acceptance_criteria>
    - 执行 summary 记录了 `registerPluginManifest`、`installOrReconcilePlugin`、`registerPluginManifestAction` 的 GitNexus impact 结果。
    - 若存在 HIGH 或 CRITICAL risk，summary 记录了暂停与升级处理。
    - 执行 summary 记录了 `gitnexus_detect_changes()` 输出。
  </acceptance_criteria>
  <verify>
    <manual>Read `.planning/phases/44-plugin-identity-and-namespace-contract/44-02-SUMMARY.md` and confirm GitNexus impact + detect_changes were logged.</manual>
  </verify>
  <done>执行器在改 DAL/action 真相入口之前完成了 blast-radius 预分析，并在收尾时校验了影响范围。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Implement the DAL-owned install/reconcile truth seam</name>
  <files>src/lib/dal/plugins.ts, src/lib/dal/plugins.test.ts</files>
  <read_first>
    @.planning/phases/44-plugin-identity-and-namespace-contract/44-CONTEXT.md
    @.planning/phases/44-plugin-identity-and-namespace-contract/44-PATTERNS.md
    @drizzle/0011_phase44_plugin_identity_namespace.sql
    @src/lib/dal/plugins.ts
    @src/lib/dal/plugins.test.ts
    @src/lib/dto/resource-ai.ts
  </read_first>
  <behavior>
    - Test 1: manual register stores `pluginKey = manifest.id`, derives and stores `dbNamespace`, and returns DTO fields directly from SQL truth.
    - Test 2: same-school second install with a different row but duplicate `pluginKey` or duplicate `dbNamespace` fails with explicit conflict error tokens.
    - Test 3: reconcile of the same installed plugin updates in place, preserves `id`, preserves operator-managed `enabled/killSwitch/lifecycleState` unless caller explicitly overrides, and keeps frozen `dbNamespace` unchanged.
    - Test 4: namespace contract corpus（供应商前缀、连字符、连续分隔符、首字符非字母、超长 key）在 44-02 完成后就直接比较 `deriveDbNamespace()` 与 Phase 44 migration/backfill 的预期输出，避免漂移拖到 44-04 才暴露。
  </behavior>
  <action>按 D-44-01、D-44-02、D-44-03、D-44-04、D-44-05、D-44-06、D-44-09、D-44-10、D-44-11、D-44-12、D-44-13、D-44-15，在 `src/lib/dal/plugins.ts` 新增集中 `deriveDbNamespace(pluginKey: string)` helper（统一 lower-case ASCII、连字符转 `_`、压缩连续分隔符、首字符非字母时补 `p_`、长度上限 48），以及 `installOrReconcilePlugin()` truth seam。实现时必须显式复用 `44-PATTERNS.md` 已锁定的 shared patterns：`src/lib/dal/plugins.ts` 的 teacher-scoped guard、`PluginManifestSchema.parse()` + `PluginRegistrationDTOSchema.parse()` 的 parse-at-boundary、以及 `src/actions/plugin-actions.ts` 的 `updateTag()` freshness 边界。该 seam 的输入至少包含 `schoolId`、`actorId`、`name`、`manifestJson`、`installSource`、可选 `forceDefaultEnabledSnapshot`；它必须：1) 解析 `PluginManifestSchema`；2) `pluginKey` 固定取 `manifest.id`；3) `sourceType` 固定为 `manifest.builtIn ? "default" : "external"`；4) 首次安装时写入 `dbNamespace = deriveDbNamespace(pluginKey)`，reconcile 已存在记录时必须保留旧 `dbNamespace` 并拒绝任何 rename/alias 尝试；5) 在 SQL 唯一约束之外做 preflight 冲突检查，对“别的记录已占用同 school + pluginKey / dbNamespace”分别抛 `PLUGIN_KEY_CONFLICT`、`PLUGIN_DB_NAMESPACE_CONFLICT`，对同插件试图改 namespace 抛 `PLUGIN_DB_NAMESPACE_FROZEN`；6) 首次插入时按 `manifest.defaultEnabled` 生成启停快照，reconcile 时保留现有 `enabled` / `killSwitchEnabled` / `lifecycleState`，不要把 operator 已停用的默认插件重新开起来；7) 对首次安装写 lifecycle transition reason `registered`，对 reconcile 写 reason `reconciled`。`deriveDbNamespace()` 的 edge-case corpus 必须与 `44-RESEARCH.md` 已锁定的 namespace contract 保持一致：至少覆盖供应商前缀、连字符、连续分隔符、首字符非字母、超长 key。并且在 44-02 完成后就直接新增 parity 校验：把这组 corpus 的期望结果同时对照 `drizzle/0011_phase44_plugin_identity_namespace.sql` 中 backfill 规则与 `deriveDbNamespace()` 输出，证明 migration/backfill 与 DAL helper 还没有漂移，再允许进入 44-03 bootstrap。把现有 `registerPluginManifest()` 改成对该 seam 的 teacher-scoped 包装，而不是继续直接 insert。同步扩展 `src/lib/dal/plugins.test.ts`：新增对 `pluginKey`/`dbNamespace` token、冲突错误 token、`reconciled`、`PLUGIN_DB_NAMESPACE_FROZEN`、`deriveDbNamespace` 的静态断言，以及上述 namespace corpus parity 断言，确保后续不会回退到 JSON/name identity 或漂移出第二套 namespace 算法。</action>
  <acceptance_criteria>
    - `src/lib/dal/plugins.ts` 导出 `installOrReconcilePlugin` 或等价命名的统一 truth seam。
    - `src/lib/dal/plugins.ts` 包含 `deriveDbNamespace(`，且不再把 `manifestJson.id` 仅作为读取时临时派生值。
    - `src/lib/dal/plugins.ts` 包含 `PLUGIN_KEY_CONFLICT`、`PLUGIN_DB_NAMESPACE_CONFLICT`、`PLUGIN_DB_NAMESPACE_FROZEN` 三个明确错误 token。
    - `registerPluginManifest(` 已改为委托统一 seam，而不是再次直接 `db.insert(pluginRegistrations).values({ name, manifestJson ... })`。
    - `src/lib/dal/plugins.test.ts` 对上述 token、reconcile 语义与 namespace contract corpus parity 有回归断言。
    - 44-02 的 automated verify 已直接比较 `deriveDbNamespace()` 与 `drizzle/0011_phase44_plugin_identity_namespace.sql` 的 namespace corpus 预期，而不是等到 44-04 才发现漂移。
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest --run src/lib/dal/plugins.test.ts && node --input-type=module --import tsx -e "import { readFileSync } from 'node:fs'; const sql = readFileSync('drizzle/0011_phase44_plugin_identity_namespace.sql', 'utf8'); const requiredTokens = ['pluginKey', 'dbNamespace']; const hasSqlContract = requiredTokens.every((token) => sql.includes(token)); if (!hasSqlContract) process.exit(1); process.exit(0);"</automated>
  </verify>
  <done>DAL 写路径只剩一个 install/reconcile 真相入口，冲突与冻结语义都能被代码与测试直接看见。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep Server Actions thin while exposing the new identity truth</name>
  <files>src/actions/plugin-actions.ts, src/actions/plugin-actions.test.ts</files>
  <read_first>
    @src/actions/plugin-actions.ts
    @src/actions/plugin-actions.test.ts
    @.planning/phases/44-plugin-identity-and-namespace-contract/44-PATTERNS.md
    @src/lib/dal/plugins.ts
  </read_first>
  <behavior>
    - Test 1: register action passes actorId into the DAL seam and returns DTOs with `pluginKey`/`dbNamespace` fields intact.
    - Test 2: DAL conflict errors surface as action errors without bypassing validation or cache invalidation rules.
    - Test 3: action layer still refreshes `plugin:registry` and `plugin:{id}` tags after successful register/reconcile.
  </behavior>
  <action>按 D-44-04、D-44-11、D-44-13，在 `src/actions/plugin-actions.ts` 保持 `safeParse -> requireCurrentActorId -> DAL -> updateTag -> success/error` 结构，不把 `manifest.id`、`dbNamespace` 生成规则或冲突检查下沉到 action。这里必须显式复用 `44-PATTERNS.md` 已锁定的 action analog：沿用现有 `src/actions/plugin-actions.ts` 的 Server Action success/failure pattern 和 mutation freshness 规则。`registerPluginManifestAction()` 继续调用 DAL，但现在返回的数据必须原样保留 `pluginKey`、`dbNamespace`、`sourceType`、`installSource` 以供后续 surface 消费；遇到 `PLUGIN_KEY_CONFLICT` / `PLUGIN_DB_NAMESPACE_CONFLICT` / `PLUGIN_DB_NAMESPACE_FROZEN` 时，action 直接把这些 token 作为错误信息向上返回。同步更新 `src/actions/plugin-actions.test.ts` 中的 mock DTO shape，断言 register action 的请求/响应与 cache invalidation 仍然只围绕 DAL seam 发生，不重新引入 `manifestJson.id` 推断或其它第二套身份逻辑。</action>
  <acceptance_criteria>
    - `src/actions/plugin-actions.ts` 仍未直接导入 `db` 或在 action 内生成 `dbNamespace`。
    - `src/actions/plugin-actions.test.ts` 的 mock DTO 含 `pluginKey`、`dbNamespace`、`sourceType`、`installSource`。
    - register action 成功路径继续调用 `updateTag("plugin:registry")` 与 `updateTag("plugin:plugin-1")`。
    - register action 失败路径对 `PLUGIN_KEY_CONFLICT` / `PLUGIN_DB_NAMESPACE_CONFLICT` 返回明确错误字符串。
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest --run src/actions/plugin-actions.test.ts src/lib/dal/plugins.test.ts</automated>
  </verify>
  <done>Server Action 边界保持干净，正式 identity/namespace truth 与冲突语义都从 DAL 透传出来。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated actor → plugin install/reconcile DAL | 用户输入的 manifest/name/installSource 进入正式学校级插件真相 |
| Server Action → DAL | action 只能调用受约束的 DAL，不得绕过冲突检查或直接写库 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-05 | S | `installOrReconcilePlugin()` | mitigate | 强制 `pluginKey = manifest.id`，拒绝用 `name` 或 payload name 作为 canonical identity |
| T-44-06 | T | `installOrReconcilePlugin()` conflict path | mitigate | DAL preflight + SQL 唯一约束双保险，冲突返回明确 token，不允许 silent overwrite |
| T-44-07 | E | `registerPluginManifestAction` | mitigate | 保持 `requireCurrentActorId()` + `assertTeacherManagerScope()`，action 不直接写 DB |
| T-44-08 | R | lifecycle / reconcile updates | mitigate | reconcile 继续 append lifecycle transition，reason 使用 `registered` / `reconciled` 区分 |
| T-44-09 | I | conflict error propagation | accept | 返回冲突 token 仅暴露治理原因，不包含敏感 secret；用于 honest operator feedback |
</threat_model>

<verification>
- `pnpm exec vitest --run src/lib/dal/plugins.test.ts src/actions/plugin-actions.test.ts`
</verification>

<success_criteria>
- `installOrReconcilePlugin()` 成为正式写入 seam。
- 学校范围冲突会被 DAL 明确拒绝并返回 token。
- Action 层仍只负责认证、输入校验和 cache invalidation。
</success_criteria>

<output>
After completion, create `.planning/phases/44-plugin-identity-and-namespace-contract/44-02-SUMMARY.md`
</output>
