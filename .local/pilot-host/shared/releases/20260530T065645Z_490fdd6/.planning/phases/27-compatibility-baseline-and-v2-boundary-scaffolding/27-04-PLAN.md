---
phase: 27-compatibility-baseline-and-v2-boundary-scaffolding
plan: 04
type: execute
wave: 2
depends_on: [27-03]
files_modified:
  - src/features/runtime-platform/seams/index.ts
  - src/features/runtime-platform/seams/database/contract.ts
  - src/features/runtime-platform/seams/database/sqlite-adapter.ts
  - src/features/runtime-platform/seams/event-bus/contract.ts
  - src/features/runtime-platform/seams/event-bus/default-adapter.ts
  - src/features/runtime-platform/seams/transport/contract.ts
  - src/features/runtime-platform/seams/transport/sse-adapter.ts
  - src/features/runtime-platform/host-actions/guards.ts
  - src/features/runtime-platform/host-actions/runtime-host.ts
  - src/features/runtime-platform/host-actions/plugin-host.ts
  - src/features/runtime-platform/seams/seams.test.ts
  - src/features/runtime-platform/host-actions/guards.test.ts
autonomous: true
requirements: [SAFE-02, ARCH-03]
must_haves:
  truths:
    - "D-14 / D-16：PostgreSQL、Redis/Event Bus、WebSocket 对应的 future seams 已显式存在，并集中在 `runtime-platform/seams`。"
    - "D-17：当前运行时仍以 SQLite/SSE/现有 classroom-session truth path 为默认实现，truth ownership 不漂移。"
    - "D-15 / SAFE-02：runtime / plugin host actions 继续执行现有 teacher/school/DTO 边界，且 Phase 27 不暴露任何 cutover toggle、provider switch 或双写真相源。"
  artifacts:
    - path: "src/features/runtime-platform/seams/index.ts"
      provides: "centralized seam exports"
      exports: ["database", "event-bus", "transport"]
    - path: "src/features/runtime-platform/host-actions/guards.ts"
      provides: "runtime/plugin host guard wrapper"
      contains: "assert" 
    - path: "src/features/runtime-platform/seams/seams.test.ts"
      provides: "default-only seam posture regression"
      contains: "describe(\"runtime-platform seams\""
  key_links:
    - from: "src/features/runtime-platform/host-actions/runtime-host.ts"
      to: "src/features/runtime-platform/host-actions/guards.ts"
      via: "guard wrapper import"
      pattern: "./guards"
    - from: "src/features/runtime-platform/seams/transport/sse-adapter.ts"
      to: "src/features/runtime-platform/seams/transport/contract.ts"
      via: "default transport implementation"
      pattern: "implements"
---

<objective>
建立 future infra seams 与 host action guard layer，保证后续可替换性，但本阶段绝不提前 cutover。

Purpose: 先把 PostgreSQL / Redis/Event Bus / WebSocket 的演进接缝和 runtime/plugin host guard 链路落到代码结构里，为后续 phase 提供安全迁移点。
Output: centralized seam contracts + default adapters、runtime/plugin host guards、focused regression tests。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-compatibility-baseline-and-v2-boundary-scaffolding/27-CONTEXT.md
@.planning/phases/27-compatibility-baseline-and-v2-boundary-scaffolding/27-RESEARCH.md
@src/lib/cache-policy.ts
@src/lib/dto/classroom.ts
@scripts/verify-phase5-classroom.ts
@scripts/verify-phase18-schedule.ts

<interfaces>
Relevant current truths:

```ts
// cache-policy.ts
cacheTags.classroom(sessionId)

// dto/classroom.ts
ClassroomActionResultDTOSchema
ClassroomEventDTOSchema
```

Pattern to preserve from existing verifiers:

```ts
runPnpm(["test", "--run", ...], "label")
readFileSync(path, "utf8")
static token guards before focused tests
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 centralized seams 与 default adapters</name>
  <read_first>
    - .planning/phases/27-compatibility-baseline-and-v2-boundary-scaffolding/27-CONTEXT.md
    - .planning/phases/27-compatibility-baseline-and-v2-boundary-scaffolding/27-RESEARCH.md
    - src/lib/cache-policy.ts
    - src/lib/dto/classroom.ts
  </read_first>
  <files>src/features/runtime-platform/seams/index.ts, src/features/runtime-platform/seams/database/contract.ts, src/features/runtime-platform/seams/database/sqlite-adapter.ts, src/features/runtime-platform/seams/event-bus/contract.ts, src/features/runtime-platform/seams/event-bus/default-adapter.ts, src/features/runtime-platform/seams/transport/contract.ts, src/features/runtime-platform/seams/transport/sse-adapter.ts</files>
  <action>按 D-14、D-15、D-16、D-17 建立 `runtime-platform/seams` 集中子域：database seam 只声明 future PostgreSQL-ready adapter contract，并提供当前 SQLite/default adapter；event-bus seam 只声明 canonical event publish/subscribe contract，并提供 current default adapter（可为 in-process/no-op，但必须是实际实现，不是注释）；transport seam 只声明 future WebSocket-ready transport contract，并提供当前 SSE-compatible adapter。所有 default adapters 都必须清楚表达“当前 truth ownership 仍在现有 SQLite + classroom/session write path”，禁止出现 `USE_POSTGRES`, `ENABLE_REDIS`, `ENABLE_WEBSOCKET`, `cutover`, `switchProvider` 等运行时切换开关。</action>
  <acceptance_criteria>
    - `src/features/runtime-platform/seams/index.ts` 集中导出 database/event-bus/transport 三类 seams。
    - 每类 seam 都同时存在 `contract` 与 `default adapter` 文件，不是纯注释占位。
    - seam 源码中不出现 provider toggle 或 hidden switch 令牌。
    - transport default adapter 明确保持 SSE posture，而不是偷偷切成 WebSocket 主链路。
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>未来基础设施接缝已集中存在，且当前默认实现仍忠于现有 SQLite/SSE truth ownership。</done>
</task>

<task type="auto">
  <name>Task 2: 为 runtime/plugin host actions 建立 guard wrappers 并锁定 default-only 姿态</name>
  <read_first>
    - src/features/runtime-platform/contracts/permissions.ts
    - src/features/runtime-platform/contracts/descriptors.ts
    - src/features/runtime-platform/seams/index.ts
    - scripts/verify-phase18-schedule.ts
  </read_first>
  <files>src/features/runtime-platform/host-actions/guards.ts, src/features/runtime-platform/host-actions/runtime-host.ts, src/features/runtime-platform/host-actions/plugin-host.ts, src/features/runtime-platform/seams/seams.test.ts, src/features/runtime-platform/host-actions/guards.test.ts</files>
  <action>按 SAFE-02 与 D-14~D-17 建立统一 host action guard layer：`guards.ts` 负责封装现有 server-side authz、school scope 和 DTO shaping；`runtime-host.ts` 与 `plugin-host.ts` 只能通过 guard wrapper 进入 seam 或后续 host operation contract，不得直接执行外部 side effect。focused tests 必须锁定三件事：guard wrapper 必须要求 actor scope + school scope + parsed DTO；seams 只有 default adapters 被导出和使用；任何 `cutover` / `switch` / `ENABLE_*` 令牌都会导致测试失败。</action>
  <acceptance_criteria>
    - `runtime-host.ts` 与 `plugin-host.ts` 都 import guard wrapper，而不是直接做外部调用。
    - `guards.test.ts` 覆盖未授权 actor、缺失 school scope、DTO parse 失败三类拒绝路径。
    - `seams.test.ts` 覆盖 database/event-bus/transport 仅 default adapter 暴露，无 provider toggle。
    - host action 与 seam 代码中不出现 `eval(`、`dangerouslySetInnerHTML`、`db` bypass、`switchProvider`。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/features/runtime-platform/seams/seams.test.ts src/features/runtime-platform/host-actions/guards.test.ts</automated>
  </verify>
  <done>runtime/plugin host side boundary 已具备默认安全姿态：guarded、school-scoped、DTO-shaped、无 cutover switch。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime/plugin host request → host guards | 不可信 runtime/plugin 请求进入主系统前必须再次执行 authz 与 scope 收敛 |
| seam contract → default adapter | 未来可替换接缝若暴露当前切换开关，会在未完成验证前改变 production posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04-01 | E | runtime/plugin host actions | mitigate | 所有 host action 统一走 `guards.ts`，强制 actor authz、school scope、DTO parse，再允许进入 adapter contract |
| T-27-04-02 | T | seam adapters | mitigate | tests 静态禁止 `ENABLE_*` / `cutover` / provider switch，default adapter 是唯一导出实现 |
| T-27-04-03 | I | bridge/event transport posture | mitigate | transport default adapter 保持 SSE-compatible contract，不暴露 websocket-only runtime data path |
</threat_model>

<verification>
- `pnpm typecheck`
- `pnpm test --run src/features/runtime-platform/seams/seams.test.ts src/features/runtime-platform/host-actions/guards.test.ts`
- 读取 seams 与 host-actions 源码，确认无 toggle / cutover / direct bypass 令牌
</verification>

<success_criteria>
- PostgreSQL、Redis/Event Bus、WebSocket 的 future seams 已显式编码，但不要求真实服务存在。
- runtime/plugin host action 的新入口不会绕过现有 server-side 安全边界。
- 当前课堂/会话 truth ownership 仍停留在现有 SQLite + classroom/session 路径。
</success_criteria>

<output>
After completion, create `.planning/phases/27-compatibility-baseline-and-v2-boundary-scaffolding/27-04-SUMMARY.md`
</output>
