---
phase: 24-live-classroom-operations-and-formative-evaluation
plan: 04
type: execute
wave: 4
depends_on:
  - 24-01
  - 24-02
  - 24-03
files_modified:
  - src/lib/dal/classroom.test.ts
  - src/actions/classroom-actions.test.ts
  - src/components/classroom/classroom-roster-panel.test.tsx
  - src/components/classroom/classroom-student-evaluation-form.test.tsx
  - src/components/classroom/classroom-student-detail-panel.test.tsx
  - scripts/verify-phase24-classroom-evaluation.ts
  - package.json
autonomous: true
requirements:
  - ACT-03
  - EVAL-01
  - EVAL-02
must_haves:
  truths:
    - Phase 24 需要专属 verifier，后续 Phase 25-26 继续改 analytics 或 UI 时不能悄悄破坏 `/classroom` 的评价主工作流。
    - regression 覆盖必须同时锁住 monitoring DTO、teacher-only evaluation action 与 single-student detail panel，不是只测一个 UI 文案。
    - 验证命令必须可独立运行，便于后续阶段快速确认课堂运行与评价边界没有回退。
  artifacts:
    - path: scripts/verify-phase24-classroom-evaluation.ts
      provides: dedicated phase verifier for classroom monitoring and evaluation workflow
      contains: verify:phase24
    - path: src/components/classroom/classroom-student-detail-panel.test.tsx
      provides: focused regression coverage for same-route evidence/evaluation panel
      contains: 课堂证据
    - path: src/actions/classroom-actions.test.ts
      provides: action-level guards for teacher-only formative evaluation writes
      contains: recordStudentFormativeEvaluationAction
  key_links:
    - from: scripts/verify-phase24-classroom-evaluation.ts
      to: src/app/(classroom)/classroom/page.tsx
      via: verifier guards same-route `studentId` and `detailTab` classroom workflow
      pattern: studentId|detailTab|getClassroomStudentDetailDTO
    - from: scripts/verify-phase24-classroom-evaluation.ts
      to: src/actions/classroom-actions.ts
      via: verifier guards fixed evaluation write path and classroom cache invalidation
      pattern: recordStudentFormativeEvaluationAction|updateTag
---

<objective>
为 Phase 24 建立 focused regression 与 `verify:phase24`，锁定 `/classroom` 监控、teacher-only 过程评价写入、以及单学生 evidence/evaluation detail panel 的核心边界。

Purpose: 给后续 analytics 和产品化阶段留下可重复执行的安全网，而不是依赖口头约束。
Output: focused tests plus a dedicated phase verifier command.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-live-classroom-operations-and-formative-evaluation/24-CONTEXT.md
@src/app/(classroom)/classroom/page.tsx
@src/lib/dal/classroom.test.ts
@src/actions/classroom-actions.test.ts
@src/components/classroom/classroom-timeline-panel.test.tsx
@scripts/verify-phase22-orchestration.ts
@scripts/verify-phase23-student-activity.ts
@package.json

<interfaces>
Phase 24 invariants to lock:
```ts
ClassroomRosterSummaryDTOSchema
needsAttention
recordStudentFormativeEvaluationAction
kind: "formative-evaluation"
studentId
detailTab
ClassroomStudentDetailPanel
查看证据与评价
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand focused regression coverage for monitoring, evaluation writes, and the unified student detail panel</name>
  <files>src/lib/dal/classroom.test.ts, src/actions/classroom-actions.test.ts, src/components/classroom/classroom-roster-panel.test.tsx, src/components/classroom/classroom-student-evaluation-form.test.tsx, src/components/classroom/classroom-student-detail-panel.test.tsx</files>
  <read_first>src/lib/dal/classroom.test.ts, src/actions/classroom-actions.test.ts, src/components/classroom/classroom-timeline-panel.test.tsx, src/components/classroom/classroom-roster-panel.tsx, src/components/classroom/classroom-student-evaluation-form.tsx, src/components/classroom/classroom-student-detail-panel.tsx, src/lib/dal/classroom.ts, src/actions/classroom-actions.ts</read_first>
  <behavior>
    - Test 1: DAL tests assert monitoring summary fields and formative-evaluation filtering on the student detail read model.
    - Test 2: action tests assert the new teacher-only evaluation action validates input and invalidates classroom cache.
    - Test 3: component tests assert roster entry wording, fixed evaluation labels, and the dual-tab student detail panel.
  </behavior>
  <action>Update `src/lib/dal/classroom.test.ts` to assert `monitoringSummary`, `needsAttention`, `submissionCount`, `recordStudentFormativeEvaluation`, and `getClassroomStudentDetailDTO` behaviors. Update `src/actions/classroom-actions.test.ts` to assert `recordStudentFormativeEvaluationAction`, `RecordStudentFormativeEvaluationInputSchema.safeParse`, and `updateTag(cacheTags.classroom(parsed.data.sessionId))`. Create `src/components/classroom/classroom-roster-panel.test.tsx` to assert the roster shows `查看证据与评价`, `需要关注`, and `已提交`. Create `src/components/classroom/classroom-student-evaluation-form.test.tsx` to assert the fixed tier labels and tag chips. Create `src/components/classroom/classroom-student-detail-panel.test.tsx` to assert `课堂证据` and `过程评价` tabs plus latest participation summary rendering. Keep the tests source-accurate and grep-verifiable; do not replace them with broad snapshots.</action>
  <acceptance_criteria>
    - `src/lib/dal/classroom.test.ts` contains `monitoringSummary`, `needsAttention`, `recordStudentFormativeEvaluation`, and `getClassroomStudentDetailDTO`.
    - `src/actions/classroom-actions.test.ts` contains `recordStudentFormativeEvaluationAction` and classroom cache invalidation assertions.
    - `src/components/classroom/classroom-roster-panel.test.tsx` contains `查看证据与评价`, `需要关注`, and `已提交`.
    - `src/components/classroom/classroom-student-detail-panel.test.tsx` contains `课堂证据` and `过程评价`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/classroom.test.ts src/actions/classroom-actions.test.ts src/components/classroom/classroom-roster-panel.test.tsx src/components/classroom/classroom-student-evaluation-form.test.tsx src/components/classroom/classroom-student-detail-panel.test.tsx</automated>
  </verify>
  <done>Phase 24 gains focused regression coverage across DTO, DAL, action, and panel layers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `verify:phase24` to guard the fixed classroom evaluation workflow</name>
  <files>scripts/verify-phase24-classroom-evaluation.ts, package.json</files>
  <read_first>scripts/verify-phase22-orchestration.ts, scripts/verify-phase23-student-activity.ts, src/app/(classroom)/classroom/page.tsx, src/components/classroom/classroom-roster-panel.tsx, src/components/classroom/classroom-student-evaluation-form.tsx, src/components/classroom/classroom-student-detail-panel.tsx, src/actions/classroom-actions.ts, src/lib/dal/classroom.ts, package.json</read_first>
  <behavior>
    - Test 1: verifier fails if `/classroom` loses the same-route student detail workflow.
    - Test 2: verifier fails if the fixed 3-tier evaluation write path drifts away from classroom evidence or cache invalidation.
    - Test 3: verifier runs the focused Phase 24 regression suite.
  </behavior>
  <action>Create `scripts/verify-phase24-classroom-evaluation.ts` and register `verify:phase24` in `package.json`, following the repo's existing phase verifier pattern. The static checks must cover at least these items: (1) `src/app/(classroom)/classroom/page.tsx` still contains `studentId`, `detailTab`, and `getClassroomStudentDetailDTO`; (2) `src/components/classroom/classroom-roster-panel.tsx` still contains `查看证据与评价`, `需要关注`, and `已提交`; (3) `src/components/classroom/classroom-student-evaluation-form.tsx` still contains `积极参与`, `正常参与`, and `需要关注`; (4) `src/components/classroom/classroom-student-detail-panel.tsx` contains `课堂证据` and `过程评价`; (5) `src/actions/classroom-actions.ts` contains `recordStudentFormativeEvaluationAction` and `updateTag(cacheTags.classroom(parsed.data.sessionId))`; and (6) `src/lib/dal/classroom.ts` contains `kind: "formative-evaluation"`. Then run the focused Phase 24 regression files from the verifier. Keep the verifier concrete and repo-consistent.</action>
  <acceptance_criteria>
    - `scripts/verify-phase24-classroom-evaluation.ts` exists and contains checks for `studentId`, `detailTab`, `recordStudentFormativeEvaluationAction`, `查看证据与评价`, and `kind: "formative-evaluation"`.
    - `scripts/verify-phase24-classroom-evaluation.ts` runs `pnpm test --run` against the focused Phase 24 regression files.
    - `package.json` contains `verify:phase24` pointing to `tsx scripts/verify-phase24-classroom-evaluation.ts`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase24</automated>
  </verify>
  <done>Future phases can re-run one command to verify Phase 24's classroom monitoring and evaluation invariants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository source -> phase verifier | verifier decides whether Phase 24 classroom monitoring and evaluation invariants still hold |
| focused tests -> runtime workflow | tests must catch route, action, and panel regressions before later phases ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-10 | T | `scripts/verify-phase24-classroom-evaluation.ts` | mitigate | Statically lock same-route detail params, evaluation action names, and formative-evaluation payload markers. |
| T-24-11 | D | focused regression suite | mitigate | Run targeted DAL/action/component tests so later UI or analytics work cannot silently break the workflow. |
| T-24-12 | R | package verification entry | mitigate | Expose `verify:phase24` in `package.json` so the invariant set stays executable and reviewable. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/classroom.test.ts src/actions/classroom-actions.test.ts src/components/classroom/classroom-roster-panel.test.tsx src/components/classroom/classroom-student-evaluation-form.test.tsx src/components/classroom/classroom-student-detail-panel.test.tsx`
- `pnpm verify:phase24`
</verification>

<success_criteria>
- Phase 24 has focused regression coverage for monitoring, evaluation writes, and the same-route student detail workflow.
- Phase 24 has a dedicated verification command.
- later classroom and analytics work cannot silently remove the `/classroom` formative evaluation main path.
</success_criteria>

<output>
After completion, create `.planning/phases/24-live-classroom-operations-and-formative-evaluation/24-04-SUMMARY.md`
</output>
