---
phase: 18-teaching-schedule-os
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - src/lib/dal/schedule-import.ts
  - src/lib/dal/schedule-import.test.ts
  - src/actions/schedule-import-actions.ts
  - src/components/surfaces/schedule-import-review-surface.tsx
  - src/components/surfaces/schedule-import-review-surface.test.tsx
  - src/app/(teacher)/teacher/schedule/import/page.tsx
autonomous: true
requirements:
  - SCHEDULE-01
  - SCHEDULE-02
  - SCHEDULE-09
must_haves:
  truths:
    - D-01: 所有导入数据先进入 staging/import review，未审批前不会影响 runtime schedule。
    - D-02: 每条导入记录都能展示校验结果、映射结果、冲突原因和审批状态。
    - D-03/D-13/D-14: approved rows 通过 teacher-scoped transaction 写入 normalized model，并显式失效 import/agenda tags。
  artifacts:
    - path: src/lib/dal/schedule-import.ts
      provides: row-level validation, conflict detection, approval-safe apply path
      contains: approveScheduleImport
    - path: src/actions/schedule-import-actions.ts
      provides: server action entrypoints for draft/review/apply
      contains: PUBLISH_BLOCKED
    - path: src/components/surfaces/schedule-import-review-surface.tsx
      provides: two-stage review workspace
      contains: 审核通过并写入课表
  key_links:
    - from: src/actions/schedule-import-actions.ts
      to: src/lib/dal/schedule-import.ts
      via: validated review/apply mutations
      pattern: approveScheduleImport
    - from: src/components/surfaces/schedule-import-review-surface.tsx
      to: src/actions/schedule-import-actions.ts
      via: blocked approve button reflects row-level blockers
      pattern: 审核通过并写入课表
---

<objective>
把课表导入真正做成“审核台”：导入先落 staging，再按行校验、按行映射、按行处理冲突，最后只把已批准行写入 normalized schedule model。

Purpose: 直接实现 D-01/D-02/D-03，避免 Phase 18 退化成“一键导入 + 模糊错误提示”。
Output: import DAL、import server actions、row-level review surface、`/teacher/schedule/import` route。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-teaching-schedule-os/18-CONTEXT.md
@.planning/phases/18-teaching-schedule-os/18-RESEARCH.md
@.planning/phases/18-teaching-schedule-os/18-PATTERNS.md
@.planning/phases/18-teaching-schedule-os/18-01-PLAN.md
@src/actions/course-authoring-actions.ts
@src/lib/dal/course-authoring.ts
@src/lib/dto/schedule.ts

<interfaces>
From Phase 18 foundation output:
```ts
export const ScheduleImportDraftInputSchema = z.object({
  schoolId: z.string(),
  sourceType: z.enum(["csv", "xlsx", "connector"]),
  sourceLabel: z.string(),
  rows: z.array(z.object({
    sourceRowKey: z.string(),
    termName: z.string(),
    weekday: z.number().int(),
    bellSlotLabel: z.string(),
    className: z.string(),
    courseTitle: z.string(),
    teacherName: z.string(),
    roomLabel: z.string().nullable(),
  })),
});
```

Use the existing action result shape:
```ts
type ActionResult<T> = { ok: true; data: T } | { ok: false; error: string; message: string; issues?: unknown[] };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement row-level staging, validation, conflict detection, and approval-safe apply</name>
  <files>src/lib/dal/schedule-import.ts, src/lib/dal/schedule-import.test.ts, src/actions/schedule-import-actions.ts</files>
  <read_first>src/lib/dto/schedule.ts, src/actions/course-authoring-actions.ts, src/lib/dal/course-authoring.ts, src/lib/cache-policy.ts, .planning/phases/18-teaching-schedule-os/18-RESEARCH.md</read_first>
  <behavior>
    - Test 1: per D-01, importing rows creates a staging batch and row records instead of writing directly into `scheduleRecurringEntry`.
    - Test 2: per D-02, each row resolves to one of `validation_failed`, `mapping_review`, `conflict_review`, `ready_to_apply`, `approved`, or `rejected` with row-level reasons.
    - Test 3: per D-03, `approveScheduleImport()` only writes approved/ready rows into normalized tables and preserves `sourceBatchId` / `sourceRowId` lineage.
  </behavior>
  <action>Create `src/lib/dal/schedule-import.ts` with teacher-scoped functions to draft an import batch, validate each row, derive mapping summaries, detect duplicates/conflicts against existing `scheduleTeachingAssignment` + `scheduleRecurringEntry`, and approve selected rows in one transaction. Use the exact row status values from `src/lib/dto/schedule.ts`. The apply path must create or reuse `scheduleTerm`, `scheduleWeekPattern`, `scheduleBellSlot`, `scheduleTeachingAssignment`, and `scheduleRecurringEntry` records while writing lineage fields back to the normalized records per D-03. Then add `src/actions/schedule-import-actions.ts` using the same action result pattern as course/lesson actions: `safeParse`, `assertActiveTeacher`, DAL call, and `updateTag()` for `scheduleImportBatch`, `scheduleImportSchool`, `teacherScheduleAgenda`, and `classScheduleAgenda`. When blocker rows remain, return a structured blocked result such as `APPROVE_IMPORT_BLOCKED`; do not silently skip invalid rows.</action>
  <acceptance_criteria>
    - `src/lib/dal/schedule-import.ts` contains `validation_failed`, `conflict_review`, `ready_to_apply`, and `approved`.
    - `src/lib/dal/schedule-import.ts` contains `sourceBatchId` and `sourceRowId` lineage handling.
    - `src/actions/schedule-import-actions.ts` contains `APPROVE_IMPORT_BLOCKED` and `updateTag(`.
    - `src/lib/dal/schedule-import.test.ts` asserts no direct write to recurring schedule before approval.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/schedule-import.test.ts</automated>
  </verify>
  <done>The import backend now behaves like a reviewable staging pipeline rather than a direct write-through upload endpoint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the two-stage import review workspace with row-level approvals</name>
  <files>src/components/surfaces/schedule-import-review-surface.tsx, src/components/surfaces/schedule-import-review-surface.test.tsx, src/app/(teacher)/teacher/schedule/import/page.tsx</files>
  <read_first>src/lib/dto/schedule.ts, src/components/surfaces/teacher-dashboard-surface.tsx, .planning/phases/18-teaching-schedule-os/18-UI-SPEC.md, src/app/(teacher)/teacher/page.tsx</read_first>
  <behavior>
    - Test 1: per D-02, the surface shows batch summary plus row-level review cards with validation, mapping, conflict, and approval state.
    - Test 2: the primary action `审核通过并写入课表` is disabled while blocker rows remain and surfaces a nearby blocker summary.
    - Test 3: the page remains a review workspace, not a one-click wizard, and uses Chinese copy with tonal cards per D-16.
  </behavior>
  <action>Create `/teacher/schedule/import/page.tsx` as a thin page that loads the teacher-scoped import review DTO and renders `ScheduleImportReviewSurface`. In `src/components/surfaces/schedule-import-review-surface.tsx`, implement the exact two-level workspace required by the UI-SPEC: (1) batch summary cards showing source, import time, school scope, total rows, pass/conflict/pending counts; (2) row review cards or expandable rows showing validation issues, mapping summary, conflict summary, and approval state. The row UI must use the Chinese status labels `待审核`, `校验失败`, `映射待确认`, `冲突待处理`, `可入库`, `已批准`, `已拒绝`. Keep the main CTA label exactly `审核通过并写入课表`; disable it when unresolved blocker rows remain and show a blocker summary next to the CTA instead of only a toast.</action>
  <acceptance_criteria>
    - `src/components/surfaces/schedule-import-review-surface.tsx` contains `审核通过并写入课表`.
    - `src/components/surfaces/schedule-import-review-surface.tsx` contains `校验失败`, `映射待确认`, and `冲突待处理`.
    - `src/app/(teacher)/teacher/schedule/import/page.tsx` imports `ScheduleImportReviewSurface`.
    - `src/components/surfaces/schedule-import-review-surface.test.tsx` asserts the primary CTA disabled path when blockers exist.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/surfaces/schedule-import-review-surface.test.tsx</automated>
  </verify>
  <done>Teachers can review each imported row, understand why it is blocked, and approve only safe rows into the normalized schedule domain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uploaded rows / connector output → import DAL | Untrusted schedule input crosses into school-scoped server logic |
| import review DTO → teacher UI | Review summaries are safe to show; raw payloads are not |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | T | `src/lib/dal/schedule-import.ts` | mitigate | Require staging + row statuses before any normalized write; approved apply path runs in one transaction. |
| T-18-06 | I | `src/components/surfaces/schedule-import-review-surface.tsx` | mitigate | Render validation/mapping/conflict summaries, not raw import JSON. |
| T-18-07 | E | `src/actions/schedule-import-actions.ts` | mitigate | Gate all import review/apply actions behind `assertActiveTeacher()` and school-scoped DAL checks. |
| T-18-08 | R | `src/lib/dal/schedule-import.ts` | mitigate | Persist reviewer id, approval state, and source lineage for every approved/rejected row. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/schedule-import.test.ts`
- `pnpm test --run src/components/surfaces/schedule-import-review-surface.test.tsx`
</verification>

<success_criteria>
- Import rows are staged and reviewed before any runtime schedule write.
- Teachers can see row-level validation, mapping, conflict, and approval states.
- Approved rows write lineage-preserving normalized records through a teacher-scoped transaction.
</success_criteria>

<output>
After completion, create `.planning/phases/18-teaching-schedule-os/18-02-SUMMARY.md`
</output>
