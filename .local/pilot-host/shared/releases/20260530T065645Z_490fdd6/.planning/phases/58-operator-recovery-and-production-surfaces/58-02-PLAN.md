---
phase: 58-operator-recovery-and-production-surfaces
plan: "02"
type: execute
wave: 2
depends_on:
  - "58-01"
files_modified:
  - src/actions/operator-posture-recovery-actions.ts
  - src/actions/operator-posture-recovery-actions.test.ts
  - src/app/settings/labs/incidents/[sessionId]/page.tsx
  - src/components/surfaces/platform-command-operator-detail-surface.tsx
  - src/components/surfaces/plugin-lifecycle-operator-surface.tsx
  - src/app/settings/labs/commands/[commandId]/page.tsx
  - src/components/classroom/classroom-control-panel.tsx
  - src/components/classroom/classroom-control-panel.test.tsx
  - src/components/surfaces/platform-command-operator-detail-surface.test.tsx
autonomous: true
requirements:
  - OPS-01
  - OPS-03
  - PLUG-03
must_haves:
  truths:
    - "operator 可以从 incident 或 classroom shell 稳定跳到 command detail，而不是依赖 Settings Labs 内联状态。"
    - "classroom shell 只保留轻量恢复与 incident 入口，不再把高风险 posture 变更当作课堂内 quick path。"
    - "classroom shell 的 degraded honesty 固定输出 `仍可信什么 / 已不可信什么 -> 影响范围 -> 推荐下一步`，与其余 operator surfaces 保持同一口径。"
    - "resume / suspend / fallback 在 detail view 内有明确的二次确认、Server Action 接线与审计写入，不停留在 disabled placeholder。"
  artifacts:
    - path: src/actions/operator-posture-recovery-actions.ts
      provides: high-risk posture recovery Server Action entrypoint
    - path: src/app/settings/labs/incidents/[sessionId]/page.tsx
      provides: stable incident detail route shell for classroom/operator CTA landing
    - path: src/components/surfaces/platform-command-operator-detail-surface.tsx
      provides: command detail drill-down surface
    - path: src/components/surfaces/plugin-lifecycle-operator-surface.tsx
      provides: detail-view confirmation host for plugin/posture-changing recovery
    - path: src/app/settings/labs/commands/[commandId]/page.tsx
      provides: stable command detail route
    - path: src/components/classroom/classroom-control-panel.tsx
      provides: escalated operator CTA and restricted shell recovery affordances
  key_links:
    - from: src/components/classroom/classroom-control-panel.tsx
      to: /settings/labs/incidents/[sessionId]
      via: incident CTA
      pattern: 查看课堂事件
    - from: /settings/labs/incidents/[sessionId]
      to: /settings/labs/commands/[commandId]
      via: related command next hop
      pattern: commandId
    - from: src/components/surfaces/platform-command-operator-detail-surface.tsx
      to: src/actions/operator-posture-recovery-actions.ts
      via: detail-view confirm for resume|suspend|fallback
      pattern: resume|suspend|fallback
---

<objective>
补齐 Phase 58 的稳定下钻节点、classroom shell operator 入口，以及高风险恢复动作的 detail-view 确认路径。

Purpose: 让 classroom shell 在 Wave 2 就能稳定落到 incident detail route，并在 detail view 内完成 `resume` / `suspend` / `fallback` 的强确认、Server Action 接线与 audited recovery boundary；Wave 3 再把该 route 扩成完整 summary-first operator surface。
Output: incident detail route shell、command detail route/surface、high-risk recovery Server Action、detail confirm host、classroom shell escalated CTA、focused UI regressions。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-CONTEXT.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-RESEARCH.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-UI-SPEC.md
@src/components/classroom/classroom-control-panel.tsx
@src/actions/classroom-actions.ts
@src/actions/plugin-actions.ts
@src/features/platform-core/observability/operator-read-model.ts
@src/features/platform-core/observability/dto.ts
@src/components/surfaces/plugin-lifecycle-operator-surface.tsx

<interfaces>
From `src/actions/classroom-actions.ts`:

```ts
export async function runCurrentVotingRecoveryAction(input: {
  sessionId: string;
  stepId: string;
  recoveryAction: "retry" | "reconcile" | "suspend" | "fallback";
}): Promise<ActionResult<unknown>>;
```

From `src/features/platform-core/observability/dto.ts`:

```ts
export type PlatformCommandOperatorDetailDTO = {
  command: PlatformCommandOperatorSummaryDTO | null;
  timeline: PlatformCommandOperatorTimelineEventDTO[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先补稳定 landing route，再抽出 command detail route</name>
  <files>src/components/surfaces/platform-command-operator-detail-surface.test.tsx, src/components/classroom/classroom-control-panel.test.tsx, src/app/settings/labs/incidents/[sessionId]/page.tsx, src/components/surfaces/platform-command-operator-detail-surface.tsx, src/app/settings/labs/commands/[commandId]/page.tsx</files>
  <behavior>
    - Test 1: `/settings/labs/incidents/[sessionId]` 在 Wave 2 已存在稳定 landing route，可供 classroom shell 与后续 relation cards 使用；先以 thin page 消费 58-01 的 read model，不要求在本 wave 完成完整 summary surface。
    - Test 2: `/settings/labs/commands/[commandId]` 直接消费 `getPlatformCommandWithTimeline()`，渲染 summary → timeline，不再依赖 `/settings/labs?commandId=` 内联状态。
    - Test 3: classroom shell 始终存在 `查看课堂事件` 入口；当 posture 为 degraded / failed / blocked 时，该入口升级为更显眼 CTA。
    - Test 4: classroom shell 的 honesty 区块固定包含 `影响范围` 行，不再只停留在边界 + next step。
    - Test 5: command detail 保留 command summary、delegation / approval、event dispatch timeline，不回退成 raw log page。
  </behavior>
  <action>先补测试，再创建 `/settings/labs/incidents/[sessionId]` 的 thin landing route，并抽出 `PlatformCommandOperatorDetailSurface` 与 `/settings/labs/commands/[commandId]`，把现有 Settings Labs 内联 command detail 收敛成稳定 next-hop。同步修改 `ClassroomControlPanel` 的 honesty 区块，使其按 D-58-08、D-58-09、D-58-10、D-58-11、D-58-22 固定输出 `仍可信什么 / 已不可信什么 -> 影响范围 -> 推荐下一步`，而不是只写边界与 next step。thin incident route 只负责让 shell CTA 与 relation href 在 Wave 2 即可落地，完整 summary-first incident surface 留给 58-03 扩展。按 D-58-07 保持 command 作为下钻 detail，不把它升级成首页锚点。按 UI-SPEC 保留 summary-first detail rhythm，不做 dense console。</action>
  <verify>
    <automated>pnpm exec vitest --run src/components/surfaces/platform-command-operator-detail-surface.test.tsx src/features/platform-core/observability/operator-read-model.test.ts</automated>
  </verify>
  <done>Wave 2 已提供稳定 incident landing route 与 command detail route，classroom shell / relation cards 不再前向依赖未创建页面；incident read model 可以输出不会失效的 href。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 detail surfaces 实现高风险恢复动作的强确认与 audited Server Action 接线</name>
  <files>src/actions/operator-posture-recovery-actions.ts, src/actions/operator-posture-recovery-actions.test.ts, src/components/surfaces/platform-command-operator-detail-surface.tsx, src/components/surfaces/platform-command-operator-detail-surface.test.tsx, src/components/surfaces/plugin-lifecycle-operator-surface.tsx</files>
  <behavior>
    - Test 1: command/plugin detail surface 对 `resume` / `suspend` / `fallback` 提供二次确认层，并固定展示 `影响范围`、`姿态变化`、`将写入的审计记录`。
    - Test 2: `operator-posture-recovery-actions` 通过现有 server-owned seams 执行高风险动作，写入 command / recovery audit / task history，并承担 `updateTag()` / `revalidatePath()`。
    - Test 3: 当高风险动作当前不可执行时，detail surface 继续显示 disabled + reason + next step，不伪装成可点按钮。
  </behavior>
  <action>新增 `operator-posture-recovery-actions`，并在 `PlatformCommandOperatorDetailSurface` 与现有 `PluginLifecycleOperatorSurface` 中接入 detail-view 强确认。按 D-58-12、D-58-13、D-58-15、D-58-24，仅允许在 detail view 执行 `resume` / `suspend` / `fallback`；确认层必须显示 `影响范围`、`姿态变化`、`将写入的审计记录`，然后复用现有 `plugin-actions` / classroom recovery / command bus seams 完成 audited mutation 与缓存失效。若当前上下文不安全，则保留 disabled + reason + next step，而不是把动作藏掉。</action>
  <verify>
    <automated>pnpm exec vitest --run src/actions/operator-posture-recovery-actions.test.ts src/components/surfaces/platform-command-operator-detail-surface.test.tsx src/actions/plugin-actions.test.ts</automated>
  </verify>
  <done>detail surfaces 已提供 `resume` / `suspend` / `fallback` 的强确认、Server Action 接线与审计写入；incident/detail next-hop 不再停留在 disabled placeholder。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 收紧 classroom shell recovery quick path 并升级 incident CTA</name>
  <files>src/components/classroom/classroom-control-panel.tsx, src/components/classroom/classroom-control-panel.test.tsx</files>
  <behavior>
    - Test 1: classroom shell 保留最小恢复入口与 incident CTA，但 `retry`/`reconcile` 之外的 posture-changing actions 不再可直接执行。
    - Test 2: shell 明确提示 `resume`/`suspend`/`fallback` 需去 incident/detail surface 确认，而不是静默消失。
    - Test 3: degraded honesty copy 固定包含 `仍可信什么 / 已不可信什么 -> 影响范围 -> 推荐下一步` 三段，不降级成 generic red alert。
  </behavior>
  <action>修改 `ClassroomControlPanel`，按 D-58-03、D-58-08、D-58-09、D-58-10、D-58-11、D-58-17、D-58-22、D-58-23、D-58-24、D-58-25 保留课堂内最小 recovery 与课堂告警，但将高风险动作从 shell quick buttons 移除：shell 只允许 `retry` / `reconcile` 走轻确认；`resume` / `suspend` / `fallback` 必须以 disabled/help copy + `查看课堂事件` 导流到已实现的 incident/detail confirm path。所有 degraded honesty copy 都必须固定包含 trust boundary、impact scope、next step 三段。保留现有 Phase 57 teacher aggregate 与 transport banner，不把 classroom shell 扩成 operator console。</action>
  <verify>
    <automated>pnpm exec vitest --run src/components/classroom/classroom-control-panel.test.tsx src/actions/classroom-actions.test.ts</automated>
  </verify>
  <done>classroom shell 已稳定暴露 incident 入口，degraded/failure posture 时 CTA 升格，高风险恢复动作不再出现在课堂内 quick path。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classroom shell → operator incident/detail routes | 课堂内仅导流，不直接承担高风险恢复确认 |
| command detail route → platform observability read model | 只读 command/event truth，不允许直接 mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-04 | E | `ClassroomControlPanel` recovery CTA | mitigate | 只保留 `retry`/`reconcile` quick path，高风险动作通过 disabled copy + detail route 导流 |
| T-58-05 | R | command/plugin detail recovery | mitigate | 高风险动作只在 detail confirm 中执行，并通过 audited Server Action 写入 command / recovery history |
| T-58-06 | I | command detail route | mitigate | command page 继续复用 school-scoped `getPlatformCommandWithTimeline()`，不得绕过 observability read model |
</threat_model>

<verification>
- `pnpm exec vitest --run src/actions/operator-posture-recovery-actions.test.ts src/components/surfaces/platform-command-operator-detail-surface.test.tsx src/components/classroom/classroom-control-panel.test.tsx src/actions/classroom-actions.test.ts`
- 手工代码检查：`ClassroomControlPanel` 不再直接渲染可点击的 `suspend` / `fallback` shell buttons。
- 手工代码检查：detail confirm host 固定展示 `影响范围`、`姿态变化`、`将写入的审计记录` 三项，不允许直接裸触发 `resume` / `suspend` / `fallback`。
</verification>

<success_criteria>
- `/settings/labs/commands/[commandId]` 存在并可承接 incident deep link。
- `/settings/labs/incidents/[sessionId]` 在 Wave 2 已存在稳定 landing route，可承接 classroom shell CTA。
- detail surfaces 上存在 `resume` / `suspend` / `fallback` 的强确认与 audited Server Action 接线。
- classroom shell 始终显示 operator entry；degraded / failed / blocked 时升级为显眼 CTA。
- classroom shell 的 honesty posture 与其余 operator surfaces 使用同一固定三段模板，且明确显示 `影响范围`。
- classroom shell 只留下轻确认恢复动作，高风险 posture 变更改由 detail surfaces 确认。
</success_criteria>

<output>
After completion, create `.planning/phases/58-operator-recovery-and-production-surfaces/58-02-SUMMARY.md`
</output>
