---
phase: quick
plan: 260515-ac7
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dto/course-authoring.ts
  - src/lib/dal/course-authoring.ts
  - src/actions/course-authoring-actions.ts
  - src/actions/course-authoring-actions.test.ts
  - src/components/surfaces/teacher-course-detail-surface.tsx
  - src/components/courses/course-detail-form.tsx
  - src/components/courses/course-detail-form.test.tsx
autonomous: true
requirements:
  - QUICK-260515-AC7
must_haves:
  truths:
    - "本次 quick 只覆盖课程删除 eligibility、blocked feedback、显式确认交互和相关测试。"
    - "删除 guardrail 不扩散到班级关联、enrollment 或其他 phase。"
    - "delete 与 archive 必须保持为两条不同语义路径，不能退化成普通状态更新。"
  artifacts:
    - path: "src/lib/dal/course-authoring.ts"
      provides: "课程删除 eligibility 判定与 blocked reason contract"
    - path: "src/actions/course-authoring-actions.ts"
      provides: "显式确认约束下的 delete action 边界"
    - path: "src/components/surfaces/teacher-course-detail-surface.tsx"
      provides: "课程详情页内的 blocked feedback 与 guarded delete 入口"
  key_links:
    - from: "src/components/surfaces/teacher-course-detail-surface.tsx"
      to: "src/actions/course-authoring-actions.ts"
      via: "课程详情页提交 guarded delete"
      pattern: "deleteCourseAction"
    - from: "src/actions/course-authoring-actions.ts"
      to: "src/lib/dal/course-authoring.ts"
      via: "teacher-scoped delete eligibility 与删除 mutation"
      pattern: "getCourseDeleteEligibility|deleteCourse"
    - from: "src/components/courses/course-detail-form.test.tsx"
      to: "src/components/courses/course-detail-form.tsx"
      via: "显式确认与 blocked feedback UI regression"
      pattern: "删除课程|确认"
---

<objective>
正式登记 Phase 14-03 / COURSE-05 的 quick 任务，只为课程删除 guardrail 建立最小执行计划。

Purpose: 把课程删除 eligibility、blocked feedback、显式确认交互与相关测试收敛进一个独立 quick，避免实现时扩散到 class association、enrollment 或其他 phase。

Output: 一个只围绕课程删除路径的最小 quick plan，供后续执行直接落地到既有 DAL、action、课程详情页与测试文件。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-course-lifecycle-and-associations/14-03-PLAN.md
@src/lib/dto/course-authoring.ts
@src/lib/dal/course-authoring.ts
@src/actions/course-authoring-actions.ts
@src/actions/course-authoring-actions.test.ts
@src/components/surfaces/teacher-course-detail-surface.tsx
@src/components/courses/course-detail-form.tsx
@src/components/courses/course-detail-form.test.tsx

<interfaces>
```ts
getTeacherCourseDetailDTO({ courseId })
getCourseDeleteEligibility({ courseId })
deleteCourseAction(input)
```

显式 out of scope:
```ts
COURSE-06 // class association
COURSE-07 // enrollment cleanup or reassignment
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为课程删除 eligibility 与 action contract 建立受限实现计划</name>
  <files>src/lib/dto/course-authoring.ts, src/lib/dal/course-authoring.ts, src/actions/course-authoring-actions.ts, src/actions/course-authoring-actions.test.ts</files>
  <action>在既有课程 authoring DTO、DAL 和 action 边界内补齐课程删除 eligibility、结构化 blocked reasons、teacher-facing blocked feedback、显式确认输入以及成功后的 cache invalidation。实现必须明确 delete 与 archive 的语义分离，只允许教师删除满足条件的课程；若被 lessons 或其他当前课程实体级 guardrail 阻止，action 必须返回可供 UI 直接呈现的结构化失败结果。不要新增任何班级关联、enrollment 清理或跨 phase 数据流。</action>
  <verify>
    <automated>pnpm test --run "src/actions/course-authoring-actions.test.ts" "src/lib/dal/course-authoring.test.ts"</automated>
  </verify>
  <done>服务端删除路径具备 eligibility 判断、显式确认约束和 blocked result contract，且测试能证明没有退化成 archive/status mutation。</done>
</task>

<task type="auto">
  <name>Task 2: 在课程详情页补显式确认交互与页内 blocked feedback</name>
  <files>src/components/surfaces/teacher-course-detail-surface.tsx, src/components/courses/course-detail-form.tsx, src/components/courses/course-detail-form.test.tsx</files>
  <action>把 guarded delete 入口收敛在课程详情工作流内，提供显式确认交互，并在删除被阻止时于当前页内展示清晰 feedback，而不是只依赖 toast。相关测试只覆盖删除确认与 blocked feedback，不扩展到班级关联、enrollment 或其他课程管理动作。</action>
  <verify>
    <automated>pnpm test --run "src/components/courses/course-detail-form.test.tsx"</automated>
  </verify>
  <done>课程详情页具备显式确认删除路径和页内 blocked-delete 证据，测试能拦截回归。</done>
</task>

</tasks>

<verification>
- 只检查课程删除 guardrail 相关文件，不新增其他 planning 或 phase 产物。
- 删除实现必须继续局限于课程实体本身，不引入 class association 或 enrollment 逻辑。
</verification>

<success_criteria>
- quick 计划范围只覆盖 COURSE-05 删除 guardrail。
- delete eligibility、blocked feedback、显式确认交互和相关测试都被明确列入执行范围。
- class association、enrollment 和其他 phase 明确保持 out of scope。
</success_criteria>

<output>
后续执行完成后创建 `.planning/quick/260515-ac7-phase-14-03-course-05-guardrail-1-eligib/260515-ac7-SUMMARY.md`。
</output>
