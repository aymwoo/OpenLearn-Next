---
phase: 23-student-in-class-activity-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - src/lib/dto/learning.ts
  - src/lib/dto/classroom.ts
  - src/lib/dal/learning.ts
  - src/lib/dal/classroom.ts
  - src/actions/classroom-actions.ts
  - src/actions/classroom-actions.test.ts
  - src/components/learning/classroom-runtime-client.tsx
  - src/components/learning/quick-response-step-card.tsx
autonomous: true
requirements:
  - ACT-02
must_haves:
  truths:
    - 课堂 quick response 继续走 Server Action + DAL + `classroomEvidence` 的 durable、append-only、可审计写链路。
    - quick response 历史按 session + step + student 读取，不覆盖旧记录，也不与 task/quiz attempt 真相源混用。
    - 学生端 quick response 看起来比 task 更轻，但缓存失效、权限校验和 timeline 审计边界不变。
  artifacts:
    - path: src/lib/dto/classroom.ts
      provides: typed student quick-response input contract on the classroom boundary
      contains: student-quick-response
    - path: src/lib/dal/classroom.ts
      provides: append-only session-scoped quick-response persistence backed by classroom evidence
      contains: recordClassroomEvidence
    - path: src/components/learning/quick-response-step-card.tsx
      provides: lightweight classroom response UI with latest plus history rendering
      contains: 提交课堂回应
  key_links:
    - from: src/actions/classroom-actions.ts
      to: src/lib/dal/classroom.ts
      via: student quick-response action writes through the same classroom evidence durability path
      pattern: recordClassroomEvidence|updateTag(cacheTags.classroom
    - from: src/lib/dal/learning.ts
      to: src/components/learning/quick-response-step-card.tsx
      via: player personal DTO reads latest and history quick responses for the active classroom step
      pattern: latestQuickResponse|quickResponseHistory
---

<objective>
为学生端增加 durable 的课堂 quick response / evidence capture：学生可以在当前课堂步骤中提交
一次新的课堂回应，服务端会 append-only 存储到 session evidence，并把最新记录和历史记录
稳定回流到 player。

Purpose: 兑现 ACT-02，同时保持现有 task/quiz lesson submission truth 与 classroom
session evidence truth 的边界清晰。
Output: typed quick-response contract, Server Action + DAL write path, player-side latest/history
read model and UI.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-student-in-class-activity-flow/23-CONTEXT.md
@.planning/phases/23-student-in-class-activity-flow/23-RESEARCH.md
@.planning/phases/23-student-in-class-activity-flow/23-UI-SPEC.md
@src/lib/dto/learning.ts
@src/lib/dto/classroom.ts
@src/lib/dal/learning.ts
@src/lib/dal/classroom.ts
@src/actions/classroom-actions.ts
@src/actions/classroom-actions.test.ts
@src/components/learning/classroom-runtime-client.tsx
@src/components/learning/task-step-card.tsx

<interfaces>
Existing durable evidence write path to reuse:
```ts
recordClassroomEvidence({
  sessionId,
  studentId,
  stepId,
  sourceType: "student-quick-response",
  evidenceType: "response",
  payload,
})
```

Current classroom evidence boundary already enforces:
```ts
if (payload.sourceType.startsWith("student-")) {
  if (payload.studentId !== user.id) throw new Error("CLASSROOM_EVIDENCE_UNAUTHORIZED")
  await ensureSessionStudentParticipant(session.id, user.id)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define a typed quick-response read/write contract on top of classroom evidence</name>
  <files>src/lib/dto/learning.ts, src/lib/dto/classroom.ts, src/lib/dal/learning.ts, src/lib/dal/classroom.ts, src/actions/classroom-actions.ts, src/actions/classroom-actions.test.ts</files>
  <read_first>src/lib/dto/learning.ts, src/lib/dto/classroom.ts, src/lib/dal/learning.ts, src/lib/dal/classroom.ts, src/actions/classroom-actions.ts, src/actions/classroom-actions.test.ts, src/lib/dal/classroom.test.ts, .planning/phases/23-student-in-class-activity-flow/23-CONTEXT.md</read_first>
  <behavior>
    - Test 1: student quick-response writes use a dedicated typed input contract and still end in `classroomEvidence` + `classroomTimeline` append-only writes.
    - Test 2: player personal DTO can read latest and history quick responses scoped by `sessionId + stepId + studentId` without mixing them into task or quiz attempts.
    - Test 3: action-layer cache invalidation still updates `cacheTags.classroom(sessionId)` and any required player personal state tags after a successful submit.
  </behavior>
  <action>Extend `src/lib/dto/classroom.ts` with a dedicated quick-response input contract that hard-codes the student evidence path instead of exposing the raw generic evidence form to the runtime client. Add a typed quick-response attempt/read model in `src/lib/dto/learning.ts` for latest and history records, including fields for `id`, `sessionId`, `stepId`, `attemptNo`, `body`, `createdAt`, and a success message that explicitly says `已记录为新的课堂回应`. In `src/lib/dal/classroom.ts`, keep persistence on top of `recordClassroomEvidence()` and `createClassroomTimelineEntry()`; if needed, add a narrow helper that wraps `sourceType: "student-quick-response"` and `evidenceType: "response"`. In `src/lib/dal/learning.ts`, read classroom evidence rows for the active session/student/step and expose them separately from task/quiz submissions. In `src/actions/classroom-actions.ts`, add a dedicated student quick-response Server Action that validates the typed input, calls the DAL helper, and invalidates the classroom cache tags. Update `src/actions/classroom-actions.test.ts` to assert the new wiring stays on the classroom evidence path.</action>
  <acceptance_criteria>
    - `src/lib/dto/classroom.ts` contains a typed quick-response input contract that encodes `student-quick-response` and `response` semantics.
    - `src/lib/dto/learning.ts` contains a typed quick-response latest/history DTO separate from `TaskAttemptDTOSchema` and `QuizAttemptDTOSchema`.
    - `src/lib/dal/learning.ts` contains logic that reads quick-response history from classroom evidence scoped by session, step, and student.
    - `src/actions/classroom-actions.ts` contains a dedicated quick-response action and still calls `updateTag(cacheTags.classroom(sessionId))`.
    - `src/actions/classroom-actions.test.ts` contains assertions for the new quick-response action path.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/actions/classroom-actions.test.ts src/lib/dal/classroom.test.ts src/lib/dal/learning.test.ts</automated>
  </verify>
  <done>the repo now has a typed, append-only, classroom-evidence-backed quick-response contract that the player can read and submit safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the lightweight quick-response card to the student runtime</name>
  <files>src/components/learning/classroom-runtime-client.tsx, src/components/learning/quick-response-step-card.tsx</files>
  <read_first>src/components/learning/classroom-runtime-client.tsx, src/components/learning/task-step-card.tsx, src/components/learning/quiz-step-card.tsx, src/actions/classroom-actions.ts, .planning/phases/23-student-in-class-activity-flow/23-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: when the active step expects a quick response, the player shows a short input, one primary CTA, durability copy, and latest/history feedback.
    - Test 2: success feedback explicitly tells the student a new classroom record was stored.
    - Test 3: task and quiz flows remain unchanged when a step does not use the quick-response path.
  </behavior>
  <action>Create `src/components/learning/quick-response-step-card.tsx` as a lightweight classroom response card. It must render one labeled input area, one format hint line, one durability line, one primary button with the exact wording `提交课堂回应`, and latest/history rendering in a secondary tonal layer. Integrate it into `src/components/learning/classroom-runtime-client.tsx` so the current-step renderer chooses quick response when the step's student activity contract indicates the evidence path is `student-quick-response` or equivalent. Keep this UI lighter than `TaskStepCard`: shorter copy, shorter input, immediate success feedback, but still append-only with `第 N 次回应` style history. Do not replace task or quiz submit cards, and do not allow the quick-response card to bypass the active classroom session requirement.</action>
  <acceptance_criteria>
    - `src/components/learning/quick-response-step-card.tsx` exists and contains the exact text `提交课堂回应` and a durability line that tells the student the response becomes a new classroom record.
    - `src/components/learning/classroom-runtime-client.tsx` contains rendering logic for the quick-response card in addition to task and quiz rendering.
    - quick-response history wording uses attempt-style ordinal copy such as `第 1 次回应` or `最新回应`, not overwrite-style copy.
    - the runtime client does not remove `TaskStepCard` or `QuizStepCard` integration for non-quick-response steps.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/learning/student-step-cards.test.ts src/components/surfaces/student-player-surfaces.test.ts</automated>
  </verify>
  <done>students can now submit a lightweight in-class response that is durably stored and visibly returned as latest plus history in the player.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| student runtime client -> classroom action | quick-response input must cross the same Server Action boundary as other durable classroom writes |
| classroom evidence store -> player read model | quick-response history must remain session-scoped classroom evidence, not lesson-wide task truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | E | `src/actions/classroom-actions.ts` | mitigate | Dedicated quick-response action validates typed input and still relies on current-user/session participant checks in the DAL. |
| T-23-06 | T | `src/lib/dal/classroom.ts` | mitigate | Persist quick responses through `classroomEvidence` and `classroomTimeline` only, preserving append-only auditability. |
| T-23-07 | I | `src/lib/dal/learning.ts` | mitigate | Read quick-response latest/history separately from task and quiz attempts so analytics and review truth do not mix domains. |
| T-23-08 | D | player runtime | accept | The UI becomes lighter, but the actual write path and cache invalidation remain on the same server boundary. |
</threat_model>

<verification>
- `pnpm test --run src/actions/classroom-actions.test.ts src/lib/dal/classroom.test.ts src/lib/dal/learning.test.ts`
- `pnpm test --run src/components/learning/student-step-cards.test.ts src/components/surfaces/student-player-surfaces.test.ts`
</verification>

<success_criteria>
- quick-response writes are durable, append-only, and classroom-session-scoped.
- player can render latest and history quick responses without breaking task/quiz flows.
- classroom action boundaries, auth checks, and cache invalidation remain explicit.
</success_criteria>

<output>
After completion, create `.planning/phases/23-student-in-class-activity-flow/23-02-SUMMARY.md`
</output>
