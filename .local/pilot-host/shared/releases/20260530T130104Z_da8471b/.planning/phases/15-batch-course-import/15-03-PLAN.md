---
phase: 15-batch-course-import
plan: 03
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
files_modified:
  - src/app/(teacher)/teacher/courses/page.tsx
  - src/components/surfaces/teacher-course-center-surface.tsx
  - src/components/surfaces/teacher-course-center-surface.test.tsx
  - src/app/(teacher)/teacher/courses/import/[batchId]/page.tsx
  - src/components/surfaces/course-import-review-surface.tsx
  - src/components/surfaces/course-import-review-surface.test.tsx
  - src/actions/course-import-actions.test.ts
  - src/lib/dal/course-import.test.ts
  - package.json
  - scripts/verify-phase15-course-import.ts
autonomous: true
requirements:
  - COURSE-08
  - COURSE-09
must_haves:
  truths:
    - D-18/D-19: 应用完成后必须有产品内结果概览和行级结果说明，并明确区分 created、updated、skipped、failed。
    - D-20: 结果页的主后续动作固定回到 `/teacher/courses`，继续衔接现有课程中心，而不是跳去任意课程详情或只留 toast。
    - Success Criteria 1/3/4: 课程中心需要可发现的导入入口、模板下载、结果回看和可回归验证，确保同校重复不会静默创建。
  artifacts:
    - path: src/components/surfaces/teacher-course-center-surface.tsx
      provides: course-center import entry and return path back from result flow
      contains: 下载 CSV 模板
    - path: src/components/surfaces/course-import-review-surface.tsx
      provides: result summary plus row-level reasons after apply
      contains: created
    - path: scripts/verify-phase15-course-import.ts
      provides: dedicated regression guard for course import boundaries
      contains: verify:phase15
  key_links:
    - from: src/components/surfaces/teacher-course-center-surface.tsx
      to: src/app/(teacher)/teacher/courses/import/[batchId]/page.tsx
      via: import entry, latest-batch continuation, and return CTA loop
      pattern: /teacher/courses/import/
    - from: package.json
      to: scripts/verify-phase15-course-import.ts
      via: dedicated phase verification command
      pattern: verify:phase15
    - from: scripts/verify-phase15-course-import.ts
      to: src/actions/course-import-actions.ts
      via: static guards on draft/apply/result invariants
      pattern: draftCourseImportAction|applyCourseImportAction
---

<objective>
把课程导入闭环收尾到课程中心主工作流：入口清晰、结果可回看、失败原因可解释，并通过专项 verifier 固定 Phase 15 的边界。

Purpose: 避免批量导入能力只停留在“能写入”，而缺少产品级 discoverability、回流路径和后续回归保护。
Output: course-center import entry, import result mode, focused regression tests, and `verify:phase15`.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-batch-course-import/15-CONTEXT.md
@.planning/phases/15-batch-course-import/15-RESEARCH.md
@.planning/phases/15-batch-course-import/15-UI-SPEC.md
@.planning/phases/15-batch-course-import/15-01-PLAN.md
@.planning/phases/15-batch-course-import/15-02-PLAN.md
@src/app/(teacher)/teacher/courses/page.tsx
@src/components/surfaces/teacher-course-center-surface.tsx
@src/components/surfaces/teacher-course-center-surface.test.tsx
@src/components/surfaces/course-import-review-surface.tsx
@src/actions/course-import-actions.ts
@package.json

<interfaces>
User-visible loop finalized in this plan:
```ts
GET /teacher/courses
GET /teacher/courses/import/template
GET /teacher/courses/import/[batchId]
```

Result summary contract that must remain visible in-product:
```ts
type CourseImportResultSummary = {
  created: number;
  updated: number;
  skipped: number;
  failed: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the course-center import entry and result-return loop</name>
  <files>src/app/(teacher)/teacher/courses/page.tsx, src/components/surfaces/teacher-course-center-surface.tsx, src/components/surfaces/teacher-course-center-surface.test.tsx, src/app/(teacher)/teacher/courses/import/[batchId]/page.tsx, src/components/surfaces/course-import-review-surface.tsx, src/components/surfaces/course-import-review-surface.test.tsx</files>
  <read_first>src/app/(teacher)/teacher/courses/page.tsx; src/components/surfaces/teacher-course-center-surface.tsx; src/components/surfaces/teacher-course-center-surface.test.tsx; src/components/surfaces/course-import-review-surface.tsx; .planning/phases/15-batch-course-import/15-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: the course center exposes an obvious import entry plus CSV template download action without degrading into a generic admin table.
    - Test 2: after apply, the import page renders result summary cards and row-level reasons for created, updated, skipped, and failed rows.
    - Test 3: the primary follow-up action on the result page returns to `/teacher/courses`, while secondary actions can include retry or template download.
  </behavior>
  <action>Update `src/components/surfaces/teacher-course-center-surface.tsx` and the course-center page so teachers can discover bulk import from the existing course management hero: add a clear import entry plus `下载 CSV 模板` action that keeps the current course-center visual language. Extend `src/components/surfaces/course-import-review-surface.tsx` and the `[batchId]` route so once a batch is applied, the page switches into a result mode with summary cards for `created`, `updated`, `skipped`, and `failed`, followed by row-level reasons grouped or listed in a high-density teacher-friendly layout. The result mode must keep the primary CTA aligned to D-20 by returning to `/teacher/courses`; do not collapse completion feedback into a toast or an exported file only. Add or update surface tests to assert the course-center import entry, result summary labels, row-level reasons, and return-to-course-center CTA.</action>
  <acceptance_criteria>
    - `src/components/surfaces/teacher-course-center-surface.tsx` contains `下载 CSV 模板` and an import entry label.
    - `src/components/surfaces/course-import-review-surface.tsx` contains `created`, `updated`, `skipped`, and `failed` summary rendering or their Chinese equivalents backed by the same four result categories.
    - `src/components/surfaces/course-import-review-surface.tsx` contains a CTA that navigates back to `/teacher/courses`.
    - `src/components/surfaces/teacher-course-center-surface.test.tsx` and `src/components/surfaces/course-import-review-surface.test.tsx` assert entry visibility and result-summary visibility.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/surfaces/teacher-course-center-surface.test.tsx src/components/surfaces/course-import-review-surface.test.tsx</automated>
  </verify>
  <done>The import workflow is now discoverable from the course center and finishes with a real result screen that routes teachers back into the same operating surface.</done>
</task>

<task type="auto">
  <name>Task 2: Add a dedicated verification command for Phase 15 import boundaries</name>
  <files>src/actions/course-import-actions.test.ts, src/lib/dal/course-import.test.ts, package.json, scripts/verify-phase15-course-import.ts</files>
  <read_first>package.json; scripts/verify-phase16-theme-layout.ts; scripts/verify-phase17-editor.ts; scripts/verify-phase18-schedule.ts; src/actions/course-import-actions.ts; src/lib/dal/course-import.ts; src/components/surfaces/teacher-course-center-surface.tsx</read_first>
  <action>Create `scripts/verify-phase15-course-import.ts` and register `verify:phase15` in `package.json`. The script must statically verify the locked Phase 15 invariants: (1) course import still uses the fixed CSV headers `标题,学科,年级,课程状态`; (2) the import path includes persisted `courseImportBatch` and `courseImportRow` staging rather than direct `courses` writes on upload; (3) apply logic still references `title + subject + grade` for match detection; (4) new rows force `draft` on create; (5) matched rows only expose `update` or `skip` decisions and do not update title/subject/grade; (6) result UI still contains four outcome categories and a return-to-course-center CTA; and (7) no import surface or action introduced `eval(`, `dangerouslySetInnerHTML`, or direct `@/db` imports in page/surface code. Pair the static verifier with focused DAL/action tests so Phase 15 can be checked before shipping or after refactors.</action>
  <acceptance_criteria>
    - `package.json` contains `verify:phase15`.
    - `scripts/verify-phase15-course-import.ts` contains checks for `courseImportBatch`, `courseImportRow`, `title + subject + grade`, and forced `draft` creation.
    - `scripts/verify-phase15-course-import.ts` contains guards against `eval(`, `dangerouslySetInnerHTML`, and direct `@/db` imports in import page or surface sources.
    - `src/actions/course-import-actions.test.ts` and `src/lib/dal/course-import.test.ts` are referenced by the focused regression command.
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase15</automated>
  </verify>
  <done>Phase 15 ends with an executable regression command that protects the CSV contract, duplicate semantics, review/apply boundary, and result UX from future drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| course-center UI -> import workflow | Teacher-facing navigation must preserve the intended review-first mental model |
| repository sources -> phase verifier | Static and focused test checks decide whether Phase 15's safety and UX guarantees still hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | I | `src/components/surfaces/course-import-review-surface.tsx` | mitigate | Keep all four result categories and row-level reasons visible so skipped or failed rows cannot disappear behind aggregate success copy. |
| T-15-10 | D | `src/components/surfaces/teacher-course-center-surface.tsx` | mitigate | Make import entry and template download discoverable from the course-center hero instead of burying them in hidden local controls. |
| T-15-11 | R | `scripts/verify-phase15-course-import.ts` | mitigate | Add deterministic source checks plus focused tests for the CSV contract, duplicate semantics, and result UI loop. |
| T-15-12 | E | `scripts/verify-phase15-course-import.ts` | mitigate | Fail verification when import pages or surfaces bypass DAL boundaries via direct DB imports or unsafe rendering shortcuts. |
</threat_model>

<verification>
- `pnpm test --run src/components/surfaces/teacher-course-center-surface.test.tsx src/components/surfaces/course-import-review-surface.test.tsx`
- `pnpm verify:phase15`
</verification>

<success_criteria>
- Course import is discoverable from `/teacher/courses` and returns there after results are reviewed.
- Teachers can inspect created, updated, skipped, and failed rows with explicit reasons in-product.
- Phase 15 has a dedicated automated verification command guarding its core import semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/15-batch-course-import/15-03-SUMMARY.md`
</output>
