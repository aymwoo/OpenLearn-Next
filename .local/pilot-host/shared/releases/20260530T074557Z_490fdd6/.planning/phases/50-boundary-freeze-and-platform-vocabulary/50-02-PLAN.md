---
phase: 50-boundary-freeze-and-platform-vocabulary
plan: "02"
type: execute
wave: 2
depends_on:
  - "50-01"
files_modified:
  - src/features/platform-core/contracts.ts
  - src/features/platform-core/index.ts
  - src/actions/plugin-actions.ts
  - src/lib/dal/plugins.ts
  - src/server/plugins/registry.ts
  - src/features/runtime-platform/seams/event-bus/contract.ts
autonomous: true
requirements:
  - BOUND-01
  - BOUND-02
  - BOUND-03
must_haves:
  truths:
    - "代码库中出现最小 platform-core authority anchor，后续 phase 有明确落点可引用。"
    - "现有 mutation producer / DAL / registry / runtime seam 文件被明确标注为各自冻结后的 posture。"
    - "这些锚点不会引入 Command Bus、Action Registry、Event Outbox 的真实实现。"
  artifacts:
    - path: src/features/platform-core/contracts.ts
      provides: contract-only authority anchor
    - path: src/features/platform-core/index.ts
      provides: barrel export for future platform-core imports
    - path: src/actions/plugin-actions.ts
      provides: future PlatformCommand producer adapter note
    - path: src/lib/dal/plugins.ts
      provides: plugin domain DAL-only note
    - path: src/server/plugins/registry.ts
      provides: static implementation catalog-only note
    - path: src/features/runtime-platform/seams/event-bus/contract.ts
      provides: runtime-only transport note
  key_links:
    - from: src/features/platform-core/contracts.ts
      to: .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-OWNERSHIP-MAP.md
      via: authority area naming parity
    - from: legacy seam comments
      to: 50-BOUNDARY-CONTRACT.md
      via: frozen vocabulary reuse
---

<objective>
在不引入平台实现的前提下，给 `platform-core` 和最易误读的 legacy seams 加上最小代码锚点与 posture 注释。

Purpose: 让 BOUND-02 不只停留在文档层；后续实现 phase 可以直接引用代码落点，但不会因为 Phase 50 提前启动真实平台机制。
Output: contract-only `platform-core` anchor + 4 个 legacy seam ownership notes。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-boundary-freeze-and-platform-vocabulary/50-BOUNDARY-CONTRACT.md
@.planning/phases/50-boundary-freeze-and-platform-vocabulary/50-OWNERSHIP-MAP.md
@.planning/phases/50-boundary-freeze-and-platform-vocabulary/50-RESEARCH.md
@.planning/phases/50-boundary-freeze-and-platform-vocabulary/50-PATTERNS.md
@src/actions/plugin-actions.ts
@src/lib/dal/plugins.ts
@src/server/plugins/registry.ts
@src/features/runtime-platform/seams/event-bus/contract.ts
@src/db/schema.ts
</context>

<must_haves>
  <truths>
    <truth>`platform-core` 目录里存在权威 anchor，但没有任何 dispatch/outbox/lifecycle 执行逻辑。</truth>
    <truth>旧入口文件都带有 Phase 50 posture 注释，后续实现者不能再误把它们当 authority。</truth>
  </truths>
  <artifacts>
    <artifact path="src/features/platform-core/contracts.ts" />
    <artifact path="src/features/platform-core/index.ts" />
    <artifact path="src/actions/plugin-actions.ts" />
    <artifact path="src/lib/dal/plugins.ts" />
    <artifact path="src/server/plugins/registry.ts" />
    <artifact path="src/features/runtime-platform/seams/event-bus/contract.ts" />
  </artifacts>
  <key_links>
    <link>`contracts.ts` 的 authority area 名称必须与 `50-OWNERSHIP-MAP.md` 一致。</link>
    <link>legacy seam 注释必须直接复用 future PlatformCommand producer / DAL only / catalog only / runtime-only 这些冻结术语。</link>
  </key_links>
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: 创建 contract-only platform-core authority anchor</name>
  <files>src/features/platform-core/contracts.ts, src/features/platform-core/index.ts</files>
  <read_first>
    - .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-BOUNDARY-CONTRACT.md
    - .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-OWNERSHIP-MAP.md
    - .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-PATTERNS.md
    - src/features/runtime-platform/seams/event-bus/contract.ts
  </read_first>
  <action>创建 `src/features/platform-core/contracts.ts` 与 `src/features/platform-core/index.ts`。`contracts.ts` 只允许包含 contract exports、literal notes、schema/type definitions，不允许写 `dispatch()`、handler registry、outbox write、lifecycle orchestration。至少导出以下符号：`PlatformAuthorityAreaSchema`、`PlatformLegacySeamPostureSchema`、`PLATFORM_CORE_AUTHORITY_NOTES`。`PlatformAuthorityAreaSchema` 必须覆盖 `command_execution`、`action_registry`、`plugin_lifecycle_orchestration`、`platform_event_outbox`；`PlatformLegacySeamPostureSchema` 必须覆盖 `future_command_producer_adapter`、`plugin_domain_dal`、`static_implementation_catalog`、`runtime_transport_only`。`PLATFORM_CORE_AUTHORITY_NOTES` 必须包含精确字符串 `Phase 50 boundary freeze: platform-core is the authoritative orchestration layer for v3.0 phase 1.`。`index.ts` 只做 barrel export；不要在这个任务里借用 Task 2 的验证来证明 anti-implementation posture，必须在本任务自证 `contracts.ts` 没有实现级符号。</action>
  <acceptance_criteria>
    - `src/features/platform-core/contracts.ts` 存在。
    - 文件内出现导出名 `PlatformAuthorityAreaSchema` 与 `PlatformLegacySeamPostureSchema`。
    - 文件内出现精确字符串 `Phase 50 boundary freeze: platform-core is the authoritative orchestration layer for v3.0 phase 1.`。
    - `src/features/platform-core/index.ts` 存在且导出 `./contracts`。
  </acceptance_criteria>
  <verify>
    <automated>rg -n '^(export const PlatformAuthorityAreaSchema|export const PlatformLegacySeamPostureSchema|export const PLATFORM_CORE_AUTHORITY_NOTES|.*Phase 50 boundary freeze: platform-core is the authoritative orchestration layer for v3.0 phase 1\.|export \* from "\./contracts";)$' "src/features/platform-core/contracts.ts" "src/features/platform-core/index.ts" && ! rg -n 'dispatch\(|register.*handler|platformEventOutbox|runtimeEventOutbox|pluginLifecycleOrchestrat|queue\.add\(' "src/features/platform-core/contracts.ts"</automated>
  </verify>
  <done>`platform-core` 获得最小 authority anchor，后续 phase 有稳定 import / comment 落点，但还没有任何 bus/registry/outbox 运行时实现。</done>
</task>

<task type="auto">
  <name>Task 2: 给 legacy seams 加上冻结后的 ownership posture 注释</name>
  <files>src/actions/plugin-actions.ts, src/lib/dal/plugins.ts, src/server/plugins/registry.ts, src/features/runtime-platform/seams/event-bus/contract.ts</files>
  <read_first>
    - .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-BOUNDARY-CONTRACT.md
    - .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-OWNERSHIP-MAP.md
    - src/actions/plugin-actions.ts
    - src/lib/dal/plugins.ts
    - src/server/plugins/registry.ts
    - src/features/runtime-platform/seams/event-bus/contract.ts
  </read_first>
  <action>只添加 ownership posture comments，不改运行逻辑。`src/actions/plugin-actions.ts` 必须加入精确注释 `Phase 50 boundary freeze: Server Actions are future PlatformCommand producer adapters; keep updateTag() at the entrypoint.`（per D-50-05/D-50-06）。`src/lib/dal/plugins.ts` 必须加入精确注释 `Phase 50 boundary freeze: plugin domain DAL only; not platform orchestration authority.`（per D-50-02）。`src/server/plugins/registry.ts` 必须加入精确注释 `Phase 50 boundary freeze: static implementation catalog only; not dynamic action authority.`（per D-50-03）。`src/features/runtime-platform/seams/event-bus/contract.ts` 必须加入精确注释 `Phase 50 boundary freeze: runtime-only transport seam; not platform event truth.`（per D-50-04/D-50-12）。这些注释必须作为单独新增的 comment lines，放在模块级 ownership / export 附近；不要重排 imports、不要改函数签名、不要改 schema shape、不要改业务分支、不要改运行时行为。`src/db/schema.ts` 的 `runtimeEventOutbox` durable-anchor posture 已在 `50-OWNERSHIP-MAP.md` 冻结，本任务不得顺手改动 schema file 或表结构，只负责让 runtime seam 注释与该 companion doc 保持一致。</action>
  <acceptance_criteria>
    - 四个目标文件都保留原有导出与运行逻辑。
    - 四个目标文件都出现上述精确 Phase 50 posture 注释。
    - 对四个目标文件的 diff 仅包含新增的精确注释行；没有删除行，也没有额外的非注释代码变更。
  </acceptance_criteria>
  <verify>
    <automated>node -e "const {execFileSync}=require('node:child_process'); const files=['src/actions/plugin-actions.ts','src/lib/dal/plugins.ts','src/server/plugins/registry.ts','src/features/runtime-platform/seams/event-bus/contract.ts']; const allowed=[ 'Phase 50 boundary freeze: Server Actions are future PlatformCommand producer adapters; keep updateTag() at the entrypoint.', 'Phase 50 boundary freeze: plugin domain DAL only; not platform orchestration authority.', 'Phase 50 boundary freeze: static implementation catalog only; not dynamic action authority.', 'Phase 50 boundary freeze: runtime-only transport seam; not platform event truth.' ]; const diff=execFileSync('git',['diff','--unified=0','HEAD','--',...files],{encoding:'utf8'}); const delta=diff.split('\n').filter((line)=>/^[+-]/.test(line)&&!/^\+\+\+|^---/.test(line)); const bad=delta.filter((line)=> line.startsWith('-') || (line.startsWith('+') && !allowed.includes(line.slice(1).trim()))); const missing=allowed.filter((line)=> !diff.includes('+'+line)); if (bad.length || missing.length) { if (bad.length) console.error('Unexpected diff lines:\n'+bad.join('\n')); if (missing.length) console.error('Missing comment lines:\n'+missing.join('\n')); process.exit(1); }"</automated>
  </verify>
  <done>代码侧已经留下 authority / adapter posture 锚点；后续 phase 不能再把这些 legacy seams 当成未定义的可扩张 authority。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs contract → code anchors | 若代码层没有对应 posture 注释，后续实现者仍可能忽略文档继续扩展旧 seam |
| platform-core anchor → later implementation | 若 anchor 内偷放执行逻辑，会提前越界到 Phase 51-53 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-04 | T | `src/features/platform-core/contracts.ts` | mitigate | 只允许 schema/type/note exports；任务与验收明确禁止 `dispatch()`、outbox write、lifecycle orchestration。 |
| T-50-05 | E | legacy mutation seams | mitigate | 在 `plugin-actions.ts`、`dal/plugins.ts`、`registry.ts` 中写入精确 posture 注释，防止旧 seam 继续越权扩张。 |
| T-50-06 | R | runtime event seam | mitigate | 在 `event-bus/contract.ts` 明确 `runtime-only transport seam; not platform event truth.`，避免后续把 runtimeEventOutbox 当平台事实层。 |
</threat_model>

<verification>
运行两条 `rg` 命令：第一条确认所有 authority/posture 注释和导出存在；第二条确认没有在本 plan 的目标文件中引入平台实现级关键字。</verification>

<success_criteria>
- `platform-core` 有 contract-only anchor，可被 Phase 51-54 直接引用。
- 旧入口文件的 ownership posture 被写入代码，而不是只留在 Phase 50 文档里。
- 该计划不引入真实 Command Bus、Action Registry、Platform Event Outbox 或 lifecycle orchestrator 实现。
</success_criteria>

<output>
After completion, create `.planning/phases/50-boundary-freeze-and-platform-vocabulary/50-02-SUMMARY.md`
</output>
