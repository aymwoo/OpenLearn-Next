---
phase: 59-deploy-release-recovery-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - src/lib/ops/env.server.ts
  - src/lib/ops/env.server.test.ts
autonomous: true
requirements:
  - ENVR-01
must_haves:
  truths:
    - "D-59-05: web 与 worker 在启动前会对必需 server env 做 fail-fast 校验，`.env.example` 与正式 env schema 成为唯一 checked-in baseline。"
    - "D-59-03: BullMQ Redis 缺失会阻断 worker-ready，而 fanout Redis 仍保持 optional posture，不把两条 Redis 语义混成单一 readiness 开关。"
  artifacts:
    - path: ".env.example"
      provides: "single-school pilot env template"
      contains: "OPENLEARN_DEPLOY_ENV=pilot-single-school"
    - path: "src/lib/ops/env.server.ts"
      provides: "Zod-backed server env parser"
      exports: ["ServerEnvSchema", "getServerEnv"]
    - path: "src/lib/ops/env.server.test.ts"
      provides: "env contract regression coverage"
      contains: "describe(\"env.server\""
  key_links:
    - from: ".env.example"
      to: "src/lib/ops/env.server.ts"
      via: "same required key set"
      pattern: "DB_FILE_NAME|BULLMQ_REDIS_URL|REDIS_URL|OPENLEARN_RUNTIME_ASSETS_ROOT"
---

<objective>
Lock the pilot environment contract into code and a checked-in template so Phase 59 stops relying on scattered `process.env` conventions.

Purpose: satisfy ENVR-01 and implement D-59-03 / D-59-05 so later release/restore work has one authoritative env surface.
Output: `.env.example`, `src/lib/ops/env.server.ts`, focused env contract tests.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/59-deploy-release-recovery-baseline/59-CONTEXT.md
@.planning/phases/59-deploy-release-recovery-baseline/59-RESEARCH.md
@.planning/phases/59-deploy-release-recovery-baseline/59-PATTERNS.md
@package.json
@server.ts
@src/db/index.ts
@src/features/async-tasks/infra/connection.ts
@src/features/runtime-platform/seams/transport/redis-fanout-connection.ts

<interfaces>
From `server.ts`:

```ts
const hostname = process.env.HOSTNAME ?? "0.0.0.0";
const port = Number(process.env.PORT ?? 3000);
```

From `src/db/index.ts`:

```ts
const client = createClient({
  url: process.env.DB_FILE_NAME || "file:local.db",
});
```

From `src/features/async-tasks/infra/connection.ts`:

```ts
const redisUrl = process.env.BULLMQ_REDIS_URL?.trim() || null;
const asyncTasksEnabled = process.env.ASYNC_TASKS_ENABLED === "true" && Boolean(redisUrl);
```

From `src/features/runtime-platform/seams/transport/redis-fanout-connection.ts`:

```ts
const redisUrl = process.env.REDIS_URL?.trim() || null;
const deployAllowsRedis = process.env.REDIS_FANOUT_ENABLED === "true" && Boolean(redisUrl);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 固化单校试点 env schema 与 `.env.example`</name>
  <read_first>
    .planning/phases/59-deploy-release-recovery-baseline/59-CONTEXT.md
    .planning/phases/59-deploy-release-recovery-baseline/59-RESEARCH.md
    server.ts
    src/db/index.ts
    src/features/async-tasks/infra/connection.ts
    src/features/runtime-platform/seams/transport/redis-fanout-connection.ts
    src/lib/ops/env.server.ts
    .env.example
  </read_first>
  <files>.env.example, src/lib/ops/env.server.ts</files>
  <action>创建 `src/lib/ops/env.server.ts`，用 `zod` 实现单一 server-owned env 解析器，覆盖 `NODE_ENV`、`HOSTNAME`、`PORT`、`DB_FILE_NAME`、`AUTH_SECRET`、`NEXT_SERVER_ACTIONS_ENCRYPTION_KEY`、`ASYNC_TASKS_ENABLED`、`BULLMQ_REDIS_URL`、`BULLMQ_PREFIX`、`REDIS_FANOUT_ENABLED`、`REDIS_URL`、`RUNTIME_INSTANCE_ID`、`WORKER_INSTANCE_ID`、`OPENLEARN_DEPLOY_ENV`、`OPENLEARN_SHARED_ROOT`、`OPENLEARN_CURRENT_ROOT`、`OPENLEARN_RUNTIME_ASSETS_ROOT`、`OPENLEARN_RELEASE_MANIFESTS_DIR`、`OPENLEARN_HEALTHCHECK_BASE_URL`。按 D-59-03 与 D-59-05 保持 BullMQ Redis 为 blocking 依赖、fanout Redis 为 optional posture；禁止把 `REDIS_URL` 与 `BULLMQ_REDIS_URL` 合并成单一 readiness 开关。同步新增 `.env.example`，给出 `OPENLEARN_DEPLOY_ENV=pilot-single-school`、`DB_FILE_NAME=file:local.db`、`ASYNC_TASKS_ENABLED=true`、`BULLMQ_REDIS_URL=redis://127.0.0.1:6379`、`REDIS_FANOUT_ENABLED=false`、`REDIS_URL=redis://127.0.0.1:6379`、`OPENLEARN_RUNTIME_ASSETS_ROOT=./runtime-assets`、`OPENLEARN_RELEASE_MANIFESTS_DIR=./ops/releases/manifests` 等示例值；真实 secrets 只保留占位符，不把任何现有 secret 写入仓库。</action>
  <acceptance_criteria>
    - `src/lib/ops/env.server.ts` 导出 `ServerEnvSchema` 与 `getServerEnv`。
    - `.env.example` 同时包含 `BULLMQ_REDIS_URL=` 与 `REDIS_URL=`，且包含 `OPENLEARN_RUNTIME_ASSETS_ROOT=` 与 `OPENLEARN_RELEASE_MANIFESTS_DIR=`。
    - `src/lib/ops/env.server.ts` 中存在对 `ASYNC_TASKS_ENABLED`、`BULLMQ_REDIS_URL`、`REDIS_FANOUT_ENABLED`、`REDIS_URL` 的显式区分，未出现“单个 REDIS 开关覆盖两条语义”的实现。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/ops/env.server.test.ts</automated>
  </verify>
  <done>仓库有一个可复用的 env parser，`.env.example` 能作为 Phase 59 后续 deploy/release/restore 的唯一 checked-in 模板。</done>
</task>

<task type="auto">
  <name>Task 2: 为 env contract 加 focused regression tests</name>
  <read_first>
    src/lib/ops/env.server.ts
    src/lib/ops/env.server.test.ts
    .planning/phases/59-deploy-release-recovery-baseline/59-VALIDATION.md
  </read_first>
  <files>src/lib/ops/env.server.test.ts</files>
  <action>新增 `src/lib/ops/env.server.test.ts`，覆盖三类行为：1) 缺少 `DB_FILE_NAME`、`AUTH_SECRET`、`NEXT_SERVER_ACTIONS_ENCRYPTION_KEY` 时 fail-fast；2) `ASYNC_TASKS_ENABLED=true` 且缺失 `BULLMQ_REDIS_URL` 时 worker posture 不能被视为 ready，落实 D-59-03；3) `REDIS_FANOUT_ENABLED=false` 或 `REDIS_URL` 缺失时仍保留 optional fanout posture，不把 fanout 误升为 blocker。测试中直接断言 schema parse 结果与 helper 输出，不 mock Zod。</action>
  <acceptance_criteria>
    - 测试文件包含“missing required env fails fast”“bullmq redis is blocking”“fanout redis stays optional”三组 case。
    - 断言中同时出现 `BULLMQ_REDIS_URL` 与 `REDIS_URL`，避免语义混用回归。
    - `pnpm test --run src/lib/ops/env.server.test.ts` 绿灯。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/ops/env.server.test.ts</automated>
  </verify>
  <done>ENVR-01 的 env contract 具备自动化回归保护，后续 plan 可直接依赖该模块。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy operator → env parser | 未校验的环境变量会在进程启动时跨入 trusted runtime。 |
| checked-in template → real pilot secrets | `.env.example` 必须描述键集合，但不能泄漏真实 secrets。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Tampering | `src/lib/ops/env.server.ts` | mitigate | 用 Zod 对所有 required env 做 fail-fast parse，阻止非法值进入 runtime；worker Redis 与 fanout Redis 分开建模。 |
| T-59-02 | Information Disclosure | `.env.example` | mitigate | 模板仅保留占位符与非敏感示例值；禁止写入真实 `AUTH_SECRET`、`NEXT_SERVER_ACTIONS_ENCRYPTION_KEY` 或外部 DSN。 |
</threat_model>

<verification>
- `pnpm test --run src/lib/ops/env.server.test.ts`
</verification>

<success_criteria>
- `src/lib/ops/env.server.ts` 成为后续 routes/scripts 的唯一 env 入口。
- `.env.example` 覆盖 Phase 59 deploy/release/restore 需要的全部键名。
- BullMQ blocking posture 与 fanout optional posture 被明确拆开，不再依赖隐式 `process.env` 约定。
</success_criteria>

<output>
After completion, create `.planning/phases/59-deploy-release-recovery-baseline/59-01-SUMMARY.md`
</output>
