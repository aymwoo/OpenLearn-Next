---
phase: 36-websocket-classroom-transport-cutover
plan: 04
type: execute
wave: 4
depends_on: ["36-02", "36-03"]
files_modified:
  - src/features/runtime-platform/seams/transport/ws-auth.test.ts
  - src/features/runtime-platform/seams/transport/ws-envelope.test.ts
  - src/features/runtime-platform/seams/transport/ws-adapter.test.ts
  - src/features/runtime-platform/seams/transport/gateway.test.ts
  - src/features/runtime-platform/seams/transport/ws-server.test.ts
  - src/components/classroom/classroom-control-panel.test.tsx
  - src/components/classroom/classroom-live-snapshot-refresh.test.tsx
  - src/components/learning/classroom-runtime-client.test.tsx
  - scripts/verify-phase36-websocket-cutover.ts
  - package.json
autonomous: true
gap_closure: true
requirements: [RTPX-03]
must_haves:
  truths:
    - "teacher、student 与 runtime host 能通过统一鉴权后的 websocket channel 建立双向通信，并由 focused verification 证明 route auth、message validation 与 rollback posture 成立。"
    - "`teacher.control`、`runtime.command`、`classroom.snapshot`、`runtime.event` 与 `transport.keepalive` 在同一合法 envelope contract 内流动，并保持 canonical actor/session scope。"
    - "当 websocket 不可用时，classroom/player 仍能退回 SSE rollback surface，并保留 locked/unlocked、snapshot recovery、error feedback；`verify:phase36` 必须对协议漂移、伪 schema 字段与 rollback 前置条件给出诚实结论。"
  artifacts:
    - path: "src/features/runtime-platform/seams/transport/ws-auth.test.ts"
      provides: "真实 schema 的 handshake auth focused coverage"
    - path: "src/features/runtime-platform/seams/transport/ws-server.test.ts"
      provides: "client message validation 与 teacher.control/runtime.command routing coverage"
    - path: "src/components/classroom/classroom-control-panel.test.tsx"
      provides: "teacher.control / runtime.command producer cutover coverage"
    - path: "src/components/learning/classroom-runtime-client.test.tsx"
      provides: "player/runtime consumer reconnect + durable snapshot parity coverage"
    - path: "scripts/verify-phase36-websocket-cutover.ts"
      provides: "Phase 36 canonical verifier with static guards, focused suites, and typecheck"
  key_links:
    - from: "scripts/verify-phase36-websocket-cutover.ts"
      to: "src/features/runtime-platform/seams/transport/ws-auth.test.ts"
      via: "focused route auth suite"
      pattern: "ws-auth.test.ts"
    - from: "scripts/verify-phase36-websocket-cutover.ts"
      to: "src/features/runtime-platform/seams/transport/ws-server.test.ts"
      via: "message validation and canonical routing suite"
      pattern: "ws-server.test.ts"
    - from: "scripts/verify-phase36-websocket-cutover.ts"
      to: "src/components/classroom/classroom-control-panel.test.tsx"
      via: "producer cutover suite"
      pattern: "classroom-control-panel.test.tsx"
    - from: "scripts/verify-phase36-websocket-cutover.ts"
      to: "src/components/learning/classroom-runtime-client.test.tsx"
      via: "snapshot parity / rollback suite"
      pattern: "classroom-runtime-client.test.tsx"
    - from: "scripts/verify-phase36-websocket-cutover.ts"
      to: "pnpm typecheck"
      via: "final compile gate"
      pattern: "typecheck"
---

<objective>
为 Phase 36 发布可执行的 focused verification 面：证明 route auth、message
validation、teacher control producer、runtime command producer、snapshot parity 与 SSE rollback
前置条件都被真实代码覆盖，并把 Phase 36 的结论固定在 verifier + tests 上。

Purpose: gap closure 不能只修代码不修证据；如果 verifier 继续停留在伪 schema
fixture、宽松断言或只看 prose，RTPX-03 仍然不具备可信完成条件。
Output: focused transport/consumer tests、`verify:phase36` 脚本、honest rollback
posture。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-websocket-classroom-transport-cutover/36-VERIFICATION.md
@.planning/phases/36-websocket-classroom-transport-cutover/36-REVIEW.md
@.planning/phases/36-websocket-classroom-transport-cutover/36-02-PLAN.md
@.planning/phases/36-websocket-classroom-transport-cutover/36-03-PLAN.md
@src/features/runtime-platform/seams/transport/ws-auth.test.ts
@src/features/runtime-platform/seams/transport/gateway.test.ts
@src/components/classroom/classroom-live-snapshot-refresh.test.tsx
@scripts/verify-phase31-transport-inspector.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 补齐 route auth、message validation、producer rollback、canonical metadata 与 consumer parity 的 focused suites</name>
  <read_first>
    - src/features/runtime-platform/seams/transport/ws-auth.test.ts
    - src/features/runtime-platform/seams/transport/ws-envelope.test.ts
    - src/features/runtime-platform/seams/transport/gateway.test.ts
    - src/components/classroom/classroom-control-panel.test.tsx
    - src/components/classroom/classroom-live-snapshot-refresh.test.tsx
    - src/features/runtime-platform/seams/transport/ws-auth.ts
    - src/features/runtime-platform/seams/transport/ws-server.ts
    - src/features/runtime-platform/seams/transport/ws-adapter.ts
    - src/components/learning/classroom-runtime-client.tsx
  </read_first>
  <files>src/features/runtime-platform/seams/transport/ws-auth.test.ts, src/features/runtime-platform/seams/transport/ws-envelope.test.ts, src/features/runtime-platform/seams/transport/ws-adapter.test.ts, src/features/runtime-platform/seams/transport/gateway.test.ts, src/features/runtime-platform/seams/transport/ws-server.test.ts, src/components/classroom/classroom-control-panel.test.tsx, src/components/classroom/classroom-live-snapshot-refresh.test.tsx, src/components/learning/classroom-runtime-client.test.tsx</files>
  <behavior>
    - Test 1: `ws-auth.test.ts` 使用真实 schema 字段与 raw upgrade request 形状，覆盖 teacher success、student success、scope mismatch、unauthorized。
    - Test 2: `ws-server.test.ts` 覆盖 invalid payload -> `transport.error`、`transport.keepalive` ack、`teacher.control` -> `applyWebSocketTeacherControlForActor()`、`runtime.command` -> `recordTeacherControlEvent()`。
    - Test 3: `classroom-control-panel.test.tsx` 覆盖 `teacher.control` websocket send、`transport.error` -> server action fallback、`runtime.command` websocket send，以及 `recordRuntimeTeacherControlAction()` rollback。
    - Test 4: `ws-adapter.test.ts` 与 `gateway.test.ts` 覆盖 canonical kind 保留，以及 supplemental websocket reject 的 trace 可观测性。
    - Test 5: `classroom-live-snapshot-refresh.test.tsx` 与 `classroom-runtime-client.test.tsx` 覆盖 WS-first、EventSource fallback、presence update、durable snapshot parity、manual reconnect。
  </behavior>
  <action>围绕 36-VERIFICATION 的 blocker 增测：1) 重写 `ws-auth.test.ts` fixture，使用 `classMembers.userId`、`classMembers.role` 与 active `memberships.status`，并显式模拟 custom Node upgrade request/cookie；2) 新增 `ws-server.test.ts`，直接验证 `handleClassroomWebSocketClientMessage` 或等价导出 helper 对 `transport.keepalive`、`teacher.control`、`runtime.command`、invalid payload 的处理，并锁定它调用 `applyWebSocketTeacherControlForActor()` 与 `recordTeacherControlEvent()`；3) 新增或扩展 `classroom-control-panel.test.tsx`，锁定 teacher 点击步骤/模式/slide 后发送 `teacher.control` envelope、收到 `transport.error` 后 fallback 到 `changeClassroomStepAction()` / `changeClassroomModeAction()` / `changeClassroomSlideAction()`，以及 teacher runtime stage 的最小 runtime control 入口会发送 `runtime.command`，失败时 fallback 到 `recordRuntimeTeacherControlAction()`；4) 新增 `ws-adapter.test.ts`，锁定 outbound envelope 保留 `parsed.kind`、`correlationId`、actor scope，不再压扁为 `classroom.snapshot`；5) 扩展 `gateway.test.ts`，验证 websocket supplemental reject 时会记录 `traceType: "stream_failed"`；6) 扩展 `classroom-live-snapshot-refresh.test.tsx` 为 `MockWebSocket + MockEventSource`，验证 ws open 优先、socket error 后 fallback；7) 扩展 `classroom-runtime-client.test.tsx`，验证 `touchClassroomPresenceAction` 在 `socket.onopen` 被调用、runtime reconnect 时保留 `snapshot_fallback` copy，并通过 durable snapshot fetch 回到 connected。不要写只断言 mock 被调用过一次的浅测试；必须锁定 exact kind、traceType、connectionState、route path 或 fallback string。</action>
  <acceptance_criteria>
    - `src/features/runtime-platform/seams/transport/ws-auth.test.ts` 不再出现 `studentId` 或 `status` 作为 `classMembers` fixture 字段。
    - `src/features/runtime-platform/seams/transport/ws-server.test.ts` 出现精确字符串 `WEBSOCKET_PAYLOAD_INVALID`、`transport.keepalive`、`applyWebSocketTeacherControlForActor` 与 `recordTeacherControlEvent`。
    - `src/components/classroom/classroom-control-panel.test.tsx` 出现精确字符串 `teacher.control`、`runtime.command`、`recordRuntimeTeacherControlAction` 与 `transport.error`。
    - `src/features/runtime-platform/seams/transport/ws-adapter.test.ts` 断言 outbound envelope 包含原始 `kind`，不是固定 `classroom.snapshot`。
    - `src/components/learning/classroom-runtime-client.test.tsx` 出现 `touchClassroomPresenceAction`、`snapshot_fallback` 与 `/api/classroom/session-1/snapshot`。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/features/runtime-platform/seams/transport/ws-auth.test.ts src/features/runtime-platform/seams/transport/ws-envelope.test.ts src/features/runtime-platform/seams/transport/ws-adapter.test.ts src/features/runtime-platform/seams/transport/ws-server.test.ts src/features/runtime-platform/seams/transport/gateway.test.ts src/components/classroom/classroom-control-panel.test.tsx src/components/classroom/classroom-live-snapshot-refresh.test.tsx src/components/learning/classroom-runtime-client.test.tsx</automated>
  </verify>
  <done>Phase 36 的 focused suites 已真实覆盖 route auth、message validation、producer rollback、teacher control/runtime command metadata 与 classroom/player parity。</done>
</task>

<task type="auto">
  <name>Task 2: 发布 `verify:phase36`，把 static guard、focused suites 与 typecheck 固定为 canonical gate</name>
  <read_first>
    - scripts/verify-phase31-transport-inspector.ts
    - .planning/phases/36-websocket-classroom-transport-cutover/36-VERIFICATION.md
    - .planning/phases/36-websocket-classroom-transport-cutover/36-REVIEW.md
    - package.json
    - src/features/runtime-platform/seams/transport/ws-auth.ts
    - src/features/runtime-platform/seams/transport/ws-server.ts
    - src/features/runtime-platform/seams/transport/gateway.ts
    - src/components/classroom/classroom-live-snapshot-refresh.tsx
    - src/components/learning/classroom-runtime-client.tsx
  </read_first>
  <files>scripts/verify-phase36-websocket-cutover.ts, package.json</files>
  <action>新增 `scripts/verify-phase36-websocket-cutover.ts` 与 `package.json` 脚本 `verify:phase36`。脚本必须按以下顺序执行：1) static guards：用非注释源码检查禁止 `classMembers.studentId`、`classMembers.status`、`classroomSessions.schoolId`、`transport.ping`、`presence.update`、`classroom.keepalive` 等漂移 token；同时确认 `route.ts` 仍返回 426 + `rollbackSurface`，并确认 `classroom-control-panel.tsx` 含 `teacher.control` / `runtime.command` producer 与 `recordRuntimeTeacherControlAction()` rollback path；2) focused suites：运行 Task 1 列出的 transport/producer/consumer tests；3) `pnpm typecheck`；4) 输出 honest rollback posture，明确 `SSE remains rollback surface` 与 `Redis fanout is out of scope for Phase 36`。脚本里不要用会误计注释的 grep gate；参考 Phase 24/31 verifier 的 `nonCommentIncludes` / `run()` 结构。不要把 Phase 37/38 的 Redis、多实例、运维 close 文档塞进这个 verifier。</action>
  <acceptance_criteria>
    - `package.json` 出现精确字符串 `"verify:phase36"`。
    - `scripts/verify-phase36-websocket-cutover.ts` 同时包含 `pnpm typecheck`、`ws-server.test.ts`、`classroom-control-panel.test.tsx`、`classroom-runtime-client.test.tsx`。
    - verifier 静态 guard 中明确检查 `transport.ping`、`presence.update`、`classMembers.studentId` 为 forbidden token。
    - verifier 输出文案中包含 `SSE rollback surface` 或等价明确说明，且包含 `Redis` 为 out-of-scope/future phase 的 honest note。
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase36</automated>
  </verify>
  <done>Phase 36 有单一 canonical gate，后续是否关闭 RTPX-03 将由 verifier 结果决定，而不是靠 summary 叙述。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier static guard → source files | 守卫若只匹配注释或错误 token，会给出假阳性 |
| focused tests → transport contract | mock fixture 若脱离真实 schema，会放过真实回归 |
| rollback proof → milestone claim | 若 verifier 不显式声明 SSE rollback posture，可能误报 cutover 已完全关闭 fallback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04-01 | T | `verify-phase36-websocket-cutover.ts` | mitigate | 采用 non-comment source guards，禁止靠注释命中 `classMembers.studentId`、`transport.ping` 等 token |
| T-36-04-02 | R | focused tests | mitigate | fixture 必须贴合真实 `memberships` / `classMembers` / `classroomSessions` 字段，并把 `pnpm typecheck` 纳入最终 gate |
| T-36-04-03 | I | rollback posture | mitigate | verifier 输出显式写明 SSE 是 rollback surface，Redis/multi-instance 不在 Phase 36 完成条件内 |
</threat_model>

<verification>
- `pnpm test --run src/features/runtime-platform/seams/transport/ws-auth.test.ts src/features/runtime-platform/seams/transport/ws-envelope.test.ts src/features/runtime-platform/seams/transport/ws-adapter.test.ts src/features/runtime-platform/seams/transport/ws-server.test.ts src/features/runtime-platform/seams/transport/gateway.test.ts src/components/classroom/classroom-control-panel.test.tsx src/components/classroom/classroom-live-snapshot-refresh.test.tsx src/components/learning/classroom-runtime-client.test.tsx`
- `pnpm verify:phase36`
</verification>

<success_criteria>
- Phase 36 的 focused verification 能诚实证明 route auth、message validation、teacher control producer、runtime command producer、snapshot parity 与 SSE rollback prerequisites。
- `pnpm verify:phase36` 成为 Phase 36 唯一外部 gate；没有新的 prose-only close claim。
- 计划范围保持在 RTPX-03，不扩展到 Redis fanout、多实例、PostgreSQL、第二 runtime 或 milestone close 文档。
</success_criteria>

<output>
After completion, create `.planning/phases/36-websocket-classroom-transport-cutover/36-04-SUMMARY.md`
</output>
