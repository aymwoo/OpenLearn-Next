---
phase: 60.1-close-gap-pilot-03-load-01-load-02-replace-dry-run-phase60-p
plan: "03"
type: execute
wave: 2
depends_on:
  - 60.1-02
files_modified:
  - package.json
  - scripts/verify-phase60-local.ts
  - scripts/verify-phase60-load-and-rehearsal.ts
  - scripts/verify-phase60-load-and-rehearsal.test.ts
  - .planning/debug/phase60-local-sqlite-busy.md
autonomous: true
requirements:
  - PILOT-03
  - LOAD-01
  - LOAD-02
requirement_scope: support-only
requirements_note: "Support/blocker-removal plan only: isolates the repo-local substitute lane and prevents false local proof, but does not itself generate the live evidence needed to close PILOT-03 / LOAD-01 / LOAD-02."
gap_closure: true
must_haves:
  truths:
    - "This is a blocker-removal plan only; it protects the local substitute lane but does not itself satisfy live closeout requirements."
    - "Repo-local Phase 60 substitute runs against an isolated SQLite proof file instead of the same `local.db` held by long-lived web and worker processes."
    - "The verifier fails closed with an explicit shared-local-db blocker when someone points localhost proof back at `file:local.db`."
    - "Local substitute execution keeps the Phase 60 close semantics honest: same machine-readable evidence contract, dry-run still rejected for closeout, manual transport fallback still manual, and local proof still not equal to final pilot-host signoff."
  artifacts:
    - path: scripts/verify-phase60-local.ts
      provides: "Repo-local wrapper that prepares an isolated Phase 60 proof DB and invokes the canonical verifier"
    - path: scripts/verify-phase60-load-and-rehearsal.ts
      provides: "Shared-local-db preflight blocker plus guidance to the isolated local wrapper"
    - path: package.json
      provides: "Exact `verify:phase60:local` entrypoint for the repo-local substitute lane"
    - path: .planning/debug/phase60-local-sqlite-busy.md
      provides: "Documented resolution and verification commands for the SQLite lock root cause"
  key_links:
    - from: package.json
      to: scripts/verify-phase60-local.ts
      via: exact script entry
      pattern: verify:phase60:local
    - from: scripts/verify-phase60-local.ts
      to: scripts/verify-phase60-load-and-rehearsal.ts
      via: spawned canonical verifier with isolated DB_FILE_NAME
      pattern: verify-phase60-load-and-rehearsal.ts
    - from: scripts/verify-phase60-load-and-rehearsal.ts
      to: .planning/debug/phase60-local-sqlite-busy.md
      via: shared-local-db blocker token documented in debug note
      pattern: PHASE60_LOCAL_DB_SHARED_WITH_APP
---

<objective>
This is a support/blocker-removal plan, not the direct live-evidence closeout plan.

Isolate the repo-local Phase 60 proof lane from the live app's shared `local.db`, and make the canonical verifier reject that unsafe posture before sample smoke starts.

Purpose: Directly close the second half of the debug root cause—verify/web/worker sharing one SQLite file—while keeping D-60.1-01 through D-60.1-05 intact.
Output: isolated local verifier wrapper, shared-local-db preflight, exact package script, and an updated debug resolution note.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60.1-close-gap-pilot-03-load-01-load-02-replace-dry-run-phase60-p/CONTEXT.md
@.planning/phases/60.1-close-gap-pilot-03-load-01-load-02-replace-dry-run-phase60-p/60.1-PATTERNS.md
@.planning/debug/phase60-local-sqlite-busy.md
@.planning/phases/60-load-degrade-pilot-rehearsal/60-04-SUMMARY.md
@package.json
@scripts/verify-phase60-load-and-rehearsal.ts
@scripts/prepare-dev-db.ts

<interfaces>
From `scripts/verify-phase60-load-and-rehearsal.ts`:

```ts
export const PHASE_60_VERIFY_SCRIPT =
  "node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/verify-phase60-load-and-rehearsal.ts";

export function getPhase60MissingLiveEnv(env: NodeJS.ProcessEnv = process.env)
export async function runPhase60Verification()
```

From `scripts/prepare-dev-db.ts`:

```ts
export async function prepareDevDb()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the shared-local-db blocker and local wrapper contract in tests first</name>
  <read_first>
    - scripts/verify-phase60-load-and-rehearsal.ts
    - package.json
    - .planning/debug/phase60-local-sqlite-busy.md
    - .planning/phases/60.1-close-gap-pilot-03-load-01-load-02-replace-dry-run-phase60-p/CONTEXT.md
    - .planning/phases/60.1-close-gap-pilot-03-load-01-load-02-replace-dry-run-phase60-p/60.1-PATTERNS.md
  </read_first>
  <pre_edit_checks>
    - Run `gitnexus_impact({ target: "runPhase60Verification", direction: "upstream" })` and `gitnexus_context({ name: "runPhase60Verification" })` before changing test coverage that locks the verifier contract.
  </pre_edit_checks>
  <files>scripts/verify-phase60-load-and-rehearsal.test.ts</files>
  <behavior>
    - Test 1: localhost proof with `DB_FILE_NAME=file:local.db` throws `PHASE60_LOCAL_DB_SHARED_WITH_APP` before the smoke stage begins.
    - Test 2: the exact package script `verify:phase60:local` points to the new wrapper entry instead of reusing the raw verifier directly.
    - Test 3: dry-run rejection and the existing machine-readable artifact contract remain intact after the local-wrapper changes per D-60.1-01 and D-60.1-02.
  </behavior>
  <acceptance_criteria>
    - `scripts/verify-phase60-load-and-rehearsal.test.ts` contains the exact blocker token `PHASE60_LOCAL_DB_SHARED_WITH_APP`.
    - The test file asserts `package.json` exposes an exact `verify:phase60:local` script string.
    - The test file still locks `smoke-result.json`, `capacity-result.json`, and `drill-results.json` as the machine-readable contracts consumed by the verifier.
    - `pnpm exec vitest --run scripts/verify-phase60-load-and-rehearsal.test.ts` fails before implementation and passes afterward.
  </acceptance_criteria>
  <action>Before editing, run the GitNexus impact/context checks listed above so the new assertions stay anchored to the existing verifier contract. Expand `scripts/verify-phase60-load-and-rehearsal.test.ts` first so the new closure behavior is explicit before implementation. Add a test for the repo-local shared-db blocker using `PHASE60_BASE_URL=http://127.0.0.1:3000` plus `DB_FILE_NAME=file:local.db`, and require the exact blocker token `PHASE60_LOCAL_DB_SHARED_WITH_APP`. Add a second assertion that `package.json` exports the exact `verify:phase60:local` script string pointing to the wrapper file from Task 2 via the `Package verifier script registration` entry in `60.1-PATTERNS.md`. Keep the current hard-gate assertions for `smoke-result.json`, `capacity-result.json`, `drill-results.json`, dry-run rejection, and stage order so D-60.1-01 and D-60.1-02 stay locked.</action>
  <verify>
    <automated>pnpm exec vitest --run scripts/verify-phase60-load-and-rehearsal.test.ts</automated>
  </verify>
  <done>The local-isolation and fail-closed requirements are captured in a focused regression suite before code changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Add the isolated local Phase 60 wrapper and fail closed on shared `local.db`</name>
  <read_first>
    - package.json
    - scripts/verify-phase60-load-and-rehearsal.ts
    - scripts/prepare-dev-db.ts
    - .planning/debug/phase60-local-sqlite-busy.md
    - .planning/phases/60.1-close-gap-pilot-03-load-01-load-02-replace-dry-run-phase60-p/CONTEXT.md
    - .planning/phases/60-load-degrade-pilot-rehearsal/60-04-SUMMARY.md
  </read_first>
  <pre_edit_checks>
    - Run `gitnexus_impact({ target: "runPhase60Verification", direction: "upstream" })` and `gitnexus_context({ name: "runPhase60Verification" })` before editing `scripts/verify-phase60-load-and-rehearsal.ts`.
    - Run `gitnexus_impact({ target: "getPhase60MissingLiveEnv", direction: "upstream" })` and `gitnexus_context({ name: "getPhase60MissingLiveEnv" })` before adding the shared-local-db preflight guard.
  </pre_edit_checks>
  <files>package.json, scripts/verify-phase60-local.ts, scripts/verify-phase60-load-and-rehearsal.ts, .planning/debug/phase60-local-sqlite-busy.md</files>
  <acceptance_criteria>
    - `package.json` exposes the exact script key `verify:phase60:local` and it runs `scripts/verify-phase60-local.ts` through the same node/tsx shim pattern used elsewhere in this repo.
    - `scripts/verify-phase60-local.ts` creates or refreshes an isolated proof DB under `/tmp/opencode/phase60-local-proof/`, sets `DB_FILE_NAME` to that copied file, runs the canonical verifier, and never points the wrapper back to `file:local.db`.
    - `scripts/verify-phase60-load-and-rehearsal.ts` throws `PHASE60_LOCAL_DB_SHARED_WITH_APP` when `PHASE60_BASE_URL` is localhost/127.0.0.1 and `DB_FILE_NAME` resolves to `file:local.db`, and the error text tells the operator to rerun `pnpm verify:phase60:local`.
    - The verifier still rejects dry-run closeout, still reads the same machine-readable artifact files, and does not change D-60.1-03 or D-60.1-04 semantics.
    - `.planning/debug/phase60-local-sqlite-busy.md` has `root_cause`, `fix`, and `verification` filled with the isolated-local-proof posture and exact rerun commands.
  </acceptance_criteria>
  <action>Before editing, run the GitNexus impact/context checks listed above so the verifier guard stays aligned with the existing call graph. Then implement Plan 03 by explicitly following the updated `60.1-PATTERNS.md` entries for `scripts/verify-phase60-local.ts`, `Package verifier script registration`, and `Debug-note resolution closure`. Create `scripts/verify-phase60-local.ts` as the repo-local substitute wrapper for D-60.1-05. The wrapper should copy the source SQLite truth from `process.env.DB_FILE_NAME || "file:local.db"` into `/tmp/opencode/phase60-local-proof/local.db`, run the migration bridge on that copied file using the existing `prepareDevDb()` pattern, then invoke the canonical `scripts/verify-phase60-load-and-rehearsal.ts` with `DB_FILE_NAME=file:/tmp/opencode/phase60-local-proof/local.db`. Update `package.json` with an exact `verify:phase60:local` script using the existing `node --require ./scripts/server-only-node-shim.cjs --import tsx ...` posture. In `scripts/verify-phase60-load-and-rehearsal.ts`, add a preflight guard that detects the unsafe repo-local substitute posture (`PHASE60_BASE_URL` on localhost/127.0.0.1 plus `DB_FILE_NAME=file:local.db`) and throws `PHASE60_LOCAL_DB_SHARED_WITH_APP: rerun pnpm verify:phase60:local` before smoke starts. Do not alter the existing machine-readable evidence filenames per D-60.1-02, do not touch deploy/rollback canonical release semantics per D-60.1-03, and do not auto-clear the manual transport fallback lane per D-60.1-04. Finish by updating `.planning/debug/phase60-local-sqlite-busy.md` from `investigating` to a filled-in resolution note with the chosen fix and rerun commands, matching the `Debug-note resolution closure` shape.</action>
  <verify>
    <automated>pnpm exec vitest --run scripts/verify-phase60-load-and-rehearsal.test.ts</automated>
  </verify>
  <done>Repo-local Phase 60 proof now has an isolated SQLite lane, and the raw verifier rejects the unsafe shared `local.db` posture with an actionable blocker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-local wrapper → copied proof DB | the wrapper creates a temporary durable truth copy for proof execution |
| canonical verifier → localhost app target | verifier traffic can be mistaken for final pilot-host evidence if local-vs-live semantics drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60.1-03-01 | Tampering | `scripts/verify-phase60-local.ts` | mitigate | Copy the source DB into an isolated `/tmp/opencode/phase60-local-proof/` target so repo-local proof does not mutate the long-lived app DB. |
| T-60.1-03-02 | Spoofing | `scripts/verify-phase60-load-and-rehearsal.ts` | mitigate | Fail closed on localhost + `file:local.db` and require the explicit local-wrapper command so unsafe shared proof cannot masquerade as valid evidence. |
| T-60.1-03-03 | Repudiation | `.planning/debug/phase60-local-sqlite-busy.md` | mitigate | Record the root cause, chosen fix, and rerun commands in the debug note so future evidence generation can be audited. |
</threat_model>

<verification>
- `pnpm exec vitest --run scripts/verify-phase60-load-and-rehearsal.test.ts`
</verification>

<success_criteria>
- This plan only isolates and hardens the repo-local substitute lane; final live evidence and milestone closeout still happen in plan `60.1-01`.
- Repo-local Phase 60 proof uses an isolated SQLite file instead of the app's shared `local.db`.
- The canonical verifier now rejects the unsafe shared-local posture before smoke starts.
- Local substitute proof remains explicitly distinct from final live pilot-host closeout.
</success_criteria>

<output>
After completion, create `.planning/phases/60.1-close-gap-pilot-03-load-01-load-02-replace-dry-run-phase60-p/60.1-03-SUMMARY.md`
</output>
