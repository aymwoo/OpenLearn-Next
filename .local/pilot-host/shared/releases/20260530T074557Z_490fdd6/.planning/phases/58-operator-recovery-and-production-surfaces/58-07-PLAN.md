---
phase: 58-operator-recovery-and-production-surfaces
plan: "07"
type: execute
wave: 6
depends_on:
  - "58-01"
  - "58-02"
  - "58-03"
files_modified:
  - src/lib/dal/classroom-incident-operator.ts
  - src/lib/dal/plugin-governance-operator.ts
  - src/lib/dal/plugin-governance-operator.test.ts
  - src/app/settings/labs/plugins/[pluginId]/page.tsx
  - src/app/settings/labs/plugins/[pluginId]/actions/[actionKey]/page.tsx
  - src/components/surfaces/plugin-lifecycle-operator-surface.tsx
  - src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx
autonomous: true
requirements:
  - OPS-01
  - PLUG-03
  - OPS-03
gap_closure: true
must_haves:
  truths:
    - "incident detail 产出的 plugin/action 深链都指向真实存在的正式 route。"
    - "PluginLifecycleOperatorSurface 已挂到 `/settings/labs/plugins/[pluginId]` 与 `/settings/labs/plugins/[pluginId]/actions/[actionKey]`，不再只是测试孤儿组件。"
    - "operator 可以从 classroom incident 继续下钻到 plugin detail 与 action detail，并在 detail host 内看到高风险恢复确认入口。"
  artifacts:
    - path: src/lib/dal/plugin-governance-operator.ts
      provides: plugin governance detail read seam for operator routes
    - path: src/app/settings/labs/plugins/[pluginId]/page.tsx
      provides: formal plugin governance detail route
    - path: src/app/settings/labs/plugins/[pluginId]/actions/[actionKey]/page.tsx
      provides: formal plugin action detail route
    - path: src/components/surfaces/plugin-lifecycle-operator-surface.tsx
      provides: mounted governance diagnostics and detail confirm host
    - path: src/lib/dal/classroom-incident-operator.ts
      provides: real plugin/action hrefs
  key_links:
    - from: src/lib/dal/classroom-incident-operator.ts
      to: /settings/labs/plugins/[pluginId]
      via: plugin related card href
      pattern: /settings/labs/plugins/
    - from: src/lib/dal/classroom-incident-operator.ts
      to: /settings/labs/plugins/[pluginId]/actions/[actionKey]
      via: action related card href
      pattern: /actions/
    - from: src/app/settings/labs/plugins/[pluginId]/page.tsx
      to: src/components/surfaces/plugin-lifecycle-operator-surface.tsx
      via: formal route mount
      pattern: PluginLifecycleOperatorSurface
---

<objective>
闭合 Gap A：补齐可达的 plugin detail / action detail route，并把 `PluginLifecycleOperatorSurface` 挂到正式页面。

Purpose: 消除 verifier 报出的断链与 orphaned surface，让 Phase 58 的 incident → plugin/action drill-down 真正可达，而不是停留在假 href。
Output: operator-scoped plugin governance read seam、2 个正式 route、focused governance surface 挂载、incident href 修复与回归测试。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-CONTEXT.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-RESEARCH.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-PATTERNS.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-VERIFICATION.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-UI-SPEC.md
@src/lib/dal/classroom-incident-operator.ts
@src/features/platform-core/actions/registry.ts
@src/app/settings/labs/commands/[commandId]/page.tsx
@src/app/settings/labs/async-tasks/[taskId]/page.tsx
@src/components/surfaces/plugin-lifecycle-operator-surface.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 operator 可复用的 plugin/action detail read seam</name>
  <read_first>
    - src/lib/dal/classroom-incident-operator.ts
    - src/features/platform-core/actions/registry.ts
    - src/app/settings/labs/commands/[commandId]/page.tsx
    - .planning/phases/58-operator-recovery-and-production-surfaces/58-VERIFICATION.md
    - .planning/phases/58-operator-recovery-and-production-surfaces/58-PATTERNS.md
  </read_first>
  <files>src/lib/dal/plugin-governance-operator.ts, src/lib/dal/plugin-governance-operator.test.ts, src/components/surfaces/plugin-lifecycle-operator-surface.tsx, src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx</files>
  <action>新增 `src/lib/dal/plugin-governance-operator.ts`，提供两个 server-only seam：`getPluginLifecycleOperatorDetailDTO({ pluginId })` 与 `getPluginActionLifecycleOperatorDetailDTO({ pluginId, actionKey })`。这两个 seam 必须按 D-58-06 继续复用现有 `readGovernanceDashboardBundle()` truth，而不是在 page 里手拼请求；并按 verifier Gap A 直接产出 route 所需的 `schoolId`、filtered dashboard、focused plugin/action identity、`backHref`、`notFound` 条件。同步扩展 `PluginLifecycleOperatorSurface` 支持 `focusedPluginId` 与 `focusedActionKey`：plugin detail route 只展示目标 plugin 的治理摘要与恢复 host，action detail route 只展示目标 action 的 blocked/executable diagnostics、disabled reason、detail confirm host；禁止回退到 marketplace surface 或整页 raw log。</action>
  <verify>
    <automated>pnpm exec vitest --run src/lib/dal/plugin-governance-operator.test.ts src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/dal/plugin-governance-operator.ts` 存在且包含 `getPluginLifecycleOperatorDetailDTO` 与 `getPluginActionLifecycleOperatorDetailDTO` 两个导出。
    - `src/components/surfaces/plugin-lifecycle-operator-surface.tsx` 出现 `focusedPluginId` 与 `focusedActionKey` props，并能过滤到单个 plugin/action 上下文。
    - 测试能证明 foreign plugin/action 不会被错误 school scope 返回，且 focused mode 不会渲染整批 unrelated plugins。
  </acceptance_criteria>
  <done>route 所需 plugin/action detail DTO 与 focused governance host 已被测试锁定，后续 page 可以薄挂载而非重新实现治理 UI。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建正式 plugin detail / action detail route 并修复 incident href</name>
  <read_first>
    - src/lib/dal/plugin-governance-operator.ts
    - src/lib/dal/classroom-incident-operator.ts
    - src/app/settings/labs/commands/[commandId]/page.tsx
    - src/app/settings/labs/async-tasks/[taskId]/page.tsx
    - .planning/phases/58-operator-recovery-and-production-surfaces/58-VERIFICATION.md
  </read_first>
  <files>src/app/settings/labs/plugins/[pluginId]/page.tsx, src/app/settings/labs/plugins/[pluginId]/actions/[actionKey]/page.tsx, src/lib/dal/classroom-incident-operator.ts, src/components/surfaces/plugin-lifecycle-operator-surface.tsx, src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx</files>
  <action>新增两个 thin page：`/settings/labs/plugins/[pluginId]` 与 `/settings/labs/plugins/[pluginId]/actions/[actionKey]`。page 只做 `params -> plugin governance operator DAL -> notFound -> PluginLifecycleOperatorSurface`，保持与 command/task detail route 同一薄页面模式。plugin page 必须挂载 `PluginLifecycleOperatorSurface` 的 focused plugin mode；action page 必须挂载 focused action mode，并保留高风险动作 `resume` / `suspend` / `fallback` 的 detail confirm host。随后更新 `src/lib/dal/classroom-incident-operator.ts`，让第 98-103 行继续生成这两个 route 的真实 href，而不是不存在的目标；不要把 href 降级成 marketplace 或 `/settings/labs` 泛入口。测试必须直接断言 route source 实际 import/render `PluginLifecycleOperatorSurface`，从而消灭 verifier 标出的 orphaned surface 反模式。</action>
  <verify>
    <automated>pnpm exec vitest --run src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx src/lib/dal/plugin-governance-operator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/settings/labs/plugins/[pluginId]/page.tsx` 与 `src/app/settings/labs/plugins/[pluginId]/actions/[actionKey]/page.tsx` 都存在。
    - 两个 page 文件都直接 import `PluginLifecycleOperatorSurface`，且调用新的 DAL seam，而不是使用 marketplace route。
    - `src/lib/dal/classroom-incident-operator.ts` 仍输出 `/settings/labs/plugins/${pluginId}` 与 `/settings/labs/plugins/${pluginId}/actions/${actionKey}`，但这次目标 route 已真实存在。
    - 从 incident related card 进入 plugin/action detail 后，能看到目标 plugin/action 的治理摘要与 detail confirm host，而不是空页或全量治理列表。
  </acceptance_criteria>
  <done>Gap A 的 route 断链与 orphaned surface 已闭合，incident → plugin/action drill-down 成为正式 product surface。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| incident DTO → plugin/action route | 只允许输出仓库真实存在且由 server-owned read seam 支撑的 href |
| plugin/action route → governance surface | route 必须消费同一 governance truth source，不允许 page 端本地拼接或 marketplace 替代 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-GA-01 | I | `plugin-governance-operator.ts` | mitigate | 复用 operator school scope 过滤 plugin/action 查询，foreign-school plugin/action 直接 notFound |
| T-58-GA-02 | T | `classroom-incident-operator.ts` href assembly | mitigate | 只输出已存在的 `/settings/labs/plugins/[pluginId]` 与 `/actions/[actionKey]` route，不再生成 hollow href |
| T-58-GA-03 | R | `PluginLifecycleOperatorSurface` route mount | mitigate | 通过 page source + component tests 固化正式挂载，防止组件再次退回测试孤岛 |
</threat_model>

<verification>
  <automated>pnpm exec vitest --run src/lib/dal/plugin-governance-operator.test.ts src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx</automated>
  <automated>python - <<'PY'
from pathlib import Path
base = Path('src/app/settings/labs/plugins')
assert (base / '[pluginId]/page.tsx').exists()
assert (base / '[pluginId]/actions/[actionKey]/page.tsx').exists()
print('plugin routes exist')
PY</automated>
</verification>

<success_criteria>
- Gap A 中 verifier 指出的两个缺口都被代码直连闭合：真实 route 存在，`PluginLifecycleOperatorSurface` 已挂到正式 page。
- `src/lib/dal/classroom-incident-operator.ts` 生成的 plugin/action href 不再指向不存在页面。
- operator 可以从 classroom incident 无需手改 URL 地进入 plugin detail 与 action detail。
</success_criteria>

<output>
After completion, create `.planning/phases/58-operator-recovery-and-production-surfaces/58-07-SUMMARY.md`
</output>
