---
phase: 13-course-center-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01-PLAN
files_modified:
  - src/lib/dto/course-authoring.ts
  - src/lib/dal/course-authoring.ts
  - src/actions/course-authoring-actions.ts
  - src/actions/course-authoring-actions.test.ts
  - src/components/courses/course-create-drawer.tsx
  - src/components/courses/course-detail-form.tsx
  - src/components/surfaces/teacher-course-center-surface.tsx
  - src/components/surfaces/teacher-course-detail-surface.tsx
autonomous: true
requirements:
  - COURSE-02
  - COURSE-03
must_haves:
  truths:
    - D-05: 教师能在课程中心用右侧抽屉新建课程，并立即在列表中看到新课程
    - D-06 D-07 D-08: 教师能在课程详情页编辑标题、学科、年级、状态，并在页内看到成功反馈与最新结果
    - D-16 D-17: 所有课程变更都通过 teacher/school-scoped DAL 与 Server Actions，且能 read-your-writes
  artifacts:
    - path: src/actions/course-authoring-actions.ts
      provides: create/update course server actions
    - path: src/components/courses/course-create-drawer.tsx
      provides: drawer-based create flow
    - path: src/components/courses/course-detail-form.tsx
      provides: inline detail edit form with success region
  key_links:
    - teacher-course-center-surface -> createCourseAction via right drawer submit
    - teacher-course-detail-surface -> updateCourseAction via inline form submit
    - course-authoring-actions -> cacheTags.teacherCourses/cacheTags.course via updateTag
---

<objective>
补齐课程新建与课程基础信息编辑闭环，把 Phase 13 的课程中心从可读升级为可写。

Purpose: 落实 D-05、D-06、D-07、D-08、D-16、D-17，让 COURSE-02 和 COURSE-03 真正可执行。
Output: 课程 create/edit Server Actions、抽屉新建表单、详情页 inline 编辑表单、页内成功反馈。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-course-center-foundation/13-CONTEXT.md
@.planning/phases/13-course-center-foundation/13-RESEARCH.md
@.planning/phases/13-course-center-foundation/13-UI-SPEC.md
@.planning/phases/13-course-center-foundation/13-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现课程 create/update mutation contract 与缓存失效</name>
  <files>src/lib/dto/course-authoring.ts, src/lib/dal/course-authoring.ts, src/actions/course-authoring-actions.ts, src/actions/course-authoring-actions.test.ts</files>
  <read_first>src/actions/lesson-authoring-actions.ts; src/lib/dal/lesson-authoring.ts; src/lib/cache-policy.ts; src/lib/dto/course-authoring.ts; src/actions/theme-actions.test.ts</read_first>
  <behavior>
    - Test 1: create action 仅接受 `schoolId,title,subject,grade,status?`，非法输入返回 `VALIDATION_ERROR`。
    - Test 2: update action 仅允许 teacher scope 内课程更新，成功后返回最新 course DTO。
    - Test 3: create/update 成功都会 `updateTag(cacheTags.teacherCourses(actorId))` 和 `updateTag(cacheTags.course(courseId))`。
  </behavior>
  <action>在 `src/lib/dto/course-authoring.ts` 补充课程表单 schema 与结果 DTO；在 `src/lib/dal/course-authoring.ts` 新增 `createCourseForTeacherScoped()` 与 `updateCourseForTeacherScoped()`，字段只围绕 `title`、`subject`、`grade`、`status`（per D-08），并复用 teacher/school scope 授权（per D-16）。在 `src/actions/course-authoring-actions.ts` 采用现有 `ActionResult` 合同和 Zod 校验，create 成功后同时失效课程中心列表 tag 与课程详情 tag；update 成功后同样失效列表与详情 tag（per D-17）。先写 `src/actions/course-authoring-actions.test.ts` 断言 validation、授权和 `updateTag()` 调用，再实现 action；测试标题中显式引用 D-08、D-16、D-17。</action>
  <acceptance_criteria>
    - `src/actions/course-authoring-actions.ts` contains `export async function createCourseAction`
    - `src/actions/course-authoring-actions.ts` contains `export async function updateCourseAction`
    - `src/actions/course-authoring-actions.ts` contains `updateTag(cacheTags.teacherCourses`
    - `src/actions/course-authoring-actions.ts` contains `updateTag(cacheTags.course(`
    - `pnpm vitest run src/actions/course-authoring-actions.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/actions/course-authoring-actions.test.ts</automated>
  </verify>
  <done>课程 create/edit 的服务端 contract 已稳定，输入、权限和 cache invalidation 都有自动化证明。</done>
</task>

<task type="auto">
  <name>Task 2: 接入抽屉新建与详情页 inline 编辑，并提供页内成功反馈</name>
  <files>src/components/courses/course-create-drawer.tsx, src/components/courses/course-detail-form.tsx, src/components/surfaces/teacher-course-center-surface.tsx, src/components/surfaces/teacher-course-detail-surface.tsx</files>
  <read_first>src/components/surfaces/teacher-course-center-surface.tsx; src/components/surfaces/teacher-course-detail-surface.tsx; src/components/ui/ghost-field.ts; src/components/ui/button.tsx; src/components/ui/card.tsx</read_first>
  <action>在课程总览页接入右侧抽屉表单，而不是独立 `new` 页面（per D-05）；抽屉字段明确为 `课程名称`、`学科`、`年级`，状态默认 `draft`。在课程详情页内实现 inline 编辑表单，而不是再跳回列表或开第二层 modal（per D-06）。保存成功后在详情页表单区顶部展示稳定的 success region，例如 `课程信息已更新，并已同步到当前课程视图`，同时刷新后的标题/学科/年级/状态必须立即反映在页面上，禁止只弹 toast 不保留页内证据（per D-07）。课程中心创建成功后，新课程卡必须立即出现在总览卡片网格中，并保留进入详情页入口；所有文案继续使用简体中文与现有 tonal surface 语言（per D-18）。</action>
  <acceptance_criteria>
    - `src/components/courses/course-create-drawer.tsx` contains `课程名称`
    - `src/components/courses/course-create-drawer.tsx` contains `学科`
    - `src/components/courses/course-detail-form.tsx` contains `课程信息已更新`
    - `src/components/surfaces/teacher-course-center-surface.tsx` contains `createCourseAction`
    - `src/components/surfaces/teacher-course-detail-surface.tsx` contains `updateCourseAction`
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/actions/course-authoring-actions.test.ts</automated>
  </verify>
  <done>教师可以在总览页抽屉中建课、在详情页内联编辑，并看到保留在页内的成功反馈与即时刷新结果。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teacher form input → server action | 未信任的表单输入进入 create/update mutation |
| server action → DAL | Action 将校验后的字段交给授权边界执行真实写入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | T | `createCourseAction` / `updateCourseAction` | mitigate | 对 `title`、`subject`、`grade`、`status` 使用 Zod schema，拒绝未声明字段和空值 |
| T-13-06 | E | `updateCourseForTeacherScoped` | mitigate | 进入 DAL 前后都复用 teacher/school scope 授权，跨 school 的 `courseId` 一律拒绝 |
| T-13-07 | R | 课程编辑成功反馈 | mitigate | 返回标准 `ActionResult` 与页内 success region，保留保存结果证据，避免只有瞬时 toast |
| T-13-08 | I | 课程详情写入后页面状态 | mitigate | create/update 后同时失效 teacher list + course detail tags，避免教师看见过期字段 |
</threat_model>

<verification>
- `pnpm vitest run src/actions/course-authoring-actions.test.ts`
- Read `src/components/surfaces/teacher-course-detail-surface.tsx` and confirm success region exists outside transient toast code
</verification>

<success_criteria>
- COURSE-02 与 COURSE-03 已由真实 teacher workflow 覆盖
- 新建课程使用右侧抽屉，不新增独立 new 页面
- 详情页编辑成功后保留页内反馈并立刻显示最新数据
</success_criteria>

<output>
After completion, create `.planning/phases/13-course-center-foundation/13-02-SUMMARY.md`
</output>
