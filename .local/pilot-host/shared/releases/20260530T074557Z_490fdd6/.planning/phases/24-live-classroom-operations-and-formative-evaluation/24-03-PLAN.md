---
phase: 24-live-classroom-operations-and-formative-evaluation
plan: 03
type: execute
wave: 3
depends_on:
  - 24-01
  - 24-02
files_modified:
  - src/app/(classroom)/classroom/page.tsx
  - src/lib/dto/classroom.ts
  - src/lib/dal/classroom.ts
  - src/components/classroom/classroom-roster-panel.tsx
  - src/components/classroom/classroom-student-detail-panel.tsx
  - src/components/classroom/classroom-control-panel.tsx
autonomous: true
requirements:
  - ACT-03
  - EVAL-01
  - EVAL-02
must_haves:
  truths:
    - 单学生 evidence / evaluation 工作流必须从 `/classroom` 名册进入，留在同一路由上下文内完成。
    - `/teacher/review` 不是 Phase 24 的主入口，不能在这里新增与 `/classroom` 竞争的双主路径。
    - detail panel 必须同时承载 evidence 与 evaluation，而不是拆成两个互不相连的独立界面。
  artifacts:
    - path: src/app/(classroom)/classroom/page.tsx
      provides: same-route student detail entry contract
      contains: studentId
    - path: src/lib/dal/classroom.ts
      provides: single-student evidence and evaluation detail DTO
      contains: getClassroomStudentDetailDTO
    - path: src/components/classroom/classroom-student-detail-panel.tsx
      provides: unified evidence / evaluation detail panel on `/classroom`
      contains: 课堂证据
  key_links:
    - from: src/components/classroom/classroom-roster-panel.tsx
      to: src/components/classroom/classroom-student-detail-panel.tsx
      via: roster row opens a same-route student detail panel
      pattern: studentId|detailTab
    - from: src/app/(classroom)/classroom/page.tsx
      to: src/lib/dal/classroom.ts
      via: page fetches optional student detail DTO with the active session snapshot
      pattern: getClassroomStudentDetailDTO
---

<objective>
把 Phase 24 的 evidence / evaluation 主工作流落到 `/classroom` 单学生详情面板：教师从名册点进学生后，在同一运行页内完成证据查看、参与度记录与观察复核。

Purpose: 收口 runtime monitoring 与 formative evaluation 的主交互面，不再让教师切到另一个 review 页面做单学生判断。
Output: same-route student detail fetch and a unified detail panel with evidence + evaluation tabs.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-live-classroom-operations-and-formative-evaluation/24-CONTEXT.md
@src/app/(classroom)/classroom/page.tsx
@src/components/classroom/classroom-control-panel.tsx
@src/components/classroom/classroom-roster-panel.tsx
@src/components/classroom/classroom-timeline-panel.tsx
@src/app/(teacher)/teacher/review/page.tsx
@src/lib/dto/classroom.ts
@src/lib/dal/classroom.ts
@src/actions/classroom-actions.ts

<interfaces>
Same-route detail entry contract:
```ts
searchParams?: Promise<{
  sessionId?: string
  studentId?: string
  detailTab?: "evidence" | "evaluation"
}>
```

Unified detail DTO:
```ts
type ClassroomStudentDetailDTO = {
  studentId: string
  studentName: string
  evidenceEntries: ClassroomEvidenceDTO[]
  evaluationEntries: StudentFormativeEvaluationEntryDTO[]
  latestParticipationLevel: "active" | "normal" | "attention" | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add same-route student detail read model and `/classroom` search-param entry</name>
  <files>src/app/(classroom)/classroom/page.tsx, src/lib/dto/classroom.ts, src/lib/dal/classroom.ts</files>
  <read_first>src/app/(classroom)/classroom/page.tsx, src/lib/dto/classroom.ts, src/lib/dal/classroom.ts, src/app/(teacher)/teacher/review/page.tsx, .planning/phases/24-live-classroom-operations-and-formative-evaluation/24-CONTEXT.md</read_first>
  <behavior>
    - Test 1: the classroom page accepts `studentId` and `detailTab` without introducing a new route.
    - Test 2: DAL returns a single student detail DTO that merges evidence history and formative evaluation history from the same session.
    - Test 3: the read path stays teacher-scoped and returns `null` when no student is selected.
  </behavior>
  <action>Update `src/app/(classroom)/classroom/page.tsx` so `searchParams` also accepts `studentId` and `detailTab`, then fetch `getClassroomStudentDetailDTO({ sessionId, studentId })` when both an active session and a selected student exist. In `src/lib/dto/classroom.ts`, add `ClassroomStudentDetailTabSchema` with exact values `evidence` and `evaluation`, plus `ClassroomStudentDetailDTOSchema` with `studentId`, `studentName`, `evidenceEntries`, `evaluationEntries`, and `latestParticipationLevel`. In `src/lib/dal/classroom.ts`, create `getClassroomStudentDetailDTO({ sessionId, studentId })` that verifies teacher scope for the session, reads the student's session-scoped evidence rows, splits rows tagged with `kind: "formative-evaluation"` into `evaluationEntries`, and keeps all other student rows in `evidenceEntries` newest-first. Do not fetch from `src/lib/dal/learning.ts` or route through `/teacher/review`.
</action>
  <acceptance_criteria>
    - `src/app/(classroom)/classroom/page.tsx` contains `studentId` and `detailTab` in `searchParams`.
    - `src/lib/dto/classroom.ts` contains `ClassroomStudentDetailTabSchema` with `evidence` and `evaluation`.
    - `src/lib/dal/classroom.ts` contains `getClassroomStudentDetailDTO` and separates `evaluationEntries` using `kind: "formative-evaluation"`.
    - `src/lib/dal/classroom.ts` does not add a dependency on `getTeacherLessonReviewDTO` or `src/lib/dal/learning.ts` for this read path.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/classroom.test.ts</automated>
  </verify>
  <done>`/classroom` gets a same-route single-student detail contract for evidence and evaluation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the student detail panel and wire roster rows into the unified workflow</name>
  <files>src/components/classroom/classroom-roster-panel.tsx, src/components/classroom/classroom-student-detail-panel.tsx, src/components/classroom/classroom-control-panel.tsx</files>
  <read_first>src/components/classroom/classroom-roster-panel.tsx, src/components/classroom/classroom-control-panel.tsx, src/components/classroom/classroom-student-evaluation-form.tsx, src/lib/dto/classroom.ts, src/components/surfaces/classroom-console-surface.tsx, .planning/phases/24-live-classroom-operations-and-formative-evaluation/24-CONTEXT.md</read_first>
  <behavior>
    - Test 1: roster rows expose an explicit entry action for opening a student detail panel.
    - Test 2: the detail panel shows evidence and evaluation tabs in one tonal panel instead of routing away.
    - Test 3: the evaluation tab embeds the fixed Phase 24 form and the evidence tab keeps multi-source student history visible.
  </behavior>
  <action>Create `src/components/classroom/classroom-student-detail-panel.tsx` as a right-side tonal detail panel component that receives `sessionId`, `detail`, and `activeTab`, renders exact tab labels `课堂证据` and `过程评价`, and shows the latest participation tier summary plus evaluation history above the form. Update `src/components/classroom/classroom-roster-panel.tsx` so every participant row includes an explicit action button labeled `查看证据与评价`, and make that button navigate within the same route by setting `studentId` and `detailTab` query params. Update `src/components/classroom/classroom-control-panel.tsx` to accept optional selected student detail props, render the new detail panel beside the existing timeline/roster stack, and keep all interaction inside the current `/classroom` runtime layout without introducing a new hero section or linking the primary flow to `/teacher/review`.</action>
  <acceptance_criteria>
    - `src/components/classroom/classroom-roster-panel.tsx` contains `查看证据与评价` and uses `studentId` plus `detailTab` in its entry action.
    - `src/components/classroom/classroom-student-detail-panel.tsx` exists and contains `课堂证据` and `过程评价`.
    - `src/components/classroom/classroom-control-panel.tsx` renders `ClassroomStudentDetailPanel`.
    - no new primary navigation link to `/teacher/review` is introduced in these files.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/classroom/classroom-roster-panel.test.tsx src/components/classroom/classroom-student-detail-panel.test.tsx</automated>
  </verify>
  <done>The user can move from roster monitoring into a single student evidence/evaluation workflow without leaving `/classroom`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| roster interaction -> same-route detail fetch | a teacher-selected student id determines which sensitive evidence/evaluation data is shown |
| detail DTO -> client panel | the panel combines multiple student evidence types and must stay correctly scoped to the active session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | I | `getClassroomStudentDetailDTO` | mitigate | Enforce teacher session scope before returning any student evidence or evaluation history. |
| T-24-08 | T | roster navigation state | mitigate | Keep selected student state in explicit `studentId` and `detailTab` params so refreshes and server fetches stay deterministic. |
| T-24-09 | D | detail panel layout | accept | The detail panel adds density to the `/classroom` sidebar area, but remains a secondary tonal surface rather than a second hero. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/classroom.test.ts src/components/classroom/classroom-roster-panel.test.tsx src/components/classroom/classroom-student-detail-panel.test.tsx`
</verification>

<success_criteria>
- teachers open a single student detail panel from the roster within `/classroom`.
- the same panel exposes both evidence and formative evaluation workflows.
- `/teacher/review` remains secondary and does not compete with the Phase 24 primary flow.
</success_criteria>

<output>
After completion, create `.planning/phases/24-live-classroom-operations-and-formative-evaluation/24-03-SUMMARY.md`
</output>
