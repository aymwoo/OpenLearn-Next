---
type: quick
slug: 260511-r3g-teacher-schedule-import-teacher-schedule
scope: /teacher/schedule
autonomous: true
files_modified:
  - src/app/(teacher)/teacher/schedule/page.tsx
  - src/app/(teacher)/teacher/schedule/import/page.tsx
  - src/components/surfaces/teacher-schedule-surface.tsx
  - src/components/surfaces/teacher-schedule-surface.test.tsx
  - src/components/surfaces/schedule-import-modal.tsx
  - src/components/surfaces/schedule-import-modal.test.tsx
  - src/components/surfaces/schedule-import-review-surface.tsx
  - src/components/surfaces/schedule-import-review-surface.test.tsx
  - src/features/schedule/runtime/server.ts
  - src/features/schedule/runtime/server.test.ts
  - src/features/schedule/shared/dto/runtime.ts
must_haves:
  truths:
    - 教师从 CSV 导入成功后会回到 `/teacher/schedule`，不会再落到独立的 `/teacher/schedule/import` 页面。
    - 最新导入批次的审核入口仍保留在主课表体验内，不会绕过 staging review 直接入库。
    - `/teacher/schedule` 主区域最后一个 section 会显示完整周课表，布局贴近 Stitch 参考图的“时间轴 + 工作日列 + 课程卡片矩阵”。
  artifacts:
    - path: src/app/(teacher)/teacher/schedule/import/page.tsx
      provides: 旧导入页回退到 `/teacher/schedule` 的重定向壳
    - path: src/features/schedule/shared/dto/runtime.ts
      provides: 主课表页可消费的完整周课表 DTO contract
    - path: src/components/surfaces/teacher-schedule-surface.tsx
      provides: 主课表页内联审核区与底部完整课表 section
  key_links:
    - from: src/components/surfaces/schedule-import-modal.tsx
      to: /teacher/schedule
      via: CSV 导入成功后的 `router.push`
    - from: src/app/(teacher)/teacher/schedule/import/page.tsx
      to: /teacher/schedule
      via: server redirect，移除独立 destination
    - from: src/features/schedule/runtime/server.ts
      to: src/components/surfaces/teacher-schedule-surface.tsx
      via: 运行时 DTO 提供周课表矩阵数据
    - from: src/components/surfaces/schedule-import-review-surface.tsx
      to: src/app/(teacher)/teacher/schedule/page.tsx
      via: 以内联 section 形式复用 staging review，而不是独立页面跳转
---

<objective>
把课表导入主流程收回 `/teacher/schedule`：导入成功回主课表页、旧 `/teacher/schedule/import` 退出主用户流，同时在主页面底部新增 Stitch 对齐的完整周课表 section。

Purpose: 满足当前“导入后留在主课表页”的交互要求，同时保留 Phase 18 已锁定的 staging review 与 normalized runtime 边界。
Output: 主课表页内联导入审核入口、旧导入页重定向、最小 DTO 扩展支持的完整周课表布局。
</objective>

<context>
@.planning/STATE.md
@src/app/(teacher)/teacher/schedule/page.tsx
@src/app/(teacher)/teacher/schedule/import/page.tsx
@src/components/surfaces/teacher-schedule-surface.tsx
@src/components/surfaces/schedule-import-modal.tsx
@src/components/surfaces/schedule-import-review-surface.tsx
@src/features/schedule/runtime/server.ts
@src/features/schedule/shared/dto/runtime.ts
@/tmp/opencode/260511-r3g-stitch-schedule-management.png

<interfaces>
From `src/features/schedule/shared/dto/runtime.ts`:

```ts
export const TeacherDailyAgendaCardDTOSchema = z.object({
  id: z.string(),
  recurringEntryId: z.string(),
  assignmentId: z.string(),
  timeLabel: z.string(),
  classLabel: z.string(),
  locationLabel: z.string(),
  status: scheduleAgendaStatusSchema,
  courseTitle: z.string(),
  overrideSummary: z.string().nullable().default(null),
  lessonLink: z.object({ courseId: z.string(), lessonId: z.string(), lessonTitle: z.string() }).nullable().default(null),
});

export const TeacherDailyAgendaDTOSchema = z.object({
  teacherId: z.string(),
  schoolId: z.string(),
  date: z.string(),
  dateLabel: z.string(),
  weekLabel: z.string(),
  nextClassCountdownLabel: z.string().nullable().default(null),
  cards: z.array(TeacherDailyAgendaCardDTOSchema).default([]),
});
```

From `src/features/schedule/runtime/server.ts`:

```ts
export async function getTeacherDailyAgendaDTO(input?: { date?: string; teacherId?: string }) {
  const scope = await assertScheduleTeacherScope();
  return getCachedTeacherDailyAgenda(actorId, scope.schoolIds, requestedDate);
}
```

From `src/components/surfaces/schedule-import-modal.tsx`:

```ts
setTimeout(() => {
  closeModal();
  router.push(`/teacher/schedule/import`);
  router.refresh();
}, 1200);
```

Stitch 参考图要点：主页面底部是完整周课表；左侧是时间列，上方是工作日列，单元格为圆角 tonal 卡片堆叠，不新增独立 schedule shell，也不要回退到原始 HTML table + 硬边框风格。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为主课表页补齐完整周课表 runtime DTO</name>
  <files>src/features/schedule/shared/dto/runtime.ts, src/features/schedule/runtime/server.ts, src/features/schedule/runtime/server.test.ts</files>
  <behavior>
    - Test 1: `getTeacherDailyAgendaDTO()` 在保留现有 `cards` 合同的同时，额外返回可渲染周课表的矩阵数据（工作日列 + 节次行 + 单元格课程摘要）。
    - Test 2: 周课表数据只来自 normalized schedule model / runtime records，不读取 raw import rows，也不把导入审核字段泄漏到 UI DTO。
    - Test 3: 当前日期对应的工作日/单元格可带出现有状态或 override 摘要，但不存在课程的格子保持显式空态而不是 `any`/缺字段。
  </behavior>
  <action>在现有 runtime 边界内做最小 DTO 扩展：为 `TeacherDailyAgendaDTOSchema` 增加周课表结构（推荐 `weekdays` + `rows` 或等价命名），由 `src/features/schedule/runtime/server.ts` 基于已存在的 active term、assignment、bell slot、recurring entry、override 数据一次性拼出可渲染矩阵。不要新增 UI 直连 DB，不要读取 `scheduleImportRow`，也不要为这次 quick task 另起新 service abstraction；直接复用当前 runtime 查询结果，保证页面拿到的是 teacher-owned DTO。</action>
  <verify>
    <automated>pnpm vitest run src/features/schedule/runtime/server.test.ts</automated>
  </verify>
  <done>主课表页已有稳定 DTO 可直接渲染完整周课表，且 schedule runtime 三层边界未被打破。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把导入审核主流程收回 `/teacher/schedule` 并移除独立导入 destination</name>
  <files>src/app/(teacher)/teacher/schedule/page.tsx, src/app/(teacher)/teacher/schedule/import/page.tsx, src/components/surfaces/schedule-import-modal.tsx, src/components/surfaces/schedule-import-modal.test.tsx, src/components/surfaces/schedule-import-review-surface.tsx, src/components/surfaces/schedule-import-review-surface.test.tsx</files>
  <behavior>
    - Test 1: CSV 导入成功后 `ScheduleImportModal` 跳转到 `/teacher/schedule`，并刷新主页数据，而不是 `/teacher/schedule/import`。
    - Test 2: 访问旧 `/teacher/schedule/import` 时会立即重定向到 `/teacher/schedule`，不再作为主用户流终点。
    - Test 3: 最新导入批次的审核 UI 仍能在主课表页内联显示并继续使用现有 `approveScheduleImportAction` 阻断逻辑，不允许跳过 staging review 直接入库。
  </behavior>
  <action>保持现有导入/审核子域不拆散：`schedule-import-modal` 只改成功后的 destination；`/teacher/schedule/import/page.tsx` 改成 server redirect；`/teacher/schedule/page.tsx` 额外加载 `getLatestScheduleImportBatchDTO()`，并让 `schedule-import-review-surface` 支持“嵌入主页面 section”的最小模式（例如 `embedded`/`compact` prop 或等价 contract），复用当前审核内容与 approve action，而不是复制一套新审核逻辑。不要绕开审核直接批准，不要把审核流程改成 client-only 状态机。</action>
  <verify>
    <automated>pnpm vitest run src/components/surfaces/schedule-import-modal.test.tsx src/components/surfaces/schedule-import-review-surface.test.tsx</automated>
  </verify>
  <done>导入成功后用户停留在 `/teacher/schedule`，旧导入页退场，但 staging review 仍然保留在主课表体验内。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 在主课表页底部渲染 Stitch 对齐的完整周课表 section</name>
  <files>src/app/(teacher)/teacher/schedule/page.tsx, src/components/surfaces/teacher-schedule-surface.tsx, src/components/surfaces/teacher-schedule-surface.test.tsx</files>
  <behavior>
    - Test 1: `/teacher/schedule` 的最后一个 section 渲染完整周课表，包含时间列、工作日列以及课程单元格摘要。
    - Test 2: “导入课程表”快捷入口不再把用户送到 `/teacher/schedule/import`；同页 CTA 仍指向 modal 或页内锚点。
    - Test 3: 页面保留现有“今日 agenda”第一层信息顺序（时间 / 班级 / 地点 / 状态），并在其后追加周课表，而不是替换掉 runtime agenda 卡片。
  </behavior>
  <action>在 `TeacherScheduleSurface` 中按 Stitch 参考图补一个底部周课表 section：顶部是 section 标题与日期范围/说明，主体是“左侧时间轴 + 右侧工作日列”的矩阵布局；每个非空单元格展示 `courseTitle / classLabel / locationLabel`，必要时加现有 `status` 或 `overrideSummary` 作为小标签。视觉上继续复用 `teacherSurfaceRhythm`、tonal surface、圆角卡片和中文文案，不新增原生 DB 查询、不生成 schedule 专用新壳层，也不要退化成临时字符串列表。同步更新 surface 测试，并把现有“导入课程表”快捷卡改为同页交互（页内锚点或等价方案），确保主用户流不再依赖 `/teacher/schedule/import`。</action>
  <verify>
    <automated>pnpm vitest run src/components/surfaces/teacher-schedule-surface.test.tsx src/components/surfaces/schedule-import-modal.test.tsx src/features/schedule/runtime/server.test.ts</automated>
  </verify>
  <done>`/teacher/schedule` 同时承载今日 agenda、导入审核与底部完整周课表，且完整周课表布局与 Stitch 节奏一致。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teacher browser → schedule import modal | 教师在客户端上传 CSV 并触发导入批次创建 |
| schedule page server → runtime/import DTO | 主课表页同时消费 runtime DTO 与导入审核 DTO，必须保持边界清晰 |
| main schedule UI → approve action | 主页面内联审核不能绕过现有审核/批准约束 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-r3g-01 | T | 主课表页内联审核 | mitigate | 继续复用 `approveScheduleImportAction` 与现有 batch/row 状态 gate，不新增“导入即批准”捷径 |
| T-quick-r3g-02 | I | 周课表 DTO | mitigate | 只暴露 runtime 所需课程摘要字段，禁止把 raw import payload / validation issues 混入周课表单元格 DTO |
| T-quick-r3g-03 | D | `/teacher/schedule` 汇总页 | accept | 本次仅增加同页读取与渲染；通过复用现有 server DTO、定向测试与最小布局扩展控制风险 |
</threat_model>

<verification>
1. 运行 runtime、modal、review surface、teacher schedule surface 的定向 Vitest。
2. 手动在 `/teacher/schedule` 上传一份合法 CSV，确认 1.2 秒后回到主课表页而不是旧导入页。
3. 确认主页面能看到最新导入审核区，并且底部最后一个 section 为完整周课表。
4. 直接访问 `/teacher/schedule/import`，确认被重定向回 `/teacher/schedule`。
</verification>

<success_criteria>
- `/teacher/schedule/import` 不再作为用户完成导入后的落点。
- `/teacher/schedule` 内仍保有 Phase 18 要求的 staging review，而不是跳过审核直接入库。
- 主课表页最后一个 section 显示完整周课表，字段来源仍是 schedule runtime DTO。
- 本 quick task 没有引入 UI 直连 DB、raw import 泄漏或新的跨域 abstraction。
</success_criteria>
