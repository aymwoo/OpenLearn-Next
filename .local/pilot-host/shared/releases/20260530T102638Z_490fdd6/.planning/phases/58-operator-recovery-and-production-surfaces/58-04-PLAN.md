---
phase: 58-operator-recovery-and-production-surfaces
plan: "04"
type: execute
wave: 5
depends_on:
  - "58-01"
  - "58-02"
  - "58-03"
  - "58-05"
  - "58-06"
files_modified:
  - scripts/verify-phase58-operator-recovery-and-surfaces.ts
  - scripts/verify-phase58-operator-recovery-and-surfaces.test.ts
  - scripts/proof-phase58-operator-recovery.ts
  - package.json
  - .planning/phases/58-operator-recovery-and-production-surfaces/58-OPERATOR-RUNBOOK.md
autonomous: true
requirements:
  - OPS-02
  - OPS-03
  - PLUG-03
  - SAFE-02
must_haves:
  truths:
    - "Phase 58 关闭时，operator recovery surface 有单命令 verifier、repo-local proof 和 support runbook excerpt。"
    - "验证脚本会硬性检查 honesty template、incident-first entry、reason-gated recovery 与 no-DB-surgery 路径。"
  artifacts:
    - path: scripts/verify-phase58-operator-recovery-and-surfaces.ts
      provides: phase-scoped verifier
    - path: scripts/verify-phase58-operator-recovery-and-surfaces.test.ts
      provides: verifier self-test
    - path: scripts/proof-phase58-operator-recovery.ts
      provides: operator walkthrough proof script
    - path: .planning/phases/58-operator-recovery-and-production-surfaces/58-OPERATOR-RUNBOOK.md
      provides: support-facing runbook excerpt
  key_links:
    - from: package.json
      to: scripts/verify-phase58-operator-recovery-and-surfaces.ts
      via: verify:phase58
      pattern: verify:phase58
    - from: package.json
      to: scripts/proof-phase58-operator-recovery.ts
      via: proof:phase58
      pattern: proof:phase58
---

<objective>
给 Phase 58 增加 close gate：focused verifier、operator walkthrough proof、support runbook excerpt。

Purpose: 把 operator recovery 与 degraded honesty 从“看起来实现了”收口为可复查 artifact，满足 proof inventory 与 SAFE-02 的硬门。
Output: `verify:phase58`、`proof:phase58`、runbook excerpt、verifier self-tests。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-pilot-scope-and-acceptance-gate/55-PROOF-INVENTORY.md
@.planning/phases/55-pilot-scope-and-acceptance-gate/55-FAILURE-RECOVERY-MATRIX.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-VALIDATION.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-05-PLAN.md
@.planning/phases/58-operator-recovery-and-production-surfaces/58-06-PLAN.md
@src/components/surfaces/classroom-incident-operator-surface.tsx
@src/components/surfaces/settings-surface.tsx
@src/components/classroom/classroom-control-panel.tsx
@scripts/verify-phase42-operator-recovery.ts
@scripts/verify-phase52-action-registry-and-lifecycle.ts
@scripts/verify-phase57-classroom-runtime.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 Phase 58 verifier 与脚本自测</name>
  <files>scripts/verify-phase58-operator-recovery-and-surfaces.ts, scripts/verify-phase58-operator-recovery-and-surfaces.test.ts, package.json</files>
  <behavior>
    - Test 1: verifier script 自测锁定 `verify:phase58` script entry、focused suite 列表、proof hard gate wiring。
    - Test 2: verifier 的静态检查必须断言 incident-first Settings Labs、honesty 三段模板、summary surface 不含 full timeline、classroom shell 无高风险 quick path。
    - Test 3: verifier 要检查 recovery 路径继续走 existing server-owned seams，不允许 DB surgery 文案或旁路 mutation。
  </behavior>
  <action>参照 Phase 42 / 52 / 57 verifier 风格，新增 `verify-phase58-operator-recovery-and-surfaces.ts` 与自测文件，并在 `package.json` 注册 `verify:phase58`。静态检查至少覆盖：incident routes 存在、`/settings/labs` first screen 是 incidents、honesty template 顺序固定、command detail route 存在、`ClassroomControlPanel` 不再直接执行 `resume` / `suspend` / `fallback`、以及 recovery surfaces 继续调用现有 Server Actions / command bus / DAL seams。</action>
  <verify>
    <automated>pnpm exec vitest --run scripts/verify-phase58-operator-recovery-and-surfaces.test.ts</automated>
  </verify>
  <done>`verify:phase58` 已注册且有自测，静态 gate 可以阻止 Phase 58 回退到 tool-first、badge-only 或 DB-surgery 式实现。</done>
</task>

<task type="auto">
  <name>Task 2: 补 operator walkthrough proof 与 support runbook excerpt</name>
  <files>scripts/proof-phase58-operator-recovery.ts, package.json, .planning/phases/58-operator-recovery-and-production-surfaces/58-OPERATOR-RUNBOOK.md</files>
  <action>新增 `proof:phase58` repo-local proof，至少覆盖两个标准场景：一个 plugin failure（如 activation_failed / plugin disabled）和一个 transport/worker degraded 场景，要求从 classroom incident 或 Settings Labs incident list 进入，完成 operator 下钻与恢复动作验证。同步写 `58-OPERATOR-RUNBOOK.md`，给出 classroom voting incident handling 的最小 support runbook：入口、可信边界、不可执行动作的正确下一步、标准恢复动作、无需 DB surgery 的证据点。runbook 内容必须回挂 55-FAILURE-RECOVERY-MATRIX 与 55-PROOF-INVENTORY，不写泛泛值守话术。</action>
  <verify>
    <automated>pnpm verify:phase58</automated>
  </verify>
  <done>Phase 58 具备单命令 verifier、repo-local proof 与 support-facing runbook excerpt；operator recovery close gate 可复跑、可复查、可交接。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier script → repository source | 只读静态检查与 focused tests，防止 Phase 58 行为回退 |
| proof script → operator surfaces | 必须沿正式 UI 与 server-owned seams 走恢复路径，不允许直接改库准备“成功结果” |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-10 | R | verifier / proof artifacts | mitigate | 用 script entry、自测、proof hard gate 和 runbook excerpt 固定 closeout evidence |
| T-58-11 | T | proof setup | mitigate | proof 只允许 repo-local truth preparation + 正式 UI/Server Action 路径，不写 DB surgery 恢复步骤 |
| T-58-12 | I | degraded honesty regressions | mitigate | verifier 静态断言固定三段模板、incident-first entry、disabled+reason action posture |
</threat_model>

<verification>
- `pnpm exec vitest --run scripts/verify-phase58-operator-recovery-and-surfaces.test.ts`
- `pnpm verify:phase58`
- `pnpm proof:phase58`
</verification>

<success_criteria>
- `package.json` 包含 `verify:phase58` 与 `proof:phase58`。
- verifier 会硬性检查 incident-first IA、honesty template、reason-gated recovery、no DB surgery。
- `58-OPERATOR-RUNBOOK.md` 存在并给出 plugin failure 与 transport/worker degraded 的标准处置入口与恢复动作。
</success_criteria>

<output>
After completion, create `.planning/phases/58-operator-recovery-and-production-surfaces/58-04-SUMMARY.md`
</output>
