---
phase: 50-boundary-freeze-and-platform-vocabulary
plan: "03"
type: execute
wave: 2
depends_on:
  - "50-01"
files_modified:
  - .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-DEFERRED-WALL.md
  - .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-PHASE-HANDOFF.md
autonomous: true
requirements:
  - BOUND-04
  - BOUND-02
must_haves:
  truths:
    - "维护者可以看到 named hard exclusions，而不是只看到模糊的 out-of-scope 口径。"
    - "Phase 51-54 有明确 handoff guardrails，知道哪些冻结边界不能回退。"
  artifacts:
    - path: .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-DEFERRED-WALL.md
      provides: named hard exclusions for v3.0 committed scope
    - path: .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-PHASE-HANDOFF.md
      provides: downstream consumption rules and forbidden shortcuts
  key_links:
    - from: 50-DEFERRED-WALL.md
      to: 50-PHASE-HANDOFF.md
      via: deferred exclusions enforced in downstream phase guidance
---

<objective>
把高风险能力的 deferred wall 与后续 Phase 51-54 的消费规则正式写成 guardrail 文档。

Purpose: 防止未来 planning/execution 在“顺手补一下”的名义下，把 QuickJS、Extension Host、PostgreSQL、Workflow Engine 等高风险能力偷带进 v3.0 committed scope。
Output: `50-DEFERRED-WALL.md` 与 `50-PHASE-HANDOFF.md`。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-boundary-freeze-and-platform-vocabulary/50-BOUNDARY-CONTRACT.md
@.planning/phases/50-boundary-freeze-and-platform-vocabulary/50-OWNERSHIP-MAP.md
@.planning/phases/50-boundary-freeze-and-platform-vocabulary/50-CONTEXT.md
@.planning/phases/50-boundary-freeze-and-platform-vocabulary/50-RESEARCH.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
</context>

<must_haves>
  <truths>
    <truth>deferred items 被点名式写出，后续 phase 无法用抽象措辞绕过。</truth>
    <truth>Phase 51-54 的消费顺序与禁止 shortcut 被正式写入 handoff 文档。</truth>
  </truths>
  <artifacts>
    <artifact path=".planning/phases/50-boundary-freeze-and-platform-vocabulary/50-DEFERRED-WALL.md" />
    <artifact path=".planning/phases/50-boundary-freeze-and-platform-vocabulary/50-PHASE-HANDOFF.md" />
  </artifacts>
  <key_links>
    <link>Deferred wall 中的命名排除项必须在 handoff 文档中再次被当作 forbidden shortcut 引用。</link>
  </key_links>
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: 写出 named hard exclusions deferred wall</name>
  <files>.planning/phases/50-boundary-freeze-and-platform-vocabulary/50-DEFERRED-WALL.md</files>
  <read_first>
    - .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-CONTEXT.md
    - .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-RESEARCH.md
    - .planning/research/SUMMARY.md
    - .planning/research/PITFALLS.md
    - .planning/ROADMAP.md
  </read_first>
  <action>创建 `50-DEFERRED-WALL.md`，必须包含 `## Named Hard Exclusions`、`## Why These Stay Deferred`、`## Anti-Smuggling Rule` 三个章节。`## Named Hard Exclusions` 必须逐行点名写出以下排除项：`QuickJS sandbox`、`Extension Host`、`PostgreSQL / pgvector cutover`、`Workflow Engine / Temporal`、`full Agent Runtime / Skill Runtime`、`distributed event bus`、`event sourcing rewrite`（per D-50-15）。在 `## Anti-Smuggling Rule` 中写出精确句子 `No Phase 51-54 plan may introduce these capabilities as prerequisite, scaffold, optional runtime, or hidden fallback.`（per D-50-14）。不要把这些排除项改写成模糊的“重型能力”“未来增强”之类抽象表述。</action>
  <acceptance_criteria>
    - 文件存在且包含 `## Named Hard Exclusions`。
    - 七个 named exclusions 全部以显式条目出现。
    - 文件内出现精确句子 `No Phase 51-54 plan may introduce these capabilities as prerequisite, scaffold, optional runtime, or hidden fallback.`。
  </acceptance_criteria>
  <verify>
    <automated>rg -n '^(## Named Hard Exclusions|## Why These Stay Deferred|## Anti-Smuggling Rule|- QuickJS sandbox|- Extension Host|- PostgreSQL / pgvector cutover|- Workflow Engine / Temporal|- full Agent Runtime / Skill Runtime|- distributed event bus|- event sourcing rewrite|No Phase 51-54 plan may introduce these capabilities as prerequisite, scaffold, optional runtime, or hidden fallback\.)$' ".planning/phases/50-boundary-freeze-and-platform-vocabulary/50-DEFERRED-WALL.md"</automated>
  </verify>
  <done>`50-DEFERRED-WALL.md` 成为 BOUND-04 的正式引用源，后续 phase 不能再把高风险能力以模糊措辞带入 committed scope。</done>
</task>

<task type="auto">
  <name>Task 2: 写出 Phase 51-54 downstream handoff guardrails</name>
  <files>.planning/phases/50-boundary-freeze-and-platform-vocabulary/50-PHASE-HANDOFF.md</files>
  <read_first>
    - .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-BOUNDARY-CONTRACT.md
    - .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-OWNERSHIP-MAP.md
    - .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-DEFERRED-WALL.md
    - .planning/ROADMAP.md
    - .planning/research/ARCHITECTURE.md
  </read_first>
  <action>创建 `50-PHASE-HANDOFF.md`，包含 `## Phase 51 consumes`、`## Phase 52 consumes`、`## Phase 53 consumes`、`## Phase 54 consumes`、`## Forbidden Shortcuts Across Phases` 五个章节。逐章写明每个 phase 只能消费哪些冻结结论，并加入以下精确 guardrail 句子：`Phase 51 must treat Server Actions, plugin host, and async processors as PlatformCommand producers, not alternative mutation authorities.`、`Phase 52 must not restore dynamic action authority to src/server/plugins/registry.ts.`、`Phase 53 must create a dedicated platform event outbox and must not reuse runtimeEventOutbox as platform event truth.`、`Phase 54 must expose descriptors and delegated metadata only; it must not imply full Agent Runtime / Skill Runtime.`。在 `## Forbidden Shortcuts Across Phases` 中再写一条 `Legacy seams may forward, annotate, or adapt; they may not reclaim authoritative ownership.`。</action>
  <acceptance_criteria>
    - 文件存在且包含四个 `Phase XX consumes` 章节与 `## Forbidden Shortcuts Across Phases`。
    - 四条 phase-specific guardrail 句子全部原样出现。
    - 文件内出现精确句子 `Legacy seams may forward, annotate, or adapt; they may not reclaim authoritative ownership.`。
  </acceptance_criteria>
  <verify>
    <automated>rg -n '^(## Phase 51 consumes|## Phase 52 consumes|## Phase 53 consumes|## Phase 54 consumes|## Forbidden Shortcuts Across Phases|Phase 51 must treat Server Actions, plugin host, and async processors as PlatformCommand producers, not alternative mutation authorities\.|Phase 52 must not restore dynamic action authority to src/server/plugins/registry.ts\.|Phase 53 must create a dedicated platform event outbox and must not reuse runtimeEventOutbox as platform event truth\.|Phase 54 must expose descriptors and delegated metadata only; it must not imply full Agent Runtime / Skill Runtime\.|Legacy seams may forward, annotate, or adapt; they may not reclaim authoritative ownership\.)$' ".planning/phases/50-boundary-freeze-and-platform-vocabulary/50-PHASE-HANDOFF.md"</automated>
  </verify>
  <done>后续 Phase 51-54 的 planner / executor 拿到正式 handoff guardrails，知道哪些 frozen decisions 必须消费、哪些 shortcut 被禁止。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 50 freeze docs → later roadmap phases | 若 deferred wall 不够硬，后续 phase 会把高风险能力偷渡进 committed scope |
| handoff guardrails → later implementation plans | 若没有 forbidden shortcuts，legacy seams 可能重新夺回 authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-07 | T | deferred wall | mitigate | 用 named hard exclusions 明确点名 QuickJS、Extension Host、PostgreSQL / pgvector、Workflow Engine / Temporal、full Agent Runtime / Skill Runtime、distributed event bus、event sourcing rewrite。 |
| T-50-08 | E | downstream phases | mitigate | 在 handoff 中逐 phase 写出 forbidden shortcuts，防止旧 seam 或高风险 substrate 重新被默认为 authority。 |
| T-50-09 | R | milestone scope | mitigate | 写出 anti-smuggling rule 与 phase-specific guardrails，确保后续 scope 漂移可被直接比对并追责。 |
</threat_model>

<verification>
执行两条 `rg` 命令确认 named exclusions 与 downstream guardrail 句子全部存在；再人工检查文档没有出现“optional runtime scaffold for later”之类软化 deferred wall 的措辞。</verification>

<success_criteria>
- `50-DEFERRED-WALL.md` 使 BOUND-04 成为可 grep、可 review 的正式 artifact。
- `50-PHASE-HANDOFF.md` 为 Phase 51-54 提供不可误读的消费边界。
- 任何后续 plan 若想引入高风险能力，都必须先显式推翻 Phase 50 artifact，而不能悄悄附带进入。
</success_criteria>

<output>
After completion, create `.planning/phases/50-boundary-freeze-and-platform-vocabulary/50-03-SUMMARY.md`
</output>
