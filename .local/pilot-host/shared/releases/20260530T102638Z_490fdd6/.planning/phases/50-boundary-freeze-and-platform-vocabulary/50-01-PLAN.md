---
phase: 50-boundary-freeze-and-platform-vocabulary
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-BOUNDARY-CONTRACT.md
  - .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-OWNERSHIP-MAP.md
autonomous: true
requirements:
  - BOUND-01
  - BOUND-02
  - BOUND-03
must_haves:
  truths:
    - "平台维护者可以读到冻结后的 command/action/event/task/runtime transport 语义，而不是靠口头约定理解。"
    - "平台维护者可以看到 platform-core 与现有 ad-hoc seams 的正式 ownership 重划。"
    - "平台维护者可以明确看到 SQLite + DAL 是 canonical truth，BullMQ/Redis/WebSocket 只是 delivery 或 orchestration substrate。"
  artifacts:
    - path: .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-BOUNDARY-CONTRACT.md
      provides: 冻结 vocabulary、command entry boundary 与 canonical truth posture
    - path: .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-OWNERSHIP-MAP.md
      provides: current anchor 到 future authoritative owner 的正式映射
  key_links:
    - from: 50-BOUNDARY-CONTRACT.md
      to: 50-OWNERSHIP-MAP.md
      via: 共享 vocabulary 与 ownership 术语
    - from: 50-OWNERSHIP-MAP.md
      to: src/features/platform-core/
      via: authoritative ownership definition
    - from: 50-OWNERSHIP-MAP.md
      to: src/db/schema.ts
      via: runtimeEventOutbox durable-anchor posture freeze
---

<objective>
冻结 Phase 50 的权威文档输入：统一 vocabulary、command producer boundary、canonical truth posture、以及 platform-core ownership map。

Purpose: 先把术语和 owner 写死，阻止后续 Phase 51-54 再把旧入口当半权威 seam 继续扩张。
Output: `50-BOUNDARY-CONTRACT.md` 与 `50-OWNERSHIP-MAP.md` 两份可直接被后续 planner / executor 引用的正式文档。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/50-boundary-freeze-and-platform-vocabulary/50-CONTEXT.md
@.planning/phases/50-boundary-freeze-and-platform-vocabulary/50-RESEARCH.md
@.planning/phases/50-boundary-freeze-and-platform-vocabulary/50-PATTERNS.md
@.planning/phases/44-plugin-identity-and-namespace-contract/44-CONTEXT.md
@.planning/phases/43-additional-validation-workloads-and-milestone-proof/43-CONTEXT.md
@.planning/phases/42-operator-visibility-and-recovery/42-CONTEXT.md
</context>

<must_haves>
  <truths>
    <truth>维护者能看到单一 vocabulary，而不是把 command、action、event、task、runtime transport 混写。</truth>
    <truth>维护者能看到 `src/features/platform-core/` 是唯一 future authoritative orchestration layer。</truth>
    <truth>维护者能看到 SQLite + DAL 与 BullMQ/Redis/WebSocket 的角色分层被正式写死。</truth>
  </truths>
  <artifacts>
    <artifact path=".planning/phases/50-boundary-freeze-and-platform-vocabulary/50-BOUNDARY-CONTRACT.md" />
    <artifact path=".planning/phases/50-boundary-freeze-and-platform-vocabulary/50-OWNERSHIP-MAP.md" />
  </artifacts>
  <key_links>
    <link>Vocabulary contract 中的术语必须与 ownership map 中的 owner 命名完全一致。</link>
    <link>Ownership map 必须把现有文件降级为 adapter-only / catalog-only / runtime-only / DAL-only posture。</link>
  </key_links>
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: 写出冻结 vocabulary 与 canonical truth contract</name>
  <files>.planning/phases/50-boundary-freeze-and-platform-vocabulary/50-BOUNDARY-CONTRACT.md</files>
  <read_first>
    - .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-CONTEXT.md
    - .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-RESEARCH.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/research/SUMMARY.md
    - .planning/research/PITFALLS.md
  </read_first>
  <action>创建 `50-BOUNDARY-CONTRACT.md`，只写 contract，不写实现设计。文件必须包含 `## Frozen Vocabulary`、`## Command Entry Boundary`、`## Canonical Truth Posture` 三个一级章节；并逐字写入以下 vocabulary 行：`command = authoritative mutation request`、`action = discoverable capability unit`、`event = after-fact fact`、`task = deferred execution / orchestration unit`、`runtime transport = delivery mechanism`。在 `## Command Entry Boundary` 下明确写出 `Server Actions`、`plugin host`、`async task processors` 都是 future `PlatformCommand` producers（per D-50-05/D-50-07）。在 `## Canonical Truth Posture` 下明确写出 `SQLite + DAL = canonical truth` 与 `BullMQ / Redis / WebSocket = delivery / orchestration substrate only`（per D-50-13），并写明 runtime transport 不是 platform event bus（per D-50-12）。不要写 command bus、action registry、event outbox 的任何运行时代码方案。</action>
  <acceptance_criteria>
    - `50-BOUNDARY-CONTRACT.md` 存在且包含 `## Frozen Vocabulary`、`## Command Entry Boundary`、`## Canonical Truth Posture`。
    - 文件内出现精确字符串 `command = authoritative mutation request`。
    - 文件内出现精确字符串 `runtime transport = delivery mechanism`。
    - 文件内出现精确字符串 `SQLite + DAL = canonical truth`。
  </acceptance_criteria>
  <verify>
    <automated>rg -n '^(## Frozen Vocabulary|## Command Entry Boundary|## Canonical Truth Posture|command = authoritative mutation request|action = discoverable capability unit|event = after-fact fact|task = deferred execution / orchestration unit|runtime transport = delivery mechanism|SQLite \+ DAL = canonical truth|BullMQ / Redis / WebSocket = delivery / orchestration substrate only)$' ".planning/phases/50-boundary-freeze-and-platform-vocabulary/50-BOUNDARY-CONTRACT.md"</automated>
  </verify>
  <done>`50-BOUNDARY-CONTRACT.md` 成为后续 phases 可直接引用的 vocabulary / truth contract，且没有混入实现 scope。</done>
</task>

<task type="auto">
  <name>Task 2: 写出 current-anchor 到 future authority 的 ownership map</name>
  <files>.planning/phases/50-boundary-freeze-and-platform-vocabulary/50-OWNERSHIP-MAP.md</files>
  <read_first>
    - .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-CONTEXT.md
    - .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-RESEARCH.md
    - .planning/phases/50-boundary-freeze-and-platform-vocabulary/50-PATTERNS.md
    - .planning/research/ARCHITECTURE.md
    - .planning/research/PITFALLS.md
  </read_first>
  <action>创建 `50-OWNERSHIP-MAP.md`，包含 `## Future Authoritative Ownership`、`## Current Anchor Reclassification`、`## Non-Authoritative Legacy Seams` 三个章节。`## Future Authoritative Ownership` 必须逐行列出：`src/features/platform-core/commands -> authoritative command execution`、`src/features/platform-core/actions -> authoritative action registry metadata`、`src/features/platform-core/plugins -> authoritative lifecycle orchestration`、`src/features/platform-core/events -> authoritative platform event outbox`（per D-50-01）。`## Current Anchor Reclassification` 必须逐行列出：`src/actions/plugin-actions.ts -> future PlatformCommand producer adapter`、`src/lib/dal/plugins.ts -> plugin domain DAL only`、`src/server/plugins/registry.ts -> static implementation catalog only`、`src/features/runtime-platform/seams/event-bus/* -> runtime-only transport seam`、`src/db/schema.ts -> runtimeEventOutbox = runtime-only durable anchor, not platform event truth`。明确写出这些旧文件与 durable anchor 都不再被视为 authoritative owner；尤其要把 `runtimeEventOutbox` 定性为 runtime delivery 账本锚点，而不是 future platform event outbox truth（per D-50-02/D-50-03/D-50-04/D-50-06/D-50-12）。</action>
  <acceptance_criteria>
    - `50-OWNERSHIP-MAP.md` 存在且包含 `## Future Authoritative Ownership` 与 `## Current Anchor Reclassification`。
    - 文件内出现精确字符串 `src/features/platform-core/commands -> authoritative command execution`。
    - 文件内出现精确字符串 `src/lib/dal/plugins.ts -> plugin domain DAL only`。
    - 文件内出现精确字符串 `src/features/runtime-platform/seams/event-bus/* -> runtime-only transport seam`。
    - 文件内出现精确字符串 `src/db/schema.ts -> runtimeEventOutbox = runtime-only durable anchor, not platform event truth`。
  </acceptance_criteria>
  <verify>
    <automated>rg -n '^(## Future Authoritative Ownership|## Current Anchor Reclassification|## Non-Authoritative Legacy Seams|src/features/platform-core/commands -> authoritative command execution|src/features/platform-core/actions -> authoritative action registry metadata|src/features/platform-core/plugins -> authoritative lifecycle orchestration|src/features/platform-core/events -> authoritative platform event outbox|src/actions/plugin-actions.ts -> future PlatformCommand producer adapter|src/lib/dal/plugins.ts -> plugin domain DAL only|src/server/plugins/registry.ts -> static implementation catalog only|src/features/runtime-platform/seams/event-bus/\* -> runtime-only transport seam|src/db/schema.ts -> runtimeEventOutbox = runtime-only durable anchor, not platform event truth)$' ".planning/phases/50-boundary-freeze-and-platform-vocabulary/50-OWNERSHIP-MAP.md"</automated>
  </verify>
  <done>`50-OWNERSHIP-MAP.md` 明确 future authoritative owner 与现有 legacy seams 的降级角色，后续 plan 不再需要自行猜测 owner。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs → downstream platform phases | 若术语或 owner 不冻结，后续 phases 会把旧 seam 重新扩张成 authority |
| canonical truth → delivery substrate | 若 SQLite/DAL 与 queue/transport 的角色混淆，会制造第二真相源 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | R | vocabulary docs | mitigate | 在 `50-BOUNDARY-CONTRACT.md` 中写死 command/action/event/task/runtime transport 的精确语义，避免后续无法追责的术语漂移。 |
| T-50-02 | T | canonical truth posture | mitigate | 在 contract 中明确 `SQLite + DAL = canonical truth`，并把 BullMQ/Redis/WebSocket 限定为 delivery/orchestration substrate only。 |
| T-50-03 | E | ownership map | mitigate | 在 `50-OWNERSHIP-MAP.md` 中把旧入口明确降级为 adapter-only / DAL-only / catalog-only / runtime-only，阻止 legacy seam 越权继续扩张。 |
</threat_model>

<verification>
执行后重新运行两条 `rg` 验证命令，确认 vocabulary、truth posture、ownership map 的精确字符串全部存在；再人工检查两份文档没有出现 command bus、outbox、registry implementation 级任务描述。</verification>

<success_criteria>
- `50-BOUNDARY-CONTRACT.md` 成为 BOUND-01 与 BOUND-03 的权威引用源。
- `50-OWNERSHIP-MAP.md` 成为 BOUND-02 的权威引用源。
- 两份文档都只冻结边界，不偷带 Phase 51-54 的实现 scope。
</success_criteria>

<output>
After completion, create `.planning/phases/50-boundary-freeze-and-platform-vocabulary/50-01-SUMMARY.md`
</output>
