---
phase: 16-theme-plugins-and-layout-orchestration
plan: 03
type: execute
wave: 3
depends_on:
  - 16-01
  - 16-02
files_modified:
  - src/components/shell/sidebar.tsx
  - src/components/shell/glass-nav.tsx
  - src/components/shell/teacher-sidebar-shell.tsx
  - src/app/(teacher)/teacher/layout.tsx
  - src/app/settings/layout.tsx
  - src/app/(library)/resources/layout.tsx
  - src/components/shell/teacher-sidebar-shell.test.tsx
autonomous: true
requirements:
  - PLUGIN-05
  - PLUGIN-06
must_haves:
  truths:
    - D-01/D-02/D-03: 教师端可以在左侧导航、顶部导航、顶部导航加左侧辅栏三种壳层之间切换，并支持 workspace 默认壳层与按页覆写
    - D-04/D-05/D-16: teacher-facing routes 全部通过 allowlisted surface contract 进入 theme-aware shell，且继续走既有主题运行链路
    - D-06/D-07/D-10/D-11: 页面编排只改变 page-level region 和 module placement，不重排业务组件内部结构
  artifacts:
    - path: src/app/(teacher)/teacher/layout.tsx
      provides: Theme-aware teacher shell runtime entry
      contains: top-nav
    - path: src/components/shell/teacher-sidebar-shell.tsx
      provides: Reusable shell component with default fallback behavior
      contains: secondary-nav
    - path: src/components/shell/teacher-sidebar-shell.test.tsx
      provides: Shell regression coverage
      contains: top-nav-secondary-rail
  key_links:
    - from: src/app/(teacher)/teacher/layout.tsx
      to: src/components/theme/theme-injector.tsx
      via: runtime metadata drives shell mode selection
      pattern: theme-layout-runtime|top-nav
    - from: src/app/settings/layout.tsx
      to: src/components/shell/teacher-sidebar-shell.tsx
      via: shared teacher-facing shell wrapper
      pattern: TeacherSidebarShell
---

<objective>
Apply the compiled theme layout runtime to the actual teacher-facing shell and route wrappers.

Purpose: Deliver D-01 through D-07 and D-16 through D-19 in the rendered teacher workspace so
themes can change navigation placement and page-level structure without opening arbitrary UI
composition.
Output: Theme-aware shell components, route wrapper adoption for teacher/settings/resources,
and focused shell regression tests.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-theme-plugins-and-layout-orchestration/16-CONTEXT.md
@.planning/phases/16-theme-plugins-and-layout-orchestration/16-RESEARCH.md
@.planning/phases/16-theme-plugins-and-layout-orchestration/16-UI-SPEC.md
@.planning/phases/16-theme-plugins-and-layout-orchestration/16-02-PLAN.md
@src/app/(teacher)/teacher/layout.tsx
@src/components/shell/sidebar.tsx
@src/components/shell/glass-nav.tsx
@src/components/shell/teacher-sidebar-shell.tsx

<interfaces>
From src/components/shell/sidebar.tsx:
```ts
type SidebarProps = {
  items: readonly SidebarItem[] | readonly { label: string; href: string; emphasis?: string }[];
  activePath?: string;
  title?: string;
};
```

From src/components/shell/glass-nav.tsx:
```tsx
export function GlassNav() {
  return <header ...><nav aria-label="主导航" ... /></header>;
}
```

From src/components/shell/teacher-sidebar-shell.tsx:
```tsx
export function TeacherSidebarShell({ children }: TeacherSidebarShellProps)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor shell primitives to consume theme shell modes and required regions</name>
  <files>src/components/shell/sidebar.tsx, src/components/shell/glass-nav.tsx, src/components/shell/teacher-sidebar-shell.tsx, src/components/shell/teacher-sidebar-shell.test.tsx</files>
  <read_first>src/components/shell/sidebar.tsx, src/components/shell/glass-nav.tsx, src/components/shell/teacher-sidebar-shell.tsx, src/components/shell/teacher-sidebar-shell.test.tsx, .planning/phases/16-theme-plugins-and-layout-orchestration/16-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: the shell primitives can render `left-nav`, `top-nav`, and `top-nav-secondary-rail` from runtime metadata.
    - Test 2: `primary-nav`, `page-header`, and `main-content` remain present in every mode.
    - Test 3: `secondary-nav` and `context-panel` only appear when the compiled runtime enables them.
  </behavior>
  <action>Refactor the shell primitives per D-01, D-02, D-06, D-09, D-10, and D-19. Update `src/components/shell/sidebar.tsx` and `src/components/shell/glass-nav.tsx` so they can serve as allowlisted `primary-nav` renderers in different shell modes. Expand `src/components/shell/teacher-sidebar-shell.tsx` into a theme-aware shell component that accepts compiled runtime metadata, renders `left-nav`, `top-nav`, or `top-nav-secondary-rail`, keeps `page-header` and `main-content` stable, and uses `secondary-nav` only as an allowlisted rail rather than a freeform slot system. Update `src/components/shell/teacher-sidebar-shell.test.tsx` to assert all three shell modes plus default fallback behavior. Do not let the shell component accept arbitrary React nodes for region injection beyond the allowlisted children/main-content contract.</action>
  <acceptance_criteria>
    - `src/components/shell/teacher-sidebar-shell.tsx` contains `left-nav`, `top-nav`, and `top-nav-secondary-rail`.
    - `src/components/shell/teacher-sidebar-shell.tsx` contains `primary-nav`, `page-header`, and `main-content`.
    - `src/components/shell/teacher-sidebar-shell.test.tsx` contains `top-nav-secondary-rail` and a fallback assertion.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/shell/teacher-sidebar-shell.test.tsx</automated>
  </verify>
  <done>The shared shell primitives can express every locked shell mode while preserving required regions and a stable default fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire teacher-facing route wrappers to the theme-aware shell</name>
  <files>src/app/(teacher)/teacher/layout.tsx, src/app/settings/layout.tsx, src/app/(library)/resources/layout.tsx</files>
  <read_first>src/app/(teacher)/teacher/layout.tsx, src/app/settings/layout.tsx, src/app/(library)/resources/layout.tsx, src/components/shell/teacher-sidebar-shell.tsx, src/components/theme/theme-injector.tsx</read_first>
  <behavior>
    - Test 1: all teacher-facing route wrappers resolve shell mode from the compiled runtime rather than hardcoded left-nav layout.
    - Test 2: routes without page-specific overrides still render through the default teacher shell contract.
    - Test 3: settings and resources stay on the same teacher-facing theme path instead of forking into separate layout logic.
  </behavior>
  <action>Update the route wrappers per D-03, D-04, D-05, D-07, and D-16. Replace the hardcoded shell structure inside `src/app/(teacher)/teacher/layout.tsx` with a consumer of the compiled theme runtime from Plan 02, so every route under `/teacher/**` passes through the allowlisted page-surface contract. Keep `/settings` and `/resources` on the same shell path by updating `src/app/settings/layout.tsx` and `src/app/(library)/resources/layout.tsx` to use the new theme-aware shell with route-surface keys instead of frozen left-sidebar behavior. Preserve current default layout when no valid theme runtime override exists. Do not special-case editor, launch, or runtime pages out of the registry; they must receive default or explicit page contracts through the same path.</action>
  <acceptance_criteria>
    - `src/app/(teacher)/teacher/layout.tsx` no longer hardcodes a single sidebar-only shell without theme runtime selection.
    - `src/app/(teacher)/teacher/layout.tsx` contains a runtime-driven shell mode branch or resolver.
    - `src/app/settings/layout.tsx` and `src/app/(library)/resources/layout.tsx` both still contain `TeacherSidebarShell` or its upgraded theme-aware equivalent.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('src/app/(teacher)/teacher/layout.tsx','utf8');const s=fs.readFileSync('src/app/settings/layout.tsx','utf8');const r=fs.readFileSync('src/app/(library)/resources/layout.tsx','utf8');if(!t.includes('top-nav')||(!s.includes('TeacherSidebarShell')&&!s.includes('ThemeAwareTeacherShell'))||(!r.includes('TeacherSidebarShell')&&!r.includes('ThemeAwareTeacherShell')))process.exit(1)"</automated>
  </verify>
  <done>Teacher, settings, and resources route families all render through the same compiled theme shell contract, with page-level overrides available across the whole teacher-facing surface registry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime metadata → shell components | Compiled theme layout data changes navigation position and region visibility |
| shared shell → route wrappers | Teacher/settings/resources pages inherit the same theme-aware shell behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-09 | T | `src/components/shell/teacher-sidebar-shell.tsx` | mitigate | Render only the three allowlisted shell modes and required regions instead of arbitrary layout trees. |
| T-16-10 | D | `src/app/(teacher)/teacher/layout.tsx` | mitigate | Preserve the current sidebar shell as the runtime fallback when no valid page override exists. |
| T-16-11 | E | `src/app/settings/layout.tsx` / `src/app/(library)/resources/layout.tsx` | mitigate | Keep all teacher-facing routes on the same compiled shell path so no separate layout bypass is introduced. |
| T-16-12 | I | `src/components/shell/sidebar.tsx` / `src/components/shell/glass-nav.tsx` | accept | Navigation labels are already public UI data; changing position does not expose new sensitive information. |
</threat_model>

<verification>
- `pnpm test --run src/components/shell/teacher-sidebar-shell.test.tsx`
- `node -e "const fs=require('fs');const s=fs.readFileSync('src/components/shell/teacher-sidebar-shell.tsx','utf8');if(!s.includes('secondary-nav')||!s.includes('main-content'))process.exit(1)"`
</verification>

<success_criteria>
- Teacher-facing pages can switch among the three locked shell modes.
- Settings and resources stay on the same teacher-facing shell orchestration path.
- The shell changes only page-level regions and navigation placement, not internal business module structure.
</success_criteria>

<output>
After completion, create `.planning/phases/16-theme-plugins-and-layout-orchestration/16-03-SUMMARY.md`
</output>
