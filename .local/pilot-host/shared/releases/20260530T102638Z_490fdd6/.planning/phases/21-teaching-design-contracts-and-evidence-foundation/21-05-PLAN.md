---
phase: 21-teaching-design-contracts-and-evidence-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - 21-01
  - 21-03
files_modified:
  - src/components/authoring/lesson-authoring-workspace.tsx
  - src/components/authoring/lesson-authoring-workspace.test.tsx
autonomous: true
gap_closure: true
requirements:
  - ORCH-01
must_haves:
  truths:
    - 教师在 editor 的每张步骤卡上都能一眼感知该环节时长，而不是只在拥挤角落看到一个容易忽略的小 badge。
    - 步骤卡时间继续来自 `teachingDesign.estimatedMinutes` 或 legacy fallback，不回退为硬编码展示文案。
    - 回归测试会验证时间是“有标签的元信息展示位”，而不只是 DOM 里出现裸 `18 min` 文本。
  artifacts:
    - path: src/components/authoring/lesson-authoring-workspace.tsx
      provides: stronger step-card duration metadata slot
      contains: 预计时长
    - path: src/components/authoring/lesson-authoring-workspace.test.tsx
      provides: UI regression for duration visibility contract
      contains: 预计时长
  key_links:
    - from: src/components/authoring/lesson-authoring-workspace.tsx
      to: src/components/authoring/lesson-authoring-workspace.test.tsx
      via: card-level labeled duration metadata is asserted semantically
      pattern: 预计时长
    - from: src/components/authoring/lesson-authoring-workspace.tsx
      to: src/lib/dto/lesson-authoring.ts
      via: `getStepMinutes(step)` still resolves teachingDesign minutes or fallback defaults
      pattern: getStepMinutes
---

<objective>
修复 editor 中“看不到模块时间”的 UAT gap：把步骤卡时间从弱感知的小角标升级成明确、稳定、带中文标签的元信息区域。

Purpose: 闭合 UAT Test 7 的 gap，落实 D-03 / D-17 / D-19，让教师在编排流程时能真实感知每个环节的时长，而不是只让数据 technically 存在。
Output: stronger FlowStepCard duration layout、可感知中文标签/图标、针对真实卡片可见性的回归测试。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-teaching-design-contracts-and-evidence-foundation/21-UAT.md
@.planning/phases/21-teaching-design-contracts-and-evidence-foundation/21-03-SUMMARY.md
@.planning/debug/editor-missing-step-time.md
@src/components/authoring/lesson-authoring-workspace.tsx
@src/components/authoring/lesson-authoring-workspace.test.tsx

<interfaces>
From `src/components/authoring/lesson-authoring-workspace.tsx`:
```ts
const totalMinutes = steps.reduce((total, step) => total + getStepMinutes(step), 0);

function getStepMinutes(step: LessonStepDTO) {
  if (step.payload.teachingDesign?.estimatedMinutes) {
    return step.payload.teachingDesign.estimatedMinutes;
  }
  if (step.type === "content") return 12;
  if (step.type === "task") return 15;
  return 8;
}
```

Current weak rendering to replace:
```tsx
<span className="rounded-[0.8rem] ...">{getStepMinutes(step)} min</span>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 把步骤时间提升为明确的卡片元信息位，而不是右上角弱感知 badge</name>
  <files>src/components/authoring/lesson-authoring-workspace.tsx</files>
  <behavior>
    - Test 1: 每张 `FlowStepCard` 都有独立的时长元信息位，包含中文标签（如“预计时长”）和分钟值，而不是只剩无标签的 `18 min` 小 chip。
    - Test 2: 长标题、长描述或多枚状态 badge 共存时，时间信息依旧稳定可见，不会被压缩到难以察觉。
    - Test 3: 时间值继续来自 `getStepMinutes(step)`，对 explicit teachingDesign 和 legacy fallback 都成立。
  </behavior>
  <action>修改 `src/components/authoring/lesson-authoring-workspace.tsx` 中的 `FlowStepCard`：把当前标题行右上角无标签 `xx min` badge 改成更明确的元信息展示位，建议放进标题下方的 metadata row 或卡片副标题区，带简体中文标签（例如 `预计时长`）和可选 `Clock3` 图标。布局上给时间信息单独的容器、`shrink-0` 保护或响应式换行策略，确保长标题/长描述/多个状态 badge 时也不会被挤没。按 UAT 诊断，禁止继续依赖右上角单个弱 badge 作为唯一时间 affordance；但不要改变 `getStepMinutes(step)` 的数据来源，也不要移除现有 `默认推断 / 待完善` cue 或 built-in source badges。</action>
  <verify>
    <automated>pnpm test --run src/components/authoring/lesson-authoring-workspace.test.tsx</automated>
  </verify>
  <done>教师在步骤卡中可以稳定看见每个环节的时长标签和值，时间信息不再只是拥挤卡片中的次要角标。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 补真实卡片可见性的 UI 回归测试，禁止“只有 DOM 文本存在”式假通过</name>
  <files>src/components/authoring/lesson-authoring-workspace.test.tsx</files>
  <behavior>
    - Test 1: 用 card-scoped 查询断言步骤卡出现“预计时长”标签及对应分钟值，而不是只搜全局 `18 min`。
    - Test 2: 至少覆盖一个长标题或多 badge 场景，证明时间元信息仍属于清晰可见的 metadata 区域。
    - Test 3: 总时长汇总仍正确显示，确保单卡时长升级没有破坏顶部汇总逻辑。
  </behavior>
  <action>更新 `src/components/authoring/lesson-authoring-workspace.test.tsx`，把当前仅检查 `18 min` 文本存在的断言升级成面向真实 UI 合同的测试：围绕目标步骤卡做 scoped query，断言存在中文标签（例如 `预计时长`）和对应分钟值，必要时用 `within()` 锁定卡片容器，避免被顶部“总时长约 xx 分钟”误判。补一条拥挤卡片场景回归（长标题、fallback badges、built-in badge 中任选其一或组合），证明新的时间元信息位仍可感知。不要再新增 `readFileSync(...).toContain()` 这类实现细节测试。</action>
  <verify>
    <automated>pnpm test --run src/components/authoring/lesson-authoring-workspace.test.tsx</automated>
  </verify>
  <done>测试会在步骤卡时间重新退化为弱 badge、无标签文本或被顶部汇总掩盖时直接失败，不再让“DOM 里有数字”误判为 gap 已关闭。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teachingDesign DTO -> editor card UI | 服务端提供的分钟数需要被教师明确感知，而不是在展示层被稀释掉 |
| card layout -> UI regression tests | 测试必须证明可感知展示合同，而不是只证明数字字符串存在 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-17 | T | `src/components/authoring/lesson-authoring-workspace.tsx` | mitigate | 保持 `getStepMinutes(step)` 为唯一时长来源，不用静态 copy 或新硬编码 badge 替代 teachingDesign/fallback 数据。 |
| T-21-18 | R | `src/components/authoring/lesson-authoring-workspace.test.tsx` | mitigate | 用 card-scoped semantic assertions 验证 labeled metadata，而不是只断言全局 `18 min` 文本。 |
| T-21-19 | D | `FlowStepCard` layout | mitigate | 给时长元信息独立容器和响应式换行策略，避免长标题/多 badge 把时间挤没。 |
| T-21-20 | I | `FlowStepCard` | accept | 时长元信息不涉及敏感数据，风险主要是可读性而非信息泄漏。 |
</threat_model>

<verification>
- `pnpm test --run src/components/authoring/lesson-authoring-workspace.test.tsx`
</verification>

<success_criteria>
- editor 步骤卡的时长信息带中文标签、稳定位置、可感知视觉权重。
- 时长继续来自 teachingDesign 或 legacy fallback，不回退成硬编码 UI。
- UI 回归测试能在时间再次退化成弱 badge 或仅剩裸文本时失败。
</success_criteria>

<output>
After completion, create `.planning/phases/21-teaching-design-contracts-and-evidence-foundation/21-05-SUMMARY.md`
</output>
