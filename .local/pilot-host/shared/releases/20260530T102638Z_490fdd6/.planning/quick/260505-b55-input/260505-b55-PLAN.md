---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/login/TestAccountHint.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - User can see test account information on the login page
    - User can click a test account to auto-fill the email and password fields
  artifacts:
    - path: src/app/(auth)/login/TestAccountHint.tsx
      provides: Test account UI and click-to-fill behavior
  key_links:
    - from: src/app/(auth)/login/TestAccountHint.tsx
      to: DOM input elements
      via: document.getElementById
---

<objective>
在登录页面增加测试账号密码提示，并支持点击后一键填充到输入框。
Purpose: 方便在开发和测试过程中快速登录，无需每次手动输入。
Output: 新增 TestAccountHint 客户端组件并集成到登录页。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 创建测试账号提示客户端组件</name>
  <files>src/app/(auth)/login/TestAccountHint.tsx</files>
  <action>
创建 `TestAccountHint` 客户端组件 ("use client")。
提供测试教师账号和学生账号的列表（例如 email 和 默认密码）。
组件中渲染这几个账号为可点击按钮，点击后通过 `document.getElementById('email')` 和 `document.getElementById('password')` 找到输入框，设置它们的 `value` 属性。
样式需遵循系统的 UI 规范（例如使用 text-sm, text-on-surface-variant 等）。
  </action>
  <verify>
    <automated>ls src/app/\(auth\)/login/TestAccountHint.tsx</automated>
  </verify>
  <done>组件创建完成并包含填充逻辑</done>
</task>

<task type="auto">
  <name>Task 2: 在登录页引入测试账号提示</name>
  <files>src/app/(auth)/login/page.tsx</files>
  <action>
引入并渲染 `TestAccountHint` 组件，放在登录表单的下方或上方（例如表单下方的 grid 中）。
确保表单的 input 依然保留 `id="email"` 和 `id="password"` 以供客户端组件查询和填充。
  </action>
  <verify>
    <automated>grep TestAccountHint src/app/\(auth\)/login/page.tsx</automated>
  </verify>
  <done>登录页面渲染出测试账号按钮</done>
</task>

</tasks>
