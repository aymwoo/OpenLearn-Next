---
phase: quick
plan: 260507-v0p
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/surfaces/class-management-surface.tsx
autonomous: true
requirements:
  - QUICK-stitch-card-view

must_haves:
  truths:
    - "学生列表支持表格视图和卡片视图两种模式，默认表格视图"
    - "卡片视图展示学生圆形头像，外围有 SVG 进度环指示器"
    - "卡片下方居中显示姓名（bold）和学号（smaller）"
    - "「学生名册」区域有 表格视图 / 卡片视图 切换按钮"
    - "表格视图保持原有 3 列布局、checkbox、筛选、分页不变"
    - "进度环颜色：≥75 使用 primary，<75 使用 tertiary"
    - "5 名学生进度值：陈宇 90 / 林梅 85 / 王强 60 / 张华 75 / 李静 95"
  artifacts:
    - path: "src/components/surfaces/class-management-surface.tsx"
      provides: "班级管理页面，含表格 & 卡片双视图"
      contains: "viewMode"
      contains: "stroke-dasharray"
      contains: "表格视图"
  key_links:
    - from: "视图切换按钮组"
      to: "viewMode state"
      via: "onClick 切换 'table' | 'card'"
      pattern: "setViewMode"
    - from: "filteredStudents 数据"
      to: "card grid map 渲染"
      via: "viewMode === 'card' 时渲染 StudentCard 列表"
      pattern: "viewMode.*card.*StudentCard"
---

<objective>
为学生名册新增「卡片视图」模式：每个学生以圆形头像 + SVG 进度环 + 姓名 + 学号的卡片展示，
通过顶部切换按钮在表格视图和卡片视图之间切换。
表格视图及其全部功能（筛选、搜索、批量操作、分页）保持不变。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
<!-- Target file: src/components/surfaces/class-management-surface.tsx (351 lines) -->
<!-- This is a "use client" component — all changes are client-side only. -->
<!-- No new files, no DB, no API — pure UI work. -->

Current student data shape (lines 28-58):
```typescript
const students = [
  { avatar: string, name: string, idNumber: string },
  // 5 entries: 陈宇, 林梅, 王强, 张华, 李静
] as const;
```

Internal components already defined (lines 282-359):
- `Metric({ label, value })` — hero section metric display
- `FilterPill({ label, active, onClick })` — toggle chip
- `IconButton({ children, label })` — icon-only button
- `PaginationButton({ children, label })` — pagination arrow

Key constants already used:
- `activeStatus` / `activeGender` state with filter pills
- `filteredStudents` derived from `students` via filter function
- Surface color tokens: `bg-surface-container-lowest`, `bg-surface-container-low`, `text-primary`, `text-tertiary`, `text-on-surface`, `text-on-surface-variant`
- Radius tokens: `rounded-[var(--radius-shell)]`, `rounded-[var(--radius-card)]`

DESIGN.md constraints:
- No-line rule: use tonal surface shifts, no 1px borders
- Surface hierarchy: surface-container-lowest cards on surface-container-low background
- Lexend font, no 1px dividers

Stitch bb82bea4 卡片设计参考（来自 planning context）:
- Grid: `grid grid-cols-3 sm:grid-cols-4 md:grid-cols-5 lg:grid-cols-6 xl:grid-cols-8 gap-2` → 适配 5 名学生用 `grid-cols-3 sm:grid-cols-4 md:grid-cols-5 gap-2`
- Card: `bg-surface-container-lowest p-2 rounded-[1.5rem] shadow flex flex-col items-center justify-center gap-1`
- Avatar container: `relative w-12 h-12`, SVG ring `absolute inset-0`, avatar `w-9 h-9 rounded-full z-10`
- Text: name `font-bold text-xs text-on-surface`, id `text-[9px] text-on-surface-variant`
- Progress ring: `<svg viewBox="0 0 36 36" class="absolute inset-0 w-full h-full transform -rotate-90">`, track `text-surface-container-highest stroke-width="3"`, progress `stroke-dasharray="X, 100" stroke-linecap="round"`
- Progress values: 陈宇 90, 林梅 85, 王强 60, 张华 75, 李静 95
- Progress color: primary for students 1/2/4/5, tertiary for student 3 (王强) — 规则: ≥75 → primary, <75 → tertiary
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 扩展数据模型 + 视图切换状态 + StudentCard 组件</name>
  <files>src/components/surfaces/class-management-surface.tsx</files>
  <action>
**步骤 A — 扩展 students 数组（替换 lines 28-58 的 as const 声明）：**

给每个 student 对象添加 `progress: number` 字段：
- 陈宇: 90 | 林梅: 85 | 王强: 60 | 张华: 75 | 李静: 95

同时给 avatar 为纯文本（如 "王"）的学生补充保留原有的 fallback 逻辑（initial 圆块）。

将 `as const` 改为普通 `const`（因为需求允许 progress 是动态值）。

**步骤 B — 在 ClassManagementSurface 组件内添加 viewMode 状态（line 61 附近）：**

```typescript
const [viewMode, setViewMode] = useState<"table" | "card">("table");
```

**步骤 C — 在文件底部（PaginationButton 之后）添加 StudentCard 内联组件：**

```typescript
function StudentCard({
  avatar,
  name,
  idNumber,
  progress,
}: {
  avatar: string;
  name: string;
  idNumber: string;
  progress: number;
}) {
  const ringColor = progress >= 75 ? "text-primary" : "text-tertiary";
  return (
    <article className="flex flex-col items-center justify-center gap-1 rounded-[1.5rem] bg-surface-container-lowest p-2 shadow-[0_4px_16px_rgba(0,0,0,0.03)] transition-colors hover:bg-white">
      {/* Avatar + ring container */}
      <div className="relative flex size-12 items-center justify-center">
        <svg
          className="absolute inset-0 size-full -rotate-90"
          viewBox="0 0 36 36"
          aria-hidden
        >
          {/* Track */}
          <path
            className="text-surface-container-highest"
            fill="none"
            stroke="currentColor"
            strokeWidth="3"
            d="M18 2.0845 a 15.9155 15.9155 0 0 1 0 31.831 a 15.9155 15.9155 0 0 1 0 -31.831"
          />
          {/* Progress arc */}
          <path
            className={ringColor}
            fill="none"
            stroke="currentColor"
            strokeWidth="3"
            strokeLinecap="round"
            strokeDasharray={`${progress}, 100`}
            d="M18 2.0845 a 15.9155 15.9155 0 0 1 0 31.831 a 15.9155 15.9155 0 0 1 0 -31.831"
          />
        </svg>
        {avatar.startsWith("http") ? (
          <img
            src={avatar}
            alt={name}
            className="relative z-10 size-9 rounded-full object-cover"
          />
        ) : (
          <div className="relative z-10 grid size-9 place-items-center rounded-full bg-primary/10 text-sm font-semibold text-primary">
            {avatar}
          </div>
        )}
      </div>
      {/* Text */}
      <div className="flex flex-col items-center">
        <span className="text-xs font-bold leading-tight text-on-surface">
          {name}
        </span>
        <span className="text-[9px] text-on-surface-variant">{idNumber}</span>
      </div>
    </article>
  );
}
```

**注意事项：**
- 使用 Tailwind 的 `size-*` 简写（w + h 同时设置）
- SVG circle path 使用 `M18 2.0845 a 15.9155 ...` — 这是以 (18,18) 为圆心、半径 15.9155 的圆弧，完整周长 ≈ 100，`stroke-dasharray="X, 100"` 直接对应百分比
- `ringColor` 规则：progress >= 75 用 primary，<75 用 tertiary
- Card 使用 `rounded-[1.5rem]` 圆角，与 Stitch 一致
- 不要添加 border — 遵循 no-line 规则
- 不要修改 hero 区域（lines 73-118）、筛选栏（lines 141-204）、分页（lines 265-277）
  </action>
  <verify>
    <automated>grep -n "progress:" src/components/surfaces/class-management-surface.tsx | head -6</automated>
  </verify>
  <done>
  students 数组每条记录含 progress 字段（90/85/60/75/95），viewMode state 已声明默认 "table"，StudentCard 组件定义存在于文件尾部，包含 SVG 进度环、头像、姓名、学号。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 集成卡片视图网格 + 切换按钮</name>
  <files>src/components/surfaces/class-management-surface.tsx</files>
  <action>
**步骤 A — 在「学生名册」标题行添加视图切换按钮：**

在现有操作按钮（Import / Plus）的 `<div>` 同级（line 126 之后），标题右侧，插入一个切换按钮组。结构如下：

```tsx
{/* 放在 <h2>学生名册</h2> 和操作按钮之间或操作按钮之前 */}
<div className="flex items-center rounded-full bg-surface-container-high p-0.5">
  <button
    type="button"
    onClick={() => setViewMode("table")}
    className={`min-h-8 rounded-full px-3 text-xs font-medium transition-colors ${
      viewMode === "table"
        ? "bg-surface-container-lowest text-primary shadow-[0_2px_8px_rgba(0,0,0,0.06)]"
        : "text-on-surface-variant hover:text-on-surface"
    }`}
  >
    表格视图
  </button>
  <button
    type="button"
    onClick={() => setViewMode("card")}
    className={`min-h-8 rounded-full px-3 text-xs font-medium transition-colors ${
      viewMode === "card"
        ? "bg-surface-container-lowest text-primary shadow-[0_2px_8px_rgba(0,0,0,0.06)]"
        : "text-on-surface-variant hover:text-on-surface"
    }`}
  >
    卡片视图
  </button>
</div>
```

按钮组布局建议：把 title + toggle 放在一行，action buttons（导入/添加）另放。布局如下：

```
flex sm:flex-row sm:items-center sm:justify-between gap-3
  left:  <h2>学生名册</h2> + toggle buttons
  right: <导入名单> <添加学生>
```

**步骤 B — 在表格列表区域下方添加卡片网格（条件渲染）：**

在现有的 `articles.map(...)` 表格列表（lines 218-263）**之后**、分页组件（line 265）**之前**，添加卡片视图：

```tsx
{/* Card view */}
{viewMode === "card" && (
  <div className="grid grid-cols-3 gap-2 sm:grid-cols-4 md:grid-cols-5">
    {filteredStudents.map((student) => (
      <StudentCard
        key={student.idNumber}
        avatar={student.avatar}
        name={student.name}
        idNumber={student.idNumber}
        progress={student.progress}
      />
    ))}
  </div>
)}
```

**步骤 C — 条件隐藏表格视图：**

将现有的表格 header（lines 207-216）和 student list（lines 218-263）用条件包裹：

```tsx
{viewMode === "table" && (
  <>
    {/* 现有 table header (3-col grid) */}
    {/* 现有 articles.map(...) */}
  </>
)}
```

**注意事项：**
- 切换按钮使用 `rounded-full` pill 样式，active 状态白色底色 + primary 文字 + 微阴影，非 active 灰色文字
- 卡片和表格共享同一 `filteredStudents` 数据源，筛选逻辑完全不变
- 分页组件在两种视图下方共用，不移动位置
- 批量操作、搜索框在查看区域上方，两种视图均可见
- 切换按钮的 `bg-surface-container-high` 作为容器色，内部 active pill 为 `bg-surface-container-lowest`
  </action>
  <verify>
    <automated>grep -n "viewMode.*card\|卡片视图\|StudentCard" src/components/surfaces/class-management-surface.tsx | head -10</automated>
  </verify>
  <done>
  页面顶部有 表格视图 / 卡片视图 pill 切换按钮。切换至卡片视图时表格隐藏、卡片网格显示。切换回表格视图时卡片隐藏、表格恢复。筛选、搜索、批量操作、分页在两种视图下均正常工作。
  </done>
</task>

</tasks>

<verification>
启动 dev server (`pnpm dev`)，访问班级管理页面：
1. 默认显示表格视图（与当前一致）
2. 点击「卡片视图」→ 表格隐藏，卡片网格显示
3. 每张卡片展示：圆形头像 + 进度环（陈宇 90% / 林梅 85% / 王强 60% / 张华 75% / 李静 95%）
4. 进度环颜色：王强 tertiary，其余 primary
5. 点击「表格视图」→ 卡片隐藏，表格恢复，布局不变
6. 筛选 pill 切换在两种视图下均有效
</verification>

<success_criteria>
- 班级管理页面支持表格/卡片双视图，默认表格
- 卡片视图：圆形头像 + SVG 进度环 + 姓名 + 学号，响应式网格
- 切换按钮 UI 对齐 Stitch tonal pill 风格
- 现有功能（筛选、搜索、批量操作、分页、hero 区域）零回归
</success_criteria>

<output>
After completion, create `.planning/quick/260507-v0p-stitch-bb82bea4/260507-v0p-SUMMARY.md`
</output>
