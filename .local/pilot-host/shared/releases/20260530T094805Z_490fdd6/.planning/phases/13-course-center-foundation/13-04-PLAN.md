---
phase: 13-course-center-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 13-01-PLAN
  - 13-02-PLAN
  - 13-03-PLAN
files_modified:
  - src/lib/dto/course-authoring.ts
  - src/lib/dal/course-authoring.ts
  - src/lib/dal/course-authoring.test.ts
autonomous: true
gap_closure: true
requirements:
  - COURSE-01
  - COURSE-02
must_haves:
  truths:
    - D-16: 教师打开 `/teacher/courses`、课程详情与课程内课时入口时，只能看到自己拥有且属于自己 active teacher school scope 的课程
    - D-16 D-17: 课程中心 DTO 同时暴露真实 teacher scope 的可建课学校元数据，而不是把 school scope 硬编码到客户端
    - COURSE-01: 同校其他教师拥有的课程不会再出现在当前教师的课程中心、详情页或 lessons entry
  artifacts:
    - path: src/lib/dal/course-authoring.ts
      provides: teacher-owned course reads and course-center scope metadata contract
    - path: src/lib/dto/course-authoring.ts
      provides: availableSchools/defaultSchoolId DTO contract for create flow
    - path: src/lib/dal/course-authoring.test.ts
      provides: regressions for ownership filtering and school-scope metadata
  key_links:
    - src/lib/dal/course-authoring.ts -> src/lib/dal/lesson-authoring.ts via assertActiveTeacher
    - src/lib/dal/course-authoring.ts -> src/db/schema.ts via courses.ownerId and schools.id
    - src/lib/dal/course-authoring.ts -> src/lib/dto/course-authoring.ts via TeacherCourseCenterDTOSchema
---

<objective>
修复 Phase 13 的读模型根因缺陷：把课程读取从“同校可见”收紧为“教师本人拥有且同校可见”，并为建课 UI 输出真实 school scope 元数据。

Purpose: 13-01/13-02 的计划写明了 teacher-scoped contract，但执行时漏掉了 `ownerId === actorId` 过滤，也没有把真实 school scope 带到课程中心 DTO，导致 COURSE-01 和 COURSE-02 同时失真。
Output: 修正后的 course-authoring DTO/DAL，以及覆盖 ownership + school metadata 的回归测试。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-course-center-foundation/13-CONTEXT.md
@.planning/phases/13-course-center-foundation/13-RESEARCH.md
@.planning/phases/13-course-center-foundation/13-VERIFICATION.md
@.planning/phases/13-course-center-foundation/13-01-SUMMARY.md
@.planning/phases/13-course-center-foundation/13-02-SUMMARY.md
@.planning/phases/13-course-center-foundation/13-03-SUMMARY.md
@.planning/phases/13-course-center-foundation/13-PATTERNS.md

<interfaces>
From `src/lib/dal/lesson-authoring.ts`:
```ts
export async function assertActiveTeacher(): Promise<{ userId: string; schoolIds: string[] }>;
```

From `src/db/schema.ts`:
```ts
export const schools = sqliteTable("school", {
  id: text("id").primaryKey(),
  name: text("name").notNull(),
});

export const courses = sqliteTable("course", {
  schoolId: text("schoolId").notNull(),
  ownerId: text("ownerId").notNull(),
  status: text("status").notNull().default("draft"),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修正课程中心/详情/课时入口的 teacher-owned 读取边界</name>
  <files>src/lib/dal/course-authoring.ts, src/lib/dal/course-authoring.test.ts</files>
  <read_first>src/lib/dal/course-authoring.ts; src/lib/dal/course-authoring.test.ts; src/lib/dal/lesson-authoring.ts; src/db/schema.ts; .planning/phases/13-course-center-foundation/13-VERIFICATION.md</read_first>
  <behavior>
    - Test 1: 同校但 `ownerId !== actorId` 的课程不会出现在 `getTeacherCourseCenterDTO()` 返回值中。
    - Test 2: 对同校 чужой课程调用 `getTeacherCourseDetailDTO({ courseId })` 会抛出 `COURSE_NOT_FOUND` 或等价拒绝，不再泄露详情。
    - Test 3: 对同校 чужой课程调用 `getTeacherCourseLessonsEntryDTO({ courseId })` 也会拒绝，确保 lessons entry 没有越权可见性。
  </behavior>
  <action>先扩展 `src/lib/dal/course-authoring.test.ts`，加入一个“同校、不同 owner”的课程样本，例如 `course-same-school-foreign`，并为课程中心、课程详情、课程内课时入口分别写回归测试，明确断言 foreign course 不可见。随后修改 `src/lib/dal/course-authoring.ts`：把 `getScopedCourses(input)` 收紧为只返回 `input.schoolIds` 范围内且 `course.ownerId === input.actorId` 的课程；`getCachedTeacherCourseDetailDTO()` 与 `getCachedTeacherCourseLessonsEntryDTO()` 必须建立在这组 owned rows 之上，禁止直接按 school scope 放行。不要新增 UI 层过滤，也不要把未授权课程伪装成可渲染 DTO（per D-16）。</action>
  <acceptance_criteria>
    - `src/lib/dal/course-authoring.ts` contains `course.ownerId === input.actorId`
    - `src/lib/dal/course-authoring.test.ts` contains `course-same-school-foreign`
    - `src/lib/dal/course-authoring.test.ts` contains `getTeacherCourseDetailDTO`
    - `src/lib/dal/course-authoring.test.ts` contains `getTeacherCourseLessonsEntryDTO`
    - `pnpm vitest run src/lib/dal/course-authoring.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/lib/dal/course-authoring.test.ts</automated>
  </verify>
  <done>课程中心、详情页和 lessons entry 都只返回当前教师拥有的课程；同校其他教师课程不再可见。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 为建课流程补齐真实 school scope DTO contract</name>
  <files>src/lib/dto/course-authoring.ts, src/lib/dal/course-authoring.ts, src/lib/dal/course-authoring.test.ts</files>
  <read_first>src/lib/dto/course-authoring.ts; src/lib/dal/course-authoring.ts; src/lib/dal/auth.ts; src/db/schema.ts; .planning/phases/13-course-center-foundation/13-VERIFICATION.md</read_first>
  <behavior>
    - Test 1: 单学校教师的 `TeacherCourseCenterDTO` 返回 `defaultSchoolId` 与唯一的 `availableSchools` 记录。
    - Test 2: 多学校教师的 `TeacherCourseCenterDTO` 返回全部 active school options，且每个 option 都包含 `id` 与 `name`。
    - Test 3: `defaultSchoolId` 不再由客户端写死为 `school-1`，而是由服务端 DTO 提供。
  </behavior>
  <action>在 `src/lib/dto/course-authoring.ts` 中新增明确 contract：`TeacherCourseScopeSchoolDTOSchema = { id: string; name: string }`，并把 `TeacherCourseCenterDTOSchema` 扩展为 `defaultSchoolId: string | null` 与 `availableSchools: TeacherCourseScopeSchoolDTO[]`。随后修改 `src/lib/dal/course-authoring.ts`：从 `input.schoolIds` 查询 `schools` 表，组装当前教师 active memberships 可用的学校列表，`defaultSchoolId` 取 `input.schoolIds[0] ?? null`，`availableSchools` 仅包含当前 scope 内学校，不允许硬编码 `school-1`。在 `src/lib/dal/course-authoring.test.ts` 增加单学校与多学校场景断言，明确检查 DTO 输出的 school ids / names，为 13-05 的 create drawer 修复提供唯一数据源。</action>
  <acceptance_criteria>
    - `src/lib/dto/course-authoring.ts` contains `TeacherCourseScopeSchoolDTOSchema`
    - `src/lib/dto/course-authoring.ts` contains `defaultSchoolId`
    - `src/lib/dto/course-authoring.ts` contains `availableSchools`
    - `src/lib/dal/course-authoring.ts` contains `db.query.schools.findMany`
    - `src/lib/dal/course-authoring.test.ts` contains `availableSchools`
    - `pnpm vitest run src/lib/dal/course-authoring.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/lib/dal/course-authoring.test.ts</automated>
  </verify>
  <done>课程中心 DTO 已输出真实 teacher school scope 元数据，后续 create UI 不再需要任何硬编码 schoolId。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teacher session → course DAL | 未信任的 session scope 驱动课程读取与 school 元数据输出 |
| course DAL → course center DTO | 服务端聚合后的 owner/school 数据进入 UI contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-13 | E | `getScopedCourses()` | mitigate | 在 DAL 内同时校验 `course.schoolId in scope.schoolIds` 与 `course.ownerId === actorId`，拒绝 same-school foreign course 泄露 |
| T-13-14 | I | `getTeacherCourseDetailDTO()` / `getTeacherCourseLessonsEntryDTO()` | mitigate | 详情与 lessons entry 只从 owned rows 中查找 `courseId`，找不到即抛 `COURSE_NOT_FOUND` |
| T-13-15 | T | `TeacherCourseCenterDTO.availableSchools` | mitigate | `availableSchools` 仅由服务端根据 active memberships + `schools` 表生成，客户端不自定义 scope |
| T-13-16 | R | gap closure regressions | mitigate | 用 Vitest 为 center/detail/lessons + school metadata 同时建立回归测试，避免再次把 teacher-scoped contract 实现成 school-only |
</threat_model>

<verification>
- `pnpm vitest run src/lib/dal/course-authoring.test.ts`
- Read `src/lib/dal/course-authoring.ts` and confirm every teacher-scoped read path now filters both `schoolId` and `ownerId`
</verification>

<success_criteria>
- COURSE-01 的 teacher-owned 可见性缺陷被关掉
- 课程详情与 lessons entry 不再泄露同校其他教师课程
- 课程中心 DTO 为建课流程输出真实 `defaultSchoolId` 与 `availableSchools`
</success_criteria>

<output>
After completion, create `.planning/phases/13-course-center-foundation/13-04-SUMMARY.md`
</output>
