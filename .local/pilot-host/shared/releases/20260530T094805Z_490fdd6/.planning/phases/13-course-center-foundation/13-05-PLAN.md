---
phase: 13-course-center-foundation
plan: 05
type: execute
wave: 5
depends_on:
  - 13-04-PLAN
files_modified:
  - src/components/courses/course-create-drawer.tsx
  - src/components/surfaces/teacher-course-center-surface.tsx
  - src/components/surfaces/teacher-course-center-surface.test.tsx
autonomous: true
gap_closure: true
requirements:
  - COURSE-02
must_haves:
  truths:
    - D-05 D-08: 教师仍然从课程总览右侧抽屉建课，但提交的 `schoolId` 来自真实 teacher scope，而不是硬编码常量
    - D-16 D-17: 单学校教师能在非 `school-1` 场景正常建课；多学校教师能显式选择目标学校后提交
    - COURSE-02: 建课成功后课程中心继续保持 read-your-writes，不因错误 schoolId 导致 create flow 失真
  artifacts:
    - path: src/components/courses/course-create-drawer.tsx
      provides: scoped create drawer without hardcoded school id
    - path: src/components/surfaces/teacher-course-center-surface.tsx
      provides: teacher course center wiring for defaultSchoolId and availableSchools
    - path: src/components/surfaces/teacher-course-center-surface.test.tsx
      provides: integration regressions for non-school-1 and multi-school create flows
  key_links:
    - src/components/surfaces/teacher-course-center-surface.tsx -> src/components/courses/course-create-drawer.tsx via defaultSchoolId/availableSchools props
    - src/components/courses/course-create-drawer.tsx -> createCourseAction via selectedSchoolId submit payload
    - src/components/surfaces/teacher-course-center-surface.test.tsx -> createCourseAction via rendered drawer integration
---

<objective>
修复课程创建 UI 的 school scope 传递：移除 `school-1` 硬编码，让抽屉从课程中心 DTO 接收默认学校与多学校选项。

Purpose: 13-02 的 create drawer 把 school scope 写死在客户端，导致非 `school-1` 与多学校教师都无法可靠建课；本计划把这个缺陷闭环到真实 teacher scope contract。
Output: 无硬编码 schoolId 的 create drawer、课程中心 wiring，以及覆盖单学校/多学校场景的集成回归测试。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-course-center-foundation/13-CONTEXT.md
@.planning/phases/13-course-center-foundation/13-RESEARCH.md
@.planning/phases/13-course-center-foundation/13-VERIFICATION.md
@.planning/phases/13-course-center-foundation/13-04-PLAN.md
@.planning/phases/13-course-center-foundation/13-UI-SPEC.md

<interfaces>
From `src/lib/dto/course-authoring.ts` after 13-04:
```ts
type TeacherCourseScopeSchoolDTO = {
  id: string;
  name: string;
};

type TeacherCourseCenterDTO = {
  defaultSchoolId: string | null;
  availableSchools: TeacherCourseScopeSchoolDTO[];
  courses: TeacherCourseCardDTO[];
  includeArchived: boolean;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为课程中心建课流程写集成回归测试</name>
  <files>src/components/surfaces/teacher-course-center-surface.test.tsx</files>
  <read_first>src/components/surfaces/teacher-course-center-surface.tsx; src/components/courses/course-create-drawer.tsx; src/components/surfaces/settings-surface.test.tsx; .planning/phases/13-course-center-foundation/13-VERIFICATION.md</read_first>
  <behavior>
    - Test 1: 当 `defaultSchoolId` 为 `school-9` 且只有一个 `availableSchools` 时，提交 payload 使用 `school-9`，绝不回退到 `school-1`。
    - Test 2: 当 `availableSchools` 有两个以上时，抽屉会渲染学校选择控件，用户切换到 `school-2` 后提交 payload 使用 `school-2`。
    - Test 3: source 中不再存在 `schoolId = "school-1"` 默认值。
  </behavior>
  <action>新增 `src/components/surfaces/teacher-course-center-surface.test.tsx`（`@vitest-environment jsdom`），mock `createCourseAction`、`useRouter().refresh()` 与必要的 Next primitives，直接渲染 `TeacherCourseCenterSurface`。测试用 DTO 必须覆盖两个场景：一是 `defaultSchoolId: "school-9"` + 单学校；二是 `availableSchools: [{id:"school-1",name:"晨曦学校"},{id:"school-2",name:"北校区"}]` + 多学校切换。通过打开抽屉、填写标题/学科/年级、提交表单，明确断言 create payload 里的 `schoolId` 等于 DTO 提供或用户选择的真实值，并同时用源码断言去掉 `schoolId = "school-1"` 这个硬编码根因。</action>
  <acceptance_criteria>
    - `src/components/surfaces/teacher-course-center-surface.test.tsx` exists
    - `src/components/surfaces/teacher-course-center-surface.test.tsx` contains `school-9`
    - `src/components/surfaces/teacher-course-center-surface.test.tsx` contains `school-2`
    - `src/components/surfaces/teacher-course-center-surface.test.tsx` contains `schoolId = "school-1"`
    - `pnpm vitest run src/components/surfaces/teacher-course-center-surface.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/components/surfaces/teacher-course-center-surface.test.tsx</automated>
  </verify>
  <done>已有自动化证据证明 create flow 在非 `school-1` 与多学校场景下都会提交真实 scope，而不是静态常量。</done>
</task>

<task type="auto">
  <name>Task 2: 将建课抽屉改为消费真实 teacher school scope</name>
  <files>src/components/courses/course-create-drawer.tsx, src/components/surfaces/teacher-course-center-surface.tsx</files>
  <read_first>src/components/courses/course-create-drawer.tsx; src/components/surfaces/teacher-course-center-surface.tsx; src/components/ui/ghost-field.ts; src/components/ui/button.tsx; src/components/surfaces/teacher-course-center-surface.test.tsx</read_first>
  <action>修改 `src/components/courses/course-create-drawer.tsx`：删除 `schoolId = "school-1"` 默认值，改为接收 `defaultSchoolId: string | null` 与 `availableSchools: Array<{ id: string; name: string }>`。组件内部维护 `selectedSchoolId`，初始值取 `defaultSchoolId ?? availableSchools[0]?.id ?? ""`；若 `availableSchools.length > 1`，在抽屉表单中渲染明确标签的学校 `<select>`；若只有一个学校，显示只读学校摘要；若没有可用学校，则禁用提交并展示 `当前教师身份没有可用学校，暂时无法创建课程。`。提交时把 `selectedSchoolId` 传给 `createCourseAction`。同步修改 `src/components/surfaces/teacher-course-center-surface.tsx` 两处 `CourseCreateDrawer` 调用，显式传入 `data.defaultSchoolId` 与 `data.availableSchools`，禁止再依赖任何隐式默认值。不要更改抽屉触发方式、中文文案风格或 read-your-writes 的 `router.refresh()` 行为（per D-05、D-17）。</action>
  <acceptance_criteria>
    - `src/components/courses/course-create-drawer.tsx` does not contain `schoolId = "school-1"`
    - `src/components/courses/course-create-drawer.tsx` contains `defaultSchoolId`
    - `src/components/courses/course-create-drawer.tsx` contains `availableSchools`
    - `src/components/courses/course-create-drawer.tsx` contains `当前教师身份没有可用学校`
    - `src/components/surfaces/teacher-course-center-surface.tsx` contains `defaultSchoolId={data.defaultSchoolId}`
    - `src/components/surfaces/teacher-course-center-surface.tsx` contains `availableSchools={data.availableSchools}`
    - `pnpm vitest run src/components/surfaces/teacher-course-center-surface.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/components/surfaces/teacher-course-center-surface.test.tsx</automated>
  </verify>
  <done>建课抽屉已完全从服务端 DTO 获取 school scope；单学校和多学校教师都能提交真实 `schoolId`。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| course center DTO → create drawer | 服务端提供的 school scope 元数据进入客户端表单 |
| create drawer form → createCourseAction | 未信任的学校选择值与课程字段进入 Server Action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-17 | T | `CourseCreateDrawer` selected school state | mitigate | `selectedSchoolId` 只从 `defaultSchoolId` / `availableSchools` 初始化并通过受控表单提交，不再硬编码或静态拼接 |
| T-13-18 | E | multi-school create flow | mitigate | UI 仅暴露服务端 DTO 授权过的学校选项；即使客户端篡改值，后续 Action/DAL 仍由 `assertSchoolAccess()` 拦截 |
| T-13-19 | D | single-school non-`school-1` path | mitigate | 回归测试显式覆盖 `school-9` 场景，避免未来再次把默认学校退化成常量 |
| T-13-20 | R | create flow regression | mitigate | 用 surface-level integration test 覆盖抽屉打开、填写、切换学校、提交 payload 的完整路径 |
</threat_model>

<verification>
- `pnpm vitest run src/components/surfaces/teacher-course-center-surface.test.tsx`
- Read `src/components/courses/course-create-drawer.tsx` and confirm there is no hardcoded `school-1` default
</verification>

<success_criteria>
- COURSE-02 的建课 school scope 缺陷被关掉
- 单学校非 `school-1` 教师可以正确建课
- 多学校教师可以在抽屉内显式选择目标学校并提交真实 `schoolId`
</success_criteria>

<output>
After completion, create `.planning/phases/13-course-center-foundation/13-05-SUMMARY.md`
</output>
