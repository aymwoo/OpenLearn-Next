---
phase: 17-teacher-flow-editor-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dto/lesson-authoring.ts
  - src/lib/dal/lesson-authoring.ts
  - src/lib/dal/lesson-authoring.test.ts
  - src/actions/lesson-authoring-actions.ts
  - src/actions/lesson-authoring-actions.test.ts
autonomous: true
requirements:
  - LESSON-03
  - LESSON-04
  - LESSON-08
  - PLUGIN-01
  - PLUGIN-02
must_haves:
  truths:
    - D-05/D-08: lesson flow 中的 built-in teaching-step 来源可追踪，readiness 能识别不可用 built-in plugin 与结构化阻断问题
    - D-07/D-09/D-10/D-11: preview 与 publish gate 都走 teacher-owned DAL + Server Actions + cache invalidation 链路
    - D-08/D-09: publish 不能再只靠前端最小字段判断，服务端会在真正发布前复用同一套 readiness 规则
  artifacts:
    - path: src/lib/dto/lesson-authoring.ts
      provides: built-in provenance + preview/readiness DTO contracts
      contains: builtInSource
    - path: src/lib/dal/lesson-authoring.ts
      provides: teacher-owned preview/readiness helpers
      contains: getLessonPublishReadinessDTO
    - path: src/actions/lesson-authoring-actions.ts
      provides: publish action with readiness enforcement
      contains: publishLessonAction
  key_links:
    - from: src/lib/dto/lesson-authoring.ts
      to: src/lib/dal/lesson-authoring.ts
      via: readiness/preview DTO parsing and built-in provenance metadata
      pattern: builtInSource|getLessonPublishReadinessDTO
    - from: src/lib/dal/lesson-authoring.ts
      to: src/actions/lesson-authoring-actions.ts
      via: publish path must block on readiness before snapshot creation
      pattern: getLessonPublishReadinessDTO|publishLessonAction
---

<objective>
建立 Phase 17 的后端合同：保留内置教学环节来源信息，补齐 preview/readiness DTO，
并把发布阻断收口到现有 lesson authoring server path。

Purpose: 先解决“无法判断 built-in plugin 是否仍可用、无法统一 preview/readiness/publish gate”
这个地基问题，给后续 UI 与 preview route 提供稳定 contract。
Output: built-in provenance schema、teacher-owned readiness/preview helpers、publish action
服务端阻断与回归测试。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-teacher-flow-editor-enhancement/17-CONTEXT.md
@.planning/phases/17-teacher-flow-editor-enhancement/17-RESEARCH.md
@src/lib/dto/lesson-authoring.ts
@src/lib/dal/lesson-authoring.ts
@src/actions/lesson-authoring-actions.ts
@src/lib/dal/plugins.ts

<interfaces>
From src/lib/dto/lesson-authoring.ts:
```ts
export const lessonStepPayloadSchema = z.discriminatedUnion("type", [
  contentStepPayloadSchema,
  taskStepPayloadSchema,
  quizStepPayloadSchema,
]);

export const LessonEditorDTOSchema = z.object({
  course: CourseDTOSchema,
  lesson: LessonSummaryDTOSchema,
  steps: z.array(LessonStepDTOSchema),
  materials: z.array(LessonMaterialDTOSchema),
  publishState: z.object({ canPublish: z.boolean() }),
});
```

From src/lib/dal/plugins.ts:
```ts
export async function listBuiltInTeachingStepTemplates(input: { actorId: string; schoolId: string })
export async function listPluginsForSchool(input: { actorId: string; schoolId: string })
```

From src/actions/lesson-authoring-actions.ts:
```ts
export async function publishLessonAction(input: FormData | Record<string, unknown>): Promise<ActionResult<unknown>>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define built-in provenance and teacher preview/readiness DTO contracts</name>
  <files>src/lib/dto/lesson-authoring.ts, src/lib/dal/lesson-authoring.ts, src/lib/dal/lesson-authoring.test.ts</files>
  <read_first>src/lib/dto/lesson-authoring.ts, src/lib/dal/lesson-authoring.ts, src/lib/dal/plugins.ts, src/lib/dto/resource-ai.ts, .planning/phases/17-teacher-flow-editor-enhancement/17-CONTEXT.md, .planning/phases/17-teacher-flow-editor-enhancement/17-RESEARCH.md</read_first>
  <behavior>
    - Test 1: built-in teaching-step additions preserve `pluginId`, `builtInKey`, and `pluginName` on the saved lesson-step payload per D-05.
    - Test 2: readiness DTO returns blocking issues for missing lesson title/objective, no active steps, invalid raw step payload, and unavailable built-in plugin per D-08.
    - Test 3: teacher preview DTO returns ordered active steps, materials, and built-in source labels using the same teacher-owned scope as `getLessonEditorDTO` per D-03 and D-07.
  </behavior>
  <action>Extend `src/lib/dto/lesson-authoring.ts` with a reusable `builtInSource` schema `{ pluginId: string, builtInKey: BuiltInTeachingStepKey, pluginName: string }` and add it as an optional field on `content`, `task`, and `quiz` payloads so built-in origin is retained after insertion (per D-04 and D-05). In `src/lib/dal/lesson-authoring.ts`, add `getLessonPublishReadinessDTO({ lessonId })` and `getTeacherLessonPreviewDTO({ lessonId })`. `getLessonPublishReadinessDTO` must read the raw step rows for the lesson, safe-parse each payload, collect blocking issues with stable codes such as `LESSON_TITLE_REQUIRED`, `LESSON_OBJECTIVE_REQUIRED`, `NO_ACTIVE_STEPS`, `STEP_PAYLOAD_INVALID`, and `BUILT_IN_PLUGIN_UNAVAILABLE`, and derive `canPublish`. `getTeacherLessonPreviewDTO` must return the current draft lesson summary, ordered non-archived steps, material references, and built-in source labels for preview use. Reuse current teacher-owned course/lesson scope; do not create a second authoring DAL or move this work into client components.</action>
  <acceptance_criteria>
    - `src/lib/dto/lesson-authoring.ts` contains `builtInSource` and preview/readiness DTO schemas.
    - `src/lib/dal/lesson-authoring.ts` contains `getLessonPublishReadinessDTO` and `getTeacherLessonPreviewDTO`.
    - `src/lib/dal/lesson-authoring.ts` contains `BUILT_IN_PLUGIN_UNAVAILABLE` and `STEP_PAYLOAD_INVALID` issue codes.
    - `src/lib/dal/lesson-authoring.test.ts` asserts built-in provenance retention and readiness blocking cases.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/lesson-authoring.test.ts</automated>
  </verify>
  <done>Lesson authoring now has stable server DTOs for preview and publish readiness, and every built-in step keeps enough provenance to validate plugin availability later.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce publish readiness through the existing lesson authoring actions</name>
  <files>src/actions/lesson-authoring-actions.ts, src/actions/lesson-authoring-actions.test.ts</files>
  <read_first>src/actions/lesson-authoring-actions.ts, src/actions/lesson-authoring-actions.test.ts, src/lib/dal/lesson-authoring.ts, src/lib/cache-policy.ts</read_first>
  <behavior>
    - Test 1: `publishLessonAction` returns a structured blocked result when readiness reports blocking issues per D-09.
    - Test 2: successful publish still invalidates `teacherCourses`, `course`, `lesson`, and `steps` cache tags per D-11.
    - Test 3: publish action keeps existing error mapping for `CONFLICT`, `UNAUTHORIZED`, and `NOT_FOUND` without introducing a bypass path.
  </behavior>
  <action>Update `src/actions/lesson-authoring-actions.ts` so `publishLessonAction()` calls `getLessonPublishReadinessDTO()` before `publishLesson()`. When readiness returns blocking issues, return `{ ok: false, error: "PUBLISH_BLOCKED", message: "发布前检查未通过。", issues: [...] }` instead of attempting snapshot creation (per D-08 and D-09). Preserve the current `updateTag(cacheTags.teacherCourses(actorId))`, `course`, `lesson`, and `steps` invalidation on successful publish, and keep the same authorization / conflict / not-found mapping path. Do not create a parallel publish route or move publish gating into the client.</action>
  <acceptance_criteria>
    - `src/actions/lesson-authoring-actions.ts` contains `PUBLISH_BLOCKED`.
    - `src/actions/lesson-authoring-actions.ts` calls `getLessonPublishReadinessDTO` before `publishLesson`.
    - `src/actions/lesson-authoring-actions.test.ts` contains a blocked publish assertion and still asserts lesson/steps cache invalidation on success.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/actions/lesson-authoring-actions.test.ts</automated>
  </verify>
  <done>The publish action now shares the same readiness gate as the future UI and cannot publish a draft that fails structural checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lesson-step payload JSON → readiness parser | Stored step payloads become publish-blocking validation input |
| plugin registry → lesson publish gate | Enabled built-in plugin state influences whether a draft may be published |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | T | `src/lib/dal/lesson-authoring.ts` | mitigate | Safe-parse raw payloads and emit structured issue codes so invalid step JSON cannot silently slip into publish snapshots. |
| T-17-02 | E | `src/lib/dto/lesson-authoring.ts` | mitigate | Persist built-in provenance metadata in the step payload so publish/readiness can validate plugin availability against the allowlisted registry. |
| T-17-03 | R | `src/actions/lesson-authoring-actions.ts` | mitigate | Enforce readiness inside `publishLessonAction` so blocked publishes are auditable and not just front-end hints. |
| T-17-04 | I | `src/lib/dal/lesson-authoring.ts` | accept | Preview/readiness DTOs expose only teacher-owned lesson metadata already visible in the editor. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/lesson-authoring.test.ts src/actions/lesson-authoring-actions.test.ts`
- `node -e "const fs=require('fs');const s=fs.readFileSync('src/actions/lesson-authoring-actions.ts','utf8');if(!s.includes('PUBLISH_BLOCKED')||!s.includes('getLessonPublishReadinessDTO'))process.exit(1)"`
</verification>

<success_criteria>
- Built-in steps retain plugin provenance after insertion into the lesson flow.
- Preview and publish readiness are available through teacher-owned server DTOs.
- Publish cannot bypass readiness via the existing Server Action path.
</success_criteria>

<output>
After completion, create `.planning/phases/17-teacher-flow-editor-enhancement/17-01-SUMMARY.md`
</output>
