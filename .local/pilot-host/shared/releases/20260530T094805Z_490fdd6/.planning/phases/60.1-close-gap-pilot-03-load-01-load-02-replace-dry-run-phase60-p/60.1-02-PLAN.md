---
phase: 60.1-close-gap-pilot-03-load-01-load-02-replace-dry-run-phase60-p
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/index.ts
  - src/db/index.test.ts
  - scripts/load/phase60-fixtures.ts
  - scripts/load/phase60-fixtures.test.ts
autonomous: true
requirements:
  - PILOT-03
  - LOAD-01
  - LOAD-02
requirement_scope: support-only
requirements_note: "Support/blocker-removal plan only: improves repo-local substitute stability for Phase 60 evidence generation, but does not itself create live pilot-target evidence or close PILOT-03 / LOAD-01 / LOAD-02."
gap_closure: true
must_haves:
  truths:
    - "This is a blocker-removal plan only; by itself it does not satisfy the live evidence closeout required by PILOT-03 / LOAD-01 / LOAD-02."
    - "Repo-local Phase 60 fixture seeding no longer fails immediately on transient SQLite write contention from the local pilot-host substitute topology."
    - "Local file SQLite connections enter an explicit concurrency posture before Phase 60 proof writes begin."
    - "If lock contention persists, the failure becomes an explicit Phase 60 blocker naming the shared DB posture instead of a raw SQLITE_BUSY stack."
  artifacts:
    - path: src/db/index.ts
      provides: "Singleton libSQL client with local SQLite concurrency bootstrap"
    - path: scripts/load/phase60-fixtures.ts
      provides: "Busy-aware Phase 60 fixture seeding on the canonical @/db path"
    - path: src/db/index.test.ts
      provides: "Focused regression coverage for local SQLite concurrency bootstrap"
    - path: scripts/load/phase60-fixtures.test.ts
      provides: "Focused regression coverage for SQLITE_BUSY retry and blocker behavior"
  key_links:
    - from: src/db/index.ts
      to: scripts/load/phase60-fixtures.ts
      via: ensureLocalSqliteConcurrencyPragmas import
      pattern: ensureLocalSqliteConcurrencyPragmas
    - from: scripts/load/phase60-fixtures.ts
      to: scripts/proof-phase60-load-smoke.ts
      via: ensurePhase60Fixtures proof seeding
      pattern: ensurePhase60Fixtures
---

<objective>
This is a support/blocker-removal plan, not the direct live-evidence closeout plan.

Stabilize the repo-local Phase 60 substitute lane by fixing the missing local SQLite concurrency posture and by making fixture seeding handle short write-lock windows honestly.

Purpose: Close the SQLite root cause called out in `.planning/debug/phase60-local-sqlite-busy.md` without redesigning Phase 60 or bypassing the canonical `@/db` singleton.
Output: local SQLite bootstrap in `src/db/index.ts`, busy-aware fixture seeding, and focused regression tests.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60.1-close-gap-pilot-03-load-01-load-02-replace-dry-run-phase60-p/CONTEXT.md
@.planning/phases/60.1-close-gap-pilot-03-load-01-load-02-replace-dry-run-phase60-p/60.1-PATTERNS.md
@.planning/phases/60-load-degrade-pilot-rehearsal/60-02-SUMMARY.md
@.planning/debug/phase60-local-sqlite-busy.md
@src/db/index.ts
@scripts/load/phase60-fixtures.ts

<interfaces>
From `src/db/index.ts`:

```ts
const client = createClient({
  url: process.env.DB_FILE_NAME || "file:local.db",
});

export const db = drizzle(client, { schema });
```

From `scripts/load/phase60-fixtures.ts`:

```ts
export async function ensurePhase60Fixtures(options?: { dryRun?: boolean; baseUrl?: string })
async function upsertUser(input: { email: string; name: string; studentNumber?: string | null })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the singleton libSQL client into a local SQLite concurrency posture</name>
  <read_first>
    - src/db/index.ts
    - .planning/phases/60.1-close-gap-pilot-03-load-01-load-02-replace-dry-run-phase60-p/CONTEXT.md
    - .planning/phases/60.1-close-gap-pilot-03-load-01-load-02-replace-dry-run-phase60-p/60.1-PATTERNS.md
    - .planning/debug/phase60-local-sqlite-busy.md
  </read_first>
  <pre_edit_checks>
    - Run `gitnexus_impact({ target: "db", direction: "upstream" })` and `gitnexus_context({ name: "db" })` before editing the singleton export in `src/db/index.ts`.
  </pre_edit_checks>
  <files>src/db/index.ts, src/db/index.test.ts</files>
  <behavior>
    - Test 1: local `file:` SQLite URLs expose one cached bootstrap helper that applies `PRAGMA journal_mode = WAL` and `PRAGMA busy_timeout = 5000` before proof writes proceed.
    - Test 2: non-local libSQL URLs keep the existing singleton client shape and skip local-only PRAGMA work.
    - Test 3: the module still exports the same `db = drizzle(client, { schema })` truth and does not instantiate a second DB client.
  </behavior>
  <acceptance_criteria>
    - `src/db/index.ts` still contains exactly one `createClient(` call and still exports `db = drizzle(client, { schema })`.
    - `src/db/index.ts` exports `ensureLocalSqliteConcurrencyPragmas` and contains the exact strings `PRAGMA journal_mode = WAL` and `PRAGMA busy_timeout = 5000`.
    - The helper is cached so repeated imports do not re-run PRAGMAs on every call.
    - `src/db/index.test.ts` proves local-file bootstrap vs. remote no-op behavior with `pnpm exec vitest --run src/db/index.test.ts`.
  </acceptance_criteria>
  <action>Before editing, run the GitNexus impact/context checks listed above and keep the blast radius confined to the existing singleton `db` export. Per the root cause in `.planning/debug/phase60-local-sqlite-busy.md` and the singleton boundary note in `60.1-PATTERNS.md`, update `src/db/index.ts` in place rather than creating a second client. Keep the existing `createClient({ url: process.env.DB_FILE_NAME || "file:local.db" })` + `drizzle(client, { schema })` shape, and add a cached exported helper named `ensureLocalSqliteConcurrencyPragmas()` that only runs for local `file:` SQLite URLs. Have that helper execute `PRAGMA journal_mode = WAL`, `PRAGMA busy_timeout = 5000`, and `PRAGMA synchronous = NORMAL` through the existing client, returning a shared promise so concurrent callers reuse one bootstrap. Add `src/db/index.test.ts` to lock the local-vs-remote branching and the single-client invariant per D-60.1-05.</action>
  <verify>
    <automated>pnpm exec vitest --run src/db/index.test.ts</automated>
  </verify>
  <done>The canonical `@/db` singleton now exposes an explicit local SQLite concurrency posture without changing remote/libsql semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make Phase 60 fixture seeding wait through transient locks and fail with an explicit blocker</name>
  <read_first>
    - scripts/load/phase60-fixtures.ts
    - src/db/index.ts
    - .planning/debug/phase60-local-sqlite-busy.md
    - .planning/phases/60.1-close-gap-pilot-03-load-01-load-02-replace-dry-run-phase60-p/CONTEXT.md
    - .planning/phases/60.1-close-gap-pilot-03-load-01-load-02-replace-dry-run-phase60-p/60.1-PATTERNS.md
  </read_first>
  <pre_edit_checks>
    - Run `gitnexus_impact({ target: "ensurePhase60Fixtures", direction: "upstream" })` and `gitnexus_context({ name: "ensurePhase60Fixtures" })` before editing the main fixture-seeding path.
    - Run `gitnexus_impact({ target: "upsertUser", direction: "upstream" })` and `gitnexus_context({ name: "upsertUser" })` before changing the hot lock-contention write path.
  </pre_edit_checks>
  <files>scripts/load/phase60-fixtures.ts, scripts/load/phase60-fixtures.test.ts</files>
  <behavior>
    - Test 1: `ensurePhase60Fixtures()` awaits the shared SQLite bootstrap before the first query on the live path.
    - Test 2: lock-prone writes retry only on `SQLITE_BUSY` using a bounded backoff window, then continue on success.
    - Test 3: when the retry budget is exhausted, the script throws `PHASE60_LOCAL_SQLITE_BUSY_BLOCKER` with the configured DB file in the message instead of a raw Drizzle stack.
  </behavior>
  <acceptance_criteria>
    - `scripts/load/phase60-fixtures.ts` imports `ensureLocalSqliteConcurrencyPragmas` from `@/db` and awaits it on the non-dry-run path before the first `db.select` or `db.insert`.
    - The file contains a bounded retry helper that matches on `SQLITE_BUSY` and includes the exact token `PHASE60_LOCAL_SQLITE_BUSY_BLOCKER` in the final error path.
    - The fixture writer preserves the existing single-entry `ensurePhase60Fixtures()` flow and still writes one `phase60-fixtures.generated.json` file.
    - `scripts/load/phase60-fixtures.test.ts` covers successful retry and exhausted-retry blocker behavior with `pnpm exec vitest --run scripts/load/phase60-fixtures.test.ts`.
  </acceptance_criteria>
  <action>Before editing, run the GitNexus impact/context checks listed above so the retry path changes stay on the existing `ensurePhase60Fixtures()` / `upsertUser()` execution flow. Update `scripts/load/phase60-fixtures.ts` per D-60.1-05 and the debug root cause. Keep the existing single-entry seed contract and single fixture file output, but import `ensureLocalSqliteConcurrencyPragmas()` and await it before the first live DB operation. Add a local helper such as `withSqliteBusyRetry(label, fn)` that retries only when the underlying libSQL/Drizzle error contains `SQLITE_BUSY`; use a concrete bounded budget of 8 attempts with a 250ms base delay capped at 1000ms. Route the known hotspots called out in `60.1-PATTERNS.md`—`upsertUser()` plus the school/course/class/session seeding writes—through that helper. On exhaustion, throw `PHASE60_LOCAL_SQLITE_BUSY_BLOCKER: shared SQLite truth still locked for ${process.env.DB_FILE_NAME || "file:local.db"}` and mention stopping stale `server.ts` / worker holders or switching to the isolated local-proof lane from plan 03. Add `scripts/load/phase60-fixtures.test.ts` to lock retry-only-on-busy behavior and the explicit blocker token.</action>
  <verify>
    <automated>pnpm exec vitest --run scripts/load/phase60-fixtures.test.ts</automated>
  </verify>
  <done>Phase 60 live fixture seeding can absorb transient local lock windows and surfaces a named blocker when the shared DB posture remains unsafe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local proof scripts → SQLite truth file | repo-local proof writes cross from CLI automation into the durable classroom truth store |
| multiple local processes → shared `file:` SQLite client | web, worker, and verifier contend on one truth file and can deny service to each other |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60.1-02-01 | Denial of Service | `src/db/index.ts` local client posture | mitigate | Apply WAL + busy-timeout bootstrap on the singleton client so short write locks do not immediately abort proof runs. |
| T-60.1-02-02 | Tampering | `scripts/load/phase60-fixtures.ts` retry path | mitigate | Retry only on `SQLITE_BUSY`, keep the single proof scope, and throw an explicit blocker instead of silently swallowing other DB failures. |
</threat_model>

<verification>
- `pnpm exec vitest --run src/db/index.test.ts`
- `pnpm exec vitest --run scripts/load/phase60-fixtures.test.ts`
</verification>

<success_criteria>
- This plan only removes repo-local SQLite blockers; live pilot-target evidence is still produced by plan `60.1-01`.
- Local Phase 60 fixture seeding no longer dies on the first transient SQLite lock.
- The canonical `@/db` path gains an explicit local concurrency posture without introducing a second client.
- Persistent lock contention now reports a named, actionable blocker tied to the shared DB file.
</success_criteria>

<output>
After completion, create `.planning/phases/60.1-close-gap-pilot-03-load-01-load-02-replace-dry-run-phase60-p/60.1-02-SUMMARY.md`
</output>
