---
phase: 25-teaching-data-capture-and-session-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dto/classroom.ts
  - src/lib/dal/classroom.ts
  - src/app/(classroom)/classroom/page.tsx
autonomous: true
requirements:
  - ANALYTICS-01
must_haves:
  truths:
    - Phase 25 的 session recap 必须继续留在 `/classroom` 域内，不新开 analytics 主路由，也不让 `/teacher/review` 抢主入口。
    - recap 读模型必须复用 Phase 21-24 已沉淀的 session-first durable truth，不能新造 analytics snapshot table 或第二真相源。
    - lesson-domain attempts / feedback 只能作为 `待反馈提交` 的只读桥接输入，不得反向复制回 classroom domain。
  artifacts:
    - path: src/lib/dto/classroom.ts
      provides: session recap DTO contracts and deterministic metric shapes
      contains: ClassroomSessionRecapDTOSchema
    - path: src/lib/dal/classroom.ts
      provides: session-first recap aggregation and history session list
      contains: getClassroomSessionRecapDTO
    - path: src/app/(classroom)/classroom/page.tsx
      provides: live vs ended/history session server-side route selection
      contains: getClassroomSessionRecapDTO
  key_links:
    - from: src/lib/dal/classroom.ts
      to: src/app/(classroom)/classroom/page.tsx
      via: page selects recap DTO for ended/history session contexts inside `/classroom`
      pattern: getClassroomSessionRecapDTO|sessionId
    - from: src/lib/dto/classroom.ts
      to: src/lib/dal/classroom.ts
      via: recap summary/workload/student/step contracts freeze the aggregation semantics
      pattern: ClassroomSessionRecapDTOSchema|participationBuckets|workload
---

<objective>
为 Phase 25 建立 session-first recap 的核心读模型、DTO 与 `/classroom` 路由分支，
让课堂结束后和历史回看都能在同一域内读取 deterministic summary，而不是继续停留在 live runtime 或跳到新的 analytics 页面。

Purpose: 先把数据合同与主路由切换锁死，后续 UI 只需消费稳定 recap DTO。
Output: recap DTO schemas, DAL aggregation helpers, and same-domain route selection for ended/history sessions.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-teaching-data-capture-and-session-analytics/25-CONTEXT.md
@.planning/phases/25-teaching-data-capture-and-session-analytics/25-RESEARCH.md
@.planning/phases/25-teaching-data-capture-and-session-analytics/25-UI-SPEC.md
@src/app/(classroom)/classroom/page.tsx
@src/components/surfaces/classroom-console-surface.tsx
@src/lib/dto/classroom.ts
@src/lib/dal/classroom.ts
@src/lib/dal/learning.ts
@src/db/schema.ts

<interfaces>
Locked recap read entry:
```ts
type GetClassroomSessionRecapInput = {
  sessionId: string
  studentId?: string
  stepId?: string
  detailTab?: "students" | "steps"
}

type ClassroomSessionRecapDTO = {
  session: {
    id: string
    status: "ended"
    lessonId: string
    lessonTitle: string
    className: string
    startedAt: string
    endedAt: string
  }
  summary: {
    completionLabel: string
    completionCount: number
    totalStudents: number
    participationBuckets: {
      active: number
      normal: number
      attention: number
      unevaluated: number
    }
  }
  workload: {
    followUpSignalsCount: number
    pendingFeedbackCount: number
  }
  studentSummaries: ClassroomSessionRecapStudentSummaryDTO[]
  selectedStudent: ClassroomSessionRecapStudentDetailDTO | null
  stepSummaries: ClassroomSessionRecapStepSummaryDTO[]
}
```

History reopen contract:
```ts
type ClassroomConsoleSessionEntryDTO = {
  id: string
  status: "live" | "ended"
  lessonTitle: string
  className: string
  startedAt: string
  endedAt: string | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define recap DTO contracts and deterministic aggregation helpers</name>
  <files>src/lib/dto/classroom.ts, src/lib/dal/classroom.ts</files>
  <read_first>src/lib/dto/classroom.ts, src/lib/dal/classroom.ts, src/lib/dal/learning.ts, src/db/schema.ts, .planning/phases/25-teaching-data-capture-and-session-analytics/25-CONTEXT.md, .planning/phases/25-teaching-data-capture-and-session-analytics/25-RESEARCH.md</read_first>
  <behavior>
    - Test 1: recap DTO exposes completion、participation、submission/workload、student summaries、step diagnostics in one teacher-scoped contract.
    - Test 2: participation buckets explicitly include `未评价` and never infer unevaluated students as `normal`.
    - Test 3: `待反馈提交` is derived from latest attempts + `attemptFeedback`, while classroom follow-up signals stay owned by classroom truth.
  </behavior>
  <action>In `src/lib/dto/classroom.ts`, add recap contracts including `ClassroomSessionRecapDTOSchema`, `ClassroomSessionRecapSummaryDTOSchema`, `ClassroomSessionWorkloadDTOSchema`, `ClassroomSessionRecapStudentSummaryDTOSchema`, `ClassroomSessionRecapStudentDetailDTOSchema`, `ClassroomSessionRecapStepSummaryDTOSchema`, and any `detailTab` / history entry schemas needed by `/classroom`. In `src/lib/dal/classroom.ts`, create `getClassroomSessionRecapDTO({ sessionId, studentId?, stepId?, detailTab? })` and helper builders such as `buildParticipationBuckets`, `buildFeedbackWorkloadFromAttempts`, and `buildClassroomSignalWorkload`. The aggregation must remain teacher-scoped to the requested session, derive participation from latest formative evaluation rows plus participant roster, derive follow-up signals from deterministic classroom-domain conditions, and bridge latest task/quiz attempts plus `attemptFeedback` for pending feedback without persisting mirrored analytics rows.</action>
  <acceptance_criteria>
    - `src/lib/dto/classroom.ts` contains `ClassroomSessionRecapDTOSchema` and a dedicated workload schema that separates `followUpSignalsCount` from `pendingFeedbackCount`.
    - `src/lib/dal/classroom.ts` contains `getClassroomSessionRecapDTO` plus helper names aligned to participation buckets and workload bridging.
    - `src/lib/dal/classroom.ts` contains an explicit `unevaluated` or `未评价` bucket and does not default missing participation to `normal`.
    - `src/lib/dal/classroom.ts` reads latest attempts / `attemptFeedback` as a bridge and does not add a new analytics persistence table or write path.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/classroom.test.ts src/lib/dal/learning.test.ts</automated>
  </verify>
  <done>Phase 25 gets one authoritative session recap read model backed by existing truths and explicit bridge semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ended/history session selection inside `/classroom` without creating a second route family</name>
  <files>src/app/(classroom)/classroom/page.tsx, src/lib/dal/classroom.ts</files>
  <read_first>src/app/(classroom)/classroom/page.tsx, src/lib/dal/classroom.ts, src/components/surfaces/classroom-console-surface.tsx, .planning/phases/25-teaching-data-capture-and-session-analytics/25-CONTEXT.md, .planning/phases/25-teaching-data-capture-and-session-analytics/25-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: `/classroom` can reopen ended/history sessions by `sessionId` without redirecting to another teacher route.
    - Test 2: ended sessions fetch recap DTO server-side instead of rendering live runtime first and then switching client-side.
    - Test 3: the console session list exposes both live and recent ended sessions so history reopen remains inside classroom domain.
  </behavior>
  <action>Update `src/lib/dal/classroom.ts` so the console-level read model returns recent ended session entries alongside any live session entry, keeping the same teacher/class/school scope. Then update `src/app/(classroom)/classroom/page.tsx` so `searchParams.sessionId` resolves either a live or ended/history classroom session entry; when the selected session status is `ended`, fetch `getClassroomSessionRecapDTO(...)` server-side and pass it down instead of the live snapshot path. Keep the route family as `/classroom`, with same-domain state driven by `sessionId`, optional `studentId`, optional `stepId`, and optional recap detail tab params.</action>
  <acceptance_criteria>
    - `src/lib/dal/classroom.ts` extends the console/session selector DTO with recent ended or history entries.
    - `src/app/(classroom)/classroom/page.tsx` contains a server-side branch that calls `getClassroomSessionRecapDTO` when `selectedSession.status === "ended"`.
    - no new primary route such as `/teacher/analytics`, `/teacher/session-recap`, or `/teacher/review` is introduced for Phase 25 recap.
    - route state remains keyed by `sessionId` inside `/classroom`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/classroom.test.ts src/app/(classroom)/classroom/page.test.tsx</automated>
  </verify>
  <done>Ended and historical session recap become first-class `/classroom` states instead of separate analytics pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classroom durable truth -> recap aggregation | recap metrics must stay deterministic, teacher-scoped, and auditable from existing classroom/session facts |
| lesson-domain feedback bridge -> recap workload | bridged attempt metrics must not mutate classroom truth or drift into a second analytics source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | T | `src/lib/dal/classroom.ts` recap aggregation | mitigate | Keep all recap formulas in DAL helpers sourced from existing classroom/session tables; forbid client recomputation and forbid new snapshot persistence. |
| T-25-02 | I | `/classroom` ended/history selection | mitigate | Resolve session scope server-side through teacher-scoped console/session lookup before returning recap DTOs. |
| T-25-03 | R | pending feedback bridge | mitigate | Derive `待反馈提交` from latest attempts + `attemptFeedback` only and document the bridge semantics in DTO/copy. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/classroom.test.ts src/lib/dal/learning.test.ts src/app/(classroom)/classroom/page.test.tsx`
</verification>

<success_criteria>
- `/classroom` can select live, ended, and recent historical sessions without changing route family.
- session recap metrics come from a single teacher-scoped DAL contract with explicit participation and workload semantics.
- no second analytics persistence layer or recap route is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/25-teaching-data-capture-and-session-analytics/25-01-SUMMARY.md`
</output>
