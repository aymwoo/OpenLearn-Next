---
phase: 22-teacher-orchestration-workspace-and-launch-preparation
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - src/lib/dto/classroom.ts
  - src/lib/dal/classroom.ts
  - src/lib/dal/classroom.test.ts
  - src/components/classroom/classroom-launch-preview.tsx
  - src/components/classroom/classroom-launch-panel.tsx
  - src/components/surfaces/classroom-launch-surface.tsx
autonomous: true
requirements:
  - ORCH-02
  - ORCH-03
must_haves:
  truths:
    - `/teacher/launch` becomes a structured launch-preparation workspace with a class-facing run sheet as the single main stage.
    - launch readiness is graded into blockers vs non-blocking preparation cues, with blockers staying narrow.
    - launch continues to read published lesson snapshots only and still launches a session through the existing action path.
  artifacts:
    - path: src/lib/dto/classroom.ts
      provides: typed orchestration workspace, roster summary, and launch readiness contracts
      contains: ClassroomConsoleDTOSchema
    - path: src/lib/dal/classroom.ts
      provides: server-owned launch workspace aggregation from published snapshot, roster data, and readiness inputs
      contains: getClassroomConsoleDTO
    - path: src/components/surfaces/classroom-launch-surface.tsx
      provides: three-part orchestration workspace with run sheet as the main stage
      contains: 开启新课堂
  key_links:
    - from: src/lib/dal/classroom.ts
      to: src/lib/dto/classroom.ts
      via: launch workspace DTO, run-sheet summary, roster summary, and readiness buckets are all assembled server-side
      pattern: getClassroomConsoleDTO|ClassroomConsoleDTOSchema
    - from: src/components/classroom/classroom-launch-panel.tsx
      to: src/components/classroom/classroom-launch-preview.tsx
      via: launch controls stay coupled to the run sheet and published version id rather than a separate preview system
      pattern: launchPreview|publishedVersionId
---

<objective>
把 `/teacher/launch` 升级成真正的开课前 orchestration workspace：主舞台展示 class-facing
run sheet，次级区承载整班摘要与 readiness 分层，同时保持现有 published-snapshot launch
边界和 live-session recovery 次级位置。

Purpose: 兑现 ORCH-02 / ORCH-03 的 teacher-ready launch preparation，而不是继续停留在
“选课时 + 选班级”的最薄交互。
Output: classroom-side orchestration DTO、DAL aggregation、launch surface/run-sheet UI。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-teacher-orchestration-workspace-and-launch-preparation/22-CONTEXT.md
@.planning/phases/22-teacher-orchestration-workspace-and-launch-preparation/22-RESEARCH.md
@.planning/phases/22-teacher-orchestration-workspace-and-launch-preparation/22-UI-SPEC.md
@src/app/(teacher)/teacher/launch/page.tsx
@src/lib/dto/classroom.ts
@src/lib/dal/classroom.ts
@src/components/surfaces/classroom-launch-surface.tsx
@src/components/classroom/classroom-launch-panel.tsx
@src/components/classroom/classroom-launch-preview.tsx

<interfaces>
Current launch-side contract:
```ts
ClassroomConsoleDTO = {
  liveSessions: ClassroomLiveSessionSummaryDTO[]
  publishedLessons: ClassroomLaunchLessonOptionDTO[]
  emptyStateCopy: string
  launchPreviewEmptyState: ClassroomLaunchPreviewEmptyStateDTO
}
```

Current launch action dependency that must remain intact:
```ts
FormData.append('lessonId', selectedLessonId)
FormData.append('publishedVersionId', selectedLesson!.publishedVersionId)
FormData.append('classId', selectedClassId)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define classroom-side orchestration and readiness DTOs on top of published snapshot launch data</name>
  <files>src/lib/dto/classroom.ts, src/lib/dal/classroom.ts, src/lib/dal/classroom.test.ts</files>
  <read_first>src/lib/dto/classroom.ts, src/lib/dal/classroom.ts, src/lib/dal/classroom.test.ts, src/lib/dto/lesson-authoring.ts, src/lib/dal/lesson-authoring.ts, .planning/phases/22-teacher-orchestration-workspace-and-launch-preparation/22-CONTEXT.md, .planning/phases/22-teacher-orchestration-workspace-and-launch-preparation/22-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `getClassroomConsoleDTO()` exposes typed roster summary and launch-readiness buckets for each launchable lesson/class context.
    - Test 2: blockers remain narrow: no published lessons and no launchable roster/class path become hard blockers; inferred teaching design or weak evidence/material setup stay non-blocking.
    - Test 3: run-sheet data still comes only from published lesson snapshots and keeps the current `publishedVersionId` launch contract intact.
  </behavior>
  <action>Extend `src/lib/dto/classroom.ts` with the orchestration workspace contract needed by `/teacher/launch`. Add explicit typed shapes for roster summary, launch readiness groups (`blockingIssues`, `attentionIssues`, `advisoryIssues`), and any run-sheet summary/meta block that the surface needs. Then update `src/lib/dal/classroom.ts` so `getClassroomConsoleDTO()` computes those fields by combining: (1) current published lessons and linked classes, (2) class member counts for whole-class launch summary, (3) existing `buildLaunchPreview()` output, and (4) lesson-side readiness/preparation facts where relevant. Preserve the existing `launchPreview` published-snapshot assembly and keep `launchClassroomSession()` unchanged in its fundamental input contract. Do not add subgroup launch, multi-class launch, or session-specific editable emphasis state.</action>
  <acceptance_criteria>
    - `src/lib/dto/classroom.ts` contains typed fields for roster summary and readiness buckets on the launch-side contract.
    - `src/lib/dal/classroom.ts` contains logic that computes `blockingIssues`, `attentionIssues`, and `advisoryIssues` for launch preparation.
    - `src/lib/dal/classroom.ts` still contains `buildLaunchPreview(snapshot, lesson.id, lesson.title)` or equivalent published-snapshot usage.
    - `src/lib/dal/classroom.test.ts` contains assertions for narrow blockers and non-blocking inferred/material/evidence cues.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/classroom.test.ts</automated>
  </verify>
  <done>`/teacher/launch` now has a server-owned orchestration workspace contract instead of relying on thin lesson/class selectors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Turn `/teacher/launch` into a three-part orchestration workspace with a main run sheet</name>
  <files>src/components/classroom/classroom-launch-preview.tsx, src/components/classroom/classroom-launch-panel.tsx, src/components/surfaces/classroom-launch-surface.tsx</files>
  <read_first>src/components/classroom/classroom-launch-preview.tsx, src/components/classroom/classroom-launch-panel.tsx, src/components/surfaces/classroom-launch-surface.tsx, src/components/surfaces/teacher-lesson-preview-surface.tsx, src/components/authoring/authoring-status-panel.tsx, .planning/phases/22-teacher-orchestration-workspace-and-launch-preparation/22-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: run sheet becomes the main visual stage and still renders steps, materials, and evidence reminders.
    - Test 2: readiness panel clearly separates `阻断项 / 需关注 / 建议完善` and does not block on default inference alone.
    - Test 3: live session recovery remains secondary, and launch controls still submit the same lesson/class/publishedVersion triplet.
  </behavior>
  <action>Refactor `src/components/surfaces/classroom-launch-surface.tsx`, `src/components/classroom/classroom-launch-panel.tsx`, and `src/components/classroom/classroom-launch-preview.tsx` into the Phase 22 three-part orchestration workspace. Make the run sheet the main stage inside the primary launch area, explicitly foregrounding step order, estimated minutes, material cues, and evidence reminders. Add a roster summary area that only describes whole-class launch scope and class size; do not add student exclusion or subgroup controls. Add a readiness panel with the exact three Chinese bucket labels `阻断项`, `需关注`, and `建议完善`. Keep the current `launchClassroomSessionAction()` flow and make sure form submission still appends `lessonId`, `publishedVersionId`, and `classId`. Preserve the secondary live-session recovery card area rather than promoting it to a co-equal hero.</action>
  <acceptance_criteria>
    - `src/components/surfaces/classroom-launch-surface.tsx` still contains recovery/live classroom UI and also contains a main run-sheet-oriented launch workspace.
    - `src/components/classroom/classroom-launch-preview.tsx` contains materials and evidence reminder rendering and keeps the published-snapshot wording.
    - `src/components/classroom/classroom-launch-panel.tsx` still appends `publishedVersionId` to the launch form data and contains the exact labels `阻断项`, `需关注`, and `建议完善` or renders them through children.
    - none of the modified launch components contain subgroup/exclusion UI strings such as `排除学生`, `小组`, or `子集启动`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/classroom/classroom-launch-panel.test.tsx</automated>
  </verify>
  <done>the launch page now behaves like a teacher-ready orchestration workspace while preserving existing launch safety constraints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published lesson snapshot -> launch workspace UI | all class-facing run-sheet truths must still come from the published snapshot path |
| classroom launch controls -> session creation action | UI restructuring must not mutate the existing launch action contract or add unauthorized launch scopes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | T | `src/lib/dal/classroom.ts` | mitigate | Keep launch workspace aggregation server-side and derive readiness from typed facts, not client-only heuristics. |
| T-22-06 | E | `src/components/classroom/classroom-launch-panel.tsx` | mitigate | Preserve whole-class-only launch and the current `lessonId + publishedVersionId + classId` submission contract. |
| T-22-07 | I | `src/components/classroom/classroom-launch-preview.tsx` | mitigate | Continue to label run-sheet content as published-snapshot-driven and avoid draft lesson leakage. |
| T-22-08 | D | `src/components/surfaces/classroom-launch-surface.tsx` | mitigate | Keep live recovery secondary so the primary launch flow remains clear and does not split attention. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/classroom.test.ts`
- `pnpm test --run src/components/classroom/classroom-launch-panel.test.tsx`
</verification>

<success_criteria>
- `/teacher/launch` has a main run sheet, roster summary, and graded readiness panel.
- launch blockers stay narrow while quality gaps remain visible but non-blocking.
- classroom launch still depends on the current published snapshot and session action contract.
</success_criteria>

<output>
After completion, create `.planning/phases/22-teacher-orchestration-workspace-and-launch-preparation/22-02-SUMMARY.md`
</output>
