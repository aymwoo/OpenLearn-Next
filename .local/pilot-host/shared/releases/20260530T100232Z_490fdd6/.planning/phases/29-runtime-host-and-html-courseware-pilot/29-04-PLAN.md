---
phase: 29-runtime-host-and-html-courseware-pilot
plan: 04
type: execute
wave: 4
depends_on: [29-01, 29-02, 29-03]
files_modified:
  - src/app/runtime/html-courseware/pilot/page.tsx
  - src/features/runtime-platform/host/runtime-host.test.tsx
  - src/components/surfaces/student-player-surfaces.test.ts
  - src/components/surfaces/classroom-console-surface.test.tsx
  - src/actions/classroom-actions.test.ts
  - scripts/verify-phase29-runtime-host.ts
  - package.json
autonomous: true
requirements: [RHOST-01, RHOST-02, RHOST-03]
must_haves:
  truths:
    - "D-11 / D-12：仓库存在一个本地 built-in HTML runtime pilot entry，可在 sandboxed host 中真实运行一次互动。"
    - "D-13：该 pilot 的 submit 继续复用现有 runtime submit truth bridge，而不是 client-only 成功提示。"
    - "Phase 29 的 proof 只证明 pilot 可运行，不把 Phase 32 的最终 hardening、milestone proof 和 `RHOST-04` close 提前吞进来。"
  artifacts:
    - path: "src/app/runtime/html-courseware/pilot/page.tsx"
      provides: "local built-in HTML runtime pilot entry"
      contains: "postMessage"
    - path: "scripts/verify-phase29-runtime-host.ts"
      provides: "canonical phase verifier for Runtime Host pilot"
      contains: "verify:phase29"
    - path: "package.json"
      provides: "phase verification command"
      contains: "verify:phase29"
  key_links:
    - from: "src/app/runtime/html-courseware/pilot/page.tsx"
      to: "src/features/runtime-platform/host/runtime-host-bridge.ts"
      via: "typed host <-> iframe browser bridge"
      pattern: "postMessage"
    - from: "scripts/verify-phase29-runtime-host.ts"
      to: "src/actions/classroom-actions.ts"
      via: "phase gate locks runtime submit wiring to current trusted boundary"
      pattern: "submitRuntimeStateAction|runtime-submit"
---

<objective>
补齐本地 HTML runtime pilot entry，并为 Phase 29 建立 end-to-end proof 和专属
`verify:phase29` 安全门。

Purpose: 没有真实运行的 runtime asset 与相应回归门，Phase 29 只能算“宿主壳存在”，
不算 runtime-hosted pilot 成立。
Output: local runtime pilot entry, focused regression coverage, `verify:phase29`.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-runtime-host-and-html-courseware-pilot/29-CONTEXT.md
@.planning/phases/29-runtime-host-and-html-courseware-pilot/29-RESEARCH.md
@src/features/runtime-platform/host/runtime-host-client.tsx
@src/features/runtime-platform/host/runtime-host-bridge.ts
@src/actions/classroom-actions.ts
@src/features/runtime-platform/classroom/runtime-session.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立本地 HTML courseware runtime pilot entry 并跑通一次真实互动</name>
  <read_first>
    - src/features/runtime-platform/host/runtime-host-client.tsx
    - src/features/runtime-platform/host/runtime-host-bridge.ts
    - src/features/runtime-platform/contracts/bridge.ts
    - src/actions/classroom-actions.ts
  </read_first>
  <files>src/app/runtime/html-courseware/pilot/page.tsx, src/features/runtime-platform/host/runtime-host.test.tsx, src/actions/classroom-actions.test.ts</files>
  <action>新增本地 built-in runtime entry，例如 `src/app/runtime/html-courseware/pilot/page.tsx`，让 HTML runtime pilot 能在 sandboxed iframe 内真实运行。该 runtime entry 必须通过 typed browser bridge 与 host 通信，至少覆盖：runtime ready、一次真实互动输入、可选 save、structured submit、height-change。runtime 内的 submit 不得只做 client-only success text，必须把结构化 payload 交回现有 host bridge，再由现有 runtime submit server boundary 落到 durable truth。补测试锁定：pilot page 确实使用 `postMessage` 或等价 typed bridge；`classroom-actions.test.ts` 或等价测试仍证明 runtime submit 走 trusted action path，而不是 iframe 直写 DAL/DB。</action>
  <acceptance_criteria>
    - `src/app/runtime/html-courseware/pilot/page.tsx` 存在且包含浏览器桥接代码。
    - pilot entry 至少存在一次真实互动输入和一次 structured submit 触发。
    - `classroom-actions.test.ts` 仍锁定 runtime submit 的 trusted boundary。
    - runtime pilot 源码中不出现 `db.`, DAL import, `fetch('/api/...')` 直接绕开宿主写 truth 的路径。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/features/runtime-platform/host/runtime-host.test.tsx src/actions/classroom-actions.test.ts</automated>
  </verify>
  <done>本地 HTML runtime pilot 已能在共享 Runtime Host 内完成真实互动与结构化提交。</done>
</task>

<task type="auto">
  <name>Task 2: 建立 `verify:phase29`，锁住 host pilot、surface wiring 与 trusted submit path</name>
  <read_first>
    - package.json
    - scripts/verify-phase28-runtime-bridge.ts
    - src/components/surfaces/student-player-surfaces.test.ts
    - src/components/surfaces/classroom-console-surface.test.tsx
    - src/features/runtime-platform/host/runtime-host.test.tsx
  </read_first>
  <files>scripts/verify-phase29-runtime-host.ts, package.json, src/components/surfaces/student-player-surfaces.test.ts, src/components/surfaces/classroom-console-surface.test.tsx</files>
  <action>新增 `scripts/verify-phase29-runtime-host.ts` 并在 `package.json` 注册 `verify:phase29`。verifier 必须先做静态 guards：检查 runtime host shared files存在、preview/player/classroom 三处都出现 shared Runtime Host 使用点、built-in HTML runtime entry 存在且是本地 route、runtime submit 仍指向 trusted host action 或 server action、源码中不存在 iframe 直写 DB/DAL shortcut。然后组合运行 focused suites，至少包括 `src/features/runtime-platform/host/runtime-host.test.tsx`、`src/components/surfaces/student-player-surfaces.test.ts`、`src/components/surfaces/classroom-console-surface.test.tsx`、`src/actions/classroom-actions.test.ts`。输出要区分 host drift、surface drift、submit-path drift 三类 failure label。不要把 Phase 32 的最终 milestone proof 条件塞进本 verifier。</action>
  <acceptance_criteria>
    - `package.json` 出现 `verify:phase29`。
    - verifier 脚本包含 shared host、surface wiring、本地 runtime entry、trusted submit path 四类静态 guard。
    - verifier 会执行 host/player/classroom/action 的 focused suites。
    - verifier 输出含 `host drift`、`surface drift`、`submit-path drift` 或等价分类文案。
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase29</automated>
  </verify>
  <done>Phase 29 拥有单一 canonical gate，可持续保护 Runtime Host pilot，不把最终 milestone hardening 提前混入当前 phase。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local runtime pilot -> shared host | pilot 运行时代码仍是不可信浏览器代码，只能通过 host bridge 请求操作 |
| phase verifier -> future runtime changes | 若无专属 verifier，后续 phase 很容易静默破坏 host 或 trusted submit path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-29-04-01 | E | local runtime pilot | mitigate | pilot runtime 只通过 typed browser bridge 和 host action path 工作，不直写 truth |
| T-29-04-02 | R | phase drift | mitigate | `verify:phase29` 用静态 guard + focused suites 锁住 shared host 和 submit path |
| T-29-04-03 | T | scope creep into Phase 32 | mitigate | verifier 只检查 pilot host 成立，不绑定最终 milestone hardening 与 `RHOST-04` close |

</threat_model>

<verification>
- `pnpm test --run src/features/runtime-platform/host/runtime-host.test.tsx src/components/surfaces/student-player-surfaces.test.ts src/components/surfaces/classroom-console-surface.test.tsx src/actions/classroom-actions.test.ts`
- `pnpm verify:phase29`
- 读取 `src/app/runtime/html-courseware/pilot/page.tsx`，确认本地 built-in runtime entry 存在
</verification>

<success_criteria>
- 仓库存在一个可在共享 Runtime Host 内运行的本地 HTML runtime pilot。
- pilot 互动与 submit 继续通过 trusted host boundary。
- Phase 29 拥有专属 canonical verifier。
</success_criteria>

<output>
After completion, create `.planning/phases/29-runtime-host-and-html-courseware-pilot/29-04-SUMMARY.md`
</output>
