---
phase: 60-load-degrade-pilot-rehearsal
plan: "02"
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - scripts/load/phase60-fixtures.ts
  - scripts/load/phase60-capacity.k6.js
  - scripts/proof-phase60-load-smoke.ts
  - ops/releases/evidence/phase60/smoke-result.json
  - ops/releases/evidence/phase60/capacity-result.json
autonomous: true
requirements: [PILOT-03, LOAD-01]
user_setup: []
must_haves:
  truths:
    - "D-60-01 and D-60-04: the real classroom-voting sample chain still passes under a dedicated Phase 60 smoke gate, and browser proof is not reused as capacity proof."
    - "D-60-03 and PILOT-03: capacity proof models 5 concurrent classrooms with 40 student actors each, not one pooled 200-user bucket."
    - "D-60-02: capacity and smoke tracks are separate hard-blocking artifacts, so browser success cannot be mistaken for load proof."
  artifacts:
    - path: scripts/proof-phase60-load-smoke.ts
      provides: "Real sample-chain smoke orchestrator for Phase 60"
    - path: scripts/load/phase60-fixtures.ts
      provides: "Deterministic classroom/session actor seeding for the 40/5 pilot model"
    - path: scripts/load/phase60-capacity.k6.js
      provides: "k6 protocol load gate with 5 classroom-affined scenarios"
    - path: ops/releases/evidence/phase60/smoke-result.json
      provides: "Structured sample-smoke output consumed by the Phase 60 rehearsal summary"
    - path: ops/releases/evidence/phase60/capacity-result.json
      provides: "Structured 40/5 capacity output consumed by the Phase 60 rehearsal summary"
  key_links:
    - from: scripts/proof-phase60-load-smoke.ts
      to: scripts/proof-phase57-classroom-runtime.ts
      via: imported browser proof reuse
      pattern: runPhase57BrowserProof
    - from: scripts/load/phase60-capacity.k6.js
      to: scripts/load/phase60-thresholds.js
      via: shared capacity constants and thresholds
      pattern: phase60-thresholds
    - from: ops/releases/evidence/phase60/smoke-result.json
      to: ops/releases/evidence/phase60/rehearsal-summary.md
      via: structured smoke result contract
      pattern: smoke-result.json
    - from: ops/releases/evidence/phase60/capacity-result.json
      to: ops/releases/evidence/phase60/rehearsal-summary.md
      via: structured capacity result contract
      pattern: capacity-result.json
---

<objective>
Deliver the dual-track happy-path proof required for Phase 60: one real sample-chain smoke path and one classroom-affined 40/5 capacity gate.

Purpose: Implement D-60-01 through D-60-04 without letting browser automation masquerade as capacity evidence.
Output: sample-smoke runner, deterministic load fixtures, and a k6 capacity scenario script.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-load-degrade-pilot-rehearsal/60-CONTEXT.md
@.planning/phases/60-load-degrade-pilot-rehearsal/60-RESEARCH.md
@.planning/phases/60-load-degrade-pilot-rehearsal/60-PATTERNS.md
@.planning/phases/57-classroom-runtime-sample-chain/57-05-SUMMARY.md
@package.json
@scripts/proof-phase57-classroom-runtime.ts
@scripts/verify-phase57-classroom-runtime.ts
@scripts/load/phase60-thresholds.js

<interfaces>
From `scripts/proof-phase57-classroom-runtime.ts`:

```ts
export async function runPhase57BrowserProof();
```

Relevant proof-context helpers already in the file:

```ts
async function ensureProofCourseContext();
async function prepareVotingProofContext();
async function createSessionCookie(...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build deterministic Phase 60 sample-smoke orchestration and classroom-affined fixtures</name>
  <read_first>
    - scripts/proof-phase57-classroom-runtime.ts
    - scripts/verify-phase57-classroom-runtime.ts
    - scripts/load/phase60-thresholds.js
    - .planning/phases/60-load-degrade-pilot-rehearsal/60-RESEARCH.md
    - .planning/phases/60-load-degrade-pilot-rehearsal/60-PATTERNS.md
    - .planning/phases/60-load-degrade-pilot-rehearsal/60-CONTEXT.md
  </read_first>
  <files>scripts/load/phase60-fixtures.ts, scripts/proof-phase60-load-smoke.ts, ops/releases/evidence/phase60/smoke-result.json</files>
  <behavior>
    - Test 1: fixture generation returns 5 classroom-affined rehearsal contexts with 40 student actors each per D-60-03.
    - Test 2: sample smoke runs Phase 56 design/publish gate first, then reuses the real Phase 57 browser proof for launch-to-student-completion instead of inventing a second browser flow.
    - Test 3: smoke output records success/failure per stage so the Phase 60 summary can later consume it without screenshots.
  </behavior>
  <acceptance_criteria>
    - `scripts/load/phase60-fixtures.ts` creates or refreshes a dedicated Phase 60 proof scope with 5 classroom/session contexts, each bound to 40 students, and does not collapse actors into one pooled set.
    - `scripts/proof-phase60-load-smoke.ts` first runs the teacher design/publish prerequisite with `pnpm verify:phase56`, then executes real browser proof by importing `runPhase57BrowserProof()` for one of the seeded classrooms, and writes `ops/releases/evidence/phase60/smoke-result.json`.
    - `ops/releases/evidence/phase60/smoke-result.json` contains exactly these top-level keys: `checkedAt`, `proofScopeId`, `classroomSessionId`, `designPublish`, `browserProof`, `status`, `blockingFailure`, and `nextStep`, so Plan 04 can aggregate it without manual interpretation.
    - The implementation stays on real DB/auth/session seams: no direct fake API bypass, no anonymous load users, and no hardcoded success JSON.
  </acceptance_criteria>
  <action>Create `scripts/load/phase60-fixtures.ts` and `scripts/proof-phase60-load-smoke.ts` per D-60-01, D-60-03, D-60-04, and PILOT-03. Reuse the Phase 57 proof-school and cookie-seeding patterns, but expand the fixture generator to return five named classroom/session contexts with forty student actors each so later capacity runs preserve classroom affinity. In `scripts/proof-phase60-load-smoke.ts`, call `pnpm verify:phase56` to keep teacher design and publish truth in the chain, then import `runPhase57BrowserProof()` to prove the live teacher/student path still works. Write the smoke result to `ops/releases/evidence/phase60/smoke-result.json` with the fixed contract `checkedAt`, `proofScopeId`, `classroomSessionId`, `designPublish`, `browserProof`, `status`, `blockingFailure`, and `nextStep`, and do not create a second browser-only capacity path.</action>
  <verify>
    <automated>node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/proof-phase60-load-smoke.ts</automated>
  </verify>
  <done>Phase 60 has a real sample-smoke runner that proves design/publish plus browser runtime without conflating it with capacity.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the 5-classroom x 40-student k6 capacity gate</name>
  <read_first>
    - scripts/load/phase60-thresholds.js
    - scripts/load/phase60-fixtures.ts
    - scripts/verify-phase60-load-and-rehearsal.ts
    - .planning/phases/60-load-degrade-pilot-rehearsal/60-RESEARCH.md
    - .planning/phases/60-load-degrade-pilot-rehearsal/60-PATTERNS.md
  </read_first>
  <files>scripts/load/phase60-capacity.k6.js, ops/releases/evidence/phase60/capacity-result.json</files>
  <acceptance_criteria>
    - The script defines five named classroom scenarios, each with forty actors, and preserves classroom tags in metrics and summary output.
    - Thresholds import the shared values from `scripts/load/phase60-thresholds.js`; no duplicate numeric literals for capacity or stop rules remain in the k6 script.
    - `ops/releases/evidence/phase60/capacity-result.json` is the fixed output contract for later aggregation and contains `checkedAt`, `scenarioResults`, `thresholds`, `status`, `blockingFailure`, and `nextStep`, where each `scenarioResults` entry includes `classroom`, `actors`, `iterations`, `checksRate`, and `httpReqFailedRate`.
    - The command path works with host `k6` when installed and with `docker run --rm --network host -v "$PWD:/work" -w /work grafana/k6:latest run scripts/load/phase60-capacity.k6.js` when host `k6` is absent.
  </acceptance_criteria>
  <action>Implement `scripts/load/phase60-capacity.k6.js` as the protocol-layer proof required by D-60-01 through D-60-04 and PILOT-03. Model five concurrent classroom scenarios, each bound to one seeded session and forty student actors from `scripts/load/phase60-fixtures.ts`; do not use a loose 200-user pool. Import the shared constants from `scripts/load/phase60-thresholds.js`, emit classroom-tagged metrics, and make any threshold breach abort the run as a close blocker per D-60-02. At the end of the run, write `ops/releases/evidence/phase60/capacity-result.json` with `checkedAt`, `scenarioResults`, `thresholds`, `status`, `blockingFailure`, and `nextStep`, keeping per-classroom fields explicit so Plan 04 can aggregate the result without hand-written notes. Keep the script transport-aware enough to hit the real classroom voting seams, but do not move browser work into k6.</action>
  <verify>
    <automated>docker run --rm --network host -v "$PWD:/work" -w /work grafana/k6:latest run scripts/load/phase60-capacity.k6.js</automated>
  </verify>
  <done>The Phase 60 capacity gate proves 40/5 with classroom affinity and can fail the milestone on protocol-layer overload independently of browser smoke.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| load fixtures -> auth/session seams | seeded teacher/student identities must remain within formal pilot auth boundaries |
| k6 actors -> classroom/runtime write paths | protocol load must exercise real truth seams without corrupting unrelated data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-03 | Elevation of Privilege | `scripts/load/phase60-fixtures.ts` | mitigate | Seed formal teacher/student actors and session cookies using existing Phase 57 patterns; do not introduce anonymous or bypass identities. |
| T-60-04 | Tampering | `scripts/load/phase60-capacity.k6.js` | mitigate | Scope all rehearsal fixtures to dedicated Phase 60 classrooms and tags so load traffic cannot be mistaken for normal pilot truth. |
</threat_model>

<verification>
- `node --require ./scripts/server-only-node-shim.cjs --import tsx scripts/proof-phase60-load-smoke.ts`
- `docker run --rm --network host -v "$PWD:/work" -w /work grafana/k6:latest run scripts/load/phase60-capacity.k6.js`
</verification>

<success_criteria>
- One explicit smoke track proves the real classroom-voting chain still works.
- One explicit k6 track proves 5 classrooms x 40 students with classroom affinity.
- Browser and protocol evidence stay separate but both are hard blockers.
</success_criteria>

<output>
After completion, create `.planning/phases/60-load-degrade-pilot-rehearsal/60-02-SUMMARY.md`.
</output>
