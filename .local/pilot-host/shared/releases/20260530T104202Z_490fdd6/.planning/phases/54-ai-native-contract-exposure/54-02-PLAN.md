---
phase: 54-ai-native-contract-exposure
plan: "02"
type: execute
wave: 1
depends_on:
  - "54-01"
files_modified:
  - src/features/platform-core/ai-contracts/registry.ts
  - src/features/platform-core/ai-contracts/read-model.ts
  - src/features/platform-core/ai-contracts/registry.test.ts
  - src/features/platform-core/ai-contracts/read-model.test.ts
autonomous: true
requirements:
  - AINT-01
  - AINT-02
  - AINT-05
must_haves:
  truths:
    - "command/action/capability discovery 必须来自真实 code-owned source，而不是未来愿景列表。"
    - "read model 只能 server-side 组合 source contracts，UI 不得直连 registry internals。"
---

<objective>
构建 Phase 54 的 command/action/capability discovery registry 与 server read model。
</objective>

<tasks>
  <task type="auto" tdd="true">
    <name>Task 1: Project real command/action/capability descriptors</name>
    <files>src/features/platform-core/ai-contracts/registry.ts, src/features/platform-core/ai-contracts/read-model.ts, src/features/platform-core/ai-contracts/registry.test.ts, src/features/platform-core/ai-contracts/read-model.test.ts</files>
    <acceptance_criteria>
      - command descriptor 列表只包含当前 repo 已存在的真实 command type。
      - action descriptor 列表复用 Phase 52 registry/static catalog truth。
      - capability descriptor 列表复用 runtime/platform permission contract truth。
      - 提供统一 list/read-model API，例如 `listPlatformCommands()` / `listPlatformActions()` / `listPlatformCapabilities()`。
    </acceptance_criteria>
    <verify>
      <automated>pnpm vitest run src/features/platform-core/ai-contracts/registry.test.ts src/features/platform-core/ai-contracts/read-model.test.ts</automated>
    </verify>
  </task>
</tasks>
