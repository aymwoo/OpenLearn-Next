---
phase: 60-load-degrade-pilot-rehearsal
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/verify-phase60-load-and-rehearsal.ts
  - scripts/verify-phase60-load-and-rehearsal.test.ts
  - scripts/load/phase60-thresholds.js
  - scripts/load/phase60-thresholds.test.ts
autonomous: true
requirements: [PILOT-03, LOAD-01, LOAD-02, OPS-02]
user_setup: []
must_haves:
  truths:
    - "D-60-01 and D-60-02: Phase 60 has one repo-local hard gate that fails on any blocked smoke, capacity, drill, or rollback stage."
    - "D-60-08, D-60-09, D-60-10, and D-60-11: reconnect recovery, worker backlog window, partial failure ratio, and degraded duration use one shared numeric source, and any breach is treated with blocker-grade one-vote veto semantics rather than advisory wording."
    - "D-60-03 and D-60-04: the gate preserves the dual-track split, where browser smoke proves the real sample chain and k6 proves 5 classrooms x 40 students capacity."
  artifacts:
    - path: package.json
      provides: "Exact verify:phase60 script entry"
    - path: scripts/verify-phase60-load-and-rehearsal.ts
      provides: "Phase 60 verifier orchestration and static contract checks"
    - path: scripts/load/phase60-thresholds.js
      provides: "Shared stop-rule numbers and 40/5 capacity constants"
  key_links:
    - from: package.json
      to: scripts/verify-phase60-load-and-rehearsal.ts
      via: verify:phase60 script entry
      pattern: "verify:phase60"
    - from: scripts/verify-phase60-load-and-rehearsal.ts
      to: scripts/load/phase60-thresholds.js
      via: shared stop-rule imports
      pattern: "phase60-thresholds"
---

<objective>
Lock the Phase 60 close-gate contract before any load or rehearsal implementation lands.

Purpose: Make D-60-01, D-60-02, D-60-08, D-60-10, and D-60-11 executable instead of advisory by freezing one verifier entrypoint and one numeric stop-rule source.
Output: `verify:phase60` wiring, verifier self-tests, and a shared stop-rule module consumed by later plans.
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-load-degrade-pilot-rehearsal/60-CONTEXT.md
@.planning/phases/60-load-degrade-pilot-rehearsal/60-RESEARCH.md
@.planning/phases/60-load-degrade-pilot-rehearsal/60-PATTERNS.md
@.planning/phases/60-load-degrade-pilot-rehearsal/60-VALIDATION.md
@.planning/phases/55-pilot-scope-and-acceptance-gate/55-PROOF-INVENTORY.md
@.planning/phases/55-pilot-scope-and-acceptance-gate/55-FAILURE-RECOVERY-MATRIX.md
@package.json
@scripts/verify-phase57-classroom-runtime.ts
@scripts/verify-phase58-operator-recovery-and-surfaces.ts
@scripts/verify-phase59-deploy-release.ts

<interfaces>
From `scripts/verify-phase57-classroom-runtime.ts`:

```ts
export function getPhase57VerificationSuitePaths();
export async function runPhase57Verification();
```

From `scripts/verify-phase59-deploy-release.ts`:

```ts
export function getPhase59RequiredArtifacts();
export function getPhase59FocusedSuitePaths();
export async function runPhase59Verification();
```

Existing ready truth reused later:

```ts
export async function getReadyPayload();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze Phase 60 stop-rule constants and shared capacity contract</name>
  <read_first>
    - .planning/phases/60-load-degrade-pilot-rehearsal/60-CONTEXT.md
    - .planning/phases/60-load-degrade-pilot-rehearsal/60-RESEARCH.md
    - .planning/phases/60-load-degrade-pilot-rehearsal/60-VALIDATION.md
    - .planning/phases/55-pilot-scope-and-acceptance-gate/55-PILOT-CONTRACT.md
  </read_first>
  <files>scripts/load/phase60-thresholds.js, scripts/load/phase60-thresholds.test.ts</files>
  <behavior>
    - Test 1: exported classroom capacity constants equal 5 concurrent classrooms and 40 student actors per classroom per D-60-03.
    - Test 2: exported stop rules use one shared object with concrete values for reconnect recovery, worker backlog window, partial failure ratio, and degraded duration per D-60-10.
    - Test 3: helper output marks threshold breaches as blocker or escalation inputs rather than advisory-only text per D-60-08 and D-60-11.
  </behavior>
  <acceptance_criteria>
    - `scripts/load/phase60-thresholds.js` exports exactly one shared numeric source for `classrooms`, `studentsPerClassroom`, `reconnectRecoveryMs`, `workerBacklogWindowMs`, `partialFailureRatioMax`, and `degradedDurationMs`.
    - Concrete values are locked to `5`, `40`, `15000`, `120000`, `0.02`, and `180000`; later plans must import these values instead of duplicating literals.
    - `scripts/load/phase60-thresholds.test.ts` proves those values and the blocker classification contract with `pnpm exec vitest --run scripts/load/phase60-thresholds.test.ts`.
  </acceptance_criteria>
  <action>Create `scripts/load/phase60-thresholds.js` as the single stop-rule source per D-60-03, D-60-08, D-60-10, and D-60-11. Export the fixed 40/5 capacity constants plus concrete numeric thresholds: reconnect recovery must complete within 15000 ms, worker backlog blocking window must stay at or below 120000 ms, partial failure ratio must stay below 0.02, and degraded posture may not remain unresolved beyond 180000 ms. Also export a small pure helper that later plans can reuse to classify a measured threshold as `pass`, `close-blocker`, or `escalate`. Add `scripts/load/phase60-thresholds.test.ts` first, then implement the module so later k6 and rehearsal code import this file instead of copying numbers.</action>
  <verify>
    <automated>pnpm exec vitest --run scripts/load/phase60-thresholds.test.ts</automated>
  </verify>
  <done>All four D-60-10 dimensions have concrete numbers in one shared file, and the classification helper is locked by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold the exact `verify:phase60` hard gate and self-test contract</name>
  <read_first>
    - package.json
    - scripts/verify-phase57-classroom-runtime.ts
    - scripts/verify-phase58-operator-recovery-and-surfaces.ts
    - scripts/verify-phase59-deploy-release.ts
    - scripts/load/phase60-thresholds.js
    - .planning/phases/60-load-degrade-pilot-rehearsal/60-CONTEXT.md
    - .planning/phases/60-load-degrade-pilot-rehearsal/60-PATTERNS.md
  </read_first>
  <files>package.json, scripts/verify-phase60-load-and-rehearsal.ts, scripts/verify-phase60-load-and-rehearsal.test.ts</files>
  <behavior>
    - Test 1: package.json exposes the exact `verify:phase60` command string.
    - Test 2: the verifier locks the stage order `static -> sample smoke -> capacity -> drills -> rollout/rollback rehearsal -> summary` per D-60-01, D-60-02, and D-60-12.
    - Test 3: the verifier static contract requires downstream artifacts and forbids shell heredoc shortcuts, while allowing an explicit `PHASE60_REHEARSAL_MODE=dry-run` and `PHASE60_K6_MODE=dry-run` authoring mode without weakening default close semantics.
  </behavior>
  <acceptance_criteria>
    - `package.json` contains `verify:phase60` with the same `node --require ./scripts/server-only-node-shim.cjs --import tsx ...` style used by Phase 57-59 verifiers.
    - `scripts/verify-phase60-load-and-rehearsal.ts` exports required-artifact and focused-suite helpers, imports `scripts/load/phase60-thresholds.js`, and reserves hard-fail stages for sample smoke, capacity gate, drills, rehearsal, and summary artifact validation.
    - `scripts/verify-phase60-load-and-rehearsal.test.ts` locks the exact script entry, required artifact list, stage order, k6 Docker fallback contract, and dry-run authoring flags.
  </acceptance_criteria>
  <action>Using the Phase 57-59 verifier pattern, create `scripts/verify-phase60-load-and-rehearsal.ts` and its self-test. Reference D-60-01 and D-60-04 by making the browser track a dedicated sample-smoke stage only, and D-60-02 by making every major stage process-exit on failure. Reference D-60-08 through D-60-11 by importing the shared stop rules and treating threshold breaches as blockers or escalation outcomes, not warnings. The verifier must statically require these future artifacts: `scripts/proof-phase60-load-smoke.ts`, `scripts/load/phase60-fixtures.ts`, `scripts/load/phase60-capacity.k6.js`, `scripts/load/phase60-drill-classifier.js`, `scripts/load/phase60-drills.k6.js`, `scripts/rehearse-phase60-rollout-rollback.ts`, `ops/releases/evidence/phase60/rehearsal-summary.md`, `ops/releases/evidence/phase60/rollout-notes.md`, `ops/releases/evidence/phase60/rollback-notes.md`, and `ops/releases/evidence/phase60/transport-fallback-notes.md`. Add explicit local-vs-Docker k6 resolution so later plans can run with host `k6` or `grafana/k6:latest` without changing command semantics.</action>
  <verify>
    <automated>pnpm exec vitest --run scripts/verify-phase60-load-and-rehearsal.test.ts scripts/load/phase60-thresholds.test.ts</automated>
  </verify>
  <done>`verify:phase60` exists as a hard-gate contract, its script entry is exact, and later plans can fill the locked artifact list without renegotiating stage order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier -> local shell / Docker k6 | phase automation executes external commands and must not silently skip blocked stages |
| shared thresholds -> later smoke/drill/rehearsal scripts | one numeric source controls close-vs-escalate decisions across multiple artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Tampering | `scripts/load/phase60-thresholds.js` | mitigate | Lock all D-60-10 numbers and classifier outputs in `scripts/load/phase60-thresholds.test.ts`; later plans must import instead of duplicating literals. |
| T-60-02 | Repudiation | `scripts/verify-phase60-load-and-rehearsal.ts` | mitigate | Encode a fixed stage order and required artifact list in self-tests so a missing smoke, k6, or rehearsal stage cannot be hand-waved at closeout. |
</threat_model>

<verification>
- `pnpm exec vitest --run scripts/load/phase60-thresholds.test.ts scripts/verify-phase60-load-and-rehearsal.test.ts`
</verification>

<success_criteria>
- Phase 60 has a single hard-gate entrypoint in `package.json`.
- The four numeric stop-rule dimensions are concretely frozen and reusable.
- The verifier contract locks the dual-track structure and future artifact list before implementation begins.
</success_criteria>

<output>
After completion, create `.planning/phases/60-load-degrade-pilot-rehearsal/60-01-SUMMARY.md`.
</output>
