---
phase: 56-voting-plugin-contract-and-authoring-integration
plan: "02"
type: execute
wave: 2
depends_on:
  - "56-01"
files_modified:
  - src/lib/dal/lesson-authoring.ts
  - src/actions/lesson-authoring-actions.ts
  - src/lib/dal/plugin-data.ts
  - src/lib/dal/lesson-authoring.test.ts
autonomous: true
requirements:
  - CHAIN-02
  - PLUG-02
  - SAFE-01
  - SAFE-02
must_haves:
  truths:
    - "publish / republish 必须冻结课堂投票插件可执行配置，而不是在课堂运行期回读 draft extension。"
    - "publish preflight 必须覆盖 voting plugin config validity、enablement 与 compatibility。"
    - "关键写路径仍然走 SQLite + DAL，不允许 runtime-only config。"
---

<objective>
把课堂投票插件接入 publish readiness 与 snapshot freeze 流程，确保发布前能阻断非法配置，发布后课堂读取的是冻结的可执行 contract。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-pilot-scope-and-acceptance-gate/55-FAILURE-RECOVERY-MATRIX.md
@.planning/phases/56-voting-plugin-contract-and-authoring-integration/56-CONTEXT.md
@.planning/phases/56-voting-plugin-contract-and-authoring-integration/56-RESEARCH.md
@src/lib/dal/lesson-authoring.ts
@src/actions/lesson-authoring-actions.ts
@src/lib/dal/plugin-data.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend publish readiness and snapshot freeze for the voting plugin sample</name>
  <files>src/lib/dal/lesson-authoring.ts, src/actions/lesson-authoring-actions.ts, src/lib/dal/plugin-data.ts, src/lib/dal/lesson-authoring.test.ts</files>
  <read_first>
    - src/lib/dal/lesson-authoring.ts
    - src/lib/dal/plugin-data.ts
    - src/actions/lesson-authoring-actions.ts
    - .planning/phases/56-voting-plugin-contract-and-authoring-integration/56-CONTEXT.md
  </read_first>
  <action>扩展 `getLessonPublishReadinessDTO()` 与 `publishLesson()`，让课堂投票插件样板进入正式 publish gate。至少补齐三类检查：plugin config schema invalid、plugin disabled/kill-switched/lifecycle incompatible、missing executable voting config。publish blocked 返回 teacher/operator 可解释的问题，而不是 generic `PUBLISH_BLOCKED`。发布成功时，课堂投票插件的可执行配置必须被冻结进 published snapshot 或等价 publish-bound contract，确保课堂运行不会回读草稿态 extension。若需要访问 plugin extension 数据，只允许通过 DAL，并保持 SQLite + DAL authoritative write path。补 focused tests，证明 publish gate 能阻断非法 voting config，且 publish snapshot 确实冻结了样板所需的 contract。</action>
  <acceptance_criteria>
    - publish readiness 包含 voting plugin-specific blockers。
    - publish snapshot 冻结 voting plugin executable config。
    - classroom runtime 不需要回读草稿态 plugin extension 才能获得发布版本配置。
    - focused tests 覆盖 blocked publish 和 snapshot freeze。
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/lib/dal/lesson-authoring.test.ts</automated>
  </verify>
</task>

</tasks>

<output>
After completion, create `.planning/phases/56-voting-plugin-contract-and-authoring-integration/56-02-SUMMARY.md`
</output>
