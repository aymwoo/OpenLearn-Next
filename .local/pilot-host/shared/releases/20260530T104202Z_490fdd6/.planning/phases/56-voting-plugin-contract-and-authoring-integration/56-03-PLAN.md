---
phase: 56-voting-plugin-contract-and-authoring-integration
plan: "03"
type: execute
wave: 3
depends_on:
  - "56-01"
  - "56-02"
files_modified:
  - scripts/verify-phase56-voting-authoring.ts
  - package.json
autonomous: true
requirements:
  - PLUG-01
  - CHAIN-01
  - CHAIN-02
  - SAFE-01
  - SAFE-02
must_haves:
  truths:
    - "Phase 56 需要 focused regression gate，阻止 core step type 漂移、draft-state leakage 与 plugin config truth drift。"
    - "verifier 必须是 repo-local，可在无外部 infra 下执行。"
---

<objective>
为 Phase 56 增加 focused proof 与 regression gate，锁死课堂投票 authoring contract、publish freeze 与 truth boundary，不让后续实现回退。
</objective>

<context>
@.planning/phases/55-pilot-scope-and-acceptance-gate/55-PROOF-INVENTORY.md
@.planning/phases/56-voting-plugin-contract-and-authoring-integration/56-CONTEXT.md
@.planning/phases/56-voting-plugin-contract-and-authoring-integration/56-RESEARCH.md
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add verify:phase56 regression gate for voting authoring and publish freeze boundaries</name>
  <files>scripts/verify-phase56-voting-authoring.ts, package.json</files>
  <read_first>
    - .planning/phases/56-voting-plugin-contract-and-authoring-integration/56-RESEARCH.md
    - .planning/phases/55-pilot-scope-and-acceptance-gate/55-PROOF-INVENTORY.md
    - package.json
  </read_first>
  <action>创建 `scripts/verify-phase56-voting-authoring.ts` 与 `package.json#verify:phase56`。脚本至少静态守住四个边界：不得新增第四种核心 step type；不得把课堂投票 plugin config truth 退化为 runtime-only/draft-only state；不得让 publish 重新只返回 generic blocker 而缺少 voting plugin reason；不得绕过 DAL 直接读取或写入 plugin extension truth。随后串起 Phase 56 focused suites，覆盖 authoring workspace、built-in/plugin template resolution、lesson authoring publish readiness/snapshot freeze 相关测试。保持 gate repo-local，不依赖 Redis/WebSocket/worker 外部环境。</action>
  <acceptance_criteria>
    - `verify:phase56` 已注册。
    - verifier 能静态阻止 Phase 56 关键边界回退。
    - verifier 可在无外部 infra 环境下运行 focused suites。
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase56</automated>
  </verify>
</task>

</tasks>

<output>
After completion, create `.planning/phases/56-voting-plugin-contract-and-authoring-integration/56-03-SUMMARY.md`
</output>
