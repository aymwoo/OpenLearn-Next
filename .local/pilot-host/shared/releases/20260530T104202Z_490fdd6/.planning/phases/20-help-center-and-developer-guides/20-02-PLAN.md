---
phase: 20-help-center-and-developer-guides
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/lib/help/help-center-content.ts
  - src/app/help/plugins/page.tsx
  - src/app/help/themes/page.tsx
  - src/components/surfaces/help-guide-detail-surface.tsx
  - src/components/surfaces/help-guide-detail-surface.test.tsx
autonomous: true
requirements:
  - Extension phase — product help center and developer-facing implementation guidance.
must_haves:
  truths:
    - D-07/D-08/D-09: 插件与主题指南必须以当前代码实现为事实来源，并明确区分当前可用、使用边界、后续扩展
    - D-10/D-11/D-12: 开发者页可以有最小代码示例，但示例必须与当前 manifest/theme/action/runtime 合同一致
    - D-16/D-17: 插件页覆盖 hook anchors、allowlisted actions、permission requirements、school-scoped activation；主题页覆盖 `manifest.theme -> registerThemeTokens / setActiveThemeAction -> ThemeInjector -> teacher shell`
  artifacts:
    - path: src/app/help/plugins/page.tsx
      provides: codebase-accurate plugin development guide
      contains: schedule.assistant
    - path: src/app/help/themes/page.tsx
      provides: codebase-accurate theme development guide
      contains: setActiveThemeAction
    - path: src/lib/help/help-center-content.ts
      provides: centralized plugin/theme guide sections, code samples, and state-labeled notes
      contains: registerThemeTokensAction
  key_links:
    - from: src/app/help/plugins/page.tsx
      to: src/server/plugins/registry.ts
      via: plugin guide documents the current hook/action allowlist and permission map
      pattern: PLUGIN_ACTION_PERMISSION_REQUIREMENTS
    - from: src/app/help/themes/page.tsx
      to: src/actions/theme-actions.ts
      via: theme guide documents the current registration and activation flow
      pattern: setActiveThemeAction|registerThemeTokensAction
---

<objective>
把 `/help/plugins` 和 `/help/themes` 两个开发者指南写成与当前代码库一致、可执行理解、不过度承诺的正式产品内容页。

Purpose: 让开发者不需要反向读源码，也不会被理想化文档误导；同时把插件和主题的当前边界说清楚。
Output: 两个 detail page 的完整正文、最小代码示例、边界说明和可扩展提示，并带上必要的页面级测试/回归检查。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-help-center-and-developer-guides/20-CONTEXT.md
@.planning/phases/20-help-center-and-developer-guides/20-RESEARCH.md
@.planning/phases/20-help-center-and-developer-guides/20-UI-SPEC.md
@.planning/phases/20-help-center-and-developer-guides/20-01-PLAN.md
@docs/plugin-system-review.md
@docs/theme-system-design.md
@src/lib/dto/resource-ai.ts
@src/server/plugins/registry.ts
@src/lib/dal/plugins.ts
@src/actions/plugin-actions.ts
@src/lib/dal/themes.ts
@src/actions/theme-actions.ts
@src/components/theme/theme-injector.tsx
@src/components/shell/teacher-sidebar-shell.tsx

<interfaces>
Current plugin contract to document:
```ts
PluginManifestSchema = z.object({
  id: z.string(),
  version: z.string(),
  permissions: z.array(z.string()).default([]),
  anchors: z.array(PluginHookAnchorSchema),
  actions: z.array(PluginActionSchema),
  builtIn: z.boolean().default(false),
  defaultEnabled: z.boolean().default(false),
  nonDeletable: z.boolean().default(false),
  theme: ThemeTokenRegistrySchema.optional(),
});
```

Current theme runtime path to document:
```ts
registerThemeTokensAction()
setActiveThemeAction()
getCurrentActorThemeRuntimeState()
compileThemeLayoutRuntime()
TeacherSidebarShell / ThemeInjector
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the plugin development guide against the live manifest and allowlist contracts</name>
  <files>src/lib/help/help-center-content.ts, src/app/help/plugins/page.tsx, src/components/surfaces/help-guide-detail-surface.tsx</files>
  <read_first>src/lib/help/help-center-content.ts, src/app/help/plugins/page.tsx, src/lib/dto/resource-ai.ts, src/server/plugins/registry.ts, src/lib/dal/plugins.ts, src/actions/plugin-actions.ts, docs/plugin-system-review.md, .planning/phases/20-help-center-and-developer-guides/20-CONTEXT.md</read_first>
  <action>Populate the plugin guide content per D-07, D-08, D-09, D-10, D-11, D-12, and D-16. In `src/lib/help/help-center-content.ts`, add the `/help/plugins` section sequence exactly in this order: (1) page intro and audience, (2) `当前可用` manifest contract, hook anchors, allowlisted actions, and permission requirements, (3) school-scoped activation path covering `registerPluginManifestAction`, `setPluginEnabledAction`, `runPluginHookAction`, DAL scope checks, and audit behavior, (4) a minimum manifest/action example using the current `PluginManifestSchema` fields and current action names, (5) `使用边界` notes that explicitly state no arbitrary JS, no direct DB access, no plugin-to-plugin channel, and no external marketplace, and (6) a separate `后续扩展` block for anything not implemented today. Render that content through `/help/plugins` using the shared detail-page template. Do not claim unsupported anchors or actions, and do not write a generic ecosystem guide detached from the current repo contracts.</action>
  <acceptance_criteria>
    - `src/lib/help/help-center-content.ts` contains `schedule.assistant`, `createScheduleOverrideProposal`, `setPluginEnabledAction`, and `runPluginHookAction` in the plugin guide section.
    - `src/app/help/plugins/page.tsx` renders the plugin guide through the shared detail template.
    - The plugin guide contains `当前可用`, `使用边界`, and `后续扩展` as separate labeled sections.
    - The plugin guide includes at least one code example containing `id`, `version`, `anchors`, and `actions`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const content=fs.readFileSync('src/lib/help/help-center-content.ts','utf8');if(!content.includes('schedule.assistant')||!content.includes('createScheduleOverrideProposal')||!content.includes('setPluginEnabledAction')||!content.includes('runPluginHookAction')||!content.includes('当前可用')||!content.includes('使用边界')||!content.includes('后续扩展'))process.exit(1)"</automated>
  </verify>
  <done>`/help/plugins` 已经把 manifest、allowlist、权限和 school-scoped 激活路径讲清楚，且内容忠实对应当前代码实现。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the theme development guide and lock the plugin/theme guide rendering contract</name>
  <files>src/lib/help/help-center-content.ts, src/app/help/themes/page.tsx, src/components/surfaces/help-guide-detail-surface.tsx, src/components/surfaces/help-guide-detail-surface.test.tsx</files>
  <read_first>src/lib/help/help-center-content.ts, src/app/help/themes/page.tsx, src/lib/dto/resource-ai.ts, src/lib/dal/themes.ts, src/actions/theme-actions.ts, src/server/themes/tokens.ts, src/components/theme/theme-injector.tsx, src/components/shell/teacher-sidebar-shell.tsx, docs/theme-system-design.md</read_first>
  <behavior>
    - Test 1: theme guide renders a separate `当前可用`, `使用边界`, and `后续扩展` structure.
    - Test 2: theme guide references the real runtime chain from `manifest.theme` to `TeacherSidebarShell`.
    - Test 3: developer detail pages can render code examples, while teacher help content stays out of this template scope.
  </behavior>
  <action>Populate the `/help/themes` guide per D-07, D-08, D-09, D-10, D-11, D-12, and D-17. In `src/lib/help/help-center-content.ts`, add theme-guide sections in this order: (1) page intro and audience, (2) `当前可用` token and layout contract fields grounded in `ThemeTokenRegistrySchema`, (3) runtime chain covering `manifest.theme -> registerThemeTokensAction -> setActiveThemeAction -> getCurrentActorThemeRuntimeState -> compileThemeLayoutRuntime -> ThemeInjector / TeacherSidebarShell`, (4) layout contract and fallback notes for shell/pages/school scope, (5) a minimum theme token and page override example, (6) `使用边界` notes that explicitly state Lexend-only, no docs-only layout, and no alternate theme runtime path, and (7) a separate `后续扩展` block for dark/light variants or marketplace-style expansion that is not current behavior. Add `src/components/surfaces/help-guide-detail-surface.test.tsx` to assert that the detail template can render labeled sections, code blocks, return link, and aside coverage summary for developer pages. Keep the tests semantic and text-driven, not screenshot/snapshot-heavy.</action>
  <acceptance_criteria>
    - `src/lib/help/help-center-content.ts` contains `registerThemeTokensAction`, `setActiveThemeAction`, `ThemeInjector`, and `TeacherSidebarShell` in the theme guide section.
    - `src/app/help/themes/page.tsx` renders the theme guide through the shared detail template.
    - `src/components/surfaces/help-guide-detail-surface.test.tsx` contains assertions for `返回帮助中心`, `本页覆盖`, and a code-example rendering path.
    - The theme guide contains separate `当前可用`, `使用边界`, and `后续扩展` sections.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/surfaces/help-guide-detail-surface.test.tsx</automated>
  </verify>
  <done>`/help/themes` 已经把主题 token、layout runtime 和生效链路讲清楚，并且 detail page 模板有回归保护。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| codebase contract -> developer guide content | internal plugin/theme APIs are exposed as user-facing guidance |
| structured guide content -> detail surface rendering | developer-facing content and code examples become visible UI blocks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | I | `src/lib/help/help-center-content.ts` | mitigate | Source plugin and theme guide content from the live DTO/DAL/action contracts so the docs do not overstate current capabilities. |
| T-20-06 | T | `src/app/help/plugins/page.tsx` | mitigate | Keep plugin examples aligned to the current allowlist and school-scoped activation path only. |
| T-20-07 | T | `src/app/help/themes/page.tsx` | mitigate | Keep theme documentation on the single runtime path through actions, DAL, `ThemeInjector`, and `TeacherSidebarShell`; forbid alternate runtime narratives. |
| T-20-08 | R | `src/components/surfaces/help-guide-detail-surface.test.tsx` | accept | Tests are repository-local safety checks and do not cross a sensitive boundary. |
</threat_model>

<verification>
- `pnpm test --run src/components/surfaces/help-guide-detail-surface.test.tsx`
- `pnpm typecheck`
</verification>

<success_criteria>
- 插件与主题指南都对齐当前代码合同，而不是理想化 API。
- 两个页面都能清晰区分当前可用、使用边界、后续扩展。
- 共享 detail template 的关键开发者页表现有测试保护。
</success_criteria>

<output>
After completion, create `.planning/phases/20-help-center-and-developer-guides/20-02-SUMMARY.md`
</output>
