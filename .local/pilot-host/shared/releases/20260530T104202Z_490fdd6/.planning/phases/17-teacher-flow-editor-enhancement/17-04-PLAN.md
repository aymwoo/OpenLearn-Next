---
phase: 17-teacher-flow-editor-enhancement
plan: 04
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
  - 17-03
files_modified:
  - src/components/authoring/authoring-status-panel.tsx
  - src/components/authoring/authoring-status-panel.test.tsx
  - src/components/surfaces/lesson-editor-surface.tsx
  - package.json
  - scripts/verify-phase17-editor.ts
autonomous: true
requirements:
  - LESSON-08
  - PLUGIN-01
  - PLUGIN-02
  - PLUGIN-05
must_haves:
  truths:
    - D-08/D-09: editor 显示结构化 publish-readiness 阻断项，且 publish CTA 与服务端阻断使用同一 readiness contract
    - D-07/D-08: preview、保存与发布反馈都出现在当前 editor shell 中，不再只是静态占位文案
    - D-10/D-11/D-12: Phase 17 有专门验证脚本检查 DAL + Server Actions、cache invalidation、preview route 和 plugin safety 约束
  artifacts:
    - path: src/components/authoring/authoring-status-panel.tsx
      provides: structured publish-readiness panel
      contains: blockingIssues
    - path: scripts/verify-phase17-editor.ts
      provides: phase-specific verification guard
      contains: verify:phase17
    - path: package.json
      provides: executable phase verification command
      contains: verify:phase17
  key_links:
    - from: src/components/authoring/authoring-status-panel.tsx
      to: src/actions/lesson-authoring-actions.ts
      via: readiness panel and publish action share the same blocked/ready semantics
      pattern: PUBLISH_BLOCKED|publishLessonAction
    - from: package.json
      to: scripts/verify-phase17-editor.ts
      via: dedicated editor-enhancement verification command
      pattern: verify:phase17
---

<objective>
把发布准备面板升级为真正的结构化守门员，并给 Phase 17 增加专门的验证入口，确保预览、
readiness、plugin safety 与缓存边界长期可回归。

Purpose: 让本阶段以“可解释的发布阻断 + 可执行验证”结束，而不是只留下更复杂的 UI。
Output: structured readiness panel、editor shell wiring、phase-specific verify script。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-teacher-flow-editor-enhancement/17-CONTEXT.md
@.planning/phases/17-teacher-flow-editor-enhancement/17-RESEARCH.md
@.planning/phases/17-teacher-flow-editor-enhancement/17-UI-SPEC.md
@.planning/phases/17-teacher-flow-editor-enhancement/17-01-PLAN.md
@src/components/authoring/authoring-status-panel.tsx
@src/components/surfaces/lesson-editor-surface.tsx
@package.json

<interfaces>
From Plan 17-01 expected output:
```ts
type LessonPublishReadinessDTO = {
  canPublish: boolean;
  blockingIssues: Array<{ code: string; title: string; detail: string }>;
  warnings: Array<{ code: string; title: string; detail: string }>;
}
```

From src/actions/lesson-authoring-actions.ts:
```ts
export async function publishLessonAction(...): Promise<{ ok: false; error: "PUBLISH_BLOCKED"; issues: ... } | { ok: true; data: ... }>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the status panel with a structured publish-readiness gate</name>
  <files>src/components/authoring/authoring-status-panel.tsx, src/components/authoring/authoring-status-panel.test.tsx, src/components/surfaces/lesson-editor-surface.tsx</files>
  <read_first>src/components/authoring/authoring-status-panel.tsx, src/components/surfaces/lesson-editor-surface.tsx, src/actions/lesson-authoring-actions.ts, src/lib/dto/lesson-authoring.ts, .planning/phases/17-teacher-flow-editor-enhancement/17-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: readiness panel renders blocking issues and warnings as structured lists instead of a single sentence per D-08.
    - Test 2: publish CTA is disabled when blocking issues exist and surfaces preview/save/publish state in the current shell.
    - Test 3: panel copy includes unavailable built-in plugin and invalid payload issue paths when those issue codes are present.
  </behavior>
  <action>Refactor `src/components/authoring/authoring-status-panel.tsx` to accept structured readiness data from Plan 17-01 rather than deriving `canPublish` from a three-field boolean. Render blocking issues and warnings as separate Chinese lists with stable labels, and disable the publish form submit when `blockingIssues.length > 0` (per D-08). Update `src/components/surfaces/lesson-editor-surface.tsx` to pass the readiness DTO, preserve the existing save/publish area, and keep preview/save/publish feedback inside the current editor shell. The panel must explicitly handle issue codes such as `STEP_PAYLOAD_INVALID` and `BUILT_IN_PLUGIN_UNAVAILABLE`; do not fall back to a generic one-line message.</action>
  <acceptance_criteria>
    - `src/components/authoring/authoring-status-panel.tsx` contains `blockingIssues` and `warnings`.
    - `src/components/authoring/authoring-status-panel.tsx` contains `BUILT_IN_PLUGIN_UNAVAILABLE` or matching issue-code handling.
    - `src/components/authoring/authoring-status-panel.test.tsx` asserts disabled publish state and structured issue rendering.
    - `src/components/surfaces/lesson-editor-surface.tsx` passes readiness data into `AuthoringStatusPanel`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/authoring/authoring-status-panel.test.tsx</automated>
  </verify>
  <done>The status panel becomes a real publish gate: teachers can see exactly what blocks release, and the disabled publish CTA matches server-side readiness rules.</done>
</task>

<task type="auto">
  <name>Task 2: Add a dedicated verification script for the Phase 17 editor contract</name>
  <files>package.json, scripts/verify-phase17-editor.ts</files>
  <read_first>package.json, scripts/verify-phase12-launch-and-builtins.ts, scripts/verify-phase16-theme-layout.ts, src/actions/lesson-authoring-actions.ts, src/app/(teacher)/teacher/editor/preview/page.tsx</read_first>
  <action>Create `scripts/verify-phase17-editor.ts` and register `verify:phase17` in `package.json`. The script must verify the specific invariants of this phase: (1) `/teacher/editor` still depends on explicit `courseId` + `lessonId`; (2) `/teacher/editor/preview` exists and uses `getTeacherLessonPreviewDTO`; (3) lesson authoring actions include `PUBLISH_BLOCKED` and readiness enforcement; (4) the workspace still uses Server Actions for add/reorder/archive/duplicate rather than direct DB access; (5) the status panel renders structured readiness lists; and (6) no unsafe plugin execution shortcuts such as `eval(`, `dangerouslySetInnerHTML`, or direct DB access were introduced into the editor/plugin path. Reuse the existing phase verify-script style so the result is executable in CI or before shipping.</action>
  <acceptance_criteria>
    - `package.json` contains `verify:phase17`.
    - `scripts/verify-phase17-editor.ts` contains checks for `/teacher/editor/preview`, `getTeacherLessonPreviewDTO`, and `PUBLISH_BLOCKED`.
    - `scripts/verify-phase17-editor.ts` contains guards against `eval(` and `dangerouslySetInnerHTML` in the editor/plugin path.
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase17</automated>
  </verify>
  <done>Phase 17 ends with an automated verification command that checks the core editor, preview, readiness, and plugin-safety guarantees instead of relying on manual inspection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| readiness DTO → publish/status UI | Server-derived blocking issues drive teacher-visible publish controls |
| repository source → verify script | Verification determines whether editor safety and routing invariants still hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-13 | T | `src/components/authoring/authoring-status-panel.tsx` | mitigate | Render stable issue-code-driven blocking lists so the UI cannot silently ignore server readiness problems. |
| T-17-14 | R | `scripts/verify-phase17-editor.ts` | mitigate | Add deterministic source checks for preview route, readiness gating, and unsafe editor/plugin patterns. |
| T-17-15 | E | `src/components/surfaces/lesson-editor-surface.tsx` | mitigate | Keep publish and preview controls wired to the existing teacher-owned server path rather than client-only shortcuts. |
| T-17-16 | I | `scripts/verify-phase17-editor.ts` | accept | Verification checks only source invariants and does not expose new runtime data. |
</threat_model>

<verification>
- `pnpm test --run src/components/authoring/authoring-status-panel.test.tsx`
- `pnpm verify:phase17`
</verification>

<success_criteria>
- Teachers see structured publish-readiness blockers and warnings in the editor.
- The publish button state matches the server-enforced readiness contract.
- Phase 17 ships with an executable verification command for preview, readiness, and plugin-safety invariants.
</success_criteria>

<output>
After completion, create `.planning/phases/17-teacher-flow-editor-enhancement/17-04-SUMMARY.md`
</output>
