---
phase: 19-teacher-shell-route-metadata-system
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/components/shell/teacher-sidebar-shell.tsx
  - src/app/(teacher)/teacher/layout.tsx
  - src/app/settings/layout.tsx
  - src/app/(library)/resources/layout.tsx
autonomous: true
requirements:
  - Extension phase — teacher shell architecture hardening and future layout-variant expansion.
must_haves:
  truths:
    - D-03/D-07/D-09: `/teacher` keeps current square/full-width/immersive visuals after the route-string checks are removed
    - D-04/D-05/D-06: `teacher-sidebar-shell.tsx` renders from centralized resolver output instead of local business route logic
    - D-08/D-10: teacher, settings, and resources stay on one shared shell path during the migration
  artifacts:
    - path: src/components/shell/teacher-sidebar-shell.tsx
      provides: shell renderer that consumes resolver output only
      contains: shellConfig
    - path: src/app/(teacher)/teacher/layout.tsx
      provides: teacher layout entry that still resolves allowlisted route keys
      contains: resolveTeacherThemeRouteSurface
    - path: src/app/settings/layout.tsx
      provides: shared settings shell consumer on the same path
      contains: TeacherSidebarShell
  key_links:
    - from: src/components/shell/teacher-sidebar-shell.tsx
      to: src/lib/theme-layout/shell-surface-resolver.ts
      via: render path consumes centralized shellVariant/shellConfig/surfaceMetadata
      pattern: getShellSurfaceConfig
    - from: src/app/(teacher)/teacher/layout.tsx
      to: src/components/shell/teacher-sidebar-shell.tsx
      via: route layout passes allowlisted route surface identity into the shared shell path
      pattern: TeacherSidebarShell
---

<objective>
Refactor the teacher-facing shell render path to consume centralized shell config instead of route string conditionals.

Purpose: Remove business route knowledge from `teacher-sidebar-shell.tsx` while keeping today’s `/teacher`, `/settings`, and `/resources` visuals stable.
Output: Resolver-driven shell renderer and updated route-layout consumers on the same shared shell path.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-teacher-shell-route-metadata-system/19-CONTEXT.md
@.planning/phases/19-teacher-shell-route-metadata-system/19-RESEARCH.md
@.planning/phases/19-teacher-shell-route-metadata-system/19-UI-SPEC.md
@.planning/phases/19-teacher-shell-route-metadata-system/19-01-PLAN.md
@src/components/shell/teacher-sidebar-shell.tsx
@src/app/(teacher)/teacher/layout.tsx
@src/app/settings/layout.tsx
@src/app/(library)/resources/layout.tsx

<interfaces>
From src/lib/theme-layout/shell-surface-resolver.ts (created by 19-01):
```ts
export function getShellSurfaceConfig(input: { routeKey: TeacherThemeRouteKey; layoutRuntime: ThemeLayoutRuntime }): {
  shellVariant: string;
  shellConfig: {
    mode: ThemeShellMode;
    radius: "rounded" | "square";
    width: "default" | "full-width";
    chrome: "default" | "immersive" | "minimal" | "presentation" | "fullscreen" | "focus";
  };
  surfaceMetadata: {
    routeKey: TeacherThemeRouteKey;
    label: string;
    summary: ThemeLayoutSummary;
  };
};
```

Current render anti-pattern to remove from src/components/shell/teacher-sidebar-shell.tsx:
```ts
const isTeacherHome = routeKey === "/teacher";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor TeacherSidebarShellFrame to render only from resolved shell config</name>
  <files>src/components/shell/teacher-sidebar-shell.tsx</files>
  <read_first>src/components/shell/teacher-sidebar-shell.tsx, src/lib/theme-layout/shell-surface-resolver.ts, src/lib/dal/themes.ts, .planning/phases/19-teacher-shell-route-metadata-system/19-UI-SPEC.md, .planning/quick/260511-bbe-teacher-main-main-div-100/260511-bbe-SUMMARY.md, .planning/quick/260511-bn1-teacher-data-region-page-header-page-hea/260511-bn1-SUMMARY.md</read_first>
  <action>Refactor `src/components/shell/teacher-sidebar-shell.tsx` per D-03, D-04, D-05, D-06, D-07, D-08, and D-09 so the render body consumes only centralized resolver output. Keep the async wrapper fetching `layoutRuntime` via `getCurrentActorThemeRuntimeState()`, but have it call `getShellSurfaceConfig({ routeKey, layoutRuntime })` and pass only `shellVariant`, `shellConfig`, and `surfaceMetadata` into `TeacherSidebarShellFrame`. Remove `const isTeacherHome = routeKey === "/teacher";` and the local `resolveSurfaceLabel()` switch. Replace those branches with explicit checks against resolver output: `shellConfig.radius === "square"` controls square shell corners, `shellConfig.width === "full-width"` controls the `/teacher` full-width behavior, and `shellConfig.chrome === "immersive"` controls the current immersive shell framing. Keep `page-header` on the real header block, keep `/teacher` square/full-width visuals unchanged, and do not add any new visible presentation/focus/fullscreen UI behavior in this phase.</action>
  <acceptance_criteria>
    - `src/components/shell/teacher-sidebar-shell.tsx` does not contain `routeKey === "/teacher"`.
    - `src/components/shell/teacher-sidebar-shell.tsx` does not contain `function resolveSurfaceLabel(`.
    - `src/components/shell/teacher-sidebar-shell.tsx` contains `getShellSurfaceConfig`, `shellConfig`, and `surfaceMetadata`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>The teacher shell render body no longer knows which route is “special”; it renders entirely from resolved shell config while preserving the live `/teacher` appearance.</done>
</task>

<task type="auto">
  <name>Task 2: Keep all teacher-facing layout entrypoints on the shared resolver-driven shell path</name>
  <files>src/app/(teacher)/teacher/layout.tsx, src/app/settings/layout.tsx, src/app/(library)/resources/layout.tsx</files>
  <read_first>src/app/(teacher)/teacher/layout.tsx, src/app/settings/layout.tsx, src/app/(library)/resources/layout.tsx, src/components/shell/teacher-sidebar-shell.tsx, src/lib/theme-layout/route-surface-registry.ts</read_first>
  <action>Update the teacher-facing route wrappers per D-05, D-06, D-08, D-09, and D-10 so `/teacher`, `/settings`, and `/resources` all continue to enter the same `TeacherSidebarShell` path after the resolver migration. In `src/app/(teacher)/teacher/layout.tsx`, keep `resolveTeacherThemeRouteSurface()` as the route-key source of truth, but ensure the fallback and main layout both feed the shared shell contract without reintroducing route-specific UI logic or header copy heuristics. In `src/app/settings/layout.tsx` and `src/app/(library)/resources/layout.tsx`, keep the shared shell wrapper and make any prop adjustments needed for the new resolver-driven contract, while preserving current header titles and descriptions where they are intentionally provided. Do not fork a second shell implementation for settings or resources.</action>
  <acceptance_criteria>
    - `src/app/(teacher)/teacher/layout.tsx` contains `resolveTeacherThemeRouteSurface` and `TeacherSidebarShell`.
    - `src/app/settings/layout.tsx` contains `TeacherSidebarShell`.
    - `src/app/(library)/resources/layout.tsx` contains `TeacherSidebarShell`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('src/app/(teacher)/teacher/layout.tsx','utf8');const s=fs.readFileSync('src/app/settings/layout.tsx','utf8');const r=fs.readFileSync('src/app/(library)/resources/layout.tsx','utf8');if(!t.includes('resolveTeacherThemeRouteSurface')||!t.includes('TeacherSidebarShell')||!s.includes('TeacherSidebarShell')||!r.includes('TeacherSidebarShell'))process.exit(1)"</automated>
  </verify>
  <done>Teacher, settings, and resources layouts all stay on the same resolver-driven shell entry path, so the migration remains low-risk and does not create a parallel shell system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell resolver -> shell renderer | Centralized config directly changes visible shell structure |
| route layouts -> shared shell wrapper | Teacher/settings/resources inherit the same resolver-driven shell behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | T | `src/components/shell/teacher-sidebar-shell.tsx` | mitigate | Remove route-string checks and bind shell behavior only to typed resolver output fields. |
| T-19-06 | D | `src/components/shell/teacher-sidebar-shell.tsx` | mitigate | Preserve current `/teacher` square/full-width/immersive behavior through explicit `shellConfig` fields so refactor does not drift visually. |
| T-19-07 | E | `src/app/settings/layout.tsx` / `src/app/(library)/resources/layout.tsx` | mitigate | Keep both routes on the same shared shell wrapper instead of forking resolver logic into separate layouts. |
| T-19-08 | R | `src/app/(teacher)/teacher/layout.tsx` | accept | Route-key resolution remains auditable through the existing allowlisted registry and does not expose sensitive data. |
</threat_model>

<verification>
- `pnpm typecheck`
- `node -e "const fs=require('fs');const shell=fs.readFileSync('src/components/shell/teacher-sidebar-shell.tsx','utf8');if(shell.includes('routeKey === \"/teacher\"'))process.exit(1)"`
</verification>

<success_criteria>
- `teacher-sidebar-shell.tsx` no longer contains business route checks.
- `/teacher` keeps the current square/full-width visuals after the refactor.
- `/settings` and `/resources` remain on the same shared shell path.
</success_criteria>

<output>
After completion, create `.planning/phases/19-teacher-shell-route-metadata-system/19-02-SUMMARY.md`
</output>
