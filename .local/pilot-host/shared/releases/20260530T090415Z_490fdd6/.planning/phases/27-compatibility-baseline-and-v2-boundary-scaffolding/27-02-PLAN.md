---
phase: 27-compatibility-baseline-and-v2-boundary-scaffolding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/runtime-platform/index.ts
  - src/features/runtime-platform/shared/boundary-map.ts
  - src/features/runtime-platform/authoring/index.ts
  - src/features/runtime-platform/launch/index.ts
  - src/features/runtime-platform/classroom/index.ts
  - src/features/runtime-platform/player/index.ts
  - src/features/runtime-platform/plugins/index.ts
  - src/app/(teacher)/teacher/editor/page.tsx
  - src/app/(teacher)/teacher/launch/page.tsx
  - src/app/(classroom)/classroom/page.tsx
  - src/app/(student)/student/player/page.tsx
autonomous: true
requirements: [ARCH-01]
must_haves:
  truths:
    - "D-06 / D-07：Teacher 和 student 主链 route consumers 通过 runtime-platform public APIs 访问运行时相关能力，同时 legacy 入口继续保留为 compatibility shims。"
    - "D-05 / D-08：`runtime-platform` 以单根 + 子域形式存在，而不是多平级 feature roots，也不在本阶段同步拆 authoring/launch/player/classroom 独立根。"
    - "D-09：plugin 相关边界在同一个 `runtime-platform` 根下拥有占位子域与 ownership posture。"
  artifacts:
    - path: "src/features/runtime-platform/index.ts"
      provides: "runtime-platform root public barrel"
      exports: ["authoring", "launch", "classroom", "player", "plugins"]
    - path: "src/features/runtime-platform/shared/boundary-map.ts"
      provides: "runtime-platform migration rules"
      contains: "runtimePlatformBoundaryMap"
    - path: "src/app/(teacher)/teacher/editor/page.tsx"
      provides: "editor route consuming runtime-platform authoring API"
  key_links:
    - from: "src/app/(teacher)/teacher/editor/page.tsx"
      to: "src/features/runtime-platform/authoring/index.ts"
      via: "ES module import"
      pattern: "@/features/runtime-platform/authoring"
    - from: "src/app/(classroom)/classroom/page.tsx"
      to: "src/features/runtime-platform/classroom/index.ts"
      via: "ES module import"
      pattern: "@/features/runtime-platform/classroom"
---

<objective>
建立 `runtime-platform` 单根 feature boundary，并让四条课堂主链 route consumer 先改走显式 public APIs。

Purpose: 先把依赖入口固定到新边界，后续 V2 改造才能局部迁移而不是在 `src/app` 与 `src/lib/*` 间继续深度耦合。
Output: `runtime-platform` root/subdomain barrels、boundary map，以及四个 route page 的新 import posture。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-compatibility-baseline-and-v2-boundary-scaffolding/27-CONTEXT.md
@.planning/phases/27-compatibility-baseline-and-v2-boundary-scaffolding/27-RESEARCH.md
@.planning/phases/27-compatibility-baseline-and-v2-boundary-scaffolding/27-PATTERNS.md
@src/features/schedule/index.ts
@src/features/schedule/shared/boundary-map.ts
@src/app/(teacher)/teacher/editor/page.tsx
@src/app/(teacher)/teacher/launch/page.tsx
@src/app/(classroom)/classroom/page.tsx
@src/app/(student)/student/player/page.tsx

<interfaces>
From `src/features/schedule/index.ts`:

```ts
export * from "./assistant";
export * from "./import";
export * from "./operations";
export * from "./reminders";
export * from "./runtime";
export * from "./shared";
```

From `src/features/schedule/shared/boundary-map.ts`:

```ts
export const scheduleFeatureBoundaryMap = {
  publicEntrypoints: [...],
  implementationSources: {...},
  rules: [...],
} as const;
```

From current route consumers:

```ts
// /teacher/editor
assertActiveTeacher, getLessonEditorDTO, getTeacherAuthoringOverview

// /teacher/launch
getClassroomConsoleDTO

// /classroom
getClassroomConsoleDTO, getClassroomSessionRecapDTO,
getClassroomSnapshotDTO, getClassroomStudentDetailDTO

// /student/player
getStudentDashboardDTO, assertStudentCanOpenPlayer,
getStudentPlayerPersonalDTO, getStudentPlayerShellDTO
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 runtime-platform 根与子域 public barrels</name>
  <read_first>
    - src/features/schedule/index.ts
    - src/features/schedule/shared/boundary-map.ts
    - .planning/phases/27-compatibility-baseline-and-v2-boundary-scaffolding/27-CONTEXT.md
    - .planning/phases/27-compatibility-baseline-and-v2-boundary-scaffolding/27-RESEARCH.md
  </read_first>
  <files>src/features/runtime-platform/index.ts, src/features/runtime-platform/shared/boundary-map.ts, src/features/runtime-platform/authoring/index.ts, src/features/runtime-platform/launch/index.ts, src/features/runtime-platform/classroom/index.ts, src/features/runtime-platform/player/index.ts, src/features/runtime-platform/plugins/index.ts</files>
  <action>参照 schedule feature root 建立单一 `src/features/runtime-platform` 根，按 D-05、D-08、D-09 创建 `authoring`、`launch`、`classroom`、`player`、`plugins` 子域与根 barrel；`shared/boundary-map.ts` 必须显式列出 publicEntrypoints、当前 implementationSources、compatibility rules，并写明 route consumers 以后只能走 `@/features/runtime-platform/*`，不得继续从 `src/app` 直接深引 `@/lib/dal/*`。本任务只建立 public API 和 migration map，不新增 route、不改变 URL、不启用 runtime host / event bus cutover。</action>
  <acceptance_criteria>
    - `src/features/runtime-platform/index.ts` 导出 `authoring`、`launch`、`classroom`、`player`、`plugins` 子域。
    - `src/features/runtime-platform/shared/boundary-map.ts` 包含 `publicEntrypoints`、`implementationSources`、`rules` 三段，并出现 `runtime-platform` 与 `compatibility` 规则文本。
    - boundary map 明确写出 `legacy routes keep working` 或等价兼容语义，落实 D-04。
    - 新根目录中不出现 `event bus cutover`、`WebSocket enable`、`Redis required` 等提前切换语义。
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>`runtime-platform` 成为可被 route consumer 使用的单根 public API，且 migration rules 已编码到 boundary map。</done>
</task>

<task type="auto">
  <name>Task 2: 将课堂主链 route consumers 改接到 runtime-platform public APIs</name>
  <read_first>
    - src/app/(teacher)/teacher/editor/page.tsx
    - src/app/(teacher)/teacher/launch/page.tsx
    - src/app/(classroom)/classroom/page.tsx
    - src/app/(student)/student/player/page.tsx
    - src/features/runtime-platform/index.ts
    - src/features/runtime-platform/authoring/index.ts
    - src/features/runtime-platform/launch/index.ts
    - src/features/runtime-platform/classroom/index.ts
    - src/features/runtime-platform/player/index.ts
  </read_first>
  <files>src/app/(teacher)/teacher/editor/page.tsx, src/app/(teacher)/teacher/launch/page.tsx, src/app/(classroom)/classroom/page.tsx, src/app/(student)/student/player/page.tsx</files>
  <action>按 D-06 把四个 route page 的 import posture 收敛到 `@/features/runtime-platform/*` public APIs：`/teacher/editor` 继续守住 `courseId + lessonId` guardrail，`/teacher/launch` 继续只读 published snapshot console DTO，`/classroom` 继续在同一路径承接 live snapshot + ended recap + student detail，`/student/player` 继续保留 shell/personal split 与 Suspense personal loader。不得新增 route string branching、不得修改现有 query 参数合同、不得把旧入口删除或改名。</action>
  <acceptance_criteria>
    - 四个 route page 均包含 `@/features/runtime-platform/` import，且不再直接 import `@/lib/dal/lesson-authoring`、`@/lib/dal/classroom`、`@/lib/dal/learning`。
    - `/teacher/editor` 仍保留 `courseId` 与 `lessonId` 双重 guard 文案/逻辑。
    - `/classroom` 仍保留 `sessionId` 驱动的 `live` / `ended` 分支读取。
    - `/student/player` 仍保留 `Suspense` + `getStudentPlayerShellDTO` + `getStudentPlayerPersonalDTO` 分离加载。
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>课堂主链路由开始直接依赖 runtime-platform 边界，但用户可见行为与入口约束保持不变。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route page → feature public API | route consumer 进入新的 runtime-platform 边界，若继续深引 legacy 实现会绕开迁移控制 |
| feature boundary → legacy implementation | 迁移期双入口并存，若无清晰规则会出现漂移或不一致行为 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | T | route imports | mitigate | 仅允许 route pages import `@/features/runtime-platform/*`，并在 boundary map + verifier 中静态检查 direct legacy imports |
| T-27-02-02 | E | runtime-platform public barrel | mitigate | public barrel 只导出 allowlisted subdomains，不暴露 deep implementation paths 或 ad hoc re-export |
| T-27-02-03 | R | migration period dual entry | mitigate | boundary map 明确 compatibility rules，后续 `verify:phase27` 失败即 loud fail，防止 route drift 无记录发生 |
</threat_model>

<verification>
- `pnpm typecheck`
- 读取四个 route page，确认 imports 已切到 `@/features/runtime-platform/*`
- 读取 `src/features/runtime-platform/shared/boundary-map.ts`，确认包含 compatibility rules
</verification>

<success_criteria>
- `runtime-platform` 成为课堂主链 route consumers 的显式入口。
- 当前 editor / launch / classroom / player 路由 URL、query 参数和行为语义未漂移。
- 仓库中已存在可继续增量迁移的 boundary map，而不是一次性 big-bang 改造指令。
</success_criteria>

<output>
After completion, create `.planning/phases/27-compatibility-baseline-and-v2-boundary-scaffolding/27-02-SUMMARY.md`
</output>
