---
phase: 26-cross-session-trends-and-stitch-productization
plan: 06
type: execute
wave: 5
depends_on:
  - 26-01
  - 26-02
  - 26-03
  - 26-04
  - 26-05
files_modified:
  - scripts/verify-phase26-trends-productization.ts
  - package.json
autonomous: true
requirements:
  - ANALYTICS-02
  - UI-05
must_haves:
  truths:
    - Phase 26 的 major teacher routes 存在一条可重复执行的一键质量门，统一守住 trends、dashboard、editor、launch、classroom、review、help、settings 与 trends / recap 双入口。
    - 后续改动不能静默重新引入第二 analytics truth source、review-first drift、`/teacher/reports` 主入口回流，或 responsive / no-horizontal-scroll contract 退化。
    - major route 的关键回归点被统一验证覆盖，而不是依赖人工肉眼回归里程碑质量。
  artifacts:
    - path: scripts/verify-phase26-trends-productization.ts
      provides: final phase verifier with static guards and focused route suite
      contains: verify:phase26
    - path: package.json
      provides: repo-level entry point for the phase verifier
      contains: verify:phase26
  key_links:
    - from: package.json
      to: scripts/verify-phase26-trends-productization.ts
      via: `verify:phase26`
      pattern: verify:phase26
    - from: scripts/verify-phase26-trends-productization.ts
      to: focused test files
      via: static guards run first, then the major-route regression suite proves behavior
      pattern: pnpm test --run
---

<objective>
为 Phase 26 建立最终验证闭环，固定 trends、dual-entry、route metadata 与 productization
路由集，保证里程碑收尾质量不会在后续改动中静默下滑。

Purpose: 用 phase-specific verifier 把前面分散的 focused tests 和 static guards 收口成一个可重复执行的质量门。
Output: `verify:phase26` plus static guards against analytics and productization drift.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/26-cross-session-trends-and-stitch-productization/26-CONTEXT.md
@.planning/phases/26-cross-session-trends-and-stitch-productization/26-RESEARCH.md
@.planning/phases/26-cross-session-trends-and-stitch-productization/26-UI-SPEC.md
@scripts/verify-phase25-session-analytics.ts
@scripts/verify-phase19-shell-route-metadata.ts
@package.json
</context>

<interfaces>
Verifier pattern already in repo:
```json
{
  "scripts": {
    "verify:phase25": "tsx scripts/verify-phase25-session-analytics.ts"
  }
}
```

Expected Phase 26 proof surface:
```bash
pnpm test --run \
  src/lib/dal/classroom.test.ts \
  src/lib/theme-layout/shell-surface-resolver.test.ts \
  src/components/shell/teacher-sidebar-shell.test.tsx \
  src/app/(teacher)/teacher/trends/page.test.tsx \
  src/components/surfaces/teacher-trends-surface.test.tsx \
  src/components/classroom/classroom-session-recap-surface.test.tsx \
  src/components/surfaces/lesson-editor-surface.test.tsx \
  src/components/classroom/classroom-launch-panel.test.tsx \
  src/components/surfaces/classroom-console-surface.test.tsx \
  src/components/learning/teacher-review-surface.test.ts \
  src/components/surfaces/teacher-dashboard-surface.test.tsx \
  src/components/surfaces/help-center-overview-surface.test.tsx \
  src/components/surfaces/settings-surface.test.tsx
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add `verify:phase26` static guards and wire the distributed regression suite</name>
  <files>scripts/verify-phase26-trends-productization.ts, package.json</files>
  <read_first>package.json, scripts/verify-phase25-session-analytics.ts, scripts/verify-phase19-shell-route-metadata.ts, .planning/phases/26-cross-session-trends-and-stitch-productization/26-CONTEXT.md, .planning/phases/26-cross-session-trends-and-stitch-productization/26-RESEARCH.md</read_first>
  <acceptance_criteria>
    - `package.json` contains `verify:phase26`.
    - `scripts/verify-phase26-trends-productization.ts` checks required files before running tests.
    - the verifier fails if `/teacher/trends` is missing from the route registry/navigation, if `/teacher/reports` returns as the primary analytics nav, if `/teacher/review` becomes the primary trend jump, or if a new analytics persistence path appears.
    - the verifier fails if the recap-to-trends deep link disappears, or if `teacher-trends-surface.tsx` reintroduces `overflow-x-auto` or loses the required desktop split class.
    - the verifier fails if dashboard/help/settings/classroom surfaces stop reusing `teacherSurfaceRhythm` or `surfaceWidths`.
    - the verifier runs the focused Phase 26 test suite rather than outputting prose only.
  </acceptance_criteria>
  <action>Create `scripts/verify-phase26-trends-productization.ts` following the repo verifier pattern: required-file guards, source-string/static checks, then a focused `pnpm test --run ...` suite. Static checks must assert that `route-surface-registry.ts` includes `/teacher/trends`, `resolveTeacherThemeRouteSurface()` resolves it, navigation sources point to `/teacher/trends` instead of `/teacher/reports`, the trends surface/page keep `/classroom?sessionId=` as the primary detail CTA, `src/components/classroom/classroom-session-recap-surface.tsx` still exposes `/teacher/trends?classId=` deep links, `/teacher/review` is not the primary jump, `src/lib/dal/classroom.ts` still contains no analytics snapshot/materialized write path, `src/components/surfaces/teacher-trends-surface.tsx` retains the required desktop split class and does not contain `overflow-x-auto`, and dashboard/help/settings/classroom surfaces continue to reference `teacherSurfaceRhythm` plus `surfaceWidths`. Then run the distributed test suite: `src/lib/dal/classroom.test.ts`, `src/lib/theme-layout/shell-surface-resolver.test.ts`, `src/components/shell/teacher-sidebar-shell.test.tsx`, `src/app/(teacher)/teacher/trends/page.test.tsx`, `src/components/surfaces/teacher-trends-surface.test.tsx`, `src/components/classroom/classroom-session-recap-surface.test.tsx`, `src/components/surfaces/lesson-editor-surface.test.tsx`, `src/components/classroom/classroom-launch-panel.test.tsx`, `src/components/surfaces/classroom-console-surface.test.tsx`, `src/components/learning/teacher-review-surface.test.ts`, `src/components/surfaces/teacher-dashboard-surface.test.tsx`, `src/components/surfaces/help-center-overview-surface.test.tsx`, and `src/components/surfaces/settings-surface.test.tsx`. Add `verify:phase26` to `package.json` and keep the command repo-local via `tsx`.</action>
  <verify>
    <automated>pnpm verify:phase26</automated>
  </verify>
  <done>The milestone ends with a repeatable verifier that guards both analytics safety boundaries and UI productization drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase implementation -> verifier | static checks and route tests must catch drift before future features degrade the milestone finish line |
| navigation/route metadata -> teacher product surface | the trends entry and shell metadata must remain aligned across all sources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-16 | T | Phase 26 verifier coverage | mitigate | Add `verify:phase26` with required-file checks and the distributed route/productization test suite. |
| T-26-17 | I | analytics route and truth drift | mitigate | Fail verification if `/teacher/reports` returns, `/teacher/review` becomes the primary trend jump, or analytics persistence writes appear. |
| T-26-18 | R | shell metadata / product skeleton erosion | mitigate | Assert `/teacher/trends` exists in route metadata and touched surfaces continue to reuse the shared teacher skeleton through static guards and tests. |
</threat_model>

<verification>
- `pnpm verify:phase26`
</verification>

<success_criteria>
- Phase 26 has a dedicated verifier covering trends route safety and productized route quality.
- Focused tests from Plans 26-01 through 26-05 are wired into one repeatable quality gate.
- Future work cannot silently reintroduce a second analytics truth source or a detached reporting route.
</success_criteria>

<output>
After completion, create `.planning/phases/26-cross-session-trends-and-stitch-productization/26-06-SUMMARY.md`
</output>
