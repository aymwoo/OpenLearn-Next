---
phase: 18-teaching-schedule-os
plan: 06
type: execute
wave: 5
depends_on:
  - 18-02
  - 18-03
  - 18-04
  - 18-05
files_modified:
  - src/lib/dal/schedule-assistant.ts
  - src/lib/dal/schedule-assistant.test.ts
  - src/actions/schedule-assistant-actions.ts
  - src/components/surfaces/schedule-assistant-surface.tsx
  - src/app/(teacher)/teacher/schedule/assistant/page.tsx
  - src/lib/dto/resource-ai.ts
  - src/server/plugins/registry.ts
  - src/lib/dal/plugins.ts
  - scripts/verify-phase18-schedule.ts
  - package.json
autonomous: true
requirements:
  - SCHEDULE-07
  - SCHEDULE-08
  - SCHEDULE-09
must_haves:
  truths:
    - D-10/D-11: AI 助手只能产出 mapping/conflict/override 建议，不直接写 schedule runtime。
    - D-15: plugin 扩展只暴露 allowlisted schedule hooks/actions，并继续走 proposal/result contract。
    - D-16: assistant surface 用中文、tonal cards、proposal approval flow，不伪装成“自动执行器”。
  artifacts:
    - path: src/lib/dal/schedule-assistant.ts
      provides: proposal-only assistant DAL
      contains: createScheduleAssistantProposal
    - path: src/server/plugins/registry.ts
      provides: schedule extension allowlist
      contains: createScheduleOverrideProposal
    - path: scripts/verify-phase18-schedule.ts
      provides: phase-specific schedule verification command
      contains: verify:phase18
  key_links:
    - from: src/actions/schedule-assistant-actions.ts
      to: src/lib/dal/schedule-assistant.ts
      via: proposal creation and explicit approval path
      pattern: SCHEDULE_ASSISTANT_APPROVAL_BLOCKED
    - from: src/server/plugins/registry.ts
      to: src/lib/dal/plugins.ts
      via: allowlisted schedule actions dispatch through existing plugin safety layer
      pattern: createScheduleOverrideProposal|createScheduleReminderDraft
---

<objective>
收口 Phase 18 的 AI 助手与插件扩展能力：助手只给建议、插件只给 allowlisted proposal，并追加专用 verify script 守住三层架构与安全边界。

Purpose: 完成 D-10/D-11/D-15 的最后闭环，防止“智能课表”滑向自动改课或任意扩展执行。
Output: assistant DAL/actions/surface、schedule plugin allowlist、`verify:phase18`。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-teaching-schedule-os/18-CONTEXT.md
@.planning/phases/18-teaching-schedule-os/18-RESEARCH.md
@.planning/phases/18-teaching-schedule-os/18-UI-SPEC.md
@.planning/phases/18-teaching-schedule-os/18-PATTERNS.md
@src/lib/dto/resource-ai.ts
@src/server/plugins/registry.ts
@src/lib/dal/plugins.ts
@scripts/verify-phase17-editor.ts

<interfaces>
Extend the existing plugin action allowlist pattern:
```ts
export const PLUGIN_ACTION_ALLOWLIST = [
  "addStepSuggestion",
  "annotateLesson",
  "createNotificationStub",
  // schedule actions added here must still return proposal payloads only
];
```

Assistant action results should keep the existing shape:
```ts
type ActionResult<T> = { ok: true; data: T } | { ok: false; error: string; message: string; issues?: unknown[] };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement proposal-only schedule assistant flows with explicit approval gates</name>
  <files>src/lib/dal/schedule-assistant.ts, src/lib/dal/schedule-assistant.test.ts, src/actions/schedule-assistant-actions.ts</files>
  <read_first>src/lib/dto/schedule.ts, src/lib/dto/resource-ai.ts, src/actions/lesson-authoring-actions.ts, .planning/phases/18-teaching-schedule-os/18-RESEARCH.md</read_first>
  <behavior>
    - Test 1: per D-10/D-11, assistant requests create proposals for import mappings, conflict explanations, or override suggestions but never write `scheduleOverride` directly.
    - Test 2: approval creates a draft/proposal state that still requires a human-confirmed schedule mutation path.
    - Test 3: rejected proposals remain auditable and do not mutate runtime schedule data.
  </behavior>
  <action>Create `src/lib/dal/schedule-assistant.ts` to store and read schedule assistant proposals for three first-release use cases exactly from D-11: import mapping suggestion, conflict explanation, and override suggestion. Persist them in `scheduleAssistantProposal` with statuses such as `pending`, `approved`, `rejected`, and `draft_created`; do not invent a status that implies automatic execution. Add `src/actions/schedule-assistant-actions.ts` that uses `safeParse + assertActiveTeacher + updateTag()` and returns structured blocks such as `SCHEDULE_ASSISTANT_APPROVAL_BLOCKED` when a proposal is missing scope, target, or approval context. Approval may create a prefilled draft payload for the existing import review or override flow, but it must not directly insert/update runtime schedule tables.</action>
  <acceptance_criteria>
    - `src/lib/dal/schedule-assistant.ts` contains proposal types for import mapping, conflict explanation, and override suggestion.
    - `src/lib/dal/schedule-assistant.ts` does not write `scheduleOverride` or `scheduleRecurringEntry` directly in the approval path.
    - `src/actions/schedule-assistant-actions.ts` contains `SCHEDULE_ASSISTANT_APPROVAL_BLOCKED`.
    - `src/lib/dal/schedule-assistant.test.ts` asserts rejected proposals do not mutate runtime schedule tables.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/schedule-assistant.test.ts</automated>
  </verify>
  <done>The AI assistant behaves like a suggestion engine with explicit human approval, not an autonomous schedule operator.</done>
</task>

<task type="auto">
  <name>Task 2: Add schedule plugin allowlists, assistant surface, and a phase-specific verification command</name>
  <files>src/components/surfaces/schedule-assistant-surface.tsx, src/app/(teacher)/teacher/schedule/assistant/page.tsx, src/lib/dto/resource-ai.ts, src/server/plugins/registry.ts, src/lib/dal/plugins.ts, scripts/verify-phase18-schedule.ts, package.json</files>
  <read_first>src/lib/dto/resource-ai.ts, src/server/plugins/registry.ts, src/lib/dal/plugins.ts, scripts/verify-phase17-editor.ts, .planning/phases/18-teaching-schedule-os/18-UI-SPEC.md</read_first>
  <action>Extend `src/lib/dto/resource-ai.ts`, `src/server/plugins/registry.ts`, and `src/lib/dal/plugins.ts` with schedule-specific, proposal-only extension points. Add explicit hook anchors and allowlisted actions for schedule workflows only if they return draft/proposal payloads, for example `createScheduleOverrideProposal`, `createScheduleReminderDraft`, and `annotateScheduleConflict`; do not add any action that directly commits runtime schedule changes. Then create `/teacher/schedule/assistant/page.tsx` plus `ScheduleAssistantSurface` to render proposal cards with the exact human-review framing from the UI-SPEC: suggestion title, reason, impact scope, fields requiring confirmation, and actions like `查看建议`, `采纳为草案`, `去人工确认`. Finally create `scripts/verify-phase18-schedule.ts` and register `verify:phase18` in `package.json`. The verify script must statically prove these invariants: (1) schedule surfaces do not import `db` directly; (2) runtime DAL does not expose `scheduleImportRow` to teacher agenda DTOs; (3) assistant actions return proposal/draft states, not direct writes; (4) plugin registry contains only allowlisted schedule proposal actions; and (5) no `eval(` or `dangerouslySetInnerHTML` appears in the schedule/plugin path.</action>
  <acceptance_criteria>
    - `src/server/plugins/registry.ts` contains schedule allowlist entries such as `createScheduleOverrideProposal` and/or `createScheduleReminderDraft`.
    - `src/lib/dto/resource-ai.ts` contains typed schedule proposal payloads instead of direct mutation payloads.
    - `src/components/surfaces/schedule-assistant-surface.tsx` contains `查看建议`, `采纳为草案`, and `去人工确认`.
    - `package.json` contains `verify:phase18`.
    - `scripts/verify-phase18-schedule.ts` contains checks for `scheduleImportRow`, direct `db` imports in schedule surfaces, and unsafe patterns like `eval(`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase18</automated>
  </verify>
  <done>Phase 18 ends with approval-gated assistant and plugin extension workflows plus an executable verifier that guards the core schedule architecture and safety rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI / plugin suggestion generation → schedule proposal store | Semi-trusted generated content crosses into approval-gated schedule workflows |
| proposal surface → human approval path | Teacher/operator decisions control whether a suggestion becomes a draft |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-21 | E | `src/actions/schedule-assistant-actions.ts` | mitigate | Keep assistant approval path proposal-only and block direct runtime schedule writes. |
| T-18-22 | T | `src/server/plugins/registry.ts` | mitigate | Add only allowlisted schedule proposal actions; deny arbitrary schedule mutations through plugin dispatch. |
| T-18-23 | I | `src/components/surfaces/schedule-assistant-surface.tsx` | mitigate | Show human-review fields and proposal scope only; never expose raw import payloads or secrets. |
| T-18-24 | R | `scripts/verify-phase18-schedule.ts` | mitigate | Enforce static checks for raw-row leakage, direct DB imports, unsafe plugin patterns, and proposal-only boundaries. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/schedule-assistant.test.ts`
- `pnpm verify:phase18`
</verification>

<success_criteria>
- AI suggestions stay approval-gated and cannot mutate schedule runtime directly.
- Plugin extensions use allowlisted schedule proposal actions only.
- Phase 18 ships with an executable verifier for schedule architecture and safety boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/18-teaching-schedule-os/18-06-SUMMARY.md`
</output>
