---
phase: 54-ai-native-contract-exposure
plan: "03"
type: execute
wave: 2
depends_on:
  - "54-02"
files_modified:
  - src/features/platform-core/commands/contracts.ts
  - src/features/platform-core/events/contracts.ts
  - src/features/platform-core/ai-contracts/delegation.ts
  - src/features/platform-core/ai-contracts/delegation.test.ts
autonomous: true
requirements:
  - AINT-03
  - AINT-04
must_haves:
  truths:
    - "delegated metadata 是 annotation/posture，不是 implicit authority elevation。"
    - "approval metadata 只交付 summary/reference seam，不落 full workflow engine。"
---

<objective>
为 command/event/audit contract 补 delegated actor 与 approval metadata 的 shared summary contract。
</objective>

<tasks>
  <task type="auto" tdd="true">
    <name>Task 1: Add delegated actor and approval metadata contracts</name>
    <files>src/features/platform-core/commands/contracts.ts, src/features/platform-core/events/contracts.ts, src/features/platform-core/ai-contracts/delegation.ts, src/features/platform-core/ai-contracts/delegation.test.ts</files>
    <acceptance_criteria>
      - delegated metadata 区分 delegated agent actor 与 existing actorScope。
      - approval posture 通过 summary/reference 字段暴露，不带完整 object snapshot。
      - 不允许 delegated metadata 被解释为默认高权限执行。
    </acceptance_criteria>
    <verify>
      <automated>pnpm vitest run src/features/platform-core/ai-contracts/delegation.test.ts</automated>
    </verify>
  </task>
</tasks>
