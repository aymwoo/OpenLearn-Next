---
phase: 54-ai-native-contract-exposure
plan: "04"
type: execute
wave: 2
depends_on:
  - "54-03"
files_modified:
  - src/components/surfaces/settings-surface.tsx
  - src/components/surfaces/settings-surface.test.tsx
  - scripts/verify-phase54-ai-contracts.ts
  - package.json
autonomous: true
requirements:
  - AINT-01
  - AINT-04
  - AINT-05
must_haves:
  truths:
    - "若有 UI，只能是最小 discoverability/operator surface，不是 full Agent console。"
    - "verify:phase54 必须自动阻止 authority drift、privilege escalation、full runtime scope creep。"
---

<objective>
为 Phase 54 增加最小 discoverability surface 与 focused regression gate，完成 phase close posture。
</objective>

<tasks>
  <task type="auto" tdd="true">
    <name>Task 1: Add minimal discoverability surface and verify gate</name>
    <files>src/components/surfaces/settings-surface.tsx, src/components/surfaces/settings-surface.test.tsx, scripts/verify-phase54-ai-contracts.ts, package.json</files>
    <acceptance_criteria>
      - `/settings/labs` 或现有 operator/developer entrypoint 能看到最小 command/action/capability discoverability panel。
      - 新增 `verify:phase54`，静态阻止 descriptor reclaim authority、delegated metadata imply privilege escalation、以及 Agent Runtime / Skill Runtime / Workflow Engine scope creep。
      - verifier 串起 Phase 54 focused suites。
    </acceptance_criteria>
    <verify>
      <automated>pnpm verify:phase54</automated>
    </verify>
  </task>
</tasks>
