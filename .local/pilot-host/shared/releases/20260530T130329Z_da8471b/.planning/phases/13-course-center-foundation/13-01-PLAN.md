---
phase: 13-course-center-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/cache-policy.ts
  - src/lib/dto/course-authoring.ts
  - src/lib/dal/course-authoring.ts
  - src/lib/dal/course-authoring.test.ts
  - src/app/(teacher)/teacher/courses/page.tsx
  - src/app/(teacher)/teacher/courses/[courseId]/page.tsx
  - src/components/surfaces/teacher-course-center-surface.tsx
  - src/components/surfaces/teacher-course-detail-surface.tsx
autonomous: true
requirements:
  - COURSE-01
must_haves:
  truths:
    - D-16: 教师能打开 /teacher/courses 并只看到自己 teacher/school scope 内的课程
    - D-13 D-14 D-15: 课程总览默认隐藏 archived 课程，并按 draft→published→archived 优先级与 updatedAt 倒序排序
    - D-01 D-02 D-03 D-04 D-18: 教师看到卡片网格课程总览，课程卡显式状态 badge，点击后进入独立课程详情页而不是直接进入 editor
  artifacts:
    - path: src/lib/dal/course-authoring.ts
      provides: teacher-scoped course center and detail read model
    - path: src/app/(teacher)/teacher/courses/page.tsx
      provides: teacher course center route
    - path: src/app/(teacher)/teacher/courses/[courseId]/page.tsx
      provides: independent course detail route
  key_links:
    - src/app/(teacher)/teacher/courses/page.tsx -> src/lib/dal/course-authoring.ts via getTeacherCourseCenterDTO
    - src/app/(teacher)/teacher/courses/[courseId]/page.tsx -> src/lib/dal/course-authoring.ts via getTeacherCourseDetailDTO
    - src/lib/dal/course-authoring.ts -> src/lib/cache-policy.ts via cacheTags.teacherCourses/cacheTags.course
---

<objective>
建立 Phase 13 的课程中心读模型、路由骨架和详情页入口，把教师从占位课程库切到真实的 teacher-scoped 课程工作流。

Purpose: 先把 D-01、D-02、D-03、D-04、D-13、D-14、D-15、D-16、D-17、D-18 固化为可读的服务端 contract，后续 create/edit 与 lesson handoff 才有稳定承载面。
Output: 课程中心 DTO/DAL、teacher route、课程卡片网格、独立详情页只读骨架。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-course-center-foundation/13-CONTEXT.md
@.planning/phases/13-course-center-foundation/13-RESEARCH.md
@.planning/phases/13-course-center-foundation/13-UI-SPEC.md
@.planning/phases/13-course-center-foundation/13-PATTERNS.md

<interfaces>
From `src/lib/dal/lesson-authoring.ts`:
```ts
export async function assertActiveTeacher(): Promise<{ userId: string; schoolIds: string[] }>
```

From `src/lib/dto/lesson-authoring.ts`:
```ts
export const CourseDTOSchema = z.object({
  id: z.string(),
  schoolId: z.string(),
  ownerId: z.string(),
  title: z.string(),
  subject: z.string(),
  grade: z.string(),
  status: z.string(),
  lessonCount: z.number().int().nonnegative().default(0),
  classLabels: z.array(z.string()).default([]),
  enrollmentCount: z.number().int().nonnegative().default(0),
  updatedAt: z.string(),
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义课程中心 DTO、缓存标签和 teacher-scoped 读模型</name>
  <files>src/lib/cache-policy.ts, src/lib/dto/course-authoring.ts, src/lib/dal/course-authoring.ts, src/lib/dal/course-authoring.test.ts</files>
  <read_first>src/lib/cache-policy.ts; src/lib/dal/lesson-authoring.ts; src/lib/dto/lesson-authoring.ts; src/db/schema.ts; src/lib/dal/lesson-authoring.test.ts</read_first>
  <behavior>
    - Test 1: 非教师或跨 school 课程不会出现在课程中心 DTO 中。
    - Test 2: 默认列表隐藏 archived，并按 D-14 的状态优先级与 updatedAt 倒序返回。
    - Test 3: 课程详情 DTO 提供 title、subject、grade、status、lesson summaries、class links、enrollment count。
  </behavior>
  <action>新增 `cacheTags.teacherCourses(actorId)`，并新建 `src/lib/dto/course-authoring.ts` 定义 `TeacherCourseCardDTO`、`TeacherCourseCenterDTO`、`TeacherCourseDetailDTO`、`CourseLessonEntryDTO` 等 schema。实现 `src/lib/dal/course-authoring.ts`，复用 `assertActiveTeacher()` 与 `assertSchoolAccess()`，禁止任何 UI 直连 DB。列表查询必须只读取 actor 的 active teacher memberships 所在 school 内课程（per D-16），默认过滤 `status !== "archived"`，仅当显式 `includeArchived` 为 true 时才返回 archived（per D-15）。排序必须用明确状态权重 `draft=0, published=1, archived=2`，然后 `updatedAt desc`（per D-13、D-14）。DTO 中保留 `lessonCount`、`classLabels`、`enrollmentCount`、`updatedAt`，并为详情页额外返回当前课程课时摘要列表，为后续 D-09~D-12 做准备。先写 `src/lib/dal/course-authoring.test.ts` 锁定 teacher scope、排序和过滤行为，再实现 DAL；测试名称与注释中显式引用 D-13、D-14、D-15、D-16。</action>
  <acceptance_criteria>
    - `src/lib/cache-policy.ts` contains `teacherCourses: (actorId: string)`
    - `src/lib/dal/course-authoring.ts` contains `export async function getTeacherCourseCenterDTO`
    - `src/lib/dal/course-authoring.ts` contains `export async function getTeacherCourseDetailDTO`
    - `src/lib/dal/course-authoring.ts` contains explicit status ordering for `draft`, `published`, `archived`
    - `pnpm vitest run src/lib/dal/course-authoring.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/lib/dal/course-authoring.test.ts</automated>
  </verify>
  <done>课程中心和课程详情都有稳定的 server-side DTO/DAL contract，且已用自动化测试证明 teacher/school scope、排序与 archived 过滤正确。</done>
</task>

<task type="auto">
  <name>Task 2: 建立课程总览页与独立课程详情页只读骨架</name>
  <files>src/app/(teacher)/teacher/courses/page.tsx, src/app/(teacher)/teacher/courses/[courseId]/page.tsx, src/components/surfaces/teacher-course-center-surface.tsx, src/components/surfaces/teacher-course-detail-surface.tsx</files>
  <read_first>src/app/(library)/courses/page.tsx; src/components/surfaces/library-surface.tsx; src/components/surfaces/lesson-editor-surface.tsx; src/app/(teacher)/teacher/layout.tsx; src/components/ui/ghost-field.ts</read_first>
  <action>把 `/teacher/courses` 落成真实教师路由，读取 `getTeacherCourseCenterDTO()`，不再复用 demo `courseCards`（per D-02、D-18）。课程中心必须保持“总览页 + 独立详情页”两级结构，不做列表页内联展开或三栏工作台（per D-01）；课程卡必须显式渲染状态 badge（per D-03），并把主进入动作固定为跳转 `/teacher/courses/[courseId]`（per D-04），禁止直接跳 editor。详情页先实现 read-only 概览骨架：展示课程标题、学科、年级、状态、更新时间、课时数、班级链接、学生数，以及“进入课时管理”预留 CTA 区域；视觉沿用 tonal cards、中文 copy、Lexend、无 1px border，与 Stitch `课程中心` 同语义（per D-18）。总览页保留 archived 显式筛选入口，但默认态不展示 archived（per D-15）。</action>
  <acceptance_criteria>
    - `src/app/(teacher)/teacher/courses/page.tsx` imports `getTeacherCourseCenterDTO`
    - `src/app/(teacher)/teacher/courses/[courseId]/page.tsx` imports `getTeacherCourseDetailDTO`
    - `src/components/surfaces/teacher-course-center-surface.tsx` contains `/teacher/courses/`
    - `src/components/surfaces/teacher-course-center-surface.tsx` contains status badge rendering
    - `src/components/surfaces/teacher-course-detail-surface.tsx` contains `进入课时管理`
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/lib/dal/course-authoring.test.ts</automated>
  </verify>
  <done>教师可以打开 `/teacher/courses` 看真实课程卡片，并进入独立详情页；课程读取来源已经从 demo surface 切换为 teacher-scoped DTO。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teacher request → course DAL | 未信任的 route params 和 session 上下文进入课程查询 |
| teacher course route → cached read model | 课程总览和详情通过缓存读取 teacher-scoped 数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | E | `getTeacherCourseCenterDTO` | mitigate | 复用 `assertActiveTeacher()` 与 school filter，只返回 active teacher memberships 覆盖的 school 课程 |
| T-13-02 | I | `/teacher/courses/[courseId]` | mitigate | 详情读取前校验 `courseId` 属于 actor school scope，跨 school 一律拒绝 |
| T-13-03 | T | 课程排序/过滤读模型 | mitigate | 把 D-13~D-15 的排序与 archived 默认隐藏写成可测试 DAL 逻辑，避免 UI 本地二次排序漂移 |
| T-13-04 | D | 课程中心缓存 | mitigate | 读模型使用 `cacheTags.teacherCourses(actorId)` 与 `cacheTags.course(courseId)`，为后续写入路径预留显式失效点 |
</threat_model>

<verification>
- `pnpm vitest run src/lib/dal/course-authoring.test.ts`
- Read `src/app/(teacher)/teacher/courses/page.tsx` and confirm it no longer imports demo `LibrarySurface`
</verification>

<success_criteria>
- `/teacher/courses` 已成为真实教师课程中心
- 课程详情页已存在且只读信息完整
- COURSE-01 对应的列表字段、排序与作用域均有自动化证明
</success_criteria>

<output>
After completion, create `.planning/phases/13-course-center-foundation/13-01-SUMMARY.md`
</output>
