---
phase: quick
plan: 260517-k2u
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auth/auth.config.ts
  - src/lib/auth/auth.ts
  - src/proxy.ts
  - src/lib/auth/auth.config.test.ts
autonomous: true
requirements:
  - QUICK-auth-localhost-untrustedhost
---

<objective>
修复 `next start` 下 Auth.js `UntrustedHost` 导致的本地 production-like 登录 500。

Purpose: 保持现有角色鉴权、Credentials 登录和路由守卫不变，只让 repo-local `localhost` / `127.0.0.1` 在本地生产启动时可通过 Auth.js host 校验。
Output: 一个仅收敛在 Auth.js host trust 边界的最小修复，外加定向回归测试与 quick 任务记录。
</objective>

<context>
@.planning/STATE.md
@src/lib/auth/auth.config.ts
@src/lib/auth/auth.ts
@src/proxy.ts
@src/lib/auth/auth.config.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 只对白名单本地 host 放开 Auth.js trustHost</name>
  <files>src/lib/auth/auth.config.ts, src/lib/auth/auth.ts, src/proxy.ts</files>
  <action>新增本地 host helper，并仅在请求 host 为 `localhost` 或 `127.0.0.1` 时注入 `trustHost: true`；不修改现有 provider、JWT、session、role guard 和 redirect 逻辑。</action>
  <done>`next start` 下 repo-local localhost/127.0.0.1 不再因 `UntrustedHost` 报 500，其他 host 默认仍不信任。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 补本地 host trust 回归测试</name>
  <files>src/lib/auth/auth.config.test.ts</files>
  <action>增加 focused tests，锁定 `localhost`、`127.0.0.1` 与带端口形式会被识别，非本地 host 仍被拒绝。</action>
  <verify>
    <automated>pnpm vitest run src/lib/auth/auth.config.test.ts src/lib/auth/auth.test.ts src/actions/auth-actions.test.ts src/app/login/page.test.tsx</automated>
  </verify>
  <done>本次修复边界有自动化覆盖，且不会放宽到任意 host。</done>
</task>

</tasks>

<verification>
- `pnpm vitest run src/lib/auth/auth.config.test.ts src/lib/auth/auth.test.ts src/actions/auth-actions.test.ts src/app/login/page.test.tsx`
- `AUTH_SECRET=test-secret pnpm build`
</verification>

<success_criteria>
- [x] 只对 repo-local `localhost` / `127.0.0.1` 开启 Auth.js `trustHost`
- [x] 不改变现有角色鉴权与登录逻辑
- [x] 增加 focused 回归测试
- [x] 返回 quick 任务目录、改动文件、验证结果与剩余风险
</success_criteria>

<output>
After completion, create `.planning/quick/260517-k2u-auth-localhost-untrustedhost/260517-k2u-SUMMARY.md`
</output>
