---
quick_id: 260511-bn1
title: /teacher page-header 铺满父容器并移除冗余 header 包裹
status: planned
created_at: 2026-05-11
---

# Quick Task 260511-bn1 Plan

## Goal

让 `/teacher` 页面 `data-region="page-header"` 对应的首屏头部左右都铺满父容器宽度，
删除壳层里多余的 `page-header` 包裹 `div/section`，并移除教师页布局里
`"top-nav、left-nav 与 top-nav-secondary-rail 都通过统一 theme-layout-runtime 进入教师端壳层。"`
这段说明文本，同时继续遵守 [Phase 16] 已锁定的统一
`theme-layout-runtime` 教师端壳层约束。

## Tasks

1. **收敛 teacher shell 的 page-header 结构与铺满宽度行为**
   - **Files:** `src/components/shell/teacher-sidebar-shell.tsx`, `src/components/surfaces/stage-hero.tsx`
   - **Action:** 依据 [Phase 16] 统一壳层决策，重构 `TeacherSidebarShellFrame` 的头部区域，移除单独的 `data-region="page-header"` 外层包裹节点，让真实 header 内容直接成为 `main` 内的首个区块；同时校正 `StageHero`/fallback header 的内容宽度约束，确保 `/teacher` 页头部左右边缘与父容器对齐，不再因为额外 wrapper 或局部 `max-w-*` 形成窄版心。不得破坏 `left-nav`、`top-nav`、`top-nav-secondary-rail` 三种 shell mode 的共享 runtime 路径。
   - **Verify:** `pnpm exec vitest --run src/components/shell/teacher-sidebar-shell.test.tsx`
   - **Done:** `/teacher` 头部在共享 shell 下横向铺满父容器，`page-header` 不再由独立 wrapper 节点承载，现有 theme-aware shell 结构仍成立。

2. **移除教师首页硬编码 header 说明文案**
   - **Files:** `src/app/(teacher)/teacher/layout.tsx`
   - **Action:** 删除 `/teacher` layout 里传入的硬编码 `headerDescription` 文案，不再把 “top-nav、left-nav 与 top-nav-secondary-rail 都通过统一 theme-layout-runtime 进入教师端壳层。” 渲染到教师工作台头部；保留标题与动作区，头部说明改回由共享 surface summary/默认描述兜底，而不是在页面层再次插入实现细节文案。
   - **Verify:** `pnpm exec vitest --run src/components/shell/teacher-sidebar-shell.test.tsx`
   - **Done:** `/teacher` 首屏不再显示该实现说明文本，layout 仍通过统一 teacher shell 渲染页面头部。

3. **补齐定向回归测试，锁定 header 全宽与文案删除**
   - **Files:** `src/components/shell/teacher-sidebar-shell.test.tsx`, `src/components/surfaces/teacher-dashboard-surface.test.tsx`
   - **Action:** 更新源码断言测试，覆盖三项约束：`TeacherSidebarShell` 不再保留独立 `page-header` wrapper、`/teacher` layout 不再包含上述硬编码说明字符串、教师工作台首屏头部继续保持 `w-full`/无局部窄版心回归。测试要聚焦这次 header shell 调整，不扩展到其它 route。
   - **Verify:** `pnpm exec vitest --run src/components/shell/teacher-sidebar-shell.test.tsx src/components/surfaces/teacher-dashboard-surface.test.tsx`
   - **Done:** 本次 `/teacher` page-header 结构与宽度约束有自动化测试守护，后续 shell 或 surface 调整若带回 wrapper/说明文案/窄版心会直接失败。
