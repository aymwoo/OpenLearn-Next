---
phase: quick
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "User can see the new OpenLearn-Next branded navigation bar"
    - "User can see the hero messaging and featured course image"
    - "User can see the login form for students and mentors"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "New home page UI matching Stitch design"
  key_links: []
---

<objective>
Refactor the home page (`src/app/page.tsx`) to match the provided Stitch screen design (Stitch Project ID: 5322129002350954765, Screen 1).

Purpose: Establish the initial landing and login splash screen as per the intended clean-tech, tonal aesthetic defined in DESIGN.md and the provided HTML.
Output: Updated `src/app/page.tsx` incorporating the new layout, styling, and static components (navbar, hero, login form).
</objective>

<context>
@.planning/quick/260506-04q-stitch-stitch-project-id-532212900235095/stitch_screen.html
@src/app/page.tsx
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement new home page layout and styles</name>
  <files>src/app/page.tsx</files>
  <action>
    Replace the existing `src/app/page.tsx` content with the structure and Tailwind classes provided in the `.planning/quick/.../stitch_screen.html`.
    - Extract the `<nav>` into the page (or keep it inline for this quick task).
    - Convert `class=` to `className=`.
    - Use `lucide-react` icons (Search, Bell, ShoppingCart) instead of the Google Material Symbols.
    - Replace standard `<a>` with Next.js `<Link>` components if applicable.
    - Add the hero section with the brand messaging and the image placeholder.
    - Add the login card section with the "Student/Mentor" tabs, email/password inputs, and login button.
    - Ensure Tailwind colors match the project's configured design tokens (use `bg-surface`, `text-primary`, etc., falling back to arbitrary values or standard tailwind colors if the exact custom variable from the stitch config is not yet globally defined, though try to align with the provided tailwind config snippet in the HTML).
  </action>
  <verify>
    <automated>grep -c 'bg-surface-container-lowest' src/app/page.tsx || echo "Done"</automated>
  </verify>
  <done>The home page code matches the semantic structure and visual appearance of the stitch screen html.</done>
</task>

</tasks>

<success_criteria>
The `src/app/page.tsx` file has been completely refactored to implement the target UI.
</success_criteria>
