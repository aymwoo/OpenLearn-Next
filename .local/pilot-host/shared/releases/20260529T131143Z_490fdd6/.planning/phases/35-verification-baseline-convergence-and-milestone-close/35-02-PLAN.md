---
phase: 35-verification-baseline-convergence-and-milestone-close
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - scripts/verify-phase35-milestone-close.ts
  - package.json
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
must_haves:
  truths:
    - Phase 35 需要一个单一 canonical gate，把 `verify:phase33`、`verify:phase34`、milestone-scoped lint 与全量 typecheck 收口到同一条 close 证据链。
    - 这个 gate 可以承认 repo-wide lint 仍有历史 backlog，但必须明确区分 active milestone scope 与 out-of-scope baseline noise。
    - 不能为了 close gate 伪造“全仓 lint 通过”；应把剩余 lint errors 解析成显式 out-of-scope 列表。
  artifacts:
    - path: scripts/verify-phase35-milestone-close.ts
      provides: canonical milestone-close verifier and baseline partitioning
    - path: package.json
      provides: verify:phase35 script registration
---

<objective>
建立 Phase 35 的单一验证入口，把 milestone prerequisite、milestone-scoped lint baseline、全量 typecheck 与 honest backlog partition 一次性收口。

Purpose: 没有单一 `verify:phase35`，milestone close 仍会散落在人工命令、口头说明和历史 note 中，无法作为可信 close gate。
Output: canonical phase 35 verifier plus command registration.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: add the canonical phase 35 milestone-close verifier</name>
  <files>scripts/verify-phase35-milestone-close.ts, package.json</files>
  <read_first>package.json; scripts/verify-phase33-auth-data-durability.ts; scripts/verify-phase34-course-membership.ts; .planning/ROADMAP.md; .planning/PROJECT.md</read_first>
  <action>Add `scripts/verify-phase35-milestone-close.ts` and register `verify:phase35` in `package.json`. The script must run `verify:phase33`, `verify:phase34`, a milestone-scoped `eslint` command over the active auth/data/classroom/course-membership/verification files, the full `pnpm typecheck`, and the full `pnpm lint` in capture mode. Parse the full lint output so the verifier fails only if it finds lint `error` lines inside the declared milestone scope; all other remaining repo-wide lint errors must be printed as explicit out-of-scope backlog with file paths. The verifier must also print that `typecheck` is now clean and that runtime or platform expansion remains deferred outside this milestone close.</action>
  <acceptance_criteria>
    - `package.json` contains `verify:phase35`.
    - `scripts/verify-phase35-milestone-close.ts` runs `verify:phase33` and `verify:phase34`.
    - `scripts/verify-phase35-milestone-close.ts` runs both milestone-scoped `eslint` and full `pnpm lint`.
    - `scripts/verify-phase35-milestone-close.ts` distinguishes milestone-scope lint errors from out-of-scope lint backlog by file path.
    - `scripts/verify-phase35-milestone-close.ts` runs `pnpm typecheck`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase35</automated>
  </verify>
  <done>Phase 35 has a single executable close gate instead of a manual milestone checklist.</done>
</task>

</tasks>

<verification>
- `pnpm verify:phase35`
</verification>

<output>
After completion, create `.planning/phases/35-verification-baseline-convergence-and-milestone-close/35-02-SUMMARY.md`
</output>
