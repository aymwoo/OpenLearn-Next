## Deferred Items

- `npm run lint` still fails due to pre-existing out-of-scope errors in unrelated files such as `src/actions/plugin-actions.ts`, `src/actions/resource-actions.ts`, `src/components/learning/classroom-runtime-client.tsx`, multiple DAL files, and root utility scripts. This quick task only modified the home/login flow files, so those existing lint failures were not changed.
