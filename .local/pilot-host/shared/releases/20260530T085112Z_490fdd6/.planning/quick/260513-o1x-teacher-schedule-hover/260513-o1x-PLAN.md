---
phase: quick
plan: 260513-o1x
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/surfaces/teacher-schedule-surface.tsx
  - src/components/surfaces/teacher-schedule-week-grid.tsx
  - src/components/surfaces/teacher-schedule-surface.test.tsx
autonomous: true
requirements:
  - QUICK-teacher-schedule-hover
must_haves:
  truths:
    - "教师在 /teacher/schedule 主课表中先看到班级名，再看到下方课程胶囊，整体密度明显高于当前实现。"
    - "课程卡默认不展示冗余明细，hover 或 focus 时才显示时间、地点、状态、教师等补充信息。"
    - "点击某节课程后会弹出无边框快捷操作面板，并以图标按钮提供相关操作。"
  artifacts:
    - path: "src/components/surfaces/teacher-schedule-surface.tsx"
      provides: "服务端壳层与现有 DTO/批次逻辑保持不变，只把主课表交给交互式周视图组件。"
    - path: "src/components/surfaces/teacher-schedule-week-grid.tsx"
      provides: "紧凑课程卡、hover 明细层、选中态快捷操作面板。"
    - path: "src/components/surfaces/teacher-schedule-surface.test.tsx"
      provides: "紧凑布局与交互回归测试。"
  key_links:
    - from: "src/components/surfaces/teacher-schedule-surface.tsx"
      to: "src/components/surfaces/teacher-schedule-week-grid.tsx"
      via: "传入 TeacherWeeklyScheduleDTO 与 viewMode 数据"
      pattern: "<TeacherScheduleWeekGrid"
    - from: "src/components/surfaces/teacher-schedule-week-grid.tsx"
      to: "快捷操作路由"
      via: "选中课程后的图标按钮 Link"
      pattern: "href=\"/teacher/schedule/changes\"|href=\"/teacher/schedule/reminders\"|href=\"/teacher/classes\""
---

<objective>
重构 `/teacher/schedule` 的主课表视图，让课表回到“班级优先、课程次要、明细按需浮出”的紧凑操作面。

Purpose: 降低课表信息噪音，让教师在同屏看到更多课次，并把调课/提醒等高频操作收敛到选中课程后的快捷面板。
Output: 一个新的交互式周课表组件、更新后的 surface 接线，以及覆盖 hover/选中态的回归测试。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@src/components/surfaces/teacher-schedule-surface.tsx
@src/components/surfaces/teacher-schedule-surface.test.tsx
@src/features/schedule/shared/dto/runtime.ts

<interfaces>
From src/features/schedule/shared/dto/runtime.ts:
```ts
export type TeacherWeeklyScheduleCellDTO = {
  id: string;
  classLabel: string;
  teacherLabel: string | null;
  locationLabel: string;
  courseTitle: string;
  status: "正常" | "进行中" | "即将开始" | "已变更" | "代课" | "停课";
  overrideSummary: string | null;
  timeLabel: string;
};
```

From src/components/surfaces/teacher-schedule-surface.tsx:
```tsx
export function TeacherScheduleSurface({
  data,
  latestImportBatch,
  importBatches,
  currentTeacherName,
}: { ... })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 抽离交互式主课表组件并保留现有服务端数据链路</name>
  <files>src/components/surfaces/teacher-schedule-surface.tsx, src/components/surfaces/teacher-schedule-week-grid.tsx</files>
  <action>保持 `TeacherScheduleSurface` 现有批次判定、主课表来源、空态与历史学期区逻辑不变，只把“主课表”网格抽到新的 client 组件 `teacher-schedule-week-grid.tsx`。新组件继续消费现有 `TeacherWeeklyScheduleDTO`/`viewMode`，不要新增数据库读取、不要改 DAL、不要引入新的 UI 库。课程卡默认第一视觉只展示班级名称；课程名改成下方无边框胶囊按钮；时间、地点、状态、教师、overrideSummary 收进 hover/focus 浮层，保证键盘 focus 也能触发，不做仅鼠标可见的实现。</action>
  <verify>
    <automated>pnpm vitest run "src/components/surfaces/teacher-schedule-surface.test.tsx"</automated>
  </verify>
  <done>主课表已经由独立交互组件承载；服务端 surface 仍负责数据装配；课程卡视觉层次变为“班级主标题 + 下方课程胶囊 + 按需浮出明细”。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现选中课程快捷操作面板与紧凑交互回归</name>
  <files>src/components/surfaces/teacher-schedule-week-grid.tsx, src/components/surfaces/teacher-schedule-surface.test.tsx</files>
  <behavior>
    - Test 1: 课程卡默认优先显示班级名，课程名以胶囊按钮形式单独出现，地点/时间等明细默认不常驻。
    - Test 2: hover 或 focus 某节课程时，会出现包含时间、地点、状态、教师/变更说明的浮层。
    - Test 3: 点击课程胶囊后，弹出无边框快捷操作面板，至少提供“调课管理 / 提醒配置 / 查看班级”三个图标按钮，并带 `aria-label`。
  </behavior>
  <action>在新周课表组件中加入本地选中状态，点击课程胶囊后打开 anchored 快捷面板；按钮继续复用现有教师端路由（`/teacher/schedule/changes`、`/teacher/schedule/reminders`、`/teacher/classes`），不要新增 action、不要伪造未落地功能。面板与浮层统一采用 tonal / shadow 语言，禁止 1px divider、禁止显式边框。测试覆盖紧凑文案层次、hover/focus 浮层与选中面板图标按钮，避免只断言字符串存在。</action>
  <verify>
    <automated>pnpm vitest run "src/components/surfaces/teacher-schedule-surface.test.tsx"</automated>
  </verify>
  <done>选中课程后会出现可访问的快捷面板，图标按钮能跳到现有相关页面；测试能稳定证明 hover/focus/selected 三类状态存在且符合紧凑视图目标。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 教师点击/悬停 → 课程交互组件 | 非持久化 UI 输入切换 hover/selected 状态 |
| 快捷操作按钮 → 教师端已有路由 | 用户从主课表跳转到调课、提醒、班级等后续页面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-o1x-01 | T | teacher-schedule-week-grid.tsx | mitigate | 快捷操作只链接到既有教师路由，不新增写入入口，不在前端拼接任意外部 URL。 |
| T-quick-o1x-02 | I | hover 明细浮层 | mitigate | 浮层只显示当前 cell DTO 已有字段，不额外泄漏隐藏数据；管理员/教师视角仍沿用现有 DTO。 |
| T-quick-o1x-03 | D | 选中态快捷面板 | accept | 本地选中状态仅影响当前页面展示，属于低风险 UI 状态；通过点击切换和可访问标签降低误操作。 |
</threat_model>

<verification>
- `pnpm vitest run "src/components/surfaces/teacher-schedule-surface.test.tsx"`
</verification>

<success_criteria>
- 主课表单元默认只突出班级名与课程胶囊，视觉密度高于当前 stacked card 版本。
- hover/focus 才展示详细元信息，且键盘用户可触达。
- 点击课程后出现无边框快捷操作面板，图标按钮具备清晰 `aria-label` 并连接既有页面。
</success_criteria>

<output>
After completion, create `.planning/quick/260513-o1x-teacher-schedule-hover/260513-o1x-SUMMARY.md`
</output>
