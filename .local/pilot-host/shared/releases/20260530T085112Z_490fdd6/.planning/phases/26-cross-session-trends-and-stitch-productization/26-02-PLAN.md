---
phase: 26-cross-session-trends-and-stitch-productization
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - src/lib/theme-layout/route-surface-registry.ts
  - src/lib/theme-layout/shell-surface-resolver.test.ts
  - src/lib/navigation.ts
  - src/components/shell/teacher-sidebar-shell.tsx
  - src/components/shell/teacher-sidebar-shell.test.tsx
  - src/app/(teacher)/teacher/trends/page.tsx
  - src/app/(teacher)/teacher/trends/page.test.tsx
  - src/components/surfaces/teacher-trends-surface.tsx
  - src/components/surfaces/teacher-trends-surface.test.tsx
autonomous: true
requirements:
  - ANALYTICS-02
  - UI-05
must_haves:
  truths:
    - 教师能从既有 teacher shell 和主导航直接进入独立 `/teacher/trends` 页面，不复活 `/teacher/reports` 之类的孤立老路由。
    - `/teacher/trends` 首屏默认呈现 class-first recent-session view，并先在页内展开异常详情，再提供回到 `/classroom` 的主 CTA。
    - trends 页面继续属于既有 teacher shell 与 Stitch 骨架，保持 mobile 单列优先、desktop 稳定分栏与无横向滚动的 analytics cards。
  artifacts:
    - path: src/lib/theme-layout/route-surface-registry.ts
      provides: shell metadata contract for `/teacher/trends`
      contains: /teacher/trends
    - path: src/app/(teacher)/teacher/trends/page.tsx
      provides: first-class trends route server entry
      contains: getTeacherRecentSessionTrendDTO
    - path: src/components/surfaces/teacher-trends-surface.tsx
      provides: class-first trend UI with inline detail and classroom-first CTAs
      contains: /classroom?sessionId=
  key_links:
    - from: src/lib/theme-layout/route-surface-registry.ts
      to: src/app/(teacher)/teacher/trends/page.tsx
      via: route metadata must resolve the trends page through the teacher shell contract
      pattern: /teacher/trends
    - from: src/components/surfaces/teacher-trends-surface.tsx
      to: /classroom
      via: primary CTA on inline detail uses `primaryRecapHref` instead of a client-invented jump
      pattern: /classroom\?sessionId=
---

<objective>
把 Phase 26 的 trends 交付成独立教师入口，先完成主导航、shell metadata、
class-first trends route 与页内 drill-down 主链。

Purpose: 先把 `/teacher/trends` 作为稳定 teacher route 立起来，让 ANALYTICS-02 有真实入口，
并保持 `/classroom` 作为下一跳主域。
Output: `/teacher/trends` route, teacher shell registration, navigation entry, and trends surface with inline detail.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-cross-session-trends-and-stitch-productization/26-CONTEXT.md
@.planning/phases/26-cross-session-trends-and-stitch-productization/26-RESEARCH.md
@.planning/phases/26-cross-session-trends-and-stitch-productization/26-UI-SPEC.md
@.planning/phases/25-teaching-data-capture-and-session-analytics/25-02-SUMMARY.md
@src/lib/theme-layout/route-surface-registry.ts
@src/lib/navigation.ts
@src/components/shell/teacher-sidebar-shell.tsx
@src/app/(classroom)/classroom/page.tsx
</context>

<interfaces>
Current route shell contract from `src/lib/theme-layout/route-surface-registry.ts`:
```ts
export const TEACHER_THEME_ROUTE_KEYS = [
  "/teacher",
  "/teacher/review",
  "/teacher/launch",
  "/teacher/editor",
  "/settings",
  "/help",
] as const;

export type TeacherThemeRouteSurface = {
  label: string;
  defaultSplit: "30/70" | "40/60" | "50/50" | "60/40";
  shell: { mode: "left-nav" | "top-nav" | "top-nav-secondary-rail"; radius: "rounded" | "square"; width: "default" | "full-width"; chrome: "default" | "immersive" | "minimal" | "presentation" | "fullscreen" | "focus"; };
};
```

Current teacher navigation sources:
```ts
export const teacherNavigationItems = [
  { label: "工作台", href: "/teacher" },
  { label: "课时编辑", href: "/teacher/editor" },
  { label: "课堂运行", href: "/classroom" },
  { label: "资源中心", href: "/resources" },
];
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Register `/teacher/trends` as a first-class teacher shell route and navigation entry</name>
  <files>src/lib/theme-layout/route-surface-registry.ts, src/lib/theme-layout/shell-surface-resolver.test.ts, src/lib/navigation.ts, src/components/shell/teacher-sidebar-shell.tsx, src/components/shell/teacher-sidebar-shell.test.tsx</files>
  <read_first>src/lib/theme-layout/route-surface-registry.ts, src/lib/theme-layout/shell-surface-resolver.test.ts, src/lib/navigation.ts, src/components/shell/teacher-sidebar-shell.tsx, src/components/shell/teacher-sidebar-shell.test.tsx, .planning/phases/26-cross-session-trends-and-stitch-productization/26-CONTEXT.md, .planning/phases/26-cross-session-trends-and-stitch-productization/26-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - `src/lib/theme-layout/route-surface-registry.ts` contains `/teacher/trends` in `TEACHER_THEME_ROUTE_KEYS`, a corresponding surface entry, and a resolver branch for pathname prefixes.
    - the `/teacher/trends` shell config is `left-nav`, `rounded`, `default`, `default` per UI-SPEC.
    - `src/lib/navigation.ts` and `src/components/shell/teacher-sidebar-shell.tsx` expose a visible trends entry and no longer point teachers to `/teacher/reports` as the analytics primary route.
    - `src/lib/theme-layout/shell-surface-resolver.test.ts` contains `/teacher/trends` route resolution assertions.
    - `src/components/shell/teacher-sidebar-shell.test.tsx` asserts a visible Simplified Chinese trends entry.
    - navigation labels use Simplified Chinese and describe trends as a teacher-facing product entry.
  </acceptance_criteria>
  <action>Update `src/lib/theme-layout/route-surface-registry.ts` to add a new module key for trends, register `/teacher/trends`, and resolve any `/teacher/trends` pathname to that route key with shell `{ mode: "left-nav", radius: "rounded", width: "default", chrome: "default" }` per D-01/D-02/D-13/D-18. In `src/lib/navigation.ts` and `src/components/shell/teacher-sidebar-shell.tsx`, add a visible Chinese navigation item labeled `班级趋势` targeting `/teacher/trends`, and remove or replace the dead `/teacher/reports` primary analytics entry so the trends route becomes the first-class teacher navigation destination. Update `src/lib/theme-layout/shell-surface-resolver.test.ts` and `src/components/shell/teacher-sidebar-shell.test.tsx` so they explicitly prove `/teacher/trends` resolves through the existing teacher shell and appears in the visible sidebar navigation.</action>
  <verify>
    <automated>pnpm test --run src/lib/theme-layout/shell-surface-resolver.test.ts src/components/shell/teacher-sidebar-shell.test.tsx</automated>
  </verify>
  <done>Teachers can reach trends from the existing shell/navigation system without adding a parallel analytics layout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the trends page with inline anomaly detail and classroom-first next actions</name>
  <files>src/app/(teacher)/teacher/trends/page.tsx, src/app/(teacher)/teacher/trends/page.test.tsx, src/components/surfaces/teacher-trends-surface.tsx, src/components/surfaces/teacher-trends-surface.test.tsx</files>
  <read_first>src/app/(classroom)/classroom/page.tsx, src/components/surfaces/teacher-dashboard-surface.tsx, src/components/surfaces/classroom-console-surface.tsx, .planning/phases/26-cross-session-trends-and-stitch-productization/26-CONTEXT.md, .planning/phases/26-cross-session-trends-and-stitch-productization/26-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: `/teacher/trends` defaults to class-first recent-session view and renders all available sessions when fewer than 4 ended sessions exist.
    - Test 2: selecting an anomaly opens inline detail on the trends page first, showing session summary, impacted students, and key signals per D-09/D-10.
    - Test 3: the inline detail primary CTA targets `/classroom?sessionId=...&recapTab=students`, while `/teacher/review` remains a secondary follow-up only when feedback work exists.
  </behavior>
  <action>Create `src/app/(teacher)/teacher/trends/page.tsx` as the server route for the new trends entry. Read `searchParams` for `classId`, optional `lessonId`, optional `studentId`, optional `sessionId`, optional `view`, and optional `limit`, defaulting to the class-first latest-4 sessions response from `getTeacherRecentSessionTrendDTO()`. Build `src/components/surfaces/teacher-trends-surface.tsx` to use the existing teacher product skeleton: one hero, compact summary metrics laid out as `grid gap-4 md:grid-cols-2`, a class-first recent-session comparison area, a secondary student focus list, and an inline detail area using a stable desktop split `xl:grid-cols-[minmax(0,1fr)_minmax(280px,0.4fr)]`. Keep the mobile posture single-column first, and do not introduce `overflow-x-auto` on analytics cards or detail panels. The inline detail must render the selected session summary, affected students, key signals, a primary CTA using the DAL-provided `/classroom?sessionId={id}&recapTab=students` href, and only an optional secondary `/teacher/review?...` CTA when the trend detail exposes pending feedback follow-up. Add focused route and surface tests for default class-first posture, inline expansion, responsive classes, and CTA hierarchy.</action>
  <acceptance_criteria>
    - `src/app/(teacher)/teacher/trends/page.tsx` exists and calls `getTeacherRecentSessionTrendDTO`.
    - `src/components/surfaces/teacher-trends-surface.tsx` contains inline detail content for session summary, impacted students, and key signals before navigation actions.
    - the primary CTA in `teacher-trends-surface.tsx` targets `/classroom?sessionId=` and not `/teacher/review`.
    - `src/components/surfaces/teacher-trends-surface.tsx` contains `grid gap-4 md:grid-cols-2`, contains `xl:grid-cols-[minmax(0,1fr)_minmax(280px,0.4fr)]`, and does not contain `overflow-x-auto`.
    - `src/app/(teacher)/teacher/trends/page.test.tsx` and `src/components/surfaces/teacher-trends-surface.test.tsx` cover class-first default behavior, inline drill-down, and CTA hierarchy.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/app/(teacher)/teacher/trends/page.test.tsx src/components/surfaces/teacher-trends-surface.test.tsx</automated>
  </verify>
  <done>The trends route is a first-class teacher entry: teachers can enter from the shared shell, understand anomalies inline, and return to `/classroom` as the primary next action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teacher shell navigation -> trends route | new route must inherit existing teacher shell metadata and not bypass the route registry |
| trend detail UI -> follow-up routes | UI must preserve single-session classroom context and not escalate review to the default path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | T | route metadata and shell registration | mitigate | Add `/teacher/trends` only through `route-surface-registry.ts` and update resolver/navigation sources together. |
| T-26-05 | I | inline detail CTA hierarchy | mitigate | Render classroom recap as the only primary CTA and gate review links as secondary follow-up only. |
| T-26-06 | E | dead analytics route drift | mitigate | Replace `/teacher/reports` primary analytics navigation with `/teacher/trends` and cover it in tests/static guards later. |
</threat_model>

<verification>
- `pnpm test --run src/lib/theme-layout/shell-surface-resolver.test.ts src/components/shell/teacher-sidebar-shell.test.tsx src/app/(teacher)/teacher/trends/page.test.tsx src/components/surfaces/teacher-trends-surface.test.tsx`
</verification>

<success_criteria>
- `/teacher/trends` is reachable from teacher navigation and registered in the teacher shell metadata system.
- Trends interaction stays inline-first and classroom-first.
- The route uses the existing Stitch-aligned teacher shell rather than a parallel analytics framework.
</success_criteria>

<output>
After completion, create `.planning/phases/26-cross-session-trends-and-stitch-productization/26-02-SUMMARY.md`
</output>
