---
phase: 40-bullmq-infra-seam-and-worker-reliability-posture
plan: 02
type: execute
wave: 2
depends_on:
  - 40-01
files_modified:
  - src/db/schema.ts
  - drizzle/0005_phase40_async_task_runtime_projection.sql
  - drizzle/meta/_journal.json
  - drizzle/meta/0005_snapshot.json
  - src/features/async-tasks/infra/queue-events.ts
  - src/features/async-tasks/worker/bootstrap.ts
  - src/features/async-tasks/server/enqueue.ts
  - src/lib/dal/async-tasks.ts
  - src/lib/dal/async-tasks.test.ts
autonomous: true
requirements:
  - ATP-05
  - ATP-08
  - ATP-09
must_haves:
  truths:
    - Queue runtime events are projected back into SQLite durable task truth rather than exposed directly from BullMQ state.
    - Worker restart, stalled jobs, and shutdown leave inspectable recovery posture instead of silently losing execution state.
    - Failure reasons and attempt history are durably inspectable for later operator surfaces.
  artifacts:
    - path: src/features/async-tasks/infra/queue-events.ts
      provides: QueueEvents projector and runtime-to-ledger mapping
    - path: src/db/schema.ts
      provides: Additional runtime projection fields or attempt history persistence
    - path: src/lib/dal/async-tasks.ts
      provides: Durable DTO reads that include runtime history and recovery posture
  key_links:
    - from: src/features/async-tasks/infra/queue-events.ts
      to: src/db/schema.ts
      via: projector writes runtime events into durable task tables
      pattern: asyncTask
    - from: src/features/async-tasks/worker/bootstrap.ts
      to: src/features/async-tasks/infra/queue-events.ts
      via: worker lifecycle initializes and closes runtime projector
      pattern: QueueEvents
---

<objective>
把 BullMQ runtime 信号投影成 durable truth：新增 QueueEvents projector、attempt/failure/recovery history、以及 graceful shutdown/stalled honesty。

Purpose: 让 queued、running、retrying、completed、failed、stalled 等运行态成为应用内 inspectable 事实，而不是只存在于 Redis/BullMQ 内部。
Output: runtime projection layer、必要 schema/migration、durable history read model、focused recovery tests。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-bullmq-infra-seam-and-worker-reliability-posture/40-CONTEXT.md
@.planning/phases/40-bullmq-infra-seam-and-worker-reliability-posture/40-RESEARCH.md
@.planning/phases/40-bullmq-infra-seam-and-worker-reliability-posture/40-PATTERNS.md
@.planning/phases/40-bullmq-infra-seam-and-worker-reliability-posture/40-01-PLAN.md

<interfaces>
From Context7 BullMQ docs:
```ts
queueEvents.on('completed', ({ jobId, returnvalue }) => { ... });
queueEvents.on('failed', ({ jobId, failedReason }) => { ... });
queueEvents.on('progress', ({ jobId, data }) => { ... });
queueEvents.on('stalled', ({ jobId }) => { ... });
await worker.close();
```

From src/features/runtime-platform/seams/transport/redis-fanout-manager.ts:
```ts
connections.subscriber.on("ready", async () => {
  this.subscriberReady = true;
  this.subscribedTopics.clear();
  await this.restoreDesiredSubscriptions();
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 QueueEvents projector 与 durable runtime projection</name>
  <files>src/features/async-tasks/infra/queue-events.ts, src/features/async-tasks/worker/bootstrap.ts, src/features/async-tasks/server/enqueue.ts, src/db/schema.ts, drizzle/0005_phase40_async_task_runtime_projection.sql, drizzle/meta/_journal.json, drizzle/meta/0005_snapshot.json</files>
  <read_first>src/features/async-tasks/server/enqueue.ts, src/db/schema.ts, src/features/runtime-platform/seams/transport/redis-fanout-manager.ts, src/features/async-tasks/infra/bullmq.ts, .planning/phases/40-bullmq-infra-seam-and-worker-reliability-posture/40-RESEARCH.md</read_first>
  <behavior>
    - Test 1: when queue dispatch succeeds, durable task row records stable `queueJobId` and transitions into queued posture.
    - Test 2: QueueEvents projector maps waiting/active/progress/completed/failed/stalled signals into latest snapshot plus append-only event history.
    - Test 3: projector writes honest stalled or recovery posture instead of erasing intermediate failure context.
  </behavior>
  <action>创建 `src/features/async-tasks/infra/queue-events.ts`，集中监听 BullMQ `QueueEvents` 并把 `waiting`、`active`、`progress`、`completed`、`failed`、`stalled`、必要时 `deduplicated` 映射到 SQLite durable truth。按最小正确改动原则更新 `src/db/schema.ts`：若现有 `asyncTask` / `asyncTaskEvent` 字段不足以表达 attempts、failure reason、recovery posture 或 latest runtime metadata，则新增最少字段或最小补充表，并写 `0005` migration 与 metadata。同步更新 `src/features/async-tasks/server/enqueue.ts`，让真实 BullMQ dispatch 成功后写回 `queueJobId` 与 queued/dispatched posture；若 dispatch 失败仍保持 `dispatch_failed` truth。将 projector 接入 worker bootstrap 的生命周期中，但保持 projector 本身可独立测试。先写 focused tests，再实现直到通过。</action>
  <acceptance_criteria>
    - `src/features/async-tasks/infra/queue-events.ts` listens to BullMQ runtime events and persists durable updates.
    - Successful dispatch writes `queueJobId` back into `asyncTasks`.
    - Schema/migration changes exist if needed to represent attempt or recovery posture.
    - Runtime state projection still uses SQLite as the source of truth for DAL consumers.
  </acceptance_criteria>
  <verify>
    <automated>pnpm db:migrate && pnpm test --run src/features/async-tasks/infra/queue-events.test.ts src/features/async-tasks/server/enqueue.runtime.test.ts</automated>
  </verify>
  <done>runtime events 已被 durable 化，ATP-05 的闭环不再停留在理论层。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 补 attempt/failure history、graceful shutdown 与 DAL 读取面</name>
  <files>src/features/async-tasks/worker/bootstrap.ts, src/lib/dal/async-tasks.ts, src/lib/dal/async-tasks.test.ts, src/db/schema.ts, src/features/async-tasks/infra/queue-events.ts</files>
  <read_first>src/lib/dal/async-tasks.ts, src/features/async-tasks/worker/bootstrap.ts, src/features/async-tasks/infra/queue-events.ts, .planning/research/PITFALLS.md</read_first>
  <behavior>
    - Test 1: graceful shutdown closes worker and QueueEvents handles and leaves a durable recovery trail.
    - Test 2: DAL detail DTO includes failure reason, attempt or retry history, and recovery posture sourced from SQLite.
    - Test 3: stalled or restart recovery results in inspectable durable events rather than disappearing state.
  </behavior>
  <action>在 `src/features/async-tasks/worker/bootstrap.ts` 中加入显式 shutdown orchestration：至少处理 `SIGTERM` / `SIGINT`，调用 `worker.close()` 和 `queueEvents.close()`，并在必要时把 shutdown/recovery posture 记录进 durable event history。扩展 `src/lib/dal/async-tasks.ts` 与测试，使 task detail DTO 能读出 failure reason、attempt history、latest recovery posture，而这些信息必须只来自 SQLite。若需要，在 `queue-events.ts` 中固定 attempt-event payload schema 或 recovery event vocabulary。重点证明 ATP-08/09：worker 关闭、重启、stalled recovery 都是可 inspect 的应用事实，而不是只能查 BullMQ 内部状态。</action>
  <acceptance_criteria>
    - Worker bootstrap source contains explicit graceful shutdown handling.
    - DAL detail read returns durable failure and recovery context.
    - Focused tests cover stalled/recovery or shutdown projection paths.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/features/async-tasks/worker/bootstrap.test.ts src/lib/dal/async-tasks.test.ts && pnpm typecheck</automated>
  </verify>
  <done>failure、attempt、recovery posture 已能被后续 operator surface 直接消费，无需再对账 Redis runtime state。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BullMQ runtime events → SQLite projection | runtime signal 必须被正确映射成 durable truth，不能丢状态或伪装成功 |
| worker shutdown/restart → durable history | 进程生命周期变化必须留下 inspectable posture，避免 silent loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04 | R | `src/features/async-tasks/infra/queue-events.ts` | mitigate | 通过 append-only durable event history 记录 runtime transitions 和 recovery posture |
| T-40-05 | D | `src/features/async-tasks/worker/bootstrap.ts` | mitigate | graceful shutdown + stalled honesty，避免 silent drop 或 orphaned running state |
| T-40-06 | I | `src/lib/dal/async-tasks.ts` | mitigate | DAL 只从 SQLite 读取 failure/attempt/recovery context，防止直接暴露 BullMQ internals |
</threat_model>

<verification>
- `pnpm db:migrate`
- `pnpm test --run src/features/async-tasks/infra/queue-events.test.ts src/features/async-tasks/server/enqueue.runtime.test.ts`
- `pnpm test --run src/features/async-tasks/worker/bootstrap.test.ts src/lib/dal/async-tasks.test.ts`
- `pnpm typecheck`
</verification>

<success_criteria>
- queued/running/completed/failed/stalled 等运行态已经被 durable 化。
- graceful shutdown 与 recovery posture 有代码和测试证明。
- DAL 可以只读 SQLite 就得到 failure/attempt/recovery 详情。
</success_criteria>

<output>
After completion, create `.planning/phases/40-bullmq-infra-seam-and-worker-reliability-posture/40-02-SUMMARY.md`
</output>
