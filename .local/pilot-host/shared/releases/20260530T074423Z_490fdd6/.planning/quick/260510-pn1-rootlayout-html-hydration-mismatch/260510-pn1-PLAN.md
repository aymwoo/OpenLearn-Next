---
phase: quick
plan: 260510-pn1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/layout.test.tsx
autonomous: true
requirements:
  - QUICK-rootlayout-html-hydration-mismatch
must_haves:
  truths:
    - 浏览器扩展向根 `<html>` 注入额外属性时，应用不再因为该节点产生 hydration mismatch 噪音。
    - RootLayout 仍继续输出既有 `lang="zh-CN"`、Lexend body class 和 `ThemeInjector` 注入路径。
    - 仓库存在一个 focused regression test，后续不会把根节点 hydration guard 再删掉。
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "根 html 节点 hydration mismatch guard"
    - path: "src/app/layout.test.tsx"
      provides: "RootLayout 根节点回归测试"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/layout.test.tsx"
      via: "rendered html/body contract assertions"
      pattern: "suppressHydrationWarning|lang=\"zh-CN\"|ThemeInjector"
---

<objective>
给 RootLayout 的根 `<html>` 节点补上最小范围的 hydration guard，忽略浏览器扩展注入属性导致的 mismatch，同时保持现有字体、主题注入与页面壳层不变。

Purpose: 这个问题属于宿主环境噪音，不是应用真实数据不一致；修复点必须收敛在根节点，避免把真正的 hydration 问题大面积静默掉。
Output: 一个只作用于根 `<html>` 的 guard，以及一个覆盖 RootLayout contract 的回归测试。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@src/app/layout.tsx

<interfaces>
From src/app/layout.tsx:
```tsx
export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="zh-CN">
      <body className={lexend.variable}>
        <Suspense fallback={null}>
          <ThemeInjector />
        </Suspense>
        {children}
      </body>
    </html>
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 只在根 html 节点收口 hydration mismatch guard</name>
  <files>src/app/layout.tsx, src/app/layout.test.tsx</files>
  <behavior>
    - Test 1: `RootLayout` 渲染出的根 `<html>` 节点带有 `suppressHydrationWarning`，用于忽略浏览器扩展注入属性造成的 hydration mismatch。
    - Test 2: `lang="zh-CN"`、`body` 上的 Lexend variable class、`ThemeInjector` 的 Suspense 包裹关系继续保留，没有把 guard 扩散到 `body` 或整棵子树。
  </behavior>
  <action>先新增 `src/app/layout.test.tsx`，直接渲染 `RootLayout` 并断言根 `<html>` contract；随后在 `src/app/layout.tsx` 上只给 `<html>` 增加 `suppressHydrationWarning`。保持修复最小化：不要改动 metadata、不要移动 `ThemeInjector`、不要把 `suppressHydrationWarning` 加到 `body` 或其它业务节点，因为那会掩盖真实应用 hydration 缺陷。若测试需要 mock `ThemeInjector`，沿用仓库现有 Vitest/RTL 写法，保证断言聚焦在 root layout contract 而不是实现细节。</action>
  <verify>
    <automated>pnpm vitest run src/app/layout.test.tsx</automated>
  </verify>
  <done>RootLayout 只在根 `<html>` 节点忽略扩展注入属性导致的 mismatch；其余现有布局、字体和主题注入行为保持不变，并有回归测试守护。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser extension DOM mutation → React root hydration | 非应用代码会在根 `<html>` 注入属性，触发宿主环境噪音 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | T | `src/app/layout.tsx` | mitigate | 仅在根 `<html>` 使用 `suppressHydrationWarning`，限制噪音抑制范围，避免真实业务节点 mismatch 被静默 |
| T-quick-02 | I | `src/app/layout.test.tsx` | mitigate | 用 focused regression test 固定 `lang`、font class、Suspense + `ThemeInjector` contract，防止修复时顺手破坏壳层基础结构 |
</threat_model>

<verification>
- `pnpm vitest run src/app/layout.test.tsx`
</verification>

<success_criteria>
- [ ] 浏览器扩展注入根 `<html>` 属性时，不再出现该节点的 hydration mismatch 噪音
- [ ] `RootLayout` 继续保留 `lang="zh-CN"`、Lexend body class 和 `ThemeInjector` 注入路径
- [ ] 回归测试能稳定证明 guard 只作用于根 `<html>` 节点
</success_criteria>

<output>
After completion, create `.planning/quick/260510-pn1-rootlayout-html-hydration-mismatch/260510-pn1-SUMMARY.md`
</output>
