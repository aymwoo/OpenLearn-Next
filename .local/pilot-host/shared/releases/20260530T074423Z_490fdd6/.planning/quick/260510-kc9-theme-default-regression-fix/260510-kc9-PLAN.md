---
phase: quick
plan: 260510-kc9
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/theme/theme-injector.tsx
  - src/components/theme/theme-injector.test.tsx
  - src/components/shell/teacher-sidebar-shell.tsx
  - src/components/shell/student-shell.tsx
  - src/components/surfaces/surface-widths.ts
  - src/components/surfaces/home-surface.tsx
  - src/components/surfaces/settings-surface.tsx
  - src/components/surfaces/settings-surface.test.tsx
  - src/components/surfaces/plugin-marketplace-surface.tsx
  - src/components/surfaces/students-management-surface.tsx
  - scripts/verify-theme-default-regression.ts
  - package.json
autonomous: true
requirements:
  - QUICK-theme-default-regression-fix
must_haves:
  truths:
    - 默认主题在没有 `activeThemeId` 时恢复稳定的基线界面，不再出现深浅层级混杂。
    - 首页与相关 surface 的文案/版心不再被回归的局部 `max-width` 限制压窄。
    - 主题插件仍只能通过既有 `activeThemeId -> DAL -> ThemeInjector -> shell` 路径影响布局与外观。
    - 仓库存在一个可重复执行的快速回归入口，能证明默认主题 fallback 与宽度 contract 未再次退化。
  artifacts:
    - path: "src/components/theme/theme-injector.tsx"
      provides: "默认主题与插件主题共用的 runtime fallback truth source"
    - path: "src/components/surfaces/surface-widths.ts"
      provides: "跨首页/设置/学生/插件等页面共享的版心宽度 contract"
    - path: "scripts/verify-theme-default-regression.ts"
      provides: "默认主题回归验证入口"
  key_links:
    - from: "src/components/theme/theme-injector.tsx"
      to: "src/components/shell/teacher-sidebar-shell.tsx"
      via: "default layout runtime + active theme runtime"
      pattern: "DEFAULT_THEME_LAYOUT_RUNTIME|data-theme-layout-source"
    - from: "src/components/surfaces/surface-widths.ts"
      to: "src/components/surfaces/home-surface.tsx"
      via: "shared shell/content width tokens"
      pattern: "surfaceWidths|contentWidth|shellWidth"
    - from: "scripts/verify-theme-default-regression.ts"
      to: "package.json"
      via: "pnpm verify:theme-default-regression"
      pattern: "verify:theme-default-regression"
---

<objective>
定位并修复主题插件改动后污染默认主题的 runtime fallback，同时把首页与相关页面重新收紧到统一的宽度 contract，避免默认主题再次出现深浅混乱和不必要的窄段落。

Purpose: 默认主题是所有未启用主题插件学校的基线；一旦 fallback 被插件态布局或局部 hardcoded 宽度污染，首页、设置页、学生端等多个 surface 都会同时退化。
Output: 一个稳定的 default-theme runtime path、一个共享 surface width contract，以及一个 focused 的默认主题回归验证入口。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@.planning/phases/16-theme-plugins-and-layout-orchestration/16-04-SUMMARY.md
@.planning/quick/260510-bxx-unify-theme-shells/260510-bxx-SUMMARY.md
@.planning/quick/260507-r7m-teacher-copy-width/260507-r7m-SUMMARY.md
@.planning/quick/260507-rto-max-width/260507-rto-SUMMARY.md
@src/components/theme/theme-injector.tsx
@src/components/shell/teacher-sidebar-shell.tsx
@src/components/shell/student-shell.tsx
@src/components/surfaces/settings-surface.tsx
@src/components/surfaces/home-surface.tsx
@src/components/surfaces/teacher-surface-rhythm.ts

<interfaces>
From src/components/theme/theme-injector.tsx:
```typescript
const layoutRuntime = themeRuntime?.layoutRuntime ?? DEFAULT_THEME_LAYOUT_RUNTIME;
<meta
  id="theme-layout-runtime"
  data-theme-layout-runtime={JSON.stringify(layoutRuntime)}
  data-theme-layout-source={themeRuntime ? "active-theme" : "default"}
/>
```

From src/components/shell/teacher-sidebar-shell.tsx:
```typescript
const surface = layoutRuntime.pages[routeKey] ?? layoutRuntime.defaultSurface;
const shellMode = ALLOWLISTED_SHELL_MODES.includes(surface.shellMode)
  ? surface.shellMode
  : "left-nav";
```

From src/components/surfaces/teacher-surface-rhythm.ts:
```typescript
export const teacherSurfaceRhythm = {
  hero: "rounded-[var(--radius-shell)] bg-surface-container-low p-6 shadow-ambient sm:p-8",
  section: "rounded-[var(--radius-shell)] bg-surface-container-low p-5 shadow-ambient sm:p-6",
  card: "rounded-[var(--radius-card)]",
  cardInset: "rounded-[var(--radius-card)] bg-surface-container-lowest shadow-ambient",
} as const;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 收口默认主题 fallback，阻断插件态壳层污染</name>
  <files>src/components/theme/theme-injector.tsx, src/components/theme/theme-injector.test.tsx, src/components/shell/teacher-sidebar-shell.tsx, src/components/shell/student-shell.tsx</files>
  <behavior>
    - Test 1: 无 `activeThemeId` 时，`ThemeInjector` 明确输出 `data-theme-layout-source="default"`，teacher/settings/student 壳层使用系统默认 layout/tokens，不再出现插件态深色/浅色层级混杂。
    - Test 2: 有效主题插件启用后，现有 allowlisted `shellMode`、layout runtime 与 CSS variables 仍通过既有 runtime path 生效，不被这次默认主题修复破坏。
  </behavior>
  <action>先定位 root cause：核对 `themeRuntime?.layoutRuntime ?? DEFAULT_THEME_LAYOUT_RUNTIME` 与 teacher/student shell 上的 aurora framing、surface tonal 层级，找出哪些视觉只应该属于插件主题、哪些必须属于默认基线。然后把默认路径重新收敛成单一 truth source：无 `activeThemeId` 时只走系统默认 layout/tokens，不能继续把 phase 16 主题插件编排结果泄漏到默认主题；保留 Phase 16 已锁定的 `activeThemeId -> DAL -> ThemeInjector -> TeacherSidebarShell` 单一路径，不新增平行主题状态、页面内局部 hardcode fallback、或客户端旁路。补上 focused regression test，直接覆盖 default source、allowlisted shell mode 和默认主题下 teacher/student 壳层层次。</action>
  <verify>
    <automated>pnpm vitest run src/components/theme/theme-injector.test.tsx src/components/surfaces/settings-surface.test.tsx</automated>
  </verify>
  <done>默认主题重新成为稳定基线：未启用主题插件时，teacher/settings/student 的外层舞台与内容层次一致；启用主题插件时，既有 runtime path 仍照常生效。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 提炼共享版心 contract，移除回归的窄文案与局部 max-width</name>
  <files>src/components/surfaces/surface-widths.ts, src/components/surfaces/home-surface.tsx, src/components/surfaces/settings-surface.tsx, src/components/surfaces/settings-surface.test.tsx, src/components/surfaces/plugin-marketplace-surface.tsx, src/components/surfaces/students-management-surface.tsx</files>
  <behavior>
    - Test 1: 首页、设置页及其它受影响 surface 改为消费同一套宽度 token/contract，不再各自散落 `max-w-[1280px]`、`max-w-[1360px]` 之类的局部 magic number。
    - Test 2: 首页继续保持 260507-rto 已修正的 Stitch 对齐宽度，不再把说明文案或 hero 文案收窄回旧的段落上限；设置页和相关 surface 也不再重新出现不必要的窄版心。
  </behavior>
  <action>新增一个 surface-level 版心/容器 contract，统一 public、teacher-adjacent 与 settings-family 页面使用的 outer shell/content width，再把 `home-surface`、`settings-surface` 以及仍残留旧宽度上限的相关 surface 切到这条共享路径。首页必须保留 260507-rto 已确认的宽版心结果；不要把问题简化成“只删首页一个 max-width”，而要系统清理 theme/plugin 最近改动后重新冒出的局部宽度限制。不要引入 `prose`，不要新增新的页面级 magic number，也不要破坏现有 `teacherSurfaceRhythm` 的圆角/间距语义。</action>
  <verify>
    <automated>pnpm vitest run src/components/surfaces/settings-surface.test.tsx && ! pnpm exec rg 'max-w-\[(1280|1360)px\]' src/components/surfaces/home-surface.tsx src/components/surfaces/settings-surface.tsx src/components/surfaces/plugin-marketplace-surface.tsx src/components/surfaces/students-management-surface.tsx</automated>
  </verify>
  <done>首页与相关 surface 回到共享版心 contract；默认主题下不再出现回归的窄文案和局部 width drift。</done>
</task>

<task type="auto">
  <name>Task 3: 补一个默认主题快速回归入口</name>
  <files>scripts/verify-theme-default-regression.ts, package.json</files>
  <action>新增一个 quick-specific verifier，把 Task 1/2 的 focused tests 与静态 width guard 串成单命令入口 `verify:theme-default-regression`，用于后续主题插件和 layout orchestration 迭代前后的快速回归。保持它 fast and focused：只验证默认主题 fallback、关键 surface 的 shared width contract 与禁止回归的局部宽度写法；不要把整仓 lint/typecheck 塞进这个 quick verifier。</action>
  <verify>
    <automated>pnpm verify:theme-default-regression</automated>
  </verify>
  <done>仓库存在稳定的单命令回归入口，后续可以重复证明默认主题基线没有再被主题插件改坏。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `activeThemeId` / school theme runtime → shell UI | 非受信主题选择与插件 runtime 只能通过现有 DAL + runtime 编排影响界面，不能旁路默认主题基线 |
| plugin manifest layout → default theme fallback | 插件布局数据是受限输入，默认主题不能被插件态 layout/shell 视觉污染 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | T | `theme-injector` / shell runtime | mitigate | 默认主题显式回落到 `DEFAULT_THEME_LAYOUT_RUNTIME` 与系统 token，不接受插件态外观残留继续污染无主题路径 |
| T-quick-02 | I | surface width contract | mitigate | 通过 shared width contract + quick verifier 防止页面再次偷偷引入局部窄版心，避免默认主题界面出现不一致信息呈现 |
</threat_model>

<verification>
- `pnpm vitest run src/components/theme/theme-injector.test.tsx src/components/surfaces/settings-surface.test.tsx`
- `pnpm vitest run src/components/surfaces/settings-surface.test.tsx`
- `pnpm verify:theme-default-regression`
</verification>

<success_criteria>
- [ ] 默认主题在 teacher/settings/student 上重新表现为统一基线，不再出现深浅层级混杂
- [ ] 首页和其它受影响页面不再出现回归的窄段落/窄版心
- [ ] 主题插件仍只能沿用既有 runtime path 生效，没有新增平行主题状态
- [ ] 仓库新增 focused regression 入口，后续能快速防止同类退化再次发生
</success_criteria>

<output>
After completion, create `.planning/quick/260510-kc9-theme-default-regression-fix/260510-kc9-SUMMARY.md`
</output>
