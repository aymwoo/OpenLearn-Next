---
phase: 16-theme-plugins-and-layout-orchestration
plan: 04
type: execute
wave: 4
depends_on:
  - 16-01
  - 16-02
  - 16-03
files_modified:
  - src/components/surfaces/settings-surface.tsx
  - src/components/surfaces/settings-surface.test.tsx
  - package.json
  - scripts/verify-phase16-theme-layout.ts
autonomous: true
requirements:
  - PLUGIN-06
must_haves:
  truths:
    - D-15/D-19: 设置页能用中文结构摘要解释主题的导航模式、比例和辅助区域状态
    - D-13/D-14/D-18: richer theme 缺失或局部回退时，教师仍能理解当前主题实际生效的布局结构
    - D-16/D-17/D-18: Phase 16 有专门回归验证覆盖 theme layout 的安全与运行路径
  artifacts:
    - path: src/components/surfaces/settings-surface.tsx
      provides: Theme structure summary and understandable settings copy
      contains: 结构摘要
    - path: scripts/verify-phase16-theme-layout.ts
      provides: Phase 16 verification guard
      contains: theme-layout-runtime
    - path: package.json
      provides: Phase 16 verify script
      contains: verify:phase16
  key_links:
    - from: src/components/surfaces/settings-surface.tsx
      to: src/lib/dal/themes.ts
      via: summary consumes compiled runtime DTOs
      pattern: getValidThemesForSchool|结构摘要
    - from: package.json
      to: scripts/verify-phase16-theme-layout.ts
      via: dedicated phase verification command
      pattern: verify:phase16
---

<objective>
Make richer theme layouts understandable in settings and protect them with dedicated regression
coverage.

Purpose: Deliver D-15, D-16, D-18, and D-19 in user-visible settings copy and give Phase 16 a
deterministic verification entry point before execution finishes.
Output: Settings structure summaries, updated tests, and a phase-specific verify script.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-theme-plugins-and-layout-orchestration/16-CONTEXT.md
@.planning/phases/16-theme-plugins-and-layout-orchestration/16-UI-SPEC.md
@.planning/phases/16-theme-plugins-and-layout-orchestration/16-03-PLAN.md
@src/components/surfaces/settings-surface.tsx
@src/components/surfaces/settings-surface.test.tsx
@package.json

<interfaces>
From src/components/surfaces/settings-surface.tsx:
```ts
export async function SettingsSurface({ mode }: SettingsSurfaceProps)
```

From package.json:
```json
{
  "scripts": {
    "verify:phase11": "tsx scripts/verify-phase11-readiness.ts",
    "verify:phase12": "tsx scripts/verify-phase12-launch-and-builtins.ts"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace name-based theme copy with runtime-driven structure summaries</name>
  <files>src/components/surfaces/settings-surface.tsx, src/components/surfaces/settings-surface.test.tsx</files>
  <read_first>src/components/surfaces/settings-surface.tsx, src/components/surfaces/settings-surface.test.tsx, src/lib/dal/themes.ts, .planning/phases/16-theme-plugins-and-layout-orchestration/16-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: settings cards show shell mode, main split, and helper-region status from compiled runtime data.
    - Test 2: summaries stay readable when a theme falls back partially at region level.
    - Test 3: the UI no longer depends on theme-name heuristics like “星夜” or “晨光” to explain layout behavior.
  </behavior>
  <action>Update `src/components/surfaces/settings-surface.tsx` per D-13, D-14, and D-15 so theme cards render a Chinese `结构摘要` block sourced from the compiled runtime DTO returned by the theme DAL. The summary must explicitly describe shell mode (`左侧导航` / `顶部导航` / `顶部导航 + 左侧辅栏`), main split (`主内容 60:40` etc.), and helper-region state such as context panel or footer visibility. If a theme is partially falling back, show that in the summary instead of silently pretending the requested layout fully applied. Remove the `getThemeDescription(themeName)` name-guessing path and replace the existing tests in `src/components/surfaces/settings-surface.test.tsx` with assertions for runtime-driven summaries, partial-fallback copy, and continued Simplified Chinese UI strings.</action>
  <acceptance_criteria>
    - `src/components/surfaces/settings-surface.tsx` contains `结构摘要`.
    - `src/components/surfaces/settings-surface.tsx` does not contain `function getThemeDescription(themeName: string)`.
    - `src/components/surfaces/settings-surface.test.tsx` contains assertions for shell mode text and fallback summary text.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/surfaces/settings-surface.test.tsx</automated>
  </verify>
  <done>Settings cards explain actual active layout structure in Chinese and remain truthful when only part of a theme layout is applied.</done>
</task>

<task type="auto">
  <name>Task 2: Add a dedicated phase verification guard for theme layout orchestration</name>
  <files>package.json, scripts/verify-phase16-theme-layout.ts</files>
  <read_first>package.json, scripts/verify-phase11-readiness.ts, scripts/verify-phase12-launch-and-builtins.ts, src/components/theme/theme-injector.tsx, src/app/(teacher)/teacher/layout.tsx</read_first>
  <action>Create `scripts/verify-phase16-theme-layout.ts` and register `verify:phase16` in `package.json`. The script must verify the single runtime path and the locked layout guarantees: (1) `manifest.theme` still feeds the active theme runtime, (2) `ThemeInjector` exposes `theme-layout-runtime`, (3) the teacher shell recognizes `left-nav`, `top-nav`, and `top-nav-secondary-rail`, (4) settings UI contains `结构摘要`, and (5) no freeform manifest fields such as `className`, `script`, or raw `style` were introduced into the theme contract. Reuse the existing verify-script style from phases 11 and 12; do not add a manual-only checklist.</action>
  <acceptance_criteria>
    - `package.json` contains `verify:phase16`.
    - `scripts/verify-phase16-theme-layout.ts` contains `theme-layout-runtime`, `top-nav-secondary-rail`, and `结构摘要` checks.
    - `scripts/verify-phase16-theme-layout.ts` contains a guard rejecting `className` or `script` in the theme contract source.
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase16</automated>
  </verify>
  <done>Phase 16 finishes with an executable verification script that checks both user-visible summaries and the safety/runtime invariants for richer theme layouts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiled runtime DTO → settings copy | Internal layout metadata becomes teacher-facing explanation text |
| repository sources → verify script | Verification determines whether unsafe theme fields or missing runtime wiring slipped in |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-13 | T | `src/components/surfaces/settings-surface.tsx` | mitigate | Render structure summaries from compiled runtime DTOs instead of theme-name heuristics so the displayed layout state cannot drift silently. |
| T-16-14 | R | `scripts/verify-phase16-theme-layout.ts` | mitigate | Add deterministic source checks and a dedicated `verify:phase16` command so layout/runtime invariants are auditable after implementation. |
| T-16-15 | I | `src/components/surfaces/settings-surface.tsx` | accept | The summary exposes only shell mode and layout structure, not sensitive school or user data. |
| T-16-16 | E | `scripts/verify-phase16-theme-layout.ts` | mitigate | Fail verification when theme-contract sources contain disallowed freeform fields such as `className`, `script`, or raw style injection paths. |
</threat_model>

<verification>
- `pnpm test --run src/components/surfaces/settings-surface.test.tsx`
- `pnpm verify:phase16`
</verification>

<success_criteria>
- Settings explains richer themes with runtime-driven Chinese structure summaries.
- Partial-fallback states remain understandable to teachers.
- Phase 16 ships with a dedicated automated verification command.
</success_criteria>

<output>
After completion, create `.planning/phases/16-theme-plugins-and-layout-orchestration/16-04-SUMMARY.md`
</output>
