---
phase: 55-pilot-scope-and-acceptance-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/55-pilot-scope-and-acceptance-gate/55-PILOT-CONTRACT.md
autonomous: true
requirements:
  - PILOT-01
  - PILOT-03
must_haves:
  truths:
    - "`v3.1` 必须被正式定义为 single-school pilot production readiness，而不是泛生产化升级。"
    - "课堂投票插件是唯一真实样板，主链路固定为教师设计 -> 发布 -> 开课 -> 学生课堂完成。"
    - "40/5 容量口径和 deferred wall 必须进入正式 contract。"
  artifacts:
    - path: .planning/phases/55-pilot-scope-and-acceptance-gate/55-PILOT-CONTRACT.md
      provides: 冻结试点范围、样板链路、容量口径、baseline truth 与 deferred wall
---

<objective>
冻结 `v3.1` 的单校试点 contract，明确样板插件、主链路、容量口径、baseline truth 与 out-of-scope，阻止 milestone 继续漂移。
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.planning/phases/55-pilot-scope-and-acceptance-gate/55-CONTEXT.md
@.planning/phases/55-pilot-scope-and-acceptance-gate/55-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the authoritative pilot contract</name>
  <files>.planning/phases/55-pilot-scope-and-acceptance-gate/55-PILOT-CONTRACT.md</files>
  <read_first>
    - .planning/phases/55-pilot-scope-and-acceptance-gate/55-CONTEXT.md
    - .planning/phases/55-pilot-scope-and-acceptance-gate/55-RESEARCH.md
    - .planning/research/SUMMARY.md
    - .planning/research/FEATURES.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>创建 `55-PILOT-CONTRACT.md`，只写 milestone contract，不写实现设计。文件必须包含 `## Pilot Definition`、`## Sample Chain`、`## Baseline Truths`、`## Capacity Envelope`、`## Deferred Wall` 五个一级章节。`## Pilot Definition` 必须明确写出 `v3.1 = single-school pilot production readiness (plugin-first)` 与 `sample plugin = classroom voting`。`## Sample Chain` 必须明确写出 `teacher design -> publish -> launch -> student completion -> teacher/operator verification`。`## Baseline Truths` 必须明确写出既有 WebSocket-first、optional Redis fanout、BullMQ worker、SQLite + DAL truth、plugin lifecycle/command/event baseline 属于已存在能力。`## Capacity Envelope` 必须精确写入 `40 students per classroom` 与 `5 simultaneous classrooms`。`## Deferred Wall` 必须点名多校多租户、plugin marketplace、Agent Runtime、PostgreSQL/Kubernetes/重型 observability 平台迁移不属于本 milestone。</action>
  <acceptance_criteria>
    - 文件存在且包含指定五个一级章节。
    - 文件内出现精确字符串 `v3.1 = single-school pilot production readiness (plugin-first)`。
    - 文件内出现精确字符串 `sample plugin = classroom voting`。
    - 文件内出现精确字符串 `40 students per classroom`。
    - 文件内出现精确字符串 `5 simultaneous classrooms`。
  </acceptance_criteria>
  <verify>
    <automated>rg -n '^(## Pilot Definition|## Sample Chain|## Baseline Truths|## Capacity Envelope|## Deferred Wall|v3.1 = single-school pilot production readiness \(plugin-first\)|sample plugin = classroom voting|40 students per classroom|5 simultaneous classrooms)$' ".planning/phases/55-pilot-scope-and-acceptance-gate/55-PILOT-CONTRACT.md"</automated>
  </verify>
  <done>`55-PILOT-CONTRACT.md` 成为 Phase 56-60 的权威试点范围输入，不再允许后续 plan 自行改写 milestone framing。</done>
</task>

</tasks>

<success_criteria>
- `55-PILOT-CONTRACT.md` 成为 `PILOT-01` 与 `PILOT-03` 的权威输入。
- 文档明确承认 baseline，并冻结 deferred wall，避免 scope 漂移。
</success_criteria>

<output>
After completion, create `.planning/phases/55-pilot-scope-and-acceptance-gate/55-01-SUMMARY.md`
</output>
