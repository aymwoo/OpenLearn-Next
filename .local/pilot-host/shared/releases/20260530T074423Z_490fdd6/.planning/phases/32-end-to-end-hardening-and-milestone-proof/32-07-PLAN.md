---
phase: 32-end-to-end-hardening-and-milestone-proof
plan: 07
type: execute
wave: 4
depends_on: ["32-03", "32-05"]
files_modified:
  - src/features/runtime-platform/host/runtime-host-frame.tsx
  - src/features/runtime-platform/host/runtime-host.test.tsx
  - scripts/verify-phase32-end-to-end.ts
  - .planning/phases/32-end-to-end-hardening-and-milestone-proof/32-VALIDATION.md
autonomous: true
requirements: [RHOST-04]
gap_closure: true
must_haves:
  truths:
    - "教师从 seeded lesson 开课后，学生端 runtime iframe 能正常启动，不再因 opaque-origin/403 卡在‘等待 iframe ready’。"
    - "修复继续保留现有 runtime/platform 安全 posture：iframe 仍是受限 sandbox，不引入第二套 runtime 文档或新 truth path。"
    - "`verify:phase32` 会显式锁住 same-origin sandbox posture，并在 sandbox 被移除或被过度放宽时失败。"
  artifacts:
    - path: "src/features/runtime-platform/host/runtime-host-frame.tsx"
      provides: "与当前 Next app route 启动模型一致的 iframe sandbox token 组合"
    - path: "src/features/runtime-platform/host/runtime-host.test.tsx"
      provides: "对 sandbox token、ready 启动链路与最小权限边界的 focused regression"
    - path: "scripts/verify-phase32-end-to-end.ts"
      provides: "把 sandbox startup posture 纳入 Phase 32 canonical close gate 的 drift guard"
    - path: ".planning/phases/32-end-to-end-hardening-and-milestone-proof/32-VALIDATION.md"
      provides: "gap closure 后的 automated evidence 与 exit criteria 更新"
  key_links:
    - from: "src/features/runtime-platform/host/runtime-host-frame.tsx"
      to: "src/app/runtime/html-courseware/pilot/page.tsx"
      via: "iframe sandbox token 让同源 Next runtime page 能完成启动并发送 `runtime-frame-ready`"
      pattern: "allow-same-origin"
    - from: "scripts/verify-phase32-end-to-end.ts"
      to: "src/features/runtime-platform/host/runtime-host-frame.tsx"
      via: "static guard 锁定 same-origin + minimal sandbox posture"
      pattern: "sandbox=\"allow-scripts allow-forms allow-same-origin\""
---

<objective>
关闭 UAT Test 2 的剩余 major gap：最小修正 runtime iframe sandbox，使当前
`/runtime/html-courseware/pilot` 这条同源 Next app route 能正常启动，并把这条
启动前提收口到 `verify:phase32`。

Purpose: root cause 已确认不是 host handshake 再次回退，而是 iframe 仍以
opaque origin 加载当前依赖 app layout / global CSS / protected route 的 pilot。
本计划必须按 D-01、D-03、D-13、D-16 保留既有 canonical proof path，只修正
启动模型与 close gate，不新增独立 runtime 文档、不做架构翻修。

Output: same-origin sandbox fix, focused host regression, Phase 32 close-gate drift guard update.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-CONTEXT.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-RESEARCH.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-UAT.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-03-SUMMARY.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-05-SUMMARY.md
@.planning/debug/phase32-runtime-still-waits-for-iframe-ready-with-403.md
@src/features/runtime-platform/host/runtime-host-frame.tsx
@src/features/runtime-platform/host/runtime-host.test.tsx
@src/app/runtime/html-courseware/pilot/page.tsx
@src/proxy.ts
@scripts/verify-phase32-end-to-end.ts

<interfaces>
From `src/features/runtime-platform/host/runtime-host-frame.tsx`:
```tsx
<iframe
  ref={iframeRef}
  title={`${title} runtime`}
  src={src}
  className="block w-full border-0"
  style={{ height: frameHeight }}
  sandbox="allow-scripts allow-forms"
/>
```

From `src/app/runtime/html-courseware/pilot/page.tsx`:
```tsx
postToHost({
  channel: RUNTIME_HOST_BRIDGE_CHANNEL,
  kind: "runtime-frame-ready",
  runtimeInstanceId: "runtime-pilot-pending",
  sentAt: new Date().toISOString(),
})
```

From `src/proxy.ts`:
```ts
export const config = {
  matcher: ['/((?!api|_next|favicon.ico).*)'],
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 让 shared Runtime Host 的 sandbox 与当前 pilot 启动模型对齐</name>
  <files>src/features/runtime-platform/host/runtime-host-frame.tsx, src/features/runtime-platform/host/runtime-host.test.tsx</files>
  <behavior>
    - Test 1: `RuntimeHostFrame` 渲染的 iframe sandbox 包含 `allow-same-origin`，同时继续保留 `allow-scripts allow-forms`。
    - Test 2: sandbox 不新增 `allow-top-navigation`、`allow-popups`、`allow-downloads` 或等价更宽权限。
    - Test 3: 现有 host ready/bootstrap focused tests 继续通过，证明修复没有回退 32-05 的 pre-bootstrap handshake 语义。
  </behavior>
  <action>按 D-01、D-03、D-16 做最小修复：在 `runtime-host-frame.tsx` 中把 iframe sandbox 收敛为当前同源 Next app route 所需的最小可运行集合，即在既有 `allow-scripts allow-forms` 基础上补 `allow-same-origin`，从而让 `/runtime/html-courseware/pilot` 不再以 opaque origin 启动。不要新建独立 runtime HTML 文档、不要把 pilot 挪出当前 app route、不要改 `src/proxy.ts` matcher、不要移除 sandbox，也不要额外放开 top navigation / popup / download 能力。同步扩展 `runtime-host.test.tsx`，用语义断言锁住 sandbox token 组合和最小权限边界，而不是写注释型字符串检查。</action>
  <verify>
    <automated>pnpm test --run src/features/runtime-platform/host/runtime-host.test.tsx</automated>
  </verify>
  <done>seeded proof iframe 能以同源姿态启动当前 pilot 路由，同时 sandbox 仍保持最小权限。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把 same-origin 启动前提纳入 `verify:phase32` 的 close gate</name>
  <files>scripts/verify-phase32-end-to-end.ts, .planning/phases/32-end-to-end-hardening-and-milestone-proof/32-VALIDATION.md</files>
  <behavior>
    - Test 1: `verify:phase32` 的 drift guard 会在 `runtime-host-frame.tsx` 丢失 `allow-same-origin` 时失败。
    - Test 2: `verify:phase32` 同时会在 sandbox 被扩成更宽权限时失败，避免用修 bug 为名弱化安全 posture。
    - Test 3: `32-VALIDATION.md` 明确把这条 gap closure 记为 automated evidence，而不是手工口头约定。
  </behavior>
  <action>更新 `scripts/verify-phase32-end-to-end.ts`：在现有 `classroom live refresh drift`、`classroom proof drift` 等 guard 之外，新增长驻的 sandbox startup drift guard，明确要求 `runtime-host-frame.tsx` 保持 `allow-same-origin` 且仍是最小 allowlist；guard 失败信息要直接说明这是为了解决 UAT Test 2 的 opaque-origin 启动问题。不要新建第二个 verify 命令，不要把检查写成会被注释或文案误伤的 grep gate。随后更新 `32-VALIDATION.md`，把本次 gap closure 映射到 `RHOST-04` 的 automated evidence、sampling 和 exit criteria，说明 `pnpm verify:phase32` 必须持续覆盖 iframe same-origin startup posture。</action>
  <verify>
    <automated>pnpm verify:phase32</automated>
  </verify>
  <done>Phase 32 close gate 会持续守住“当前 pilot 是受限 same-origin sandbox app route”这一启动合同，后续回归会被自动拦截。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host iframe sandbox -> runtime app route | 需要允许同源启动当前 first-party route，但不能顺手放开更宽浏览器能力 |
| verifier drift guard -> security posture | close gate 必须锁住最小权限 sandbox，避免未来修复回归成过宽沙箱 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-07-01 | E | runtime iframe sandbox | mitigate | 只补 `allow-same-origin`，继续禁止 top navigation、popups、downloads，并保持 route 仍走现有 host bridge + server boundary |
| T-32-07-02 | T | phase32 verifier | mitigate | 在 `verify:phase32` 增加 sandbox startup drift guard，同时检查 required token 与 forbidden token |
| T-32-07-03 | I | runtime startup model | accept | 当前 pilot 是 repo-local first-party route，允许 same-origin 是为了让既有 app shell 正常启动；不新增跨域或第三方内容 |
</threat_model>

<verification>
- `pnpm test --run src/features/runtime-platform/host/runtime-host.test.tsx`
- `pnpm verify:phase32`
</verification>

<success_criteria>
- UAT Test 2 的 root cause 被最小修复：current pilot 不再因 opaque origin + 403 卡在 `等待 iframe ready`。
- shared Runtime Host 仍保持受限 sandbox posture，没有引入第二套 runtime 文档或更宽浏览器权限。
- `verify:phase32` 与 `32-VALIDATION.md` 都把 same-origin startup posture 视为 Phase 32 close 的长期自动化合同。
</success_criteria>

<output>
After completion, create `.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-07-SUMMARY.md`
</output>
