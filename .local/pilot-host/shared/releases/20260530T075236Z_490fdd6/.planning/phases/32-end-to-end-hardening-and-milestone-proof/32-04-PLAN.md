---
phase: 32-end-to-end-hardening-and-milestone-proof
plan: 04
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - src/components/surfaces/classroom-launch-surface.tsx
  - src/components/classroom/classroom-launch-panel.test.tsx
  - src/components/classroom/classroom-control-panel.tsx
  - src/lib/dal/runtime-inspector.test.ts
  - src/components/surfaces/runtime-inspector-surface.tsx
  - src/app/settings/labs/runtime-inspector/page.tsx
  - src/components/surfaces/runtime-inspector-surface.test.tsx
  - src/components/surfaces/classroom-console-surface.test.tsx
  - .planning/phases/32-end-to-end-hardening-and-milestone-proof/32-DEMO-HANDOFF.md
autonomous: true
requirements: [RHOST-04]
must_haves:
  truths:
    - "per D-07/D-11/D-12，教师先在 `/classroom` 看到 proof 成功/异常，再通过 `runtimeSessionId` 进入 inspector 审查 proof timeline 或 event log。"
    - "per D-14/D-16，proof handoff 依靠 seeded demo 入口说明和现有 surface affordance，而不是额外 dashboard。"
  artifacts:
    - path: "src/components/surfaces/classroom-launch-surface.tsx"
      provides: "teacher launch proof affordance for the canonical seeded demo path"
    - path: "src/components/classroom/classroom-control-panel.tsx"
      provides: "teacher-first proof success/failure cues and inspector CTA"
    - path: ".planning/phases/32-end-to-end-hardening-and-milestone-proof/32-DEMO-HANDOFF.md"
      provides: "explicit seeded demo entry and drill-down instructions"
  key_links:
    - from: "src/components/classroom/classroom-control-panel.tsx"
      to: "src/app/settings/labs/runtime-inspector/page.tsx"
      via: "runtimeSessionId deep link"
      pattern: "runtimeSessionId"
    - from: ".planning/phases/32-end-to-end-hardening-and-milestone-proof/32-DEMO-HANDOFF.md"
      to: "scripts/bootstrap-dev-db.ts"
      via: "deterministic demo setup instructions"
      pattern: "db:bootstrap:dev"
---

<objective>
把 Phase 32 proof 做成可发现、可演示、可排障的产品化链路：launch 可发现，classroom
先确认，inspector 二次钻取，并留下明确 demo handoff 文档。

Purpose: 即使底层 proof 已成立，如果入口、教师反馈和 handoff 不清晰，milestone 仍不可
交付给下游执行者或人类演示者。
Output: proof affordances on existing surfaces plus explicit handoff documentation.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-CONTEXT.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-PATTERNS.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-UI-SPEC.md
@src/components/surfaces/classroom-launch-surface.tsx
@src/components/classroom/classroom-control-panel.tsx
@src/components/surfaces/runtime-inspector-surface.tsx
@src/app/settings/labs/runtime-inspector/page.tsx
@scripts/bootstrap-dev-db.ts

<interfaces>
From `src/components/surfaces/classroom-launch-surface.tsx`:
```tsx
<ClassroomLaunchPanel
  publishedLessons={consoleData.publishedLessons}
  successHref="/classroom"
/>
```

From `src/components/classroom/classroom-control-panel.tsx`:
```tsx
const currentSnapshot = conflict?.latest || initialSnapshot
const currentStep = currentSnapshot.steps.find(...)
```

From `src/app/settings/labs/runtime-inspector/page.tsx`:
```tsx
const inspector = await getRuntimeInspectorDTO({ runtimeSessionId: resolvedSearchParams.runtimeSessionId })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 launch/classroom/inspector 三处补齐 proof affordance</name>
  <read_first>src/components/surfaces/classroom-launch-surface.tsx, src/components/classroom/classroom-launch-panel.test.tsx, src/components/classroom/classroom-control-panel.tsx, src/lib/dal/runtime-inspector.test.ts, src/components/surfaces/runtime-inspector-surface.tsx, src/app/settings/labs/runtime-inspector/page.tsx</read_first>
  <files>src/components/surfaces/classroom-launch-surface.tsx, src/components/classroom/classroom-launch-panel.test.tsx, src/components/classroom/classroom-control-panel.tsx, src/lib/dal/runtime-inspector.test.ts, src/components/surfaces/runtime-inspector-surface.tsx, src/app/settings/labs/runtime-inspector/page.tsx, src/components/surfaces/runtime-inspector-surface.test.tsx, src/components/surfaces/classroom-console-surface.test.tsx</files>
  <behavior>
    - Test 1: `/teacher/launch` 对 canonical seeded proof lesson 有可发现但次级的 demo affordance。
    - Test 2: `/classroom` 对当前 runtime submit success/failure 有 teacher-first 提示，并提供 inspector deep-link CTA。
    - Test 3: inspector 首屏能明显表达“当前 proof 会话”，并继续围绕 query 传入的 `runtimeSessionId` 展示可审查 canonical proof 的统一 timeline 或 event log。
  </behavior>
  <action>复用 `32-PATTERNS.md` 的 Pattern 4 与 Pattern 5（teacher-first live proof surface + runtime inspector deep-link posture），按 D-07、D-11、D-12、D-16 和 `32-UI-SPEC.md` 产品化现有 surfaces。`classroom-launch-surface.tsx` 中增加一个轻量 proof affordance，指向 seeded demo 入口但不取代主 CTA。`classroom-control-panel.tsx` 中增加 server-owned proof summary 呈现：成功时明确告知教师已有学生完成当前互动提交；失败时明确告知存在 runtime submit 异常；若已拿到 `runtimeSessionId`，提供次级 CTA 跳到 `/settings/labs/runtime-inspector?runtimeSessionId=...`。`runtime-inspector-surface.tsx`、page 和 inspector DAL 保持独立 unified timeline posture，但补充更清晰的“当前 proof 会话”文案，并确保操作者能审查 canonical proof 的关键 timeline 或 event log（至少含 runtime interaction 或 submit truth、governance 或 transport trace），而不是只看到空的 deep-link 外壳。不要新增 milestone dashboard，不要把 inspector 变成 classroom 主视图。</action>
  <acceptance_criteria>
    - `classroom-control-panel.tsx` 中存在可 grep 的 inspector deep-link 到 `runtimeSessionId`
    - `classroom-launch-surface.tsx` 中存在 proof/demo discoverability copy，但主 CTA 仍是开启新课堂
    - `classroom-launch-panel.test.tsx`、`runtime-inspector-surface.test.tsx`、`runtime-inspector.test.ts` 与 `classroom-console-surface.test.tsx` 覆盖 proof affordance、deep-link posture 与 reviewable timeline posture
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/classroom/classroom-launch-panel.test.tsx src/lib/dal/runtime-inspector.test.ts src/components/surfaces/runtime-inspector-surface.test.tsx src/components/surfaces/classroom-console-surface.test.tsx</automated>
  </verify>
  <done>proof 已在现有产品 surface 中可发现、可确认、可二次排障，且未引入新主路由或新 dashboard。</done>
</task>

<task type="auto">
  <name>Task 2: 写出 explicit demo handoff 文档</name>
  <read_first>.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-CONTEXT.md, .planning/phases/32-end-to-end-hardening-and-milestone-proof/32-PATTERNS.md, scripts/bootstrap-dev-db.ts, package.json</read_first>
  <files>.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-DEMO-HANDOFF.md</files>
  <action>按 D-13、D-14 写一份 repo-local handoff 文档，文件名固定为 `.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-DEMO-HANDOFF.md`。文档必须明确列出：1) bootstrap 命令（优先 `pnpm db:bootstrap:dev` 或实际 repo 命令）；2) teacher/student 登录账号；3) canonical proof 演示顺序：editor/publish -> launch/classroom -> student submit -> classroom first-feedback -> inspector drill-down；4) 标准排障第二步：当 classroom 提示异常时，使用 `runtimeSessionId` 打开 inspector；5) 任何已知非目标范围（不涉及 PostgreSQL/Redis/WebSocket cutover、不新增 milestone dashboard）。不要写成模糊 README，不要要求 downstream 自己 reverse-engineer demo path。</action>
  <acceptance_criteria>
    - `32-DEMO-HANDOFF.md` 包含 teacher/student 账号、bootstrap 命令、proof chain、inspector second-step drill-down
    - 文档明确写出不做的内容：无 cutover、无新 dashboard、无 player-direct canonical proof
  </acceptance_criteria>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path

text = Path('.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-DEMO-HANDOFF.md').read_text()
required_groups = {
    'accounts': ['teacher@example.com', 'student@example.com'],
    'bootstrap': ['db:bootstrap:dev'],
    'proof_chain': ['editor/publish', 'launch/classroom', 'student submit', 'classroom first-feedback', 'inspector drill-down'],
    'inspector_second_step': ['runtimeSessionId', '第二步'],
    'out_of_scope': ['PostgreSQL', 'Redis', 'WebSocket', 'dashboard', 'player-direct'],
}
missing = {
    group: [item for item in values if item not in text]
    for group, values in required_groups.items()
}
missing = {group: values for group, values in missing.items() if values}
raise SystemExit(1 if missing else 0)
PY</automated>
  </verify>
  <done>任何执行者或人类开发者都能直接按文档跑通 canonical proof，并知道异常时第二步去哪里查。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| classroom UI -> inspector route | 如果 deep-link 不受控，可能泄露错误 session 或弱化 classroom-first posture |
| demo handoff doc -> real repo setup | 如果文档依赖隐式知识，下游会走错 proof path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-04-01 | I | inspector deep-link CTA | mitigate | CTA 仅使用当前 proof 的 `runtimeSessionId`，不放宽 inspector 自有 scope 规则 |
| T-32-04-02 | R | demo handoff doc | mitigate | 文档显式写出 bootstrap、账号、proof 顺序和 second-step drill-down，避免口口相传式演示 |
| T-32-04-03 | S | launch/classroom proof affordance | mitigate | 保持现有 surfaces 与主 CTA，不创建误导性的第二主入口或 dashboard |

</threat_model>

<verification>
- `pnpm test --run src/components/surfaces/runtime-inspector-surface.test.tsx src/components/surfaces/classroom-console-surface.test.tsx`
- `pnpm test --run src/components/classroom/classroom-launch-panel.test.tsx src/lib/dal/runtime-inspector.test.ts src/components/surfaces/runtime-inspector-surface.test.tsx src/components/surfaces/classroom-console-surface.test.tsx`
- handoff doc presence check command above exits 0
</verification>

<success_criteria>
- 现有 surfaces 已表达 canonical proof 入口、first confirmation、reviewable event log 与 second-step drill-down。
- proof handoff 文档已明确且 repo-local。
- milestone 交付不依赖口头说明或隐式知识。
</success_criteria>

<output>
After completion, create `.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-04-SUMMARY.md`
</output>
