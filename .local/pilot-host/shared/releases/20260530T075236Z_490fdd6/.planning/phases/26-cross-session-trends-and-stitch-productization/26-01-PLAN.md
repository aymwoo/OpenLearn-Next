---
phase: 26-cross-session-trends-and-stitch-productization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dto/classroom.ts
  - src/lib/dal/classroom.ts
  - src/lib/dal/classroom.test.ts
autonomous: true
requirements:
  - ANALYTICS-02
must_haves:
  truths:
    - 教师默认先看到最近 4 次真实课堂 session 的班级趋势，而不是 lesson-first 汇总或 student-first 首屏。
    - 教师在 trends 与 `/classroom` 中看到的班级/学生信号保持同一口径，因为两者复用同一份 classroom evidence truth。
    - 每个异常趋势点都保留可回到单次 `/classroom?sessionId=...` 的原始课堂指针与受影响学生集合。
  artifacts:
    - path: src/lib/dto/classroom.ts
      provides: cross-session trend DTO contracts for class trend, student trend, and inline detail
      contains: ClassroomRecentSessionTrendDTOSchema
    - path: src/lib/dal/classroom.ts
      provides: teacher-scoped recent-session trend aggregation helper
      contains: getTeacherRecentSessionTrendDTO
    - path: src/lib/dal/classroom.test.ts
      provides: deterministic trend aggregation regression coverage
      contains: getTeacherRecentSessionTrendDTO
  key_links:
    - from: src/lib/dal/classroom.ts
      to: src/lib/dto/classroom.ts
      via: trend aggregation must parse to stable DTO schemas before any route consumes it
      pattern: getTeacherRecentSessionTrendDTO|ClassroomRecentSessionTrendDTOSchema
    - from: src/lib/dal/classroom.ts
      to: /classroom
      via: inline detail exposes recap href pointers instead of inventing a second analytics workflow
      pattern: primaryRecapHref|sessionId
---

<objective>
为 Phase 26 先定义 recent-session 趋势数据合同与 teacher-scoped 聚合读模型，
把 class-first trends 锁定在现有 classroom recap truth 之上。

Purpose: 先把 ANALYTICS-02 的事实层与 ranking/window 口径定死，后续路由和产品化只消费稳定 DTO。
Output: cross-session trend DTO schemas, recent-session aggregation helper, and targeted DAL tests.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-cross-session-trends-and-stitch-productization/26-CONTEXT.md
@.planning/phases/26-cross-session-trends-and-stitch-productization/26-RESEARCH.md
@.planning/phases/26-cross-session-trends-and-stitch-productization/26-UI-SPEC.md
@.planning/phases/25-teaching-data-capture-and-session-analytics/25-01-SUMMARY.md
@src/lib/dto/classroom.ts
@src/lib/dal/classroom.ts

<interfaces>
Existing recap contracts to extend from `src/lib/dto/classroom.ts`:
```ts
export type ClassroomConsoleSessionEntryDTO = z.infer<typeof ClassroomConsoleSessionEntryDTOSchema>;
export type ClassroomSessionRecapSummaryDTO = z.infer<typeof ClassroomSessionRecapSummaryDTOSchema>;
export type ClassroomSessionRecapStudentSummaryDTO = z.infer<typeof ClassroomSessionRecapStudentSummaryDTOSchema>;
export type ClassroomSessionRecapDTO = z.infer<typeof ClassroomSessionRecapDTOSchema>;
```

Existing DAL entry points from `src/lib/dal/classroom.ts`:
```ts
export async function getClassroomConsoleDTO() {}
export async function getClassroomSessionRecapDTO(rawInput: unknown) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define trend DTO contracts for class-first recent-session comparisons</name>
  <files>src/lib/dto/classroom.ts</files>
  <read_first>src/lib/dto/classroom.ts, .planning/phases/26-cross-session-trends-and-stitch-productization/26-CONTEXT.md, .planning/phases/26-cross-session-trends-and-stitch-productization/26-RESEARCH.md, .planning/phases/26-cross-session-trends-and-stitch-productization/26-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: trend input schema and top-level DTO default to class-first recent-session comparison with `view: "sessions"` and a latest-4 window.
    - Test 2: inline anomaly detail includes session summary, impacted students, key signals, and `primaryRecapHref` per D-09/D-10/D-11.
    - Test 3: student trend summaries are secondary/read-through data under the class trend response, not a separate primary payload shape.
  </behavior>
  <action>In `src/lib/dto/classroom.ts`, add typed contracts for Phase 26 trends: a `ClassroomTrendViewSchema` with explicit default `sessions`, a `GetTeacherRecentSessionTrendInputSchema` that accepts `classId`, optional `lessonId`, optional `studentId`, optional `sessionId`, optional `view`, and optional `limit`, a `ClassroomTrendSessionPointDTOSchema` for the latest 4 ended sessions, a `ClassroomClassTrendSummaryDTOSchema`, a `ClassroomStudentTrendSummaryDTOSchema`, an inline `ClassroomTrendDetailDTOSchema`, and a top-level `ClassroomRecentSessionTrendDTOSchema`. The DTO must encode class-first posture per D-05/D-06/D-07, keep `view: "sessions"` as the default comparison unit in both input and response, preserve explicit `未评价` semantics, and expose `primaryRecapHref` plus optional `secondaryReviewHref` strings instead of forcing client-side route composition.</action>
  <acceptance_criteria>
    - `src/lib/dto/classroom.ts` contains `GetTeacherRecentSessionTrendInputSchema` and `ClassroomRecentSessionTrendDTOSchema`.
    - `GetTeacherRecentSessionTrendInputSchema` contains optional `view` input defaulting to `sessions`.
    - `src/lib/dto/classroom.ts` contains a latest-4 session window field or default matching the UI-SPEC contract.
    - `src/lib/dto/classroom.ts` contains `primaryRecapHref` and does not define a review-first primary action field.
    - `src/lib/dto/classroom.ts` keeps `未评价` as an explicit trend/state label source.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/classroom.test.ts</automated>
  </verify>
  <done>Trend routes can rely on one stable DTO family for class trend summaries, student drill-down, and recap deep links.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement teacher-scoped recent-session trend aggregation without a second truth source</name>
  <files>src/lib/dal/classroom.ts, src/lib/dal/classroom.test.ts</files>
  <read_first>src/lib/dal/classroom.ts, src/lib/dal/classroom.test.ts, src/lib/dto/classroom.ts, .planning/phases/25-teaching-data-capture-and-session-analytics/25-01-SUMMARY.md, .planning/phases/26-cross-session-trends-and-stitch-productization/26-CONTEXT.md, .planning/phases/26-cross-session-trends-and-stitch-productization/26-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `getTeacherRecentSessionTrendDTO()` returns the latest 4 ended sessions in teacher scope, not a lesson-aggregate-only view.
    - Test 2: student ranking is deterministic, prioritizing repeated `需要关注`, explicit `未评价`, and missing task/quiz submissions before neutral students.
    - Test 3: selected anomaly detail returns session summary, impacted students, key signals, and deep-link pointers while never inserting or updating analytics persistence rows.
  </behavior>
  <action>In `src/lib/dal/classroom.ts`, implement `getTeacherRecentSessionTrendDTO(rawInput: unknown)` alongside small helper builders for session windowing, class delta summaries, student ranking, and inline detail assembly. Reuse existing ended-session / recap truth from `classroomSessions`, `classroomParticipants`, `classroomEvidence`, `classroomTimeline`, latest task submissions, latest quiz attempts, and `attemptFeedback`; do not create new tables, snapshot rows, or write paths. Use the latest 4 ended sessions by default per UI-SPEC, compute class-level deltas for completion, submission, follow-up signals, pending feedback, `需要关注`, and `未评价`, and rank student trend summaries by `needsFollowUpSessions DESC`, `unevaluatedSessions DESC`, `missingSubmissionSessions DESC`, then student name for deterministic ties. Add DAL tests covering teacher scoping, latest-4 behavior, explicit `未评价`, and recap/review href generation.</action>
  <acceptance_criteria>
    - `src/lib/dal/classroom.ts` contains `getTeacherRecentSessionTrendDTO`.
    - `src/lib/dal/classroom.ts` reads ended classroom sessions and does not contain `insert(` or `update(` statements for any analytics-specific persistence path.
    - `src/lib/dal/classroom.test.ts` contains coverage for latest-4 windowing, deterministic student ordering, and `primaryRecapHref`.
    - no route string or DTO field makes `/teacher/review` the default first jump from trend detail.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/classroom.test.ts</automated>
  </verify>
  <done>Phase 26 has one teacher-scoped recent-session trend read model derived entirely from existing classroom evidence truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classroom truth -> trend aggregation | only durable classroom/session facts may cross into cross-session metrics |
| trend detail -> follow-up routes | links must preserve single-session classroom context and avoid forcing users into the wrong workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | T | `src/lib/dal/classroom.ts` trend aggregation | mitigate | Reuse existing classroom/session tables only; forbid new analytics snapshot writes and keep formulas in teacher-scoped DAL helpers. |
| T-26-02 | I | trend detail route pointers | mitigate | Emit `primaryRecapHref` to `/classroom?sessionId=...` from DAL/DTO so client code cannot silently drift to a review-first path. |
| T-26-03 | R | student ranking and window selection | mitigate | Fix default window to latest 4 ended sessions and define deterministic tie-break ordering in tests. |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/classroom.test.ts`
</verification>

<success_criteria>
- Cross-session trends are computed from existing classroom truth with no second analytics store.
- The default response is class-first recent-session data with latest-4 behavior.
- Trend detail retains raw session pointers and impacted student lists for downstream UI drill-down.
</success_criteria>

<output>
After completion, create `.planning/phases/26-cross-session-trends-and-stitch-productization/26-01-SUMMARY.md`
</output>
