---
phase: 29-runtime-host-and-html-courseware-pilot
plan: 03
type: execute
wave: 3
depends_on: [29-01]
files_modified:
  - src/lib/dto/lesson-authoring.ts
  - src/lib/dto/resource-ai.ts
  - src/server/plugins/registry.ts
  - src/lib/dal/plugins.builtins.test.ts
  - src/components/authoring/lesson-authoring-workspace.test.tsx
  - src/lib/dal/lesson-authoring.test.ts
autonomous: true
requirements: [RHOST-03]
must_haves:
  truths:
    - "D-09：内置 HTML runtime step 继续走 built-in template 注入链，不新增 runtime 专用 step family。"
    - "D-10：该 built-in step 的 `payload.runtime` descriptor 继续随 published snapshot 冻结。"
    - "D-11：首个 HTML runtime pilot 只能是本地 built-in entry，不接受 remote runtime source。"
  artifacts:
    - path: "src/lib/dto/resource-ai.ts"
      provides: "new built-in HTML runtime teaching-step definition"
      contains: "html-courseware"
    - path: "src/server/plugins/registry.ts"
      provides: "allowlisted built-in template dispatch for the HTML runtime pilot"
      contains: "insertBuiltInTeachingStepTemplate"
    - path: "src/lib/dal/lesson-authoring.test.ts"
      provides: "publish snapshot freeze regression for the built-in runtime step"
      contains: "payload.runtime"
  key_links:
    - from: "src/lib/dto/resource-ai.ts"
      to: "src/lib/dto/lesson-authoring.ts"
      via: "built-in template initialPayload uses existing lessonStepPayloadSchema"
      pattern: "initialPayload"
    - from: "src/server/plugins/registry.ts"
      to: "src/lib/dal/plugins.ts"
      via: "built-in template dispatch remains on the current allowlisted plugin action path"
      pattern: "insertBuiltInTeachingStepTemplate"
---

<objective>
通过现有 built-in teaching-step 机制，把一个本地 HTML courseware runtime pilot
接入 editor 与 publish 链路。

Purpose: 让教师能在现有编排工作流里真实添加并发布 runtime-capable step，而不是靠
手工构造 payload 或私有入口。
Output: built-in HTML runtime template definition, authoring regression coverage, publish freeze proof.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-runtime-host-and-html-courseware-pilot/29-CONTEXT.md
@.planning/phases/29-runtime-host-and-html-courseware-pilot/29-RESEARCH.md
@src/lib/dto/lesson-authoring.ts
@src/lib/dto/resource-ai.ts
@src/lib/dal/plugins.ts
@src/server/plugins/registry.ts
@src/components/authoring/lesson-authoring-workspace.tsx
@src/lib/dal/lesson-authoring.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义一个内置 HTML runtime teaching-step template</name>
  <read_first>
    - src/lib/dto/resource-ai.ts
    - src/server/plugins/registry.ts
    - src/lib/dto/lesson-authoring.ts
    - src/lib/dal/plugins.ts
  </read_first>
  <files>src/lib/dto/resource-ai.ts, src/server/plugins/registry.ts, src/lib/dal/plugins.builtins.test.ts</files>
  <action>在当前 built-in teaching-step 定义集合中新增一个 HTML runtime pilot 定义，例如新增 `builtInKey`、`pluginName`、`title`、`summary`、`stepType`、`initialTitle`、`initialPayload`。`initialPayload` 必须仍然是现有 `lessonStepPayloadSchema` 可接受的 payload，并显式携带 `payload.runtime` descriptor：`kind` 使用现有 `html-courseware`，`entry.sandbox` 固定 `iframe`，`entry.bootstrap` 指向本地 built-in runtime entry，`submitTarget` 保持当前 Phase 28 允许的桥接语义，`requestedCapabilities` 只声明本 pilot 实际需要的最小集合。不要引入 remote URL 字段、第三方 source 字段或新的 lesson step `type`。补 `plugins.builtins.test.ts`，锁定该 built-in template 继续通过 `insertBuiltInTeachingStepTemplate` 取回，并且模板 payload 已带 runtime descriptor。</action>
  <acceptance_criteria>
    - `resource-ai.ts` 出现新的 built-in HTML runtime 定义，且 `initialPayload` 带 `payload.runtime`。
    - 新定义使用现有 `html-courseware` 与 `iframe` contract 值。
    - 不新增新的 lesson step `type` 常量。
    - `plugins.builtins.test.ts` 包含 built-in HTML runtime template parse 或 dispatch 断言。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/lib/dal/plugins.builtins.test.ts</automated>
  </verify>
  <done>仓库存在一个本地 built-in HTML runtime teaching-step template，可通过当前 plugin/template 链被 editor 发现。</done>
</task>

<task type="auto">
  <name>Task 2: 锁定 editor 注入与 publish snapshot 冻结行为</name>
  <read_first>
    - src/components/authoring/lesson-authoring-workspace.tsx
    - src/components/authoring/lesson-authoring-workspace.test.tsx
    - src/lib/dal/lesson-authoring.ts
    - src/lib/dal/lesson-authoring.test.ts
  </read_first>
  <files>src/components/authoring/lesson-authoring-workspace.test.tsx, src/lib/dal/lesson-authoring.test.ts</files>
  <action>补 focused regressions，锁定 editor 和 publish 行为。`lesson-authoring-workspace.test.tsx` 要证明新 built-in HTML runtime step 会出现在现有 built-in templates 资源库中，并仍通过当前 `addLessonStepAction()` 路径注入 lesson flow。`lesson-authoring.test.ts` 要证明该 built-in runtime step 发布后，`publishedLessonVersions.snapshotJson.steps[*].payload.runtime` 仍保留完整 descriptor，而不是降格成 `runtimeId` 或丢失 bootstrap/sandbox/submitTarget 字段。必要时补充 `lesson-authoring.ts` 现有测试样例数据，但不要改出新的 publish truth path。</action>
  <acceptance_criteria>
    - `lesson-authoring-workspace.test.tsx` 覆盖 built-in HTML runtime template 在资源库中可见。
    - `lesson-authoring-workspace.test.tsx` 断言它仍通过当前 add-step 行为注入 lesson flow。
    - `lesson-authoring.test.ts` 断言发布后的 snapshot 中保留完整 `payload.runtime` 对象。
    - 测试中不出现新的 runtime 专用 publish path 或 side-channel persistence。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/components/authoring/lesson-authoring-workspace.test.tsx src/lib/dal/lesson-authoring.test.ts</automated>
  </verify>
  <done>教师可以在现有 editor 流程里添加并发布内置 HTML runtime step，且 descriptor 继续被冻结在现有 snapshot truth 中。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| built-in template definition -> authoring UI | 如果模板绕开现有 schema，会把不受控 payload 注入 lesson truth |
| publish snapshot -> runtime bootstrap | 如果 snapshot 丢 descriptor 字段，后续 runtime host 会依赖可变 side source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-29-03-01 | T | built-in runtime template | mitigate | built-in template 继续走 `lessonStepPayloadSchema` 和 allowlisted plugin action path |
| T-29-03-02 | E | remote runtime source creep | mitigate | 不引入 remote bootstrap URL 或 third-party runtime source 字段 |
| T-29-03-03 | R | publish drift | mitigate | 用 `lesson-authoring.test.ts` 锁住 snapshot 中完整 runtime descriptor freeze 行为 |

</threat_model>

<verification>
- `pnpm test --run src/lib/dal/plugins.builtins.test.ts`
- `pnpm test --run src/components/authoring/lesson-authoring-workspace.test.tsx src/lib/dal/lesson-authoring.test.ts`
- 读取 `src/lib/dto/resource-ai.ts`，确认 built-in HTML runtime 模板存在且仍是现有 step payload
</verification>

<success_criteria>
- editor 里可通过现有 built-in templates 添加 HTML runtime pilot step。
- 该 step 继续是现有 lesson step payload，不是新 family。
- publish 后完整 runtime descriptor 保留在 snapshot truth 中。
</success_criteria>

<output>
After completion, create `.planning/phases/29-runtime-host-and-html-courseware-pilot/29-03-SUMMARY.md`
</output>
