---
phase: 33-project-level-auth-data-and-classroom-durability-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auth/auth.ts
  - src/lib/auth/auth.config.ts
  - src/lib/auth/auth.test.ts
  - src/lib/dal/auth.ts
  - src/lib/dto/user.ts
  - src/lib/dto/membership.ts
  - src/actions/auth-actions.ts
  - src/actions/auth-actions.test.ts
  - src/proxy.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]
must_haves:
  truths:
    - "Auth.js v5 + Drizzle auth tables 继续是唯一 session truth，不新增第二套身份系统。"
    - "teacher、student、admin 的 route entry、future role vocabulary 与 `proxy.ts` protected family truth 保持一致。"
    - "Proxy 只做 unauthenticated route-family protection；角色、学校与资源 scope 校验继续留在 DAL 或 Server Actions。"
  artifacts:
    - path: "src/proxy.ts"
      provides: "explicit protected route families and redirect posture"
    - path: "src/lib/dal/auth.ts"
      provides: "shared current-actor and role or school resolution contract"
    - path: "src/actions/auth-actions.ts"
      provides: "role-aware sign-in flow aligned with workspace entry"
    - path: "src/lib/dto/membership.ts"
      provides: "future-role-safe membership vocabulary for server code"
  key_links:
    - from: "src/actions/auth-actions.ts"
      to: "src/proxy.ts"
      via: "shared role-aware workspace entry posture"
      pattern: "teacher|student|admin"
    - from: "src/lib/dal/auth.ts"
      to: "src/lib/dto/user.ts"
      via: "sanitized actor DTO instead of raw row"
      pattern: "DTO"
---

<objective>
把现有 Auth.js sign-in、session truth、future role vocabulary 与 `proxy.ts` route-family
保护收成一个一致的 auth entry contract，为后续 DAL or classroom 收紧提供单一 actor truth。

Purpose: 如果登录入口、角色模型与 route protection 继续各自为政，后续任何 DAL 收紧都会出现
“能登录但进错工作区”或“Proxy 放过了，但服务端再兜底”的体验和安全漂移。
Output: shared actor resolution, role-aware workspace entry, explicit protected families, focused tests.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-project-level-auth-data-and-classroom-durability-closure/33-CONTEXT.md
@src/lib/auth/auth.ts
@src/lib/auth/auth.config.ts
@src/lib/dal/auth.ts
@src/actions/auth-actions.ts
@src/proxy.ts
@src/lib/dto/user.ts
@src/lib/dto/membership.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 对齐 Auth.js session truth、role vocabulary 与 workspace entry</name>
  <files>src/lib/auth/auth.ts, src/lib/auth/auth.config.ts, src/lib/dal/auth.ts, src/lib/dto/user.ts, src/lib/dto/membership.ts, src/actions/auth-actions.ts, src/lib/auth/auth.test.ts, src/actions/auth-actions.test.ts</files>
  <behavior>
    - Test 1: sign-in 后 teacher、student、admin 会进入 role-appropriate workspace，而 future roles 仅在 server code 中可建模，不暴露未完成 UI。
    - Test 2: shared actor DTO 或 membership resolution 不暴露 raw user row、password、provider token 等敏感字段。
  </behavior>
  <action>复用现有 Auth.js v5 配置与 Drizzle auth tables，补一个统一 actor resolution contract，让 `auth-actions`、DAL 与后续 guards 共用同一 role vocabulary。补齐 teacher、student、admin 当前入口，并在 membership or user DTO 中显式建模 `super_admin`、`school_admin`、`parent`、`developer`、`ai_agent` 这些 future roles，但不要把未完成 workflow 放进 UI。避免再让 sign-in action、session callback、DAL 各自推断角色。</action>
  <verify>
    <automated>pnpm test --run src/lib/auth/auth.test.ts src/actions/auth-actions.test.ts</automated>
  </verify>
  <done>session truth、角色建模与登录后入口已对齐，满足 `AUTH-01`、`AUTH-02`、`AUTH-03` 的上半段。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 收紧 `proxy.ts` 的 protected route families</name>
  <files>src/proxy.ts, src/lib/auth/auth.config.test.ts</files>
  <behavior>
    - Test 1: 未登录用户会被从 teacher、student、classroom、admin 与受保护 API family 重定向。
    - Test 2: public routes、runtime public assets 与 Next internals 不会被错误拦截。
  </behavior>
  <action>把当前过宽 matcher 与“全局放行再依赖页面内部兜底”的 posture，收口成显式 protected families truth。优先覆盖 `AUTH-04` 要求的 teacher、student、classroom、admin 和 API route families，并保持 Proxy 只处理 unauthenticated redirect，不在 Proxy 内塞入资源级授权逻辑。测试要锁住 public versus protected family 边界，避免后续 route 增长再次漂移。</action>
  <verify>
    <automated>pnpm test --run src/lib/auth/auth.config.test.ts</automated>
  </verify>
  <done>`proxy.ts` 已成为诚实的 route-family 保护层，而不是含糊 matcher。</done>
</task>

</tasks>

<verification>
- `pnpm test --run src/lib/auth/auth.test.ts src/actions/auth-actions.test.ts src/lib/auth/auth.config.test.ts`
</verification>

<risks>
- 现有登录流可能隐含依赖 teacher or student 二元角色，future role vocabulary 收口时要避免打破当前体验。
- `proxy.ts` 若 matcher 过宽，容易挡住公共页面；若过窄，又会继续留下受保护 family 漏网。
</risks>

<dependencies>
- 依赖 Phase 27-32 已稳定的 route 结构与 workspace 根路径。
- 为 33-02 的 DAL or Server Action scope 收紧提供单一 actor truth。
</dependencies>

<output>
After completion, create `.planning/phases/33-project-level-auth-data-and-classroom-durability-closure/33-01-SUMMARY.md`
</output>
