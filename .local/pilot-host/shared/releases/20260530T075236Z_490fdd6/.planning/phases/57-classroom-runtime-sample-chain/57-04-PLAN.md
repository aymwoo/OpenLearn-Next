---
phase: 57-classroom-runtime-sample-chain
plan: "04"
type: execute
wave: 4
depends_on:
  - 57-02
  - 57-03
files_modified:
  - src/lib/dal/classroom.ts
  - src/actions/classroom-actions.ts
  - src/components/classroom/classroom-control-panel.tsx
  - src/components/classroom/classroom-roster-panel.tsx
  - src/lib/dal/classroom.test.ts
  - src/actions/classroom-actions.test.ts
  - src/components/classroom/classroom-control-panel.test.tsx
  - src/components/classroom/classroom-roster-panel.test.tsx
autonomous: true
requirements:
  - CHAIN-05
  - PLUG-03
must_haves:
  truths:
    - "teacher 首屏先看到实时汇总与未完成名单，而不是实名明细或原始日志。"
    - "投票结束后结果视图冻结，但实名结果仍可按需展开。"
    - "提交失败或异常学生会出现在 teacher 可见的 failure/incomplete 区块。"
    - "当前 classroom shell 会暴露最小可执行 recovery actions（retry / reconcile / suspend / fallback），不需要人工改库。"
  artifacts:
    - path: src/lib/dal/classroom.ts
      provides: "voting aggregation / incomplete roster / teacher-visible failure consequence read model"
    - path: src/actions/classroom-actions.ts
      provides: "当前 voting round 的最小 recovery action entrypoints"
    - path: src/components/classroom/classroom-control-panel.tsx
      provides: "teacher result surface with folded named detail and recovery action tray"
    - path: src/components/classroom/classroom-roster-panel.tsx
      provides: "未完成名单与状态 token"
  key_links:
    - from: src/lib/dal/classroom.ts
      to: src/components/classroom/classroom-control-panel.tsx
      via: ClassroomSnapshotDTO current voting round fields
      pattern: "currentVotingRound"
    - from: src/lib/dal/classroom.ts
      to: src/components/classroom/classroom-roster-panel.tsx
      via: incomplete roster subset
      pattern: "未完成名单|remainingCount"
    - from: src/components/classroom/classroom-control-panel.tsx
      to: src/actions/classroom-actions.ts
      via: current-round recovery actions
      pattern: "retry|reconcile|suspend|fallback"
---

<objective>
把 voting round 的 authoritative aggregation、未完成名单和 teacher-visible failure consequences 做成 classroom shell 内的正式结果面板。

Purpose: 满足 D-57-13 / D-57-14 / D-57-15 / D-57-16 与 CHAIN-05 / PLUG-03，不让 teacher 只能读 raw log。
Output: DAL 聚合结果、control panel 内结果区块、roster incomplete section、当前 round recovery action tray，以及相关回归测试。
</objective>

<execution_context>
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/Develop/OpenLearn-Next/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-classroom-runtime-sample-chain/57-CONTEXT.md
@.planning/phases/57-classroom-runtime-sample-chain/57-RESEARCH.md
@.planning/phases/57-classroom-runtime-sample-chain/57-UI-SPEC.md
@.planning/phases/57-classroom-runtime-sample-chain/57-02-PLAN.md
@.planning/phases/57-classroom-runtime-sample-chain/57-03-PLAN.md
@.planning/phases/55-pilot-scope-and-acceptance-gate/55-FAILURE-RECOVERY-MATRIX.md

<interfaces>
From src/lib/dal/classroom.ts:
```ts
export async function getClassroomSnapshotDTO(input: { sessionId: string })
// already returns participants, monitoringSummary, steps, transportStatus, teacherTimeline
```

From src/components/classroom/classroom-control-panel.tsx:
```tsx
const currentSnapshot = conflict?.latest || liveSnapshot || initialSnapshot
const submittedRuntimeParticipants = currentSnapshot.participants.filter(
  (participant) => participant.runtimeProof?.status === 'submitted',
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为 teacher result aggregation / incomplete roster 补行为测试</name>
  <read_first>
    - src/lib/dal/classroom.test.ts
    - src/actions/classroom-actions.test.ts
    - src/components/classroom/classroom-control-panel.test.tsx
    - src/components/classroom/classroom-roster-panel.test.tsx
    - .planning/phases/57-classroom-runtime-sample-chain/57-CONTEXT.md
    - .planning/phases/57-classroom-runtime-sample-chain/57-UI-SPEC.md
    - .planning/phases/55-pilot-scope-and-acceptance-gate/55-FAILURE-RECOVERY-MATRIX.md
  </read_first>
  <files>src/lib/dal/classroom.test.ts, src/actions/classroom-actions.test.ts, src/components/classroom/classroom-control-panel.test.tsx, src/components/classroom/classroom-roster-panel.test.tsx</files>
  <action>新增失败用例覆盖 D-57-13 / D-57-14 / D-57-15 / D-57-16 与 PLUG-03：1) snapshot/read model 返回当前 voting round 的 aggregate counts、remaining count、named results folded flag、isFrozen；2) control panel 首屏顺序必须先看到 `实时汇总` 与 `未完成名单`，实名结果默认折叠；3) teacher-ended round 后结果冻结；4) 提交失败或状态异常人数会显示在结果区，而不是只留在 inspector link；5) 当前 classroom shell 会暴露最小 recovery actions（`重试同步` / `重新对账` / `暂停本轮` / `切换到课堂内回退处理`），并通过 server action / control chain 触发，而不是要求人工改库。测试不要只做 source includes，至少对 DTO 数据、可见文本和 action dispatch 做断言。</action>
  <acceptance_criteria>
    - `src/lib/dal/classroom.test.ts` 包含 aggregate/incomplete/frozen result 的 DTO 断言。
    - `src/actions/classroom-actions.test.ts` 包含 current-round recovery action dispatch / invalidation 断言。
    - `src/components/classroom/classroom-control-panel.test.tsx` 断言结果面板顺序与 folded named detail trigger。
    - `src/components/classroom/classroom-roster-panel.test.tsx` 断言未完成名单状态 token（`未提交` / `离线` / `重新连接中`）。
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/lib/dal/classroom.test.ts src/actions/classroom-actions.test.ts src/components/classroom/classroom-control-panel.test.tsx src/components/classroom/classroom-roster-panel.test.tsx</automated>
  </verify>
  <done>teacher result surface 的结构、排序和 failure consequence 有可执行红灯测试。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 authoritative voting result read model 与 teacher surface</name>
  <read_first>
    - src/lib/dal/classroom.ts
    - src/actions/classroom-actions.ts
    - src/components/classroom/classroom-control-panel.tsx
    - src/components/classroom/classroom-roster-panel.tsx
    - src/lib/dto/classroom.ts
    - .planning/phases/57-classroom-runtime-sample-chain/57-CONTEXT.md
    - .planning/phases/57-classroom-runtime-sample-chain/57-UI-SPEC.md
  </read_first>
  <files>src/lib/dal/classroom.ts, src/actions/classroom-actions.ts, src/components/classroom/classroom-control-panel.tsx, src/components/classroom/classroom-roster-panel.tsx</files>
  <action>在 `getClassroomSnapshotDTO()` 的 authoritative read model 中聚合当前 voting round：从 system `classroomEvidence` round artifact + latest quiz/task submissions + runtime proof/failure rows 计算 `optionCounts`, `optionPercentages`, `submittedCount`, `remainingCount`, `incompleteStudents[]`, `namedResults[]`, `failureCount`, `isFrozen`, `roundStatus`。`incompleteStudents[]` 需给出最小状态 token（`未提交` / `离线` / `重新连接中`），并把 failed runtimeProof 计入 teacher-visible consequence（per PLUG-03）。同时在 `src/actions/classroom-actions.ts` 为当前 round 增加最小 recovery entrypoints，至少覆盖 `retry`, `reconcile`, `suspend`, `fallback` 四类动作，且全部走现有 classroom control / server action / cache invalidation 主链，不新增独立 operator 页面。`classroom-control-panel.tsx` 在现有 shell 内新增结果区块与 recovery action tray，顺序固定为 `实时汇总` → `未完成名单` → `实名结果` 折叠区 → `恢复动作`；投票进行中实时刷新，teacher 结束后显示 `本轮已结束` 并冻结数值（per D-57-16）。`classroom-roster-panel.tsx` 复用同一 read model，把未完成名单做成独立列表/empty state，不新增 standalone result page 或表格化重布局。</action>
  <acceptance_criteria>
    - `getClassroomSnapshotDTO()` 返回的当前 voting round 聚合不依赖浏览器端重放 raw events。
    - `classroom-control-panel.tsx` 有可见的 `实时汇总`、`未完成名单`、`展开实名结果`/`收起实名结果` 文案，且实名结果默认折叠。
    - `classroom-control-panel.tsx` 有可见的 recovery actions 文案，且动作仍走 `classroom-actions.ts` 而不是浏览器直改状态。
    - `classroom-roster-panel.tsx` 在所有学生都已提交时显示 `全班已提交，可由老师决定何时结束本轮投票。`，否则显示独立未完成名单区块。
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/lib/dal/classroom.test.ts src/actions/classroom-actions.test.ts src/components/classroom/classroom-control-panel.test.tsx src/components/classroom/classroom-roster-panel.test.tsx</automated>
  </verify>
  <done>teacher/operator 能在既有 classroom shell 里直接看到可信 aggregate、未完成名单、异常后果与最小 recovery actions，不再依赖 raw log/inspector 或人工改库才能控场。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite read model → teacher classroom UI | teacher 结果面板必须只消费 authoritative aggregation，而不是 client 端拼 raw events |
| runtime failure rows → teacher-visible consequence | failure taxonomy 要在 teacher 面板诚实暴露，但不提前进入 operator surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-07 | I | `getClassroomSnapshotDTO()` aggregation | mitigate | 所有 aggregate / incomplete / named detail 统一从 DAL read model 计算，不允许浏览器重放 raw classroom events。 |
| T-57-08 | D | teacher result surface | mitigate | 把 failureCount 与 incomplete roster 置于主视图，避免异常仅藏在 inspector 链接里导致 teacher 无法及时干预。 |
| T-57-09 | R | frozen result view | mitigate | round closed 后根据 durable close artifact 固定 `isFrozen=true` 与 `roundStatus="closed"`，阻止晚到提交覆盖 teacher 结果。 |
</threat_model>

<verification>
- 运行 `pnpm vitest run src/lib/dal/classroom.test.ts src/components/classroom/classroom-control-panel.test.tsx src/components/classroom/classroom-roster-panel.test.tsx`
- 手动确认 control panel 结果顺序为 aggregate → incomplete → folded named detail
- 手动确认 recovery action tray 只作用于当前 classroom session / current voting round
</verification>

<success_criteria>
- teacher 首屏看的是全局汇总与未完成名单。
- round closed 后结果冻结，实名结果仍可按需展开。
- failure consequence 在 teacher 课堂面板可见，不需要先打开 inspector。
- 当前课堂内存在最小可执行 retry / reconcile / suspend / fallback 入口，不要求人工改库。
</success_criteria>

<output>
After completion, create `.planning/phases/57-classroom-runtime-sample-chain/57-04-SUMMARY.md`
</output>
