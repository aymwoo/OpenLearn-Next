---
phase: 39-async-contracts-and-durable-task-truth
plan: 03
type: execute
wave: 3
depends_on:
  - 39-01
  - 39-02
files_modified:
  - src/features/async-tasks/server/enqueue.ts
  - src/features/async-tasks/server/enqueue.test.ts
  - src/actions/async-task-actions.ts
  - src/actions/async-task-actions.test.ts
  - src/features/async-tasks/index.ts
  - scripts/verify-phase39-async-tasks.ts
  - package.json
autonomous: true
requirements:
  - ATP-02
must_haves:
  truths:
    - Server-side code can create durable async task records only through one platform enqueue seam.
    - Creating a task record does not automatically claim queue dispatch success; honest intermediate states are preserved.
    - Phase 39 has a dedicated verification gate that proves contracts, ledger reads, and enqueue boundary discipline.
  artifacts:
    - path: src/features/async-tasks/server/enqueue.ts
      provides: Canonical enqueue orchestration seam
    - path: src/actions/async-task-actions.ts
      provides: Server Action example that enters the platform seam instead of touching queue state directly
    - path: scripts/verify-phase39-async-tasks.ts
      provides: Phase-specific static guard and focused suite gate
  key_links:
    - from: src/actions/async-task-actions.ts
      to: src/features/async-tasks/server/enqueue.ts
      via: Server Action delegates to canonical enqueue seam
      pattern: enqueueAsyncTask
    - from: scripts/verify-phase39-async-tasks.ts
      to: package.json
      via: verify command registration
      pattern: verify:phase39
---

<objective>
建立统一 application enqueue boundary，并给 Phase 39 增加 dedicated verifier，证明 ATP-02 已被锁定为平台规则而不是口头约定。

Purpose: 把“只能通过一个 enqueue seam 创建 durable task + intent”变成可执行边界，为后续真实 workload 和 BullMQ worker 接入铺路。
Output: enqueue seam、server-action integration example、focused tests、`verify:phase39` gate。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-async-contracts-and-durable-task-truth/39-CONTEXT.md
@.planning/phases/39-async-contracts-and-durable-task-truth/39-RESEARCH.md
@.planning/phases/39-async-contracts-and-durable-task-truth/39-PATTERNS.md
@.planning/phases/39-async-contracts-and-durable-task-truth/39-01-PLAN.md
@.planning/phases/39-async-contracts-and-durable-task-truth/39-02-PLAN.md

<interfaces>
From src/actions/course-authoring-actions.ts:
```ts
const parsed = CourseCreateInputSchema.safeParse(normalized);
if (!parsed.success) return validationError();

const actor = await assertActiveTeacher();
const course = await createCourseForTeacherScoped(parsed.data);
```

From src/features/runtime-platform/seams/transport/gateway.ts:
```ts
const [attempt] = await db.insert(transportDeliveryAttempts).values({
  truthPersisted: event.truthPersisted,
  deliveryAttempted: Boolean(adapter),
  attemptStatus: adapter ? "pending" : "skipped",
}).returning();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 canonical enqueue seam 与 server-side integration example</name>
  <files>src/features/async-tasks/server/enqueue.ts, src/features/async-tasks/server/enqueue.test.ts, src/actions/async-task-actions.ts, src/actions/async-task-actions.test.ts, src/features/async-tasks/index.ts</files>
  <read_first>src/features/runtime-platform/seams/transport/gateway.ts, src/actions/course-authoring-actions.ts, src/features/async-tasks/shared/contract.ts, src/features/async-tasks/shared/dto.ts, src/lib/dal/async-tasks.ts</read_first>
  <behavior>
    - Test 1: canonical enqueue seam persists a durable task record with pending/dispatch posture before any future queue integration.
    - Test 2: if dispatch is not yet available, seam returns honest intent state instead of pretending the task is queued per D-39-09 to D-39-11.
    - Test 3: server action delegates into the seam and never imports DB or BullMQ clients directly per D-39-12.
  </behavior>
  <action>创建 `src/features/async-tasks/server/enqueue.ts`，导出 canonical `enqueueAsyncTask()`（或同义命名）入口：输入必须先经 registry/task definition parse，然后在 SQLite 中创建主任务记录和 enqueue intent/event，返回 durable task DTO；此函数只负责 Phase 39 的 truth write + honest status，不接真正的 BullMQ Queue.add。若实现里需要 dispatch stub，也必须明确返回 `pending_enqueue` / `dispatching` / `dispatch_failed` 之一，不能把 durable record 直接标成 queued per D-39-09 至 D-39-11。创建 `src/actions/async-task-actions.ts` 作为最小 server-side integration example，沿用 `safeParse -> assert actor -> call seam -> return action result` 模式，证明 server action 通过统一平台 seam 入场而不是直接触达 queue/DB；该 action 可以是 developer-only / internal bootstrap example，但不能变成新的真实 workload。更新 `src/features/async-tasks/index.ts` 以 re-export enqueue seam。先写 enqueue/action tests，再写实现直到通过。</action>
  <acceptance_criteria>
    - `src/features/async-tasks/server/enqueue.ts` exports a canonical enqueue function.
    - Enqueue seam source contains durable task create/intention write before any future queue integration point.
    - `src/actions/async-task-actions.ts` does not import `@/db` or `bullmq`.
    - `src/actions/async-task-actions.test.ts` asserts the action delegates to the async-task seam and returns honest intermediate status.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/features/async-tasks/server/enqueue.test.ts src/actions/async-task-actions.test.ts</automated>
  </verify>
  <done>服务器侧已存在唯一 enqueue boundary；后续真实 feature server orchestrator 只能通过该 seam 创建 durable async task truth。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 添加 verify:phase39 gate，锁住 contract / ledger / enqueue seam</name>
  <files>scripts/verify-phase39-async-tasks.ts, package.json, src/features/async-tasks/server/enqueue.test.ts, src/lib/dal/async-tasks.test.ts</files>
  <read_first>scripts/verify-phase15-course-import.ts, scripts/verify-phase37-redis-fanout.ts, package.json, src/features/async-tasks/server/enqueue.ts, src/lib/dal/async-tasks.ts</read_first>
  <behavior>
    - Test 1: verifier fails if package.json is missing `verify:phase39`.
    - Test 2: verifier fails if async task contracts lose required visibility/entity/progress/result tokens.
    - Test 3: verifier runs focused tests and typecheck for the new platform slice.
  </behavior>
  <action>创建 `scripts/verify-phase39-async-tasks.ts`，沿用仓库 phase verifier 结构：先做 static source guards，再执行 focused Vitest suites，必要时串 `pnpm typecheck`。静态 guard 至少要检查以下事实：`src/features/async-tasks/shared/contract.ts` 存在 required platform schemas；`src/db/schema.ts` 存在 async task ledger/event tables；`src/features/async-tasks/server/enqueue.ts` 是 canonical enqueue seam；`src/actions/async-task-actions.ts` 不直接 import DB 或 BullMQ；honest enqueue vocabulary 仍保留 `pending_enqueue` / `dispatch_failed` 或等价 token；SQLite truth 读取存在于 `src/lib/dal/async-tasks.ts`。在 `package.json` 注册 `verify:phase39` 指向该脚本。focused suite 至少运行 async-task contract、DAL、enqueue、action 相关测试，并串 `pnpm typecheck`。不要把 Phase 40 的 worker/BullMQ runtime smoke 混进此 verifier。</action>
  <acceptance_criteria>
    - `scripts/verify-phase39-async-tasks.ts` exists and contains static checks plus focused suite execution.
    - `package.json` contains `"verify:phase39"` and references the new verifier script.
    - Verifier checks for async task tables, contract schemas, canonical enqueue seam, and action boundary discipline.
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase39</automated>
  </verify>
  <done>Phase 39 交付声明已经可执行：任何 contract drift、schema drift 或 enqueue boundary 漏洞都能被单一 verifier 立即发现。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server action input → enqueue seam | 未信任输入进入平台，必须先 parse 和 actor-scope 验证 |
| enqueue seam → durable truth tables | platform writes must preserve honest intermediate state and ownership fields |
| verifier → source tree | verification gate must detect direct DB/queue shortcuts before execution phases build on them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-07 | S | `src/actions/async-task-actions.ts` | mitigate | Action first parses input and asserts actor scope before calling enqueue seam |
| T-39-08 | T | `src/features/async-tasks/server/enqueue.ts` | mitigate | Seam persists durable record + intent before later dispatch integration and preserves honest failure states |
| T-39-09 | R | `scripts/verify-phase39-async-tasks.ts` | mitigate | Dedicated verifier records canonical source checks and focused suite output so boundary drift is auditable |
</threat_model>

<verification>
- `pnpm test --run src/features/async-tasks/server/enqueue.test.ts src/actions/async-task-actions.test.ts`
- `pnpm verify:phase39`
</verification>

<success_criteria>
- ATP-02 被具体化为单一 enqueue seam，而不是团队约定。
- Server Action integration example 证明 UI/route 不会直接碰 queue 或 DB shortcut。
- `verify:phase39` 成为后续执行与验收的唯一 phase gate。
</success_criteria>

<output>
After completion, create `.planning/phases/39-async-contracts-and-durable-task-truth/39-03-SUMMARY.md`
</output>
