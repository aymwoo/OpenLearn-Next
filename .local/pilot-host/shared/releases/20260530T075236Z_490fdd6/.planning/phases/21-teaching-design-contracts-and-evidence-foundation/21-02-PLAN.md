---
phase: 21-teaching-design-contracts-and-evidence-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - src/db/schema.ts
  - src/lib/dto/classroom.ts
  - src/lib/dal/classroom.ts
  - src/lib/dal/classroom.test.ts
  - src/actions/classroom-actions.ts
  - src/actions/classroom-actions.test.ts
autonomous: true
requirements:
  - EVAL-03
must_haves:
  truths:
    - 课堂 session 会持久化 presence、intervention 与 evidence timeline，而不是只留在 SSE/UI 内存里。
    - 学生 presence touch 与后续 evidence write 通过 DAL + Server Actions 落库，并显式刷新 session cache。
    - intervention/evidence 合同保持 teacher-scoped 或 participant-scoped，不引入客户端直连 DB。
  artifacts:
    - path: src/db/schema.ts
      provides: durable classroom evidence and timeline tables with cascade ownership
      contains: classroomEvidence
    - path: src/lib/dto/classroom.ts
      provides: typed evidence/intervention input and timeline DTO contracts
      contains: ClassroomTimelineEntryDTOSchema
    - path: src/actions/classroom-actions.ts
      provides: server actions for evidence/intervention writes and presence timeline refresh
      contains: recordClassroomEvidenceAction
  key_links:
    - from: src/actions/classroom-actions.ts
      to: src/lib/dal/classroom.ts
      via: server actions validate input, delegate writes, then update classroom session cache
      pattern: updateTag\(cacheTags\.classroom
    - from: src/lib/dal/classroom.ts
      to: src/db/schema.ts
      via: presence/evidence/intervention writes use new classroom timeline tables under session ownership
      pattern: classroomTimeline|classroomEvidence
---

<objective>
为 classroom session 补齐 durable evidence foundation：presence 变化、教师 intervention、
课堂证据都要有持久化 contract，供后续 runtime 评估与 recap 直接复用。

Purpose: 提前把 EVAL-03 的数据真相落地，避免后续 formative evaluation 和 analytics
阶段继续从 transient UI state 反推课堂过程。
Output: classroom evidence/timeline schema、DAL 写路径、server actions 与回归测试。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-teaching-schedule-os/18-01-SUMMARY.md
@.planning/phases/18-teaching-schedule-os/18-03-SUMMARY.md
@.planning/phases/18-teaching-schedule-os/18-04-SUMMARY.md
@src/db/schema.ts
@src/lib/dto/classroom.ts
@src/lib/dal/classroom.ts
@src/actions/classroom-actions.ts
@src/lib/cache-policy.ts

<interfaces>
From `src/lib/dto/classroom.ts`:
```ts
export const TouchClassroomPresenceInputSchema = z.object({
  sessionId: z.string().min(1),
  connectionState: ClassroomConnectionStateSchema,
  currentStepId: z.string().nullable().optional(),
})

export const ClassroomEventDTOSchema = z.object({
  id: z.string(),
  sessionId: z.string(),
  version: z.number().int(),
  type: z.enum(["launched", "active_step_changed", "lock_mode_changed", "slide_changed", "snapshot_refreshed", "ended"]),
  actorId: z.string(),
  payload: z.unknown(),
  createdAt: z.string(),
})
```

From `src/actions/classroom-actions.ts`:
```ts
export async function touchClassroomPresenceAction(...) {
  const parsed = TouchClassroomPresenceInputSchema.safeParse(...)
  await updateClassroomParticipantConnection(...)
  updateTag(cacheTags.classroom(parsed.data.sessionId))
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义 classroom evidence 与 timeline persistence contract</name>
  <files>src/db/schema.ts, src/lib/dto/classroom.ts, src/lib/dal/classroom.ts, src/lib/dal/classroom.test.ts</files>
  <behavior>
    - Test 1: presence 更新会写入 durable timeline entry，而不只是更新 participant 当前状态。
    - Test 2: teacher intervention 会落为独立 timeline/event 记录，并带 sessionId、stepId、studentId/target scope。
    - Test 3: evidence 记录按 session ownership cascade delete，并能和课堂 step 关联。
  </behavior>
  <action>在 `src/db/schema.ts` 新增两类 classroom-owned 持久化对象：其一是结构化 `classroomEvidence`（至少包含 `sessionId`、`studentId`、`stepId`、`sourceType`、`evidenceType`、`payloadJson`、`capturedById`、`createdAt`）；其二是统一 `classroomTimeline`（至少包含 `sessionId`、`studentId?`、`stepId?`、`entryType`、`actorId`、`payloadJson`、`createdAt`），所有外键都使用 `onDelete: cascade`。在 `src/lib/dto/classroom.ts` 增加 evidence/intervention/timeline 的 input 与 DTO schema，其中 intervention 严格按 D-14 / D-15 建模为 `title + body + targetScope + studentId? + stepId?`，默认仅教师内部可见；evidence contract 继续围绕 typed payload，而不是自然语言长文。于 `src/lib/dal/classroom.ts` 增加 `recordClassroomEvidence()`、`recordClassroomIntervention()`，并让 `updateClassroomParticipantConnection()` 在 connectionState 或 currentStepId 变化时写入 presence timeline。按 the agent's discretion 统一 timeline `entryType` 为稳定枚举，例如 `presence_changed`、`evidence_captured`、`intervention_noted`。不要把这些记录塞回 SSE memory，也不要让 UI 直接拼 SQL。</action>
  <verify>
    <automated>pnpm test --run src/lib/dal/classroom.test.ts</automated>
  </verify>
  <done>classroom session 已经拥有 durable evidence/timeline 真相源，presence、evidence、intervention 不再只能从当前 snapshot 猜测。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 通过 classroom Server Actions 暴露 evidence 与 intervention 写入口</name>
  <files>src/actions/classroom-actions.ts, src/actions/classroom-actions.test.ts</files>
  <behavior>
    - Test 1: evidence/intervention action 都先走 Zod schema，再进入 DAL。
    - Test 2: 写入成功后会 `updateTag(cacheTags.classroom(sessionId))`，保持 runtime read-your-writes。
    - Test 3: 未授权 actor 无法借由 action 绕过 teacher/participant scope。
  </behavior>
  <action>在 `src/actions/classroom-actions.ts` 新增 `recordClassroomEvidenceAction` 与 `recordClassroomInterventionAction`，分别调用新的 classroom DAL 写路径，并复用当前 `ActionResult` 风格返回结构化错误。学生 evidence write 只允许当前登录学生为自己记录；teacher intervention 只允许 classroom teacher 记录，且必须接受 D-14 的 `title + body` 和 D-15 的 `targetScope`（单学生或全班）输入。所有成功写入都必须显式 `updateTag(cacheTags.classroom(sessionId))`。同步扩展 `src/actions/classroom-actions.test.ts`，从现有 presence action 测试风格继续验证 schema gate、DAL wiring、冲突/未授权错误映射和 cache invalidation。不要创建 route handler，也不要把写逻辑塞进 client component。</action>
  <verify>
    <automated>pnpm test --run src/actions/classroom-actions.test.ts</automated>
  </verify>
  <done>Phase 21 的 evidence foundation 已通过 Server Actions 可写、可鉴权、可失效缓存，为后续 student runtime 和 teacher evaluation 页面提供稳定服务端入口。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| student/teacher action input -> classroom DAL | 未受信输入进入课堂证据与干预写链路 |
| classroom DAL -> SQLite classroom evidence tables | 服务端持久化结果成为后续评估与分析的事实来源 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | T | `src/actions/classroom-actions.ts` | mitigate | evidence/intervention action 必须先用 Zod schema 校验，再调用 DAL。 |
| T-21-06 | E | `src/lib/dal/classroom.ts` | mitigate | intervention 写路径仅允许 classroom teacher；student evidence 仅允许当前参与学生为自己写入。 |
| T-21-07 | R | `src/db/schema.ts` | mitigate | evidence 与 timeline 记录包含 actorId、sessionId、stepId 和 createdAt，保证课堂过程可审计。 |
| T-21-08 | D | `src/actions/classroom-actions.ts` | mitigate | 每次成功写入都 `updateTag(cacheTags.classroom(sessionId))`，避免 runtime surface 继续读旧 snapshot。 |
</threat_model>

<verification>
- `pnpm test --run src/lib/dal/classroom.test.ts`
- `pnpm test --run src/actions/classroom-actions.test.ts`
</verification>

<success_criteria>
- presence、intervention、evidence 都有 durable session-owned persistence contract。
- classroom 写操作继续通过 DAL + Server Actions，且显式刷新课堂 cache。
- 后续 evaluation/analytics phase 可以直接复用这些数据，不必再补 UI-only 采集链路。
</success_criteria>

<output>
After completion, create `.planning/phases/21-teaching-design-contracts-and-evidence-foundation/21-02-SUMMARY.md`
</output>
