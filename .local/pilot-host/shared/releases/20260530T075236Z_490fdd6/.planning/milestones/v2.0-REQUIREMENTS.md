# Requirements Archive: v2.0 Runtime Platform Foundations

**Archived:** 2026-05-17
**Status:** ARCHIVED (Phase 27-32 complete; project-level safety and course membership gaps remain outside this milestone close)

For current requirements, see `.planning/REQUIREMENTS.md`.

## Archive note

- 这份 archive 只用于记录 `v2.0 Runtime Platform Foundations` 的真实 close scope：Phase 27-32。
- 本 milestone 已完成 runtime foundation、governance、transport boundary 与 end-to-end proof path。
- `AUTH-01` ~ `AUTH-06`、`DATA-01` ~ `DATA-05`、`CLASS-05`、`COURSE-07` 仍是项目级 gap，不因本次归档自动变为 complete。

## Archived milestone requirements

- [x] **SAFE-01**: 兼容性回归证明现有 teacher authoring、publish、launch、student player 和 classroom control 流程在 V2 边界改动后仍可用。
- [x] **SAFE-02**: 新 runtime 和 plugin host action 在外部执行前继续遵守既有 server-side authz、school scope 与 DTO shaping 规则。
- [x] **SAFE-03**: runtime 与 classroom pilot state 已具备 durable truth，不再只依赖 SSE 或内存态。
- [x] **ARCH-01**: runtime-platform 能力通过显式 feature public APIs 和 compatibility re-export 暴露。
- [x] **ARCH-02**: bridge、event、permissions、descriptors 已具备 extracted shared contract seam。
- [x] **ARCH-03**: PostgreSQL、Redis/Event Bus、WebSocket 已建立 future seam，但未作为本 milestone cutover 条件。
- [x] **BRDG-01** ~ **BRDG-04**: versioned runtime descriptor、TeachingBridge schema、bootstrap capability context、typed result envelope 已落地。
- [x] **RTSE-01** ~ **RTSE-04**: runtime session persistence、canonical event log、host-side write path 与 cache invalidation discipline 已落地。
- [x] **RHOST-01** ~ **RHOST-04**: shared Runtime Host、sandboxed iframe、built-in HTML runtime authoring 与 end-to-end interaction-submit proof 已落地。
- [x] **GOVR-01** ~ **GOVR-04**: capability gate、manifest v2、lifecycle state 与 inspector audit timeline 已落地。
- [x] **TRNS-01** ~ **TRNS-02**: transport boundary 与 durable truth ownership 已落地。
