---
phase: 60.1-close-gap-pilot-03-load-01-load-02-replace-dry-run-phase60-p
plan: "03"
subsystem: local-proof-isolation
tags: [phase60, local-proof, sqlite, verifier, blocker-removal]
completed: 2026-05-29
---

# Phase 60.1 Plan 03 Summary

## Accomplishments

- 在 `package.json` 中新增 `verify:phase60:local`，明确把 repo-local substitute lane 从原始 `verify:phase60` 分离出来。
- 在 `scripts/verify-phase60-load-and-rehearsal.ts` 中新增 shared-local-db preflight：当 `PHASE60_BASE_URL` 指向 `localhost/127.0.0.1` 且 `DB_FILE_NAME=file:local.db` 时，直接 fail closed 并抛出 `PHASE60_LOCAL_DB_SHARED_WITH_APP: rerun pnpm verify:phase60:local`。
- 扩展 `scripts/verify-phase60-load-and-rehearsal.test.ts`，锁定 `verify:phase60:local` package script 契约与 shared `local.db` blocker 行为，同时保留原有 machine-readable evidence contract 与 dry-run rejection 断言。
- 新增 `scripts/verify-phase60-local.ts`，把源 SQLite truth 复制到 `/tmp/opencode/phase60-local-proof/local.db`，再在 isolated proof DB 上执行 canonical verifier。
- 在 wrapper 中对 copied DB 的 migration metadata 缺口做最小兜底：若 `prepareDevDb()` 只因 `drizzle/meta/_journal.json` 缺失已检测 tag 而失败，则保留已复制的 truth schema 并继续 verifier，而不扩大成通用 migration 流程重构。
- 更新 `.planning/debug/phase60-local-sqlite-busy.md`，把根因、修复、验证路径和 rerun 命令补齐到 resolved 状态。

## Verification

- `pnpm exec vitest --run scripts/verify-phase60-load-and-rehearsal.test.ts`
- `pnpm exec eslint scripts/verify-phase60-load-and-rehearsal.ts scripts/verify-phase60-load-and-rehearsal.test.ts scripts/verify-phase60-local.ts`
- `DB_FILE_NAME=file:local.db PHASE60_BASE_URL=http://127.0.0.1:3000 OPENLEARN_SHARED_ROOT=/tmp/shared OPENLEARN_CURRENT_ROOT=/tmp/current OPENLEARN_HEALTHCHECK_BASE_URL=http://127.0.0.1:3000 pnpm verify:phase60`
- `pnpm verify:phase60:local`

## Notes

- `verify:phase60` 的 repo-local shared-db posture 现在会 fail closed；不会再把与本地 web/worker 共用的 `file:local.db` 当作可接受的 Phase 60 proof lane。
- `verify:phase60:local` 已经能在 isolated SQLite copy 上进入 canonical verifier，当前剩余 blocker 已回到预期的 live target env 要求，而不再是 shared SQLite lock 或 copied DB metadata 噪音。
- 该计划仍是 support/blocker-removal only；`PILOT-03` / `LOAD-01` / `LOAD-02` 的 live evidence closeout 仍由 `60.1-01` 完成。
