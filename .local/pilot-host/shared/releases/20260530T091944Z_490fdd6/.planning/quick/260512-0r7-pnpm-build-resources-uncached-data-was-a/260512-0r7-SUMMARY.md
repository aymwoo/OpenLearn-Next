---
phase: quick
plan: 260512-0r7
status: complete
---

# Quick summary

已完成：修复 `pnpm build` 时 `/resources` 路由触发的 `Uncached data was accessed outside of <Suspense>`。

- 将 `src/app/(library)/resources/layout.tsx` 改为同步 layout，并在内部使用显式 `Suspense` 包裹异步内容路径。
- 为 `/resources` 增加静态 `ResourcesShellFallback`，复用 `TeacherSidebarShellFrame` + `DEFAULT_THEME_LAYOUT_RUNTIME`，避免 fallback 阶段读取 theme/runtime 动态数据。
- 保持全局 `ToastProvider` 和现有 `useToast()` 调用方式不变；修复点仅收敛在 `/resources` 路由边界。

验证：

- `pnpm build` 不再报 `/resources` 的 blocking route；构建继续推进后，当前剩余失败点变为 `/teacher/courses/[courseId]`，堆栈指向 `src/components/shell/sidebar.tsx:36` 的 `usePathname()`，与本次 `/resources` 修复无关。
