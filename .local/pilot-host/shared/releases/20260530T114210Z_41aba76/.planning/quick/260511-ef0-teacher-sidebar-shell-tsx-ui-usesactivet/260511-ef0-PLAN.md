---
phase: quick-260511-ef0-teacher-sidebar-shell-tsx-ui-usesactivet
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/theme-layout/shell-surface-resolver.ts
  - src/lib/theme-layout/shell-surface-resolver.test.ts
  - src/components/shell/teacher-sidebar-shell.tsx
  - src/components/shell/teacher-sidebar-shell.test.tsx
autonomous: true
requirements:
  - PH19-shell-metadata-contract
must_haves:
  truths:
    - "TeacherSidebarShellFrame 从集中 resolver 读取 theme state、shell mode、route mode 与 surface variant 的 UI 决策，不再在 JSX 内分散推导多组布尔值与嵌套三元表达式。"
    - "默认主题与 active-theme 在 /teacher 及共享教师壳层上的视觉输出保持现状。"
    - "shell state 的关键组合拥有自动化测试，回归时能直接定位 resolver 或 shell 消费层。"
  artifacts:
    - path: "src/lib/theme-layout/shell-surface-resolver.ts"
      provides: "集中 shell UI state resolver 与类型"
    - path: "src/components/shell/teacher-sidebar-shell.tsx"
      provides: "消费集中 resolver 的简化 shell 渲染"
    - path: "src/lib/theme-layout/shell-surface-resolver.test.ts"
      provides: "状态组合测试"
    - path: "src/components/shell/teacher-sidebar-shell.test.tsx"
      provides: "shell 集成约束测试"
  key_links:
    - from: "src/components/shell/teacher-sidebar-shell.tsx"
      to: "src/lib/theme-layout/shell-surface-resolver.ts"
      via: "集中 UI state 解析"
      pattern: "getShellSurfaceConfig|resolve.*Shell.*State"
    - from: "src/lib/theme-layout/shell-surface-resolver.test.ts"
      to: "src/lib/theme-layout/shell-surface-resolver.ts"
      via: "组合测试矩阵"
      pattern: "describe\(\"shell surface resolver\""
---

<objective>
把 TeacherSidebarShell 的 UI 状态决策集中到 resolver，减少 JSX 中的
ternary nesting 和条件爆炸，同时保持 Phase 19 已交付的 shell 行为与
视觉不变。

Purpose: 让后续新增 shell 组合时先扩展 resolver 与测试，而不是继续把
theme / route / shell 条件堆回组件。
Output: 一个集中 shell UI state resolver、一次最小化 shell 组件重构、
以及覆盖关键状态组合的回归测试。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./AGENTS.md
@src/components/shell/teacher-sidebar-shell.tsx
@src/lib/theme-layout/shell-surface-resolver.ts
@src/lib/theme-layout/shell-surface-resolver.test.ts
@src/components/shell/teacher-sidebar-shell.test.tsx

<interfaces>
From src/components/shell/teacher-sidebar-shell.tsx:
```ts
type TeacherSidebarShellFrameProps = TeacherSidebarShellProps & {
  shellVariant?: ThemeShellConfig["mode"];
  shellConfig?: ThemeShellConfig;
  surfaceMetadata?: ShellSurfaceMetadata;
  themeSource?: "default" | "active-theme";
};
```

From src/lib/theme-layout/shell-surface-resolver.ts:
```ts
export type ShellVariant = ThemeShellMode;

export function getShellSurfaceConfig(input: {
  routeKey: TeacherThemeRouteKey;
  layoutRuntime: ThemeLayoutRuntime;
}): {
  shellVariant: ShellVariant;
  shellConfig: ThemeShellConfig;
  surfaceMetadata: ShellSurfaceMetadata;
};
```

Current JSX-local decisions to absorb into resolver:
```ts
const usesActiveThemeShell = themeSource === "active-theme";
const isSquareShell = shellConfig.radius === "square";
const isFullWidthShell = shellConfig.width === "full-width";
const isImmersiveChrome = shellConfig.chrome === "immersive";
const secondaryNavVisible = surfaceMetadata.regions.some(...);
const contextPanelVisible = surfaceMetadata.regions.some(...);
const pageFooterVisible = surfaceMetadata.regions.some(...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 提炼集中 shell UI state resolver</name>
  <files>src/lib/theme-layout/shell-surface-resolver.ts, src/lib/theme-layout/shell-surface-resolver.test.ts</files>
  <behavior>
    - Test 1: active-theme + square + full-width + immersive 组合解析出当前 /teacher 主页所需的 wrapper、main、header、content 决策。
    - Test 2: default theme + rounded + default width + default chrome 组合保持共享教师壳层决策。
    - Test 3: secondary-nav、context-panel、page-footer 的可见性由 metadata regions 决定，而不是 JSX 另行推导。
  </behavior>
  <action>在现有 `getShellSurfaceConfig` 旁新增或扩展一个纯函数 resolver，输入限定为 `themeSource`、`shellVariant`/`shellConfig`、`surfaceMetadata`，输出一个 typed 的 shell UI state 结果对象。把 `usesActiveThemeShell`、`isSquareShell`、`isFullWidthShell`、`isImmersiveChrome`、各 region visibility，以及 shell/header/main/content/footer/context-panel 需要的 className 或显式 flags 统一收口到这里。不要新增任何 `routeKey === ...` 形式的 UI 分支；继续遵守 Phase 19 的 metadata-first 合同，让 `/teacher` 的 square/full-width/immersive 行为仍只来自 runtime metadata。</action>
  <verify>
    <automated>pnpm vitest run src/lib/theme-layout/shell-surface-resolver.test.ts</automated>
  </verify>
  <done>resolver 文件导出单一可复用的 shell UI state 类型与函数，能够完整表达当前默认主题与 active-theme 两条渲染路径。</done>
</task>

<task type="auto">
  <name>Task 2: 让 TeacherSidebarShellFrame 只消费 resolver 结果</name>
  <files>src/components/shell/teacher-sidebar-shell.tsx</files>
  <action>重构 `TeacherSidebarShellFrame`，把当前 JSX 顶部的 theme/shell/route/surface 布尔计算与嵌套 className ternary 改为读取 Task 1 的集中 resolver 结果。保留现有 props contract、`data-theme-*` / `data-region` 标记、`AuroraShell` 包裹关系、header/footer/context panel DOM 位置与文案，不做视觉改版，也不要顺手改其它 Phase 19 之外的结构。允许保留必要的 `headerActions ? ... : null` 这类简单渲染条件，但 UI state 决策本身不得继续散落在 JSX 内。</action>
  <verify>
    <automated>pnpm vitest run src/components/shell/teacher-sidebar-shell.test.tsx</automated>
  </verify>
  <done>`teacher-sidebar-shell.tsx` 中不再内联维护多组 shell 状态布尔与深层三元表达式，组件主要职责变成渲染 resolver 已决定的状态。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 补齐状态组合与集成回归测试</name>
  <files>src/lib/theme-layout/shell-surface-resolver.test.ts, src/components/shell/teacher-sidebar-shell.test.tsx</files>
  <behavior>
    - Test 1: resolver 测试覆盖 default vs active-theme、square vs rounded、full-width vs default、immersive vs default chrome 的代表性组合。
    - Test 2: shell 集成测试证明组件消费 resolver，而不是回退到 route string branching 或复制旧布尔逻辑。
    - Test 3: 回归时能区分 resolver 合同破坏与 shell 渲染接线破坏。
  </behavior>
  <action>扩展现有测试，优先把“状态组合正确性”放进 `shell-surface-resolver.test.ts`，使用最小 fixture builder 基于 `DEFAULT_THEME_LAYOUT_RUNTIME` 构造组合；`teacher-sidebar-shell.test.tsx` 只保留壳层接线与反回归约束，例如仍走 shared resolver、仍无 `routeKey === "/teacher"` 这类 route-string branching、仍保留关键 data attributes。不要引入大体量快照测试，也不要把测试重心放到脆弱的完整源码字符串比对。</action>
  <verify>
    <automated>pnpm vitest run src/lib/theme-layout/shell-surface-resolver.test.ts src/components/shell/teacher-sidebar-shell.test.tsx</automated>
  </verify>
  <done>测试在不改变 UI 的前提下覆盖关键状态组合，后续若有人把状态判断重新塞回 JSX 或破坏 metadata contract，会得到明确失败信号。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| theme runtime → resolver | 主题运行时元数据进入 UI state 决策层，若分散实现会导致同一 metadata 被不同 JSX 条件解释成不同视觉结果。 |
| resolver → shell render | 集中状态对象进入渲染层，若字段含义不清或缺少测试，容易出现无声 UI 漂移。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260511-ef0-01 | T | `shell-surface-resolver.ts` | mitigate | 把 theme/shell/region 决策集中到单一 typed resolver，并用组合测试锁定 square/full-width/immersive 与 rounded/default 合同。 |
| T-quick-260511-ef0-02 | E | `teacher-sidebar-shell.tsx` | mitigate | 禁止回退到 `routeKey` 直比较或分散 JSX 条件，组件仅消费 resolver 输出，避免局部绕过 metadata contract。 |
| T-quick-260511-ef0-03 | R | test coverage | mitigate | 在 resolver 与 shell 两层分别保留回归测试，确保 UI 漂移时可追溯到决策层或接线层。 |
</threat_model>

<verification>
- 运行 `pnpm vitest run src/lib/theme-layout/shell-surface-resolver.test.ts src/components/shell/teacher-sidebar-shell.test.tsx`
- 自查 `teacher-sidebar-shell.tsx`：不再出现成组的 shell state 本地布尔推导与深层 className ternary；保留现有 data attributes 与 DOM 区域标记。
</verification>

<success_criteria>
- `TeacherSidebarShellFrame` 的 theme/shell/surface UI 状态由集中 resolver 提供。
- `/teacher` 当前 square/full-width/immersive 行为与共享 rounded shell 行为保持不变。
- 关键状态组合测试通过，且能防止 route-string branching 与 JSX 条件爆炸回归。
</success_criteria>

<output>
After completion, create `.planning/quick/260511-ef0-teacher-sidebar-shell-tsx-ui-usesactivet/260511-ef0-SUMMARY.md`
</output>
