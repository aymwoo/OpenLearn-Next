---
phase: 52-action-registry-plugin-lifecycle-governance
plan: "06"
type: execute
wave: 5
depends_on:
  - 52-05
files_modified:
  - src/lib/dal/plugins.ts
  - src/lib/dal/plugins.test.ts
  - src/features/platform-core/plugins/governance-projection.ts
  - src/features/platform-core/plugins/governance-projection.test.ts
  - src/components/surfaces/plugin-lifecycle-operator-surface.tsx
  - src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx
autonomous: true
gap_closure: true
requirements:
  - LIFE-01
  - LIFE-06
must_haves:
  truths:
    - "retain uninstall 写入的 `uninstalledAt` / `uninstallRetentionMode` 会进入 governance snapshot，并真实投影为 `uninstalled` 五态。"
    - "operator diagnostics 可以把 retain uninstall 插件识别为 `uninstalled`，而不是继续回落成 `installed`。"
    - "`uninstalled` 只作为审计/治理状态出现，不再继续渲染可执行主动作。"
  artifacts:
    - "src/lib/dal/plugins.ts 把 retain uninstall metadata 送入 governance snapshot read model。"
    - "src/features/platform-core/plugins/governance-projection.ts 让 `uninstalled` 成为可达的外部生命周期状态。"
    - "src/components/surfaces/plugin-lifecycle-operator-surface.tsx 对 `uninstalled` 行执行审计态渲染。"
  key_links:
    - "pluginRegistrations.uninstalledAt / uninstallRetentionMode -> listPluginGovernanceSnapshotRecords() -> projectPluginGovernance()"
    - "projectPluginGovernance().lifecycle.state === 'uninstalled' -> PluginLifecycleOperatorSurface diagnostics rendering"
---

<objective>
补齐 retain uninstall 的治理读模型闭环，让 `uninstalled` 从 schema/文案占位变成真实外部生命周期状态，并在 operator diagnostics 中诚实呈现。

Purpose: 当前 gap 的根因是 retain uninstall metadata 已写库，但 snapshot/projection/UI 没有消费，导致 LIFE-01 的五态 contract 仍然是空壳。
Output: retain uninstall metadata read-model wiring、`uninstalled` projection、审计态 UI 回归测试。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-action-registry-plugin-lifecycle-governance/52-CONTEXT.md
@.planning/phases/52-action-registry-plugin-lifecycle-governance/52-RESEARCH.md
@.planning/phases/52-action-registry-plugin-lifecycle-governance/52-VERIFICATION.md
@.planning/phases/52-action-registry-plugin-lifecycle-governance/52-action-registry-plugin-lifecycle-governance-04-SUMMARY.md
@.planning/phases/52-action-registry-plugin-lifecycle-governance/52-action-registry-plugin-lifecycle-governance-05-SUMMARY.md
@src/lib/dal/plugins.ts
@src/features/platform-core/plugins/governance-projection.ts
@src/components/surfaces/plugin-lifecycle-operator-surface.tsx

<interfaces>
From `src/lib/dal/plugins.ts`:

```ts
export type PluginGovernanceSnapshotRecord = PluginGovernanceProjectionInput;

export async function listPluginGovernanceSnapshotRecords(
  input: PluginManagerScopeInput,
): Promise<PluginGovernanceSnapshotRecord[]>;
```

From `src/features/platform-core/plugins/governance-projection.ts`:

```ts
function mapLifecycleState(input: PluginGovernanceProjectionInput): GovernanceLifecycleState;

export type PluginGovernanceProjectionRow = {
  lifecycle: {
    state: GovernanceLifecycleState;
    blocked: boolean;
    reasonCode: PluginGovernanceReasonCode | null;
    recommendedRecoveryAction: PluginRecoveryAction | null;
  };
};
```

From `src/components/surfaces/plugin-lifecycle-operator-surface.tsx`:

```ts
const lifecycleLabel = {
  installed: "已安装",
  enabled: "已启用",
  active: "运行中",
  suspended: "已挂起",
  uninstalled: "已卸载",
} as const;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 把 retain uninstall metadata 接入 governance snapshot 与 lifecycle projection</name>
  <files>src/lib/dal/plugins.ts, src/lib/dal/plugins.test.ts, src/features/platform-core/plugins/governance-projection.ts, src/features/platform-core/plugins/governance-projection.test.ts</files>
  <read_first>src/lib/dal/plugins.ts, src/lib/dal/plugins.test.ts, src/features/platform-core/plugins/governance-projection.ts, src/features/platform-core/plugins/governance-projection.test.ts, .planning/phases/52-action-registry-plugin-lifecycle-governance/52-VERIFICATION.md</read_first>
  <acceptance_criteria>
    - `src/lib/dal/plugins.ts` 的 `listPluginGovernanceSnapshotRecords()` 返回值包含 `uninstalledAt` 与 `uninstallRetentionMode`，不再在 snapshot read path 丢掉 retain uninstall metadata。
    - `src/features/platform-core/plugins/governance-projection.ts` 在 `uninstallRetentionMode === "retain"` 且 `uninstalledAt !== null` 时，把 lifecycle 映射为 `uninstalled`，并保持 `executable === false`。
    - retain uninstall row 的 projection 输出 `reasonCode: "not_installed"`、`recommendedRecoveryAction: null`，避免把 `uninstalled` 混回普通 enable/retry 恢复语义。
    - `src/lib/dal/plugins.test.ts` 与 `src/features/platform-core/plugins/governance-projection.test.ts` 新增 retained uninstall case，断言 snapshot + projection 都能产出 `uninstalled`。
  </acceptance_criteria>
  <action>按 D-52-05、D-52-06、D-52-13、D-52-16，把 retain uninstall metadata 从写库真相继续接到读模型。先在 `src/lib/dal/plugins.ts` 扩展 `PluginGovernanceProjectionInput` 输入，让 `listPluginGovernanceSnapshotRecords()` 明确返回 `uninstalledAt` 和 `uninstallRetentionMode`；保留现有 preflight/cascade 逻辑，不新增第二真相源。然后在 `src/features/platform-core/plugins/governance-projection.ts` 的 `mapLifecycleState()` 最前面增加 retain uninstall 分支：当 row 已写入 `uninstalledAt` 且 `uninstallRetentionMode === "retain"` 时，外部 lifecycle 固定映射为 `uninstalled`，`blocked = true`，`reasonCode = "not_installed"`，`recommendedRecoveryAction = null`，禁止它回落到 `installed` 或 `enabled`。同步补充两个测试文件：`src/lib/dal/plugins.test.ts` 断言 snapshot read path 真的带出 retain uninstall metadata；`src/features/platform-core/plugins/governance-projection.test.ts` 断言 retained row 会投影为 `uninstalled`，且不会进入 executable catalog。</action>
  <verify>
    <automated>pnpm exec vitest run src/lib/dal/plugins.test.ts src/features/platform-core/plugins/governance-projection.test.ts</automated>
  </verify>
  <done>retain uninstall 不再是只有数据库知道的隐藏状态，governance projection 能真实产出 `uninstalled` 五态。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 让 operator diagnostics 把 `uninstalled` 渲染为审计态而非可执行态</name>
  <files>src/components/surfaces/plugin-lifecycle-operator-surface.tsx, src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx</files>
  <read_first>src/components/surfaces/plugin-lifecycle-operator-surface.tsx, src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx, .planning/phases/52-action-registry-plugin-lifecycle-governance/52-UI-SPEC.md, .planning/phases/52-action-registry-plugin-lifecycle-governance/52-VERIFICATION.md</read_first>
  <acceptance_criteria>
    - diagnostics view 中 `lifecycleState === "uninstalled"` 的插件会显示 `已卸载` badge 与审计说明，不再渲染主 toggle 按钮。
    - `plugin-lifecycle-operator-surface.tsx` 对 `uninstalled` 使用 UI-SPEC 中“仅审计视图出现 / 无主动作”约束，而不是沿用默认 `启用插件` fallback。
    - `src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx` 覆盖 retained uninstall row：能看到 `已卸载`，且看不到 lifecycle 主动作按钮。
  </acceptance_criteria>
  <action>按 52-UI-SPEC 的 lifecycle presentation 约束与 D-52-05，把 `src/components/surfaces/plugin-lifecycle-operator-surface.tsx` 的 diagnostics rendering 改成显式识别 `uninstalled`。新增类似 `showPrimaryLifecycleAction()` 的 helper：`uninstalled` 返回 `false`，其余状态维持现有治理动作逻辑。为 `uninstalled` row 渲染只读说明文案，例如“该插件处于 retain-uninstall 审计态；历史数据保留，但不会作为当前可执行扩展参与治理动作。”，并保持 blocked diagnostics / uninstall preflight summary 仍可见。同步在 `src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx` 添加 retained uninstall fixture，断言 diagnostics 卡出现 `已卸载` badge、保留 preflight summary、且不显示主动作按钮，防止 UI 再把它当成可恢复可执行态。</action>
  <verify>
    <automated>pnpm exec vitest run src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx</automated>
  </verify>
  <done>学校操作员能真实区分 `uninstalled`，并看到它是 retain-uninstall 的审计态而不是普通启用/停用态。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retain uninstall DB truth -> governance projection | 如果读模型漏掉 retain metadata，UI 会对外撒谎，破坏 lifecycle contract |
| governance projection -> operator diagnostics | `uninstalled` 必须只表现为审计态，不得被前端误渲染成可执行主动作 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-06-01 | T | `listPluginGovernanceSnapshotRecords()` | mitigate | 把 `uninstalledAt` / `uninstallRetentionMode` 明确纳入 snapshot 输入，避免 projection 被旧字段覆盖。 |
| T-52-06-02 | I | `mapLifecycleState()` | mitigate | retain uninstall 分支优先映射为 `uninstalled`，禁止回落成 `installed`。 |
| T-52-06-03 | E | `PluginLifecycleOperatorSurface` | mitigate | `uninstalled` 行隐藏主治理动作，只保留审计说明与 preflight 摘要。 |
</threat_model>

<verification>
- `pnpm exec vitest run src/lib/dal/plugins.test.ts src/features/platform-core/plugins/governance-projection.test.ts src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx`
</verification>

<success_criteria>
- retain uninstall metadata 进入 governance snapshot 和 projection。
- operator diagnostics 能真实呈现 `uninstalled`。
- `uninstalled` 不再被 UI 误渲染为可执行主动作。
</success_criteria>

<output>
After completion, create `.planning/phases/52-action-registry-plugin-lifecycle-governance/52-action-registry-plugin-lifecycle-governance-06-SUMMARY.md`
</output>
