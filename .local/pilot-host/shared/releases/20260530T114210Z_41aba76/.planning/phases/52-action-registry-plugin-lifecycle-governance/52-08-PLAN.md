---
phase: 52-action-registry-plugin-lifecycle-governance
plan: "08"
type: execute
wave: 6
depends_on:
  - 52-06
  - 52-07
files_modified:
  - src/components/surfaces/plugin-lifecycle-operator-surface.tsx
  - src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx
  - scripts/verify-phase52-action-registry-and-lifecycle.ts
autonomous: true
gap_closure: true
requirements:
  - ACTN-03
  - LIFE-01
  - LIFE-03
must_haves:
  truths:
    - "operator diagnostics 的恢复按钮会按 `recommendedRecoveryAction` 触发显式 `enable` / `resume` / `retry` / `reconcile`，不再一律回落到 enable toggle。"
    - "dependency-blocked 插件在 UI 上可以执行 `reconcile`，而不是只看到不可执行文案。"
    - "`verify:phase52` 会同时守住 `uninstalled` projection 与 `plugin.reconcile` wiring，避免再次假阳性通过。"
  artifacts:
    - "src/components/surfaces/plugin-lifecycle-operator-surface.tsx 使用 reason-aware recovery dispatch。"
    - "src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx 覆盖 reconcile/retry/resume/uninstalled UI 行为。"
    - "scripts/verify-phase52-action-registry-and-lifecycle.ts 新增 uninstalled + reconcile 的静态与 focused-suite 守卫。"
  key_links:
    - "pluginLifecycleRows.recommendedRecoveryAction -> operator surface recovery button -> server action dispatch"
    - "verify:phase52 -> static checks for plugin.reconcile and uninstalled projection -> focused vitest suites"
---

<objective>
把新的治理真相真正落实到 operator UI 与 phase close gate：让 diagnostics recovery 按 recommendation 执行真实命令，并让 `verify:phase52` 对这两类 gap 建立长期回归防线。

Purpose: 只有 command surface 还不够；如果 UI 继续把所有恢复动作都发成 enable，或者 verifier 仍不检查 `uninstalled` / `reconcile`，Phase 52 会再次出现“实现了但没被守住”的假闭环。
Output: reason-aware recovery UI、focused UI tests、加强版 `verify:phase52`。
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-action-registry-plugin-lifecycle-governance/52-CONTEXT.md
@.planning/phases/52-action-registry-plugin-lifecycle-governance/52-UI-SPEC.md
@.planning/phases/52-action-registry-plugin-lifecycle-governance/52-VERIFICATION.md
@.planning/phases/52-action-registry-plugin-lifecycle-governance/52-06-PLAN.md
@.planning/phases/52-action-registry-plugin-lifecycle-governance/52-07-PLAN.md
@src/components/surfaces/plugin-lifecycle-operator-surface.tsx
@scripts/verify-phase52-action-registry-and-lifecycle.ts

<interfaces>
From `src/components/surfaces/plugin-lifecycle-operator-surface.tsx`:

```ts
const recoveryActionLabel = {
  enable: "启用插件",
  retry: "重试恢复",
  resume: "解除挂起",
  reconcile: "运行 reconcile",
  confirm_cleanup: "确认 cleanup",
} as const;
```

Expected from Plan 07 `src/actions/plugin-actions.ts`:

```ts
export async function reconcilePluginAction(data: {
  schoolId: string;
  pluginId: string;
  reason: string;
  targetState?: "enabled" | "mounted" | "ready";
});

export async function retryPluginAction(data: {
  schoolId: string;
  pluginId: string;
  commandId: string;
  reason: string;
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 把 operator diagnostics 按 `recommendedRecoveryAction` 接到真实恢复命令</name>
  <files>src/components/surfaces/plugin-lifecycle-operator-surface.tsx, src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx</files>
  <read_first>src/components/surfaces/plugin-lifecycle-operator-surface.tsx, src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx, src/actions/plugin-actions.ts, .planning/phases/52-action-registry-plugin-lifecycle-governance/52-UI-SPEC.md, .planning/phases/52-action-registry-plugin-lifecycle-governance/52-VERIFICATION.md</read_first>
  <acceptance_criteria>
    - diagnostics 主按钮不再统一调用 `setPluginEnabledAction`；它会按 `recommendedRecoveryAction` 分发到明确 action：`enable -> setPluginEnabledAction`，`resume -> transitionPluginLifecycleAction(targetState="enabled")` 或 kill-switch 解除分支，`retry -> retryPluginAction`，`reconcile -> reconcilePluginAction`。
    - `dependency_missing` / `dependency_cycle` 行在 UI 中点击主按钮会触发 `reconcilePluginAction`，不再显示不可执行死按钮。
    - `uninstalled` 行继续无主动作，保持 Plan 06 的审计态约束。
    - `src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx` 覆盖 reconcile、retry、resume 与 uninstalled 四类行为，确保按钮 dispatch 的是正确 server action。
  </acceptance_criteria>
  <action>在 `src/components/surfaces/plugin-lifecycle-operator-surface.tsx` 中新增显式 recovery dispatcher，按 `plugin.recommendedRecoveryAction` 选择具体 server action，而不是继续让 `submitToggle()` 吞掉全部治理动作。具体规则固定为：`enable` 调 `setPluginEnabledAction({ enabled: true })`；`resume` 调显式 resume 路径（优先 `transitionPluginLifecycleAction({ targetState: "enabled", reason: "resume" })`，不要再用 enable 冒充 resume）；`retry` 调 Plan 07 暴露的 `retryPluginAction()`；`reconcile` 调 Plan 07 暴露的 `reconcilePluginAction()`；`confirm_cleanup` 仍只留在卸载 dialog 内，不作为 diagnostics 主按钮。保持 `uninstalled` 无主动作。同步更新 `src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx`：新增 dependency-blocked fixture，断言主按钮触发 `reconcilePluginAction`；新增 activation-failed fixture，断言触发 `retryPluginAction`；新增 kill-switch fixture，断言触发 resume 路径； retained uninstall fixture 继续断言无主动作。</action>
  <verify>
    <automated>pnpm exec vitest run src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx</automated>
  </verify>
  <done>operator diagnostics 上展示的恢复动作都真正可执行，并与 reason code 保持一致。</done>
</task>

<task type="auto">
  <name>Task 2: 强化 `verify:phase52`，阻止 `uninstalled` 与 `reconcile` 再次漂移</name>
  <files>scripts/verify-phase52-action-registry-and-lifecycle.ts</files>
  <read_first>scripts/verify-phase52-action-registry-and-lifecycle.ts, src/features/platform-core/commands/contracts.ts, src/actions/plugin-actions.ts, src/features/runtime-platform/host-actions/plugin-host.ts, src/features/platform-core/plugins/governance-projection.ts, src/lib/dal/plugins.ts, src/components/surfaces/plugin-lifecycle-operator-surface.tsx</read_first>
  <acceptance_criteria>
    - verifier 静态检查 `contracts.ts`、`registry.ts`、`plugin-governance.ts`、`plugin-actions.ts`、`plugin-host.ts` 都包含 `plugin.reconcile`。
    - verifier 静态检查 `dal/plugins.ts` + `governance-projection.ts` 确认存在 `uninstalledAt` / `uninstallRetentionMode` wiring 与 `state: "uninstalled"` 映射。
    - verifier 静态检查 operator surface 不再把所有恢复动作都发到 `setPluginEnabledAction`。
    - `pnpm run verify:phase52` 的 focused suites 保留 `handlers/plugins.test.ts`、`plugin-host.phase52.test.ts`、`plugin-lifecycle-operator-surface.test.tsx`，确保 reconcile/uninstalled regression 会真正 fail。
  </acceptance_criteria>
  <action>更新 `scripts/verify-phase52-action-registry-and-lifecycle.ts`，把 Phase 52 close gate 提升为两类新增断言。第一类是 `plugin.reconcile` wiring：静态检查 `contracts.ts`、`registry.ts`、`producers/plugin-governance.ts`、`actions/plugin-actions.ts`、`host-actions/plugin-host.ts` 都含 `plugin.reconcile` 或对应 dispatch 分支；focused suites 继续跑 `src/features/platform-core/commands/handlers/plugins.test.ts`、`src/actions/plugin-actions.test.ts`、`src/features/runtime-platform/host-actions/plugin-host.phase52.test.ts`。第二类是 `uninstalled` truth：静态检查 `src/lib/dal/plugins.ts` 中 `uninstalledAt` / `uninstallRetentionMode` 进入 snapshot，`src/features/platform-core/plugins/governance-projection.ts` 中存在 `state: "uninstalled"` 的映射分支，并继续跑 `src/features/platform-core/plugins/governance-projection.test.ts` 与 `src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx`。同时增加一条 UI drift guard：如果 operator surface 仍把 dependency/retry/resume recovery 全部导向 `setPluginEnabledAction`，`verify:phase52` 必须失败。</action>
  <verify>
    <automated>pnpm run verify:phase52</automated>
  </verify>
  <done>Phase 52 close gate 能自动拦截 `uninstalled` / `reconcile` 的任何回退。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| governance read model -> operator recovery button | UI 必须忠实执行推荐恢复动作，不能把所有治理语义压扁成 enable toggle |
| phase verifier -> future refactors | 如果 verifier 不检查 `uninstalled` / `reconcile`，后续修改会重新引入假阳性 close gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-08-01 | T | `PluginLifecycleOperatorSurface` | mitigate | 用 reason-aware dispatcher 把每类恢复动作映射到明确 server action，拒绝 generic enable fallback。 |
| T-52-08-02 | I | `verify:phase52` | mitigate | 静态 guard + focused suites 同时检查 `uninstalled` truth 和 `plugin.reconcile` wiring。 |
| T-52-08-03 | E | dependency recovery UI | mitigate | `reconcile` 仅在 recommendedRecoveryAction 指示时出现并 dispatch，防止 UI 自行发明恢复路径。 |
</threat_model>

<verification>
- `pnpm exec vitest run src/components/surfaces/plugin-lifecycle-operator-surface.test.tsx`
- `pnpm run verify:phase52`
</verification>

<success_criteria>
- operator diagnostics 的恢复按钮与治理 recommendation 一致。
- dependency-blocked 插件可从 UI 走显式 reconcile。
- `verify:phase52` 对 `uninstalled` 与 `reconcile` 建立长期回归防线。
</success_criteria>

<output>
After completion, create `.planning/phases/52-action-registry-plugin-lifecycle-governance/52-action-registry-plugin-lifecycle-governance-08-SUMMARY.md`
</output>
