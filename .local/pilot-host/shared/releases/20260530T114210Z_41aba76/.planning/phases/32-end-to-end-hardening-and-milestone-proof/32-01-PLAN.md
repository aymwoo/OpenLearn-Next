---
phase: 32-end-to-end-hardening-and-milestone-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/bootstrap-dev-db.ts
  - src/actions/classroom-actions.ts
  - src/features/runtime-platform/classroom/runtime-session-contracts.ts
  - src/features/runtime-platform/classroom/runtime-session.ts
  - src/lib/dal/classroom.ts
  - src/lib/dal/lesson-authoring.test.ts
  - src/features/runtime-platform/classroom/runtime-session.test.ts
  - src/lib/dal/classroom.test.ts
autonomous: true
requirements: [RHOST-04]
must_haves:
  truths:
    - "per D-01/D-02/D-03，canonical proof 使用可重复 seeded lesson/class/runtime step，并且 editor/publish leg 仍会把该 runtime proof step 冻结进 published snapshot，而不是任意真实 lesson。"
    - "per D-04/D-07/D-11/D-12，真实 submit truth 会显式携带 runtimeSessionId 与结构化摘要，让 classroom first-feedback 和 inspector deep-link 都能消费。"
  artifacts:
    - path: "scripts/bootstrap-dev-db.ts"
      provides: "deterministic runtime proof seed lesson, class, and published snapshot"
    - path: "src/features/runtime-platform/classroom/runtime-session.ts"
      provides: "durable runtime submit result carrying runtimeSessionId and structured proof summary"
    - path: "src/lib/dal/classroom.ts"
      provides: "classroom read model fields needed for teacher-first success/failure feedback"
  key_links:
    - from: "scripts/bootstrap-dev-db.ts"
      to: "src/lib/dto/resource-ai.ts"
      via: "seeded HTML runtime step uses existing htmlCourseware descriptor contract"
      pattern: "html-courseware"
    - from: "src/features/runtime-platform/classroom/runtime-session.ts"
      to: "src/lib/dal/runtime-inspector.ts"
      via: "runtimeSessionId anchors later inspector drill-down"
      pattern: "runtimeSessionId"
---

<objective>
定义 Phase 32 的 deterministic proof foundation：固定 seeded demo path，并把
`runtimeSessionId + structured submit summary` 线程化到真实 truth path 与
classroom read model。

Purpose: 没有稳定 seed 与可消费的 submit truth，后续 terminal posture、教师反馈、
inspector deep-link 和 `verify:phase32` 都会漂移成一次性手工演示。
Output: deterministic demo seed, runtime submit contract additions, classroom-first proof data.
</objective>

<execution_context>
@/home/wuxf/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-CONTEXT.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-RESEARCH.md
@.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-PATTERNS.md
@scripts/bootstrap-dev-db.ts
@src/actions/classroom-actions.ts
@src/lib/dto/resource-ai.ts
@src/lib/dal/lesson-authoring.test.ts
@src/features/runtime-platform/classroom/runtime-session-contracts.ts
@src/features/runtime-platform/classroom/runtime-session.ts
@src/lib/dal/classroom.ts

<interfaces>
From `scripts/bootstrap-dev-db.ts`:
```ts
const DEV_STEP_DEFINITIONS = [ ... ];
async function publishLessonVersion(input: { lessonId: string; teacherId: string; ...; steps: Array<typeof lessonSteps.$inferSelect>; })
```

From `src/features/runtime-platform/classroom/runtime-session-contracts.ts`:
```ts
export const RuntimeBootstrapDTOSchema = z.object({
  sessionId: z.string().min(1),
  latestStateSummary: RuntimeStateSummarySchema.nullable(),
});
```

From `src/actions/classroom-actions.ts` submit path:
```ts
export async function submitRuntimeStateAction(...)
updateTag(cacheTags.classroom(parsed.data.payload.classroomSessionId));
updateTag(cacheTags.progress(result.lessonId, result.actorId));
updateTag(cacheTags.submission(result.lessonId, result.actorId));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 固定 canonical demo seed lesson 与 published runtime snapshot</name>
  <read_first>scripts/bootstrap-dev-db.ts, src/lib/dto/resource-ai.ts, src/lib/dal/plugins.builtins.test.ts, src/lib/dal/lesson-authoring.test.ts</read_first>
  <files>scripts/bootstrap-dev-db.ts, src/features/runtime-platform/classroom/runtime-session.test.ts, src/lib/dal/lesson-authoring.test.ts</files>
  <behavior>
    - Test 1: dev bootstrap 产出的 lesson steps 中至少有一个 built-in HTML runtime proof step，并且 published snapshot 保留该 `payload.runtime` descriptor。
    - Test 2: teacher@example.com / student@example.com 与既有班级、课程、课时仍保持 deterministic，可重复重建 proof 环境。
    - Test 3: canonical proof step 继续走现有 editor/publish snapshot freeze 链路，不会在 publish 时丢失 runtime descriptor。
  </behavior>
  <action>扩展 `scripts/bootstrap-dev-db.ts` 中的 `DEV_STEP_DEFINITIONS`，按 D-01、D-02、D-03 使用现有 `htmlCourseware` contract 生成唯一 canonical proof step，而不是手写第二套 runtime payload。seed 后的 lesson 仍通过现有 `publishLessonVersion()` 冻结 snapshotJson。不要新建第二个 demo bootstrap 脚本，不要改 test actors，不要把 proof lesson 变成任意 runtime 列表。同步补 focused tests：一条验证 bootstrap 生成的 published snapshot 中含 `kind: "html-courseware"`、`bootstrap: "/runtime/html-courseware/pilot"`、`submitTarget.primary: "classroom-evidence"` 的 proof step；另一条复用现有 lesson authoring publish-snapshot 模式，锁住 canonical proof 的 editor/publish leg 仍会把 runtime descriptor 冻结进 published snapshot。</action>
  <acceptance_criteria>
    - `scripts/bootstrap-dev-db.ts` 包含唯一 proof step 的 `payload.runtime.kind === "html-courseware"` 事实
    - `scripts/bootstrap-dev-db.ts` 继续打印 teacher/student 账号，不引入第二套 demo actor
    - focused tests 对 published snapshot 中的 runtime descriptor 与 editor/publish freeze 行为做结构断言并通过
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/features/runtime-platform/classroom/runtime-session.test.ts src/lib/dal/lesson-authoring.test.ts</automated>
  </verify>
  <done>开发环境 bootstrap 后可以稳定拿到一条固定 proof lesson path，后续计划无需手工反推 demo 数据。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把 runtimeSessionId 与 submit summary 线程化到课堂真相读模型</name>
  <read_first>src/features/runtime-platform/classroom/runtime-session-contracts.ts, src/features/runtime-platform/classroom/runtime-session.ts, src/lib/dal/classroom.ts, src/lib/dal/runtime-inspector.ts</read_first>
  <files>src/actions/classroom-actions.ts, src/features/runtime-platform/classroom/runtime-session-contracts.ts, src/features/runtime-platform/classroom/runtime-session.ts, src/lib/dal/classroom.ts, src/lib/dal/classroom.test.ts</files>
  <behavior>
    - Test 1: runtime submit 的 durable result 明确返回 `runtimeSessionId`、`classroomSessionId`、`lessonId`、`actorId` 和一段 UI-ready summary。
    - Test 2: classroom read model 能给当前步骤/学生监控提供 first-feedback 所需的 proof fields，而不是让 UI 自己拼 transport 或 runtime state。
  </behavior>
  <action>按 D-04、D-07、D-11、D-12 扩展 `submitRuntimeSessionState()` 及其相关 contract，返回可直接供 UI 使用的 proof summary，至少包含 `runtimeSessionId`、`classroomSessionId`、`lessonId`、`actorId`、`submittedAt` 与一个由服务端派生的小型 summary 对象。若 `submitRuntimeStateAction()` 目前承担 host-facing envelope 转发层，则同步更新 `src/actions/classroom-actions.ts`，确保这些字段能沿真实 server action 边界传给后续 host or UI 消费者。随后在 `src/lib/dal/classroom.ts` 中，把当前课堂的 monitoring/current-step read model 扩到能表达“某学生已完成当前 runtime submit”或“最近一次 runtime submit 失败/缺失”的 server-owned DTO 字段。不要让 classroom 组件直接去查 inspector DAL，也不要让 UI 重新解析 runtime state JSON 才知道 proof 是否完成。</action>
  <acceptance_criteria>
    - `runtime-session.ts` 中的 submit result contract 明确包含 `runtimeSessionId`
    - 若 `classroom-actions.ts` 存在转发层，新的 proof fields 能沿 `submitRuntimeStateAction()` 被 host-facing 调用方消费
    - `classroom.ts` 暴露的 DTO 中有可 grep 的 proof summary / last runtime submit 字段
    - `src/lib/dal/classroom.test.ts` 对 classroom first-feedback 字段做语义断言并通过
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --run src/features/runtime-platform/classroom/runtime-session.test.ts src/lib/dal/classroom.test.ts</automated>
  </verify>
  <done>后续 UI 可以直接在 `/classroom` 和 inspector deep-link 中消费统一的 runtime submit truth，而不是拼凑多源状态。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| bootstrap script -> published lesson snapshot | 如果 seed 漂移，proof 将失去可重复性并破坏 downstream handoff |
| runtime submit truth -> classroom read model | 如果 UI 自己拼 proof 状态，会形成第二真相源 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-01-01 | T | bootstrap-dev-db proof seed | mitigate | proof lesson 固定复用现有 htmlCourseware descriptor，并继续走 editor/publish snapshot freeze，不创建第二套 payload 语义 |
| T-32-01-02 | R | runtime submit result | mitigate | submit result 必带 `runtimeSessionId`、`classroomSessionId`、`actorId`、`submittedAt` 以便后续审计与 drill-down |
| T-32-01-03 | I | classroom first-feedback DTO | mitigate | 只输出 UI-ready summary，不泄露 raw runtime state JSON 或跨角色 inspector 数据 |

</threat_model>

<verification>
- `pnpm test --run src/features/runtime-platform/classroom/runtime-session.test.ts src/lib/dal/lesson-authoring.test.ts src/lib/dal/classroom.test.ts`
</verification>

<success_criteria>
- canonical demo lesson path 可重复 bootstrap。
- canonical proof 的 editor/publish leg 仍可把 runtime descriptor 冻结进 published snapshot。
- runtime submit truth 已稳定暴露 `runtimeSessionId` 和 structured summary。
- classroom read model 已具备教师 first-feedback 所需 proof data。
</success_criteria>

<output>
After completion, create `.planning/phases/32-end-to-end-hardening-and-milestone-proof/32-01-SUMMARY.md`
</output>
